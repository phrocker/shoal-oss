# Sharkbite → Shoal compatibility matrix

Exhaustive enumeration of the public Sharkbite compatibility contract and its
mapping onto Shoal. This document is the coverage gate for
[phrocker/sharkbite#108](https://github.com/phrocker/sharkbite/issues/108) and
Shoal issue [#81](https://github.com/phrocker/shoal-oss/issues/81) (umbrella
[#59](https://github.com/phrocker/shoal-oss/issues/59)).

<a id="sec-1"></a>

## 1. Status of this document

| Field | Value |
| --- | --- |
| Document status | Normative gate. Binding on all Sharkbite-compatibility work. Revision 36 — completes the twelve-row streaming cursor tranche with ABI 1.13.0 capability 24, owned scanner/batch/high-level cursors, bounded owned chunks, deadline and one-shot cancellation, parent-close joining, stable errors, allocation-failure, C11/C++11, and race evidence. Revision 35 — records the public column-visibility surface, moving [SB-ERR-010](#sec-18) and 30 [§19.2](#sec-19-2) members to `Missing C ABI`, and records six more pinned defects in [§21](#sec-21). Revision 34 — completes the four-row compatibility-error tranche with ABI 1.12.0 capability 23, stable Sharkbite source categories, distinct ClientException/RuntimeError binding classes, immutable library-owned names, allocation-free concurrent getters, C11/C++11, and race evidence. Revision 33 — records the public streaming scan cursor, moving the four [§9](#sec-9) rows, the [§20](#sec-20) result-iteration row and the [§19.2](#sec-19-2) `getResultSet` member to `Missing C ABI`, and records two more pinned defects in [§21](#sec-21). Revision 32 — completes the five-row high-level scanner facade with ABI 1.11.0 capability 22, copied binary columns, atomic settings snapshots, owned single/multi-range results, deadlines, one-shot cancellation, client-close joining, stable errors, allocation-failure, C11/C++11, and race evidence. Revision 31 — completes the eight-row high-level client facade with ABI 1.10.0 capability 21, copied configuration/table/binary authorizations, mutable defaults, owned scanner and lazy writer results, connector-close coordination, stable errors, allocation-failure, C11/C++11, and race evidence. Revision 30 — completes one-shot scan cancellation and connector cache invalidation with ABI 1.9.0 capability 20, owned cancellation handles, copied table IDs, cancel/free joining, concurrent cancellation, stable errors, allocation-failure, C11/C++11, and race evidence. Revision 29 — completes row-bounded table flush and constraint administration with ABI 1.8.0 capability 19, copied binary bounds, owned ordered constraint snapshots, forward-sized views, deadlines, connector-close cancellation, allocation-failure, C11/C++11, and race evidence. Revision 28 — completes the four-row high-level buffered-writer C ABI with ABI 1.7.0 capability 18, lazy writer creation, copied binary inputs, row-change and close flushing, timestamp parity, connector-close cancellation, concurrent close, structured failures, allocation-failure, C11/C++11, and race evidence. Revision 27 — records constraint administration and row-bounded flush, moving three [§11](#sec-11) and [§19.2](#sec-19-2) rows to `Missing C ABI` and reclassifying the flush row from `Behavior mismatch` because the bounded capability is not on the ABI, pins the exact interface gap that keeps online compaction out of Go, and records two more pinned defects in [§21](#sec-21). Revision 26 — completes the 17-row data-model value C ABI with ABI 1.6.0 capability 17, copied binary-safe key/range/authorization inputs, immutable authorization handles, owned key/value and text/list results, allocation-failure, concurrent-getter, C11/C++11, and race evidence. Revision 25 — completes the 31-row RFile and stream C ABI with ABI 1.5.0 capability 16, copied binary-safe inputs, owned handles/results, deadlines, concurrent close, allocation-failure, C11/C++11, and race evidence. Revision 24 — completes the configuration and instance-topology C ABI: twelve §6 rows move from `Missing C ABI` to `Covered` with binary-safe copied inputs, owned results, cancellation, allocation-failure, C11/C++11, and race evidence. Revision 23 — records the public data-model value types: the `Authorizations` set, the `Range` predicates and text form, and the `Key` text form move six [§8](#sec-8) rows from `Missing Go` to `Missing C ABI`, two `Key` text rows are reclassified from `Behavior mismatch` to `Missing C ABI` because they never had a Go mapping, and eight [§19.2](#sec-19-2) member rows follow them, and two newly proved defects are recorded in [§21](#sec-21), one of which withdraws [SB-KEEP-016](#sec-22). Revision 22 — reclassifies the thirty-one RFile and stream rows of [§15](#sec-15) that a new public `rfile` Go package now implements from `Missing Go` to `Missing C ABI` under rule 2 of [§4.2](#sec-4), leaves [SB-RFILE-012](#sec-15) `Missing Go` because Shoal's writer still emits only the default locality group, and records four newly proved Sharkbite defects in [§21](#sec-21); no row became `Covered`. Revision 21 — adds owned range and iterator-setting descriptor ABIs, proves timestamped mutation deletion at the linked C boundary, and records exact C11/C++11/race evidence for thirteen data-model rows. Revision 20 reclassifies the twelve client-configuration and instance-topology rows of [§6](#sec-6) from `Missing Go` and `Behavior mismatch` to `Missing C ABI` under rule 2 of [§4.2](#sec-4), because Shoal now implements and tests that surface in Go; no row became `Covered` and the revision-19 classifications are otherwise untouched. Revision 19 added the merged connector-identity C ABI and exact C11/C++11/race evidence for the eight Sharkbite instance-name, instance-ID, and principal aliases. Revision 18 applied the seventeenth independent audit: merged table, namespace, security, split, and capability ABI slices are re-audited end to end, stale connector/C++/error/gap rows are reclassified to the exact merged surfaces, and only capability discovery now reaches `Covered`. Revision 17 applied the sixteenth audit: merged table, namespace, security, and split administration are repinned to their exact Go and stable C ABI evidence, while Python semantic differences remain `Behavior mismatch`. Revision 16 applied the fifteenth audit: every registration row cites its full multi-line span and preserves the exact keyword-argument defaults. Revision 15 applied the fourteenth audit: the dead `sharkbite_iterator` embedded module is enumerated registration by registration in [§19.4](#sec-19-4) instead of being dropped as declaration-free, and [SB-SCAN-016](#sec-9) no longer claims a functioning server-side Python iterator capability. Revision 14 applied the thirteenth audit: the 363-header partition is stated as arithmetic that sums (204 live + 152 vendored + 7 declaration-free) and every namespace-variable figure in prose is re-derived from the headers, with checks 24 and 34 rewritten to scan every occurrence. Revision 13 applied the twelfth audit: forward declarations and using-declarations are type and name contracts rather than variables, and the namespace-variable population is derived independently (8 variables, 5 of them `static`). Revision 12 applied the eleventh audit: public bit-fields enumerated, function-pointer typedefs classified as aliases, macro invocations excluded as non-declarations, and namespace-scope `static` variables described as internal-linkage per-translation-unit state. Revision 11 applied the tenth audit: friend operators enumerated as namespace-scope obligations, out-of-class definitions merged into their in-class declarations, and protected/private members excluded with recorded access. Revision 10 applied the ninth audit: templated members no longer mask memberless types, conversion operators are parsed, template-dependent enum values are never presented as evaluated ordinals, and every narrative count is reconciled mechanically. Revision 9 applied the eighth audit: anonymous-namespace scope leaks repaired, vendoring decided by path and own license, namespace-scope and `extern "C"` declarations enumerated, typedef enums parsed, include-guard artifacts removed, and a twenty-check verifier that compares the inventory against the headers ([§19.1](#sec-19-1), [§19.2](#sec-19-2), [§19.3](#sec-19-3)). |
| Tracking issue | Shoal [#81](https://github.com/phrocker/shoal-oss/issues/81) — "docs: define and audit complete Sharkbite compatibility matrix" (parent [#59](https://github.com/phrocker/shoal-oss/issues/59); upstream target [phrocker/sharkbite#108](https://github.com/phrocker/sharkbite/issues/108)) |
| Sharkbite reference | `phrocker/sharkbite` @ `7f2625f74331b0cd4a75dc0484949c40f1409686` ("Bump accumulo-core from 2.0.0 to 2.0.1 in /native-iterators-jni (#100)", 2022-07-22) |
| Sharkbite release line | `sharkbite` 1.2.0.3 on PyPI (`setup.py:34-35`) |
| Shoal reference | `phrocker/shoal-oss` exact audited baseline for revision 36 `1ff5663f97879f45589c58d172e6526cb71d3e4b` ("Merge PR #147: publish the column-visibility expression surface") plus the streaming cursor C ABI introduced in this revision |
| Shoal C ABI version | `SHOAL_ABI_VERSION 1u` (`capi/include/shoal_types.h`) |
| Rows | 3203 (2811 required by the [§2.2](#sec-2) release gate) |
| Covered rows | **100** — 42 of 333 in revision 1, 14 of 368 in revision 2, 3 of 385 in revision 3, 1 of 473 in revision 4, 0 of 542 in revision 5, 0 of 2358 in revision 6, 0 of 2938 in revision 7, 0 of 3029 in revision 8, 0 of 3175 in revision 9, 0 of 3177 in revision 10, 0 of 3171 in revision 11, 0 of 3174 in revision 12, 0 of 3168 in revision 13, 0 of 3203 in revision 17, 1 of 3203 in revision 18, 1 of 3203 in revision 19, 1 of 3203 in revision 20, and 1 of 3203 in revision 21, 13 of 3203 in revision 24, 44 of 3203 in revision 25, 61 of 3203 in revisions 26 and 27, 65 of 3203 in revision 28, 69 of 3203 in revision 29, 71 of 3203 in revision 30, 79 of 3203 in revision 31, 84 of 3203 in revisions 32 and 33, 88 of 3203 in revisions 34 and 35, and 100 of 3203 in revision 36; see [§25.4](#sec-25) |

This document does **not** authorize any Python implementation work. See
[§2 Release gate](#sec-2).

<a id="sec-2"></a>

## 2. Release gate (normative)

This gate is the deliverable of Shoal issue
[#81](https://github.com/phrocker/shoal-oss/issues/81); that issue is where
gate status, the independent omission/classification audit, and any approval
decisions are recorded. Closing #81 requires this document plus that audit —
not the document alone.

There are **two** gates, and conflating them is what made revision 1's single
gate circular: no row can reach `Covered` until code exists, so a gate that
demands universal `Covered` before any work starts would forbid the work that
satisfies it.

### 2.1 Implementation-entry gate

Work on a Sharkbite-compatible Python layer may begin when, for the rows it
touches:

1. The row is enumerated here with its Sharkbite signature, defaults, and
   layer mapping — that is, the contract is known before it is implemented.
2. Its prerequisite stage is met ([§23](#sec-23)): Go parity before C ABI
   parity, C ABI parity before compatibility tests, tests before wheels.
3. Any divergence it depends on is either approved in [§26](#sec-26) or
   explicitly recorded as an open question with a named owner.

This gate is per-row and forward-looking. It permits prototyping, spikes, and
merged implementation work behind a feature flag. It does **not** permit
publishing, naming, or describing anything as Sharkbite-compatible.

### 2.2 Final release gate

> **Gate.** No release, no publication under the name `sharkbite`, and no
> "Sharkbite compatible" claim until **every required row** in [§5](#sec-5)
> through [§20](#sec-20) is either `Covered` with at least one **named**
> automated test on each layer it claims (Go and, where applicable, C ABI), or
> is recorded as `Intentional divergence` **and** has recorded explicit human
> approval in [§26](#sec-26).

**Required rows** are every matrix row whose status is not
`Not required (rationale required)`. As of revision 36 that is 2811 of 3203 rows,
and **only 100 are satisfied** ([SB-XCUT-012](#sec-20), the twelve
configuration/topology rows in [§6](#sec-6), the 31 RFile/stream rows in
[§15](#sec-15), the 17 data-model value rows in [§8](#sec-8) and
[§19.2](#sec-19-2), the four buffered-writer rows in [§10](#sec-10), the four row-bounded flush/constraint rows in [§11](#sec-11) and [§19.2](#sec-19-2), the connector invalidation/cancellation rows in [§7](#sec-7) and [§20](#sec-20), the eight high-level client rows in [§10.1](#sec-10-1), the five high-level scanner rows in [§10.1](#sec-10-1), the four compatibility-error rows in [§18](#sec-18), and the twelve streaming cursor rows in [§9](#sec-9), [§10.1](#sec-10-1), [§19.2](#sec-19-2), and [§20](#sec-20)): the gate is nowhere near open, and no part
of this document should be read as authorising a release. `Not required` rows are excluded from the
gate by construction — that status exists precisely to record, with a stated
rationale, a surface that carries no obligation. They are still audited: a
wrong `Not required` is a gate failure, which is why each one must name its
rationale in Notes.

Corollaries, all binding:

1. **No partial public API.** A Python module that exposes a subset of the
   Sharkbite surface must not be published under the name `sharkbite`, must not
   be described as "Sharkbite compatible", and must not be advertised as a
   drop-in replacement. Sharkbite users import one module and expect the whole
   1.2.x surface; a subset silently converts a compile-time port into a
   runtime `AttributeError` for every consumer.
2. **No name-based coverage claims.** A row may only be marked `Covered` when
   the Shoal symbol has been read and its semantics compared against the
   Sharkbite behavior. Matching identifiers (`Scanner`, `Mutation`, `flush`,
   `close`) are not evidence.
3. **Ordered prerequisites.** Work proceeds strictly in the order
   Go parity → C ABI parity → compatibility tests → Python/wheels
   ([§23](#sec-23)). A row cannot advance past a layer whose predecessor is
   unmet.
4. **Named tests only.** "Covered by the existing suite" is not evidence. Each
   `Covered` row names test functions or C assertion blocks.
5. **Divergence requires approval.** `Intentional divergence` rows are
   proposals until a named approver signs [§26](#sec-26). Unapproved
   divergences block the gate exactly like `Missing Go`.
6. **Live-cluster conformance is part of the gate.** Unit tests alone cannot
   close rows whose semantics are defined by a real tablet server
   ([§24](#sec-24)).

<a id="sec-3"></a>

## 3. Pinned sources and validation method

### 3.1 Sharkbite inputs actually inspected

| Input | Path | What was extracted |
| --- | --- | --- |
| Python binding | `src/python/pysharkbite.cpp` (621 lines) | Every `pybind11::class_`, `enum_`, `def`, `def_static`, `def_readonly`, `init`, default argument, dunder, and exception registration. Read in full. |
| Python package | `sharkbite/__init__.py` (197 lines) | `AccumuloBase`, `AccumuloWriter`, `AccumuloScanner`, `AccumuloIterator`, `from .pysharkbite import *` re-export. Every constructor, default, and method enumerated in [§10.1](#sec-10). |
| PyTorch package | `sharkbite/torch.py` (69 lines) | `AccumuloCluster`, `AccumuloDataset`, `AccumuloValueDataset`, `coerce`, `__iter__`, `__next__`. Enumerated in [§9.2](#sec-9). |
| pandas package | `pandashark/__init__.py` (111 lines), `pandashark/pandassharkbite.py` (73 lines), `pandashark/torch.py` (69 lines, byte-identical to `sharkbite/torch.py`), `pandashark.py` (50-line second setuptools script) | `read_accumulo`, `read_accumulo_nex`, `DataFrameIterator`, `get_columns`, iteration, and a duplicate PyTorch surface. Enumerated in [§9.3](#sec-9-3). |
| Torch package | `sharkbite/torch.py`, `pandashark/` | `AccumuloDataset`, `AccumuloValueDataset`, `AccumuloCluster`, `read_accumulo`. |
| Packaging | `setup.py`, `PYTHONREADME.md`, `CMakeLists.txt`, `bootstrapper.sh`, `.github/workflows/ccpp.yml` | Distribution name/version, `python_requires`, ext-module wiring, module target `pysharkbite`, CI matrix. |
| Public headers | All 363 headers under `include/**` (`*.h`, `*.hpp`), scanned semantically, not sampled | Signatures and defaults behind each binding, exception hierarchy, ownership annotations, and one record per public declaration — members, namespace-scope functions and operators, aliases, constants, macros, and enum ordinals. The 363 headers partition exactly: **205** hold at least one row, **152** are vendored (`include/extern/libhdfs3/**` (98), generated Thrift (50), the redistributed `include/data/extern/concurrentqueue/**` (3), and Folly-derived `include/utils/Uri.h`), and **6** are declaration-free — they contribute only include guards and includes, declare nothing, and register nothing: `include/data/constructs/tables/tables.h`, `include/interconnect/Connector.h`, `include/interconnect/transport/TransportIdentifiers.h`, `include/jni/IteratorExecutor.h`, `include/jni/PythonIterator.h`, and `include/python/TableOps.h`. `include/jni/PythonBindings.h` is **not** among them: its pybind11 registrations are a public surface even though they are not C++ declarations, and they are enumerated in [§19.4](#sec-19-4). 205 + 152 + 6 = 363. These header counts are recomputed mechanically and must agree with [§19.1](#sec-19-1). Method and exclusions: [§19.1](#sec-19-1). |
| Python tests | `test/python/*.py` (10 files), `test/python/testmodule/__init__.py`, `test/MainExecutor.py`, `test/performance/QuarterMillionWrites.py` | Behavioral contracts asserted against a live cluster. |
| C++/integration tests | `test/vandv/*.h`, `test/vandv/IntegrationTest.cpp`, `test/constructs/*.cpp`, `test/zookeeper/*.cpp` | Error-code assertions, tablet-move behavior, security matrix. |
| Examples | `examples/*.py`, `examples/CppExample.cpp`, `c-library-examples/python/**` | User-facing call sequences, including flat-C-API consumers. |

### 3.2 Shoal inputs actually inspected

`accumulo/*.go` (17 non-test files, all exported declarations extracted;
`table_bulk_import.go` is new since the previous pin),
`accumulo/*_test.go` (14 files, 94 `func Test…(t *testing.T)` functions,
names extracted mechanically),
`capi/include/shoal.h`, `capi/include/shoal_types.h`, `capi/README.md`,
`capi/tests/{lifecycle.c,result_bridge.c,header_cpp_test.cpp,test_seam.h}`,
`cmd/shoal-capi/*.go`, `Makefile`, `.github/workflows/ci.yml`,
`.github/workflows/hdfs-integration.yml`, `README.md`, `ARCHITECTURE.md`,
and the `internal/` packages named in matrix rows.

### 3.3 Evidence-validation method

Every Shoal symbol, path, and test name cited below was validated against the
`origin/main` worktree, not against memory or a prior summary:

1. **Path existence** — each cited file was confirmed present with
   `Test-Path` or a direct read.
2. **Symbol existence** — exported Go declarations were extracted mechanically
   (`^(func|type|const|var)` over `accumulo/*.go`) and cross-checked against
   file/line before citation. C ABI declarations were extracted from
   `capi/include/shoal.h` and `capi/include/shoal_types.h` by full read.
3. **Test names** — extracted mechanically (`^func Test`) from
   `accumulo/*_test.go` and `cmd/shoal-capi/*_test.go`; C assertion blocks were
   read from `capi/tests/*.c` and cited by stable function/file anchors rather
   than mutable local line numbers wherever the matrix format permits.
4. **Absence claims** — every "absent" claim was produced by a negative search
   (for example, no `Namespace`, `Permission`, `GrantAuthorizations`,
   `AddSplits`, `ListSplits`, `Compact`, `ImportTable`, `ExportTable`,
   `CloneTable`, `Online`/`Offline`, `Merge`, or `DeleteRows` symbol exists in
   `accumulo/`), not by omission from a summary.
5. **C++ inventory** — the complete public-declaration inventory in
   [§19](#sec-19) is produced by a semantic scanner over every header, and the
   resulting rows are checked back against that inventory by the ten-check
   verifier in [§19.3](#sec-19-3): row count identity, orphan citation,
   unenumerated declaration, parser artifact, scope leak, empty enum, hidden
   duplicate, malformed row, callable misclassification, and silent drop.
6. **Sharkbite claims** — validated against the pinned clone. Where an
   intermediate summary disagreed with the source (for example a claimed
   three-argument `Range(start, end, bool)` call in
   `examples/pythonexample.py`), the source won: that file uses the four-argument
   form at `examples/pythonexample.py:92`.

### 3.4 Disposition of review comments on PR #92 (`fa78d1c`)

**Delivery status.** PR [#92](https://github.com/phrocker/shoal-oss/pull/92) has
been **merged**, as `24d338cd705f1dd40d272406ac42d24c41d3f0d7` ("docs: define
Sharkbite compatibility coverage gate (#92)"), verified locally with
`git log --all --grep='#92'`. It carries pre-audit text: 350 matrix rows of
which **41 are `Covered`**, the name-matched duplicate mappings, and the
truncated C++ enumeration — none of which survive the audits recorded in
[§25.4](#sec-25). Everything from revision 3 onward, including this revision, is
therefore a correction to already-merged content and must land through a **new
follow-up pull request**; #92 is closed and cannot be updated. Until that
follow-up merges, the merged text on `main` must be read as historical, and the
gate in [§2.2](#sec-2) is defined by this text, not by it.

The correction is not cosmetic: on the merged text a reader would conclude that
41 contracts are already satisfied. Every one of those claims has since been
withdrawn for lack of exact evidence, which is why the follow-up is
release-blocking rather than editorial.

Eight review comments were filed against revision 1's head. Each is resolved
here, with the row or section that carries the fix:

| # | Comment | Disposition |
| --- | --- | --- |
| 1 | Shoal pin was not a valid commit | [§1](#sec-1) now carries `1c2944798faf5a5deb659065dfea0bee23593df0`, verified with `git rev-parse origin/main`. |
| 2 | The gate was circular: nothing can become `Covered` before implementation, and `Not required` rows were swept into the requirement set | Split into an implementation-entry gate ([§2.1](#sec-2)) and a final release gate ([§2.2](#sec-2)), with "required rows" defined as every row whose status is not `Not required`. |
| 3 | Timeout mapping claimed `Covered` without semantic Go **and** C evidence | [SB-CFG-017](#sec-6) is `Behavior mismatch`; the cited tests are identified as pool-conflict and `struct_size` checks, and the closing test is named. |
| 4 | `read_accumulo_nex` and the `pandashark.torch` / `DataFrameIterator` surface were not enumerated | [SB-PANDA-003](#sec-9-3) (`read_accumulo_nex`), [SB-PANDA-004](#sec-9-3)–[SB-PANDA-006](#sec-9-3) and [SB-PANDA-013](#sec-9-3) (`DataFrameIterator` constructor, `get_columns`, `__iter__`, `__next__`), [SB-PANDA-014](#sec-9-3)–[SB-PANDA-021](#sec-9-3) (`pandashark.torch`, byte-identical to `sharkbite/torch.py`). |
| 5 | `AccumuloBase`/`AccumuloWriter`/`AccumuloScanner` constructors and `get`/`fetch_range`/`fetch_ranges`/`__del__`/`select_column` were missing | [§10.1](#sec-10-1): [SB-BASE-001](#sec-10-1), [SB-BASE-008](#sec-10-1), [SB-BASE-009](#sec-10-1), [SB-BASE-011](#sec-10-1)–[SB-BASE-014](#sec-10-1), [SB-BASE-020](#sec-10-1). |
| 6 | PyTorch constructors, `coerce`, `__iter__`, `__next__` were grouped | [§9.2](#sec-9-2): [SB-TORCH-002](#sec-9-2)–[SB-TORCH-008](#sec-9-2), one row per member. |
| 7 | `getErrorCode` was treated as a Python contract | [SB-ERR-002](#sec-18) records it as C++-only (unbound in `pysharkbite.cpp`) with the C++ mapping obligation kept in [SB-GAP-T-005](#sec-23). |
| 8 | The real pybind11 behavior for unregistered C++ exceptions and private fields was not recorded | [SB-ERR-016](#sec-18) (generic `RuntimeError` for everything except the two registered types) and [SB-UNSAFE-028](#sec-21) (`DataFrameIterator`'s dead `_iterator` versus name-mangled `__iterator`). |

<a id="sec-4"></a>

## 4. How to read the matrix

### 4.1 Columns

| Column | Meaning |
| --- | --- |
| ID | Stable row anchor. Never renumber; retire with a tombstone instead. |
| Sharkbite | Python-visible symbol (or C++ symbol for [§19](#sec-19)) with its pinned signature/defaults. |
| Shoal Go | Exact Go symbol in `accumulo/` (or an internal package, marked as such) or `—` when absent. |
| Shoal C ABI | Exact declaration in `capi/include/shoal.h` / `shoal_types.h`, or `—` when absent. |
| Evidence | Named tests, on both layers where both are claimed. `—` means no test exists. |
| Status | One of the six values below. |
| Notes | Semantics, defaults, ownership, and shim requirements. |

### 4.2 Status decision procedure

Applied top-down; the first matching rule wins.

1. **`Missing Go`** — no Go implementation exists in `accumulo/` or a reachable
   internal package. This is the bottom-most gap and always blocks first.
2. **`Missing C ABI`** — a Go implementation exists but the capability is not
   reachable through `capi/include/shoal.h`. This bucket also covers rows whose
   only remaining work is above the ABI (wheels, packaging, Python shims),
   because such rows are still unreachable from Python today.
3. **`Behavior mismatch`** — reachable on both layers, but user-visible
   semantics differ (types, defaults, ordering, lifetime, error class,
   binary handling, streaming vs materialized).
4. **`Intentional divergence (approval required)`** — deliberately not
   reproduced; requires a signed entry in [§26](#sec-26).
5. **`Not required (rationale required)`** — out of the compatibility contract;
   requires a stated rationale in the Notes column.
6. **`Covered`** — reachable with equivalent semantics on every layer the row
   claims, with named tests on each of those layers.

A row only owes evidence for the layers its `Shoal Go` and `Shoal C ABI`
columns actually name. Where a column reads `n/a`, `implicit`, or `—`, no
separate test is owed for that layer, and the row must say why in Notes.
`cmd/shoal-capi/*_test.go` counts as C ABI evidence: those tests exercise the
exported ABI entry points directly, and `TestSharedLibraryCABI`
(`cmd/shoal-capi/cabi_test.go`) is the harness that builds the `c-shared`
library and compiles, links, and runs `main()` in `capi/tests/lifecycle.c` and
`main()` in `capi/tests/result_bridge.c` against it.

**Unproven equivalence counts as difference.** A capability that is reachable
on both layers but whose Sharkbite-visible semantics are not demonstrated by a
named test is recorded as `Behavior mismatch`, not `Covered`, and its Notes
must name the test that would close it. Tests that only prove argument
validation, struct sizing, handle lifecycle, or construction do **not** prove
the semantics of the operation they configure, and a test that links against
internal implementation files rather than the exported library does not prove
anything about the ABI. Two independent audits have now applied this rule: the
first reclassified 26 rows (marked `audit #92` in Notes), the second a further
11 (marked `audit #2`).

### 4.3 Layer legend

`Go` = `accumulo/` package. `ABI` = `capi/include/shoal.h`.
`internal` = present in Shoal but not exported (`internal/...`); an internal
implementation never satisfies a row on its own, because Sharkbite consumers
cannot reach it — it only shortens the Go-parity work.

<a id="sec-5"></a>

## 5. Matrix: packaging, imports, and wheels (`SB-PKG`)

Sharkbite's import contract is part of its public API: `PYTHONREADME.md:10`
states "package import is now **sharkbite** not **pysharkbite**", while
`sharkbite/__init__.py:5` re-exports the native module, so **both** names are
importable and both are used by the pinned tests and examples.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-PKG-001 | Distribution `sharkbite`, version `1.2.0.3` (`setup.py:34-35`) | — | — | — | Missing C ABI | No Python packaging exists in Shoal. `Makefile` has `build`, `capi`, `test`, `test-hdfs`, `vet`, `clean` only — no wheel/sdist target. |
| SB-PKG-002 | `import sharkbite` (`examples/hdfswrite.py`, `PYTHONREADME.md:10`) | — | — | — | Missing C ABI | Top-level package must exist and export the full native surface. |
| SB-PKG-003 | `import pysharkbite` (native module; `test/python/testmodule/__init__.py:64`) | — | — | — | Missing C ABI | Legacy module name still used by every pinned Python test. Must remain importable. |
| SB-PKG-004 | `from .pysharkbite import *` (`sharkbite/__init__.py:5`) | — | — | — | Missing C ABI | Star-export means every native symbol is reachable as `sharkbite.X`. Shim must reproduce the exact export set. |
| SB-PKG-005 | `pkgutil.extend_path` namespace extension (`sharkbite/__init__.py:1-3`) | — | — | — | Not required (rationale required) | Rationale: namespace-package extension has no Shoal analogue and no pinned consumer; reproducing it risks import-shadowing. Record as divergence if any downstream relies on it. |
| SB-PKG-006 | `ext_modules=[CMakeExtension('sharkbite.pysharkbite')]` (`setup.py:42`) | — | — | — | Not required (rationale required) | Rationale: build mechanism, not API. Shoal will ship a `cffi`/`ctypes` binding over `libshoal`, not a CMake extension. Note the pinned `setup.py` never sets `cmdclass`, so the committed packaging path cannot actually build the extension. |
| SB-PKG-007 | `python_requires='>=3.6'` (`setup.py:49`) | — | — | — | Missing C ABI | Shoal must decide and record a floor (3.9+ recommended); 3.6 is EOL. Divergence must be approved if the floor is raised. |
| SB-PKG-008 | Platform classifiers: Linux or macOS only, chosen at build time (`setup.py:27-31,44-48`) | — | — | — | Missing C ABI | No Windows wheel in Sharkbite. Shoal's ABI declares `__declspec(dllexport)`/`__cdecl` for `_WIN32` (`capi/include/shoal_types.h`), so Windows support is possible and must be an explicit decision. |
| SB-PKG-009 | `zip_safe=False`, `packages=['sharkbite']` (`setup.py:43,50`) | — | — | — | Missing C ABI | Wheel layout contract. |
| SB-PKG-010 | Shared object must be `ctypes.cdll.LoadLibrary`-able before `import pysharkbite` (`test/python/testmodule/__init__.py:63-64`, `test/MainExecutor.py`) | — | — | — | Behavior mismatch | Sharkbite tests preload the `.so` explicitly. A Shoal binding over `libshoal` must not require preloading; the shim should make the `-s/--solocation` argument a no-op so pinned test drivers still run. |
| SB-PKG-011 | CI builds one Ubuntu configuration, no wheel job (`.github/workflows/ccpp.yml`) | — | — | Shoal CI: `.github/workflows/ci.yml` build/vet/race jobs | Missing C ABI | Shoal has no wheel or `manylinux` job. Wheel matrix (CPython × platform) must be defined before any release. |
| SB-PKG-012 | `sharkbite.torch` submodule import (`examples/torchexample.py`, `sharkbite/torch.py`) | — | — | — | Missing C ABI | Shipped inside the `sharkbite` distribution (`setup.py:50`). Members enumerated in [§9.2](#sec-9). |
| SB-PKG-013 | `pandashark` package: `read_accumulo`, `read_accumulo_nex`, `DataFrameIterator` (`pandashark/__init__.py`, `pandashark/pandassharkbite.py`, `examples/dataframe.py:68-69`) | — | — | — | Missing C ABI | audit #92: this row previously dismissed pandashark as out of scope. It is not. `pandashark` ships in the Sharkbite repository, is built from a second setuptools script committed beside `setup.py` (`pandashark.py:33-50`, dist `pandashark` 1.1.1.1, `packages=['pandashark']`), and imports the `sharkbite` package directly (`pandashark/__init__.py:47`), consuming `AccumuloBase`, `AccumuloScanner`, and `AccumuloIterator`. It therefore breaks whenever the high-level `sharkbite` API changes and is part of the compatibility contract. Its surface is enumerated row by row in [§9.3](#sec-9). Note for auditors: the two distributions are separate artifacts on PyPI, but they are **not** independent — `pandashark` is unusable without a matching `sharkbite`. |
| SB-PKG-014 | Accumulo version support: 1.6.x–2.x (`PYTHONREADME.md:9`) | `accumulo.DefaultAccumuloVersion = "4.0.0-SNAPSHOT"`; `normalizeConnectorOptions` rejects non-`4.` (`accumulo/config.go:13,55-61`) | `shoal_connector_config.accumulo_version` (`capi/include/shoal_types.h`) | `TestNewConnectorRejectsUnsupportedVersion` (`accumulo/connector_test.go:42`); `main()` in `capi/tests/lifecycle.c` | Intentional divergence (approval required) | Shoal targets Accumulo 4 only. Every Sharkbite user on 1.x/2.x is unsupported. This is the single largest divergence in the document and must be approved explicitly, with a documented migration statement. |

<a id="sec-6"></a>

## 6. Matrix: configuration, instance, and credentials (`SB-CFG`)

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-CFG-001 | `Configuration()` (`pysharkbite.cpp:57-58`) | `NewConfiguration()` (`accumulo/configuration.go:39`) with `ZooKeeperConfig.Configuration` (`accumulo/config.go:33`) | `shoal_configuration_create`, `shoal_configuration_free` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Constructs the same untyped string bag Sharkbite constructs, so arbitrary keys such as `FILE_SYSTEM_ROOT` are accepted and readable rather than silently dropped. Transport wiring stays in the typed `ConnectorOptions` struct (`accumulo/config.go:37`), which is Shoal's own surface and is mapped by [SB-CFG-017](#sec-6). The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-002 | `Configuration.set(key, value)` (`pysharkbite.cpp:59`) | `Configuration.Set(name, value string)` (`accumulo/configuration.go:44`) | `shoal_configuration_set` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Last write wins, exactly as Sharkbite's `configurationMap[name] = value`. Safe for concurrent use, which the C++ map is not; the type must not be copied after first use, so `Clone` exists for independent copies. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-003 | `Configuration.get(key)` (`pysharkbite.cpp:60`) | `Configuration.Get(name string) string` (`accumulo/configuration.go:59`) | `shoal_configuration_get`, `shoal_bytes_result_get`, `shoal_bytes_result_free` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Returns the empty string for an unset name, matching Sharkbite's `get(name)`, which delegates to `get(name, "")`. `Configuration.Lookup` (`accumulo/configuration.go:77`) is the Go-idiomatic form that distinguishes unset from set-to-empty, which Sharkbite cannot express. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-004 | `Configuration.get(key, default)` (`pysharkbite.cpp:61`) | `Configuration.GetOr(name, def string) string` (`accumulo/configuration.go:66`) | `shoal_configuration_get_or` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Returns `def` only when the name is unset; a name set to the empty string returns the empty string, matching the C++ map lookup. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-005 | `Configuration.getLong(key, default) -> uint32` (`pysharkbite.cpp:62`; `include/data/constructs/configuration/Configuration.h:58`) | `Configuration.GetUint32Or(name string, def uint32) uint32` (`accumulo/configuration.go:109`) | `shoal_configuration_get_uint32_or` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Returns `def` when unset and 0 when the stored value is not numeric, matching `atol` on a non-numeric string. **Safe shim for unsafe C behavior:** Sharkbite truncates a `long` into `uint32_t`, so a negative value wraps and an out-of-range value is undefined behavior; Shoal saturates instead (negative to 0, above `math.MaxUint32` to `math.MaxUint32`). The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-024 | `Configuration::getLong(name) const` single-argument overload (`include/data/constructs/configuration/Configuration.h:57`) | `Configuration.GetUint32(name string) uint32` (`accumulo/configuration.go:96`) | `shoal_configuration_get_uint32` (`capi/include/shoal.h`) | `TestConfigurationGetAndSet`, `TestConfigurationGetUint32`, `TestConfigurationKeysLenAndClone`, `TestConfigurationNilReceiverIsSafe`, `TestConfigurationConcurrentUse` (`accumulo/configuration_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Unset or non-numeric yields 0, the observable result of Sharkbite's single-argument `getLong`. **Safe shim for unsafe C behavior:** the pinned C++ implementation evaluates `atol(get(name, 0).c_str())`, and that `0` is converted to `std::string`, which is undefined behavior for a null pointer; Shoal reads the unset name as the empty string instead. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-025 | `virtual Configuration::~Configuration()` (`include/data/constructs/configuration/Configuration.h:39`) | Go GC | — | — | Not required (rationale required) | Rationale: virtual destructor with no Python or Go analogue; `Configuration` is held by `shared_ptr` and `enable_shared_from_this` (`include/data/constructs/configuration/Configuration.h:30`), so destruction is refcount-driven. Recorded to complete the class's member list. |
| SB-CFG-006 | `Instance` opaque base class (`pysharkbite.cpp:64`) | `Instance` interface (`accumulo/instance.go:23`) | Folded into `shoal_connector_config.bootstrap` (`capi/include/shoal_types.h`) | `TestNewStaticInstance` (`accumulo/instance_test.go:12`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite exposes `Instance` as a Python type used for isinstance-style polymorphism; Shoal's ABI has no instance handle, only a bootstrap discriminator. Shim must synthesize an `Instance` object. |
| SB-CFG-007 | `ZookeeperInstance(instance, zookeepers, timeoutMs, Configuration)` (`pysharkbite.cpp:69-70`; `include/data/constructs/client/zookeeperinstance.h:76-82`) | `NewZooKeeperInstance(ctx context.Context, cfg ZooKeeperConfig)` (`accumulo/instance.go:96 107`) with `ZooKeeperConfig` (`accumulo/config.go:22`) | `SHOAL_BOOTSTRAP_ZOOKEEPER` + `zookeeper_servers`, `zookeeper_session_timeout_ms` (`capi/include/shoal_types.h`) | `TestNewZooKeeperInstanceResolvesAndClosesOnce` (`accumulo/instance_test.go:22`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The only constructor bound to Python. Sharkbite takes a comma-separated string and a millisecond `uint32`; Shoal takes `[]string` + `time.Duration` and requires a `context.Context` the Python signature has no slot for. Shim must split the string and own the context. |
| SB-CFG-026 | `explicit ZookeeperInstance(const ZookeeperInstance *zki)` copy-from-pointer constructor (`include/data/constructs/client/zookeeperinstance.h:66-69`) | — (`Instance` is an interface; no copy constructor) | — | — | Behavior mismatch | C++-only: delegates to the four-argument constructor with the source's fields, so it re-runs ZooKeeper resolution and allocates a second `ZooCache`/`ZooKeeper` pair rather than sharing the original's. Not reachable from Python. |
| SB-CFG-027 | `explicit ZookeeperInstance(std::string in, std::string zks, uint32_t zkTimeoutMs)` three-argument constructor (`include/data/constructs/client/zookeeperinstance.h:120-124`) | — | — | — | Behavior mismatch | C++-only and **unusable as written**: it never initialises `myConfiguration` yet dereferences it at `include/data/constructs/client/zookeeperinstance.h:125` ([SB-UNSAFE-029](#sec-21)). Any Shoal-backed replacement must take a configuration or default one. |
| SB-CFG-028 | `ZookeeperInstance::~ZookeeperInstance()` (`include/data/constructs/client/zookeeperinstance.h:158-164`) | `Instance.Close() error` (`accumulo/instance.go:143`) | Folded into `shoal_connector_close`/`_free` (`capi/README.md`: a connector owns its bootstrap instance) | `TestNewZooKeeperInstanceResolvesAndClosesOnce` (`accumulo/instance_test.go:22`) | Behavior mismatch | Non-virtual destructor that manually `delete`s the raw `ZooCache` and `ZooKeeper` pointers; Shoal's `Close` is explicit and idempotent, and Python never sees a destructor call site. After `Close` the live topology accessors report `ErrInstanceClosed` (`accumulo/errors.go:71`) and `Locator.Close` (`internal/zk/locator.go:115`) drops every scoped connection it handed out, so a closed instance can never reopen a ZooKeeper session. |
| SB-CFG-029 | `ZookeeperInstance::getRootTabletLocation()` (`include/data/constructs/client/zookeeperinstance.h:170`) | `Instance.RootTabletLocation(ctx)` (`accumulo/instance.go:40`), `zkLocator.RootTabletLocation` (`accumulo/topology.go:85`) | `shoal_connector_get_root_tablet_location` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | **Accumulo 4 wire divergence, same capability.** Sharkbite reads the Accumulo 1.x znode `<root>/root_tablet/location` and splits the value on a pipe character; Accumulo 4 stores `RootTabletMetadata` JSON at `/accumulo/<id>/root_tablet`, which `Locator.RootTabletLocation` (`internal/zk/locator.go:189`) decodes. Returns `ErrTabletNotLocated` while the root tablet is unassigned, where Sharkbite returns `NULL` cast to `std::string`, which is undefined behavior. Resolved on every call, never cached, honors `ctx` cancellation in flight, and reports `ErrInstanceClosed` (`accumulo/errors.go:71`) after `Instance.Close` for every context kind. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-030 | `ZookeeperInstance::getMasterLocations()` (`include/data/constructs/client/zookeeperinstance.h:176`) | `Instance.ManagerLocations(ctx)` (`accumulo/instance.go:47`), `zkLocator.ManagerLocations` (`accumulo/topology.go:120`), `ManagerAddresses` (`internal/zk/manager.go:164`) | `shoal_connector_get_manager_locations`, `shoal_string_list_get` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `TestManagerAddressesOrdersByLockSequenceAndDeduplicates`, `TestManagerAddressesDoesNotPromoteQueuedCandidateOverPlaceholderHolder`, `TestManagerAddressesSkipsQueuedBootstrapDescriptors` (`internal/zk/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | **Accumulo 4 wire divergence, same capability.** Lock children of `/accumulo/<id>/managers/lock` are validated and ordered exactly as `ServiceLock.validateAndSort` does, and each lock node's `ServiceLockData` JSON contributes its `MANAGER` descriptor. The lock holder is the lowest-sequence node that still exists; if it advertises only a bootstrap descriptor or the `0.0.0.0:0` placeholder the result is `ErrManagerUnavailable` rather than a queued candidate promoted into first place, so the first address is always the active manager. Sharkbite returns an empty vector when nothing is advertised, which a caller cannot distinguish from a ZooKeeper failure. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-031 | `ZookeeperInstance::getZookeepers() const` (`include/data/constructs/client/zookeeperinstance.h:189`) | `Instance.ZooKeepers()` (`accumulo/instance.go:56`), `zkLocator.ZooKeepers` (`accumulo/topology.go:173`) | `shoal_connector_get_zookeepers`, `shoal_string_list_get` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Returns a defensive copy of `ZooKeeperConfig.Servers`, so neither the caller's slice nor a previous result can mutate what the instance reports, and it keeps answering after `Close`. Sharkbite returns the raw comma-separated string it was constructed with. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-032 | `ZookeeperInstance::getConfiguration()` returning `const Configuration *` (`include/data/constructs/client/zookeeperinstance.h:194`) | `Instance.Configuration()` (`accumulo/instance.go:64`), `zkLocator.Configuration` (`accumulo/topology.go:187`) | `shoal_connector_get_configuration` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | The instance stores a `Clone` of `ZooKeeperConfig.Configuration` (`accumulo/config.go:33`) taken at construction, so later mutation of the caller's configuration cannot change what the instance reports, and the returned pointer is the instance's own configuration, not a per-call copy. Never nil, including for `NewStaticInstance`, and still answering after `Close`. Sharkbite hands back a `const Configuration *` that aliases the shared pointer it was constructed with. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-033 | `ZookeeperInstance::getZooCache()` (`include/data/constructs/client/zookeeperinstance.h:204`) | `internal/cache` — **internal** | — | — | Not required (rationale required) | Rationale: exposes the raw `ZooCache *` the instance owns and deletes; it is an internal implementation detail that leaked into the public header, and no pinned test, example, or Python binding uses it. Reproducing it would export Shoal's cache. |
| SB-CFG-034 | `ZookeeperInstance::getInstanceCache()` (`include/data/constructs/client/zookeeperinstance.h:213`) | `internal/cache` — **internal** | — | — | Not required (rationale required) | Rationale: as SB-CFG-033 — the same pointer returned under a second name and base type. |
| SB-CFG-035 | `virtual ZookeeperInstance::getServers()` returning `vector<shared_ptr<ServerConnection>>` (`include/data/constructs/client/zookeeperinstance.h:222`) | `Instance.Servers(ctx)` (`accumulo/instance.go:52`), `zkLocator.Servers` (`accumulo/topology.go:149`), `ServerConnection` (`accumulo/topology.go:34`), `ClientServices` (`internal/zk/manager.go:327`) | `shoal_connector_get_servers`, `shoal_server_list_get` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `TestClientServicesReportsKindGroupAndOrder`, `TestClientServicesOrdersByServerIdentity`, `TestClientServicesPromotesNextSurvivingServerLock`, `TestClientServicesUsesOnlyTheSurvivingHolderLock` (`internal/zk/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | **Accumulo 4 wire divergence, richer result.** Sharkbite reads the flat 1.x `<root>/tservers` layout; Accumulo 4 publishes lock znodes under `/accumulo/<id>/{tservers,sservers,compactors}/<resource-group>/<server>`, so every endpoint carries `Kind` and `Group` in addition to host and port. Ordered by role, then resource group, then the publishing ZooKeeper server, then advertised address, which is exactly what `ClientServices` sorts by; an address published twice inside one group is reported once. Each server's lock holder is the lowest-sequence lock node that still exists, so a handoff between listing and reading promotes the next surviving node instead of dropping the server. Endpoints with no host or port 0 are rejected as errors rather than silently dropped, and `ErrClientServiceUnavailable` is returned when nothing advertises the client service. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-036 | `ZookeeperInstance::getRoot()` (`include/data/constructs/client/zookeeperinstance.h:228`) | `Instance.Root()` (`accumulo/instance.go:60`), `zkLocator.Root` (`accumulo/topology.go:180`), `InstanceRoot` (`internal/zk/locator.go:207`) | `shoal_connector_get_root`, `shoal_bytes_result_get` (`capi/include/shoal.h`) | `TestInstanceRootTabletLocation`, `TestInstanceManagerLocations`, `TestInstanceServers`, `TestInstanceZooKeepersRootAndConfiguration`, `TestStaticInstanceTopology`, `TestInstanceTopologyCancelsInFlightReads`, `TestInstanceTopologyAfterCloseRejectsLiveDiscovery` (`accumulo/topology_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Returns `/accumulo/<instance-id>`, the same value Sharkbite composes from `ZROOT` and the resolved instance ID. Defined for `NewStaticInstance` too, because it depends only on the instance ID, and it remains valid after `Close`. The stable ABI copies binary-safe inputs and returns only owned handles/results; free functions are NULL-safe and idempotent. |
| SB-CFG-008 | `ZookeeperInstance(None, None, 1000, None)` raises (`test/python/TestBadOperations.py:67-71`) | `newZooKeeperInstance` validation (`accumulo/instance.go:63`) | `SHOAL_STATUS_INVALID_ARGUMENT` (`capi/include/shoal_types.h`) | `TestNewZooKeeperInstanceValidatesConfig` (`accumulo/instance_test.go:54`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Both reject, but Sharkbite raises `ClientException`/`TypeError`; Shoal returns an error value. Exception class mapping is [SB-ERR-002](#sec-18). |
| SB-CFG-009 | `ZookeeperInstance.getInstanceName()` (`pysharkbite.cpp:71`) | `InstanceInfo` via `Instance.Info()` (`accumulo/instance.go:16,23`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_name` | `TestReadConnectorIdentityUsesCapturedValues`, `TestReadConnectorIdentityHonorsDeadline` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI copies the immutable name into an owned result; both alias spellings and Python exception mapping remain shim work. |
| SB-CFG-019 | `ZookeeperInstance.instance_name()` alias (`pysharkbite.cpp:72`) | `InstanceInfo` via `Instance.Info()` (`accumulo/instance.go:16,23`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_name` | `TestReadConnectorIdentityUsesCapturedValues` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-CFG-009](#sec-6); the ABI value is available, but the shim must register this independent alias. |
| SB-CFG-010 | `ZookeeperInstance.getInstanceId(retry=False)` (`pysharkbite.cpp:74`) | `InstanceInfo.ID` via `Instance.Info()` / `Connector.Instance()` (`accumulo/instance.go:16`, `accumulo/connector.go:135`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_id` | `TestReadConnectorIdentityUsesCapturedValues`, `TestReadConnectorIdentityHonorsDeadline` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI returns the connector's immutable copied ID; Sharkbite's `retry` keyword and Python exception behavior remain shim policy. |
| SB-CFG-020 | `ZookeeperInstance.instance_id(retry=False)` alias (`pysharkbite.cpp:73`) | `InstanceInfo.ID` via `Instance.Info()` / `Connector.Instance()` (`accumulo/instance.go:16`, `accumulo/connector.go:135`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_id` | `TestReadConnectorIdentityUsesCapturedValues` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-CFG-010](#sec-6); the flat ABI exposes the stable value, while the shim must register the alias and define `retry`. |
| SB-CFG-011 | Static/standalone instance (no direct Sharkbite equivalent) | `NewStaticInstance(name, id)` (`accumulo/instance.go:153`) | `SHOAL_BOOTSTRAP_STATIC` (`capi/include/shoal_types.h`) | `TestNewStaticInstance` (`accumulo/instance_test.go:12`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal-only capability with no Sharkbite counterpart. Listed so the matrix is complete in both directions; it adds no compatibility obligation. |
| SB-CFG-012 | `AuthInfo(username, password, instanceId)` (`pysharkbite.cpp:76-77`) | `PasswordCredentials(principal string, password []byte)` (`accumulo/credentials.go:21`) | `shoal_connector_config.principal` / `password` / `password_length` (`capi/include/shoal_types.h`) | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite binds the instance ID into the credential object; Shoal binds it at `NewConnector`. Sharkbite takes `str` passwords, Shoal takes `[]byte` — the shim must accept `str` and `bytes` and must not transcode `bytes`. |
| SB-CFG-013 | `AuthInfo.getUserName()` (`pysharkbite.cpp:78`) | `Credentials.Principal()` (`accumulo/credentials.go:34`), `Connector.Principal()` (`accumulo/connector.go:142`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.principal` | `TestReadConnectorIdentityUsesCapturedValues`, `TestReadConnectorIdentityConcurrentCloseCancelsAndJoins` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The authenticated principal is copied into owned result memory; constructing Sharkbite's separate `AuthInfo` object remains shim work. |
| SB-CFG-021 | `AuthInfo.username()` alias (`pysharkbite.cpp:79`) | `Credentials.Principal()` (`accumulo/credentials.go:34`), `Connector.Principal()` (`accumulo/connector.go:142`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.principal` | `TestReadConnectorIdentityConcurrentCloseCancelsAndJoins` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-CFG-013](#sec-6); the ABI value is available, but the shim must register the independent alias. |
| SB-CFG-014 | `AuthInfo.getPassword()` (`pysharkbite.cpp:81`) | — (deliberately unreadable; `Credentials.String()`/`GoString()` redact, `accumulo/credentials.go:37,42`) | — | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Intentional divergence (approval required) | Sharkbite hands the plaintext password back to the caller. Shoal deliberately redacts. Reproducing a password getter re-introduces a credential-leak path (it lands in logs via `repr`). Proposal: raise `AttributeError` with a migration message, or return the value only when the caller passed it in-process. Requires approval. |
| SB-CFG-022 | `AuthInfo.password()` alias (`pysharkbite.cpp:80`) | — (deliberately unreadable; `Credentials.String()`/`GoString()` redact, `accumulo/credentials.go:37,42`) | — | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Intentional divergence (approval required) | Second published spelling of [SB-CFG-014](#sec-6); pybind11 registers it as an independent attribute, so a shim must expose both. Same layer mapping, status, and evidence as SB-CFG-014. |
| SB-CFG-015 | `AuthInfo.getInstanceId()` (`pysharkbite.cpp:82`) | `Connector.Instance().ID` (`accumulo/connector.go:135`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_id` | `TestReadConnectorIdentityUsesCapturedValues`, `TestReadConnectorIdentityHonorsDeadline` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The connected instance ID is copied into owned result memory; constructing Sharkbite's separate `AuthInfo` object remains shim work. |
| SB-CFG-023 | `AuthInfo.instance_id()` alias (`pysharkbite.cpp:83`) | `Connector.Instance().ID` (`accumulo/connector.go:135`) | `shoal_connector_get_identity` → `shoal_connector_identity_view.instance_id` | `TestReadConnectorIdentityUsesCapturedValues` (`cmd/shoal-capi/identity_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-CFG-015](#sec-6); the ABI value is available, but the shim must register the independent alias. |
| SB-CFG-016 | `AuthInfo` copy assignment silently drops the password (`include/data/constructs/security/AuthInfo.h:60-67`) | `Credentials.clone()` copies all fields (`accumulo/credentials.go:44`) | n/a | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Not required (rationale required) | Rationale: this is an upstream defect, not a contract. See [§21](#sec-21). Do not reproduce. |
| SB-CFG-017 | ZooKeeper session timeout is the only timeout in the Sharkbite public API (`include/data/constructs/client/zookeeperinstance.h:89-104`) | `ZooKeeperConfig.SessionTimeout`, `ConnectorOptions.DialTimeout`, `IdleTimeout` (`accumulo/config.go:22-42`) | `zookeeper_session_timeout_ms`, `bootstrap_timeout_ms`, `dial_timeout_ms` (`capi/include/shoal_types.h`) | `TestNewConnectorRejectsConflictingPoolOptions` (`accumulo/connector_test.go:70`) proves pool-option conflict rejection only; `main()` in `capi/tests/lifecycle.c` proves `struct_size` initialisation only | Behavior mismatch | audit #92: neither cited test proves that a Sharkbite ZooKeeper timeout maps onto `SessionTimeout` or is honoured. Defaults also differ — a zero timeout selects Shoal's 30 s default (`accumulo/instance.go:13`, `capi/README.md`), while Sharkbite has no default and every pinned caller passes 1000 ms. Closes when a test asserts the resolved session timeout for an explicit value and for zero. |
| SB-CFG-018 | Instance secret (not exposed in Sharkbite Python) | `ZooKeeperConfig.InstanceSecret` (`accumulo/config.go:26`) | `shoal_connector_config.instance_secret` (`capi/include/shoal_types.h`) | `TestNewZooKeeperInstanceValidatesConfig` (`accumulo/instance_test.go:54`) | Not required (rationale required) | Rationale: Shoal-only; no Sharkbite obligation. |

<a id="sec-7"></a>

## 7. Matrix: connector and session (`SB-CONN`)

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-CONN-001 | `AccumuloConnector(AuthInfo, Instance)` (`pysharkbite.cpp:259`) | `accumulo.NewConnector(instance, credentials, ConnectorOptions)` (`accumulo/connector.go:36`) | `shoal_connector_create` (`capi/include/shoal.h`) | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`) uses a fake instance; `main()` in `capi/tests/lifecycle.c` uses `SHOAL_BOOTSTRAP_STATIC` | Behavior mismatch | audit #2: every Sharkbite connector is built from a `ZookeeperInstance` ([SB-CFG-007](#sec-6)) with a 1000 ms session timeout, and neither cited test constructs a ZooKeeper-backed connector. What is proven is handle creation and lifecycle, not the Sharkbite construction path. Closes with the ZooKeeper-bootstrap test named in [SB-CONN-002](#sec-7). |
| SB-CONN-002 | `AccumuloConnector(instance, zookeepers, username, password)` convenience ctor (`pysharkbite.cpp:260-265`) | Compose `NewZooKeeperInstance` + `PasswordCredentials` + `NewConnector` | Compose `shoal_connector_config` with `SHOAL_BOOTSTRAP_ZOOKEEPER` | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`) uses a fake instance, not ZooKeeper composition; `main()` in `capi/tests/lifecycle.c` uses `SHOAL_BOOTSTRAP_STATIC` | Behavior mismatch | audit #92: no test exercises the ZooKeeper-composed convenience path, and none pins the 1000 ms session timeout this ctor hardcodes (`pysharkbite.cpp:262`) against Shoal's 30 s default. Closes with a ZooKeeper-bootstrap test asserting the composed instance, principal, and resolved timeout. |
| SB-CONN-003 | `AccumuloConnector.tableOps(table) -> AccumuloTableOperations` (`pysharkbite.cpp:269`) | No handle type; ops are `Connector` methods taking a table name (`accumulo/table_admin.go:76`) | No handle type; scanner/writer configs take `table_name`/`table_id` (`capi/include/shoal_types.h`) | `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`) | Behavior mismatch | Sharkbite binds a table to a stateful handle whose methods take no table argument. Shim must provide the handle object and hold the table name. |
| SB-CONN-004 | `AccumuloConnector.securityOps() -> SecurityOperations` (`pysharkbite.cpp:266`) | Security administration methods on `Connector` (`accumulo/security.go`) | Flat `shoal_connector_*user*`, authorization, and permission exports | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies` (`accumulo/security_test.go`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Shoal exposes the complete operations directly on the connector rather than returning a stateful `SecurityOperations` handle; the Python shim must restore that object shape. See [§13](#sec-13). |
| SB-CONN-005 | `AccumuloConnector.namespaceOps() -> AccumuloNamespaceOperations` (`pysharkbite.cpp:267`) | Namespace administration methods on `Connector` (`accumulo/namespace_admin.go`, `accumulo/namespace_discovery.go`, `accumulo/namespace_properties.go`) | Flat `shoal_connector_*namespace*` exports with owned results | `TestNamespaceDiscoveryListingAndLookup` (`accumulo/namespace_discovery_test.go`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Shoal exposes the complete operations directly on the connector rather than returning a namespace-bound handle; the Python shim must restore handle-relative defaults and return types. See [§12](#sec-12). |
| SB-CONN-006 | `AccumuloConnector.tableInfo() -> AccumuloTableInfo` (`pysharkbite.cpp:270`) | `Connector.Tables`, `TableByName`, `TableByID` (`accumulo/table_admin.go:29`, `accumulo/discovery.go:110,132`) | `shoal_connector_list_tables`, `shoal_connector_table_exists`, and owned `shoal_table_list_result` (`capi/include/shoal.h`) | `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`); `TestDiscoveryTableLookupAndRouting` (`accumulo/discovery_test.go:312`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Shoal can supply the same lookup material through connector-scoped list and existence calls, but not as a dedicated `AccumuloTableInfo` handle. A shim must project those calls into Sharkbite's helper object. |
| SB-CONN-007 | `AccumuloConnector.getStatistics() -> AccumuloInfo` (`pysharkbite.cpp:268`) | — | — | — | Missing Go | Cluster status RPC absent. See [§14](#sec-14). |
| SB-CONN-008 | Connector has no explicit `close()` in Python; lifetime ends with refcount | `Connector.Close() error` (`accumulo/connector.go:138`) | `shoal_connector_close` / `shoal_connector_free` (`capi/include/shoal.h`) | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Shoal requires explicit close for deterministic shutdown. Shim must close on `__del__` **and** support `with`; it must not rely on GC ordering at interpreter shutdown. |
| SB-CONN-009 | Copy-assigning a connector shares pooled transports (`include/interconnect/Accumulo.h:126`) | `Connector` is a pointer type; copying the pointer shares state | **Forbidden**: "Do not copy opaque handles" (`capi/README.md`) | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`); `TestConnectorRegistryLifecycle` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | audit #92: not equivalent. Sharkbite defines copy-assignment as transport sharing; the Shoal ABI prohibits copying a handle at all, so a Python shim holding a handle must forbid `copy.copy`/`copy.deepcopy` of connector objects or synthesize sharing itself. |
| SB-CONN-010 | Per-translation-unit transport pool `ACCUMULO_COORDINATOR`, declared `static` at namespace scope (`include/interconnect/Accumulo.h:50`) | Per-connector pool (`internal/transportpool.Pool`, wired in `accumulo/connector.go:36`) | Per-connector, implicit | `TestNewConnectorCanDisablePooling` (`accumulo/connector_test.go:51`) | Intentional divergence (approval required) | Shoal scopes pooling per connector. Sharkbite's pool is **not** process-wide: `static` at namespace scope gives every translation unit that includes `Accumulo.h` its own pool object, so the sharing scope is "all connectors built in one TU", and a program linking several TUs already has several pools. The divergence is therefore between per-TU sharing and per-connector sharing, not between global and per-connector. Observable difference: connection count and failure blast radius. Requires approval as a divergence. |
| SB-CONN-011 | Process-wide table name/ID cache `Tables::getInstance()` (`include/interconnect/tableOps/TableOperations.h:30,40`) | Per-connector resolver `internal/tablenames.Resolver`; invalidation via `Connector.InvalidateTable` / `InvalidateDiscovery` (`accumulo/discovery.go:215,228`) | `shoal_connector_invalidate_table`, `shoal_connector_invalidate_discovery` (`capi/include/shoal.h`) | `TestDiscoveryInvalidationAndDefensiveCopies` (`accumulo/discovery_test.go:352`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestOwnedCancellationCancelsCurrentAndFutureScans`, `TestOwnedCancellationCloseCancelsAndJoinsActiveScan`, `TestOwnedCancellationConcurrentCancelIsIdempotent` (`cmd/shoal-capi/connector_control_export_test.go`) | Covered | Per-connector scoping is safer and should be approved as part of SB-CONN-010. Revision 30 exposes both connector-scoped invalidation operations with copied table IDs, close coordination, and linked C/C++ coverage. |
| SB-CONN-012 | Reconnect with bad credentials raises `ClientException` (`test/python/TestBadOperations.py:85-94`, `test/python/TestSecurityOperations.py:110-116`) | `NewConnector` error path (`accumulo/connector.go:36`), `ErrPermissionDenied` (`accumulo/errors.go:46`) | `SHOAL_STATUS_PERMISSION_DENIED` (`capi/include/shoal_types.h`) | — | Behavior mismatch | No Shoal test asserts an authentication failure against a live or faked security-exception path. This row cannot close without a live-cluster conformance test ([§24](#sec-24)). |
| SB-CONN-013 | No connection-level cancellation or deadline in the Python API | Every `Connector` method takes `context.Context` (`accumulo/connector.go`, `accumulo/discovery.go`, `accumulo/table_admin.go`) | Per-call `timeout_ms` plus owned one-shot cancellation handles on single and batch scans (`capi/include/shoal.h`) | `TestDiscoveryErrorsAndCancellation` (`accumulo/discovery_test.go:212`); `TestOwnedScannerDeadline` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | Shoal is strictly better, but the ABI now exposes a cancellation handle for in-flight scans, while Sharkbite still has no connection-level cancellation or deadline. See [SB-XCUT-008](#sec-20). |

<a id="sec-8"></a>

## 8. Matrix: data model (`SB-DATA`)

### 8.1 `Value`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-001 | `Value()` (`pysharkbite.cpp:273`) | `KeyValue.Value []byte` (`accumulo/scanner.go:31-34`) | `shoal_key_value_view.value` (`capi/include/shoal_types.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`); `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | Sharkbite has a mutable, shared, refcounted `Value` object; Shoal has a plain byte slice. Shim must wrap bytes in a `Value` class. |
| SB-DATA-002 | `Value.get() -> str` (`pysharkbite.cpp:274`) | `KeyValue.Value []byte` | `shoal_key_value_view.value` | `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | **Lossy.** `getValueAsString` transcodes to a Python `str`; non-UTF-8 values raise `UnicodeDecodeError`. Shoal is byte-exact. See [§21](#sec-21) for the shim rule (`errors='surrogateescape'`). |
| SB-DATA-003 | `Value.get_bytes() -> bytes` (`pysharkbite.cpp:275-279`) | `KeyValue.Value []byte` (`accumulo/scanner.go:31-34`) | `shoal_key_value_view.value` (`capi/include/shoal_types.h`), borrowed until `shoal_scan_result_free` (`capi/include/shoal.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`) decodes ASCII values only (`testEntry`, `accumulo/scanner_test.go:915`); `main()` in `capi/tests/result_bridge.c` is linked against the internal `cmd/shoal-capi/bridge.c`, **not** the exported library exercised by `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`) | Behavior mismatch | audit #2: the binary-safe claim rested on a test of internal bridge helpers, not on `shoal_scan_result_get`. No test carries an empty or NUL-containing value through the public ABI, and no Go test carries one through a scan. Closes with a `lifecycle.c` case that scans a NUL-containing value through `shoal_scan_result_get` plus a Go scan test using the same bytes. |
| SB-DATA-004 | `Value.__str__` returning `""` for a null holder (`pysharkbite.cpp:288-295`) | — | — | — | Behavior mismatch | Shim must reproduce the exact null-holder string; Python code prints values directly (`test/python/TestWithFx.py:69`). |
| SB-DATA-072 | `Value.__repr__` returning `"[]"` for a null holder (`pysharkbite.cpp:280-287`) | — | — | — | Behavior mismatch | Independently registered from SB-DATA-004 and returns a **different** string for the same null holder, so the two cannot share one row. |

### 8.2 `Key`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-005 | `Key()` (`pysharkbite.cpp:298`) | `accumulo.Key{}` (`accumulo/scanner.go:22-29`) | `shoal_key` (`capi/include/shoal_types.h`) | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) builds populated keys; `main()` in `capi/tests/lifecycle.c` initialises `shoal_range`, not `shoal_key` | Behavior mismatch | audit #92: no test covers a default-constructed key. Shoal's zero value has `Timestamp == 0` and no null/unset marker, while Sharkbite's default-constructed `Key` is a distinct empty holder whose printed form is `" : []"` ([SB-DATA-015](#sec-8)). The ABI has no `shoal_key_init`. Closes when a test pins the default timestamp and empty-component behavior on both layers. |
| SB-DATA-006 | `Key(row, cf=None, cq=None, cv=None, timestamp=9223372036854775807)` (`pysharkbite.cpp:299-300`) | `accumulo.Key{Row, ColumnFamily, ColumnQualifier, ColumnVisibility, Timestamp}` | `shoal_key` fields | `TestKeyRangeUsesAccumuloTimestampOrdering` (`accumulo/range_test.go:61`) | Behavior mismatch | Sharkbite's default timestamp is `INT64_MAX`; Shoal's zero value is `0`. Shim must inject `INT64_MAX` when unset or key ordering changes. Parameters are `const char *`, so embedded NULs truncate — see [§21](#sec-21). |
| SB-DATA-007 | `Key.setRow(row="")` (`pysharkbite.cpp:301`) | Field assignment | `shoal_key.row` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) | Behavior mismatch | Setter mutates in place; Shoal's `Key` is a value struct. Shim must keep mutability (`test/python/TestWrites.py:59-65` builds keys by mutation). |
| SB-DATA-008 | `Key.setColumnFamily(cf="")` (`pysharkbite.cpp:317-318`) | Field assignment | `shoal_key.column_family` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) | Behavior mismatch | As above. |
| SB-DATA-009 | `Key.setColumnQualifier(cq="")` (`pysharkbite.cpp:319-320`) | Field assignment | `shoal_key.column_qualifier` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) | Behavior mismatch | As above. No `setColumnVisibility` or `setTimestamp` exists in the pinned binding — a genuine Sharkbite gap the shim need not reproduce. |
| SB-DATA-010 | `Key.getRow() -> str` (`pysharkbite.cpp:322`) | `Key.Row []byte` | `shoal_key_value_view.row` | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`); `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | Binary-unsafe: returns `str`. Sharkbite has **no** bytes accessor for key components, so binary rows are unreachable from Python. Shim must add `get_row_bytes()` and keep `getRow()` for compatibility. |
| SB-DATA-011 | `Key.getColumnFamily() -> str` (`pysharkbite.cpp:323`) | `Key.ColumnFamily []byte` | `shoal_key_value_view.column_family` | `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | As above. |
| SB-DATA-012 | `Key.getColumnQualifier() -> str` (`pysharkbite.cpp:326`) | `Key.ColumnQualifier []byte` | `shoal_key_value_view.column_qualifier` | `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | As above. |
| SB-DATA-013 | `Key.getColumnVisibility() -> str` (`pysharkbite.cpp:324`) | `Key.ColumnVisibility []byte` | `shoal_key_value_view.column_visibility` | `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | As above. |
| SB-DATA-014 | `Key.getTimestamp() -> int64` (`pysharkbite.cpp:325`) | `Key.Timestamp int64` (`accumulo/scanner.go:27`) | `shoal_key_value_view.timestamp` (`capi/include/shoal_types.h`) | `TestKeyRangeUsesAccumuloTimestampOrdering` (`accumulo/range_test.go:61`) covers range bounds; scan fixtures use timestamp `17` only (`accumulo/scanner_test.go:915`); `main()` in `capi/tests/result_bridge.c` exercises `-1` against the **internal** bridge, not `shoal_scan_result_get` | Behavior mismatch | audit #2: signedness is declared on both layers but never proven across the public ABI, and Sharkbite's own flat-C struct uses an unsigned timestamp ([SB-CPP-006](#sec-19)). Closes with a negative-timestamp round trip through `shoal_scan_result_get` and a Go scan decode test. |
| SB-DATA-015 | `Key.__str__` -> `toString()`, `" : []" ` when null (`pysharkbite.cpp:302-308`) | `Key.String()` (`accumulo/key_string.go:18`) | `shoal_key_to_string` + owned `shoal_bytes_result` | `TestKeyStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | audit #114: the row recorded `Behavior mismatch` with no Go mapping at all, which rule 3 of [§4.2](#sec-4) cannot describe — a mismatch needs both layers. `Key.String()` now renders `row family:qualifier [visibility] timestamp` with the pinned escaping (`StringUtils::getEscapedString`: NUL becomes `\u0000`, and `'`, `"`, `?`, `\`, `\a`, `\b`, `\f`, `\n`, `\r`, `\t`, `\v` take their two-character forms) while the visibility is written unescaped, exactly as `Key::toString` does. Sharkbite's null-holder text `" : []"` is what an empty Go key renders, plus its timestamp. No ABI entry point formats a key. |
| SB-DATA-073 | `Key.__repr__` -> `toString()`, `" : []" ` when null (`pysharkbite.cpp:309-316`) | `Key.String()` (`accumulo/key_string.go:18`) | `shoal_key_to_string` + owned `shoal_bytes_result` | `TestKeyStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Same registration body as [SB-DATA-015](#sec-8) and the same Go method; reclassified with it (audit #114). |

### 8.3 `KeyValue`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-016 | `KeyValue()` (`pysharkbite.cpp:329`) | `accumulo.KeyValue{}` (`accumulo/scanner.go:31`) | `shoal_key_value_view` (read-only scan output) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`) covers scanned entries; `main()` in `capi/tests/result_bridge.c` allocates a result array | Behavior mismatch | audit #92: neither test covers a user-constructed empty `KeyValue`. Sharkbite's default holder carries null `Key`/`Value` shared pointers that print as `" : []"`/`"[]"`; Shoal's zero struct carries an empty-but-present key. |
| SB-DATA-017 | `KeyValue(Key, Value)` (`pysharkbite.cpp:330`) | `accumulo.KeyValue{Key: ..., Value: ...}` composite literal (`accumulo/scanner.go:31-34`) | `shoal_key_value_create`, owned `shoal_key_value_result` | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | audit #92: the previous evidence cited an internal bridge helper as if it were a public constructor. The ABI can only *read* key/value views produced by a scan; there is no way to construct one. Sharkbite's constructor exists to feed `SequentialRFile.append` ([SB-RFILE-011](#sec-15)) and Python iterator return values ([SB-SCAN-016](#sec-9)). |
| SB-DATA-018 | `KeyValue.getKey() -> Key` (`pysharkbite.cpp:331`) | `KeyValue.Key` (`accumulo/scanner.go:31-34`) | `shoal_key_value_view` row/family/qualifier/visibility fields (`capi/include/shoal_types.h`) | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`) with ASCII fixtures; `main()` in `capi/tests/result_bridge.c` tests the internal bridge only | Behavior mismatch | audit #2: same evidence defect as SB-DATA-003. The public accessor `shoal_scan_result_get` is only ever called with `NULL` in `main()` in `capi/tests/lifecycle.c`, which proves argument validation, not field extraction. |
| SB-DATA-019 | `KeyValue.getValue() -> Value` (`pysharkbite.cpp:332`) | `KeyValue.Value` (`accumulo/scanner.go:31-34`) | `shoal_key_value_view.value` (`capi/include/shoal_types.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`); `main()` in `capi/tests/result_bridge.c` tests the internal bridge only | Behavior mismatch | audit #2: as SB-DATA-018. Empty values — which `test/python/TestWrites.py:36` writes and reads back — are exercised only against the internal bridge (`result_bridge.c:21-23`), never through the exported ABI. |

### 8.4 `Range`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-020 | `Range()` — infinite range (`pysharkbite.cpp:335`) | `InfiniteRange()` (`accumulo/range.go:65`) | No constructor: caller zeroes `shoal_range`, calls `shoal_range_init`, and sets both bounds to `SHOAL_RANGE_BOUND_UNBOUNDED` (`capi/include/shoal_types.h`) | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`); `main()` in `capi/tests/lifecycle.c` proves only that `shoal_range_init` sets `struct_size` | Behavior mismatch | audit #92: the ABI has no infinite-range primitive and no test scans one; `shoal_range_init` does not set bound kinds. Go side is proven. Closes when an ABI-level test scans an unbounded range. |
| SB-DATA-021 | `Range(row)` — single row (`pysharkbite.cpp:336`) | `NewRangeRow(row []byte)` (`accumulo/range.go:57`) | No helper: caller sets both bounds to `SHOAL_RANGE_BOUND_ROW` with matching rows and inclusive flags (`capi/include/shoal_types.h`) | `TestRowRangeInclusiveEndStillIncludesWholeRow` (`accumulo/range_test.go:72`); `main()` in `capi/tests/lifecycle.c` proves struct init only | Behavior mismatch | audit #92: whole-row semantics are proven in Go only. On the ABI the caller must reconstruct them from bound kinds and inclusive flags, and no test asserts the resulting extent. |
| SB-DATA-022 | `Range(startKey, startInclusive, endKey, endInclusive, update=False)` (`pysharkbite.cpp:337-338`) | `NewKeyRange(start *Key, startInclusive bool, end *Key, endInclusive bool)` (`accumulo/range.go:42`) | `SHOAL_RANGE_BOUND_KEY` (`capi/include/shoal_types.h`) | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`); `TestRangeWireBoundaries` (`accumulo/scanner_test.go:194`) | Behavior mismatch | Shoal has no `update` parameter. In Sharkbite `update=True` rewrites the supplied keys in place. Shim must reject or emulate `update=True`; silently ignoring it changes caller-visible key state. |
| SB-DATA-023 | `Range(key, inclusive)` — 2-arg form (`pysharkbite.cpp:339`) | Compose `NewKeyRange` (`accumulo/range.go:42`) with a nil end key | Compose `shoal_range` with one `UNBOUNDED` bound | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) covers fully bounded key ranges only; `main()` in `capi/tests/lifecycle.c` proves struct init only | Behavior mismatch | audit #92: no test covers the half-open key-range composition on either layer, and Sharkbite's 2-arg form leaves the stop key infinite while marking the start key's inclusivity — the mapping is an assumption until tested. |
| SB-DATA-024 | `Range(startRow, startInclusive, endRow, endInclusive, update=False)` (`pysharkbite.cpp:340-341`) | `NewRange(startRow []byte, startInclusive bool, endRow []byte, endInclusive bool)` (`accumulo/range.go:24`) | `SHOAL_RANGE_BOUND_ROW` (`capi/include/shoal_types.h`) | `TestRangeWireBoundaries` (`accumulo/scanner_test.go:194`); `TestBatchScannerValidatesRanges` (`accumulo/scanner_test.go:855`) | Behavior mismatch | Same `update` gap as SB-DATA-022. Also: Sharkbite accepts `""` as a bound meaning "unbounded" (`test/python/TestRanges.py:120`), and `None` for key bounds (`test/python/TestRanges.py:147`); Shoal distinguishes empty from unbounded (`TestDiscoveryPreservesEmptyRowBoundaries`, `accumulo/discovery_test.go:185`). The shim must translate `""`/`None` to `SHOAL_RANGE_BOUND_UNBOUNDED` to preserve pinned test behavior. |
| SB-DATA-025 | `Range.get_start_key()` (`pysharkbite.cpp:342`) | `Range.StartKey()` (`accumulo/range.go:93`) | `shoal_range_create` → owned `shoal_range_result`; `shoal_range_get` → `shoal_range_view.start_key` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI now validates and copies the complete key, but the Python method and `Key` object still belong to the shim. |
| SB-DATA-026 | `Range.get_stop_key()` (`pysharkbite.cpp:343`) | `Range.EndKey()` (`accumulo/range.go:102`) | `shoal_range_create` → owned `shoal_range_result`; `shoal_range_get` → `shoal_range_view.end_key` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI uses `end_key`; the shim must preserve Sharkbite's published `stop` spelling. |
| SB-DATA-027 | `Range.start_key_inclusive()` (`pysharkbite.cpp:344`) | `Range.StartInclusive()` (`accumulo/range.go:110`) | `shoal_range_view.start_inclusive` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The copied value is available; the Python method shape remains shim work. |
| SB-DATA-028 | `Range.stop_key_inclusive()` (`pysharkbite.cpp:345`) | `Range.EndInclusive()` (`accumulo/range.go:115`) | `shoal_range_view.end_inclusive` | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The copied value is available; the shim must preserve Sharkbite's `stop` spelling. |
| SB-DATA-029 | `Range.inifinite_start_key()` — note the upstream typo (`pysharkbite.cpp:346`) | `Range.StartRow()` returns nil for unbounded (`accumulo/range.go:68`) | `shoal_range_view.has_start_key == 0` | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI exposes unboundedness explicitly; the shim must keep the misspelled published name and may add a corrected alias. |
| SB-DATA-030 | `Range.inifinite_stop_key()` — upstream typo (`pysharkbite.cpp:347`) | `Range.EndRow()` (`accumulo/range.go:81`) | `shoal_range_view.has_end_key == 0` | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The ABI exposes unboundedness explicitly; the shim must keep the misspelled published name. |
| SB-DATA-031 | `Range.after_end_key(key)` (`pysharkbite.cpp:348`) | `Range.AfterEndKey(key Key) bool` (`accumulo/range.go:193`) | `shoal_range_after_end_key` | `TestRangePredicatesCoverWholeRows`, `TestRangePredicatesOnKeyBoundsAndInfinities`, `TestRangePredicatesUseAccumuloTimestampOrder` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Mirrors `Range::afterEndKey`: false for an unbounded end, otherwise the end bound compared against the key, strictly for an inclusive bound and non-strictly for an exclusive one. The comparison uses the *effective* bounds ([`Range.KeyBounds`](#sec-8), `accumulo/range.go:143`), so a row bound covers every cell of the row rather than only the key that spells it; Sharkbite compares against its raw `start`/`stop` keys, which from Python are unadjusted because the binding passes `update=False` ([SB-UNSAFE-035](#sec-21)). |
| SB-DATA-032 | `Range.before_start_key(key)` (`pysharkbite.cpp:349`) | `Range.BeforeStartKey(key Key) bool` (`accumulo/range.go:210`) | `shoal_range_before_start_key` | `TestRangePredicatesCoverWholeRows`, `TestRangePredicatesOnKeyBoundsAndInfinities`, `TestRangePredicatesUseAccumuloTimestampOrder` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Mirrors `Range::beforeStartKey` with the same bound resolution as [SB-DATA-031](#sec-8). `Range.Contains` (`accumulo/range.go:228`) is the composition of the two, published because composing them is the only correct way to ask whether a key falls in a range. |
| SB-DATA-033 | `Range.__str__` via `operator<<` (`pysharkbite.cpp:350-354`) | `Range.String()` (`accumulo/range.go:241`) | `shoal_range_to_string` + owned `shoal_bytes_result` | `TestRangeStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Reproduces `operator<<` exactly, including its spacing quirks: `Range ` then `(-inf` or `[`/`(` and the bound key, a comma with no space, then `+inf) ` or the bound key with `] ` or `) `, so the text always ends in a space. A row bound renders as the first key of the row, which is what `Range(std::string)` stores, because `Key(row, cf="", cq="", cv="", ts=9223372036854775807L)` carries the maximum timestamp (`include/data/constructs/Key.h:43`). |
| SB-DATA-074 | `Range.__repr__` via `operator<<` (`pysharkbite.cpp:355-359`) | `Range.String()` (`accumulo/range.go:241`) | `shoal_range_to_string` + owned `shoal_bytes_result` | `TestRangeStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Identical body to [SB-DATA-033](#sec-8); both spellings are published and both map to the one Go method. |
| SB-DATA-034 | Reversed/invalid ranges | `NewKeyRange` rejects reversed bounds (`accumulo/range.go:40`) | Validated during scan (`capi/include/shoal.h`) | `TestKeyRangeRejectsReversedFields` (`accumulo/range_test.go:86`); `TestBatchScannerValidatesRanges` (`accumulo/scanner_test.go:855`) | Behavior mismatch | Sharkbite constructs reversed ranges without complaint and returns no rows. Shoal errors at construction. Divergence is safer but observable; shim must decide and record. |

### 8.5 `Mutation`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-035 | `Mutation(row: str)` (`pysharkbite.cpp:362`) | `accumulo.NewMutation(row []byte) (*Mutation, error)` (`accumulo/mutation.go:21`) | `shoal_mutation_create(shoal_bytes row, ...)` (`capi/include/shoal.h`) | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Shoal rejects an empty row (`accumulo/mutation.go:22`, `TestNewMutationRejectsEmptyRow`, `accumulo/mutation_test.go:27`); Sharkbite accepts it. Shim must map the new failure to `ClientException`, not `ValueError`. |
| SB-DATA-036 | `Mutation.put(cf="", cq="", cv="", timestamp=0, value="")` (`pysharkbite.cpp:363-364`) | `Mutation.Put(cf, cq, cv []byte, ts int64, value []byte)` (`accumulo/mutation.go:43`) | `shoal_mutation_put` (`capi/include/shoal.h`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Timestamp default differs in meaning: Sharkbite writes literal `0`, Shoal has `MutationLatestTimestamp` (`accumulo/mutation.go:11`) for server assignment. The shim must write literal `0` to preserve pinned behavior (`test/python/TestWrites.py:31-36` writes `1569786960`), and must not silently promote `0` to "latest". |
| SB-DATA-037 | `Mutation.put(cf, cq, cv, timestamp)` 4-arg overload (`pysharkbite.cpp:365-366`) | `Mutation.Put(..., value=nil)` (`accumulo/mutation.go:43`) | `shoal_mutation_put` with a zero-length value (`capi/include/shoal.h`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`) uses a valued put; `main()` in `capi/tests/lifecycle.c` puts a non-empty value | Behavior mismatch | audit #92: no test proves that the empty-value overload round-trips as an empty value, which `test/python/TestWrites.py:36` asserts against a live cluster. The Sharkbite overload's implicit value is a C++ default this document must pin, not assume. |
| SB-DATA-038 | `Mutation.put(cf, cq, cv)` 3-arg overload (`pysharkbite.cpp:367-368`) | `Mutation.Put(cf, cq, cv, 0, nil)` | `shoal_mutation_put` with timestamp 0 and empty value | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`) covers explicit arguments only | Behavior mismatch | audit #92: the overload's implicit timestamp (literal `0` versus server-assigned) is unproven on both layers, and choosing wrong silently changes cell versioning. Closes with a differential test asserting the stored timestamp. |
| SB-DATA-039 | `Mutation.put(cf, cq)` 2-arg overload (`pysharkbite.cpp:369-370`) | `Mutation.Put(cf, cq, nil, 0, nil)` | `shoal_mutation_put` with empty visibility and value | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`) covers explicit arguments only | Behavior mismatch | audit #92: same unproven defaults as SB-DATA-038, plus an implicit empty visibility. Reached with keywords by `sharkbite/__init__.py:87-95` and `examples/lambdaexample.py`. |
| SB-DATA-040 | Keyword-argument call form `put(cf=..., cq=..., cv=..., timestamp=..., value=...)` (`sharkbite/__init__.py:87-95`) | n/a (Go is positional) | n/a | — | Missing C ABI | Binding-layer obligation: the shim must accept exactly these keyword names. |
| SB-DATA-041 | `Mutation.putDelete(cf="", cq="", cv="", timestamp=0)` (`pysharkbite.cpp:371-372`) | `Mutation.Delete(cf, cq, cv []byte, ts int64)` (`accumulo/mutation.go:59`) | `shoal_mutation_delete` (`capi/include/shoal.h`) | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`); `main()` in `capi/tests/lifecycle.c` calls the timestamped delete and verifies the mutation update count | Behavior mismatch | The ABI path is now compiled, linked, and exercised. Python defaults, keyword names, and exception mapping remain shim work. |
| SB-DATA-042 | `Mutation.putDelete(cf, cq, cv)` 3-arg overload (`pysharkbite.cpp:373-374`) | `Mutation.DeleteLatest(cf, cq, cv)` (`accumulo/mutation.go:67`) | `shoal_mutation_delete_latest` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` (size reaches 2 after put+delete_latest) | Behavior mismatch | Sharkbite's 3-arg overload writes timestamp `0`; Shoal's `DeleteLatest` requests a **server-assigned** timestamp. Not equivalent. The shim must call `Delete(..., 0)`. |
| SB-DATA-043 | Server-assigned timestamps | `Mutation.PutLatest` (`accumulo/mutation.go:52`), `MutationLatestTimestamp` (`accumulo/mutation.go:11`) | `shoal_mutation_put_latest` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: no Sharkbite Python equivalent; Shoal superset. Exposing it in the shim is optional and additive. |
| SB-DATA-044 | Mutation has no size accessor in Python | `Mutation.Size() int` (`accumulo/mutation.go:38`) | `shoal_mutation_size` (`capi/include/shoal.h`) | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal superset; `BatchWriter.size()` is the Sharkbite analogue ([SB-WRITE-008](#sec-10)). |
| SB-DATA-045 | Mutation row accessor | `Mutation.Row() []byte` returns a copy (`accumulo/mutation.go:33`) | — | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`) | Not required (rationale required) | Rationale: Shoal superset. |
| SB-DATA-046 | Mutation reuse after `addMutation` (Sharkbite enqueues the same `shared_ptr`; the object stays aliased by the writer) | `BatchWriter.Add` encodes and snapshots (`accumulo/batch_writer.go:268`) | "BatchWriter add snapshots the mutation, so callers may immediately reuse or free it" (`capi/README.md`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | audit #92: not equivalent. Sharkbite aliases the mutation, so mutating it after `addMutation` and before the writer drains can change what is written; Shoal snapshots, so post-add mutation is inert. Shoal is safer, but a program that relied on late edits changes behavior. See the corrected [SB-KEEP-023](#sec-22), which proves only reference discard/rebind. |

### 8.6 `Authorizations`

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-047 | `Authorizations()` (`pysharkbite.cpp:454`) | `NewAuthorizations(labels ...[]byte) *Authorizations` (`accumulo/authorizations.go:34`); `ScannerOptions.Authorizations [][]byte` (`accumulo/scanner.go:103`) takes `Authorizations.List()` | `shoal_scanner_config.authorizations` + `authorization_count` (`capi/include/shoal_types.h`) | `TestAuthorizationsIsASortedDeduplicatedSet`, `TestAuthorizationsFeedTheScanPath`, `TestAuthorizationsEqualCloneAndNilReceiver` (`accumulo/value_types_test.go`) | Behavior mismatch | The set is now a first-class type rather than a bare slice: `NewAuthorizations()` with no labels is the empty set the pinned tests construct, and it is accepted by scanners and writers ([SB-KEEP-015](#sec-22)). A nil `*Authorizations` behaves as the empty set, which is what [SB-KEEP-019](#sec-22) needs from the shim. The scan configuration still crosses the ABI as a list of byte strings, so the Python object semantics remain shim work. |
| SB-DATA-048 | `Authorizations(list[str])` (`pysharkbite.cpp:455`) | `NewAuthorizations(labels ...[]byte)` (`accumulo/authorizations.go:34`) and `NewAuthorizationStrings(labels ...string)` (`accumulo/authorizations.go:44`) | as above | `TestAuthorizationsIsASortedDeduplicatedSet`, `TestAuthorizationsHoldsArbitraryBytes` (`accumulo/value_types_test.go`) | Behavior mismatch | The list form collapses duplicates and sorts, matching insertion into `std::set`; the byte-slice spelling is the primary one because a label is bytes. |
| SB-DATA-049 | `Authorizations(str)` single-auth convenience (`pysharkbite.cpp:456-460`) | `NewAuthorizationStrings(labels ...string)` (`accumulo/authorizations.go:44`) with one label | as above | `TestAuthorizationsContainsComparesBytesExactly` (`accumulo/value_types_test.go`) | Behavior mismatch | The single-label convenience is the variadic form with one argument; Sharkbite builds a one-element vector and delegates to the list constructor. |
| SB-DATA-050 | `Authorizations.addAuthorization(auth)` (`pysharkbite.cpp:461`) | `Authorizations.Add(label []byte) bool` (`accumulo/authorizations.go:54`) | — | `TestAuthorizationsIsASortedDeduplicatedSet`, `TestAuthorizationsCopiesLabels` (`accumulo/value_types_test.go`) | Behavior mismatch | Set insertion: adding a label the set already holds changes nothing, and the return value reports whether it did, which the C++ `void addAuthorization` cannot. `Remove` (`accumulo/authorizations.go:74`) has no Sharkbite equivalent and cannot break a program that never calls it. |
| SB-DATA-051 | `Authorizations.contains(auth) -> bool` (`pysharkbite.cpp:462`) | `Authorizations.Contains(label []byte) bool` (`accumulo/authorizations.go:87`) | immutable `shoal_authorizations` + `shoal_authorizations_contains` | `TestAuthorizationsContainsComparesBytesExactly` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact byte comparison over the set, like `std::find` over `authStrings`. Labels are bytes, not text, so a non-UTF-8 label is expressible and comparable. |
| SB-DATA-052 | `Authorizations.get_authorizations() -> iterable[str]` (`pysharkbite.cpp:463`) | `Authorizations.List() [][]byte` (`accumulo/authorizations.go:97`) and `Authorizations.Strings() []string` (`accumulo/authorizations.go:110`) | `shoal_authorizations_list` + owned `shoal_bytes_list_result` | `TestAuthorizationsIsASortedDeduplicatedSet`, `TestAuthorizationsCopiesLabels`, `TestAuthorizationsHoldsArbitraryBytes` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Returns the labels sorted, which is the order `get_authorizations` produces because Sharkbite stores them in a `std::set`. Both spellings return copies, so a caller cannot reach into the set through the result. |
| SB-DATA-053 | `Authorizations.__str__` (`pysharkbite.cpp:464-473`) | `Authorizations.String()` (`accumulo/authorizations.go:163`) | — | `TestAuthorizationsStringMatchesSharkbiteAndSurvivesEmpty` (`accumulo/value_types_test.go`) | Not required (rationale required) | Rationale: `__str__` is a Python object contract, not a client capability. Recorded here because the Go form is also the safe shim for [SB-UNSAFE-001](#sec-21): `[ a, b ]` for a populated set and `[ ]` for the empty set the pinned bindings crash on. |
| SB-DATA-075 | `Authorizations.__repr__` (`pysharkbite.cpp:474-484`) | `Authorizations.String()` (`accumulo/authorizations.go:163`) | — | `TestAuthorizationsStringMatchesSharkbiteAndSurvivesEmpty` (`accumulo/value_types_test.go`) | Not required (rationale required) | Rationale: identical body to [SB-DATA-053](#sec-8); same Go method and same shim. |
| SB-DATA-054 | Empty `Authorizations()` accepted by scanners and writers (`test/python/TestWrites.py:23,29`) | `Authorizations.Empty()` (`accumulo/authorizations.go:131`); `ScannerOptions.Authorizations` nil (`accumulo/scanner.go:103`) | `authorization_count = 0` accepted by scanner-config validation (`capi/include/shoal_types.h`) | `TestAuthorizationsFeedTheScanPath`, `TestAuthorizationsEqualCloneAndNilReceiver` (`accumulo/value_types_test.go`) | Behavior mismatch | An empty set is legal for scanning and writing ([SB-KEEP-015](#sec-22)); `List()` on it returns nil, which is the same thing the scan path already accepts. |

### 8.7 Iterator configuration objects

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-DATA-055 | `IterInfo(name, class, priority)` (`pysharkbite.cpp:86`) | `accumulo.NewIteratorSetting(name, className string, priority int32, options map[string]string)` (`accumulo/scanner.go:71`) | `shoal_iterator_setting` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | Shoal requires an options map argument and validates name/class/priority (`accumulo/scanner.go:394`); Sharkbite has no options on `IterInfo`. Shim must default options to `{}`. |
| SB-DATA-056 | `IterInfo(script, iteratorName, priority, type="Python")` (`pysharkbite.cpp:87-88`) | — | — | — | Missing Go | Script-carrying iterator; requires server-side Python iterator support ([SB-SCAN-016](#sec-9)). |
| SB-DATA-057 | `IterInfo.getPriority()` (`pysharkbite.cpp:89`) | `IteratorSetting.Priority()` (`accumulo/scanner.go:95`) | `shoal_iterator_setting_create` → owned result; `shoal_iterator_setting_view.priority` | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The copied priority is readable through a stable view; the Python accessor remains shim work. |
| SB-DATA-069 | `IterInfo.priority()` alias (`pysharkbite.cpp:90`) | `IteratorSetting.Priority()` (`accumulo/scanner.go:95`) | `shoal_iterator_setting_create` → owned result; `shoal_iterator_setting_view.priority` | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-DATA-057](#sec-8); the shim must register this independent alias. |
| SB-DATA-058 | `IterInfo.getName()` (`pysharkbite.cpp:91`) | `IteratorSetting.Name()` (`accumulo/scanner.go:89`) | `shoal_iterator_setting_view.name` borrowed from an owned copied result | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The copied name is readable through a stable view; the Python accessor remains shim work. |
| SB-DATA-070 | `IterInfo.name()` alias (`pysharkbite.cpp:92`) | `IteratorSetting.Name()` (`accumulo/scanner.go:89`) | `shoal_iterator_setting_view.name` borrowed from an owned copied result | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-DATA-058](#sec-8); the shim must register this independent alias. |
| SB-DATA-059 | `IterInfo.getClass()` (`pysharkbite.cpp:94`) | `IteratorSetting.ClassName()` (`accumulo/scanner.go:92`) | `shoal_iterator_setting_view.class_name` borrowed from an owned copied result | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | The copied class is readable; the reserved-word `class` alias remains a Python shim concern. |
| SB-DATA-071 | `IterInfo.class()` alias (`pysharkbite.cpp:93`) | `IteratorSetting.ClassName()` (`accumulo/scanner.go:92`) | `shoal_iterator_setting_view.class_name` borrowed from an owned copied result | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`); `TestOwnedDataDescriptorConstructionIsConcurrent` (`cmd/shoal-capi/data_descriptor_export_test.go`); `main()` in `capi/tests/lifecycle.c`; `main()` in `capi/tests/header_cpp_test.cpp` | Behavior mismatch | Second published spelling of [SB-DATA-059](#sec-8); the shim must preserve the awkward `getattr(obj, "class")()` path. |
| SB-DATA-060 | Iterator options map | `IteratorSetting.Options()` returns a defensive copy (`accumulo/scanner.go:98`) | `shoal_iterator_option` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Not required (rationale required) | Rationale: Shoal superset; Sharkbite `IterInfo` carries no options. |
| SB-DATA-061 | `PythonIterator(name, script, priority)` (`pysharkbite.cpp:97`) | — | — | — | Missing Go | See [SB-SCAN-016](#sec-9). |
| SB-DATA-062 | `PythonIterator(name, priority)` (`pysharkbite.cpp:98`) | — | — | — | Missing Go | Two-argument form used by `test/python/TestIterator.py:63` and `examples/lambdaexample.py`. |
| SB-DATA-063 | `PythonIterator.onNext(lambda_source) -> self` (`pysharkbite.cpp:103`) | — | — | — | Missing Go | Takes a **Python lambda as a source string**, evaluated server-side. Chainable (returns the iterator). |
| SB-DATA-064 | `PythonIterator.getPriority()` (`pysharkbite.cpp:99`) | — | — | — | Missing Go | Priority accessor on the Python-iterator descriptor. |
| SB-DATA-065 | `PythonIterator.priority()` alias (`pysharkbite.cpp:100`) | — | — | — | Missing Go | Second published spelling of SB-DATA-064; both must exist. |
| SB-DATA-066 | `PythonIterator.getName()` (`pysharkbite.cpp:101`) | — | — | — | Missing Go | |
| SB-DATA-067 | `PythonIterator.name()` alias (`pysharkbite.cpp:102`) | — | — | — | Missing Go | |
| SB-DATA-068 | `PythonIterator.getClass()` (`pysharkbite.cpp:104`) | — | — | — | Missing Go | Returns the server-side class name Sharkbite registers for Python iterators. Unlike `IterInfo` ([SB-DATA-059](#sec-8)) there is no reserved-word `class` alias on this type. |

<a id="sec-9"></a>

## 9. Matrix: scanners and result iteration (`SB-SCAN`)

Sharkbite exposes exactly one scanner type to Python — `scanners::BatchScanner`
bound as `BatchScanner` (`pysharkbite.cpp:434`) — returned by
`AccumuloTableOperations.createScanner(auths, threads)`, which calls
`createSharedScanner` (`include/interconnect/python/PythonStructures.h:172-175`).

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-SCAN-001 | `tableOps.createScanner(auths, threads) -> BatchScanner` (`pysharkbite.cpp:248`, `PythonStructures.h:172`) | `Connector.NewScanner(table, ScannerOptions)` (`accumulo/scanner.go:136`), `Connector.NewBatchScanner` (`accumulo/batch_scanner.go:47`) | `shoal_connector_create_scanner`, `shoal_connector_create_batch_scanner` (`capi/include/shoal.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`); `TestBatchScannerSplitsRangeAcrossTablets` (`accumulo/scanner_test.go:456`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite's `threads` argument becomes Shoal's `ScannerOptions.Parallelism` (`accumulo/scanner.go:107`). Sharkbite creates a scanner without a range and configures it afterwards; Shoal freezes options at construction. Shim must defer construction to first `getResultSet()`. Passing `None` auths must raise `ClientException` (`test/python/TestBadOperations.py:75-79`). |
| SB-SCAN-002 | `BatchScanner.addRange(Range)` (`pysharkbite.cpp:451`) | `Scanner.Scan(ctx, *Range)` / `BatchScanner.Scan(ctx, []*Range)` (`accumulo/scanner.go:168`, `accumulo/batch_scanner.go:58`) | `shoal_scanner_scan(range)`, `shoal_batch_scanner_scan(ranges, range_count)` (`capi/include/shoal.h`) | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`) | Behavior mismatch | Sharkbite accumulates ranges on the scanner; Shoal passes them per call. Also: calling `addRange` twice and re-reading `getResultSet()` in the pinned tests **replaces** the result set (`test/python/TestRanges.py:64-86`); the shim must reproduce accumulate-then-scan semantics exactly, including the "second `getResultSet()` re-executes" behavior. |
| SB-SCAN-003 | `BatchScanner.withRange(Range) -> self` fluent form (`pysharkbite.cpp:444-446`) | — (compose in Go) | — | `test/python/TestWithFx.py:50,63` | Missing C ABI | Pure binding-layer sugar over SB-SCAN-002; must return `self`. |
| SB-SCAN-004 | `BatchScanner.getResultSet() -> Results` (`pysharkbite.cpp:435`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scanner_stream`, `shoal_batch_scanner_stream`, `shoal_scan_cursor_next`, `shoal_scan_cursor_close`, `shoal_scan_cursor_free` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | The owned ABI cursor preserves bounded streaming for single and batch scanners. Each pull returns an independently owned result with at most the requested entries; close/free release and join the live stream. |
| SB-SCAN-005 | `BatchScanner.fetchColumn(cf, cq)` (`pysharkbite.cpp:436`; C++ `fetchColumn(col, colqual="")`, `include/scanner/Source.h:85`) | `ScannerOptions.Columns` with `NewColumnFamily` / `NewColumn` (`accumulo/scanner.go:104,43,48`) | `shoal_scanner_config.columns` + `shoal_column` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | The C++ default `colqual=""` is **not** declared to pybind11, so the pinned `fetchColumn(cf)` one-argument call raises `TypeError`. See [§21](#sec-21): the shim must accept one or two arguments. |
| SB-SCAN-006 | `BatchScanner.addIterator(IterInfo)` (`pysharkbite.cpp:448`) | `ScannerOptions.Iterators []IteratorSetting` (`accumulo/scanner.go:105`) | `shoal_scanner_config.iterators` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | Configuration timing only (post-construction vs construction-time); capability present on both layers. No test exercises a real server-side iterator end-to-end — see [§24](#sec-24). |
| SB-SCAN-007 | `BatchScanner.addIterator(PythonIterator)` (`pysharkbite.cpp:449`) | — | — | `test/python/TestIterator.py:63-65,84-86` | Missing Go | Overload dispatch on argument type. See SB-SCAN-016. |
| SB-SCAN-008 | `BatchScanner.setOption(ScannerOptions)` (`pysharkbite.cpp:437`) | — | — | `PYTHONREADME.md:55` | Missing Go | Neither option value has a Shoal equivalent (SB-SCAN-014/015). |
| SB-SCAN-009 | `BatchScanner.removeOption(ScannerOptions)` (`pysharkbite.cpp:447`) | — | — | — | Missing Go | |
| SB-SCAN-010 | `BatchScanner.close()` (`pysharkbite.cpp:450`) | Scanner has no `Close`; server-side scan sessions are closed inside `Scan` (`accumulo/scanner.go:254`, `scannerCloseTimeout`, `accumulo/scanner.go:15`) | `shoal_scanner_close` / `shoal_scanner_free` (`capi/include/shoal.h`) | `TestScannerCancellationStillClosesServerScan` (`accumulo/scanner_test.go:396`); `TestOwnedScannerCloseIsConcurrentAndIdempotent` (`cmd/shoal-capi/state_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Go has no scanner-level close because scans are self-contained; the ABI does. Shim maps `close()` to `shoal_scanner_close`. |
| SB-SCAN-011 | `BatchScanner.__enter__` returning the scanner (`pysharkbite.cpp:438-440`) | n/a | n/a | `test/python/TestWithFx.py:50,63` | Missing C ABI | Returns `self` so `with ... as scanner` binds the scanner, not `None`. |
| SB-SCAN-028 | `BatchScanner.__exit__(exc_type, exc_value, exc_traceback)` (`pysharkbite.cpp:441-443`) | n/a | `shoal_scanner_close` (`capi/include/shoal.h`) | `test/python/TestWithFx.py:50,63`; `main()` in `capi/tests/lifecycle.c` | Missing C ABI | Calls `close()` and returns `None`, so exceptions propagate. A shim that returns a truthy value would swallow them. |
| SB-SCAN-012 | Scanner `threads` argument (parallel tablet fan-out) (`test/python/TestWrites.py:57` uses `2`) | `ScannerOptions.Parallelism` (`accumulo/scanner.go:107`) — bounds concurrent tablet scans in `BatchScanner` only, and is ignored by `Scanner` | `shoal_scanner_config.parallelism` (`capi/include/shoal_types.h`) | `TestBatchScannerBoundsParallelismAndPreservesOrder` (`accumulo/scanner_test.go:559`) proves Shoal's own bound; `main()` in `capi/tests/lifecycle.c` proves the field is accepted | Behavior mismatch | audit #92: the `threads → Parallelism` mapping is an assumption. Sharkbite's argument sizes the scanner's internal worker pool for one `BatchScanner` regardless of tablet count; Shoal's field is ignored entirely by `Scanner` (`accumulo/scanner.go:107`). No test relates the two. |
| SB-SCAN-013 | Multi-scan RPC batching (implicit in Sharkbite) | `ScannerOptions.UseMultiScan` (`accumulo/scanner.go:110`) | `shoal_scanner_config.use_multi_scan` (`capi/include/shoal_types.h`) | `TestBatchScannerGroupsMultiScansByServer` (`accumulo/scanner_test.go:632`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal-only tuning knob; result order becomes server-defined when enabled, so the shim must leave it off by default to preserve Sharkbite ordering. |
| SB-SCAN-014 | `ScannerOptions.HedgedReads` enum value (`pysharkbite.cpp:400`, `ScannerOptions::ENABLE_HEDGED_READS = 0x1`) | — | — | `PYTHONREADME.md:31-63` (documented beta feature) | Missing Go | Concurrent RFile + RPC scan racing. Shoal has RFile reading (`internal/rfile`) and RPC scanning (`internal/scanclient`) but no hedging layer. Commented out in every pinned test (`test/python/TestHedgedReads.py:55`), so a divergence request is plausible — but it is a published, documented option and cannot be silently dropped. |
| SB-SCAN-015 | `ScannerOptions.RFileScanOnly` enum value (`pysharkbite.cpp:401`, `ENABLE_RFILE_SCANNER`) | — (`internal/rfile`, `internal/offlinecompact` are internal) | — | — | Missing Go | Direct RFile scanning bypassing tablet servers. |
| SB-SCAN-016 | Server-side **execution** of Python iterators, as documented in `PYTHONREADME.md:65-116` and exercised by `test/python/TestIterator.py` | — (`internal/iterrt` is a Go iterator runtime, not a Python one) | — | Pinned-source evidence that the C++ execution path does not function: no translation unit includes `include/jni/PythonBindings.h`; `option(PYTHON_ITERATOR_SUPPORT ... OFF)` (`CMakeLists.txt:37`); `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35`); its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`); `include/jni/IteratorExecutor.h:28-33` is empty | Not required (rationale required) | **No functioning capability is claimed for the pinned tree.** What ships and is bound is the *client-side descriptor* — `PythonIterInfo`, bound as Python `PythonIterator` (`pysharkbite.cpp:96-104`) and attachable to a scan ([SB-DATA-056](#sec-8), [SB-DATA-061](#sec-8), [SB-SCAN-007](#sec-9)) — and those rows keep their obligations. The C++ server-side runtime that would execute the script is dead code, enumerated registration by registration in [§19.4](#sec-19-4). Rationale for `Not required`: Shoal cannot be required to reproduce behavior the pinned reference does not perform, and no test in the pinned tree can demonstrate it end to end. If the project decides to ship server-side execution, that is **new capability**, tracked by [SB-DIV-007](#sec-26) and release-blocking under [§2.2](#sec-2) only once approved as in-scope — it is not a silent drop, and the prior wording that called this "the single largest functional gap" is withdrawn as unsupported. |
| SB-SCAN-017 | `Results.__iter__` (`pysharkbite.cpp:383-385`) | `[]KeyValue` (natively iterable in Go) | `shoal_scan_result_count` + `shoal_scan_result_get` (`capi/include/shoal.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`); `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | Sharkbite's `__iter__` calls `begin()`, which **restarts** the scan. Shim must reproduce restart-on-iter. |
| SB-SCAN-018 | `Results.__next__` -> `KeyValue`, raising `StopIteration` at end (`pysharkbite.cpp:396`, `include/scanner/constructs/Results.h:280-288`) | slice iteration | `shoal_scan_result_get` returns `SHOAL_STATUS_INVALID_ARGUMENT` out of range (`main()` in `capi/tests/result_bridge.c`) | `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | Sharkbite throws `pybind11::stop_iteration` from C++. Shim must convert index exhaustion into `StopIteration`. |
| SB-SCAN-019 | `Results.__aiter__` (`pysharkbite.cpp:380-382`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scanner_stream`, `shoal_batch_scanner_stream`, `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | A binding implements async iteration by running the blocking bounded pull off-thread. Deadline, one-shot cancellation, scanner close, and connector close remain active for the cursor lifetime. |
| SB-SCAN-020 | `Results.__anext__` returning an `asyncio` future (`pysharkbite.cpp:386-395`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | Bounded pulls return owned chunks and distinguish clean exhaustion from stable terminal errors. No boundary row is consumed or dropped. |
| SB-SCAN-021 | `Results.__await__` (`pysharkbite.cpp:377-379`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scanner_stream`, `shoal_batch_scanner_stream` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | Each stream call creates an independent cursor without priming it, allowing a shim to restart iteration safely instead of reproducing Sharkbite row loss. |
| SB-SCAN-022 | `asyncio.run` over multiple concurrent scans (`test/python/TestAsyncOperations.py:180-188`) | `context.Context` per call; goroutine-safe scanners | Scanner handles support concurrent scan calls (`capi/README.md`) | `TestOwnedScannerCloseCancelsAndJoinsActiveCalls` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | Sharkbite's async is cooperative-only; Shoal is genuinely concurrent. Must be validated by a live-cluster async conformance test. |
| SB-SCAN-023 | Scan batch size | `ScannerOptions.BatchSize` (`accumulo/scanner.go:102`), default 1024 (`accumulo/scanner.go:14`) | `shoal_scanner_config.batch_size` (`capi/include/shoal_types.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`) | Not required (rationale required) | Rationale: not exposed by Sharkbite Python (`AccumuloIterator` chunking is client-side only, `sharkbite/__init__.py:190-194`). Shoal superset. |
| SB-SCAN-024 | Client-side chunking `AccumuloIterator(scanner, chunkSize)` (`sharkbite/__init__.py:166-194`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | The max_entries argument provides explicit client-side chunking without materializing the scan or dropping an entry at chunk boundaries. |
| SB-SCAN-025 | Scan cleanup failures | `CleanupError{ScanID, Err}` (`accumulo/scanner.go:116`) | `SHOAL_STATUS_CLEANUP_FAILED` (`capi/include/shoal_types.h`) | `TestScannerRangeAndCleanupErrors` (`accumulo/scanner_test.go:365`); `TestBatchScannerContinuesAfterCleanupError` (`accumulo/scanner_test.go:491`) | Not required (rationale required) | Rationale: Sharkbite has no equivalent (it silently ignores close failures). Shoal superset; shim should surface it as a warning, not an exception, to preserve behavior. |
| SB-SCAN-026 | Tablet relocation during a scan (`test/vandv/testScanLocationMove.h`) | `Scanner` retries `NotServingTablet` **once**, then invalidates its locator cache and fails (`accumulo/scanner.go:227,350`) | Implicit — no separate ABI surface | `TestScannerRetriesNotServingAssignmentOnce` (`accumulo/scanner_test.go:294`) | Behavior mismatch | audit #92: retry depth differs. Sharkbite re-locates and retries in a loop driven by `NotServingException` until the scan completes; Shoal retries exactly once. Under a rolling split or migration storm Shoal surfaces an error where Sharkbite eventually succeeds. Closes only with a live-cluster relocation test ([§24](#sec-24)) plus a decision on retry depth. |
| SB-SCAN-027 | Isolation, `waitForWrites`, sampler, execution hints, busy timeout | — (present in `internal/scanclient.StartRequest`, never populated by `accumulo`) | — | — | Not required (rationale required) | Rationale: not exposed by Sharkbite either. Listed to record that the internal plumbing exists if a future row needs it. |

<a id="sec-9-2"></a>

### 9.2 PyTorch dataset surface (`SB-TORCH`)

`sharkbite/torch.py` is shipped inside the `sharkbite` distribution
(`setup.py:50` packages `sharkbite`, which contains `torch.py`), so
`import sharkbite.torch` is part of the published import contract. Every row is
`Missing C ABI`: the surface is pure Python over a scanner, so it unblocks only
after scanning is reachable from Python, and two of its members are defective
today ([SB-UNSAFE-021](#sec-21), [SB-UNSAFE-022](#sec-21)).

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-TORCH-001 | `sharkbite.torch` module import, requiring `torch.utils.data` at import time (`sharkbite/torch.py:1`) | — | — | `examples/torchexample.py` | Missing C ABI | Importing `sharkbite.torch` fails with `ModuleNotFoundError` when PyTorch is absent; the shim must keep it an optional extra with the same module path. |
| SB-TORCH-002 | `AccumuloCluster(instance, zookeepers, username, password, table=None, auths=None)`, subclassing `AccumuloBase` and `IterableDataset` (`sharkbite/torch.py:5-7`) | — | — | — | Missing C ABI | Adds no behavior over `AccumuloBase`; the value is the `IterableDataset` base class, which PyTorch's `DataLoader` type-checks. |
| SB-TORCH-003 | `AccumuloDataset(instance, zookeepers, username, password, table, auths, start_key_string, end_key_string, user_lambda=None)` (`sharkbite/torch.py:14-22`) | — | — | `examples/torchexample.py` | Missing C ABI | Constructor side effects are contractual: it builds `Range(start, True, end, False)`, calls `createScanner(auths, self._threads)`, and calls `addRange` immediately, so the scan range is fixed at construction and `_threads` is captured then ([SB-UNSAFE-023](#sec-21)). |
| SB-TORCH-004 | `AccumuloDataset.coerce(key)` (`sharkbite/torch.py:24-36`) | — | — | `examples/torchexample.py` | Missing C ABI | Two paths: with `user_lambda`, call it on the `KeyValue`; without, attempt an `int(...)` conversion. Both swallow every exception and return `0`. Defective — see [SB-UNSAFE-021](#sec-21). |
| SB-TORCH-005 | `AccumuloDataset.__iter__` (`sharkbite/torch.py:56-62`) | — | — | `examples/torchexample.py` | Missing C ABI | Lazily calls `getResultSet()` once and caches both the result set and its iterator, so a second `iter()` on the same dataset resumes rather than restarts — the opposite of `Results.__iter__` ([SB-SCAN-017](#sec-9)). |
| SB-TORCH-006 | `AccumuloDataset.__next__` (`sharkbite/torch.py:38-54`) | — | — | `examples/torchexample.py` | Missing C ABI | Raises `StopIteration` when never iterated; on exhaustion it closes the underlying scanner and clears its cached iterators, so the dataset cannot be re-iterated after one pass. A `coerce` result of `None` also terminates iteration. |
| SB-TORCH-007 | `AccumuloValueDataset(cluster, table, start_key_string, end_key_string)` (`sharkbite/torch.py:64-66`) | — | — | — | Missing C ABI | Constructor is broken in the pinned release ([SB-UNSAFE-022](#sec-21)); the intended contract is an `AccumuloDataset` whose `coerce` returns the cell value. |
| SB-TORCH-008 | `AccumuloValueDataset.coerce(key)` returning `key.getValue().get()` (`sharkbite/torch.py:68-69`) | — | — | — | Missing C ABI | Returns a decoded `str`, so it inherits the binary-safety defect of `Value.get()` ([SB-UNSAFE-002](#sec-21)). |
| SB-TORCH-009 | `torch.utils.data.DataLoader(dataset, num_workers=0)` compatibility (`examples/torchexample.py`) | — | — | `examples/torchexample.py` | Missing C ABI | The datasets must remain usable as `IterableDataset`s; `num_workers > 0` forks worker processes, which interacts with [SB-XCUT-018](#sec-20) fork safety. |

<a id="sec-9-3"></a>

### 9.3 pandas surface (`SB-PANDA`)

Enumerated because `pandashark` consumes the high-level `sharkbite` API
directly ([SB-PKG-013](#sec-5)). Every row is `Missing C ABI`.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-PANDA-001 | `pandashark.read_accumulo(scanner, columns=None, index_col=None, chunksize=1000) -> DataFrame` (`pandashark/__init__.py:64-98`) | — | — | `examples/dataframe.py:69` | Missing C ABI | Calls `scanner.select_column(column)` for every requested column ([SB-UNSAFE-007](#sec-21)), then `scanner.get(chunksize)` ([SB-UNSAFE-024](#sec-21)), builds a `DataFrameIterator`, and returns only the **first** frame. `index_col` is accepted and ignored. |
| SB-PANDA-002 | `@overload read_accumulo(connector, ranges, columns=None, index_col=None, chunksize=1000)` (`pandashark/__init__.py:51-63`) | — | — | — | Missing C ABI | Dead at runtime ([SB-UNSAFE-025](#sec-21)); documented here because the published signature is what users copy. |
| SB-PANDA-003 | `pandashark.read_accumulo_nex(iterator, columns=None, index_col=None, chunksize=1000) -> DataFrame` (`pandashark/__init__.py:100-111`) | — | — | — | Missing C ABI | Returns the next frame from an existing `DataFrameIterator`; this is the only supported way to page beyond the first frame. `columns`, `index_col`, and `chunksize` are accepted and ignored. |
| SB-PANDA-004 | `pandassharkbite.DataFrameIterator(iterator)` (`pandashark/pandassharkbite.py:36-45`) | — | — | `pandashark/__init__.py:95` | Missing C ABI | Seeds a **class-level** `_columns` set with `row`, `column`, `visibility`, `value`; because the set is shared across instances, columns accumulate process-wide. |
| SB-PANDA-005 | `DataFrameIterator.get_columns()` (`pandashark/pandassharkbite.py:51-52`) | — | — | `pandashark/__init__.py:96` | Missing C ABI | Returns the shared `_columns` set, not the columns actually present in the emitted frame. |
| SB-PANDA-006 | `DataFrameIterator.__iter__` (`pandashark/pandassharkbite.py:47-49`) | — | — | `examples/dataframe.py:69` | Missing C ABI | Delegates to the wrapped iterator's `__iter__` and returns `self`. Because `AccumuloIterator.__iter__` calls `Results.__iter__`, which **restarts** the server-side scan ([SB-SCAN-017](#sec-9)), re-iterating a `DataFrameIterator` silently re-reads from the beginning. |
| SB-PANDA-007 | `pandashark.AccumuloIterator` — a second, byte-identical copy of `sharkbite.AccumuloIterator` declared in `pandashark/pandassharkbite.py:3-9` | — | — | — | Missing C ABI | Class declaration and shared class-level attributes only; its members are enumerated as SB-PANDA-008…SB-PANDA-012. Two implementations of the same contract must stay in step, or the shim must collapse them while keeping both import paths working. |
| SB-PANDA-008 | `pandashark.AccumuloIterator(scanner)` — first constructor definition (`pandashark/pandassharkbite.py:11-13`) | — | — | — | Not required (rationale required) | Rationale: shadowed by the two-argument definition immediately below it and unreachable, exactly as in `sharkbite` ([SB-BASE-015](#sec-10-1), [SB-UNSAFE-009](#sec-21)). |
| SB-PANDA-009 | `pandashark.AccumuloIterator(scanner, chunkSize)` (`pandashark/pandassharkbite.py:15-18`) | — | — | `pandashark/__init__.py:95` | Missing C ABI | Calls `scanner.getResultSet()` eagerly in the constructor. `DataFrameIterator` wraps whatever this returns, so the scan starts before the first frame is requested. |
| SB-PANDA-010 | `pandashark.AccumuloIterator.__iter__` (`pandashark/pandassharkbite.py:20-22`) | — | — | — | Missing C ABI | Restarts the underlying scan, as [SB-BASE-017](#sec-10-1). |
| SB-PANDA-011 | `pandashark.AccumuloIterator.nextBatch()` (`pandashark/pandassharkbite.py:24-25`) | — | — | `pandashark/pandassharkbite.py:60-72` | Missing C ABI | Resets `_chunkCounter` only. This is the one caller of `nextBatch` anywhere in the pinned tree, and it is what makes the pandas paging loop non-terminating ([SB-UNSAFE-026](#sec-21)). |
| SB-PANDA-012 | `pandashark.AccumuloIterator.__next__` (`pandashark/pandassharkbite.py:27-34`) | — | — | — | Missing C ABI | Same chunk-boundary defect as [SB-BASE-019](#sec-10-1)/[SB-UNSAFE-008](#sec-21): the entry pulled on the limit call is discarded. Fixed by [SB-DIV-006](#sec-26). |
| SB-PANDA-013 | `DataFrameIterator.__next__` (`pandashark/pandassharkbite.py:60-72`) | — | — | `examples/dataframe.py:69-70` | Missing C ABI | Accumulates rows until the wrapped iterator raises `StopIteration`, then calls `nextBatch()` and returns a `DataFrame` built by the private `_make_dataframe` helper (`pandashark/pandassharkbite.py:54-59`): indexed by row, with `column` (`cf:cq`), `visibility`, `timestamp`, and `value` columns, `""` replaced by `NaN`, and all-`NaN` columns dropped. The emitted columns therefore differ from `get_columns()` ([SB-PANDA-005](#sec-9-3)), and the loop never terminates ([SB-UNSAFE-026](#sec-21)). `_make_dataframe` is private and gets no row of its own; its output shape is the contract recorded here. |
| SB-PANDA-014 | `pandashark.torch` module import (`pandashark/torch.py:1-3`) | — | — | — | Missing C ABI | A **byte-identical copy** of `sharkbite/torch.py` (verified by SHA-256) that imports `AccumuloBase` from `pandashark` rather than `sharkbite`. Both import paths are published, so the PyTorch surface exists twice and must stay in step. Requires `torch.utils.data` at import time, as [SB-TORCH-001](#sec-9-2). |
| SB-PANDA-015 | `pandashark.torch.AccumuloCluster(instance, zookeepers, username, password, table=None, auths=None)` (`pandashark/torch.py:5-7`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-002](#sec-9-2) reached through the `pandashark` package. |
| SB-PANDA-016 | `pandashark.torch.AccumuloDataset(instance, zookeepers, username, password, table, auths, start_key_string, end_key_string, user_lambda=None)` (`pandashark/torch.py:14-22`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-003](#sec-9-2), including the constructor side effects (range built, scanner created, range added). |
| SB-PANDA-017 | `pandashark.torch.AccumuloDataset.coerce(key)` (`pandashark/torch.py:24-36`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-004](#sec-9-2), carrying the same defect ([SB-UNSAFE-021](#sec-21)). |
| SB-PANDA-018 | `pandashark.torch.AccumuloDataset.__iter__` (`pandashark/torch.py:56-62`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-005](#sec-9-2); caches the result set, so re-iteration resumes rather than restarts. |
| SB-PANDA-019 | `pandashark.torch.AccumuloDataset.__next__` (`pandashark/torch.py:38-54`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-006](#sec-9-2); closes the scanner on exhaustion, so the dataset is single-pass. |
| SB-PANDA-020 | `pandashark.torch.AccumuloValueDataset(cluster, table, start_key_string, end_key_string)` (`pandashark/torch.py:64-66`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-007](#sec-9-2); broken by the same `super.__init__` defect ([SB-UNSAFE-022](#sec-21)). |
| SB-PANDA-021 | `pandashark.torch.AccumuloValueDataset.coerce(key)` (`pandashark/torch.py:68-69`) | — | — | — | Missing C ABI | Duplicate of [SB-TORCH-008](#sec-9-2); returns a decoded `str` and inherits the binary-safety defect of `Value.get()`. |

<a id="sec-10"></a>

## 10. Matrix: writers (`SB-WRITE`)

Sharkbite's Python `BatchWriter` is `writer::Sink<KeyValue>`
(`pysharkbite.cpp:486`), created by
`tableOps.createWriter(auths, threads)` → `createSharedWriter`
(`include/interconnect/python/PythonStructures.h:177-180`).

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-WRITE-001 | `tableOps.createWriter(auths, threads) -> BatchWriter` (`pysharkbite.cpp:249`) | `Connector.NewBatchWriter(ctx, table, BatchWriterOptions)` (`accumulo/batch_writer.go:225`) | `shoal_connector_create_batch_writer` (`capi/include/shoal.h`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite's `threads` maps to `MaxWriteThreads` (`accumulo/batch_writer.go:76`). Sharkbite takes authorizations at writer creation (they are unused for writes); Shoal's writer config has no auths field. Passing `None` auths must raise `ClientException` (`test/python/TestBadOperations.py:75-79`). |
| SB-WRITE-002 | `BatchWriter.addMutation(Mutation) -> bool` (`pysharkbite.cpp:488`) | `BatchWriter.Add(ctx, *Mutation) error` (`accumulo/batch_writer.go:268`) | `shoal_batch_writer_add(writer, mutation, timeout_ms, out_failure, out_error)` (`capi/include/shoal.h`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Return type differs: Sharkbite returns `bool` (enqueued), Shoal returns an error. The pinned tests ignore the return value, but `sharkbite/__init__.py` does too, so the shim should return `True` on success and raise on error. Empty mutations are rejected by Shoal (`main()` in `capi/tests/lifecycle.c`) and accepted by Sharkbite. |
| SB-WRITE-003 | `BatchWriter.flush(override)` — C++ default `override=false` is **not** exposed to Python (`pysharkbite.cpp:487`, `include/writer/Sink.h:62`) | `BatchWriter.Flush(ctx) error` (`accumulo/batch_writer.go:337`) | `shoal_batch_writer_flush` (`capi/include/shoal.h`) | `TestBatchWriterExplicitFlushResetsAutomaticDeadline` (`accumulo/batch_writer_test.go:329`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Because pybind11 does not inherit C++ defaults, `writer.flush()` raises `TypeError` in Sharkbite 1.2.0.3; only `flush(True)`/`flush(False)` work. The shim must accept zero arguments (and tolerate the legacy boolean). See [§21](#sec-21). |
| SB-WRITE-004 | `BatchWriter.close()` (`pysharkbite.cpp:495`, `Sink::close()` calls `flush(true)`) | `BatchWriter.Close(ctx) error` (`accumulo/batch_writer.go:356`) | `shoal_batch_writer_close` (`capi/include/shoal.h`) | `TestBatchWriterCloseLifecycle` (`accumulo/batch_writer_test.go:852`) proves the Go path submits a pending mutation on `Close` and is idempotent; `main()` in `capi/tests/lifecycle.c` calls `shoal_batch_writer_flush` **before** `close`, so it proves idempotence only | Behavior mismatch | audit #2: the Sharkbite contract is "close flushes" (`Sink::close()` calls `flush(true)`), and that is proven for Go but not across the ABI, where the only close test has nothing left to flush. Closes with a `lifecycle.c` case that adds a mutation and closes without flushing. |
| SB-WRITE-005 | `BatchWriter.__enter__` returning the writer (`pysharkbite.cpp:489-491`) | n/a | n/a | `test/python/TestSecurityOperations.py:55-61`; `test/python/TestWithFx.py:30-42` | Missing C ABI | Returns `self`. |
| SB-WRITE-023 | `BatchWriter.__exit__(exc_type, exc_value, exc_traceback)` (`pysharkbite.cpp:492-494`) | n/a | `shoal_batch_writer_close` (`capi/include/shoal.h`) | `test/python/TestWithFx.py:30-42`; `main()` in `capi/tests/lifecycle.c` | Missing C ABI | Calls `close()` — which flushes — and returns `None`, so a failed flush inside a `with` block raises. |
| SB-WRITE-006 | `BatchWriter.size()` — approximate queue depth (`pysharkbite.cpp:496`, `Sink::size()` → `sinkQueue.size_approx()`) | — (internal buffer accounting only, `accumulo/batch_writer.go:203-223`) | — | — | Missing Go | No exported buffered-size accessor. |
| SB-WRITE-007 | Write durability | `BatchWriterOptions.Durability` + `Durability{Default,Sync,Flush,Log,None}` (`accumulo/batch_writer.go:44-53,78`) | `shoal_durability` (`capi/include/shoal_types.h`) | `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Sharkbite exposes no durability control. Shoal superset. |
| SB-WRITE-008 | Writer memory bound | `BatchWriterOptions.MaxMemoryBytes` (default 50 MiB, `accumulo/batch_writer.go:17,57`) | `shoal_batch_writer_config.max_memory_bytes` (`capi/include/shoal_types.h`) | `TestBatchWriterMemoryBoundFlushesSynchronously` (`accumulo/batch_writer_test.go:256`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Sharkbite bounds by queue length (`Sink(uint16_t maxQueue)`), not bytes. The shim must map Sharkbite's `threads`-shaped constructor without exposing new knobs. |
| SB-WRITE-009 | Writer latency flush | `BatchWriterOptions.MaxLatency` (`accumulo/batch_writer.go:66`) | `max_latency_ms` (`capi/include/shoal_types.h`) | `TestBatchWriterAutomaticFlushesAtDeadline` (`accumulo/batch_writer_test.go:273`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal-only (issue [#53](https://github.com/phrocker/shoal-oss/issues/53) / PR [#55](https://github.com/phrocker/shoal-oss/pull/55)); Sharkbite flushes on queue pressure and close only. Defaults to disabled, preserving Sharkbite behavior. |
| SB-WRITE-010 | Per-mutation failures surfaced to the caller | `MutationRejectionError{Server, FailedExtents, ConstraintViolations, AuthorizationFailures}` (`accumulo/batch_writer.go:116`) | `shoal_write_failure` + `shoal_failed_extent_view`, `shoal_constraint_violation_view`, `shoal_authorization_failure_view` (`capi/include/shoal_types.h`) | `TestBatchWriterSurfacesAccumuloUpdateErrors` (`accumulo/batch_writer_test.go:942`); `TestFlattenWriteFailurePreservesStructuredDetails` (`cmd/shoal-capi/writer_export_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite drops server-side update errors on the floor; there is no Python-visible `MutationsRejectedException`. Shoal surfaces structured failures. This is a **safety-improving** divergence but changes control flow: code that never saw write failures will now see exceptions. Must be recorded and approved. |
| SB-WRITE-011 | Ambiguous commit ("may have partially committed") | `ErrBatchWriterFailed` (`accumulo/batch_writer.go:33`), sticky terminal state | `SHOAL_STATUS_AMBIGUOUS_WRITE` (18), `SHOAL_WRITE_FAILURE_AMBIGUOUS_COMMIT` (`capi/include/shoal_types.h`) | `TestBatchWriterDoesNotRetryAmbiguousApplyFailure` (`accumulo/batch_writer_test.go:1401`); `TestBatchWriterAmbiguousAutomaticFlushErrorIsSticky` (`accumulo/batch_writer_test.go:473`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Sharkbite has no concept of ambiguity; it retries blindly. Shoal's behavior is strictly safer and must be preserved through the shim rather than hidden. |
| SB-WRITE-012 | Retry semantics | `MaxRetries` (default 3) + `RetryBackoff` (default 100 ms) (`accumulo/batch_writer.go:20-21,70,74`), retries only provably safe failures (`accumulo/batch_writer.go:743`) | `max_retries`, `retry_backoff_ms` (`capi/include/shoal_types.h`); `SHOAL_STATUS_RETRY_EXHAUSTED` (16) | `TestBatchWriterRetryOptionsValidation` (`accumulo/batch_writer_test.go:526`); `TestBatchWriterExplicitRetryExhaustionIsSticky` (`accumulo/batch_writer_test.go:1360`); `TestBatchWriterRetryBackoffHonorsCancellation` (`accumulo/batch_writer_test.go:1434`) | Behavior mismatch | Sharkbite retries at the transport layer only (protocol-version fallback, `include/interconnect/transport/BaseTransport.h:57`) and reshuffles servers up to the server-list length (`TransportPool.h:222`). Semantics are not equivalent; document the new failure modes. |
| SB-WRITE-013 | Writer cleanup failure | `BatchWriterCleanupError` (`accumulo/batch_writer.go:135`) | `shoal_cleanup_failure_view` (`capi/include/shoal_types.h`) | `TestBatchWriterCloseCleanupErrorIsStable` (`accumulo/batch_writer_test.go:1479`); `main()` in `capi/tests/result_bridge.c` | Not required (rationale required) | Rationale: Shoal superset. |
| SB-WRITE-014 | Concurrent `addMutation` from multiple Python threads | `BatchWriter` is mutex-guarded (`accumulo/batch_writer.go:380-394`) | Writer handles are close-safe and join in-flight calls (`capi/README.md`) | `TestBatchWriterConcurrentAdds` (`accumulo/batch_writer_test.go:595`); `TestOwnedBatchWriterCloseCancelsAndJoinsActiveCalls` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | audit #92: Shoal's guarantee is proven, Sharkbite's is undefined — there is no documented statement that one `Sink` may be driven concurrently by user threads, and the locks found protect internal producer/consumer plumbing. A superset, not an equivalence, and the Python shim must additionally release the GIL ([SB-XCUT-017](#sec-20)) for the guarantee to be observable. |
| SB-WRITE-015 | Writer after connector close | `ErrConnectorClosed` (`accumulo/errors.go:65`) | `SHOAL_STATUS_CLOSED` (6) (`capi/include/shoal_types.h`) | `TestOwnedBatchWriterRejectsOperationsAfterConnectorClose` (`cmd/shoal-capi/state_test.go`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: undefined in Sharkbite (use-after-free). Shoal's defined error is required, not optional. |
| SB-WRITE-016 | `AccumuloWriter.put(row, cf, cq, cv=None, timestamp=0, value=None)` high-level helper (`sharkbite/__init__.py:73-95`) | Compose `NewMutation` + `Put` + `Add` | `shoal_accumulo_writer_put` (`capi/include/shoal.h`) | `TestOwnedAccumuloWriterIsLazyAndFlushesOnRowChangeAndClose`, `TestOwnedAccumuloWriterTimestampSubstitution` (`cmd/shoal-capi/buffered_writer_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Revision 28 adds an owned lazy writer that copies binary inputs, buffers adjacent updates by row, flushes on row change, and replaces a zero put timestamp with current Unix milliseconds. |
| SB-WRITE-017 | `AccumuloWriter.putDelete(row, cf, cq, cv="", timestamp=0)` (`sharkbite/__init__.py:97-111`) | Compose `Mutation.Delete` | `shoal_accumulo_writer_put_delete` (`capi/include/shoal.h`) | `TestOwnedAccumuloWriterIsLazyAndFlushesOnRowChangeAndClose` (`cmd/shoal-capi/buffered_writer_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | The ABI preserves the pinned asymmetry: delete timestamp zero is passed through rather than replaced with current time. |
| SB-WRITE-018 | `AccumuloWriter.delete(key: Key)` (`sharkbite/__init__.py:113-115`) | Compose | `shoal_accumulo_writer_delete` (`capi/include/shoal.h`) | `TestOwnedAccumuloWriterIsLazyAndFlushesOnRowChangeAndClose` (`cmd/shoal-capi/buffered_writer_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | The copied key's explicit timestamp is used unchanged, including the `INT64_MAX` default from SB-DATA-006. |
| SB-WRITE-019 | `AccumuloWriter.close()` flushing the pending mutation (`sharkbite/__init__.py:117-126`) | `BatchWriter.Close` | `shoal_accumulo_writer_close`, `shoal_accumulo_writer_free` (`capi/include/shoal.h`) | `TestOwnedAccumuloWriterCloseCancelsAndJoinsActiveUpdate`, `TestOwnedAccumuloWriterConnectorCloseCancelsActiveUpdate` (`cmd/shoal-capi/buffered_writer_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Covered | Close prevents new calls, cancels and joins active updates, submits the pending mutation, closes the underlying writer, maps structured failures, and remains idempotent while the handle lives. |
| SB-WRITE-020 | `AccumuloWriter.__del__` calling `close()` (`sharkbite/__init__.py:128-129`) | n/a | n/a | — | Not required (rationale required) | Rationale: `__del__`-driven network I/O at interpreter shutdown is unsafe (exceptions are swallowed, module globals may already be `None`). Shim should keep a best-effort `__del__` but must document `with`/`close()` as the contract. |
| SB-WRITE-021 | *(retired — duplicate)* `AccumuloBase.set_threads(n)` | see [SB-BASE-003](#sec-10-1) | see [SB-BASE-003](#sec-10-1) | see [SB-BASE-003](#sec-10-1) | Not required (rationale required) | audit #2: rationale — this row duplicated a method already enumerated as `SB-BASE-003` when [§10.1](#sec-10-1) was added, breaking the one-row-per-method rule. Tombstoned per [§4.1](#sec-4); the contract and its defect ([SB-UNSAFE-023](#sec-21)) live on SB-BASE-003. |
| SB-WRITE-022 | *(retired — split into distinct rows)* `AccumuloBase.set_table` / `set_authorizations` / `list_tables` / `to_scanner` | see [SB-BASE-004…SB-BASE-007](#sec-10) | see [SB-BASE-004…SB-BASE-007](#sec-10) | see [§10.1](#sec-10) | Not required (rationale required) | audit #92: rationale — this row grouped four independent public methods, which hid their individual contracts. Tombstoned rather than renumbered, per [§4.1](#sec-4). All four are now enumerated separately and none of their statuses is carried by this row. |

<a id="sec-10-1"></a>

### 10.1 High-level Python helper classes (`SB-BASE`)

`sharkbite/__init__.py` is shipped by the `sharkbite` distribution
(`setup.py:50`) and star-exports the native module, so these classes are part of
the published API even though they are pure Python. Every row is
`Missing C ABI`: each is composable from the Go client, but nothing above the C
ABI exists yet.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-BASE-001 | `AccumuloBase(instance, zookepeers, username, password, table=None, auths=None)` (`sharkbite/__init__.py:24-37`) | Compose `NewZooKeeperInstance` + `PasswordCredentials` + `NewConnector` (+ table resolution) | `shoal_client_create`, `shoal_client_close`, `shoal_client_free` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientCopiesAndSnapshotsMutableSettings` (`cmd/shoal-capi/high_level_client_export_test.go`) | Covered | Revision 31 composes and owns a connector, copies all retained configuration, and provides idempotent close/free. |
| SB-BASE-002 | `AccumuloBase` class attributes and defaults, including `_threads = 10` (`sharkbite/__init__.py:7-19`) | `BatchWriterOptions.MaxWriteThreads` default 3 (`accumulo/batch_writer.go:19`); `ScannerOptions.Parallelism` default 1 (`accumulo/scanner.go:107`) | `shoal_client_config_init` (`capi/include/shoal.h`); `shoal_client_config.thread_count` (`capi/include/shoal_types.h`) | `TestSharedLibraryCABI`; `TestOwnedClientCopiesAndSnapshotsMutableSettings` (`cmd/shoal-capi/high_level_client_export_test.go`) | Covered | Revision 31 preserves the Sharkbite default of 10 and applies it to subsequently created scanners and writers. |
| SB-BASE-003 | `AccumuloBase.set_threads(thread_count)` (`sharkbite/__init__.py:52-53`) | `BatchWriterOptions.MaxWriteThreads` / `ScannerOptions.Parallelism` | `shoal_client_set_threads` (`capi/include/shoal.h`) | `TestOwnedClientConcurrentSettingsAndSnapshots` (`cmd/shoal-capi/high_level_client_export_test.go`); `TestSharedLibraryCABI` | Covered | Copied synchronized state affects only subsequently created child handles. |
| SB-BASE-004 | `AccumuloBase.set_table(table)` (`sharkbite/__init__.py:55-58`) | `Connector.TableByName` (`accumulo/discovery.go:110`) | `shoal_client_set_table` (`capi/include/shoal.h`) | `TestOwnedClientConcurrentSettingsAndSnapshots` (`cmd/shoal-capi/high_level_client_export_test.go`); `TestSharedLibraryCABI` | Covered | The selected table is copied and synchronized; child creation requires a non-empty table. |
| SB-BASE-005 | `AccumuloBase.set_authorizations(auths)` accepting an `Authorizations`, a non-empty sequence, or `None` (`sharkbite/__init__.py:43-50`) | `ScannerOptions.Authorizations [][]byte` (`accumulo/scanner.go:103`) | `shoal_client_set_authorizations` (`capi/include/shoal.h`) | `TestOwnedClientCopiesAndSnapshotsMutableSettings`; `TestOwnedClientConcurrentSettingsAndSnapshots` (`cmd/shoal-capi/high_level_client_export_test.go`); `TestSharedLibraryCABI` | Covered | Labels are copied as binary-safe values; zero labels resets to empty authorizations. |
| SB-BASE-006 | `AccumuloBase.list_tables()` (`sharkbite/__init__.py:39-40`) | `Connector.Tables` (`accumulo/table_admin.go:29`) | `shoal_client_list_tables`, owned `shoal_table_list_result` (`capi/include/shoal.h`) | `TestSharedLibraryCABI` | Covered | Returns a sorted owned snapshot with deadline and connector-close cancellation. |
| SB-BASE-007 | `AccumuloBase.to_scanner()` (`sharkbite/__init__.py:21-22`) | Compose a second `Connector` + `Scanner` | `shoal_client_create_scanner` (`capi/include/shoal.h`) | `TestOwnedClientConcurrentSettingsAndSnapshots` (`cmd/shoal-capi/high_level_client_export_test.go`); `TestSharedLibraryCABI` | Covered | Creates an owned scanner from an atomic copied table/auth/thread snapshot while retaining connector lifetime. |
| SB-BASE-008 | `AccumuloWriter(instance, zookeepers, username, password, table=None, auths=None)` (`sharkbite/__init__.py:65-71`) | `Connector.NewBatchWriter` (`accumulo/batch_writer.go:225`) | `shoal_client_create_batch_writer` (`capi/include/shoal.h`) | `TestOwnedClientConcurrentSettingsAndSnapshots` (`cmd/shoal-capi/high_level_client_export_test.go`); `TestSharedLibraryCABI` | Covered | Returns the existing owned lazy buffered-writer handle, preserving delayed writer creation and copied table/thread settings. |
| SB-BASE-009 | `AccumuloScanner(instance, zookeepers, username, password, table=None, auths=None)` (`sharkbite/__init__.py:132-138`) | `Connector.NewScanner` (`accumulo/scanner.go:136`) | `shoal_client_create_scanner` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientCopiesColumnSelectionsIntoSnapshots` (`cmd/shoal-capi/high_level_scanner_export_test.go`) | Covered | Revision 32 creates an owned scanner from one copied table/authorization/column/thread snapshot and rejects a missing table or closed client. |
| SB-BASE-010 | Dead first definition of `class AccumuloScanner` with a `pass` constructor (`sharkbite/__init__.py:60-63`) | — | — | — | Not required (rationale required) | Rationale: shadowed by the real definition at `sharkbite/__init__.py:132`; unreachable in the pinned release. Recorded so a shim author does not resurrect it. See [SB-UNSAFE-010](#sec-21). |
| SB-BASE-011 | `AccumuloScanner.get(begin_row, end_row=None, chunksize=1000) -> AccumuloIterator` (`sharkbite/__init__.py:146-154`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_client_stream_range`, `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | A high-level client can open the requested copied range as an owned stream and retrieve bounded chunks. A shim retains any range-accumulation compatibility policy. |
| SB-BASE-012 | `AccumuloScanner.fetch_range(range)` (`sharkbite/__init__.py:156-157`) | Pass the range to `Scan` | `shoal_client_scan_range`, `shoal_client_scan_range_with_cancellation` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientScanHooksReceiveAtomicSnapshot`, `TestOwnedClientScanCancellationJoinsBeforeCloseReturns` (`cmd/shoal-capi/high_level_scanner_export_test.go`) | Covered | The ABI performs the configured range scan immediately and returns an owned result; deadlines, cancellation, and client close are coordinated. |
| SB-BASE-013 | `AccumuloScanner.fetch_ranges(ranges: list)` (`sharkbite/__init__.py:158-160`) | `BatchScanner.Scan(ctx, []*Range)` (`accumulo/batch_scanner.go:58`) | `shoal_client_scan_ranges`, `shoal_client_scan_ranges_with_cancellation` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientConcurrentColumnSelectionAndSnapshots` (`cmd/shoal-capi/high_level_scanner_export_test.go`) | Covered | Ranges are copied in caller order and executed with the atomic client settings snapshot and configured parallelism. |
| SB-BASE-014 | `AccumuloScanner.__del__` closing the scanner (`sharkbite/__init__.py:162-164`) | `Scanner` has no close; sessions close inside `Scan` (`accumulo/scanner.go:254`) | `shoal_client_close`, `shoal_client_free` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientScanCancellationJoinsBeforeCloseReturns` (`cmd/shoal-capi/high_level_scanner_export_test.go`) | Covered | Explicit idempotent close/free replaces nondeterministic interpreter destruction and cancels and joins active scans. |
| SB-BASE-015 | `AccumuloIterator(scanner)` — first constructor definition (`sharkbite/__init__.py:174-177`) | — | — | — | Not required (rationale required) | Rationale: shadowed by the two-argument definition below it and unreachable ([SB-UNSAFE-009](#sec-21)); recorded because its signature is what the class docstring implies. |
| SB-BASE-016 | `AccumuloIterator(scanner, chunkSize)` (`sharkbite/__init__.py:178-181`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_client_stream_range`, `shoal_client_stream_ranges` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | Cursor creation starts the owned stream and max_entries controls each later chunk. A shim may treat chunk size zero as unbounded policy without changing ABI ownership. |
| SB-BASE-017 | `AccumuloIterator.__iter__` (`sharkbite/__init__.py:183-185`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_client_stream_range`, `shoal_client_stream_ranges` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | A shim restarts iteration by opening a fresh independent cursor; cursor creation consumes no entry. |
| SB-BASE-018 | `AccumuloIterator.nextBatch()` (`sharkbite/__init__.py:187-188`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | Each call begins the next bounded chunk directly, so nextBatch needs only binding-side chunk state. |
| SB-BASE-019 | `AccumuloIterator.__next__` (`sharkbite/__init__.py:190-194`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | Owned chunks preserve every boundary entry and clean exhaustion, providing the approved SB-DIV-006 behavior without whole-result buffering. |
| SB-BASE-020 | `AccumuloScanner.select_column(cf, cq=None)` (`sharkbite/__init__.py:140-144`) | `ScannerOptions.Columns` with `NewColumnFamily` / `NewColumn` (`accumulo/scanner.go:104,43,48`) | `shoal_client_select_column` (`capi/include/shoal.h`) | `TestSharedLibraryCABI`; `TestOwnedClientCopiesColumnSelectionsIntoSnapshots`, `TestOwnedClientConcurrentColumnSelectionAndSnapshots` (`cmd/shoal-capi/high_level_scanner_export_test.go`) | Covered | Both family-only and family/qualifier forms copy binary inputs and affect only subsequently created or direct client scans. |

<a id="sec-11"></a>

## 11. Matrix: table operations (`SB-TABLE`)

All rows below are methods of `AccumuloTableOperations`
(`pysharkbite.cpp:238`), whose implementation is
`interconnect::PythonTableOperations`
(`include/interconnect/python/PythonStructures.h:63`). The handle is bound to
one table at construction, so no method takes a table name.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-TABLE-001 | `create(recreate=False) -> bool` (`pysharkbite.cpp:250`, `PythonStructures.h:74`) | `Connector.CreateTable(ctx, name) error` (`accumulo/table_admin.go:76`) | `shoal_connector_create_table` | `TestTableAdministrationLifecycleAndCancellation` (`accumulo/table_admin_test.go:63`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | `recreate=True` means "drop then create"; Shoal has no recreate. Return type differs (`bool` vs error), so the Python shim must compose delete/create and translate duplicate-table status. |
| SB-TABLE-002 | `exists(createIfNot=False) -> bool` (`pysharkbite.cpp:240`, `PythonStructures.h:87`) | `Connector.TableExists(ctx, name) (bool, error)` (`accumulo/table_admin.go:53`) | `shoal_connector_table_exists` | `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The `createIfNot` side effect must be reproduced; every pinned Python test calls `exists(False)` first (`test/python/TestWrites.py:15`). |
| SB-TABLE-003 | `remove() -> bool` (`pysharkbite.cpp:239`, `PythonStructures.h:80`) | `Connector.DeleteTable(ctx, name) error` (`accumulo/table_admin.go:96`) | `shoal_connector_delete_table` | `TestTableAdministrationLifecycleAndCancellation` (`accumulo/table_admin_test.go:63`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Every pinned Python test ends with `tableOperations.remove()`; the shim must translate success/status to `bool`. |
| SB-TABLE-004 | Table rename (not bound in Python) | `Connector.RenameTable(ctx, old, new) error` (`accumulo/table_admin.go:110`) | `shoal_connector_rename_table` | `TestTableMutationsUseAccumulo4FATEArguments` (`accumulo/table_admin_test.go:244`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal superset; `AccumuloTableOperations` has no rename binding. |
| SB-TABLE-005 | `flush(startRow, endRow, wait) -> int8` (`pysharkbite.cpp:242`, `PythonStructures.h:111`) | `Connector.FlushTableRange(ctx, tableName string, startRow, endRow []byte, wait bool) error` (`accumulo/table_flush.go:33`); `Connector.FlushTable` (`accumulo/table_flush.go:16`) is the unbounded form | `shoal_connector_flush_table_range` (`capi/include/shoal.h`) | `TestFlushTableRangeSendsRowBounds`, `TestFlushTableRangeRejectsReversedAndInvalidBounds`, `TestFlushTableRangeAcceptsEqualBounds` (`accumulo/table_constraints_test.go`); `TestFlushTableUsesStableIDAndAccumulo4WaitModes` (`accumulo/table_flush_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestABIDiscoveryQueriesAreConcurrentAndStable` (`cmd/shoal-capi/abi_export_test.go`) | Covered | The row range is now sent: `WaitForFlush` carries the start and end rows Accumulo uses to select the tablets it flushes, and a nil bound is the absent field that means unbounded. Bounds are copied, a reversed pair is rejected before any RPC with `ErrInvalidTableRange` (`accumulo/errors.go:83`) rather than the split-collection sentinel, and equal bounds flush the tablets covering one row. The status difference remains — Sharkbite returns `int8` 0/1 and Shoal returns an error — and `shoal_connector_flush_table` still takes no rows, so the bounded capability is not reachable through the ABI. Revision 29 exposes the owned, deadline-aware ABI with connector-close coordination and allocation-failure coverage. |
| SB-TABLE-006 | `compact(startRow, endRow, wait) -> int8` (`pysharkbite.cpp:243`, `PythonStructures.h:124`) | — | — | — | Missing Go | No online compaction trigger, and the pinned target cannot take one as Sharkbite issues it. Sharkbite drives compaction through its coordinator facade: the Accumulo-2 path builds a FATE `TABLE_COMPACT` whose fourth argument is a Thrift-serialized, default-constructed `IteratorConfig` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:101-126`), and the Accumulo-1 path reaches the one-way tablet-server `compact` RPC, which takes the manager's lock (`include/data/extern/thrift/TabletClientService.h:46`). Accumulo 4 has neither: `TabletServerClientService` has no `compact`, `ManagerClientService` has no compaction RPC, and a user compaction is a FATE operation whose argument payload is a Java-encoded `CompactionConfig` that the Thrift IDL Shoal generates from does not describe. Implementing it therefore needs a payload contract that is not in the pinned interface, which is why this row stays `Missing Go` rather than being guessed at. `internal/offlinecompact` is an out-of-band OFFLINE-fenced tool and is not reachable from `Connector`; issue [#65](https://github.com/phrocker/shoal-oss/issues/65) tracks online compaction. |
| SB-TABLE-007 | `setProperty(property, value) -> int8` (`pysharkbite.cpp:244`, `PythonStructures.h:135`) | `Connector.SetTableProperty(ctx, tableName, property, value) error` (`accumulo/table_properties.go:14`) | `shoal_connector_set_table_property` | `TestTablePropertyMutationsUseAccumulo4ManagerRPCs` (`accumulo/table_properties_test.go:12`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The shim must translate stable status/error semantics to Sharkbite's `int8` result. |
| SB-TABLE-008 | `removeProperty(property) -> int8` (`pysharkbite.cpp:245`, `PythonStructures.h:145`) | `Connector.RemoveTableProperty(ctx, tableName, property) error` (`accumulo/table_properties.go:30`) | `shoal_connector_remove_table_property` | `TestTablePropertyMutationsUseAccumulo4ManagerRPCs` (`accumulo/table_properties_test.go:12`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The shim must translate stable status/error semantics to Sharkbite's `int8` result. |
| SB-TABLE-009 | Property reads (not bound in Python) | `Connector.EffectiveTableProperties(ctx, tableName)` (`accumulo/table_property_reads.go:31`) | `shoal_connector_effective_table_properties` + owned `shoal_table_properties_result` | `TestEffectiveTablePropertiesPreservesValuesAndCopyIsolation` (`accumulo/table_property_reads_test.go:29`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: Shoal superset (issue [#54](https://github.com/phrocker/shoal-oss/issues/54) / PR [#56](https://github.com/phrocker/shoal-oss/pull/56)). |
| SB-TABLE-010 | `addSplits(set[str])` (`pysharkbite.cpp:246`, `PythonStructures.h:153`) | `Connector.AddTableSplits` (`accumulo/table_add_splits.go`) | `shoal_connector_add_table_splits` (`capi/include/shoal.h`) | `TestAddTableSplitsSortsDeduplicatesAndPreservesBinaryRows` (`accumulo/table_add_splits_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Go and C preserve arbitrary binary rows, sort/deduplicate copied inputs, and add without replacing existing splits. The Python shim must accept Sharkbite's `set[str]` shape and return convention. |
| SB-TABLE-011 | Split listing (not bound in Python) | `Connector.ListTableSplits` (`accumulo/table_splits.go`) | `shoal_connector_list_table_splits` + owned `shoal_bytes_list_result` | `TestListTableSplitsOrdersAndCopiesRows` (`accumulo/table_splits_test.go`); `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: no Sharkbite equivalent; Shoal exposes the binary-safe sibling operation as a documented superset. |
| SB-TABLE-012 | `addConstraint(className) -> int` (`pysharkbite.cpp:247`, `PythonStructures.h:162`) | `Connector.AddConstraint(ctx, tableName, className string) (int32, error)` (`accumulo/table_constraints.go:57`), with `Connector.ListConstraints` (`accumulo/table_constraints.go:227`) and `Connector.RemoveConstraint` (`accumulo/table_constraints.go:245`) | `shoal_connector_add_table_constraint`, `shoal_connector_list_table_constraints`, `shoal_connector_remove_table_constraint` (`capi/include/shoal.h`) | `TestAddConstraintAssignsTheLowestFreeNumber`, `TestAddConstraintStartsAtOneAndIsIdempotent`, `TestListConstraintsOrdersByNumberAndIgnoresNonConstraints`, `TestRemoveConstraintRemovesTheNumberedProperty`, `TestConstraintOperationsValidateTheirArguments`, `TestConstraintOperationsHonorCancellationAndClose`, `TestConcurrentAddConstraintAllocatesDistinctNumbers`, `TestAddConstraintRetriesWhenAnotherConnectorWinsTheVersion` (`accumulo/table_constraints_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestABIDiscoveryQueriesAreConcurrentAndStable` (`cmd/shoal-capi/abi_export_test.go`) | Covered | Constraint violations are surfaced on write (`accumulo/batch_writer.go:101`), and constraints can now be installed: a class is stored under `table.constraint.<number>` (`ConstraintPropertyPrefix`, `accumulo/table_constraints.go:14`) with the lowest free positive number, which is how Accumulo assigns one, and the number is returned. Installing a class the table already carries returns its existing number without writing, because a second number would run the same check twice per mutation. Allocation is a compare-and-set: the table's own properties are read with their version and written back through `modifyTableProperties`, so a second client that installs a constraint first makes the losing write fail with a concurrent modification, which is retried against the winner's state rather than overwriting it. **Safe shim for unsafe C++ behavior:** the pinned `addConstraint` is a stub that installs nothing and returns 0 ([SB-UNSAFE-036](#sec-21)), so no Sharkbite program has a working constraint to preserve. Listing and removal have no Sharkbite equivalent but are required to make the returned number usable. Revision 29 exposes the owned, deadline-aware ABI with connector-close coordination and allocation-failure coverage. |
| SB-TABLE-013 | `import(dir, fail_path, setTime=False) -> bool` (`pysharkbite.cpp:241`, `PythonStructures.h:98`) | `Connector.BulkImport(ctx, tableName, bulkDir, BulkImportOptions{SetTime})` (`accumulo/table_bulk_import.go`); `TestBulkImportUsesTableIDAndFateArguments`, `TestBulkImportResolvesTableNameNotFound`, `TestBulkImportMapsManagerErrors`, `TestBulkImportValidationCancellationAndLifecycle` | — | — | Behavior mismatch | Shoal exposes Accumulo 4 Bulk Import V2 on `Connector`, but requires a pre-staged `loadmap.json` and has no Sharkbite `fail_path` behavior or compatibility adapter. Reachable only via `getattr(tableOps, "import")(...)` because `import` is a Python keyword; the shim must expose both `import` and a usable alias such as `import_directory`. |
| SB-TABLE-014 | Table export | — (no Sharkbite Python binding either) | — | — | Not required (rationale required) | Rationale: not in the Sharkbite Python surface. Listed so future readers do not re-derive it. |
| SB-TABLE-015 | Table clone / online / offline / merge / delete-rows | — (no Sharkbite Python binding) | — | — | Not required (rationale required) | Rationale: absent from the Sharkbite Python contract; confirmed absent from `accumulo/` as well. |
| SB-TABLE-016 | `AccumuloTableInfo.table_id(table) -> str` (`pysharkbite.cpp:253`, `PythonStructures.h:40`) | `Connector.TableByName(ctx, name) (Table, error)`; `Table.ID` (`accumulo/discovery.go:110`, `accumulo/discovery.go:30`) | `shoal_connector_list_tables` + owned `shoal_table_list_result` | `TestDiscoveryTableLookupAndRouting` (`accumulo/discovery_test.go:312`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | The C ABI exposes the same ID/name pairs through `list_tables`, but not as a dedicated `table_id(table)` helper; a compatibility shim must project the pair list into Sharkbite's direct lookup. |
| SB-TABLE-017 | `AccumuloTableInfo.table_name(tableid) -> str` (`pysharkbite.cpp:254`, `PythonStructures.h:49`) | `Connector.TableByID(ctx, id) (Table, error)` (`accumulo/discovery.go:132`) | `shoal_connector_list_tables` + owned `shoal_table_list_result` | `TestDiscoveryTableLookupAndRouting` (`accumulo/discovery_test.go:312`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | The C ABI exposes the same ID/name pairs through `list_tables`, but not as a dedicated `table_name(tableid)` helper; a compatibility shim must project the pair list into Sharkbite's direct lookup. |
| SB-TABLE-018 | `AccumuloTableInfo.list_tables() -> list[str]` (`pysharkbite.cpp:255`) | `Connector.Tables(ctx) ([]Table, error)` (`accumulo/table_admin.go:29`) | `shoal_connector_list_tables` + owned `shoal_table_list_result` | `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Sharkbite returns names; Shoal returns `Table{ID, Name}` pairs. A compatibility shim must project either Go or C results down to names. |
| SB-TABLE-019 | `AccumuloTableInfo.exists(table) -> bool` (`pysharkbite.cpp:256`, `PythonStructures.h:34`) | `Connector.TableExists(ctx, name)` (`accumulo/table_admin.go:53`) | `shoal_connector_table_exists` | `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Distinct from SB-TABLE-002: takes an explicit table name and has no create side effect, but the Go/C layers still report status-or-error rather than Sharkbite's direct `bool` return. |
| SB-TABLE-020 | Tablet discovery (not bound in Python) | `Connector.Tablets`, `LocateTablet`, `InvalidateTablet`, `InvalidateTable`, `InvalidateDiscovery` (`accumulo/discovery.go:157,180,202,215,228`) | — | `TestDiscoveryInvalidationAndDefensiveCopies` (`accumulo/discovery_test.go:352`) | Not required (rationale required) | Rationale: Shoal superset; Sharkbite hides locator state entirely. |
| SB-TABLE-021 | Table-operation errors: table does not exist after `remove()` (`test/python/TestBadOperations.py:33-62`) | `ErrTableNotFound` (`accumulo/errors.go:15`), `mapManagerError` (`accumulo/table_admin.go:168`) | `SHOAL_STATUS_NOT_FOUND` (9) (`capi/include/shoal_types.h`) | `TestTableMutationsMapErrorsAndLifecycle` (`accumulo/table_admin_test.go:301`); `TestMapManagerErrorUsesServerTableName` (`accumulo/table_admin_test.go:366`); `TestStatusForTableAdministrationErrors` (`cmd/shoal-capi/table_admin_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Sharkbite raises `ClientException` with code `TABLE_NOT_FOUND` (`test/vandv/invalidscans.h`); Shoal's Go/C layers map that condition, but the compatibility shim still has to raise Sharkbite's object/result shape. Mapping is [SB-ERR-004](#sec-18). |
| SB-TABLE-022 | Creating an existing table | `ErrTableExists` (`accumulo/errors.go:18`) | `SHOAL_STATUS_ALREADY_EXISTS` (19) (`capi/include/shoal_types.h`) | `TestTableMutationsMapErrorsAndLifecycle` (`accumulo/table_admin_test.go:301`); `TestStatusForTableAdministrationErrors` (`cmd/shoal-capi/table_admin_export_test.go`); `main()` (`capi/tests/lifecycle.c`) | Behavior mismatch | Sharkbite's `create(False)` returns `False` rather than raising (`test/python/TestWrites.py:17-19`). Shoal's Go/C layers surface the condition, but the compatibility shim must return `False`, not raise. |

<a id="sec-12"></a>

## 12. Matrix: namespace operations (`SB-NS`)

`AccumuloNamespaceOperations` (`pysharkbite.cpp:229`,
`include/interconnect/python/PythonStructures.h:186`). Shoal now exposes the
complete required namespace lifecycle/property surface in Go and the stable C
ABI. Rows remain `Behavior mismatch` until the Python shim reproduces
Sharkbite's handle-relative empty-name defaults, argument order, and return
types.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-NS-001 | `list() -> list[str]` (`pysharkbite.cpp:230`, `PythonStructures.h:196`) | `Connector.Namespaces` (`accumulo/namespace_discovery.go`) | `shoal_connector_list_namespaces` + owned `shoal_namespace_list_result` (`capi/include/shoal.h`) | `TestNamespaceDiscoveryListingAndLookup` (`accumulo/namespace_discovery_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-NS-002 | `create(name="")` (`pysharkbite.cpp:236`, `PythonStructures.h:215`) | `Connector.CreateNamespace` (`accumulo/namespace_admin.go`) | `shoal_connector_create_namespace` | `TestNamespaceMutationsUseAccumulo4FATEArgumentsAndInvalidate` (`accumulo/namespace_admin_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Empty name means "the namespace this handle was constructed with". |
| SB-NS-003 | `remove(name="") -> bool` (`pysharkbite.cpp:231`, `PythonStructures.h:202`) | `Connector.DeleteNamespace` (`accumulo/namespace_admin.go`) | `shoal_connector_delete_namespace` | `TestDeleteNamespaceEmptyNamespaceSubmitsFATE` (`accumulo/namespace_admin_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-NS-004 | `exists(name="") -> bool` (`pysharkbite.cpp:232`, `PythonStructures.h:209`) | `Connector.NamespaceExists` (`accumulo/namespace_discovery.go`) | `shoal_connector_namespace_exists` | `TestNamespaceDiscoveryListingAndLookup` (`accumulo/namespace_discovery_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-NS-005 | `rename(newName, oldName="")` (`pysharkbite.cpp:233`, `PythonStructures.h:222`) | `Connector.RenameNamespace` (`accumulo/namespace_admin.go`) | `shoal_connector_rename_namespace` | `TestNamespaceMutationsUseAccumulo4FATEArgumentsAndInvalidate` (`accumulo/namespace_admin_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Argument order is (new, old) — unusual and must be preserved. |
| SB-NS-006 | `setProperty(property, value, nm="")` (`pysharkbite.cpp:234`, `PythonStructures.h:231`) | `Connector.SetNamespaceProperty` (`accumulo/namespace_properties.go`) | `shoal_connector_set_namespace_property` | `TestNamespacePropertyMutationsUseManagerRPCsAndDefaultNamespace` (`accumulo/namespace_properties_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-NS-007 | `removeProperty(property, nm="")` (`pysharkbite.cpp:235`, `PythonStructures.h:240`) | `Connector.RemoveNamespaceProperty` (`accumulo/namespace_properties.go`) | `shoal_connector_remove_namespace_property` | `TestNamespacePropertyMutationsUseManagerRPCsAndDefaultNamespace` (`accumulo/namespace_properties_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-NS-008 | Qualified `namespace.table` name resolution | `internal/tablenames.Resolver.ResolveID` handles qualified names; public namespace administration is in `accumulo/namespace_*.go` | Table/scanner/writer calls accept qualified names; namespace calls use explicit namespace names | `TestConnectorSharesNamespaceResolverAcrossNamespaceAndTableAPIs` (`accumulo/namespace_discovery_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Go and C support qualified table resolution plus namespace management; the Python handle composition remains to be implemented. |

<a id="sec-13"></a>

## 13. Matrix: security operations and permissions (`SB-SEC`)

`SecurityOperations` (`pysharkbite.cpp:594`,
`include/interconnect/python/PythonStructures.h:248`). PR #99 supplies the
complete merged Go user/authorization/permission API, and this administration
slice supplies its stable C ABI, exact permission ordinals, owned binary
authorization results, and structured security error details. Rows remain
`Behavior mismatch` until the Python layer restores Sharkbite's return-value
and `Authorizations` object contracts.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-SEC-001 | `create_user(user, password) -> int8` (`pysharkbite.cpp:595`, `PythonStructures.h:263`) | `Connector.CreateUser` (`accumulo/security.go`) | `shoal_connector_create_user` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-002 | `change_password(user, password) -> int8` (`pysharkbite.cpp:596`, `PythonStructures.h:276`) | `Connector.ChangePassword` (`accumulo/security.go`) | `shoal_connector_change_password` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-003 | `remove_user(user) -> int8` (`pysharkbite.cpp:597`, `PythonStructures.h:288`) | `Connector.DropUser` (`accumulo/security.go`) | `shoal_connector_drop_user` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-004 | `get_auths(user) -> Authorizations` (`pysharkbite.cpp:598`, `PythonStructures.h:295`) | `Connector.GetUserAuthorizations` (`accumulo/security.go`) | `shoal_connector_get_user_authorizations` + owned `shoal_bytes_list_result` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Returns `unique_ptr<Authorizations>`; Python takes ownership. |
| SB-SEC-005 | `grantAuthorizations(auths, user) -> int8` (`pysharkbite.cpp:608`, `PythonStructures.h:310`) | `Connector.ChangeUserAuthorizations` (`accumulo/security.go`) | `shoal_connector_change_user_authorizations` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Used by four of the ten pinned Python tests; without it the visibility tests cannot run at all. |
| SB-SEC-006 | `has_system_permission(user, SystemPermissions) -> bool` (`pysharkbite.cpp:599`, `PythonStructures.h:321`) | `Connector.HasSystemPermission` (`accumulo/security.go`) | `shoal_connector_has_system_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-007 | `has_table_permission(user, table, TablePermissions) -> bool` (`pysharkbite.cpp:600`, `PythonStructures.h:333`) | `Connector.HasTablePermission` (`accumulo/security.go`) | `shoal_connector_has_table_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-008 | `has_namespace_permission(user, ns, NamespacePermissions) -> bool` (`pysharkbite.cpp:601`, `PythonStructures.h:345`) | `Connector.HasNamespacePermission` (`accumulo/security.go`) | `shoal_connector_has_namespace_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-009 | `grant_system_permission(user, perm) -> int8` (`pysharkbite.cpp:602`, `PythonStructures.h:358`) | `Connector.GrantSystemPermission` (`accumulo/security.go`) | `shoal_connector_grant_system_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-010 | `revoke_system_permission(user, perm) -> int8` (`pysharkbite.cpp:603`, `PythonStructures.h:370`) | `Connector.RevokeSystemPermission` (`accumulo/security.go`) | `shoal_connector_revoke_system_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-011 | `grant_table_permission(user, table, perm) -> int8` (`pysharkbite.cpp:604`, `PythonStructures.h:384`) | `Connector.GrantTablePermission` (`accumulo/security.go`) | `shoal_connector_grant_table_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-012 | `revoke_table_permission(user, table, perm) -> int8` (`pysharkbite.cpp:605`, `PythonStructures.h:397`) | `Connector.RevokeTablePermission` (`accumulo/security.go`) | `shoal_connector_revoke_table_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-013 | `grant_namespace_permission(user, ns, perm) -> int8` (`pysharkbite.cpp:606`, `PythonStructures.h:412`) | `Connector.GrantNamespacePermission` (`accumulo/security.go`) | `shoal_connector_grant_namespace_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-014 | `revoke_namespace_permission(user, ns, perm) -> int8` (`pysharkbite.cpp:607`, `PythonStructures.h:426`) | `Connector.RevokeNamespacePermission` (`accumulo/security.go`) | `shoal_connector_revoke_namespace_permission` | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | |
| SB-SEC-015 | `SystemPermissions` enum: `GRANT`, `CREATE_TABLE`, `DROP_TABLE`, `ALTER_TABLE`, `CREATE_USER`, `ALTER_USER`, `SYSTEM`, `CREATE_NAMESPACE`, `DROP_NAMESPACE`, `ALTER_NAMESPACE` (`pysharkbite.cpp:403-413`) | `SystemPermission` exact Accumulo 4 ordinals (`accumulo/security.go`) | `shoal_system_permission` (`capi/include/shoal_types.h`) | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Arithmetic (bitmask-capable) enum; numeric values must match Accumulo's wire ordinals. Enumerated as one row deliberately: the contract is the ordinal set as a whole, because each value's numeric ordinal must match Accumulo's wire encoding and they are only meaningful together. Every value is listed in this row; a shim that ships a subset, or renumbers any value, fails this row. |
| SB-SEC-016 | `NamespacePermissions` enum: `READ`, `WRITE`, `ALTER_NAMESPACE`, `GRANT`, `ALTER_TABLE`, `CREATE_TABLE`, `DROP_TABLE`, `BULK_IMPORT`, `DROP_NAMESPACE` (`pysharkbite.cpp:415-424`) | `NamespacePermission` exact Accumulo 4 ordinals (`accumulo/security.go`) | `shoal_namespace_permission` (`capi/include/shoal_types.h`) | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Enumerated as one row deliberately: the contract is the ordinal set as a whole, because each value's numeric ordinal must match Accumulo's wire encoding and they are only meaningful together. Every value is listed in this row; a shim that ships a subset, or renumbers any value, fails this row. |
| SB-SEC-017 | `TablePermissions` enum: `READ`, `WRITE`, `GRANT`, `ALTER_TABLE`, `DROP_TABLE`, `BULK_IMPORT` (`pysharkbite.cpp:426-432`) | `TablePermission` exact Accumulo 4 ordinals (`accumulo/security.go`) | `shoal_table_permission` (`capi/include/shoal_types.h`) | `TestSecurityOperationsSelectionArgumentsAndDefensiveCopies`, `TestSecurityPermissionWireOrdinals` (`accumulo/security_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Enumerated as one row deliberately: the contract is the ordinal set as a whole, because each value's numeric ordinal must match Accumulo's wire encoding and they are only meaningful together. Every value is listed in this row; a shim that ships a subset, or renumbers any value, fails this row. |
| SB-SEC-018 | Authorization enforcement on scan (cells hidden without the label) | Server-side; Shoal passes `ScannerOptions.Authorizations` through | `shoal_scanner_config.authorizations` | `main()` in `capi/tests/lifecycle.c` (validation only) | Behavior mismatch | No Shoal test proves that a cell with visibility `blah2` is hidden when scanning with only `blah1` — the exact assertion made by `test/python/TestAuthorizations.py:68-78`. Requires live-cluster conformance ([§24](#sec-24)). `internal/visfilter` implements the evaluator for the embedded path only. |
| SB-SEC-019 | Server-side authorization failures on write | `AuthorizationFailure` (`accumulo/batch_writer.go:109`) | `shoal_authorization_failure_view` (`capi/include/shoal_types.h`) | `TestBatchWriterSurfacesAccumuloUpdateErrors` (`accumulo/batch_writer_test.go:942`); `main()` in `capi/tests/result_bridge.c` | Not required (rationale required) | Rationale: Shoal superset; Sharkbite silently discards these. |

<a id="sec-14"></a>

## 14. Matrix: cluster status and monitoring (`SB-STAT`)

Reached through `AccumuloConnector.getStatistics()` (`pysharkbite.cpp:268`) and
demonstrated by `examples/pythonstats.py`.

**All 82 capability rows in this section are
`Intentional divergence (approval required)` because the upstream capability no
longer exists.** Two rows are excluded and carry other statuses:
[SB-STAT-028](#sec-14) (`Not required` — a pybind11 registration defect in the
`TableRates` property table, not a capability) and [SB-STAT-038](#sec-14)
(`Missing Go` — a Python `dynamic_attr` property of the objects rather than a
protocol capability). Accumulo commit `f0841e4`
removed `getManagerStats`, `ManagerMonitorInfo`, and `DeadServer` together with
the legacy monitor resources — 113 commits before the pinned Accumulo 4 target
`317c288`. The surviving REST-v2, metrics, and server APIs cannot reconstruct
the `AccumuloInfo` / `TabletServerStatus` / `TableInfo` / `TableRates` /
`TableCompactions` / `Compacting` / `RecoveryStatus` / `DeadServer` object graph
these rows describe: the data is either absent, aggregated differently, or
exposed only as scrape-time metrics without the per-table and per-server
structure Sharkbite returns.

This is therefore **not** a Shoal implementation gap that effort can close, and
it must never be marked `Missing Go` (which implies it is buildable) or
`Covered`. Shoal issue [#96](https://github.com/phrocker/shoal-oss/issues/96)
attempted it and PR [#98](https://github.com/phrocker/shoal-oss/pull/98) was
**withdrawn** for this reason. The single approval decision covering the whole
section is [SB-DIV-016](#sec-26); until it is signed, these rows block the gate
exactly like any other unapproved divergence
([§2](#sec-2) corollary 5).

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-STAT-001 | `AccumuloInfo.getTableMap()` (`pysharkbite.cpp:210`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Maps table ID → `TableInfo`. |
| SB-STAT-054 | `AccumuloInfo.table_map` read-only property (`pysharkbite.cpp:211`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getTableMap()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-001. |
| SB-STAT-002 | `AccumuloInfo.getTabletServerInfo()` (`pysharkbite.cpp:212`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-055 | `AccumuloInfo.tablet_server_info` read-only property (`pysharkbite.cpp:213`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getTabletServerInfo()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-002. |
| SB-STAT-003 | `AccumuloInfo.getBadTabletServers()` (`pysharkbite.cpp:214`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-056 | `AccumuloInfo.bad_servers` read-only property (`pysharkbite.cpp:215`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getBadTabletServers()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-003. |
| SB-STAT-004 | `AccumuloInfo.getState()` (`pysharkbite.cpp:216`) | — | — | — | Intentional divergence (approval required) | Returns `CoordinatorState`. |
| SB-STAT-057 | `AccumuloInfo.state` read-only property (`pysharkbite.cpp:217`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getState()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-004. |
| SB-STAT-005 | `AccumuloInfo.getGoalState()` (`pysharkbite.cpp:218`) | — | — | — | Intentional divergence (approval required) | Returns `CoordinatorGoalState`. |
| SB-STAT-058 | `AccumuloInfo.goal_state` read-only property (`pysharkbite.cpp:219`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getGoalState()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-005. |
| SB-STAT-006 | `AccumuloInfo.getUnassignedTablets()` (`pysharkbite.cpp:220`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-059 | `AccumuloInfo.unassigned_tablets` read-only property (`pysharkbite.cpp:221`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getUnassignedTablets()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-006. |
| SB-STAT-007 | `AccumuloInfo.getServerShuttingDown()` (`pysharkbite.cpp:222`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-060 | `AccumuloInfo.servs_shutting_down` read-only property (`pysharkbite.cpp:223`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getServerShuttingDown()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-007. |
| SB-STAT-008 | `AccumuloInfo.getDeadServers()` (`pysharkbite.cpp:224`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-061 | `AccumuloInfo.dead_servers` read-only property (`pysharkbite.cpp:225`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getDeadServers()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-008. |
| SB-STAT-009 | `TabletServerStatus.getTableMap()` (`pysharkbite.cpp:159`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-062 | `TabletServerStatus.table_map` read-only property (`pysharkbite.cpp:160`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getTableMap()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-009. |
| SB-STAT-010 | `TabletServerStatus.getLastContact()` (`pysharkbite.cpp:161`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-063 | `TabletServerStatus.last_contact` read-only property (`pysharkbite.cpp:162`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getLastContact()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-010. |
| SB-STAT-011 | `TabletServerStatus.getName()` (`pysharkbite.cpp:163`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-064 | `TabletServerStatus.name` read-only property (`pysharkbite.cpp:164`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getName()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-011. |
| SB-STAT-012 | `TabletServerStatus.getOsLoad()` (`pysharkbite.cpp:165`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-065 | `TabletServerStatus.os_load` read-only property (`pysharkbite.cpp:166`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getOsLoad()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-012. |
| SB-STAT-013 | `TabletServerStatus.getLookups()` (`pysharkbite.cpp:167`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-066 | `TabletServerStatus.lookups` read-only property (`pysharkbite.cpp:168`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getLookups()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-013. |
| SB-STAT-014 | `TabletServerStatus.getIndexCacheHits()` (`pysharkbite.cpp:169`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-067 | `TabletServerStatus.index_cache_hits` read-only property (`pysharkbite.cpp:170`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getIndexCacheHits()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-014. |
| SB-STAT-015 | `TabletServerStatus.getDataCacheHits()` (`pysharkbite.cpp:171`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-068 | `TabletServerStatus.data_cache_hits` read-only property (`pysharkbite.cpp:172`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getDataCacheHits()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-015. |
| SB-STAT-016 | `TabletServerStatus.getDataCacheRequests()` (`pysharkbite.cpp:173`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-069 | `TabletServerStatus.data_cache_requests` read-only property (`pysharkbite.cpp:174`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getDataCacheRequests()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-016. |
| SB-STAT-017 | `TabletServerStatus.getLogSorts()` (`pysharkbite.cpp:175`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-070 | `TabletServerStatus.log_sorts` read-only property (`pysharkbite.cpp:176`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getLogSorts()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-017. |
| SB-STAT-018 | `TabletServerStatus.getFlushes()` (`pysharkbite.cpp:177`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-071 | `TabletServerStatus.flushes` read-only property (`pysharkbite.cpp:178`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getFlushes()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-018. |
| SB-STAT-019 | `TabletServerStatus.getSyncs()` (`pysharkbite.cpp:179`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-072 | `TabletServerStatus.syncs` read-only property (`pysharkbite.cpp:180`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getSyncs()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-019. |
| SB-STAT-020 | `TabletServerStatus.getHoldTime()` (`pysharkbite.cpp:181`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-073 | `TabletServerStatus.hold_time` read-only property (`pysharkbite.cpp:182`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getHoldTime()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-020. |
| SB-STAT-021 | `TableInfo.getRecords()` (`pysharkbite.cpp:145`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-074 | `TableInfo.records` read-only property (`pysharkbite.cpp:146`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getRecords()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-021. |
| SB-STAT-022 | `TableInfo.getRecordsInMemory()` (`pysharkbite.cpp:147`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-075 | `TableInfo.records_in_memory` read-only property (`pysharkbite.cpp:148`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getRecordsInMemory()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-022. |
| SB-STAT-023 | `TableInfo.getTablets()` (`pysharkbite.cpp:149`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-076 | `TableInfo.tablets` read-only property (`pysharkbite.cpp:150`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getTablets()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-023. |
| SB-STAT-024 | `TableInfo.getOnlineTablets()` (`pysharkbite.cpp:151`) | — | — | — | Intentional divergence (approval required) | Note the property name is `online_tables` while the getter says tablets — preserve both spellings. |
| SB-STAT-077 | `TableInfo.online_tables` read-only property (`pysharkbite.cpp:152`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getOnlineTablets()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-024. |
| SB-STAT-025 | `TableInfo.getTableRates()` (`pysharkbite.cpp:153`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-078 | `TableInfo.table_rates` read-only property (`pysharkbite.cpp:154`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getTableRates()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-025. |
| SB-STAT-026 | `TableInfo.getCompactioninfo()` (`pysharkbite.cpp:155`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Note the lowercase `i` in `getCompactioninfo`. |
| SB-STAT-079 | `TableInfo.compaction_info` read-only property (`pysharkbite.cpp:156`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getCompactioninfo()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-026. |
| SB-STAT-027 | `TableRates.getIngestRate()` (`pysharkbite.cpp:109`) | — | — | — | Intentional divergence (approval required) | Removed upstream with the rest of [§14](#sec-14); [SB-DIV-016](#sec-26). |
| SB-STAT-039 | `TableRates.getIngestRateByte()` (`pysharkbite.cpp:111`) | — | — | — | Intentional divergence (approval required) | As SB-STAT-027. |
| SB-STAT-040 | `TableRates.getQueryRate()` (`pysharkbite.cpp:113`) | — | — | — | Intentional divergence (approval required) | As SB-STAT-027. |
| SB-STAT-041 | `TableRates.getQueryRateByte()` (`pysharkbite.cpp:115`) | — | — | — | Intentional divergence (approval required) | As SB-STAT-027. |
| SB-STAT-042 | `TableRates.getScanRate()` (`pysharkbite.cpp:117`) | — | — | — | Intentional divergence (approval required) | As SB-STAT-027. |
| SB-STAT-028 | `TableRates.query_rate_byte` read-only property (`pysharkbite.cpp:110,112,114,116`) | — | — | — | Not required (rationale required) | Rationale: registered **four times** over the same member and no `ingest_rate*` property is registered at all. Reproducing the duplicate registration is meaningless; a shim should expose one property per rate. Kept out of the [SB-DIV-016](#sec-26) divergence because it records a binding defect, not a capability. |
| SB-STAT-043 | `TableRates.scan_rate` read-only property (`pysharkbite.cpp:118`) | — | — | — | Intentional divergence (approval required) | The one correctly registered `TableRates` property; removed upstream with the rest of [§14](#sec-14). |
| SB-STAT-029 | `TableCompactions.getMinorCompactions()` (`pysharkbite.cpp:137`) | — | — | — | Intentional divergence (approval required) |  |
| SB-STAT-080 | `TableCompactions.minors` read-only property (`pysharkbite.cpp:138`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getMinorCompactions()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-029. |
| SB-STAT-030 | `TableCompactions.getMajorCompactions()` (`pysharkbite.cpp:139`) | — | — | — | Intentional divergence (approval required) | Doubly removed upstream: beyond the whole-section removal recorded above, Accumulo 4 dropped major-compaction counts specifically, so even a monitor-scraping shim could not synthesise this field. Sharkbite programs read `.majors.running`/`.majors.queued` — the pattern `examples/pythonstats.py` uses for `.scans` — so the shim must choose between raising, `None`, and a zeroed `Compacting`. Recorded separately as [SB-DIV-013](#sec-26) because the choice is visible even if [SB-DIV-016](#sec-26) is approved. |
| SB-STAT-081 | `TableCompactions.majors` read-only property (`pysharkbite.cpp:140`) | — | — | — | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getMajorCompactions()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-030. |
| SB-STAT-031 | `TableCompactions.getScans()` (`pysharkbite.cpp:141`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-082 | `TableCompactions.scans` read-only property (`pysharkbite.cpp:142`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getScans()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-031. |
| SB-STAT-032 | `Compacting.getRunning()` (`pysharkbite.cpp:122`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-083 | `Compacting.running` read-only property (`pysharkbite.cpp:123`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getRunning()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-032. |
| SB-STAT-033 | `Compacting.getQueued()` (`pysharkbite.cpp:124`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) |  |
| SB-STAT-084 | `Compacting.queued` read-only property (`pysharkbite.cpp:125`) | — | — | `examples/pythonstats.py` | Intentional divergence (approval required) | Independently registered read-only property (`def_readonly`), not an alias of `getQueued()`: pybind11 exposes it as a data attribute, so both spellings exist and a shim owes both. Same status and rationale as SB-STAT-033. |
| SB-STAT-034 | `RecoveryStatus.getName()` (`pysharkbite.cpp:129`) | — | — | — | Intentional divergence (approval required) | [SB-DIV-016](#sec-26). |
| SB-STAT-044 | `RecoveryStatus.name` property (`pysharkbite.cpp:130`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-045 | `RecoveryStatus.getRuntime()` (`pysharkbite.cpp:131`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-046 | `RecoveryStatus.runtime` property (`pysharkbite.cpp:132`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-047 | `RecoveryStatus.getProgress()` (`pysharkbite.cpp:133`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-048 | `RecoveryStatus.progress` property (`pysharkbite.cpp:134`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-035 | `DeadServer.getServer()` (`pysharkbite.cpp:187`) | — | — | — | Intentional divergence (approval required) | `DeadServer` is one of the three types Accumulo `f0841e4` deleted outright ([§14](#sec-14)). |
| SB-STAT-049 | `DeadServer.server` property (`pysharkbite.cpp:188`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-050 | `DeadServer.getLastContact()` (`pysharkbite.cpp:189`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-051 | `DeadServer.last_contact` property (`pysharkbite.cpp:190`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-052 | `DeadServer.getStatus()` (`pysharkbite.cpp:191`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-053 | `DeadServer.status` property (`pysharkbite.cpp:192`) | — | — | — | Intentional divergence (approval required) | |
| SB-STAT-036 | `CoordinatorGoalState` enum: `CLEAN_STOP`, `SAFE_MODE`, `NORMAL` (`pysharkbite.cpp:194-197`) | — | — | — | Intentional divergence (approval required) | Enumerated as one row deliberately: the contract is the ordinal set as a whole, because each value's numeric ordinal must match Accumulo's wire encoding and they are only meaningful together. Every value is listed in this row; a shim that ships a subset, or renumbers any value, fails this row. |
| SB-STAT-037 | `CoordinatorState` enum: `INITIAL`, `HAVE_LOCK`, `SAFE_MODE`, `NORMAL`, `UNLOAD_METADATA_TABLETS`, `UNLOAD_ROOT_TABLET`, `STOP` (`pysharkbite.cpp:200-207`) | — | — | — | Intentional divergence (approval required) | Enumerated as one row deliberately: the contract is the ordinal set as a whole, because each value's numeric ordinal must match Accumulo's wire encoding and they are only meaningful together. Every value is listed in this row; a shim that ships a subset, or renumbers any value, fails this row. |
| SB-STAT-038 | `pybind11::dynamic_attr()` on all status classes (`pysharkbite.cpp:108,121,128,136,144,158,186,209`) | — | — | — | Missing Go | Users may attach arbitrary attributes to status objects; a shim built on `__slots__` would break that. Moot while [SB-DIV-016](#sec-26) stands, since there would be no status objects to decorate — but it is a Python-object property rather than an upstream capability, so it stays a Python-layer gap rather than a divergence. |

<a id="sec-15"></a>

## 15. Matrix: RFile, iterators, and streams (`SB-RFILE`)

Shoal now publishes an `rfile` package that is the Go form of this section's
surface: `Create` (`rfile/writer.go:50`) writes a file, `Open`
(`rfile/reader.go:70`) and `OpenSequential` (`rfile/reader.go:81`) read one,
`OpenMany` (`rfile/reader.go:145`) merges several, `KeyValueIterator`
(`rfile/reader.go:28`) is the cursor contract they all satisfy, and `Seekable`
(`rfile/seekable.go:28`) is the relocation every read takes. Keys and values
cross the boundary as copies, every operation takes a context, and the package
documents its lifecycle, cancellation, concurrency, and binary-data behavior.
One capability is still absent rather than merely unexported: the writer emits
the default locality group only ([SB-RFILE-012](#sec-15)).

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-RFILE-001 | `RFileOperations.openForWrite(path) -> SequentialRFile` (`pysharkbite.cpp:589`, `include/data/constructs/rfile/RFileOperations.h:43`) | `Create(ctx, path string, opts WriterOptions) (*Writer, error)` (`rfile/writer.go:50`) | `shoal_rfile_writer_create`, `shoal_rfile_writer_free` | `TestWriteThenReadSequentially`, `TestWriterRejectsOutOfOrderAppendAndUseAfterClose`, `TestWriterCopiesTheCallersBuffers`, `TestCreateRejectsAnUnsupportedCodecWithoutTouchingTheFile` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:35-43`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The public `rfile` package now exports Shoal's writer. `Create` validates the options first, so an unregistered codec is `ErrUnsupportedCodec` (`rfile/errors.go:24`) before anything is opened; it then creates or truncates the path and the bytes on disk are a valid RFile only once `Writer.Close` (`rfile/writer.go:145`) returns without error, which is the same two-step contract as `openForWrite` plus `close`. No ABI entry point exports it yet. |
| SB-RFILE-002 | `RFileOperations.sequentialRead(path) -> SequentialRFile` (`pysharkbite.cpp:590` → `openSequential`, `RFileOperations.h:46`) | `OpenSequential(ctx, path string) (*Reader, error)` (`rfile/reader.go:81`) | `shoal_rfile_reader_open_sequential`, `shoal_rfile_reader_free` | `TestWriteThenReadSequentially`, `TestOpenManyRejectsEmptyAndMissingPaths` (`rfile/rfile_test.go`); `TestReadsEveryLocalityGroup` (`rfile/multigroup_internal_test.go`); `test/python/TestRFileWrites.py:54`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Note the Python name (`sequentialRead`) differs from the C++ name (`openSequential`); the Python name is the contract, and the shim owns that spelling. The reader comes back already positioned on the first entry, so `HasTop` is true before any `Seek`, and `Seek` may still reposition it afterwards exactly as Sharkbite's `SequentialRFile` allows. Every locality group of the file is opened and merged, so a file written by Accumulo with a named group reads completely. |
| SB-RFILE-003 | `RFileOperations.randomSeek(path) -> RFile` (`pysharkbite.cpp:588` → `open`, `RFileOperations.h:45`) | `Open(ctx, path string) (*Reader, error)` (`rfile/reader.go:70`) | `shoal_rfile_reader_open`, `shoal_rfile_reader_free` | `TestOpenRequiresSeekBeforeReading`, `TestReaderRejectsUseAfterClose` (`rfile/rfile_test.go`); `TestReadsEveryLocalityGroup` (`rfile/multigroup_internal_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns a **raw owning pointer** in C++ (`RFile *`), unlike the other four overloads. See [§21](#sec-21). The Go reader owns every handle it opened and releases them in `Reader.Close` (`rfile/reader.go:373`), so the ownership question the raw pointer raises does not arise. Like Sharkbite's `RFile` it starts unpositioned: every accessor reports `ErrNoTop` (`rfile/errors.go:13`) until the first `Seek`. Like the sequential form it merges every locality group in the file. |
| SB-RFILE-004 | `RFileOperations.openManySequential(paths, versions=0, withDeletes=False, propogate=True, maxtimestamp=0)` (`pysharkbite.cpp:591`, `RFileOperations.h:55`) | `OpenMany(ctx, paths []string, opts MergeOptions) (*Reader, error)` (`rfile/reader.go:145`) with `MergeOptions` (`rfile/reader.go:97`) | `shoal_rfile_reader_open_many`, `shoal_rfile_merge_config` | `TestOpenManyMergesFilesInKeyOrder`, `TestOpenManyLimitsVersions`, `TestOpenManyAppliesDeletes`, `TestOpenManyAgesOffOldEntries`, `TestOpenManyClosesEveryFileItOpened` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Defaults are C++-side and not declared to pybind11, so all five arguments are required from Python. Three of the five are defective in the pinned release and are shimmed rather than reproduced: `versions` is ignored as a number ([SB-UNSAFE-030](#sec-21)), so `MergeOptions.Versions` (`rfile/reader.go:104`) honours the count and `Versions: 1` reproduces Sharkbite; `propogate=True` disables delete handling altogether ([SB-UNSAFE-031](#sec-21)), so `MergeOptions.Propagate` (`rfile/reader.go:125`) instead keeps the tombstone and suppresses what it covers, as Accumulo's `DeletingIterator` does; and `maxtimestamp` is an *earliest-allowed* timestamp whose every non-zero value throws ([SB-UNSAFE-032](#sec-21)), so `MergeOptions.MinTimestamp` (`rfile/reader.go:138`) implements the age-off the argument was meant to configure. The fourth argument, `withDeletes`, maps to `MergeOptions.ApplyDeletes` (`rfile/reader.go:111`) with Sharkbite's meaning: false merges without delete handling at all. |
| SB-RFILE-005 | Class-level binding form: `RFileOperations` is registered with `pybind11::class_` and its four methods use `.def`, not `.def_static` (`pysharkbite.cpp:587`), yet every caller invokes them as `RFileOperations.openForWrite(path)` | n/a | n/a | `test/python/TestRFileWrites.py:35` | Behavior mismatch | Binding-form contract, not a member contract: the four members themselves are [SB-RFILE-003](#sec-15) (`randomSeek`), [SB-RFILE-001](#sec-15) (`openForWrite`), [SB-RFILE-002](#sec-15) (`sequentialRead`), and [SB-RFILE-004](#sec-15) (`openManySequential`). They work only as unbound class calls; calling them on an instance fails. The shim should expose real `@staticmethod`s, which is a superset of the working call form. |
| SB-RFILE-006 | `RFile.seek(StreamRelocation)` (`pysharkbite.cpp:499` → `relocate`) | `Reader.Seek(ctx, target StreamRelocation) error` (`rfile/reader.go:281`) | `shoal_rfile_reader_seek` | `TestOpenRequiresSeekBeforeReading`, `TestSeekRepositionsAndHonorsColumnFamilies`, `TestSeekRejectsAnInvalidRelocation`, `TestSeekHonorsRowBoundsForKeysWithFamilies`, `TestSeekHonorsRowBoundsForEmptyFamiliesAndTimestamps` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:59`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Takes the same relocation object Sharkbite's `relocate` takes. A missing or range-less relocation is `ErrInvalidSeekable` (`rfile/errors.go:17`) rather than a null dereference, and seeking is repeatable: the reader may be repositioned any number of times. Row bounds are resolved to absolute key bounds through `Range.KeyBounds` (`accumulo/range.go:143`), so an exclusive start row skips the whole row and an inclusive end row keeps all of it. The boundary is the *first possible key* of the row, carrying the maximum timestamp because Accumulo sorts timestamps descending: a cell with an empty column family and a positive timestamp sorts before `Key{Row: R, Timestamp: 0}` and would otherwise fall on the wrong side of both bounds. |
| SB-RFILE-007 | `RFile.hasNext() -> bool` (`pysharkbite.cpp:500`) | `Reader.HasTop() bool` (`rfile/reader.go:306`) | `shoal_rfile_reader_has_top` | `TestWriteThenReadSequentially`, `TestOpenRequiresSeekBeforeReading` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:61`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The same predicate under Accumulo's name. It is false before the first `Seek` and after `Close`, where Sharkbite's `hasNext` on an unpositioned or closed RFile is undefined. |
| SB-RFILE-008 | `RFile.getTop() -> KeyValue` (`pysharkbite.cpp:501`) | `Reader.Top() (Entry, error)` (`rfile/reader.go:317`) with `Entry` (`rfile/entry.go:17`) | `shoal_rfile_reader_top`, owned `shoal_rfile_entry_result` | `TestWriteThenReadSequentially`, `TestValuesAndKeysAreCopies`, `TestTombstonesRoundTrip` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns the key, the value and the tombstone flag in one value, mirroring Sharkbite's `KeyValue` whose `Key` carries `isDeleted`. The bytes are copies, so the entry stays valid after `Next` and after `Close`, which the C++ `shared_ptr` does not promise for the key buffers it aliases. |
| SB-RFILE-009 | `RFile.next()` (`pysharkbite.cpp:503`) | `Reader.Next(ctx) error` (`rfile/reader.go:355`) | `shoal_rfile_reader_next` | `TestWriteThenReadSequentially`, `TestOperationsHonorCancelledContext` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:64`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Raw `next` with no `StopIteration` conversion (unlike `SequentialRFile.next`). Go reports `ErrNoTop` (`rfile/errors.go:13`) at exhaustion instead of advancing a spent cursor, so the shim decides whether this spelling raises `StopIteration` ([SB-RFILE-015](#sec-15)) or stays silent. It is also the cancellation point: a cancelled context stops the walk. |
| SB-RFILE-010 | `RFile.close()` (`pysharkbite.cpp:502`) | `Reader.Close() error` (`rfile/reader.go:373`) | `shoal_rfile_reader_close`, `shoal_rfile_reader_free` | `TestReaderRejectsUseAfterClose`, `TestOpenManyClosesEveryFileItOpened`, `TestCloseDuringConcurrentReadsIsSafe` (`rfile/rfile_test.go`); `TestReaderCloseRepeatsItsFailure` (`rfile/close_internal_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Idempotent, and every later operation reports `ErrClosed` (`rfile/errors.go:8`) instead of touching a released handle. It closes every file the reader opened, including all inputs of [SB-RFILE-004](#sec-15), a failed open closes the handles it had already taken, and a failed close is remembered so a later call cannot report success. |
| SB-RFILE-011 | `SequentialRFile.append(KeyValue) -> bool` (`pysharkbite.cpp:530`) | `Writer.Append(ctx, entry Entry) error` (`rfile/writer.go:87`) | `shoal_rfile_writer_append`, `shoal_rfile_entry` | `TestWriteThenReadSequentially`, `TestWriterRejectsOutOfOrderAppendAndUseAfterClose`, `TestWriterCopiesTheCallersBuffers`, `TestBinaryDataRoundTrips`, `TestTombstonesRoundTrip`, `TestTombstoneAndLiveEntryAtOneTimestampAppendInOrder` (`rfile/rfile_test.go`); `TestFailedAppendIsTerminal` (`rfile/close_internal_test.go`); `test/python/TestRFileWrites.py:41`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | **Safe shim for unsafe C++ behavior:** Sharkbite returns `bool` and never checks ordering ([SB-UNSAFE-033](#sec-21)), so an out-of-order append silently writes a malformed RFile; Go rejects it with `ErrOutOfOrder` (`rfile/errors.go:28`) before any byte is written. Appending after `Close` is `ErrClosed`, matching the `std::runtime_error` Sharkbite throws. Ordering uses Accumulo's full comparison including the deleted bit, so a tombstone and the live entry at one coordinate and timestamp append in that order. A failed append is terminal, because the entry has already reached the block stream: the error is latched, later appends return it, and `Close` reports it even when finalization succeeds, so a partial file is never committed as complete. The key and value are copied, so the caller may reuse its buffers, and every component is binary-safe including the tombstone flag of `Entry` (`rfile/entry.go:17`). |
| SB-RFILE-012 | `SequentialRFile.addLocalityGroup(...)` (`pysharkbite.cpp:529`) | `Writer.AddLocalityGroup(name string) error` (`rfile/writer.go:122`) — entry point only; reports `ErrLocalityGroupUnsupported` (`rfile/errors.go:33`) | — | `TestWriterRejectsOutOfOrderAppendAndUseAfterClose` (`rfile/rfile_test.go`) | Missing Go | The one row of this section with no Go capability, and the reason [SB-GAP-GO-005](#sec-23) stays open. Sharkbite closes the current group and starts a named one (`include/data/constructs/rfile/SequentialRFile.h:152-158`); Shoal's writer emits the default locality group only (`internal/rfile/writer.go:39-41`), so the public entry point exists and fails deterministically rather than accepting a group it would not write — a silent no-op would let a caller believe its families were separated on disk. Reading a multi-group file is already supported — every group is opened and merged ([SB-RFILE-002](#sec-15)) — so closing this row needs only multi-group *write* support in `internal/rfile`. Until then no other row of this section depends on it, because `append` auto-creates the default group (`src/data/constructs/rfile/SequentialRFile.cpp:182-184`). |
| SB-RFILE-013 | `SequentialRFile.seek(Seekable)` (`pysharkbite.cpp:524`) | `Reader.Seek(ctx, target StreamRelocation) error` (`rfile/reader.go:281`) | `shoal_rfile_reader_seek` | `TestSeekRepositionsAndHonorsColumnFamilies`, `TestWriteThenReadSequentially` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:59`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | One Go reader serves both Sharkbite reader classes: the difference between `RFile` and `SequentialRFile` in Python is where the cursor starts ([SB-RFILE-003](#sec-15) versus [SB-RFILE-002](#sec-15)), not a different seek contract. |
| SB-RFILE-014 | `SequentialRFile.hasNext()` (`pysharkbite.cpp:525`) | `Reader.HasTop() bool` (`rfile/reader.go:306`) | `shoal_rfile_reader_has_top` | `TestWriteThenReadSequentially` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:61`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The same method as [SB-RFILE-007](#sec-15); both Sharkbite classes publish the predicate. |
| SB-RFILE-025 | `SequentialRFile.getTop()` (`pysharkbite.cpp:526`) | `Reader.Top() (Entry, error)` (`rfile/reader.go:317`) | `shoal_rfile_reader_top`, owned `shoal_rfile_entry_result` | `TestWriteThenReadSequentially`, `TestTombstonesRoundTrip` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns the top key/value pair as one object, unlike the split `getTopKey`/`getTopValue`. `Entry` (`rfile/entry.go:17`) also carries the tombstone flag, which the split accessors cannot express because `accumulo.Key` has no delete bit. |
| SB-RFILE-026 | `SequentialRFile.getTopKey()` (`pysharkbite.cpp:527`) | `Reader.TopKey() (accumulo.Key, error)` (`rfile/reader.go:333`) | `shoal_rfile_reader_top_key` | `TestWriteThenReadSequentially`, `TestValuesAndKeysAreCopies` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:62`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The returned key is a copy and stays valid after the cursor advances. It is the scan-shaped `accumulo.Key`, so it carries no tombstone flag; a caller that needs one uses `Top` ([SB-RFILE-025](#sec-15)). |
| SB-RFILE-027 | `SequentialRFile.getTopValue()` (`pysharkbite.cpp:528`) | `Reader.TopValue() ([]byte, error)` (`rfile/reader.go:344`) | `shoal_rfile_reader_top_value`, owned `shoal_bytes_result` | `TestValuesAndKeysAreCopies`, `TestBinaryDataRoundTrips` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns a copy of the value bytes, valid after `Next` and after `Close`, and never transcoded — the defect [SB-UNSAFE-002](#sec-21) records for the Python `Value.get()`. |
| SB-RFILE-028 | `SequentialRFile.close()` (`pysharkbite.cpp:531`) | `Writer.Close() error` (`rfile/writer.go:145`) and `Reader.Close() error` (`rfile/reader.go:373`) | `shoal_rfile_writer_close`, `shoal_rfile_reader_close`, idempotent frees | `TestWriteThenReadSequentially`, `TestWriterRejectsOutOfOrderAppendAndUseAfterClose`, `TestReaderRejectsUseAfterClose` (`rfile/rfile_test.go`); `TestWriterCloseRepeatsItsFailure` (`rfile/close_internal_test.go`); `test/python/TestRFileWrites.py:43`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Required to finalise an RFile written through [SB-RFILE-011](#sec-15): the index and the trailer are written by `Close`, so a file whose `Close` did not return successfully is not a readable RFile. Idempotent on both the writer and the reader, and a failed append or finalization is remembered: every later `Close` returns the same error, so a truncated file is never reported as complete. |
| SB-RFILE-015 | `SequentialRFile.next()` raising `StopIteration` at exhaustion (`pysharkbite.cpp:532-536`) | `Reader.Next(ctx) error` (`rfile/reader.go:355`) | `shoal_rfile_reader_next` | `TestWriteThenReadSequentially` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:64`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The binding wraps `next` with a `hasNext` guard. Go's equivalent is the `ErrNoTop` (`rfile/errors.go:13`) sentinel: the shim raises `StopIteration` exactly when `Next` returns it, so the guard is unnecessary and the race between the guard and the advance cannot occur. |
| SB-RFILE-036 | `SequentialRFile.__next__` (`pysharkbite.cpp:537-542`) | `Reader.Next(ctx) error` (`rfile/reader.go:355`) | `shoal_rfile_reader_next` | `TestWriteThenReadSequentially` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Separate registration with an identical body; both spellings are published. Both map to the one Go method, so the shim registers two Python names on one implementation. |
| SB-RFILE-016 | `KeyValueIterator()` constructor (`pysharkbite.cpp:507`) | `KeyValueIterator` (`rfile/reader.go:28`) | owned `shoal_rfile_reader` iterator handle | `TestReaderSatisfiesTheIteratorContract` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Sharkbite lets Python subclass this to implement iterators. Go publishes the contract as an exported interface that any type can implement and that `*Reader` satisfies; an interface has no constructor, so the Python base-class object itself is shim-owned state. Server-side Python iterators are a different, unsolved problem ([SB-GAP-GO-008](#sec-23), [SB-SCAN-016](#sec-9)); this row is the client-side iterator contract only. |
| SB-RFILE-017 | `KeyValueIterator.seek(StreamRelocation)` (`pysharkbite.cpp:508` → `relocate`) | `KeyValueIterator.Seek(ctx, target)` (`rfile/reader.go:31`), implemented by `Reader.Seek` (`rfile/reader.go:281`) | `shoal_rfile_reader_seek` | `TestReaderSatisfiesTheIteratorContract`, `TestSeekRepositionsAndHonorsColumnFamilies` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Shoal's internal interface is a near-1:1 shape match, so the Go-parity work was export plus adaptation, not new logic: `internal/iterrt.SortedKeyValueIterator` stays internal, and the exported interface adds a context and returns errors instead of panicking. |
| SB-RFILE-029 | `KeyValueIterator.hasNext()` (`pysharkbite.cpp:509`) | `KeyValueIterator.HasTop()` (`rfile/reader.go:34`) | `shoal_rfile_reader_has_top` | `TestReaderSatisfiesTheIteratorContract` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Name differs (`hasNext` versus `HasTop`); the semantics are the same predicate, and the public interface keeps Accumulo's spelling because the row's Python name is owned by the shim. |
| SB-RFILE-030 | `KeyValueIterator.getTopKey()` (`pysharkbite.cpp:510`) | `KeyValueIterator.TopKey()` (`rfile/reader.go:40`) | `shoal_rfile_reader_top_key` | `TestReaderSatisfiesTheIteratorContract`, `TestValuesAndKeysAreCopies` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Sharkbite returns a `shared_ptr`, and Shoal's internal key is transient — invalidated by the next `Seek`/`Next` — so the exported accessor copies it. A caller can hold the key for as long as it likes. |
| SB-RFILE-031 | `KeyValueIterator.getTopValue()` (`pysharkbite.cpp:511`) | `KeyValueIterator.TopValue()` (`rfile/reader.go:43`) | `shoal_rfile_reader_top_value`, owned `shoal_bytes_result` | `TestReaderSatisfiesTheIteratorContract`, `TestValuesAndKeysAreCopies` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Same copy guarantee as SB-RFILE-030, and the value is returned as bytes, never transcoded. |
| SB-RFILE-032 | `KeyValueIterator.next()` raising `StopIteration` at exhaustion (`pysharkbite.cpp:512-516`) | `KeyValueIterator.Next(ctx)` (`rfile/reader.go:46`) | `shoal_rfile_reader_next` | `TestReaderSatisfiesTheIteratorContract`, `TestOperationsHonorCancelledContext` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | The binding wraps `Next` with a `hasNext` guard, so exhaustion surfaces as `StopIteration` rather than an error; the Go interface surfaces `ErrNoTop` (`rfile/errors.go:13`), which is what the shim translates. |
| SB-RFILE-033 | `KeyValueIterator.__next__` (`pysharkbite.cpp:517-521`) | `KeyValueIterator.Next(ctx)` (`rfile/reader.go:46`) | `shoal_rfile_reader_next` | `TestReaderSatisfiesTheIteratorContract` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Identical body to SB-RFILE-032; both spellings are published and both map to the one Go method. |
| SB-RFILE-018 | `Seekable(Range)` (`pysharkbite.cpp:581`) | `NewSeekable(keyRange *accumulo.Range) (Seekable, error)` (`rfile/seekable.go:36`) | `shoal_rfile_seekable_create`, owned `shoal_rfile_seekable` | `TestSeekableCopiesAndReportsItsRestriction`, `TestOpenRequiresSeekBeforeReading`, `TestSeekHonorsRowBoundsForKeysWithFamilies`, `TestSeekHonorsRowBoundsForEmptyFamiliesAndTimestamps` (`rfile/rfile_test.go`); `test/python/TestRFileWrites.py:56`; linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | A nil range is `ErrInvalidSeekable` (`rfile/errors.go:17`) instead of a null relocation, and `EntireFile` (`rfile/seekable.go:70`) is the unbounded relocation `sequentialRead` positions on. A row-bounded range covers the whole row on both sides, including cells with an empty column family, which `Range.KeyBounds` (`accumulo/range.go:143`) resolves into first-key-of-row boundaries. The scan-side `shoal_range` is not this type: it carries no column-family restriction, so the RFile ABI needs its own relocation. |
| SB-RFILE-019 | `Seekable(Range, list[str], bool)` — column families + inclusive flag (`pysharkbite.cpp:582`) | `NewSeekableColumns(keyRange *accumulo.Range, families [][]byte, inclusive bool) (Seekable, error)` (`rfile/seekable.go:51`) | `shoal_rfile_seekable_create` with copied binary families | `TestSeekRepositionsAndHonorsColumnFamilies`, `TestSeekableCopiesAndReportsItsRestriction` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Families are `[][]byte`, not `list[str]`, so a non-UTF-8 family is expressible — the same binary-safety fix [SB-UNSAFE-003](#sec-21) requires elsewhere. They are copied in and copied out, and both directions of the flag are exercised against a file holding two families. |
| SB-RFILE-020 | `Seekable.getRange()` (`pysharkbite.cpp:583`) | `Seekable.Range() *accumulo.Range` (`rfile/seekable.go:80`) | `shoal_rfile_seekable_get_range`, owned `shoal_range_result` | `TestSeekableCopiesAndReportsItsRestriction` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns the range the relocation was built with, so a relocation round-trips through the accessor unchanged. |
| SB-RFILE-034 | `Seekable.getColumnFamilies()` (`pysharkbite.cpp:584`) | `Seekable.ColumnFamilies() [][]byte` (`rfile/seekable.go:83`) | `shoal_rfile_seekable_column_family_count`, `shoal_rfile_seekable_get_column_family` | `TestSeekableCopiesAndReportsItsRestriction` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Returns the families supplied to the three-argument constructor ([SB-RFILE-019](#sec-15)), as a copy: mutating the result cannot change the relocation, and mutating the caller's original slices cannot either. |
| SB-RFILE-035 | `Seekable.isInclusive()` (`pysharkbite.cpp:585`) | `Seekable.Inclusive() bool` (`rfile/seekable.go:95`) | `shoal_rfile_seekable_is_inclusive` | `TestSeekableCopiesAndReportsItsRestriction` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Reports whether the column families are an inclusive or exclusive set. |
| SB-RFILE-021 | `StreamRelocation` opaque base type (`pysharkbite.cpp:578`) | `StreamRelocation` (`rfile/seekable.go:13`) | opaque owned `shoal_rfile_seekable` relocation handle | `TestSeekRepositionsAndHonorsColumnFamilies`, `TestSeekRejectsAnInvalidRelocation` (`rfile/rfile_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestRFileLifecycleCloseCancelsAndWaits`, `TestRFileLifecycleConcurrentRetainAndClose`, `TestRFileLifecycleDeadline` (`cmd/shoal-capi/rfile_export_test.go`) | Covered | Registered with no methods; exists only as the base class of `Seekable`. Go publishes it as the interface `Reader.Seek` accepts, so a caller may supply its own relocation and `Seekable` (`rfile/seekable.go:28`) is the built-in implementation. The Python type stays opaque, which the interface permits. |
| SB-RFILE-022 | Offline/standalone RFile compaction | `internal/offlinecompact.Run` + `cmd/shoal-offline-compact` — **internal/CLI** | — | `cmd/shoal-offline-compact/main_test.go` | Not required (rationale required) | Rationale: no Sharkbite equivalent (`examples/` has a `Compact` C++ target only). Recorded because it demonstrates Shoal already owns the RFile write path. |
| SB-RFILE-023 | RFile compression codecs (`compressor.h`, `zlibCompressor.h` included by `pysharkbite.cpp:33-34`) | `internal/rfile.WriterOptions.Codec` — **internal** | — | — | Not required (rationale required) | Rationale: the compressor classes are included but never bound to Python; not part of the Python contract. |
| SB-RFILE-024 | *(retired — moved)* `sharkbite.torch` datasets | see [§9.2](#sec-9) | see [§9.2](#sec-9) | see [§9.2](#sec-9) | Not required (rationale required) | audit #92: rationale — this row grouped the whole PyTorch surface inside the RFile section and hid its members. Tombstoned per [§4.1](#sec-4); the surface is enumerated as `SB-TORCH-001…SB-TORCH-009`. |

<a id="sec-16"></a>

## 16. Matrix: HDFS (`SB-HDFS`)

Sharkbite ships a full HDFS client to Python via vendored `libhdfs3`. Shoal has
an HDFS **storage backend** (`internal/storage/hdfs.Backend`,
`internal/storage/hdfs/hdfs.go:65`) built on `colinmarc/hdfs`, exercised by
`make test-hdfs` and `.github/workflows/hdfs-integration.yml`, but it is
internal, is a `storage.Backend` (open/read/create/list/remove), and is not a
Java-`DataInput`-compatible stream API.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-HDFS-001 | `Hdfs(namenode, port)` (`pysharkbite.cpp:568`) | `internal/storage/hdfs.Backend` with `Option`s — **internal** | — | `examples/hdfslist.py`; `internal/storage/hdfs/hdfs_test.go` | Missing Go | Shoal configures via `HADOOP_CONF_DIR`, not host+port. |
| SB-HDFS-002 | `Hdfs.list(path) -> list[HdfsDirEnt]` (`pysharkbite.cpp:576`) | `storage.Lister.List` — **internal** | — | `examples/hdfslist.py` | Missing Go | |
| SB-HDFS-003 | `Hdfs.write(path) -> HdfsOutputStream` (`pysharkbite.cpp:569`) | `storage.WritableBackend.Create` — **internal** | — | `examples/hdfswrite.py` | Missing Go | |
| SB-HDFS-004 | `Hdfs.read(path) -> HdfsInputStream` (`pysharkbite.cpp:570`) | `storage.Backend.Open` → `storage.File` (`ReaderAt`) — **internal** | — | `examples/hdfswrite.py` | Missing Go | Shoal exposes `ReaderAt`, not a sequential typed stream. |
| SB-HDFS-005 | `Hdfs.remove(path, recursive)` (`pysharkbite.cpp:571`) | `storage.Remover.Remove` — **internal** | — | `examples/hdfswrite.py` | Missing Go | |
| SB-HDFS-006 | `Hdfs.rename(from, to)` (`pysharkbite.cpp:572`) | — | — | — | Missing Go | |
| SB-HDFS-007 | `Hdfs.move(from, to)` (`pysharkbite.cpp:573`) | — | — | — | Missing Go | |
| SB-HDFS-008 | `Hdfs.chown(path, owner, group)` (`pysharkbite.cpp:574`) | — | — | — | Missing Go | |
| SB-HDFS-009 | `Hdfs.mkdir(path)` (`pysharkbite.cpp:575`) | — | — | — | Missing Go | |
| SB-HDFS-010 | `HdfsDirEnt.getName()` (`pysharkbite.cpp:544`) | — | — | `examples/hdfslist.py` | Missing Go | |
| SB-HDFS-015 | `HdfsDirEnt.getOwner()` (`pysharkbite.cpp:545`) | — | — | — | Missing Go | |
| SB-HDFS-016 | `HdfsDirEnt.getGroup()` (`pysharkbite.cpp:546`) | — | — | — | Missing Go | |
| SB-HDFS-017 | `HdfsDirEnt.getSize()` (`pysharkbite.cpp:547`) | — | — | — | Missing Go | |
| SB-HDFS-018 | `HdfsDirEnt.__str__` (`pysharkbite.cpp:548-550`) | — | — | — | Missing Go | Exact format is `owner group size name`, space-separated; user-visible. |
| SB-HDFS-011 | `HdfsOutputStream.writeShort(v)` (`pysharkbite.cpp:553`) | `internal/rfile/wire` Java `DataOutput` primitives — **internal** | — | — | Missing Go | Big-endian two-byte write. |
| SB-HDFS-019 | `HdfsOutputStream.writeInt(v)` (`pysharkbite.cpp:557`) | `internal/rfile/wire` — **internal** | — | — | Missing Go | Big-endian four-byte write. |
| SB-HDFS-020 | `HdfsOutputStream.writeLong(v)` (`pysharkbite.cpp:554`) | `internal/rfile/wire` — **internal** | — | — | Missing Go | Big-endian eight-byte write. |
| SB-HDFS-021 | `HdfsOutputStream.writeString(s)` (`pysharkbite.cpp:556`) | `internal/rfile/wire` — **internal** | — | `examples/hdfswrite.py` | Missing Go | Length-prefixed Java `DataOutput` string framing, not raw bytes. |
| SB-HDFS-022 | `HdfsOutputStream.write(buf, len)` (`pysharkbite.cpp:555`) | `internal/storage` `Writer` — **internal** | — | — | Missing Go | Raw byte write; the only binary-safe member of this class. |
| SB-HDFS-012 | `HdfsInputStream.readShort()` (`pysharkbite.cpp:560`) | `internal/rfile/wire` Java `DataInput` primitives — **internal** | — | — | Missing Go | |
| SB-HDFS-023 | `HdfsInputStream.readInt()` (`pysharkbite.cpp:564`) | `internal/rfile/wire` — **internal** | — | — | Missing Go | |
| SB-HDFS-024 | `HdfsInputStream.readLong()` (`pysharkbite.cpp:561`) | `internal/rfile/wire` — **internal** | — | — | Missing Go | |
| SB-HDFS-025 | `HdfsInputStream.readString()` (`pysharkbite.cpp:563`) | `internal/rfile/wire` — **internal** | — | `examples/hdfswrite.py` | Missing Go | Reads the length-prefixed framing written by [SB-HDFS-021](#sec-16). |
| SB-HDFS-026 | `HdfsInputStream.readBytes(buf, len)` (`pysharkbite.cpp:562`) | `internal/storage` `File.ReadAt` — **internal** | — | — | Missing Go | Shoal exposes positional `ReadAt` rather than a sequential cursor. |
| SB-HDFS-013 | HDFS cancellation and deadlines | `internal/storage` backends take `context.Context`; issue [#10](https://github.com/phrocker/shoal-oss/issues/10) / PR [#85](https://github.com/phrocker/shoal-oss/pull/85) complete propagation | — | `make test-hdfs`; `.github/workflows/hdfs-integration.yml` | Missing Go | Sharkbite has no cancellation at all. Landing #10 first prevents the Python layer from inheriting an uninterruptible HDFS path. |
| SB-HDFS-014 | Kerberos / secure HDFS | `internal/storage/hdfs` live tests (issue [#9](https://github.com/phrocker/shoal-oss/issues/9), merged) | — | `internal/storage/hdfs/hdfs_live_test.go` | Missing Go | Sharkbite's vendored `libhdfs3` supports GSSAPI; parity claims need an explicit statement. |

<a id="sec-17"></a>

## 17. Matrix: logging (`SB-LOG`)

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-LOG-001 | `LoggingConfiguration.enableDebugLogger()` static (`pysharkbite.cpp:66`) | — (no exported logging control in `accumulo/`) | — | `test/python/TestRanges.py:12` (commented), `test/python/TestHedgedReads.py:12` | Missing Go | Global side effect: enables debug logging for all classes. |
| SB-LOG-002 | `LoggingConfiguration.enableTraceLogger()` static (`pysharkbite.cpp:67`) | — | — | `test/python/TestRFileWrites.py:50`; `test/python/TestHedgedReads.py:12` | Missing Go | |
| SB-LOG-003 | Structured/injectable logging | `internal/metadata.Walker.WithLogger`, `internal/obs` — **internal** | — | — | Behavior mismatch | Shoal's logging is injected per component rather than toggled globally. A compatible shim needs a process-global switch that maps onto whatever the public Go API eventually exposes. |

<a id="sec-18"></a>

## 18. Matrix: exceptions and error mapping (`SB-ERR`)

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-ERR-001 | `pysharkbite.ClientException` registered as a Python exception with a translator (`pysharkbite.cpp:613-621`) | Error values, no exception type | `shoal_error_source`, `shoal_error_compatibility`, and immutable class names (`capi/include/shoal.h`) | `TestCompatibilityErrorClasses`, `TestCompatibilityErrorClassificationIsConcurrent` (`cmd/shoal-capi/error_compatibility_test.go`); linked C11/C++11 `TestSharedLibraryCABI` | Covered | ABI 1.12.0 classifies mapped application failures as Sharkbite `ClientException` and exposes that class separately from `RuntimeError`; a binding can create the Python exception type without parsing status numbers or messages. |
| SB-ERR-002 | `ClientException::getErrorCode()` and the `CLIENT_ERROR_CODES` table (0–13, including `TABLE_NOT_FOUND`, `RANGE_NOT_SPECIFIED`, `SCANNER_ALREADY_STARTED`) (`include/data/exceptions/ClientException.h:39,50-51`) — **C++ surface only**: `pysharkbite.cpp` registers the exception type (`pysharkbite.cpp:613`) and binds no `getErrorCode`, so no Python program can read a code | Sentinel errors (`accumulo/errors.go:5-56`) | `shoal_status` enum, 0–18 + 255 (`capi/include/shoal_types.h`) | `test/vandv/invalidscans.h` asserts `TABLE_NOT_FOUND` and `RANGE_NOT_SPECIFIED` by value; `main()` in `capi/tests/lifecycle.c` asserts `shoal_status` values | Behavior mismatch | audit #2: scoped, not dismissed. **Python scope:** none — the contract there is the exception *type* ([SB-ERR-001](#sec-18)), and a shim owes no numeric code. **C++ scope:** required — the code sets are different (0–13 versus 0–18 + 255, with no shared meaning per value), and `test/vandv/invalidscans.h` asserts specific numbers, so any C++ compatibility layer needs an explicit mapping table. The mapping test is [SB-GAP-T-005](#sec-23) and the C++ driver port is [SB-GAP-T-001](#sec-23) / [SB-CPP-010](#sec-19). |
| SB-ERR-003 | `pysharkbite.TApplicationException` (Thrift) registered (`pysharkbite.cpp:612`) | Thrift types are internal by design (`accumulo/batch_writer.go:116` comment) | Mapped to `shoal_status` | `TestStatusForWriterErrors` (`cmd/shoal-capi/writer_export_test.go`) | Intentional divergence (approval required) | Shoal deliberately never leaks generated Thrift types. Any Sharkbite user catching `TApplicationException` will not see it. Requires approval plus a documented replacement (`ClientException` with a transport status). |
| SB-ERR-004 | Table not found | `ErrTableNotFound` (`accumulo/errors.go:15`) | `SHOAL_STATUS_NOT_FOUND` (9) (`capi/include/shoal_types.h`) | `TestTableMutationsMapErrorsAndLifecycle` (`accumulo/table_admin_test.go:301`); `TestStatusForTableAdministrationErrors` (`cmd/shoal-capi/table_admin_export_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The raw Go and C layers now both surface the stable not-found mapping, but Sharkbite raises `ClientException` with its own object/code contract. The shim still has to translate the status into Sharkbite's exception shape. |
| SB-ERR-005 | Permission denied / `ThriftSecurityException` (`test/vandv/testSecurityOperations.h`) | `ErrPermissionDenied` (`accumulo/errors.go:46`) | `SHOAL_STATUS_PERMISSION_DENIED` (10) | — | Behavior mismatch | No named Shoal test asserts this mapping end to end; live-cluster conformance required. |
| SB-ERR-006 | `NotServingException` → transparent relocation (`include/data/exceptions/NotServingException.h:22`) | `isStaleScanError` + single retry (`accumulo/scanner.go:350`) | Implicit — no separate ABI surface | `TestScannerRetriesNotServingAssignmentOnce` (`accumulo/scanner_test.go:294`) | Behavior mismatch | audit #92: both hide the exception from the caller, but Sharkbite hides it for as many relocations as occur while Shoal hides exactly one. The second relocation in a single scan becomes a user-visible error. Paired with [SB-SCAN-026](#sec-9). |
| SB-ERR-007 | `IllegalArgumentException` (`include/data/exceptions/IllegalArgumentException.h:23`) | Plain `errors.New`/`fmt.Errorf` validation errors | `SHOAL_STATUS_INVALID_ARGUMENT` (1) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Shim must map argument validation to the Sharkbite class, not Python `ValueError`, for pinned `except` clauses to keep working. |
| SB-ERR-008 | `IllegalStateException` (`include/data/exceptions/IllegalStateException.h:23`) | `ErrBatchWriterClosed`, `ErrConnectorClosed` (`accumulo/batch_writer.go:27`, `accumulo/errors.go:51`) | `SHOAL_ERROR_SOURCE_ILLEGAL_STATE_EXCEPTION` with Python-facing `SHOAL_ERROR_COMPATIBILITY_RUNTIME_ERROR` | `TestCompatibilityErrorClasses`; linked C11/C++11 `TestSharedLibraryCABI` exercises errors after close | Covered | Closed-handle failures retain the Sharkbite source name `IllegalStateException`, while the compatibility class is `RuntimeError` because upstream did not register this C++ type with pybind11. |
| SB-ERR-009 | `HDFSException` (`include/data/exceptions/HDFSException.h:24`) | `storage.ErrNotFound`, `storage.ErrReadOnly` — **internal** | — | — | Missing Go | Blocked with the rest of HDFS ([§16](#sec-16)). |
| SB-ERR-010 | `VisibilityParseException` with `terms`/`offset` fields (`include/data/exceptions/VisibilityParseException.h:23`) | `VisibilityParseError` (`accumulo/column_visibility.go:19`), `ErrVisibilityParse` (`accumulo/column_visibility.go:13`) | — | `TestColumnVisibilityRejectsMalformedExpressions`, `TestColumnVisibilityRejectsAnUnbalancedCloseInsteadOfTruncating` (`accumulo/column_visibility_test.go`) | Missing C ABI | Field-level detail (offset within the expression) is part of the contract, and Go now carries it: `*VisibilityParseError` exposes `Reason`, `Terms` — the expression the parser was reading, which is what the pinned constructor stores under that name — and `Offset`, and it unwraps to `ErrVisibilityParse` so `errors.Is` works. **Safe shim for unsafe C++ behavior:** the pinned exception keeps `terms` and `offset` private with no accessors and its `what()` is neither `const` nor `noexcept`, so it does not override `std::exception::what()` and the message is lost through the usual catch idiom ([SB-UNSAFE-044](#sec-21)). |
| SB-ERR-011 | `IterationInterruptedException` (`include/data/exceptions/InterationInterruptedException.h:23` — filename misspelled upstream) | `context.Canceled` propagation | `SHOAL_ERROR_SOURCE_ITERATION_INTERRUPTED_EXCEPTION` with Python-facing `SHOAL_ERROR_COMPATIBILITY_RUNTIME_ERROR` | `TestScannerCancellationStillClosesServerScan`; `TestCompatibilityErrorClasses`; linked C11/C++11 cancellation assertions in `TestSharedLibraryCABI` | Covered | Cancellation retains the source name `IterationInterruptedException`; its Python-facing class is `RuntimeError`, matching pybind11 translation for the unregistered upstream type. |
| SB-ERR-012 | `APIException` (`include/interconnect/exceptions/APIException.h:23`) | — | — | — | Not required (rationale required) | Rationale: thrown only from the flat C API glue ([§19](#sec-19)), which is not part of the Python contract. |
| SB-ERR-013 | `JavaException` (`include/jni/JavaException.h:36`) | — | — | — | Not required (rationale required) | Rationale: only reachable through the JNI iterator build (`PYTHON_ITERATOR_SUPPORT`), which is tracked by SB-SCAN-016. |
| SB-ERR-014 | Pinned Python code catches `ClientException` and `RuntimeError` as **separate** entries of one `except` tuple (`test/python/TestBadOperations.py:63,71,79,94`) | n/a | `shoal_error_compatibility` returns distinct `CLIENT_EXCEPTION` or `RUNTIME_ERROR` classes | `TestCompatibilityErrorClasses`, `TestCompatibilityErrorClassificationIsConcurrent`; linked C11/C++11 `TestSharedLibraryCABI` | Covered | The stable classification is explicit and allocation-free: mapped application failures select `ClientException`, while validation, closed, cancelled, deadline, operation, and internal failures select `RuntimeError`; no inheritance assumption or message parsing is required. |
| SB-ERR-015 | Errors carry no server identity | `MutationRejectionError.Server`, `BatchWriterCleanupError.Server`, `CleanupError.ScanID` (`accumulo/batch_writer.go:117,136`, `accumulo/scanner.go:117`) | `shoal_*_view.server` fields | `TestMalformedUpdateErrorsIncludeTabletServer` (`accumulo/batch_writer_test.go:993`) | Not required (rationale required) | Rationale: Shoal superset; additive detail on the exception object. |
| SB-ERR-016 | Unregistered C++ exceptions surface in Python as generic `RuntimeError` — pybind11's default translation for anything not passed to `register_exception`/`exception<>` (`pysharkbite.cpp:612-613` registers exactly two types) | Typed errors and sentinels throughout `accumulo/` (`accumulo/errors.go:5-56`) | `shoal_status` + `shoal_error_message` (`capi/include/shoal.h`) | `test/python/TestBadOperations.py:63,71,79,94` catches `RuntimeError`, `TypeError`, and `NameError` alongside `ClientException` | Behavior mismatch | Only `ClientException` and `TApplicationException` are registered, so every other C++ throw — `IllegalArgumentException`, `IllegalStateException`, `HDFSException`, `VisibilityParseException`, `IterationInterruptedException`, `NotServingException`, `APIException`, `JavaException`, and any `std::runtime_error` — reaches Python as a bare `RuntimeError` whose only distinguishing feature is its message string. Pinned test code relies on that. A shim that raises precise typed exceptions everywhere is *more* correct but silently changes which `except` clause fires; a shim that raises `ClientException` for these paths changes it differently. The mapping must be enumerated and tested, not left to the binding layer's default. |

<a id="sec-19"></a>

## 19. Matrix: C++ public surfaces (`SB-CPP`)

**Scope: every public C++ declaration Sharkbite ships, not only the ones its
tests and examples happen to exercise.** Revision 4 scoped this section to
"surfaces used by tests and examples", which let unexercised public members
escape enumeration; that wording is withdrawn. A header under `include/` is
published to Sharkbite consumers whether or not the pinned tree calls it, so it
carries a compatibility obligation.

These rows do not gate the Python wheel — that gate is [§5](#sec-5) through
[§18](#sec-18) — but they do gate any claim that Shoal replaces Sharkbite as a
whole, and they are `required` rows under [§2.2](#sec-2) unless individually
marked `Not required` with a rationale.

The curated rows below cover the flat C API, the `TableOperations` factories,
`AuthInfo`, `Sink`, `Source`, and the test infrastructure — the surfaces with
named Shoal counterparts or named Sharkbite tests. The complete
declaration-level enumeration follows in [§19.2](#sec-19-2); where a member
appears in both, the curated row is normative and the generated row
cross-references it.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-CPP-001 | `create_connector(instance, zks, username, password)` (`include/extern/accumulo.h:108`) | `accumulo.NewConnector` (`accumulo/connector.go:36`) | `shoal_connector_create` (`capi/include/shoal.h`) | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`); `main()` in `capi/tests/lifecycle.c` uses `SHOAL_BOOTSTRAP_STATIC` | Behavior mismatch | The flat C entry point is ZooKeeper-only — four `char *` arguments, no bootstrap discriminator — while the only ABI construction test uses the static bootstrap. Same gap as [SB-CONN-001](#sec-7). |
| SB-CPP-015 | `free_connector(connector) -> int` (`include/extern/accumulo.h:111`) | `Connector.Close` (`accumulo/connector.go:138`) | `shoal_connector_free` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` frees twice | Behavior mismatch | Sharkbite returns an `int` status and leaves the caller's pointer dangling; `shoal_connector_free` returns `void`, performs a best-effort close, and NULLs the variable. A port must stop checking the return value and stop reusing the pointer. |
| SB-CPP-002 | `open_table(connector, tableName)` (`include/extern/accumulo.h:113`) | `Connector.TableByName` (`accumulo/discovery.go:110`) and table methods on `Connector` | `shoal_connector_list_tables`, `shoal_connector_table_exists`, and table-name arguments on administration/scanner/writer exports (`capi/include/shoal.h`) | `TestDiscoveryTableLookupAndRouting` (`accumulo/discovery_test.go:312`); `TestTableAdministrationListingAndExistence` (`accumulo/table_admin_test.go:14`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite returns a retained `TableOps*` handle. Shoal exposes the same lookup and operation capabilities without a table handle, so a compatibility shim must retain the selected name and compose flat connector calls. |
| SB-CPP-016 | `create_table(connector, tableName)` (`include/extern/accumulo.h:115`) | `Connector.CreateTable` (`accumulo/table_admin.go:76`) | `shoal_connector_create_table` (`capi/include/shoal.h`) | `TestTableAdministrationLifecycleAndCancellation` (`accumulo/table_admin_test.go:63`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite creates **and** opens in one call, returning the handle. Shoal only creates via a connector-scoped call, so a shim must return a synthetic `TableOps*` bound to the connector and table name. |
| SB-CPP-017 | `drop_table(tableOps) -> int` (`include/extern/accumulo.h:117`) | `Connector.DeleteTable` (`accumulo/table_admin.go:96`) | `shoal_connector_delete_table` (`capi/include/shoal.h`) | `TestTableAdministrationLifecycleAndCancellation` (`accumulo/table_admin_test.go:63`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite drops via a `TableOps*` handle; Shoal deletes through a connector-scoped call that still needs the table name. A shim must retain the owner connector and resolved name in the synthetic handle. |
| SB-CPP-018 | `free_table(tableOps) -> int` (`include/extern/accumulo.h:119`) | n/a (no handle type) | — | — | Missing C ABI | Releases the handle without dropping the table; the name is easily confused with SB-CPP-017 and a port must not merge them. |
| SB-CPP-003 | `createMutation(row)` (`include/extern/accumulo.h:123`) | `NewMutation` (`accumulo/mutation.go:21`) | `shoal_mutation_create` (`capi/include/shoal.h`) | `TestMutationPublicModel` (`accumulo/mutation_test.go:8`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | `char *` row versus length-carrying `shoal_bytes`: NUL-containing rows truncate on the Sharkbite side. Shoal also rejects an empty row where Sharkbite accepts one ([SB-DATA-035](#sec-8)). |
| SB-CPP-019 | `put(mutation, cf, cq, cv)` (`include/extern/accumulo.h:127`) | `Mutation.Put` (`accumulo/mutation.go:43`) | `shoal_mutation_put` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` puts timestamp `42` with a non-empty value | Behavior mismatch | The flat C `put` takes **no timestamp and no value** — it writes an implicit timestamp `0` and an empty value — and no Shoal test exercises that combination on either layer ([SB-DATA-037](#sec-8), [SB-DATA-038](#sec-8)). |
| SB-CPP-020 | `freeMutation(mutation)` (`include/extern/accumulo.h:125`) | Go GC | `shoal_mutation_free` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Interacts with the ownership transfer in [SB-CPP-021](#sec-19): after `addMutation` the Sharkbite wrapper is freed while its payload belongs to the writer. |
| SB-CPP-004 | `createWriter(tableOps, threads)` (`include/extern/accumulo.h:131`) | `Connector.NewBatchWriter` (`accumulo/batch_writer.go:225`) | `shoal_connector_create_batch_writer` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | `short threads` maps to `MaxWriteThreads`, whose default differs (10 versus 3, [SB-BASE-002](#sec-10-1)). |
| SB-CPP-021 | `addMutation(writer, mutation) -> int` (`include/extern/accumulo.h:133`) | `BatchWriter.Add` (`accumulo/batch_writer.go:268`) | `shoal_batch_writer_add` (`capi/include/shoal.h`) | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Ownership differs: Sharkbite transfers the mutation to the writer, Shoal snapshots it and explicitly permits immediate reuse (`capi/README.md`). A mechanical port that keeps using the mutation behaves differently on each side. |
| SB-CPP-022 | `closeWriter(writer) -> int` (`include/extern/accumulo.h:149`) | `BatchWriter.Close` (`accumulo/batch_writer.go:356`) | `shoal_batch_writer_close` (`capi/include/shoal.h`) | `TestBatchWriterCloseLifecycle` (`accumulo/batch_writer_test.go:852`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Close-flushes-pending is proven for Go but not across the ABI ([SB-WRITE-004](#sec-10)). |
| SB-CPP-005 | `createScanner(tableOps, threads)` (`include/extern/accumulo.h:129`) | `Connector.NewScanner` / `NewBatchScanner` (`accumulo/scanner.go:136`, `accumulo/batch_scanner.go:47`) | `shoal_connector_create_scanner` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | `short threads` maps to `Parallelism`, which `Scanner` ignores entirely ([SB-SCAN-012](#sec-9)). |
| SB-CPP-023 | `addRange(scanner, range) -> int` (`include/extern/accumulo.h:135`) | Range passed per `Scan` call (`accumulo/scanner.go:168`) | `shoal_scanner_scan` range argument (`capi/include/shoal.h`) | `TestRangeWireBoundaries` (`accumulo/scanner_test.go:194`) | Behavior mismatch | Sharkbite accumulates ranges on the scanner; Shoal passes them per call ([SB-SCAN-002](#sec-9)). |
| SB-CPP-024 | `addRanges(scanner, range[], size) -> int` (`include/extern/accumulo.h:137`) | `BatchScanner.Scan(ctx, []*Range)` (`accumulo/batch_scanner.go:58`) | `shoal_batch_scanner_scan` (`capi/include/shoal.h`) | `TestBatchScannerSupportsUnboundedAndMultipleRanges` (`accumulo/scanner_test.go:517`) | Behavior mismatch | Array-plus-count on both sides, but Sharkbite retains the ranges for later `hasNext` iteration. |
| SB-CPP-025 | `hasNext(scanner) -> bool` (`include/extern/accumulo.h:139`) | — (results are materialised) | — | — | Behavior mismatch | Cursor predicate with no ABI equivalent; the whole result is buffered before `shoal_scanner_scan` returns ([SB-SCAN-004](#sec-9)). |
| SB-CPP-026 | `onNext(scanner, kv) -> int` (`include/extern/accumulo.h:141`) | — | — | — | Behavior mismatch | Fills a caller-owned `CKeyValue`; the ABI has no cursor advance. |
| SB-CPP-027 | `next(scanner) -> CKeyValue *` (`include/extern/accumulo.h:143`) | — | — | — | Behavior mismatch | Allocating cursor variant, distinct from `onNext`; a port must decide who frees the returned struct. |
| SB-CPP-028 | `nextMany(scanner, kvl) -> int` (`include/extern/accumulo.h:145`) | `[]KeyValue` batch (`accumulo/scanner.go:168`) | `shoal_scan_result_count` + `shoal_scan_result_get` (`capi/include/shoal.h`) | `main()` in `capi/tests/result_bridge.c` exercises the **internal** bridge only | Behavior mismatch | Closest analogue to the ABI shape, but Sharkbite fills a caller-allocated `KeyValueList` while Shoal owns the result and lends views. |
| SB-CPP-029 | `closeScanner(scanner) -> int` (`include/extern/accumulo.h:147`) | Sessions close inside `Scan` (`accumulo/scanner.go:254`) | `shoal_scanner_close` (`capi/include/shoal.h`) | `TestScannerCancellationStillClosesServerScan` (`accumulo/scanner_test.go:396`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Sharkbite requires an explicit close after cursor use; Shoal's Go layer has no scanner close at all. |
| SB-CPP-006 | `struct CKey` (`include/extern/accumulo_data.h:17`) | `accumulo.Key` (`accumulo/scanner.go:22-29`) | `shoal_key` (`capi/include/shoal_types.h`) | `TestKeyRangePreservesFullBounds` (`accumulo/range_test.go:8`) | Behavior mismatch | `CKey` carries a `uint64_t` timestamp (`include/extern/accumulo.h:153`) while Shoal uses `int64_t` on both layers, so timestamps above `INT64_MAX` and Shoal's negative timestamps do not survive a mechanical cast. |
| SB-CPP-030 | `struct CValue` (`include/extern/accumulo_data.h:59`) | `KeyValue.Value []byte` (`accumulo/scanner.go:31-34`) | `shoal_bytes` (`capi/include/shoal_types.h`) | `main()` in `capi/tests/result_bridge.c` (internal bridge) | Behavior mismatch | Caller-allocated versus borrowed-view ownership. |
| SB-CPP-031 | `struct CKeyValue` (`include/extern/accumulo_data.h:69`) | `accumulo.KeyValue` (`accumulo/scanner.go:31-34`) | `shoal_key_value_view` (`capi/include/shoal_types.h`) | `main()` in `capi/tests/result_bridge.c` (internal bridge) | Behavior mismatch | Holds pointers to separately allocated `CKey`/`CValue`; Shoal's view is one flat borrowed struct. |
| SB-CPP-032 | `struct KeyValueList` (`include/extern/accumulo_data.h:75`) | `[]KeyValue` | `shoal_scan_result` + count/get accessors (`capi/include/shoal.h`) | — | Behavior mismatch | Caller-allocated array of pointers, filled by [SB-CPP-028](#sec-19); Shoal owns the result object instead. |
| SB-CPP-033 | `struct CRange` (`include/extern/accumulo_data.h:81`) | `Range` (`accumulo/range.go:14`) | `shoal_range` (`capi/include/shoal_types.h`) | `TestKeyRangeRejectsReversedFields` (`accumulo/range_test.go:86`) | Behavior mismatch | Two `CKey *` plus flags, with `NULL` meaning unbounded; Shoal uses an explicit `SHOAL_RANGE_BOUND_UNBOUNDED` kind and forbids mixing ROW and KEY bounds. |
| SB-CPP-034 | `struct CAuthorizations` (`include/extern/accumulo_data.h:53`) | `ScannerOptions.Authorizations [][]byte` (`accumulo/scanner.go:103`) | `shoal_scanner_config.authorizations` (`capi/include/shoal_types.h`) | `main()` in `capi/tests/lifecycle.c` (validation only) | Behavior mismatch | Sharkbite validates authorization characters ([SB-DATA-050](#sec-8)); neither Shoal layer does. |
| SB-CPP-007 | `createKey(row, cf, cq, cv, timestamp)` (`include/extern/accumulo.h:153`) | `accumulo.Key` composite literal | — | — | Behavior mismatch | Allocating constructor over `char *` fields with a `uint64_t` timestamp; no ABI equivalent, and the signedness gap of [SB-CPP-006](#sec-19) applies. |
| SB-CPP-035 | `toKey(CKey *) -> shared_ptr<Key>` (`include/extern/accumulo.h:157`) | n/a | — | — | Behavior mismatch | Converts a flat struct into the C++ object model; a Shoal port has no intermediate object to convert to. |
| SB-CPP-036 | `populateKey(CKey *, const shared_ptr<Key> &)` (`include/extern/accumulo.h:159`) | n/a | `shoal_scan_result_get` fills a caller-owned view (`capi/include/shoal.h`) | `main()` in `capi/tests/result_bridge.c` (internal bridge) | Behavior mismatch | `populateKey` **copies** into caller storage that outlives the result; `shoal_scan_result_get` hands back borrowed pointers invalidated by `shoal_scan_result_free`. A shim that maps one onto the other without copying produces use-after-free. |
| SB-CPP-008 | `TableOperations::createScanner` returning `unique_ptr` (`include/interconnect/tableOps/TableOperations.h:129`) | Go pointer with GC | Opaque handle plus explicit `shoal_scanner_close`/`_free` | `main()` in `capi/tests/lifecycle.c` frees an unset handle | Behavior mismatch | Scope-exit destruction versus mandatory explicit close; a dropped Shoal handle leaks ([SB-XCUT-001](#sec-20)). |
| SB-CPP-037 | `TableOperations::createWriter` returning `unique_ptr` (`include/interconnect/tableOps/TableOperations.h:138`) | Go pointer with GC | `shoal_batch_writer_close`/`_free` | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | As SB-CPP-008; additionally, scope-exit destruction of a Sharkbite writer flushes via `Sink::close` while a dropped Shoal handle does not. |
| SB-CPP-038 | `TableOperations::createSharedScanner` returning `shared_ptr` (`include/interconnect/tableOps/TableOperations.h:147`) | Go pointer with GC | Opaque handle | — | Behavior mismatch | The variant the Python binding actually calls (`include/interconnect/python/PythonStructures.h:172`); last-reference destruction is the closest thing Sharkbite has to Shoal's GC, but Shoal still requires an explicit close. |
| SB-CPP-039 | `TableOperations::createSharedWriter` returning `shared_ptr` (`include/interconnect/tableOps/TableOperations.h:156`) | Go pointer with GC | Opaque handle | — | Behavior mismatch | As SB-CPP-038, for writers (`include/interconnect/python/PythonStructures.h:177`). |
| SB-CPP-040 | `AuthInfo() = default` (`include/data/constructs/security/AuthInfo.h:33`) | — (`accumulo.Credentials` zero value is unusable; `PasswordCredentials` requires a principal, `accumulo/credentials.go:21`) | — | `TestPasswordCredentialsRequiresPrincipal` (`accumulo/credentials_test.go:38`) | Behavior mismatch | Sharkbite lets a caller construct an empty `AuthInfo` and pass it onward; Shoal rejects an empty principal at construction. |
| SB-CPP-041 | `AuthInfo(AuthInfo &&other) = default` (`include/data/constructs/security/AuthInfo.h:35`) | `Credentials` is copied by value (`accumulo/credentials.go:44`) | — | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Behavior mismatch | Move construction leaves the source's strings empty; Go has no move, so a port silently keeps a usable original. |
| SB-CPP-042 | `AuthInfo(AuthInfo &other) = default` (`include/data/constructs/security/AuthInfo.h:37`) | `Credentials.clone()` (`accumulo/credentials.go:44`) | — | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Behavior mismatch | Note the **non-const** reference: `AuthInfo` cannot be copy-constructed from a `const` object at all, so generic C++ code that takes `const AuthInfo &` cannot copy it. |
| SB-CPP-043 | `AuthInfo &operator=(const AuthInfo &rhs)` (`include/data/constructs/security/AuthInfo.h:60-66`) | `Credentials.clone()` copies every field (`accumulo/credentials.go:44`) | — | `TestPasswordCredentialsCopiesAndRedacts` (`accumulo/credentials_test.go:11`) | Behavior mismatch | The hand-written body copies `user` and `instanceId` but **not** `password` (`include/data/constructs/security/AuthInfo.h:64`), so copy-assigned credentials authenticate with an empty password. Do not reproduce ([SB-UNSAFE-011](#sec-21)). |
| SB-CPP-044 | `AuthInfo &operator=(AuthInfo &&other) = default` (`include/data/constructs/security/AuthInfo.h:68`) | Value assignment | — | — | Behavior mismatch | The defaulted move-assignment **does** move the password, so move and copy assignment disagree — the same object assigned two ways yields different credentials. |
| SB-CPP-045 | `virtual ~AuthInfo()` (`include/data/constructs/security/AuthInfo.h:39-40`) | Go GC | — | — | Not required (rationale required) | Rationale: a virtual destructor with no Python or Go analogue. Recorded to complete the member list; it imposes no compatibility obligation beyond allowing subclassing, which nothing in the pinned tree does. |
| SB-CPP-046 | `Sink(uint16_t maxQueue)` (`include/writer/Sink.h:43`) | `Connector.NewBatchWriter` with `BatchWriterOptions` (`accumulo/batch_writer.go:225`) | `shoal_connector_create_batch_writer` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The only Sharkbite bound is a queue **length** (and the internal queue is sized at 1.5×); Shoal bounds buffered **bytes** ([SB-WRITE-008](#sec-10)). |
| SB-CPP-047 | `bool Sink<T>::push(std::unique_ptr<T> obj)` (`include/writer/Sink.h:51`, body at `include/writer/Sink.h:90-104`) | `BatchWriter.Add` with synchronous memory-bound flush (`accumulo/batch_writer.go:268`) | `shoal_batch_writer_add` (`capi/include/shoal.h`) | `TestBatchWriterMemoryBoundFlushesSynchronously` (`accumulo/batch_writer_test.go:256`) | Behavior mismatch | `push` spins while `waitingSize() >= (maxWait()+1)*1.5`, flushing until the queue drains — a blocking back-pressure loop with no deadline. Shoal flushes synchronously against a byte budget and honours a context deadline. |
| SB-CPP-048 | `bool Sink<T>::push(std::shared_ptr<T> obj)` (`include/writer/Sink.h:57`, body at `include/writer/Sink.h:106-120`) | `BatchWriter.Add` | `shoal_batch_writer_add` | `TestBatchWriterMemoryBoundFlushesSynchronously` (`accumulo/batch_writer_test.go:256`) | Behavior mismatch | Shared-ownership overload of SB-CPP-047; the enqueued object stays aliased by the caller, which is the root of [SB-DATA-046](#sec-8). |
| SB-CPP-049 | `virtual uint64_t Sink<T>::maxWait()` (`include/writer/Sink.h:38`) | — | — | — | Not required (rationale required) | Rationale: a protected virtual extension point (declared before `public:` at `include/writer/Sink.h:42`) that subclasses override to size the back-pressure window; the base returns `0`. Shoal exposes no subclassing surface, and no Python member reaches it. Recorded because it, with SB-CPP-050, defines the `push` blocking condition. |
| SB-CPP-050 | `virtual uint64_t Sink<T>::waitingSize()` (`include/writer/Sink.h:40`) | — | — | — | Not required (rationale required) | Rationale: as SB-CPP-049; the base returns `0`, so the default `push` loop never blocks and back-pressure exists only in subclasses such as `SinkImpl`. |
| SB-CPP-051 | `virtual void Sink<T>::exit()` (`include/writer/Sink.h:80`) | — (no analogue; `BatchWriter.Close` is terminal, `accumulo/batch_writer.go:356`) | — | `TestBatchWriterCloseLifecycle` (`accumulo/batch_writer_test.go:852`) | Behavior mismatch | `exit()` calls `flush(true)` exactly like `close()` but does **not** mark the sink closed, so writing may continue afterwards. Shoal's `Close` is one-way and subsequent `Add` returns `ErrBatchWriterClosed`. |
| SB-CPP-052 | `inline virtual size_t Sink<T>::size()` (`include/writer/Sink.h:82`) | — | — | — | Not required (rationale required) | Rationale: the same member as the Python-visible `BatchWriter.size()` recorded in [SB-WRITE-006](#sec-10), which carries the obligation. Listed here only to complete the `Sink` member enumeration; counting it twice would double the gap. |
| SB-CPP-053 | `Source()` default constructor seeding `sourceOptions` with `BASIC_SCANNER` (`include/scanner/Source.h:35`) | `ScannerOptions{}` zero value (`accumulo/scanner.go:101`) | `shoal_scanner_config` + `shoal_scanner_config_init` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | Shoal's zero value means `BatchSize` 0, normalised to 1024 (`accumulo/scanner.go:14`), so the two defaults are not the same object. |
| SB-CPP-054 | `virtual void Source::addRange(std::unique_ptr<Range>) = 0` (`include/scanner/Source.h:37`) | Range passed per `Scan` call (`accumulo/scanner.go:168`) | `shoal_scanner_scan` range argument (`capi/include/shoal.h`) | `TestRangeWireBoundaries` (`accumulo/scanner_test.go:194`) | Behavior mismatch | Owning overload: the scanner takes the range. Pure virtual, so it is also a subclassing obligation. |
| SB-CPP-055 | `virtual void Source::addRange(const Range &) = 0` (`include/scanner/Source.h:39`) | as SB-CPP-054 | as SB-CPP-054 | `TestRangeWireBoundaries` (`accumulo/scanner_test.go:194`) | Behavior mismatch | Copying overload — the one the Python binding calls (`pysharkbite.cpp:451`). |
| SB-CPP-056 | `void Source::addIterators(const std::vector<IterInfo> &)` (`include/scanner/Source.h:44-46`) | `ScannerOptions.Iterators []IteratorSetting` (`accumulo/scanner.go:105`) | `shoal_scanner_config.iterators` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | Bulk append; unbound in Python, which exposes only the single-iterator form. |
| SB-CPP-057 | `void Source::addIterator(const IterInfo &)` (`include/scanner/Source.h:48-50`) | as SB-CPP-056 | as SB-CPP-056 | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | Backs [SB-SCAN-006](#sec-9). Appends without de-duplicating by name or priority. |
| SB-CPP-058 | `void Source::addPythonIterator(const PythonIterInfo &)` (`include/scanner/Source.h:52-54`) | — | — | `test/python/TestIterator.py:65` | Missing Go | Backs [SB-SCAN-007](#sec-9); requires the server-side Python iterator runtime ([SB-SCAN-016](#sec-9)). |
| SB-CPP-059 | `virtual void Source::locateFailedTablet(ranges, definitions) = 0` (`include/scanner/Source.h:56-59`) | Relocation is internal to `Scanner` (`accumulo/scanner.go:227`) | — | `TestScannerRetriesNotServingAssignmentOnce` (`accumulo/scanner_test.go:294`) | Missing Go | Public relocation hook with no Shoal analogue: Shoal never lets a caller drive tablet relocation. |
| SB-CPP-060 | `virtual shared_ptr<Results<T,BlockType>> Source::getResultSet() = 0` (`include/scanner/Source.h:61`) | `Scanner.Scan(ctx, r) ([]KeyValue, error)` (`accumulo/scanner.go:168`) | `shoal_scanner_scan` (`capi/include/shoal.h`) | `TestScannerContinuationAndCleanup` (`accumulo/scanner_test.go:220`) | Behavior mismatch | Streaming result handle versus a materialised slice — the structural gap of [SB-SCAN-004](#sec-9). |
| SB-CPP-061 | `virtual Source::~Source()` (`include/scanner/Source.h:63`) | Go GC | `shoal_scanner_free` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Not required (rationale required) | Rationale: virtual destructor completing the class's member list; the observable lifetime contract is carried by [SB-CPP-008](#sec-19) and [SB-XCUT-001](#sec-20). |
| SB-CPP-062 | `virtual void Source::close()` (`include/scanner/Source.h:65`) | — (Go closes inside `Scan`, `accumulo/scanner.go:254`) | `shoal_scanner_close` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | The base implementation is an **empty body**, so a `Source` that is not a `Scanner` silently does nothing on close; Shoal's close always tears down the server session. Backs [SB-SCAN-010](#sec-9). |
| SB-CPP-063 | `std::vector<Column> Source::getColumns() const` (`include/scanner/Source.h:67`) | `ScannerOptions.Columns` — write-only, no accessor (`accumulo/scanner.go:104`) | — | — | Missing Go | Returns the accumulated fetch-column set by value; unbound in Python and absent from Shoal. |
| SB-CPP-064 | `std::vector<IterInfo> Source::getIters() const` (`include/scanner/Source.h:69`) | `ScannerOptions.Iterators` — write-only, no accessor (`accumulo/scanner.go:105`) | — | — | Missing Go | As SB-CPP-063, for iterators. |
| SB-CPP-065 | `virtual void Source::setTableOptions(map<string,string>)` (`include/scanner/Source.h:71-73`) | — (no per-scan table options; `EffectiveTableProperties` is read-only, `accumulo/table_property_reads.go:31`) | — | — | Missing Go | Per-scanner table-option map, moved into the source. Unbound in Python and with no Shoal equivalent at any layer. |
| SB-CPP-066 | `virtual void Source::setOption(ScannerOptions)` (`include/scanner/Source.h:75`) | — | — | `PYTHONREADME.md:55` | Missing Go | Bitwise-ORs into the option mask; backs [SB-SCAN-008](#sec-9). |
| SB-CPP-067 | `virtual void Source::removeOption(ScannerOptions)` (`include/scanner/Source.h:77`) | — | — | — | Missing Go | Bitwise-ANDs the complement; backs [SB-SCAN-009](#sec-9). |
| SB-CPP-068 | `virtual bool Source::containsOption(ScannerOptions)` (`include/scanner/Source.h:79-81`) | — | — | — | Missing Go | Option predicate with no Python binding and no Shoal equivalent. |
| SB-CPP-069 | `virtual shared_ptr<Instance> Source::getInstance() = 0` (`include/scanner/Source.h:83`) | `Connector.Instance() InstanceInfo` (`accumulo/connector.go:135`) | — | `TestNewConnectorLifecycle` (`accumulo/connector_test.go:9`) | Behavior mismatch | Sharkbite returns the live `Instance` object from the scanner; Shoal returns a flat `InstanceInfo` value from the connector, and a scanner exposes nothing. |
| SB-CPP-070 | `void Source::fetchColumn(std::string col, std::string colqual = "")` (`include/scanner/Source.h:85-90`) | `NewColumnFamily` / `NewColumn` (`accumulo/scanner.go:43,48`) | `shoal_column` (`capi/include/shoal_types.h`) | `TestPublicScannerAPICompiles` (`accumulo/external_test.go:52`) | Behavior mismatch | The C++ default qualifier is **not** declared to pybind11, which is why the one-argument Python call fails ([SB-UNSAFE-006](#sec-21)). An empty qualifier selects the whole family. |
| SB-CPP-009 | `ClientExample.h` demo class (`include/ClientExample.h`) | — | — | — | Not required (rationale required) | Rationale: documentation sample, not API. |
| SB-CPP-010 | C++ integration drivers (`test/vandv/IntegrationTest.cpp`, `bigwrite.h`, `deletetest.h`, `invalidscans.h`, `invalidtableops.h`, `testAuthsChange.h`, `testEmptyRange.h`, `testScanLocationMove.h`, `testSecurityOperations.h`, `testWriteRead.h`) | — | — | — | Missing Go | These encode the real behavioral contract (error codes, tablet moves, auth changes mid-session, empty ranges). They must be ported into Shoal's live-cluster conformance suite; see [§24](#sec-24). |
| SB-CPP-011 | Catch2 unit tests (`test/constructs/TestConstructs.cpp`, `TestColumn.cpp`, `TestSecurity.cpp`, `TestStreams.cpp`, `rfile_test.cpp`, `zlibTest.cpp`) | `accumulo/*_test.go` (94 tests), `internal/rfile`, `internal/visfilter` tests | `capi/tests/*.c` run by `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`) | — (no test compares the two suites) | Not required (rationale required) | audit #92: rationale — this is a test-suite inventory, not an API surface, and a comparative "at least as strong" claim is an opinion, not evidence. The Sharkbite constructs tests are C++-only and outside the Python contract; the surfaces they exercise are already tracked by their own rows. Retained for provenance. |
| SB-CPP-012 | Mock Accumulo service (`test/services/mockAccumulo.cpp`, `TestServer.h`) | Fake adapters in `accumulo/*_test.go` | Test seams (`cmd/shoal-capi/writer_test_seam.go`, `capi/tests/test_seam.h`) | — | Not required (rationale required) | audit #2: rationale — the Sharkbite side is dead code. `test/services/CMakeLists.txt` is empty, so the mock server is never built and nothing in the pinned release depends on it. Comparing test infrastructure is not an API contract; Shoal's seams are recorded here only so a reader knows the offline-test path exists. |
| SB-CPP-013 | Java standalone-mini-Accumulo harness (`test/19x/st/**`) | — | — | — | Missing Go | Shoal has no ephemeral-cluster harness. This is the delivery vehicle for [§24](#sec-24). |
| SB-CPP-014 | `native-iterators-jni/` (JNI iterator support jar) | — | — | — | Missing Go | Server-side prerequisite for SB-SCAN-016. |

<a id="sec-19-1"></a>

### 19.1 Semantic inventory: scope, exclusions, and completeness

Three successive scanners have been rejected by audit: revision 6's was
class-only, revision 7's truncated signatures and guessed duplicate mappings,
and revision 8's silently dropped whole public classes. Revision 9 fixes the
scope- and scanner-state defects that caused those drops and records, per
failure class, what went wrong and what now proves it fixed.

**What changed mechanically in revisions 9 through 11:**

| Failure class in the previous revision | Root cause | Fix and proof |
| --- | --- | --- |
| Six class forward declarations recorded as external-linkage variables (`include/data/constructs/coordinator/AccumuloInfo.h:79`, `TabletServerStatus.h:55`, `include/scanner/constructs/Results.h:33,35,37`, `include/utils/BackTrace.h:36`) | a declaration ending in `;` fell through to the data-member branch, which took the last identifier as a variable name | `class`, `struct`, `union`, and `enum` forward declarations are type declarations; when the pinned tree defines the type they are merged into that definition, and when it does not they are rows describing an opaque type |
| `using std::void_t;` recorded as an external-linkage variable (`include/utils/TryMoveCall.h:25`) | the same fall-through, because a using-declaration also ends in `;` | using-declarations, using aliases, namespace aliases, and `using namespace` directives are classified separately: an alias is a type contract, a using-declaration re-exports a name, a namespace alias is a spelling, and a directive declares nothing |
| Five public bit-field data members absent (`include/extern/accumulo_data.h:47,85,86,87,88`) | the declarator parser took the last identifier of a declaration, which is the bit-field width, so `unsigned deleted : 1;` produced no name | bit-field widths are stripped per declarator before the name is taken, and each named bit-field is one row that records the width as part of the ABI layout |
| A function-pointer typedef recorded as a callable free function (`include/jni/JVMLoader.h:91`) | `typedef jint (*registerNatives_t)(...)` looks callable to a parameter-list test | function-pointer `typedef`/`using` declarations are aliases; the row says the alias names a callback type and declares no callable |
| pybind11 macro invocations recorded as free functions (`include/jni/PythonBindings.h:49,51`) | an ALL-CAPS macro applied to arguments was parsed as a function definition when the macro is defined in another library | macro invocations at namespace scope are recorded and excluded with a rationale; macro **definitions** and macro-declared types keep their rows |
| `static` namespace-scope variables described as externally linked, process-wide objects | linkage was never recorded, so every global got the same note | each namespace-scope variable records its linkage; `static` ones are described as internal linkage with one copy per translation unit, and the affected divergence row is corrected |
| Twelve `friend std::ostream &operator<<` functions absent (`include/data/constructs/Key.h:289,298`, `include/data/constructs/KeyExtent.h:166`, `include/data/constructs/KeyValue.h:65,72`, `include/data/constructs/Range.h:142,160`, `include/data/constructs/rfile/RFile.h:193,207`, `include/data/constructs/rfile/SequentialRFile.h:221,235`, `include/data/constructs/server/RangeDefinition.h:86`) | `friend` was a skip word, so friend functions defined inside a class body were never recorded | friend functions and operators are enumerated as namespace-scope obligations found by argument-dependent lookup, with the befriending class named in the row; `friend class X;` declarations are not records, because they declare friendship rather than a type |
| Out-of-class member definitions counted as new free functions (`Sink<T>::push`, `TransportPool<Tr>::getTransporter`, `TableOperations<K, V>::createTable`, `Worker<T>::operator=`, and 12 more) | a qualified definition at namespace scope was parsed as a fresh declaration | qualified definitions are matched to their in-class declaration by owner and member name and merged into it; 16 such definitions are recorded as merged, with the in-class access reported |
| Protected members required (`TransportPool::createNewTransport`, `Sink::enqueue`, `Sink::exceedQueue`) | those rows came from the out-of-class definitions above, which carried no access | every declaration now carries its access label; 700 protected and 346 private members are excluded by rule and counted, and no row may correspond to a non-public declaration |
| Every public `utils::StringUtils` member (`include/utils/StringUtils.h:75-263`) absent | anonymous-namespace state was a counter reset only at file scope, so the `namespace { … }` block at lines 40-58 marked the rest of the file internal | anonymity is a property of each namespace frame, popped with the frame; `StringToBool`, the trims, the comparisons, and all four `join` overloads are enumerated |
| All of `writer::Sink` (`include/writer/Sink.h:43-82`) and `cclient::data::KeyAllocatorPool` (`include/data/constructs/rfile/AllocatorPool.h:42-52`) absent | a header was classified vendored when a vendor name appeared anywhere in its first 40 lines, so any header that merely *used* `moodycamel::` or included libhdfs3 was dropped | vendoring is decided by path and by the file's own license block, never by what it references |
| `Hdfs`, `HdfsInputStream`, `HdfsOutputStream` absent although they are Python-exposed | same misclassification | all three are Sharkbite-authored wrappers and are enumerated in full |
| Namespace-scope declarations absent (`IsEmpty`, the two JNI entry points, and eight globals) | three separate causes: trailing return types containing `==` failed the callable test; `extern "C" {` opened a brace that was not modelled, so nothing inside was at namespace scope; and namespace-scope variables were excluded by rule | trailing returns accept any expression, `extern "C"` is a transparent frame, and namespace-scope variables are enumerated as globals |
| `LOG_LEVEL` (`include/logging/Logger.h:62-70`) absent | only `enum`-introduced declarations were parsed, not `typedef enum { … } NAME;` | typedef-enums are parsed and named from the trailing alias, with ordinals `trace = 0 … off = 6` |
| Include guards emitted as required rows (9 rows) | guards were recognised only when valueless, so `#define KEY 1` looked like a constant | a `#define NAME` or `#define NAME 1` immediately preceded by `#ifndef NAME` or `#if !defined(NAME)` is a guard |

The revision-8 fixes that the audit confirmed are preserved unchanged: full
untruncated signatures with balanced parameter lists, evidence-proved duplicate
mappings, the `maj`/`min` declarator split, `TabletType` and `ScannerOptions`
ordinals, document-derived cross-reference anchors, the corrected `Key`
accessor citations, and the rescoped [SB-RFILE-005](#sec-15).

**Duplicate obligations are proved, not guessed.** A generated row is
`Not required` only when one of these holds, and the rule that fired is stated
in the row:

1. **Cited declaration** — a normative row cites this exact `header:line`.
2. **Bound overload** — the member is bound into Python at a specific
   `pysharkbite.cpp` line, the binding's explicit member-function-pointer cast
   (or `pybind11::init<>` argument list) matches this declaration's parameter
   list after qualifier normalisation, and a normative row cites that binding
   line. Where a binding carries no cast, the rule fires only if the class
   declares exactly one member with that name.

Everything else is a gap and carries its parity status. `SB-KEEP`,
`SB-UNSAFE`, `SB-DIV`, and `SB-GAP` entries can never discharge a matrix
obligation, because those tables are commentary, not matrix rows.

**Scope rules, applied mechanically and recorded per record:**

| Rule | Effect | Count |
| --- | --- | --- |
| Vendored by path: `include/extern/libhdfs3/**` (98 headers), `include/extern/thrift*/**` (50), `include/data/extern/concurrentqueue/**` (3, Cameron Desrochers' queue and Jeff Preshing's semaphore under Simplified BSD/zlib) | Excluded; **non-normative**, because Sharkbite redistributes rather than authors them, and a port replaces the dependency instead of the API | 152 headers, 29853 records |
| Vendored by the file's own license block: `include/utils/Uri.h` (Facebook/Folly) | Excluded on the same ground | included in the 152 |
| Internal linkage — anonymous namespace or private/protected nested type (`utils::{anonymous}::string_traits`) | Excluded: not reachable by a consumer, so not a contract | 11 records in live headers |
| Include guard — `#define NAME` or `#define NAME 1` immediately preceded by `#ifndef NAME` / `#if !defined(NAME)` | Excluded: the token's only contract is single inclusion of its own file | 182 records in live headers |
| Private or protected class members | Excluded from rows, but recorded with their access so the exclusion is auditable rather than invisible | 700 protected, 346 private |
| Out-of-class member definitions matching an in-class declaration | Merged into that declaration: the definition is not a second obligation | 16 merged |
| `friend` functions and operators declared or defined in a class body | **Included** as namespace-scope obligations: friendship is not access control, and callers reach them by argument-dependent lookup | 12 rows |
| Constructor initialiser continuations, inline bodies, trailing-return continuations, macro continuation lines | Not records at all: joined into the declaration they belong to | 0 records emitted |
| Everything else | One row each in [§19.2](#sec-19-2) | 2626 rows |

**A header is not vendored because it uses a vendored type.**
`include/utils/ConcurrentQueue.h` is Sharkbite's own Apache-licensed wrapper and
is enumerated; the redistributed queue lives in
`include/data/extern/concurrentqueue/` and is not. `include/writer/Sink.h`
queues into that vendored type and is enumerated. Any future exclusion must
name the file's own license or its vendored path, not a reference it makes.

**Internal linkage is not the same as internal API.** An anonymous-namespace
member is unreachable from another translation unit, so it is excluded. A
`static` or `inline` free function declared in a published header is different:
every file that includes the header gets a usable definition, so it is part of
what the header offers and is enumerated.

**Live public surface, by area:**

| Area | Headers | Types | Members | Enums |
| --- | --- | --- | --- | --- |
| `include/data/**` | 125 | 159 | 1580 | 11 |
| `include/interconnect/**` | 40 | 48 | 491 | 2 |
| `include/utils/**` | 8 | 16 | 137 | 1 |
| `include/scanner/**` | 9 | 12 | 112 | 1 |
| `include/jni/**` | 11 | 11 | 104 | 0 |
| `include/logging/**` | 3 | 9 | 72 | 1 |
| `include/extern/**` | 2 | 12 | 58 | 0 |
| `include/writer/**` | 4 | 5 | 45 | 0 |
| `include/python/**` | 1 | 0 | 2 | 0 |
| `include/ClientExample.h` | 1 | 1 | 2 | 0 |
| **Total** | **204** | **273** | **2603** | **16** |

**Completeness identity:**

```text
rows = public members + enums + types with no public member
2626 =          2603 +   16 +                             7
```

Members by kind: 1517 methods, 371 constructors, 154 public data members (five of them bit-fields), 144
member operators (including two conversion operators), 143 destructors, 126
macro constants, 90 namespace-scope free functions, 19 namespace-scope
operators (12 of them `friend operator<<` overloads), 19 constants, 8
namespace-scope variables (5 `static` with internal linkage, 3 `extern` with
external linkage), 10 type aliases (one a function-pointer type), 1
using-declaration, and 1 macro-declared type
(`stop_async_iteration`, `include/python/bindings.h:28`). Two data-member rows
come from the one multi-declarator statement in the tree (`short maj, min;`,
`include/data/constructs/rfile/rfile_version.h:106`).

**What is excluded, with counts, so the exclusion is auditable:** 700 protected
and 346 private members (recorded with their access, never silently dropped),
16 out-of-class definitions merged into the in-class declaration they define, 6
forward declarations merged into the definitions they precede,
166 include guards, 11 internal-linkage declarations, and the 152 vendored
headers. Three rows revision 10 required —
`interconnect::TransportPool::createNewTransport`
(`include/interconnect/TransportPool.h:105`), `writer::Sink::enqueue`
(`include/writer/Sink.h:34`), and `writer::Sink::exceedQueue`
(`include/writer/Sink.h:32`) — are protected declarations whose out-of-class
definitions leaked into the row set; they are excluded, not reclassified, and
they no longer inflate the required total.

Named examples the audits called out, all present with their exact declaration
lines and full signatures: `StringToBool` (`include/utils/StringUtils.h:75`), the trims, the
comparisons, and all four `join` overloads
(`include/utils/StringUtils.h:186`, `:209`, `:230`, `:255`); the `Sink` constructor, destructor, both `push`
overloads, `flush`, both `addMutation` overloads, `close`, `exit`, and `size`
(`include/writer/Sink.h:43-82`); the five public `KeyAllocatorPool` methods
(`include/data/constructs/rfile/AllocatorPool.h:44-52`); `IsEmpty`
(`include/data/constructs/inputvalidation.h:42`);
`rethrow_cpp_exception_as_java_exception` and
`Java_org_apache_accumulo_tserver_tablet_NativeCompactor_callCompact`
(`include/jni/DSLIterator.h:27,29`); the globals at
`include/data/client/LocatorCache.h:62`,
`include/data/client/RootTabletLocator.h:32`,
`include/data/constructs/client/zookeeper/zookeepers.h:50`,
`include/data/exceptions/ClientException.h:39`,
`include/interconnect/Accumulo.h:50`, `include/interconnect/Master.h:50`,
`include/interconnect/TabletServer.h:57`, and
`include/interconnect/tableOps/ClientTableOps.h:234`; `to_base`
(`include/utils/EnumClass.h:29`) with the five bitmask operators at lines
40, 49, 58, 66, and 74; `fromExtent`
(`include/data/constructs/tablet/TabletType.h:33`); the `Iterator` and
`BatchScanner` aliases (`include/scanner/impl/Scanner.h:162,164`); the
`BatchWriter` alias (`include/writer/impl/SinkImpl.h:107`);
`ROOT_TABLET_EXTENT` (`include/data/constructs/KeyExtent.h:250`); the seven
configuration macros (`include/data/constructs/ConfigurationOptions.h:23-31`);
and the six interconnect constants plus the `INTERCONNECT_TYPES` enum
(`include/interconnect/InterConnect.h:47-56`).

**Enum rows carry real value sets with correct ordinals.** Each of the 16 enum
rows lists every enumerator with its ordinal, explicit where the header assigns
one and derived sequentially otherwise. `LOG_LEVEL`
(`include/logging/Logger.h:62`) is `trace = 0; debug = 1; info = 2; warn = 3;
err = 4; critical = 5; off = 6`; `TabletType`
(`include/data/constructs/tablet/TabletType.h:28`) is `ROOT = 0; METADATA = 1;
USER = 2`; `ScannerOptions` (`include/scanner/ScannerOptions.h:23`) is
`BASIC_SCANNER = 0; ENABLE_HEDGED_READS = 1; ENABLE_RFILE_SCANNER = 2`; and
`TimeUnit` (`include/utils/StringUtils.h:29`) is `DAY = 0 … NANOSECOND = 5`.

**No usage-based exclusion.** An unused public API is still published, still
compiled against, and still a compatibility obligation under this section.
Destructors are assessed individually: where the owning class is bound into
Python the destructor is `Behavior mismatch` (15 rows), otherwise `Missing Go`
(124 rows), and 4 are duplicates of normative rows.

**Exception classes are additionally verified by hand.** `ClientException`
(both constructors, the public `errorCode` accessor, `~ClientException`,
`what()`, `getErrorCode()`, and the `INVALID_*` code macros), `HDFSException`,
`IllegalArgumentException`, `IllegalStateException`,
`IterationInterruptedException`, `NotServingException`, and
`VisibilityParseException` each have every public member enumerated, and each
cross-references its normative row in [§18](#sec-18) where one exists.
`APIException` (`include/interconnect/exceptions/APIException.h:23-24`)
declares `what()` under default `class` access — it is **private**, so
`APIException` has no public member and appears as a memberless-type row rather
than a member row. That asymmetry is a fact about the pinned header, not an
omission.

<a id="sec-19-2"></a>

### 19.2 Complete public C++ member enumeration (`SB-CXX`)

One row per live public declaration, generated by the semantic scanner
described in [§19.1](#sec-19-1) and ordered by header and line. Vendored and
internal-linkage declarations are excluded by the scope rules recorded there,
not silently dropped. The `Shoal Go` and `Shoal C ABI` columns are `—`
throughout: no member here is exported by `accumulo/` or declared in
`capi/include/shoal.h`, which is what makes them gaps. Rows that duplicate a
normative row elsewhere are `Not required` and name that row.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-CXX-0001 | `ClientExample` constructor `ClientExample();` (`include/ClientExample.h:23`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. top-level public header. |
| SB-CXX-0002 | `ClientExample` destructor `virtual ~ClientExample();` (`include/ClientExample.h:24`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::ClientExample` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. top-level public header. |
| SB-CXX-0003 | `LocatorKey` constructor `LocatorKey() : instance(nullptr) {}` (`include/data/client/ExtentLocator.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0004 | `LocatorKey` constructor `LocatorKey(const LocatorKey &other)` (`include/data/client/ExtentLocator.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0005 | `LocatorKey` constructor `explicit LocatorKey(std::shared_ptr<cclient::data::Instance> instance, const std::string &table);` (`include/data/client/ExtentLocator.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0006 | `LocatorKey` public data member `std::shared_ptr<cclient::data::Instance> instance;` (`include/data/client/ExtentLocator.h:49`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0007 | `LocatorKey` public data member `std::string tableName;` (`include/data/client/ExtentLocator.h:51`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0008 | `LocatorKey` operator `LocatorKey &operator=(const LocatorKey &other) {` (`include/data/client/ExtentLocator.h:53`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0009 | `LocatorKey` operator `bool operator==(const LocatorKey &key) {` (`include/data/client/ExtentLocator.h:58`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0010 | `LocatorKey` operator `bool operator>(const LocatorKey &key) const {` (`include/data/client/ExtentLocator.h:63`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0011 | `LocatorKey` operator `bool operator<(const LocatorKey &key) const {` (`include/data/client/ExtentLocator.h:69`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0012 | `TabletLocator` constructor `TabletLocator();` (`include/data/client/ExtentLocator.h:85`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0013 | `TabletLocator` destructor `virtual ~TabletLocator();` (`include/data/client/ExtentLocator.h:86`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::TabletLocator` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0014 | `TabletLocator` method `virtual cclient::data::TabletLocation locateTablet( cclient::data::security::AuthInfo *creds, const std::string &row, bool skipRow, bool retry) = 0;` (`include/data/client/ExtentLocator.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0015 | `TabletLocator` method `virtual std::vector<cclient::data::TabletLocation> locations( cclient::data::security::AuthInfo *credentials) {` (`include/data/client/ExtentLocator.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0016 | `TabletLocator` method `virtual void binMutations( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Mutation>> *mutations, std::map<std::string, std::shared_ptr<cclient::data::TabletServerMutations>> *binnedMutations, std::set<std::string> *locations, std::vector<std::shared_ptr<cclient::data::Mutation>> *failures) = 0;` (`include/data/client/ExtentLocator.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0017 | `TabletLocator` method `virtual std::vector<std::shared_ptr<cclient::data::Range>> binRanges( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Range>> *ranges, std::set<std::string> *locations, std::map<std::string, std::map<std::shared_ptr<cclient::data::KeyExtent>, std::vector<std::shared_ptr<cclient::data::Range>>, pointer_comparator< std::shared_ptr<cclient::data::KeyExtent>>>> *binnedRanges) = 0;` (`include/data/client/ExtentLocator.h:136`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0018 | `TabletLocator` method `virtual void invalidateCache(cclient::data::KeyExtent failedExtent) = 0;` (`include/data/client/ExtentLocator.h:151`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0019 | `TabletLocator` method `virtual void invalidateCache() = 0;` (`include/data/client/ExtentLocator.h:156`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0020 | `TabletLocator` method `virtual void invalidateCache( std::vector<cclient::data::KeyExtent> keySet) = 0;` (`include/data/client/ExtentLocator.h:161`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0021 | `LocatorCache` constructor `LocatorCache();` (`include/data/client/LocatorCache.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0022 | `LocatorCache` destructor `virtual ~LocatorCache();` (`include/data/client/LocatorCache.h:38`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::LocatorCache` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0023 | `LocatorCache` method `void put(LocatorKey key, TabletLocator *locator) {` (`include/data/client/LocatorCache.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0024 | `LocatorCache` method `TabletLocator *getLocator(LocatorKey key);` (`include/data/client/LocatorCache.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0025 | `(namespace scope)` namespace-scope variable `extern LocatorCache cachedLocators;` (`include/data/client/LocatorCache.h:62`) | — | — | — | Missing Go | Namespace-scope variable with external linkage (`extern`), so consumers link against this one object and it is part of the ABI as well as the API. Shoal exposes no equivalent global. client-side data construct. |
| SB-CXX-0026 | `MetaDataLocationObtainer` constructor `explicit MetaDataLocationObtainer( std::shared_ptr<cclient::data::Instance> instance) : instance(instance), logger(logging::LoggerFactory<MetaDataLocationObtainer>::getLogger()) {` (`include/data/client/MetaDataLocationObtainer.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0027 | `MetaDataLocationObtainer` destructor `virtual ~MetaDataLocationObtainer();` (`include/data/client/MetaDataLocationObtainer.h:54`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::MetaDataLocationObtainer` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0028 | `MetaDataLocationObtainer` method `std::vector<cclient::data::TabletLocation> findTablet( cclient::data::security::AuthInfo *credentials, cclient::data::TabletLocation *source, std::string row, std::string stopRow, TabletLocator *parent) override;` (`include/data/client/MetaDataLocationObtainer.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0029 | `MetaDataLocationObtainer` method `std::vector<cclient::data::TabletLocation> findTablet( cclient::data::security::AuthInfo *credentials, std::string tabletserver, std::map<cclient::data::KeyExtent, std::vector<cclient::data::Range>> *map, TabletLocator *parent) override {` (`include/data/client/MetaDataLocationObtainer.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0030 | `(namespace scope)` namespace-scope static variable `static std::shared_ptr<cclient::data::KeyExtent> ROOT_EXTENT = std::make_shared<cclient::data::KeyExtent>("+r", "", "");` (`include/data/client/RootTabletLocator.h:32`) | — | — | — | Missing Go | Namespace-scope variable declared `static`, so it has **internal linkage**: every translation unit that includes this header gets its own independent copy, initialised separately. It is not a process-wide object and consumers cannot link against one shared instance — any port that replaces it with a single shared value changes observable behavior, and any port that keeps per-consumer state matches it. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0031 | `RootTabletLocator` constructor `explicit RootTabletLocator(std::shared_ptr<cclient::data::Instance> instance);` (`include/data/client/RootTabletLocator.h:37`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0032 | `RootTabletLocator` destructor `~RootTabletLocator();` (`include/data/client/RootTabletLocator.h:38`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::RootTabletLocator` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0033 | `RootTabletLocator` method `cclient::data::TabletLocation locateTablet( cclient::data::security::AuthInfo *creds, const std::string &row, bool skipRow, bool retry) override {` (`include/data/client/RootTabletLocator.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0034 | `RootTabletLocator` method `void binMutations( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Mutation>> *mutations, std::map<std::string, std::shared_ptr<cclient::data::TabletServerMutations>> *binnedMutations, std::set<std::string> *locations, std::vector<std::shared_ptr<cclient::data::Mutation>> *failures) override {}` (`include/data/client/RootTabletLocator.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0035 | `RootTabletLocator` method `virtual std::vector<std::shared_ptr<cclient::data::Range>> binRanges( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Range>> *ranges, std::set<std::string> *locations, std::map<std::string, std::map<std::shared_ptr<cclient::data::KeyExtent>, std::vector<std::shared_ptr<cclient::data::Range>>, pointer_comparator< std::shared_ptr<cclient::data::KeyExtent>>>> *binnedRanges) override {` (`include/data/client/RootTabletLocator.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0036 | `RootTabletLocator` method `void invalidateCache(cclient::data::KeyExtent failedExtent) override {}` (`include/data/client/RootTabletLocator.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0037 | `RootTabletLocator` method `void invalidateCache() override {}` (`include/data/client/RootTabletLocator.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0038 | `RootTabletLocator` method `void invalidateCache(std::vector<cclient::data::KeyExtent> keySet) override {}` (`include/data/client/RootTabletLocator.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0039 | `TabletLocation` constructor `TabletLocation();` (`include/data/client/TabletLocation.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0040 | `TabletLocation` constructor `explicit TabletLocation(std::shared_ptr<KeyExtent> extent, const std::string &loc) : tablet_extent(extent), tablet_location(loc), port(0) {}` (`include/data/client/TabletLocation.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0041 | `TabletLocation` constructor `explicit TabletLocation(std::shared_ptr<KeyExtent> extent, const std::string &loc, const std::string &sesh) : tablet_extent(extent), tablet_location(loc), session(sesh) {` (`include/data/client/TabletLocation.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0042 | `TabletLocation` constructor `explicit TabletLocation(TabletLocation *copy) : tablet_extent(copy->getExtent()), tablet_location(copy->getLocation()), session(copy->getSession()) {` (`include/data/client/TabletLocation.h:44`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0043 | `TabletLocation` method `std::shared_ptr<KeyExtent> getExtent() { return tablet_extent; }` (`include/data/client/TabletLocation.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0044 | `TabletLocation` method `void setExtent(const std::shared_ptr<KeyExtent> &extent) {` (`include/data/client/TabletLocation.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0045 | `TabletLocation` method `std::string getLocation() { return tablet_location; }` (`include/data/client/TabletLocation.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0046 | `TabletLocation` method `std::string getServer() { return server; }` (`include/data/client/TabletLocation.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0047 | `TabletLocation` method `std::string getSession() { return session; }` (`include/data/client/TabletLocation.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0048 | `TabletLocation` method `int getPort() { return port; }` (`include/data/client/TabletLocation.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0049 | `TabletLocation` operator `bool operator!=(std::nullptr_t type) {` (`include/data/client/TabletLocation.h:69`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0050 | `TabletLocation` operator `bool operator==(std::nullptr_t type) {` (`include/data/client/TabletLocation.h:73`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0051 | `TabletLocation` operator `TabletLocation &operator=(const TabletLocation &te) {` (`include/data/client/TabletLocation.h:77`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0052 | `TabletLocation` destructor `virtual ~TabletLocation();` (`include/data/client/TabletLocation.h:86`) | — | — | — | Missing Go | Assessed individually: `cclient::data::TabletLocation` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0053 | `TabletLocationObtainer` constructor `TabletLocationObtainer();` (`include/data/client/TabletLocationObtainer.h:37`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0054 | `TabletLocationObtainer` destructor `virtual ~TabletLocationObtainer();` (`include/data/client/TabletLocationObtainer.h:39`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::TabletLocationObtainer` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0055 | `TabletLocationObtainer` method `virtual std::vector<cclient::data::TabletLocation> findTablet( cclient::data::security::AuthInfo *credentials, cclient::data::TabletLocation *source, std::string row, std::string stopRow, TabletLocator *parent) = 0;` (`include/data/client/TabletLocationObtainer.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0056 | `TabletLocationObtainer` method `virtual std::vector<cclient::data::TabletLocation> findTablet( cclient::data::security::AuthInfo *credentials, std::string tabletserver, std::map<cclient::data::KeyExtent, std::vector<cclient::data::Range>> *map, TabletLocator *parent) = 0;` (`include/data/client/TabletLocationObtainer.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0057 | `locationComparator` operator `bool operator()(const std::string &left, const std::string &right) const {` (`include/data/client/TabletServerLocator.h:34`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0058 | `TabletServerLocator` constructor `TabletServerLocator(std::string tableId, TabletLocator *parent, TabletLocationObtainer *locator, std::shared_ptr<cclient::data::Instance> inst);` (`include/data/client/TabletServerLocator.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0059 | `TabletServerLocator` destructor `virtual ~TabletServerLocator();` (`include/data/client/TabletServerLocator.h:49`) | — | — | — | Missing Go | Assessed individually: `cclient::impl::TabletServerLocator` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0060 | `TabletServerLocator` method `virtual std::vector<cclient::data::TabletLocation> locations( cclient::data::security::AuthInfo *credentials) override {` (`include/data/client/TabletServerLocator.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0061 | `TabletServerLocator` method `cclient::data::TabletLocation locateTablet( cclient::data::security::AuthInfo *creds, const std::string &row, bool skipRow, bool retry) override {` (`include/data/client/TabletServerLocator.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0062 | `TabletServerLocator` method `inline void binMutations( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Mutation>> *mutations, std::map<std::string, std::shared_ptr<cclient::data::TabletServerMutations>> *binnedMutations, std::set<std::string> *locations, std::vector<std::shared_ptr<cclient::data::Mutation>> *failures) override {` (`include/data/client/TabletServerLocator.h:145`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0063 | `TabletServerLocator` method `std::vector<std::shared_ptr<cclient::data::Range>> binRanges( cclient::data::security::AuthInfo *credentials, std::vector<std::shared_ptr<cclient::data::Range>> *ranges, std::set<std::string> *locations, std::map<std::string, std::map<std::shared_ptr<cclient::data::KeyExtent>, std::vector<std::shared_ptr<cclient::data::Range>>, pointer_comparator< std::shared_ptr<cclient::data::KeyExtent>>>> *binnedRanges) override {` (`include/data/client/TabletServerLocator.h:181`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0064 | `TabletServerLocator` method `void invalidateCache(cclient::data::KeyExtent failedExtent) override {` (`include/data/client/TabletServerLocator.h:282`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0065 | `TabletServerLocator` method `void invalidateCache() override {` (`include/data/client/TabletServerLocator.h:287`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0066 | `TabletServerLocator` method `void invalidateCache(std::vector<cclient::data::KeyExtent> keySet) override {}` (`include/data/client/TabletServerLocator.h:293`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0067 | `AgeOffType` enum `enum AgeOffType{` (`include/data/constructs/ageoff/AgeOffConditions.h:23`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: DATA_TYPE = 0; DEFAULT = 1. Shoal exposes no equivalent type. |
| SB-CXX-0068 | `AgeOffCondition` constructor `AgeOffCondition():type(AgeOffType::DEFAULT),name("default"),earliest_allowed_timestamp(0){` (`include/data/constructs/ageoff/AgeOffConditions.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0069 | `AgeOffCondition` constructor `explicit AgeOffCondition(AgeOffType type, const std::string &name, int64_t earliest_allowed_timestamp) : type(type),name(name),earliest_allowed_timestamp(earliest_allowed_timestamp){}` (`include/data/constructs/ageoff/AgeOffConditions.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0070 | `AgeOffCondition` public data member `AgeOffType type;` (`include/data/constructs/ageoff/AgeOffConditions.h:32`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0071 | `AgeOffCondition` public data member `std::string name;` (`include/data/constructs/ageoff/AgeOffConditions.h:33`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0072 | `AgeOffCondition` public data member `int64_t earliest_allowed_timestamp;` (`include/data/constructs/ageoff/AgeOffConditions.h:34`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0073 | `AgeOffEvaluator` constructor `AgeOffEvaluator(){` (`include/data/constructs/ageoff/AgeOffConditions.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0074 | `AgeOffEvaluator` destructor `virtual ~AgeOffEvaluator(){}` (`include/data/constructs/ageoff/AgeOffConditions.h:44`) | — | — | — | Missing Go | Assessed individually: `cclient::data::AgeOffEvaluator` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0075 | `AgeOffEvaluator` constructor `explicit AgeOffEvaluator(AgeOffCondition condition){` (`include/data/constructs/ageoff/AgeOffConditions.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0076 | `AgeOffEvaluator` constructor `explicit AgeOffEvaluator(std::vector<AgeOffCondition> conditions){` (`include/data/constructs/ageoff/AgeOffConditions.h:65`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0077 | `AgeOffEvaluator` method `virtual bool filtered(const std::shared_ptr<Key> &key){` (`include/data/constructs/ageoff/AgeOffConditions.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0078 | `AgeOffEvaluator` method `std::vector<AgeOffCondition> getConditions(AgeOffType type) const{` (`include/data/constructs/ageoff/AgeOffConditions.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0079 | `AgeOffEvaluator` method `AgeOffCondition getDefaultCondition() const{` (`include/data/constructs/ageoff/AgeOffConditions.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0080 | `AgeOffEvaluator` method `bool empty() const{` (`include/data/constructs/ageoff/AgeOffConditions.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0081 | `ArrayAllocators` constructor `ArrayAllocators() {` (`include/data/constructs/AllocatorPool.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0082 | `ArrayAllocators` destructor `~ArrayAllocators() {` (`include/data/constructs/AllocatorPool.h:45`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ArrayAllocators` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0083 | `ArrayAllocators` method `T* get() {` (`include/data/constructs/AllocatorPool.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0084 | `ArrayAllocators` method `void returnItem(T *item) {` (`include/data/constructs/AllocatorPool.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0085 | `KeyManager` constructor `explicit KeyManager(size_t maxsize,ObjectAllocatorPool<Text> *txt) : maxsize(maxsize) {` (`include/data/constructs/AllocatorPool.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0086 | `KeyManager` destructor `~KeyManager() {` (`include/data/constructs/AllocatorPool.h:81`) | — | — | — | Missing Go | Assessed individually: `cclient::data::KeyManager` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0087 | `KeyManager` method `inline void add(cclient::data::Key *o) {` (`include/data/constructs/AllocatorPool.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0088 | `KeyManager` method `std::shared_ptr<cclient::data::Key> get(ObjectAllocatorPool<Text> *txt) {` (`include/data/constructs/AllocatorPool.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0089 | `ArrayAllocatorPool` constructor `ArrayAllocatorPool();` (`include/data/constructs/AllocatorPool.h:127`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0090 | `ArrayAllocatorPool` destructor `virtual ~ArrayAllocatorPool();` (`include/data/constructs/AllocatorPool.h:129`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ArrayAllocatorPool` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0091 | `ArrayAllocatorPool` method `virtual void disown(std::pair<char*, size_t>) override;` (`include/data/constructs/AllocatorPool.h:131`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0092 | `ArrayAllocatorPool` method `virtual std::pair<char*, size_t> allocateBuffer(size_t size) override;` (`include/data/constructs/AllocatorPool.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0093 | `ArrayAllocatorPool` method `virtual std::shared_ptr<Text> allocate(size_t size) override;` (`include/data/constructs/AllocatorPool.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0094 | `ArrayAllocatorPool` method `virtual void free(std::pair<char*, size_t> &&ptr) override;` (`include/data/constructs/AllocatorPool.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0095 | `ArrayAllocatorPool` method `std::shared_ptr<cclient::data::Key> newKey() {` (`include/data/constructs/AllocatorPool.h:139`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0096 | `ArrayAllocatorPool` method `static ArrayAllocatorPool* newInstance() {` (`include/data/constructs/AllocatorPool.h:143`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0097 | `ArrayAllocatorPool` method `static void returnInstance(ArrayAllocatorPool *pool) {` (`include/data/constructs/AllocatorPool.h:147`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0098 | `ArrayAllocatorPool` method `static ArrayAllocatorPool* getInstance() {` (`include/data/constructs/AllocatorPool.h:151`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0099 | `IConnector` method `virtual Scanner *createScanner(std::string tableName, cclient::data::security::Authorizations *authorizations, int numQueryThreads) = 0;` (`include/data/constructs/client/Connector.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0100 | `IConnector` destructor `virtual ~IConnector() {` (`include/data/constructs/client/Connector.h:41`) | — | — | — | Missing Go | Assessed individually: `cclient::connector::impl::IConnector` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0101 | `AccumuloConnector` constructor `explicit AccumuloConnector(ZookeeperInstance *instance, cclient::data::security::AuthInfo *auths) : myInstance(instance), credentials(NULL) {` (`include/data/constructs/client/Connector.h:49`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0102 | `AccumuloConnector` method `Scanner *createScanner(std::string tableName, cclient::data::security::Authorizations *authorizations, int numQueryThreads) {` (`include/data/constructs/client/Connector.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0103 | `Connector` constructor `Connector(ZookeeperInstance *instance, cclient::data::security::AuthInfo *auths)` (`include/data/constructs/client/Connector.h:68`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0104 | `Connector` method `Scanner * createScanner(std::string tableName, cclient::data::security::Authorizations *authorizations, int numQueryThreads) {` (`include/data/constructs/client/Connector.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0105 | `Connector` destructor `virtual ~Connector() {` (`include/data/constructs/client/Connector.h:82`) | — | — | — | Missing Go | Assessed individually: `cclient::connector::impl::Connector` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0106 | `HdfsDirEnt` constructor `explicit HdfsDirEnt(const hdfsFileInfo *);` (`include/data/constructs/client/Hdfs.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0107 | `HdfsDirEnt` method `bool isDirectory() const {` (`include/data/constructs/client/Hdfs.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0108 | `HdfsDirEnt` method `std::string getName() const {` (`include/data/constructs/client/Hdfs.h:54`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-010](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsDirEnt::getName` is the overload bound at `pysharkbite.cpp:544` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0109 | `HdfsDirEnt` method `size_t getSize() const {` (`include/data/constructs/client/Hdfs.h:59`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-017](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsDirEnt::getSize` is the overload bound at `pysharkbite.cpp:547` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0110 | `HdfsDirEnt` method `uint8_t getreplication() const {` (`include/data/constructs/client/Hdfs.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0111 | `HdfsDirEnt` method `std::string getOwner() const {` (`include/data/constructs/client/Hdfs.h:69`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-015](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsDirEnt::getOwner` is the overload bound at `pysharkbite.cpp:545` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0112 | `HdfsDirEnt` method `std::string getGroup() const {` (`include/data/constructs/client/Hdfs.h:74`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-016](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsDirEnt::getGroup` is the overload bound at `pysharkbite.cpp:546` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0113 | `HdfsDirEnt` method `uint16_t getPermissions() const {` (`include/data/constructs/client/Hdfs.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0114 | `HdfsLink` constructor `explicit HdfsLink(std::string nn, int port);` (`include/data/constructs/client/Hdfs.h:88`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-001](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::HdfsLink` is the overload bound at `pysharkbite.cpp:568` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0115 | `HdfsLink` constructor `explicit HdfsLink(std::string nn);` (`include/data/constructs/client/Hdfs.h:89`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0116 | `HdfsLink` destructor `~HdfsLink();` (`include/data/constructs/client/Hdfs.h:91`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::hdfs::HdfsLink` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0117 | `HdfsLink` method `int mkdir(const std::string &dir);` (`include/data/constructs/client/Hdfs.h:93`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-009](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::mkdir` is the overload bound at `pysharkbite.cpp:575` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0118 | `HdfsLink` method `int remove(const std::string &dir, bool recursive);` (`include/data/constructs/client/Hdfs.h:95`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-005](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::remove` is the overload bound at `pysharkbite.cpp:571` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0119 | `HdfsLink` method `int rename(const std::string &fromName, const std::string toName);` (`include/data/constructs/client/Hdfs.h:97`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-006](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::rename` is the overload bound at `pysharkbite.cpp:572` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0120 | `HdfsLink` method `int move(const std::string &from_path, const std::string &to_path);` (`include/data/constructs/client/Hdfs.h:99`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-007](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::move` is the overload bound at `pysharkbite.cpp:573` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0121 | `HdfsLink` method `int chmod(const std::string &path, int perm);` (`include/data/constructs/client/Hdfs.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0122 | `HdfsLink` method `int chown(const std::string &path, const std::string &user, const std::string &group);` (`include/data/constructs/client/Hdfs.h:103`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-008](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::chown` is the overload bound at `pysharkbite.cpp:574` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0123 | `HdfsLink` method `std::shared_ptr<cclient::data::streams::OutputStream> write(const std::string &path);` (`include/data/constructs/client/Hdfs.h:105`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-003](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::write` is the overload bound at `pysharkbite.cpp:569` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0124 | `HdfsLink` method `std::shared_ptr<cclient::data::streams::InputStream> read(const std::string &path);` (`include/data/constructs/client/Hdfs.h:106`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-004](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::read` is the overload bound at `pysharkbite.cpp:570` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0125 | `HdfsLink` method `std::vector<HdfsDirEnt> list(const std::string &dir);` (`include/data/constructs/client/Hdfs.h:108`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-002](#sec-16) already carries this obligation — `cclient::data::hdfs::HdfsLink::list` is the overload bound at `pysharkbite.cpp:576` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0126 | `HdfsLink` method `hdfsFS getHdfsReference();` (`include/data/constructs/client/Hdfs.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0127 | `Instance` constructor `Instance()` (`include/data/constructs/client/Instance.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0128 | `Instance` method `virtual std::string getRootTabletLocation() = 0;` (`include/data/constructs/client/Instance.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0129 | `Instance` method `virtual std::vector<std::string> getMasterLocations() = 0;` (`include/data/constructs/client/Instance.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0130 | `Instance` method `virtual std::vector<std::shared_ptr<interconnect::ServerConnection>> getServers() = 0;` (`include/data/constructs/client/Instance.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0131 | `Instance` method `virtual std::string getInstanceId(bool retry = false) = 0;` (`include/data/constructs/client/Instance.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0132 | `Instance` method `virtual std::string getInstanceName() const = 0;` (`include/data/constructs/client/Instance.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0133 | `Instance` method `virtual InstanceCache *getInstanceCache() = 0;` (`include/data/constructs/client/Instance.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0134 | `Instance` method `virtual const cclient::impl::Configuration *getConfiguration() = 0;` (`include/data/constructs/client/Instance.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0135 | `Instance` destructor `virtual ~Instance() {` (`include/data/constructs/client/Instance.h:84`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Instance` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0136 | `InstanceCache` constructor `InstanceCache()` (`include/data/constructs/client/InstanceCache.h:27`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0137 | `InstanceCache` destructor `virtual ~InstanceCache() {` (`include/data/constructs/client/InstanceCache.h:30`) | — | — | — | Missing Go | Assessed individually: `cclient::data::InstanceCache` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0138 | `InstanceCache` method `virtual uint8_t *getData(std::string path) = 0;` (`include/data/constructs/client/InstanceCache.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0139 | `InstanceCache` method `virtual std::vector<std::string> getChildren(const std::string &path, bool force = false) = 0;` (`include/data/constructs/client/InstanceCache.h:35`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0140 | `TabletServerMutations` constructor `explicit TabletServerMutations(std::string sessionId);` (`include/data/constructs/client/TabletServerMutations.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0141 | `TabletServerMutations` constructor `TabletServerMutations(std::string sessionId, uint32_t fails);` (`include/data/constructs/client/TabletServerMutations.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0142 | `TabletServerMutations` destructor `virtual ~TabletServerMutations();` (`include/data/constructs/client/TabletServerMutations.h:32`) | — | — | — | Missing Go | Assessed individually: `cclient::data::TabletServerMutations` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0143 | `TabletServerMutations` method `void addMutation(KeyExtent extent, const std::shared_ptr<Mutation> &m) {` (`include/data/constructs/client/TabletServerMutations.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0144 | `TabletServerMutations` method `std::map<KeyExtent, std::vector<std::shared_ptr<Mutation>>> *getMutations() {` (`include/data/constructs/client/TabletServerMutations.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0145 | `TabletServerMutations` method `void setMaxFailures(uint32_t failures) {` (`include/data/constructs/client/TabletServerMutations.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0146 | `TabletServerMutations` method `uint32_t getMaxFailures() {` (`include/data/constructs/client/TabletServerMutations.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0147 | `Event` public data member `std::string path;` (`include/data/constructs/client/zookeeper/watch.h:32`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0148 | `Event` public data member `int type;` (`include/data/constructs/client/zookeeper/watch.h:33`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0149 | `Watch` constructor `Watch() : connected(false), zookeeperReference(NULL), state(0)` (`include/data/constructs/client/zookeeper/watch.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0150 | `Watch` destructor `~Watch() {` (`include/data/constructs/client/zookeeper/watch.h:51`) | — | — | — | Missing Go | Assessed individually: `cclient::data::zookeeper::Watch` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0151 | `Watch` method `Event popEvent();` (`include/data/constructs/client/zookeeper/watch.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0152 | `Watch` method `void setHandle(zhandle_t *zh);` (`include/data/constructs/client/zookeeper/watch.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0153 | `Watch` method `bool isConnected() const {` (`include/data/constructs/client/zookeeper/watch.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0154 | `Watch` method `bool setConnected(bool val) {` (`include/data/constructs/client/zookeeper/watch.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0155 | `Watch` method `int size() const;` (`include/data/constructs/client/zookeeper/watch.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0156 | `Watch` method `void pushEvent(Event event);` (`include/data/constructs/client/zookeeper/watch.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0157 | `Watch` method `bool waitForConnected(zhandle_t *zh);` (`include/data/constructs/client/zookeeper/watch.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0158 | `Watch` method `bool waitForDisconnected(zhandle_t *zh);` (`include/data/constructs/client/zookeeper/watch.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0159 | `Watch` method `void yield(zhandle_t *zh, int i) {` (`include/data/constructs/client/zookeeper/watch.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0160 | `Watch` method `void setState(int st) {` (`include/data/constructs/client/zookeeper/watch.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0161 | `Watch` method `int getState() {` (`include/data/constructs/client/zookeeper/watch.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0162 | `WatchFn` public data member `Watch *ptr;` (`include/data/constructs/client/zookeeper/watch.h:106`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0163 | `WatchFn` public data member `void *Fn;` (`include/data/constructs/client/zookeeper/watch.h:107`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0164 | `ZooCache` constructor `explicit ZooCache(ZooKeeper *zk) : myZk(zk) {` (`include/data/constructs/client/zookeeper/zoocache.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0165 | `ZooCache` destructor `~ZooCache() {` (`include/data/constructs/client/zookeeper/zoocache.h:52`) | — | — | — | Missing Go | Assessed individually: `cclient::data::zookeeper::ZooCache` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0166 | `ZooCache` method `inline static void cache_watcher(zhandle_t *, int type, int state, const char *path, void *v) {` (`include/data/constructs/client/zookeeper/zoocache.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0167 | `ZooCache` method `uint8_t *getData(std::string path) override {` (`include/data/constructs/client/zookeeper/zoocache.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0168 | `ZooCache` method `std::vector<std::string> getChildren(const std::string &path, bool force = false) override {` (`include/data/constructs/client/zookeeper/zoocache.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0169 | `(namespace scope)` macro constant `#define TIME_BETWEEN_CONNECT_CHECKS_MS 100` (`include/data/constructs/client/zookeeper/zookeepers.h:32`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0170 | `(namespace scope)` macro constant `#define TOTAL_CONNECT_TIME_WAIT_MS 10 * 1000` (`include/data/constructs/client/zookeeper/zookeepers.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0171 | `(namespace scope)` macro constant `#define ZROOT "/accumulo"` (`include/data/constructs/client/zookeeper/zookeepers.h:35`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0172 | `(namespace scope)` macro constant `#define ZINSTANCES "/instances"` (`include/data/constructs/client/zookeeper/zookeepers.h:36`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0173 | `(namespace scope)` macro constant `#define ZMASTERS "/masters"` (`include/data/constructs/client/zookeeper/zookeepers.h:37`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0174 | `(namespace scope)` macro constant `#define ZMASTER_LOCK "/masters/lock"` (`include/data/constructs/client/zookeeper/zookeepers.h:38`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0175 | `(namespace scope)` macro constant `#define ZROOT_TABLET "/root_tablet"` (`include/data/constructs/client/zookeeper/zookeepers.h:39`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0176 | `(namespace scope)` macro constant `#define ZROOT_TABLET_LOCATION "/root_tablet/location"` (`include/data/constructs/client/zookeeper/zookeepers.h:40`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0177 | `(namespace scope)` macro constant `#define TSERVERS "/tservers"` (`include/data/constructs/client/zookeeper/zookeepers.h:41`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0178 | `(namespace scope)` namespace-scope static variable `static std::mutex syncBarrier;` (`include/data/constructs/client/zookeeper/zookeepers.h:50`) | — | — | — | Missing Go | Namespace-scope variable declared `static`, so it has **internal linkage**: every translation unit that includes this header gets its own independent copy, initialised separately. It is not a process-wide object and consumers cannot link against one shared instance — any port that replaces it with a single shared value changes observable behavior, and any port that keeps per-consumer state matches it. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0179 | `(namespace scope)` namespace-scope function `static void watcher_function(zhandle_t *, int type, int state, const char *path, void *v) {` (`include/data/constructs/client/zookeeper/zookeepers.h:52`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0180 | `ZooKeeper` constructor `explicit ZooKeeper(const char *hostPorts, uint32_t timeout) : logger(logging::LoggerFactory<ZooKeeper>::getLogger()), hostPorts((char *)hostPorts), timeout(timeout), zookeeperReference(0), myWatch(nullptr) {` (`include/data/constructs/client/zookeeper/zookeepers.h:83`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0181 | `ZooKeeper` destructor `virtual ~ZooKeeper() {` (`include/data/constructs/client/zookeeper/zookeepers.h:92`) | — | — | — | Missing Go | Assessed individually: `cclient::data::zookeeper::ZooKeeper` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0182 | `ZooKeeper` method `void setWatch(Watch *watch) {` (`include/data/constructs/client/zookeeper/zookeepers.h:99`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0183 | `ZooKeeper` method `bool isConnected() {` (`include/data/constructs/client/zookeeper/zookeepers.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0184 | `ZooKeeper` method `bool exists(std::string path, watcher_fn fn, std::shared_ptr<WatchFn> ptr = nullptr) {` (`include/data/constructs/client/zookeeper/zookeepers.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0185 | `ZooKeeper` method `char *getData(std::string path, watcher_fn fn, std::shared_ptr<WatchFn> ptr = nullptr) {` (`include/data/constructs/client/zookeeper/zookeepers.h:132`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0186 | `ZooKeeper` method `std::vector<std::string> *getChildren(std::string path, watcher_fn fn, std::shared_ptr<WatchFn> ptr = nullptr) {` (`include/data/constructs/client/zookeeper/zookeepers.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0187 | `ZooKeeper` method `void init(Watch *watch) {` (`include/data/constructs/client/zookeeper/zookeepers.h:195`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0188 | `ZooSession` constructor `explicit ZooSession(ZooKeeper *keeper, Watch *watcher) : zooKeeper(keeper) {` (`include/data/constructs/client/zookeeper/zookeepers.h:256`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0189 | `ZooSession` constructor `explicit ZooSession(ZooKeeper *keeper) : zooKeeper(keeper) {` (`include/data/constructs/client/zookeeper/zookeepers.h:263`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0190 | `ZooSession` method `ZooKeeper *getZooKeeper() {` (`include/data/constructs/client/zookeeper/zookeepers.h:269`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0191 | `ZooKeepers` method `static std::string sessionKey(const std::string &keepers, uint16_t timeout, std::string auth) {` (`include/data/constructs/client/zookeeper/zookeepers.h:284`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0192 | `ZooKeepers` method `static ZooSession *getSession(std::string zookeepers, uint16_t timeout, std::string auth) {` (`include/data/constructs/client/zookeeper/zookeepers.h:291`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0193 | `ZooKeepers` method `static ZooKeeper *connect(std::string &hosts, uint32_t timeout, std::string auth, Watch *watcher) {` (`include/data/constructs/client/zookeeper/zookeepers.h:328`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0194 | `ZookeeperInstance` constructor `explicit ZookeeperInstance(const ZookeeperInstance *zki) : ZookeeperInstance(zki->instanceName, zki->zookeeperList, zki->timeoutMs, zki->myConfiguration) {` (`include/data/constructs/client/zookeeperinstance.h:66`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-026](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:66`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0195 | `ZookeeperInstance` constructor `explicit ZookeeperInstance(std::string in, std::string zks, uint32_t zkTimeoutMs, const std::shared_ptr<cclient::impl::Configuration> &conf) : instanceName(in), zookeeperList(zks), timeoutMs(zkTimeoutMs), myConfiguration(conf) {` (`include/data/constructs/client/zookeeperinstance.h:78`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-007](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:78`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0196 | `ZookeeperInstance` constructor `explicit ZookeeperInstance(std::string in, std::string zks, uint32_t zkTimeoutMs) : instanceName(in), zookeeperList(zks), timeoutMs(zkTimeoutMs) {` (`include/data/constructs/client/zookeeperinstance.h:120`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-027](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:120`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0197 | `ZookeeperInstance` destructor `~ZookeeperInstance() {` (`include/data/constructs/client/zookeeperinstance.h:158`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-028](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:158`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0198 | `ZookeeperInstance` method `std::string getRootTabletLocation();` (`include/data/constructs/client/zookeeperinstance.h:170`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-029](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:170`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0199 | `ZookeeperInstance` method `std::vector<std::string> getMasterLocations();` (`include/data/constructs/client/zookeeperinstance.h:176`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-030](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:176`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0200 | `ZookeeperInstance` method `std::string getInstanceId(bool retry = false);` (`include/data/constructs/client/zookeeperinstance.h:182`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-020](#sec-6) already carries this obligation — `cclient::data::zookeeper::ZookeeperInstance::getInstanceId` is the overload bound at `pysharkbite.cpp:73` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0201 | `ZookeeperInstance` method `std::string getInstanceName() const;` (`include/data/constructs/client/zookeeperinstance.h:187`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-009](#sec-6) already carries this obligation — `cclient::data::zookeeper::ZookeeperInstance::getInstanceName` is the overload bound at `pysharkbite.cpp:71` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0202 | `ZookeeperInstance` method `std::string getZookeepers() const;` (`include/data/constructs/client/zookeeperinstance.h:189`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-031](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:189`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0203 | `ZookeeperInstance` method `const cclient::impl::Configuration *getConfiguration();` (`include/data/constructs/client/zookeeperinstance.h:194`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-032](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:194`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0204 | `ZookeeperInstance` method `ZooCache *getZooCache() {` (`include/data/constructs/client/zookeeperinstance.h:204`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-033](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:204`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0205 | `ZookeeperInstance` method `InstanceCache *getInstanceCache() {` (`include/data/constructs/client/zookeeperinstance.h:213`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-034](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:213`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0206 | `ZookeeperInstance` method `virtual std::vector<std::shared_ptr<interconnect::ServerConnection>> getServers();` (`include/data/constructs/client/zookeeperinstance.h:222`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-035](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:222`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0207 | `ZookeeperInstance` method `std::string getRoot();` (`include/data/constructs/client/zookeeperinstance.h:228`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-036](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/client/zookeeperinstance.h:228`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0208 | `Column` constructor `explicit Column(const std::string &columFam) : Column(columFam, std::string(), std::string()) {` (`include/data/constructs/column.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0209 | `Column` constructor `explicit Column(const std::string &columFam, const std::string &columnQual) : Column(columFam, columnQual, std::string()) {` (`include/data/constructs/column.h:49`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0210 | `Column` constructor `explicit Column(const std::string &columnFam, const std::string &columnQual, const std::string &columnVis) : columnFamily(columnFam), columnQualifier(columnQual), columnVisibility( columnVis) {` (`include/data/constructs/column.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0211 | `Column` destructor `virtual ~Column();` (`include/data/constructs/column.h:70`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Column` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0212 | `Column` operator `virtual bool operator==(const Column &other) const;` (`include/data/constructs/column.h:71`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0213 | `Column` operator `bool operator <(const Column &rhs) const;` (`include/data/constructs/column.h:72`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0214 | `Column` operator `bool operator <(const Column *rhs) const {` (`include/data/constructs/column.h:74`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0215 | `Column` method `uint64_t write(cclient::data::streams::OutputStream *outStream) override;` (`include/data/constructs/column.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0216 | `Column` method `void setColFamily(const char *r, uint32_t size);` (`include/data/constructs/column.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0217 | `Column` method `std::string getColFamily() const {` (`include/data/constructs/column.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0218 | `Column` method `void setColQualifier(const char *r, uint32_t size);` (`include/data/constructs/column.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0219 | `Column` method `std::string getColQualifier() const {` (`include/data/constructs/column.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0220 | `Column` method `void setColVisibility(const char *r, uint32_t size);` (`include/data/constructs/column.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0221 | `Column` method `std::string getColVisibility() const {` (`include/data/constructs/column.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0222 | `Column` operator `bool operator==(const Column &other) {` (`include/data/constructs/column.h:97`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0223 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/constructs/compressor/algorithm.h:20`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0224 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/constructs/compressor/algorithm.h:21`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0225 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/constructs/compressor/algorithm.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0226 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/constructs/compressor/algorithm.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0227 | `Algorithm` constructor `Algorithm() : compressionAlgo("")` (`include/data/constructs/compressor/algorithm.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0228 | `Algorithm` constructor `explicit Algorithm(const std::string &compressionName) : compressionAlgo(compressionName) {` (`include/data/constructs/compressor/algorithm.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0229 | `Algorithm` constructor `Algorithm(const Algorithm &other) = default;` (`include/data/constructs/compressor/algorithm.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0230 | `Algorithm` constructor `Algorithm(Algorithm &&other) = default;` (`include/data/constructs/compressor/algorithm.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0231 | `Algorithm` method `void setAlgorithm(const std::string &algorithm) {` (`include/data/constructs/compressor/algorithm.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0232 | `Algorithm` method `std::string getName() const {` (`include/data/constructs/compressor/algorithm.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0233 | `Algorithm` operator `Algorithm & operator=(const Algorithm &other) = default;` (`include/data/constructs/compressor/algorithm.h:71`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0234 | `Algorithm` operator `Algorithm & operator=(Algorithm &&other) = default;` (`include/data/constructs/compressor/algorithm.h:74`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0235 | `Algorithm` method `std::string getAlgorithm() const {` (`include/data/constructs/compressor/algorithm.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0236 | `CompressionAlgorithm` constructor `CompressionAlgorithm() : Algorithm("")` (`include/data/constructs/compressor/compression_algorithm.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0237 | `CompressionAlgorithm` constructor `explicit CompressionAlgorithm(const std::string compressionName) : Algorithm(compressionName) {` (`include/data/constructs/compressor/compression_algorithm.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0238 | `CompressionAlgorithm` constructor `explicit CompressionAlgorithm(const Algorithm &other) : Algorithm(other.getAlgorithm()) {` (`include/data/constructs/compressor/compression_algorithm.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0239 | `CompressionAlgorithm` constructor `CompressionAlgorithm(const CompressionAlgorithm &other) = default;` (`include/data/constructs/compressor/compression_algorithm.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0240 | `CompressionAlgorithm` constructor `CompressionAlgorithm(CompressionAlgorithm &&other) = default;` (`include/data/constructs/compressor/compression_algorithm.h:44`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0241 | `CompressionAlgorithm` operator `CompressionAlgorithm & operator=(const Algorithm &other) {` (`include/data/constructs/compressor/compression_algorithm.h:47`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0242 | `CompressionAlgorithm` operator `CompressionAlgorithm & operator=(const CompressionAlgorithm &other) = default;` (`include/data/constructs/compressor/compression_algorithm.h:54`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0243 | `CompressionAlgorithm` operator `CompressionAlgorithm & operator=(CompressionAlgorithm &&other) = default;` (`include/data/constructs/compressor/compression_algorithm.h:57`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0244 | `Compressor` constructor `Compressor() : len(0), off(0), stream_offset(0)` (`include/data/constructs/compressor/compressor.h:36`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0245 | `Compressor` destructor `virtual ~Compressor() {` (`include/data/constructs/compressor/compressor.h:41`) | — | — | — | Missing Go | Assessed individually: `cclient::data::compression::Compressor` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0246 | `Compressor` method `virtual std::unique_ptr<Compressor> newInstance() = 0;` (`include/data/constructs/compressor/compressor.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0247 | `Compressor` method `void setInput(const char *b, uint32_t offset, uint32_t length);` (`include/data/constructs/compressor/compressor.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0248 | `Compressor` method `void setStreamOffset(uint32_t soff) {` (`include/data/constructs/compressor/compressor.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0249 | `Compressor` method `uint32_t getStreamOffset() {` (`include/data/constructs/compressor/compressor.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0250 | `Compressor` method `virtual void compress(cclient::data::streams::OutputStream *out_stream) = 0;` (`include/data/constructs/compressor/compressor.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0251 | `Compressor` method `virtual void decompress(cclient::data::streams::ByteOutputStream *out_stream, char *in_buf = nullptr, size_t size = 0) {` (`include/data/constructs/compressor/compressor.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0252 | `Compressor` method `virtual uint32_t getBufferSize() = 0;` (`include/data/constructs/compressor/compressor.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0253 | `Compressor` method `virtual uint32_t getCompressionOverHead() = 0;` (`include/data/constructs/compressor/compressor.h:109`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0254 | `Compressor` method `virtual uint32_t bytesWritten() = 0;` (`include/data/constructs/compressor/compressor.h:116`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0255 | `Compressor` method `virtual uint32_t getCompressedSize() = 0;` (`include/data/constructs/compressor/compressor.h:122`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0256 | `Compressor` method `CompressionAlgorithm &getAlgorithm() {` (`include/data/constructs/compressor/compressor.h:128`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0257 | `Compressor` method `virtual std::string getName() = 0;` (`include/data/constructs/compressor/compressor.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0258 | `Compressor` method `int getBytesRead() {` (`include/data/constructs/compressor/compressor.h:145`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0259 | `CompressorFactory` method `static ZLibCompressor *getZlibCompressor() {` (`include/data/constructs/compressor/CompressorFactory.h:33`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0260 | `CompressorFactory` method `static std::unique_ptr<Compressor> create(const CompressionAlgorithm &algo) {` (`include/data/constructs/compressor/CompressorFactory.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0261 | `(namespace scope)` macro constant `#define GZ_NAME "gz"` (`include/data/constructs/compressor/zlibCompressor.h:28`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0262 | `ZLibCompressor` constructor `explicit ZLibCompressor();` (`include/data/constructs/compressor/zlibCompressor.h:40`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0263 | `ZLibCompressor` constructor `explicit ZLibCompressor(uint32_t in_len);` (`include/data/constructs/compressor/zlibCompressor.h:45`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0264 | `ZLibCompressor` destructor `virtual ~ZLibCompressor();` (`include/data/constructs/compressor/zlibCompressor.h:47`) | — | — | — | Missing Go | Assessed individually: `cclient::data::compression::ZLibCompressor` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0265 | `ZLibCompressor` method `virtual std::unique_ptr<Compressor> newInstance() {` (`include/data/constructs/compressor/zlibCompressor.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0266 | `ZLibCompressor` method `uint32_t getBufferSize() {` (`include/data/constructs/compressor/zlibCompressor.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0267 | `ZLibCompressor` method `uint32_t getCompressionOverHead() {` (`include/data/constructs/compressor/zlibCompressor.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0268 | `ZLibCompressor` method `uint32_t bytesWritten() {` (`include/data/constructs/compressor/zlibCompressor.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0269 | `ZLibCompressor` method `uint32_t getCompressedSize() {` (`include/data/constructs/compressor/zlibCompressor.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0270 | `ZLibCompressor` method `void compress(cclient::data::streams::OutputStream *out_stream);` (`include/data/constructs/compressor/zlibCompressor.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0271 | `ZLibCompressor` method `void decompress(cclient::data::streams::ByteOutputStream *out_stream, char *in_buf = nullptr, size_t size = 0);` (`include/data/constructs/compressor/zlibCompressor.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0272 | `(namespace scope)` alias `using CompressorObj = cclient::data::compression::Compressor;` (`include/data/constructs/CompressorPool.h:34`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. client-side data construct. |
| SB-CXX-0273 | `CompressorPool` constructor `CompressorPool();` (`include/data/constructs/CompressorPool.h:53`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0274 | `CompressorPool` destructor `virtual ~CompressorPool();` (`include/data/constructs/CompressorPool.h:55`) | — | — | — | Missing Go | Assessed individually: `cclient::data::CompressorPool` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0275 | `CompressorPool` method `virtual void disown(std::pair<char*, size_t>) override;` (`include/data/constructs/CompressorPool.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0276 | `CompressorPool` method `virtual std::pair<char*, size_t> allocateBuffer(size_t size) override;` (`include/data/constructs/CompressorPool.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0277 | `CompressorPool` method `virtual std::shared_ptr<Text> allocate(size_t size) override;` (`include/data/constructs/CompressorPool.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0278 | `CompressorPool` method `virtual void free(std::pair<char*, size_t> &&ptr) override;` (`include/data/constructs/CompressorPool.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0279 | `CompressorPool` method `std::shared_ptr<cclient::data::Key> newKey() {` (`include/data/constructs/CompressorPool.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0280 | `CompressorPool` method `static CompressorPool* newInstance() {` (`include/data/constructs/CompressorPool.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0281 | `CompressorPool` method `static void returnInstance(CompressorPool *pool) {` (`include/data/constructs/CompressorPool.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0282 | `CompressorPool` method `static CompressorPool* getInstance() {` (`include/data/constructs/CompressorPool.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0283 | `Configuration` constructor `Configuration()` (`include/data/constructs/configuration/Configuration.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-001](#sec-6) already carries this obligation — `cclient::impl::Configuration::Configuration` is the overload bound at `pysharkbite.cpp:58` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0284 | `Configuration` destructor `virtual ~Configuration();` (`include/data/constructs/configuration/Configuration.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-025](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/configuration/Configuration.h:39`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0285 | `Configuration` method `void set(const std::string &name, const std::string &value);` (`include/data/constructs/configuration/Configuration.h:45`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-002](#sec-6) already carries this obligation — `cclient::impl::Configuration::set` is the overload bound at `pysharkbite.cpp:59` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0286 | `Configuration` method `std::string get(const std::string &name) const;` (`include/data/constructs/configuration/Configuration.h:50`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-003](#sec-6) already carries this obligation — `cclient::impl::Configuration::get` is the overload bound at `pysharkbite.cpp:60` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0287 | `Configuration` method `std::string get(const std::string &name, const std::string &def) const;` (`include/data/constructs/configuration/Configuration.h:55`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-004](#sec-6) already carries this obligation — `cclient::impl::Configuration::get` is the overload bound at `pysharkbite.cpp:61` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0288 | `Configuration` method `uint32_t getLong(const std::string &name) const;` (`include/data/constructs/configuration/Configuration.h:57`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-024](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/configuration/Configuration.h:57`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0289 | `Configuration` method `uint32_t getLong(const std::string &name, uint32_t def) const;` (`include/data/constructs/configuration/Configuration.h:58`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-005](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/configuration/Configuration.h:58`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0290 | `(namespace scope)` macro constant `#define FILE_SYSTEM_ROOT_CFG "FILE_SYSTEM_ROOT"` (`include/data/constructs/ConfigurationOptions.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0291 | `(namespace scope)` macro constant `#define TABLE_SUFFIX "/tables"` (`include/data/constructs/ConfigurationOptions.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0292 | `(namespace scope)` macro constant `#define TABLE_GET_NAME "/name"` (`include/data/constructs/ConfigurationOptions.h:27`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0293 | `(namespace scope)` macro constant `#define TABLE_GET_NAMESPACE "/namespace"` (`include/data/constructs/ConfigurationOptions.h:28`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0294 | `(namespace scope)` macro constant `#define TABLE_GET_NAMESPACES "/namespaces"` (`include/data/constructs/ConfigurationOptions.h:29`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0295 | `(namespace scope)` macro constant `#define DEFAULT_NAMESPACE_ID "+default"` (`include/data/constructs/ConfigurationOptions.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0296 | `(namespace scope)` macro constant `#define DEFAULT_NAMESPACE "+"` (`include/data/constructs/ConfigurationOptions.h:31`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0297 | `SharedPool` alias `using ptr_type = std::shared_ptr<T, External_Deleter >;` (`include/data/constructs/ConstructPool.h:55`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. client-side data construct. |
| SB-CXX-0298 | `SharedPool` constructor `SharedPool() : this_ptr_(new SharedPool<T>*(this)) {}` (`include/data/constructs/ConstructPool.h:57`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0299 | `SharedPool` destructor `virtual ~SharedPool(){}` (`include/data/constructs/ConstructPool.h:58`) | — | — | — | Missing Go | Assessed individually: `cclient::data::SharedPool` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0300 | `SharedPool` method `void add(std::shared_ptr<T> t) {` (`include/data/constructs/ConstructPool.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0301 | `SharedPool` method `ptr_type acquire(ObjectAllocatorPool<Text> *x) {` (`include/data/constructs/ConstructPool.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0302 | `SharedPool` method `bool empty() const {` (`include/data/constructs/ConstructPool.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0303 | `SharedPool` method `size_t size() const {` (`include/data/constructs/ConstructPool.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0304 | `DeadServer` public data member `std::string server;` (`include/data/constructs/coordinator/AccumuloInfo.h:32`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-049](#sec-14) already carries this obligation — `cclient::data::DeadServer::server` is the overload bound at `pysharkbite.cpp:188` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0305 | `DeadServer` public data member `int64_t lastStatus;` (`include/data/constructs/coordinator/AccumuloInfo.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-051](#sec-14) already carries this obligation — `cclient::data::DeadServer::lastStatus` is the overload bound at `pysharkbite.cpp:190` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0306 | `DeadServer` public data member `std::string status;` (`include/data/constructs/coordinator/AccumuloInfo.h:34`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-053](#sec-14) already carries this obligation — `cclient::data::DeadServer::status` is the overload bound at `pysharkbite.cpp:192` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0307 | `DeadServer` constructor `explicit DeadServer(const std::string &server, int64_t last, std::string status) : server(server), lastStatus(last), status(status) {` (`include/data/constructs/coordinator/AccumuloInfo.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0308 | `DeadServer` method `auto getServer() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-035](#sec-14) already carries this obligation — `cclient::data::DeadServer::getServer` is the overload bound at `pysharkbite.cpp:187` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0309 | `DeadServer` method `auto getLastContact() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:44`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-050](#sec-14) already carries this obligation — `cclient::data::DeadServer::getLastContact` is the overload bound at `pysharkbite.cpp:189` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0310 | `DeadServer` method `auto getStatus() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-052](#sec-14) already carries this obligation — `cclient::data::DeadServer::getStatus` is the overload bound at `pysharkbite.cpp:191` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0311 | `CoordinatorState` struct (no public members) `struct CoordinatorState {` (`include/data/constructs/coordinator/AccumuloInfo.h:55`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. client-side data construct. |
| SB-CXX-0312 | `type` enum `enum type {` (`include/data/constructs/coordinator/AccumuloInfo.h:57`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: INITIAL = 0; HAVE_LOCK = 1; SAFE_MODE = 2; NORMAL = 3; UNLOAD_METADATA_TABLETS = 4; UNLOAD_ROOT_TABLET = 5; STOP = 6. Shoal exposes no equivalent type. |
| SB-CXX-0313 | `CoordinatorGoalState` struct (no public members) `struct CoordinatorGoalState {` (`include/data/constructs/coordinator/AccumuloInfo.h:69`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. client-side data construct. |
| SB-CXX-0314 | `type` enum `enum type {` (`include/data/constructs/coordinator/AccumuloInfo.h:71`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: CLEAN_STOP = 0; SAFE_MODE = 1; NORMAL = 2. Shoal exposes no equivalent type. |
| SB-CXX-0315 | `AccumuloInfo` method `static AccumuloInfoBuilder make();` (`include/data/constructs/coordinator/AccumuloInfo.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0316 | `AccumuloInfo` public data member `std::map<std::string, TableInfo> tableMap;` (`include/data/constructs/coordinator/AccumuloInfo.h:101`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-054](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::tableMap` is the overload bound at `pysharkbite.cpp:211` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0317 | `AccumuloInfo` public data member `std::vector<TabletServerStatus> tServerInfo;` (`include/data/constructs/coordinator/AccumuloInfo.h:102`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-055](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::tServerInfo` is the overload bound at `pysharkbite.cpp:213` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0318 | `AccumuloInfo` public data member `std::map<std::string, int8_t> badTServers;` (`include/data/constructs/coordinator/AccumuloInfo.h:103`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-056](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::badTServers` is the overload bound at `pysharkbite.cpp:215` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0319 | `AccumuloInfo` public data member `CoordinatorState::type state;` (`include/data/constructs/coordinator/AccumuloInfo.h:104`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-057](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::state` is the overload bound at `pysharkbite.cpp:217` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0320 | `AccumuloInfo` public data member `CoordinatorGoalState::type goalState;` (`include/data/constructs/coordinator/AccumuloInfo.h:105`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-058](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::goalState` is the overload bound at `pysharkbite.cpp:219` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0321 | `AccumuloInfo` public data member `int32_t unassignedTablets;` (`include/data/constructs/coordinator/AccumuloInfo.h:106`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-059](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::unassignedTablets` is the overload bound at `pysharkbite.cpp:221` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0322 | `AccumuloInfo` public data member `std::set<std::string> serversShuttingDown;` (`include/data/constructs/coordinator/AccumuloInfo.h:107`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-060](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::serversShuttingDown` is the overload bound at `pysharkbite.cpp:223` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0323 | `AccumuloInfo` public data member `std::vector<DeadServer> deadTabletServers;` (`include/data/constructs/coordinator/AccumuloInfo.h:108`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-061](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::deadTabletServers` is the overload bound at `pysharkbite.cpp:225` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0324 | `AccumuloInfo` method `auto getTableMap() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:110`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-001](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getTableMap` is the overload bound at `pysharkbite.cpp:210` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0325 | `AccumuloInfo` method `auto getTabletServerInfo() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:115`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-002](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getTabletServerInfo` is the overload bound at `pysharkbite.cpp:212` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0326 | `AccumuloInfo` method `auto getBadTabletServers() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:120`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-003](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getBadTabletServers` is the overload bound at `pysharkbite.cpp:214` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0327 | `AccumuloInfo` method `auto getState() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:125`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-004](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getState` is the overload bound at `pysharkbite.cpp:216` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0328 | `AccumuloInfo` method `auto getGoalState() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:130`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-005](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getGoalState` is the overload bound at `pysharkbite.cpp:218` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0329 | `AccumuloInfo` method `auto getUnassignedTablets() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:135`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-006](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getUnassignedTablets` is the overload bound at `pysharkbite.cpp:220` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0330 | `AccumuloInfo` method `auto getServerShuttingDown() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:140`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-007](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getServerShuttingDown` is the overload bound at `pysharkbite.cpp:222` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0331 | `AccumuloInfo` method `auto getDeadServers() const {` (`include/data/constructs/coordinator/AccumuloInfo.h:145`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-008](#sec-14) already carries this obligation — `cclient::data::AccumuloInfo::getDeadServers` is the overload bound at `pysharkbite.cpp:224` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0332 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &tableMap(const std::map<std::string, TableInfo> &tableMap) {` (`include/data/constructs/coordinator/AccumuloInfo.h:154`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0333 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &tabletServerInfo(const std::vector<TabletServerStatus> &tServerInfo) {` (`include/data/constructs/coordinator/AccumuloInfo.h:160`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0334 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &badTabletServers(const std::map<std::string, int8_t> &badTServers) {` (`include/data/constructs/coordinator/AccumuloInfo.h:166`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0335 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &state(CoordinatorState::type state) {` (`include/data/constructs/coordinator/AccumuloInfo.h:172`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0336 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &goalState(CoordinatorGoalState::type goalState) {` (`include/data/constructs/coordinator/AccumuloInfo.h:178`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0337 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &unassignedTablets(int32_t unassignedTablets) {` (`include/data/constructs/coordinator/AccumuloInfo.h:184`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0338 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &serversShuttingDown(const std::set<std::string> &serversShuttingDown) {` (`include/data/constructs/coordinator/AccumuloInfo.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0339 | `AccumuloInfoBuilder` method `AccumuloInfoBuilder &deadTabletServers(const std::vector<DeadServer> &deadTabletServers) {` (`include/data/constructs/coordinator/AccumuloInfo.h:196`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0340 | `AccumuloInfoBuilder` operator `operator AccumuloInfo &&() {` (`include/data/constructs/coordinator/AccumuloInfo.h:202`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. This is a **conversion operator**: it makes the type implicitly convertible, so a port must decide between an explicit accessor and an implicit conversion, and the choice is user-visible. client-side data construct. |
| SB-CXX-0341 | `TableRates` public data member `double ingestRate;` (`include/data/constructs/coordinator/TableInfo.h:29`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0342 | `TableRates` public data member `double ingestByteRate;` (`include/data/constructs/coordinator/TableInfo.h:30`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0343 | `TableRates` public data member `double queryRate;` (`include/data/constructs/coordinator/TableInfo.h:31`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0344 | `TableRates` public data member `double queryByteRate;` (`include/data/constructs/coordinator/TableInfo.h:32`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-028](#sec-14) already carries this obligation — `cclient::data::TableRates::queryByteRate` is the overload bound at `pysharkbite.cpp:110` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0345 | `TableRates` public data member `double scanRate;` (`include/data/constructs/coordinator/TableInfo.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-043](#sec-14) already carries this obligation — `cclient::data::TableRates::scanRate` is the overload bound at `pysharkbite.cpp:118` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0346 | `TableRates` constructor `explicit TableRates(double ingestRate, double ingestByteRate, double queryRate, double queryByteRate, double scanRate) : ingestRate(ingestRate), ingestByteRate(ingestByteRate), queryRate(queryRate), queryByteRate(queryByteRate), scanRate(scanRate) {` (`include/data/constructs/coordinator/TableInfo.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0347 | `TableRates` method `auto getIngestRate() const {` (`include/data/constructs/coordinator/TableInfo.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-027](#sec-14) already carries this obligation — `cclient::data::TableRates::getIngestRate` is the overload bound at `pysharkbite.cpp:109` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0348 | `TableRates` method `auto getIngestRateByte() const {` (`include/data/constructs/coordinator/TableInfo.h:44`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-039](#sec-14) already carries this obligation — `cclient::data::TableRates::getIngestRateByte` is the overload bound at `pysharkbite.cpp:111` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0349 | `TableRates` method `auto getQueryRate() const {` (`include/data/constructs/coordinator/TableInfo.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-040](#sec-14) already carries this obligation — `cclient::data::TableRates::getQueryRate` is the overload bound at `pysharkbite.cpp:113` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0350 | `TableRates` method `auto getQueryRateByte() const {` (`include/data/constructs/coordinator/TableInfo.h:54`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-041](#sec-14) already carries this obligation — `cclient::data::TableRates::getQueryRateByte` is the overload bound at `pysharkbite.cpp:115` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0351 | `TableRates` method `auto getScanRate() const {` (`include/data/constructs/coordinator/TableInfo.h:59`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-042](#sec-14) already carries this obligation — `cclient::data::TableRates::getScanRate` is the overload bound at `pysharkbite.cpp:117` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0352 | `Compacting` public data member `int32_t running;` (`include/data/constructs/coordinator/TableInfo.h:68`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-083](#sec-14) already carries this obligation — `cclient::data::Compacting::running` is the overload bound at `pysharkbite.cpp:123` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0353 | `Compacting` public data member `int32_t queued;` (`include/data/constructs/coordinator/TableInfo.h:69`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-084](#sec-14) already carries this obligation — `cclient::data::Compacting::queued` is the overload bound at `pysharkbite.cpp:125` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0354 | `Compacting` constructor `explicit Compacting(int32_t running, int32_t queued) : running(running), queued(queued) {` (`include/data/constructs/coordinator/TableInfo.h:71`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0355 | `Compacting` method `auto getRunning() const {` (`include/data/constructs/coordinator/TableInfo.h:75`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-032](#sec-14) already carries this obligation — `cclient::data::Compacting::getRunning` is the overload bound at `pysharkbite.cpp:122` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0356 | `Compacting` method `auto getQueued() const {` (`include/data/constructs/coordinator/TableInfo.h:80`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-033](#sec-14) already carries this obligation — `cclient::data::Compacting::getQueued` is the overload bound at `pysharkbite.cpp:124` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0357 | `TableCompactions` public data member `Compacting minors;` (`include/data/constructs/coordinator/TableInfo.h:89`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-080](#sec-14) already carries this obligation — `cclient::data::TableCompactions::minors` is the overload bound at `pysharkbite.cpp:138` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0358 | `TableCompactions` public data member `Compacting majors;` (`include/data/constructs/coordinator/TableInfo.h:90`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-081](#sec-14) already carries this obligation — `cclient::data::TableCompactions::majors` is the overload bound at `pysharkbite.cpp:140` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0359 | `TableCompactions` public data member `Compacting scans;` (`include/data/constructs/coordinator/TableInfo.h:91`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-082](#sec-14) already carries this obligation — `cclient::data::TableCompactions::scans` is the overload bound at `pysharkbite.cpp:142` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0360 | `TableCompactions` constructor `explicit TableCompactions(Compacting minors, Compacting majors, Compacting scans) : minors(minors), majors(majors), scans(scans) {` (`include/data/constructs/coordinator/TableInfo.h:93`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0361 | `TableCompactions` method `auto getMinorCompactions() const {` (`include/data/constructs/coordinator/TableInfo.h:97`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-029](#sec-14) already carries this obligation — `cclient::data::TableCompactions::getMinorCompactions` is the overload bound at `pysharkbite.cpp:137` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0362 | `TableCompactions` method `auto getMajorCompactions() const {` (`include/data/constructs/coordinator/TableInfo.h:102`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-030](#sec-14) already carries this obligation — `cclient::data::TableCompactions::getMajorCompactions` is the overload bound at `pysharkbite.cpp:139` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0363 | `TableCompactions` method `auto getScans() const {` (`include/data/constructs/coordinator/TableInfo.h:107`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-031](#sec-14) already carries this obligation — `cclient::data::TableCompactions::getScans` is the overload bound at `pysharkbite.cpp:141` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0364 | `TableInfo` public data member `int64_t recs;` (`include/data/constructs/coordinator/TableInfo.h:116`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-074](#sec-14) already carries this obligation — `cclient::data::TableInfo::recs` is the overload bound at `pysharkbite.cpp:146` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0365 | `TableInfo` public data member `int64_t recsInMemory;` (`include/data/constructs/coordinator/TableInfo.h:117`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-075](#sec-14) already carries this obligation — `cclient::data::TableInfo::recsInMemory` is the overload bound at `pysharkbite.cpp:148` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0366 | `TableInfo` public data member `int32_t tablets;` (`include/data/constructs/coordinator/TableInfo.h:118`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-076](#sec-14) already carries this obligation — `cclient::data::TableInfo::tablets` is the overload bound at `pysharkbite.cpp:150` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0367 | `TableInfo` public data member `int32_t onlineTablets;` (`include/data/constructs/coordinator/TableInfo.h:119`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-077](#sec-14) already carries this obligation — `cclient::data::TableInfo::onlineTablets` is the overload bound at `pysharkbite.cpp:152` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0368 | `TableInfo` public data member `TableRates rates;` (`include/data/constructs/coordinator/TableInfo.h:120`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-078](#sec-14) already carries this obligation — `cclient::data::TableInfo::rates` is the overload bound at `pysharkbite.cpp:154` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0369 | `TableInfo` public data member `TableCompactions compactionInfo;` (`include/data/constructs/coordinator/TableInfo.h:121`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-079](#sec-14) already carries this obligation — `cclient::data::TableInfo::compactionInfo` is the overload bound at `pysharkbite.cpp:156` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0370 | `TableInfo` constructor `explicit TableInfo( int64_t recs, int64_t recsInMemory, int32_t tablets, int32_t onlineTablets, TableRates rates, TableCompactions compactions) : recs(recs), recsInMemory(recsInMemory), tablets(tablets), onlineTablets(onlineTablets), rates(rates), compactionInfo(compactions) {` (`include/data/constructs/coordinator/TableInfo.h:123`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0371 | `TableInfo` method `auto getRecords() const {` (`include/data/constructs/coordinator/TableInfo.h:128`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-021](#sec-14) already carries this obligation — `cclient::data::TableInfo::getRecords` is the overload bound at `pysharkbite.cpp:145` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0372 | `TableInfo` method `auto getRecordsInMemory() const {` (`include/data/constructs/coordinator/TableInfo.h:133`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-022](#sec-14) already carries this obligation — `cclient::data::TableInfo::getRecordsInMemory` is the overload bound at `pysharkbite.cpp:147` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0373 | `TableInfo` method `auto getTablets() const {` (`include/data/constructs/coordinator/TableInfo.h:138`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-023](#sec-14) already carries this obligation — `cclient::data::TableInfo::getTablets` is the overload bound at `pysharkbite.cpp:149` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0374 | `TableInfo` method `auto getOnlineTablets() const {` (`include/data/constructs/coordinator/TableInfo.h:143`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-024](#sec-14) already carries this obligation — `cclient::data::TableInfo::getOnlineTablets` is the overload bound at `pysharkbite.cpp:151` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0375 | `TableInfo` method `auto getTableRates() const {` (`include/data/constructs/coordinator/TableInfo.h:148`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-025](#sec-14) already carries this obligation — `cclient::data::TableInfo::getTableRates` is the overload bound at `pysharkbite.cpp:153` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0376 | `TableInfo` method `auto getCompactioninfo() const {` (`include/data/constructs/coordinator/TableInfo.h:153`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-026](#sec-14) already carries this obligation — `cclient::data::TableInfo::getCompactioninfo` is the overload bound at `pysharkbite.cpp:155` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0377 | `RecoveryStatus` public data member `std::string name;` (`include/data/constructs/coordinator/TabletServerStatus.h:30`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-044](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::name` is the overload bound at `pysharkbite.cpp:130` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0378 | `RecoveryStatus` public data member `int32_t runtime;` (`include/data/constructs/coordinator/TabletServerStatus.h:31`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-046](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::runtime` is the overload bound at `pysharkbite.cpp:132` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0379 | `RecoveryStatus` public data member `double progress;` (`include/data/constructs/coordinator/TabletServerStatus.h:32`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-048](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::progress` is the overload bound at `pysharkbite.cpp:134` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0380 | `RecoveryStatus` constructor `explicit RecoveryStatus(std::string name, int32_t runtime, double progress) : name(name), runtime(runtime), progress(progress) {` (`include/data/constructs/coordinator/TabletServerStatus.h:33`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0381 | `RecoveryStatus` method `auto getName() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-034](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::getName` is the overload bound at `pysharkbite.cpp:129` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0382 | `RecoveryStatus` method `auto getRuntime() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:44`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-045](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::getRuntime` is the overload bound at `pysharkbite.cpp:131` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0383 | `RecoveryStatus` method `auto getProgress() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-047](#sec-14) already carries this obligation — `cclient::data::RecoveryStatus::getProgress` is the overload bound at `pysharkbite.cpp:133` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0384 | `TabletServerStatus` method `static TabletServerStatusBuilder make();` (`include/data/constructs/coordinator/TabletServerStatus.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0385 | `TabletServerStatus` method `auto getTableMap() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:62`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-009](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getTableMap` is the overload bound at `pysharkbite.cpp:159` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0386 | `TabletServerStatus` method `auto getLastContact() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:67`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-010](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getLastContact` is the overload bound at `pysharkbite.cpp:161` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0387 | `TabletServerStatus` method `auto getName() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:72`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-011](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getName` is the overload bound at `pysharkbite.cpp:163` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0388 | `TabletServerStatus` method `auto getOsLoad() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:77`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-012](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getOsLoad` is the overload bound at `pysharkbite.cpp:165` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0389 | `TabletServerStatus` method `auto getLookups() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:82`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-013](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getLookups` is the overload bound at `pysharkbite.cpp:167` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0390 | `TabletServerStatus` method `auto getIndexCacheHits() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:87`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-014](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getIndexCacheHits` is the overload bound at `pysharkbite.cpp:169` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0391 | `TabletServerStatus` method `auto getDataCacheHits() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:92`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-015](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getDataCacheHits` is the overload bound at `pysharkbite.cpp:171` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0392 | `TabletServerStatus` method `auto getDataCacheRequests() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:97`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-016](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getDataCacheRequests` is the overload bound at `pysharkbite.cpp:173` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0393 | `TabletServerStatus` method `auto getLogSorts() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:102`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-017](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getLogSorts` is the overload bound at `pysharkbite.cpp:175` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0394 | `TabletServerStatus` method `auto getFlushes() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:107`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-018](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getFlushes` is the overload bound at `pysharkbite.cpp:177` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0395 | `TabletServerStatus` method `auto getSyncs() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:112`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-019](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getSyncs` is the overload bound at `pysharkbite.cpp:179` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0396 | `TabletServerStatus` method `auto getHoldTime() const {` (`include/data/constructs/coordinator/TabletServerStatus.h:116`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-020](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::getHoldTime` is the overload bound at `pysharkbite.cpp:181` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0397 | `TabletServerStatus` public data member `std::map<std::string, cclient::data::TableInfo> tableMap;` (`include/data/constructs/coordinator/TabletServerStatus.h:120`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-062](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::tableMap` is the overload bound at `pysharkbite.cpp:160` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0398 | `TabletServerStatus` public data member `int64_t lastContact;` (`include/data/constructs/coordinator/TabletServerStatus.h:121`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-063](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::lastContact` is the overload bound at `pysharkbite.cpp:162` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0399 | `TabletServerStatus` public data member `std::string name;` (`include/data/constructs/coordinator/TabletServerStatus.h:122`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-064](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::name` is the overload bound at `pysharkbite.cpp:164` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0400 | `TabletServerStatus` public data member `double osLoad;` (`include/data/constructs/coordinator/TabletServerStatus.h:123`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-065](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::osLoad` is the overload bound at `pysharkbite.cpp:166` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0401 | `TabletServerStatus` public data member `int64_t holdTime;` (`include/data/constructs/coordinator/TabletServerStatus.h:124`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-073](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::holdTime` is the overload bound at `pysharkbite.cpp:182` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0402 | `TabletServerStatus` public data member `int64_t lookups;` (`include/data/constructs/coordinator/TabletServerStatus.h:125`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-066](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::lookups` is the overload bound at `pysharkbite.cpp:168` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0403 | `TabletServerStatus` public data member `int64_t indexCacheHits;` (`include/data/constructs/coordinator/TabletServerStatus.h:126`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-067](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::indexCacheHits` is the overload bound at `pysharkbite.cpp:170` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0404 | `TabletServerStatus` public data member `int64_t indexCacheRequest;` (`include/data/constructs/coordinator/TabletServerStatus.h:127`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0405 | `TabletServerStatus` public data member `int64_t dataCacheHits;` (`include/data/constructs/coordinator/TabletServerStatus.h:128`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-068](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::dataCacheHits` is the overload bound at `pysharkbite.cpp:172` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0406 | `TabletServerStatus` public data member `int64_t dataCacheRequest;` (`include/data/constructs/coordinator/TabletServerStatus.h:129`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-069](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::dataCacheRequest` is the overload bound at `pysharkbite.cpp:174` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0407 | `TabletServerStatus` public data member `std::vector<RecoveryStatus> logSorts;` (`include/data/constructs/coordinator/TabletServerStatus.h:130`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-070](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::logSorts` is the overload bound at `pysharkbite.cpp:176` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0408 | `TabletServerStatus` public data member `int64_t flushs;` (`include/data/constructs/coordinator/TabletServerStatus.h:131`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-071](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::flushs` is the overload bound at `pysharkbite.cpp:178` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0409 | `TabletServerStatus` public data member `int64_t syncs;` (`include/data/constructs/coordinator/TabletServerStatus.h:132`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-STAT-072](#sec-14) already carries this obligation — `cclient::data::TabletServerStatus::syncs` is the overload bound at `pysharkbite.cpp:180` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0410 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &tableMap(std::map<std::string, TableInfo> map) {` (`include/data/constructs/coordinator/TabletServerStatus.h:144`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0411 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &lastContact(int64_t lastContact) {` (`include/data/constructs/coordinator/TabletServerStatus.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0412 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &name(std::string name) {` (`include/data/constructs/coordinator/TabletServerStatus.h:156`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0413 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &osLoad(double osLoad) {` (`include/data/constructs/coordinator/TabletServerStatus.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0414 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &holdTime(int64_t holdTime) {` (`include/data/constructs/coordinator/TabletServerStatus.h:168`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0415 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &lookups(int64_t lookups) {` (`include/data/constructs/coordinator/TabletServerStatus.h:174`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0416 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &indexCacheHits(int64_t indexCacheHits) {` (`include/data/constructs/coordinator/TabletServerStatus.h:180`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0417 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &indexCacheRequest(int64_t indexCacheRequest) {` (`include/data/constructs/coordinator/TabletServerStatus.h:186`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0418 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &dataCacheHits(int64_t dataCacheHits) {` (`include/data/constructs/coordinator/TabletServerStatus.h:192`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0419 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &dataCacheRequest(int64_t dataCacheRequest) {` (`include/data/constructs/coordinator/TabletServerStatus.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0420 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &logSorts(std::vector<RecoveryStatus> logSorts) {` (`include/data/constructs/coordinator/TabletServerStatus.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0421 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &flushs(int64_t flushs) {` (`include/data/constructs/coordinator/TabletServerStatus.h:210`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0422 | `TabletServerStatusBuilder` method `TabletServerStatusBuilder &syncs(int64_t syncs) {` (`include/data/constructs/coordinator/TabletServerStatus.h:216`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0423 | `TabletServerStatusBuilder` operator `operator TabletServerStatus &&() {` (`include/data/constructs/coordinator/TabletServerStatus.h:222`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. This is a **conversion operator**: it makes the type implicitly convertible, so a port must decide between an explicit accessor and an implicit conversion, and the choice is user-visible. client-side data construct. |
| SB-CXX-0424 | `has_size` class (no public members) `class has_size {` (`include/data/constructs/inputvalidation.h:28`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. client-side data construct. |
| SB-CXX-0425 | `(anonymous)` enum `enum {` (`include/data/constructs/inputvalidation.h:36`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)) with a **template-dependent** value set: the enumerator initialiser is a constant expression over template parameters, so it has no single numeric ordinal in the pinned header and none is claimed here. Enumerators and initialiser text exactly as written: value = sizeof(test<T>(0)) == sizeof(char) (not evaluated). Its value is fixed only at instantiation, which is itself part of the contract a port must reproduce. Shoal exposes no equivalent type. |
| SB-CXX-0426 | `(namespace scope)` namespace-scope function `static auto IsEmpty( T *t) -> typename std::enable_if<has_size<T>::value ==1, bool>::type {` (`include/data/constructs/inputvalidation.h:42`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0427 | `(namespace scope)` namespace-scope function `static auto IsEmpty( T *t) -> typename std::enable_if<not has_size<T>::value , bool>::type {` (`include/data/constructs/inputvalidation.h:48`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0428 | `(namespace scope)` namespace-scope function `static auto IsEmpty(char *t)-> decltype(nullptr !=t, bool()) {` (`include/data/constructs/inputvalidation.h:54`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0429 | `(namespace scope)` namespace-scope function `bool IsEmpty() {` (`include/data/constructs/inputvalidation.h:61`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0430 | `(namespace scope)` namespace-scope function `static bool isValidPort(uint32_t port) {` (`include/data/constructs/inputvalidation.h:66`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0431 | `pointer_comparator` operator `bool operator()( T x, T y ) const {` (`include/data/constructs/inputvalidation.h:77`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0432 | `(namespace scope)` namespace-scope function `inline size_t array_length(T data[N]) {` (`include/data/constructs/inputvalidation.h:83`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0433 | `(namespace scope)` namespace-scope function `size_t array_length(const T (&x)[N]) {` (`include/data/constructs/inputvalidation.h:89`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0434 | `(namespace scope)` namespace-scope function `static std::vector<std::string> split(std::string str, char delim) {` (`include/data/constructs/inputvalidation.h:94`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-0435 | `Iterator` constructor `Iterator(const C & f) : val(static_cast<val_t>(f)) {}` (`include/data/constructs/inputvalidation.h:115`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0436 | `Iterator` constructor `Iterator() : val(static_cast<val_t>(beginVal)) {}` (`include/data/constructs/inputvalidation.h:116`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0437 | `Iterator` operator `Iterator operator++() {` (`include/data/constructs/inputvalidation.h:117`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0438 | `Iterator` operator `C operator*() { return static_cast<C>(val); }` (`include/data/constructs/inputvalidation.h:121`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0439 | `Iterator` method `Iterator begin() { return *this; }` (`include/data/constructs/inputvalidation.h:122`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0440 | `Iterator` method `Iterator end() {` (`include/data/constructs/inputvalidation.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0441 | `Iterator` operator `bool operator!=(const Iterator& i) { return val != i.val; }` (`include/data/constructs/inputvalidation.h:127`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0442 | `InstanceVersion` method `static bool isDefined(const std::string &instance) {` (`include/data/constructs/InstanceVersion.h:28`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0443 | `InstanceVersion` method `static int getVersion(const std::string &instance) {` (`include/data/constructs/InstanceVersion.h:33`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0444 | `InstanceVersion` method `static void setVersion(const std::string &instance, int version) {` (`include/data/constructs/InstanceVersion.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0445 | `ITERATOR_TYPES` enum `enum class ITERATOR_TYPES {` (`include/data/constructs/IterInfo.h:25`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: majc = 1; minc = 2; scan = 4. Shoal exposes no equivalent type. |
| SB-CXX-0446 | `(namespace scope)` alias `typedef Iterator<ITERATOR_TYPES, ITERATOR_TYPES::majc, ITERATOR_TYPES::scan> TypeIterator;` (`include/data/constructs/IterInfo.h:32`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. client-side data construct. |
| SB-CXX-0447 | `(namespace scope)` namespace-scope operator `inline ITERATOR_TYPES operator&#124;(ITERATOR_TYPES a, ITERATOR_TYPES b) {` (`include/data/constructs/IterInfo.h:34`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0448 | `(namespace scope)` namespace-scope operator `inline std::ostream& operator <<(std::ostream &out, const ITERATOR_TYPES &obj) {` (`include/data/constructs/IterInfo.h:38`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0449 | `IterInfo` constructor `IterInfo()` (`include/data/constructs/IterInfo.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0450 | `IterInfo` constructor `explicit IterInfo(const std::string &name, const std::string &cl, uint32_t pri) : iterName(name), iterClass(cl), priority(pri) {` (`include/data/constructs/IterInfo.h:71`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-055](#sec-8) already carries this obligation — `cclient::data::IterInfo::IterInfo` is the overload bound at `pysharkbite.cpp:86` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0451 | `IterInfo` constructor `explicit IterInfo(const std::string &name, const std::string &cl, uint32_t pri, const std::map<std::string, std::string> &opts) : IterInfo(name, cl, pri) {` (`include/data/constructs/IterInfo.h:85`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0452 | `IterInfo` constructor `explicit IterInfo(const std::string &name, const std::string &dsl, uint32_t pri, const std::string &type) : iterName("NativeDSLIterator"), iterClass("org.poma.accumulo.NativeDSLIterator"), priority(pri) {` (`include/data/constructs/IterInfo.h:91`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-056](#sec-8) already carries this obligation — `cclient::data::IterInfo::IterInfo` is the overload bound at `pysharkbite.cpp:87` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0453 | `IterInfo` destructor `virtual ~IterInfo() {` (`include/data/constructs/IterInfo.h:103`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::IterInfo` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0454 | `IterInfo` method `uint32_t getPriority() const {` (`include/data/constructs/IterInfo.h:111`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-057](#sec-8) already carries this obligation — `cclient::data::IterInfo::getPriority` is the overload bound at `pysharkbite.cpp:89` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0455 | `IterInfo` method `bool empty() const {` (`include/data/constructs/IterInfo.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0456 | `IterInfo` method `std::string getName() const {` (`include/data/constructs/IterInfo.h:126`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-058](#sec-8) already carries this obligation — `cclient::data::IterInfo::getName` is the overload bound at `pysharkbite.cpp:91` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0457 | `IterInfo` method `std::string getClass() const {` (`include/data/constructs/IterInfo.h:134`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-071](#sec-8) already carries this obligation — `cclient::data::IterInfo::getClass` is the overload bound at `pysharkbite.cpp:93` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0458 | `IterInfo` method `virtual bool addOption(std::string optionName, std::string optionValue) {` (`include/data/constructs/IterInfo.h:144`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0459 | `IterInfo` method `std::string getOption(const std::string &value, const std::string &def = "") const {` (`include/data/constructs/IterInfo.h:156`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0460 | `IterInfo` method `virtual const std::map<std::string, std::string> getOptions() const {` (`include/data/constructs/IterInfo.h:169`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0461 | `Key` constructor `explicit Key(cclient::data::ObjectAllocatorPool<Text> *pool);` (`include/data/constructs/Key.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0462 | `Key` constructor `Key(const char *const userRow = nullptr);` (`include/data/constructs/Key.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0463 | `Key` constructor `explicit Key(const std::string &row, const std::string &cf = "", const std::string &cq = "", const std::string &cv = "", int64_t ts = 9223372036854775807L) : Key() {` (`include/data/constructs/Key.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0464 | `Key` constructor `explicit Key(const char *rw, const char *cf = nullptr, const char *cq = nullptr, const char *cv = nullptr, int64_t ts = 9223372036854775807L) : Key() {` (`include/data/constructs/Key.h:54`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-006](#sec-8) already carries this obligation — `cclient::data::Key::Key` is the overload bound at `pysharkbite.cpp:299` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0465 | `Key` constructor `explicit Key(std::shared_ptr<Key> other) : Key() {` (`include/data/constructs/Key.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0466 | `Key` destructor `virtual ~Key();` (`include/data/constructs/Key.h:101`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::Key` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0467 | `Key` method `bool empty() {` (`include/data/constructs/Key.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0468 | `Key` method `void setRow(const char *r, uint32_t size, bool takeOwnership = false) {` (`include/data/constructs/Key.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0469 | `Key` method `void setRow(const char *r, uint32_t size, uint32_t maxSize, bool takeOwnership = false);` (`include/data/constructs/Key.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0470 | `Key` method `void setRow(const std::shared_ptr<Text>&);` (`include/data/constructs/Key.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0471 | `Key` method `void setColumnFamily(const std::shared_ptr<Text>&);` (`include/data/constructs/Key.h:114`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0472 | `Key` method `void setColumnQualifier(const std::shared_ptr<Text>&);` (`include/data/constructs/Key.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0473 | `Key` method `void setColumnVisibility(const std::shared_ptr<Text>&);` (`include/data/constructs/Key.h:116`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0474 | `Key` method `void setRow(const std::string &row) {` (`include/data/constructs/Key.h:118`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-007](#sec-8) already carries this obligation — `cclient::data::Key::setRow` is the overload bound at `pysharkbite.cpp:301` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0475 | `Key` method `std::pair<char*, size_t> getRow() const {` (`include/data/constructs/Key.h:122`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0476 | `Key` method `std::string getRowStr() const {` (`include/data/constructs/Key.h:129`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-010](#sec-8) already carries this obligation — `cclient::data::Key::getRowStr` is the overload bound at `pysharkbite.cpp:322` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0477 | `Key` method `void setColFamily(const char *r, uint32_t size, bool takeOwnership = false) {` (`include/data/constructs/Key.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0478 | `Key` method `void setColFamily(const char *r, uint32_t size, uint32_t maxSize, bool takeOwnership = false);` (`include/data/constructs/Key.h:139`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0479 | `Key` method `void setColFamily(const std::string &st) {` (`include/data/constructs/Key.h:141`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-008](#sec-8) already carries this obligation — `cclient::data::Key::setColFamily` is the overload bound at `pysharkbite.cpp:318` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0480 | `Key` method `inline std::pair<char*, size_t> getColFamily() const {` (`include/data/constructs/Key.h:145`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0481 | `Key` method `inline std::string getColFamilyStr() const {` (`include/data/constructs/Key.h:152`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-011](#sec-8) already carries this obligation — `cclient::data::Key::getColFamilyStr` is the overload bound at `pysharkbite.cpp:323` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0482 | `Key` method `void setColQualifier(const char *r, uint32_t size, bool takeOwnership = false) {` (`include/data/constructs/Key.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0483 | `Key` method `void setColQualifier(const char *r, uint32_t size, uint32_t maxSize, bool takeOwnership = false);` (`include/data/constructs/Key.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0484 | `Key` method `void setColQualifier(const std::string &st) {` (`include/data/constructs/Key.h:164`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-009](#sec-8) already carries this obligation — `cclient::data::Key::setColQualifier` is the overload bound at `pysharkbite.cpp:320` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0485 | `Key` method `std::pair<char*, size_t> getColQualifier() const {` (`include/data/constructs/Key.h:168`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0486 | `Key` method `std::string getColQualifierStr() const {` (`include/data/constructs/Key.h:174`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-012](#sec-8) already carries this obligation — `cclient::data::Key::getColQualifierStr` is the overload bound at `pysharkbite.cpp:326` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0487 | `Key` method `void setColVisibility(const char *r, uint32_t size, bool takeOwnership = false) {` (`include/data/constructs/Key.h:180`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0488 | `Key` method `void setColVisibility(const char *r, uint32_t size, uint32_t maxSize, bool takeOwnership = false);` (`include/data/constructs/Key.h:184`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0489 | `Key` method `void setColVisibility(const std::string &st) {` (`include/data/constructs/Key.h:186`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0490 | `Key` method `std::pair<char*, size_t> getColVisibility() const{` (`include/data/constructs/Key.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0491 | `Key` method `std::string getColVisibilityStr() const{` (`include/data/constructs/Key.h:196`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-013](#sec-8) already carries this obligation — `cclient::data::Key::getColVisibilityStr` is the overload bound at `pysharkbite.cpp:324` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0492 | `Key` method `int64_t getTimeStamp() {` (`include/data/constructs/Key.h:202`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-014](#sec-8) already carries this obligation — `cclient::data::Key::getTimeStamp` is the overload bound at `pysharkbite.cpp:325` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0493 | `Key` method `void setTimeStamp(const int64_t ts) {` (`include/data/constructs/Key.h:206`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0494 | `Key` method `bool isDeleted() {` (`include/data/constructs/Key.h:210`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0495 | `Key` method `void setDisownColumnQualifier() {` (`include/data/constructs/Key.h:214`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0496 | `Key` method `void setDisownColumnFamily() {` (`include/data/constructs/Key.h:218`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-XCUT-003](#sec-20) already carries this obligation — that row cites this exact declaration (`include/data/constructs/Key.h:218`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0497 | `Key` method `void setDisownColumnVisibility() {` (`include/data/constructs/Key.h:222`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-XCUT-003](#sec-20) already carries this obligation — that row cites this exact declaration (`include/data/constructs/Key.h:222`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0498 | `Key` method `void setDisownRow() {` (`include/data/constructs/Key.h:226`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-XCUT-003](#sec-20) already carries this obligation — that row cites this exact declaration (`include/data/constructs/Key.h:226`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0499 | `Key` method `void setDeleted(bool isDeleted) {` (`include/data/constructs/Key.h:230`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0500 | `Key` method `std::shared_ptr<StreamInterface> getStream() override {` (`include/data/constructs/Key.h:234`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0501 | `Key` operator `bool operator <=(const Key *rhs) const {` (`include/data/constructs/Key.h:238`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0502 | `Key` operator `bool operator <=(const Key &rhs) const {` (`include/data/constructs/Key.h:244`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0503 | `Key` method `int compare(const std::shared_ptr<Key> &other);` (`include/data/constructs/Key.h:250`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0504 | `Key` method `int compareToVisibility(const std::shared_ptr<Key> &other);` (`include/data/constructs/Key.h:252`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0505 | `Key` operator `bool operator <(const Key &rhs) const;` (`include/data/constructs/Key.h:255`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0506 | `Key` operator `bool operator <(const Key *rhs) const {` (`include/data/constructs/Key.h:257`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0507 | `Key` operator `bool operator ==(const Key &rhs) const;` (`include/data/constructs/Key.h:264`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0508 | `Key` operator `bool operator !=(const Key &rhs) const {` (`include/data/constructs/Key.h:265`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0509 | `Key` operator `bool operator ==(const Key *rhs) const {` (`include/data/constructs/Key.h:269`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0510 | `Key` operator `bool operator !=(const Key *rhs) const {` (`include/data/constructs/Key.h:274`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0511 | `Key` method `std::string toString() {` (`include/data/constructs/Key.h:280`) | `Key.String()` (`accumulo/key_string.go:18`) | `shoal_key_to_string` | `TestKeyStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact Go counterpart, added with [SB-DATA-015](#sec-8): same layout, same escaping, and the visibility left unescaped. A C++ consumer calling `toString` gets text Shoal can now produce, so the remaining gap is the ABI. |
| SB-CXX-0512 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, Key &rhs) {` (`include/data/constructs/Key.h:289`) | `Key.String()` (`accumulo/key_string.go:18`) | `shoal_key_to_string` | `TestKeyStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | The stream operator writes exactly what `toString` builds ([SB-CXX-0511](#sec-19-2)), so it maps to the same Go method. |
| SB-CXX-0513 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, Key *rhs) {` (`include/data/constructs/Key.h:298`) | `Key.String()` (`accumulo/key_string.go:18`) | `shoal_key_to_string` | `TestKeyStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Pointer overload that delegates to [SB-CXX-0512](#sec-19-2); same Go method. It is also the overload `std::shared_ptr`'s stream operator reaches by argument-dependent lookup, which is how a range renders its bound keys ([SB-CXX-0618](#sec-19-2)). |
| SB-CXX-0514 | `Key` method `uint64_t write(cclient::data::streams::OutputStream *outStream) override;` (`include/data/constructs/Key.h:303`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0515 | `Key` method `uint64_t read(cclient::data::streams::InputStream *in) override;` (`include/data/constructs/Key.h:306`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0516 | `Key` method `size_t size() const {` (`include/data/constructs/Key.h:308`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0517 | `Key` method `size_t length() const {` (`include/data/constructs/Key.h:312`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0518 | `Key` method `size_t getRowSize() const {` (`include/data/constructs/Key.h:316`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0519 | `Key` method `size_t getColumnFamilySize() const {` (`include/data/constructs/Key.h:320`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0520 | `Key` method `size_t getColQualifierSize() const {` (`include/data/constructs/Key.h:324`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0521 | `Key` method `size_t getColumnVisibilitySize() const {` (`include/data/constructs/Key.h:328`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0522 | `Key` method `void setObjectPool(ObjectAllocatorPool<Text> *p) {` (`include/data/constructs/Key.h:332`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0523 | `KeyExtent` constructor `explicit KeyExtent(const std::string &flattenedText, std::shared_ptr<Value> prevEndRow);` (`include/data/constructs/KeyExtent.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0524 | `KeyExtent` constructor `explicit KeyExtent(const std::string &tableIdIn, const std::string &endRowIn) : KeyExtent(tableIdIn, endRowIn, "") {` (`include/data/constructs/KeyExtent.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0525 | `KeyExtent` constructor `explicit KeyExtent(KeyExtent *copy) : tableId(copy->getTableId()), endRow(copy->getEndRow()), prevEndRow(copy->getPrevEndRow()) {` (`include/data/constructs/KeyExtent.h:58`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0526 | `KeyExtent` constructor `explicit KeyExtent(std::string tableIdIn, std::string endRowIn, std::string prevEndRowIn) {` (`include/data/constructs/KeyExtent.h:71`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0527 | `KeyExtent` method `static std::string createMetadataEntry(std::string table, std::string row) {` (`include/data/constructs/KeyExtent.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0528 | `KeyExtent` destructor `~KeyExtent();` (`include/data/constructs/KeyExtent.h:104`) | — | — | — | Missing Go | Assessed individually: `cclient::data::KeyExtent` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0529 | `KeyExtent` operator `KeyExtent& operator=(const KeyExtent &other) {` (`include/data/constructs/KeyExtent.h:107`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0530 | `KeyExtent` method `void setFileLocations(const std::vector<std::string> &otherLocs){` (`include/data/constructs/KeyExtent.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0531 | `KeyExtent` method `std::vector<std::string> getFileLocations() const {` (`include/data/constructs/KeyExtent.h:119`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0532 | `KeyExtent` operator `bool operator <(const KeyExtent &rhs) const {` (`include/data/constructs/KeyExtent.h:123`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0533 | `KeyExtent` operator `bool operator ==(const KeyExtent &rhs) const {` (`include/data/constructs/KeyExtent.h:146`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0534 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, KeyExtent *rhs) {` (`include/data/constructs/KeyExtent.h:166`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `KeyExtent` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0535 | `KeyExtent` method `void setTableId(const std::string &id) {` (`include/data/constructs/KeyExtent.h:171`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0536 | `KeyExtent` method `const std::string getTableId() const {` (`include/data/constructs/KeyExtent.h:175`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0537 | `KeyExtent` method `const std::string getEndRow() const {` (`include/data/constructs/KeyExtent.h:179`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0538 | `KeyExtent` method `std::string getPrevEndRow() const {` (`include/data/constructs/KeyExtent.h:183`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0539 | `KeyExtent` method `std::shared_ptr<cclient::data::Range> toRange() const {` (`include/data/constructs/KeyExtent.h:187`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0540 | `KeyExtent` method `void setPrevEndRow(const std::string &prev) {` (`include/data/constructs/KeyExtent.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0541 | `(namespace scope)` constant `static const KeyExtent ROOT_TABLET_EXTENT( METADATA_TABLE_ID, KeyExtent::createMetadataEntry(METADATA_TABLE_ID, ""));` (`include/data/constructs/KeyExtent.h:250`) | — | — | — | Missing Go | Public constant object or value. Declared `static` at namespace scope, so it has **internal linkage**: each translation unit that includes this header gets its own copy, and no consumer links against a single shared object. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0542 | `KeyValue` constructor `KeyValue();` (`include/data/constructs/KeyValue.h:41`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-016](#sec-8) already carries this obligation — `cclient::data::KeyValue::KeyValue` is the overload bound at `pysharkbite.cpp:329` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0543 | `KeyValue` destructor `virtual ~KeyValue();` (`include/data/constructs/KeyValue.h:43`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::KeyValue` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0544 | `KeyValue` constructor `explicit KeyValue(const std::shared_ptr<Key> &, const std::shared_ptr<Value> &);` (`include/data/constructs/KeyValue.h:45`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-017](#sec-8) already carries this obligation — `cclient::data::KeyValue::KeyValue` is the overload bound at `pysharkbite.cpp:330` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0545 | `KeyValue` method `void setKey(std::shared_ptr<Key> k, bool set_ownership = false);` (`include/data/constructs/KeyValue.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0546 | `KeyValue` method `std::shared_ptr<Key> getKey();` (`include/data/constructs/KeyValue.h:52`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-018](#sec-8) already carries this obligation — `cclient::data::KeyValue::getKey` is the overload bound at `pysharkbite.cpp:331` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0547 | `KeyValue` method `inline bool hasOwnerShip() {` (`include/data/constructs/KeyValue.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0548 | `KeyValue` method `std::shared_ptr<StreamInterface> getStream() override;` (`include/data/constructs/KeyValue.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0549 | `KeyValue` method `std::shared_ptr<Value> getValue();` (`include/data/constructs/KeyValue.h:61`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-019](#sec-8) already carries this obligation — `cclient::data::KeyValue::getValue` is the overload bound at `pysharkbite.cpp:332` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0550 | `KeyValue` method `void setValue(const std::shared_ptr<Value> &v);` (`include/data/constructs/KeyValue.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0551 | `(namespace scope)` friend operator `inline std::ostream & operator << (std::ostream &out, const KeyValue &rhs) {` (`include/data/constructs/KeyValue.h:65`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `KeyValue` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0552 | `(namespace scope)` friend operator `inline std::ostream & operator << (std::ostream &out, KeyValue *rhs) {` (`include/data/constructs/KeyValue.h:72`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `KeyValue` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0553 | `KeyValue` method `void setValue(uint8_t *b, size_t size);` (`include/data/constructs/KeyValue.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0554 | `KeyValue` method `uint64_t write (cclient::data::streams::OutputStream *outStream) override;` (`include/data/constructs/KeyValue.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0555 | `KeyValue` operator `KeyValue &operator=(const KeyValue &other);` (`include/data/constructs/KeyValue.h:79`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0556 | `KeyValue` operator `bool operator <(const KeyValue &rhs) const;` (`include/data/constructs/KeyValue.h:80`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0557 | `KeyValue` operator `bool operator <(const KeyValue *rhs) const;` (`include/data/constructs/KeyValue.h:81`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0558 | `KeyValue` operator `bool operator == (const KeyValue *rhs) const;` (`include/data/constructs/KeyValue.h:82`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0559 | `KeyValue` operator `bool operator == (const KeyValue & rhs) const;` (`include/data/constructs/KeyValue.h:83`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0560 | `KeyValue` operator `bool operator != (const KeyValue &rhs) const;` (`include/data/constructs/KeyValue.h:84`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0561 | `KeyValue` operator `bool operator != (const KeyValue *rhs) const;` (`include/data/constructs/KeyValue.h:85`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0562 | `movable_ptr` public data member `T *pointer;` (`include/data/constructs/MovablePointer.h:22`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0563 | `movable_ptr` constructor `movable_ptr(T *ptr=0) : pointer(ptr) {}` (`include/data/constructs/MovablePointer.h:23`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0564 | `movable_ptr` operator `movable_ptr<T>& operator=(T *ptr) { pointer = ptr; return *this; }` (`include/data/constructs/MovablePointer.h:24`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0565 | `movable_ptr` constructor `movable_ptr(movable_ptr<T> && other)` (`include/data/constructs/MovablePointer.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0566 | `movable_ptr` operator `movable_ptr<T>& operator=(movable_ptr<T> && other) {` (`include/data/constructs/MovablePointer.h:30`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0567 | `movable_ptr` operator `T* operator->() const { return pointer; }` (`include/data/constructs/MovablePointer.h:36`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0568 | `movable_ptr` operator `T& operator*() const { return *pointer; }` (`include/data/constructs/MovablePointer.h:37`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0569 | `movable_ptr` constructor `movable_ptr(movable_ptr<T> const & other) = default;` (`include/data/constructs/MovablePointer.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0570 | `movable_ptr` operator `movable_ptr<T> & operator=(movable_ptr<T> const & other) = default;` (`include/data/constructs/MovablePointer.h:40`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0571 | `(namespace scope)` macro constant `# define BSWAP_32(x) ((((x) & 0x000000ff) << 24) &#124; (((x) & 0x0000ff00) << 8) &#124; (((x) & 0x00ff0000) >> 8) &#124; (((x) & 0xff000000) >> 24))` (`include/data/constructs/Mutation.h:28`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0572 | `(namespace scope)` macro constant `#define BSWAP_64(x) (((uint64_t)(x) << 56) &#124; (((uint64_t)(x) << 40) & 0xff000000000000ULL) &#124; (((uint64_t)(x) << 24) & 0xff0000000000ULL) &#124; (((uint64_t)(x) << 8) & 0xff00000000ULL) &#124; (((uint64_t)(x) >> 8) & 0xff000000ULL) &#124; (((uint64_t)(x) >> 24) & 0xff0000ULL) &#124; (((uint64_t)(x) >> 40) & 0xff00ULL) &#124; ((uint64_t)(x) >> 56))` (`include/data/constructs/Mutation.h:32`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0573 | `Mutation` constructor `explicit Mutation(const std::string &row);` (`include/data/constructs/Mutation.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0574 | `Mutation` method `void putDelete(const std::string &cf, const std::string &cq, const std::string &cv, int64_t ts){` (`include/data/constructs/Mutation.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-041](#sec-8) already carries this obligation — `cclient::data::Mutation::putDelete` is the overload bound at `pysharkbite.cpp:371` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0575 | `Mutation` method `void putDelete(const std::string &cf, const std::string &cq, const std::string &cv){` (`include/data/constructs/Mutation.h:53`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-042](#sec-8) already carries this obligation — `cclient::data::Mutation::putDelete` is the overload bound at `pysharkbite.cpp:373` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0576 | `Mutation` method `void put(const std::string &cf, const std::string &cq, const std::string &cv, int64_t ts, bool deleted);` (`include/data/constructs/Mutation.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0577 | `Mutation` method `void put(const std::string &cf, const std::string &cq, const std::string &cv, int64_t ts, bool deleted, uint8_t *value, uint64_t value_len);` (`include/data/constructs/Mutation.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0578 | `Mutation` method `void put(const std::string &cf, const std::string &cq);` (`include/data/constructs/Mutation.h:64`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-039](#sec-8) already carries this obligation — `cclient::data::Mutation::put` is the overload bound at `pysharkbite.cpp:369` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0579 | `Mutation` method `void put(const std::string &cf, const std::string &cq, const std::string &cv);` (`include/data/constructs/Mutation.h:66`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-038](#sec-8) already carries this obligation — `cclient::data::Mutation::put` is the overload bound at `pysharkbite.cpp:367` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0580 | `Mutation` method `void put(const std::string &cf, const std::string &cq, const std::string &cv, int64_t ts);` (`include/data/constructs/Mutation.h:68`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-037](#sec-8) already carries this obligation — `cclient::data::Mutation::put` is the overload bound at `pysharkbite.cpp:365` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0581 | `Mutation` method `void put(const std::string &cf, const std::string &cq , const std::string &cv, int64_t ts, const std::string &value);` (`include/data/constructs/Mutation.h:70`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-036](#sec-8) already carries this obligation — `cclient::data::Mutation::put` is the overload bound at `pysharkbite.cpp:363` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0582 | `Mutation` destructor `virtual ~Mutation();` (`include/data/constructs/Mutation.h:71`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::Mutation` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0583 | `Mutation` method `std::string getRow() const {` (`include/data/constructs/Mutation.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0584 | `Mutation` method `int32_t size() {` (`include/data/constructs/Mutation.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0585 | `Mutation` method `std::pair<uint8_t*, size_t> getData() {` (`include/data/constructs/Mutation.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0586 | `Mutation` method `std::string getDataStr() const {` (`include/data/constructs/Mutation.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0587 | `ObjectAllocatorPool` method `virtual void disown(std::pair<char*,size_t> ) = 0;` (`include/data/constructs/ObjectPool.h:35`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0588 | `ObjectAllocatorPool` method `virtual std::pair<char*,size_t> allocateBuffer(size_t size) = 0;` (`include/data/constructs/ObjectPool.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0589 | `ObjectAllocatorPool` method `virtual std::shared_ptr<T> allocate(size_t size) = 0;` (`include/data/constructs/ObjectPool.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0590 | `ObjectAllocatorPool` method `virtual void free(std::pair<char*, size_t> &&ptr) = 0;` (`include/data/constructs/ObjectPool.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0591 | `KeyPredicate` constructor `KeyPredicate(){` (`include/data/constructs/predicates/key/KeyPredicates.h:26`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0592 | `KeyPredicate` destructor `virtual ~KeyPredicate(){}` (`include/data/constructs/predicates/key/KeyPredicates.h:28`) | — | — | — | Missing Go | Assessed individually: `cclient::data::KeyPredicate` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0593 | `KeyPredicate` method `virtual bool acceptRow(const std::shared_ptr<Text> &row){` (`include/data/constructs/predicates/key/KeyPredicates.h:30`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0594 | `KeyPredicate` method `virtual bool accept(const std::shared_ptr<Key> &key) = 0;` (`include/data/constructs/predicates/key/KeyPredicates.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0595 | `PythonIterInfo` constructor `explicit PythonIterInfo(const std::string &name, uint32_t pri);` (`include/data/constructs/PythonIterInfo.h:37`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-062](#sec-8) already carries this obligation — `cclient::data::python::PythonIterInfo::PythonIterInfo` is the overload bound at `pysharkbite.cpp:98` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0596 | `PythonIterInfo` constructor `explicit PythonIterInfo(const std::string &name, const std::string &idsl, uint32_t pri);` (`include/data/constructs/PythonIterInfo.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-061](#sec-8) already carries this obligation — `cclient::data::python::PythonIterInfo::PythonIterInfo` is the overload bound at `pysharkbite.cpp:97` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0597 | `PythonIterInfo` destructor `virtual ~PythonIterInfo();` (`include/data/constructs/PythonIterInfo.h:44`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::python::PythonIterInfo` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0598 | `PythonIterInfo` method `PythonIterInfo& onNext(const std::string &lambda);` (`include/data/constructs/PythonIterInfo.h:46`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-063](#sec-8) already carries this obligation — `cclient::data::python::PythonIterInfo::onNext` is the overload bound at `pysharkbite.cpp:103` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0599 | `PythonIterInfo` method `const std::map<std::string, std::string> getOptions() const override;` (`include/data/constructs/PythonIterInfo.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0600 | `Range` constructor `Range();` (`include/data/constructs/Range.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-020](#sec-8) already carries this obligation — `cclient::data::Range::Range` is the overload bound at `pysharkbite.cpp:335` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0601 | `Range` constructor `explicit Range(const Range &range) = delete;` (`include/data/constructs/Range.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0602 | `Range` constructor `explicit Range(const std::string &row);` (`include/data/constructs/Range.h:44`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-021](#sec-8) already carries this obligation — `cclient::data::Range::Range` is the overload bound at `pysharkbite.cpp:336` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0603 | `Range` constructor `explicit Range(const std::string &startRow, bool startInclusive, const std::string &endRow, bool endKeyInclusive, bool update = true);` (`include/data/constructs/Range.h:53`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-024](#sec-8) already carries this obligation — `cclient::data::Range::Range` is the overload bound at `pysharkbite.cpp:340` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0604 | `Range` constructor `explicit Range(const std::shared_ptr<Key> &startKey, bool startInclusive, const std::shared_ptr<Key> &endKey, bool endKeyInclusive, bool update = true);` (`include/data/constructs/Range.h:62`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0605 | `Range` constructor `explicit Range(const std::shared_ptr<Key> &endKey, bool endKeyInclusive) : Range(0, false, endKey, endKeyInclusive) {` (`include/data/constructs/Range.h:71`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0606 | `Range` constructor `explicit Range(bool startInclusive, const std::shared_ptr<Key> &startKey) : Range(startKey, startInclusive, 0, false) {` (`include/data/constructs/Range.h:83`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0607 | `Range` constructor `Range(Range &&) = default;` (`include/data/constructs/Range.h:88`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0608 | `Range` method `std::shared_ptr<Key> getStartKey() const{` (`include/data/constructs/Range.h:93`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-025](#sec-8) already carries this obligation — `cclient::data::Range::getStartKey` is the overload bound at `pysharkbite.cpp:342` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0609 | `Range` method `std::shared_ptr<Key> getStopKey() const{` (`include/data/constructs/Range.h:100`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-026](#sec-8) already carries this obligation — `cclient::data::Range::getStopKey` is the overload bound at `pysharkbite.cpp:343` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0610 | `Range` method `bool getStartKeyInclusive() const{` (`include/data/constructs/Range.h:107`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-027](#sec-8) already carries this obligation — `cclient::data::Range::getStartKeyInclusive` is the overload bound at `pysharkbite.cpp:344` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0611 | `Range` method `bool getStopKeyInclusive() const{` (`include/data/constructs/Range.h:114`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-028](#sec-8) already carries this obligation — `cclient::data::Range::getStopKeyInclusive` is the overload bound at `pysharkbite.cpp:345` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0612 | `Range` method `bool getInfiniteStartKey() const{` (`include/data/constructs/Range.h:121`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-029](#sec-8) already carries this obligation — `cclient::data::Range::getInfiniteStartKey` is the overload bound at `pysharkbite.cpp:346` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0613 | `Range` method `bool getInfiniteStopKey() const{` (`include/data/constructs/Range.h:128`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-030](#sec-8) already carries this obligation — `cclient::data::Range::getInfiniteStopKey` is the overload bound at `pysharkbite.cpp:347` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0614 | `Range` method `bool afterEndKey(const std::shared_ptr<Key> &key) const;` (`include/data/constructs/Range.h:132`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-031](#sec-8) already carries this obligation — `cclient::data::Range::afterEndKey` is the overload bound at `pysharkbite.cpp:348` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0615 | `Range` method `bool beforeStartKey(const std::shared_ptr<Key> &key) const;` (`include/data/constructs/Range.h:134`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-032](#sec-8) already carries this obligation — `cclient::data::Range::beforeStartKey` is the overload bound at `pysharkbite.cpp:349` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0616 | `Range` method `std::shared_ptr<Range> intersect(const std::shared_ptr<Range> &range) const;` (`include/data/constructs/Range.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0617 | `Range` destructor `virtual ~Range();` (`include/data/constructs/Range.h:139`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::Range` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0618 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, const Range &rhs) {` (`include/data/constructs/Range.h:142`) | `Range.String()` (`accumulo/range.go:241`) | `shoal_range_to_string` | `TestRangeStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact Go counterpart, added with [SB-DATA-033](#sec-8). The bound keys reach [SB-CXX-0513](#sec-19-2) through `std::operator<<(std::ostream&, const std::shared_ptr<T>&)`, which the standard defines as `os << p.get()`; argument-dependent lookup on `Key*` then selects Sharkbite's own overload, so a bound renders as `Key::toString` text rather than as an address. |
| SB-CXX-0619 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, Range *rhs) {` (`include/data/constructs/Range.h:160`) | `Range.String()` (`accumulo/range.go:241`) | `shoal_range_to_string` | `TestRangeStringMatchesSharkbiteFormat` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Pointer overload that delegates to [SB-CXX-0618](#sec-19-2); same Go method. |
| SB-CXX-0620 | `Range` operator `bool operator==(const Range &other) {` (`include/data/constructs/Range.h:166`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0621 | `KeyAllocatorPool` method `void intern(std::pair<char*,size_t> pair);` (`include/data/constructs/rfile/AllocatorPool.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0622 | `KeyAllocatorPool` method `bool decrement(std::pair<char*,size_t> pair);` (`include/data/constructs/rfile/AllocatorPool.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0623 | `KeyAllocatorPool` method `std::pair<char*,size_t> allocate(size_t size);` (`include/data/constructs/rfile/AllocatorPool.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0624 | `KeyAllocatorPool` method `void free(std::pair<char*,size_t> &&ptr);` (`include/data/constructs/rfile/AllocatorPool.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0625 | `KeyAllocatorPool` method `static KeyAllocatorPool* getInstance() {` (`include/data/constructs/rfile/AllocatorPool.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0626 | `BlockStreambuffer` constructor `explicit BlockStreambuffer(size_t s) : bfptr(new char[s]), max_size(s) {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0627 | `BlockStreambuffer` destructor `virtual ~BlockStreambuffer() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:36`) | — | — | — | Missing Go | Assessed individually: `cclient::data::BlockStreambuffer` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0628 | `BlockStreambuffer` method `char *getPtr() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0629 | `BlockStreambuffer` method `virtual std::streamsize xsputn(const char * s, std::streamsize n) = 0;` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0630 | `BlockCompressorStream` constructor `BlockCompressorStream(OutputStream *out_stream, std::unique_ptr<cclient::data::compression::Compressor> compressor, BlockRegion *region);` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:61`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0631 | `BlockCompressorStream` constructor `BlockCompressorStream(InputStream *in_stream,std::unique_ptr<cclient::data::compression::Compressor> decompressor,BlockRegion *region);` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:64`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0632 | `BlockCompressorStream` destructor `~BlockCompressorStream();` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:66`) | — | — | — | Missing Go | Assessed individually: `cclient::data::BlockCompressorStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0633 | `BlockCompressorStream` method `inline virtual int sync() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0634 | `BlockCompressorStream` method `uint32_t bytesWritten() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0635 | `BlockCompressorStream` method `uint64_t getPos() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0636 | `BlockCompressorStream` method `virtual uint64_t writeEncodedLong(const int64_t n = 0) {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0637 | `BlockCompressorStream` method `void flush() {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0638 | `BlockCompressorStream` method `inline int overflow( int ch ) {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:136`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0639 | `BlockCompressorStream` method `inline std::streamsize xsputn(const char * s, std::streamsize n) {` (`include/data/constructs/rfile/bcfile/block_comp_stream.h:148`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0640 | `(namespace scope)` constant `static const uint8_t B_MAGIC_BCFILE[16] = { 0xd1, 0x11, 0xd3, 0x68, 0x91, 0xb5, 0xd7, 0xb6, 0x39, 0xdf, 0x41, 0x40, 0x92, 0xba, 0xe1, 0x50 };` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:27`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `{ 0xd1, 0x11, 0xd3, 0x68, 0x91, 0xb5, 0xd7, 0xb6, 0x39, 0xdf, 0x41, 0x40, 0x92, 0xba, 0xe1, 0x50 }`. Declared `static` at namespace scope, so it has **internal linkage**: each translation unit that includes this header gets its own copy, and no consumer links against a single shared object. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0641 | `BlockCompressedFile` constructor `explicit BlockCompressedFile(std::unique_ptr<cclient::data::compression::Compressor> compressor) : compressorRef(std::move(compressor)), in_stream(nullptr) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0642 | `BlockCompressedFile` constructor `BlockCompressedFile(cclient::data::streams::InputStream *in_stream, long fileLength)` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0643 | `BlockCompressedFile` method `void setDataIndex(DataIndex dataIndex) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0644 | `BlockCompressedFile` method `std::unique_ptr<cclient::data::compression::Compressor> cloneCompressor() {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0645 | `BlockCompressedFile` method `DataIndex* getDataIndex() {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0646 | `BlockCompressedFile` method `MetaIndex* getMetaIndex() {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0647 | `BlockCompressedFile` method `cclient::data::streams::DataOutputStream* createCompressorStream(cclient::data::streams::OutputStream *out, MetaIndexEntry *entry) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0648 | `BlockCompressedFile` method `cclient::data::streams::DataOutputStream* createDataStream(cclient::data::streams::OutputStream *out) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0649 | `BlockCompressedFile` method `MetaIndexEntry* prepareNewEntry(std::string name) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0650 | `BlockCompressedFile` method `uint64_t write(cclient::data::streams::OutputStream *out) {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:106`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0651 | `BlockCompressedFile` method `void close() {` (`include/data/constructs/rfile/bcfile/BlockCompressedFile.h:136`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0652 | `BlockRegion` constructor `BlockRegion()` (`include/data/constructs/rfile/bcfile/BlockRegion.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0653 | `BlockRegion` constructor `explicit BlockRegion(uint64_t offset, uint64_t compressedSize, uint64_t rawSize, std::unique_ptr<cclient::data::compression::Compressor> compressor) : offset(offset), compressedSize(compressedSize), rawSize(rawSize), compressor(std::move(compressor)) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0654 | `BlockRegion` constructor `BlockRegion(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0655 | `BlockRegion` constructor `BlockRegion(const BlockRegion &copy) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0656 | `BlockRegion` destructor `virtual ~BlockRegion() {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:60`) | — | — | — | Missing Go | Assessed individually: `cclient::data::BlockRegion` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0657 | `BlockRegion` method `void setOffset(uint64_t off) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0658 | `BlockRegion` method `void setCompressedSize(uint64_t csize) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0659 | `BlockRegion` method `void setRawSize(uint64_t rsize) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0660 | `BlockRegion` method `void setCompressor(std::unique_ptr<cclient::data::compression::Compressor> comp) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0661 | `BlockRegion` method `cclient::data::compression::CompressionAlgorithm getAlgorithm(){` (`include/data/constructs/rfile/bcfile/BlockRegion.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0662 | `BlockRegion` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/bcfile/BlockRegion.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0663 | `BlockRegion` method `uint64_t write(cclient::data::streams::OutputStream *out);` (`include/data/constructs/rfile/bcfile/BlockRegion.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0664 | `BlockRegion` method `std::unique_ptr<cclient::data::streams::InputStream> readDataStream(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:114`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0665 | `BlockRegion` method `std::unique_ptr<cclient::data::streams::InputStream> assimilateDataStream(cclient::data::streams::InputStream *in, std::vector<uint8_t> *compressed, cclient::data::streams::ByteOutputStream *ref, moodycamel::ConcurrentQueue<cclient::data::streams::ByteOutputStream*> *queueref) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0666 | `BlockRegion` operator `BlockRegion& operator=(const BlockRegion &other) {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:163`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0667 | `BlockRegion` method `uint64_t getOffset() {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:172`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0668 | `BlockRegion` method `uint64_t getCompressedSize() {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:176`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0669 | `BlockRegion` method `uint64_t getRawSize() {` (`include/data/constructs/rfile/bcfile/BlockRegion.h:180`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0670 | `DataIndex` constructor `explicit DataIndex(cclient::data::compression::Compressor *compressor);` (`include/data/constructs/rfile/bcfile/data_index.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0671 | `DataIndex` constructor `DataIndex();` (`include/data/constructs/rfile/bcfile/data_index.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0672 | `DataIndex` destructor `~DataIndex();` (`include/data/constructs/rfile/bcfile/data_index.h:33`) | — | — | — | Missing Go | Assessed individually: `cclient::data::DataIndex` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0673 | `DataIndex` method `void setCompressionAlgorithm(cclient::data::compression::Compressor *compressor) {` (`include/data/constructs/rfile/bcfile/data_index.h:35`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0674 | `DataIndex` method `BlockRegion* addBlockRegion(BlockRegion region) {` (`include/data/constructs/rfile/bcfile/data_index.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0675 | `DataIndex` method `BlockRegion* addBlockRegion() {` (`include/data/constructs/rfile/bcfile/data_index.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0676 | `DataIndex` method `BlockRegion* getBlockRegion(int index) {` (`include/data/constructs/rfile/bcfile/data_index.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0677 | `DataIndex` method `cclient::data::compression::CompressionAlgorithm getCompressionAlgorithm() {` (`include/data/constructs/rfile/bcfile/data_index.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0678 | `DataIndex` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/bcfile/data_index.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0679 | `DataIndex` method `uint64_t write(cclient::data::streams::OutputStream *out);` (`include/data/constructs/rfile/bcfile/data_index.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0680 | `DataIndex` operator `DataIndex& operator=(const DataIndex &other) {` (`include/data/constructs/rfile/bcfile/data_index.h:67`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0681 | `MetaIndexEntry` constructor `MetaIndexEntry()` (`include/data/constructs/rfile/bcfile/meta_index.h:40`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0682 | `MetaIndexEntry` destructor `virtual ~MetaIndexEntry() {` (`include/data/constructs/rfile/bcfile/meta_index.h:46`) | — | — | — | Missing Go | Assessed individually: `cclient::data::MetaIndexEntry` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0683 | `MetaIndexEntry` constructor `explicit MetaIndexEntry(std::unique_ptr<cclient::data::compression::Compressor> compressor) : metaName(""), comp(std::move(compressor)) {` (`include/data/constructs/rfile/bcfile/meta_index.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0684 | `MetaIndexEntry` constructor `explicit MetaIndexEntry(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/bcfile/meta_index.h:57`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0685 | `MetaIndexEntry` constructor `MetaIndexEntry(std::string metameta, BlockRegion reg)` (`include/data/constructs/rfile/bcfile/meta_index.h:61`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0686 | `MetaIndexEntry` method `cclient::data::streams::InputStream* readDataStream(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/bcfile/meta_index.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0687 | `MetaIndexEntry` method `uint64_t read(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/bcfile/meta_index.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0688 | `MetaIndexEntry` method `uint64_t write(cclient::data::streams::OutputStream *out) {` (`include/data/constructs/rfile/bcfile/meta_index.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0689 | `MetaIndexEntry` method `std::string getMetaName() {` (`include/data/constructs/rfile/bcfile/meta_index.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0690 | `MetaIndexEntry` operator `MetaIndexEntry& operator=(const MetaIndexEntry &other) {` (`include/data/constructs/rfile/bcfile/meta_index.h:111`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0691 | `MetaIndexEntry` method `BlockRegion* getRegion() {` (`include/data/constructs/rfile/bcfile/meta_index.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0692 | `MetaIndexEntry` method `void setName(std::string name) {` (`include/data/constructs/rfile/bcfile/meta_index.h:129`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0693 | `MetaIndexEntry` method `void setAlgorithm(const cclient::data::compression::Algorithm &algo) {` (`include/data/constructs/rfile/bcfile/meta_index.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0694 | `MetaIndexEntry` method `cclient::data::compression::CompressionAlgorithm getAlgorithm() {` (`include/data/constructs/rfile/bcfile/meta_index.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0695 | `MetaIndexEntry` method `void setBlockRegion(BlockRegion reg) {` (`include/data/constructs/rfile/bcfile/meta_index.h:142`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0696 | `MetaIndex` constructor `MetaIndex();` (`include/data/constructs/rfile/bcfile/meta_index.h:162`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0697 | `MetaIndex` destructor `virtual ~MetaIndex();` (`include/data/constructs/rfile/bcfile/meta_index.h:164`) | — | — | — | Missing Go | Assessed individually: `cclient::data::MetaIndex` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0698 | `MetaIndex` method `void addEntry(const std::shared_ptr<MetaIndexEntry> &indexEntry, cclient::data::streams::DataOutputStream *out);` (`include/data/constructs/rfile/bcfile/meta_index.h:172`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0699 | `MetaIndex` method `MetaIndexEntry* getEntry(const std::string &name);` (`include/data/constructs/rfile/bcfile/meta_index.h:181`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0700 | `MetaIndex` method `MetaIndexEntry* prepareNewEntry(const std::string &name, std::unique_ptr<cclient::data::compression::Compressor> comp);` (`include/data/constructs/rfile/bcfile/meta_index.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0701 | `MetaIndex` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/bcfile/meta_index.h:193`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0702 | `MetaIndex` method `uint64_t write(cclient::data::streams::OutputStream *out);` (`include/data/constructs/rfile/bcfile/meta_index.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0703 | `MetaIndex` method `std::map<std::string, std::shared_ptr<MetaIndexEntry>>* getEntries();` (`include/data/constructs/rfile/bcfile/meta_index.h:200`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0704 | `BaseMetaBlock` destructor `virtual ~BaseMetaBlock() {` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:27`) | — | — | — | Missing Go | Assessed individually: `cclient::data::BaseMetaBlock` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0705 | `BaseMetaBlock` method `virtual uint32_t getCurrentPosition() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:31`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0706 | `BaseMetaBlock` method `virtual std::shared_ptr<data::streams::StreamInterface> getBlock() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0707 | `BaseMetaBlock` method `virtual std::shared_ptr<BaseMetaBlock> getPreviousBlock() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0708 | `BaseMetaBlock` method `virtual std::shared_ptr<BaseMetaBlock> getNextBlock() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0709 | `BaseMetaBlock` method `virtual std::shared_ptr<BaseMetaBlock> getNext() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0710 | `BaseMetaBlock` method `virtual std::shared_ptr<IndexMetaBlock> getIndexBlock() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0711 | `BaseMetaBlock` method `virtual uint32_t getOffset() = 0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0712 | `BaseMetaBlock` method `virtual bool hasNextKey() =0;` (`include/data/constructs/rfile/meta/BaseMetaBlock.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0713 | `Block` constructor `Block(BlockLookup *blockStore, std::shared_ptr<Block> parent, std::shared_ptr<IndexBlock> block,const std::shared_ptr<cclient::data::Key> &keyref=nullptr)` (`include/data/constructs/rfile/meta/Block.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0714 | `Block` constructor `Block(BlockLookup *blockStore, std::shared_ptr<IndexBlock> block, const std::shared_ptr<cclient::data::Key> &keyref=nullptr)` (`include/data/constructs/rfile/meta/Block.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0715 | `Block` constructor `Block(std::shared_ptr<Block> blockStore, std::shared_ptr<IndexBlock> block, const std::shared_ptr<cclient::data::Key> &keyref=nullptr)` (`include/data/constructs/rfile/meta/Block.h:54`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0716 | `Block` destructor `~Block() {` (`include/data/constructs/rfile/meta/Block.h:60`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Block` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0717 | `Block` method `uint32_t getCurrentPosition() {` (`include/data/constructs/rfile/meta/Block.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0718 | `Block` method `std::shared_ptr<IndexMetaBlock> getIndexBlock() {` (`include/data/constructs/rfile/meta/Block.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0719 | `Block` method `std::shared_ptr<data::streams::StreamInterface> getBlock() {` (`include/data/constructs/rfile/meta/Block.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0720 | `Block` method `uint32_t getOffset() {` (`include/data/constructs/rfile/meta/Block.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0721 | `Block` method `bool hasNextKey() {` (`include/data/constructs/rfile/meta/Block.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0722 | `Block` method `std::shared_ptr<IndexBlock> getIndexBlock(const std::shared_ptr<IndexEntry> &ie) {` (`include/data/constructs/rfile/meta/Block.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0723 | `Block` method `std::shared_ptr<Block> lookup(const std::shared_ptr<Key> &key);` (`include/data/constructs/rfile/meta/Block.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0724 | `Block` method `virtual std::shared_ptr<BaseMetaBlock> getNextBlock() {` (`include/data/constructs/rfile/meta/Block.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0725 | `Block` method `virtual std::shared_ptr<BaseMetaBlock> getNext() {` (`include/data/constructs/rfile/meta/Block.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0726 | `Block` method `std::shared_ptr<Block> getFirst() {` (`include/data/constructs/rfile/meta/Block.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0727 | `Block` method `std::shared_ptr<Block> getLast() {` (`include/data/constructs/rfile/meta/Block.h:132`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0728 | `Block` method `std::shared_ptr<BaseMetaBlock> getPrevious() {` (`include/data/constructs/rfile/meta/Block.h:151`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0729 | `Block` method `std::shared_ptr<BaseMetaBlock> getPreviousBlock() {` (`include/data/constructs/rfile/meta/Block.h:169`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0730 | `BlockLookup` constructor `BlockLookup() {` (`include/data/constructs/rfile/meta/BlockLookup.h:23`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0731 | `BlockLookup` destructor `virtual ~BlockLookup() {` (`include/data/constructs/rfile/meta/BlockLookup.h:26`) | — | — | — | Missing Go | Assessed individually: `cclient::data::BlockLookup` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0732 | `BlockLookup` method `virtual std::shared_ptr<IndexBlock> getIndexBlock(const std::shared_ptr<IndexEntry> &ie) = 0;` (`include/data/constructs/rfile/meta/BlockLookup.h:30`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0733 | `IndexBlock` constructor `explicit IndexBlock(int version);` (`include/data/constructs/rfile/meta/IndexBlock.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0734 | `IndexBlock` destructor `virtual ~IndexBlock();` (`include/data/constructs/rfile/meta/IndexBlock.h:45`) | — | — | — | Missing Go | Assessed individually: `cclient::data::IndexBlock` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0735 | `IndexBlock` method `std::shared_ptr<SerializedIndex> getIndex();` (`include/data/constructs/rfile/meta/IndexBlock.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0736 | `IndexBlock` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/meta/IndexBlock.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0737 | `IndexBlock` method `std::shared_ptr<KeyIndex> getKeyIndex();` (`include/data/constructs/rfile/meta/IndexBlock.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0738 | `IndexBlock` method `int getLevel() ;` (`include/data/constructs/rfile/meta/IndexBlock.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0739 | `IndexBlock` method `int getOffset();` (`include/data/constructs/rfile/meta/IndexBlock.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0740 | `IndexBlock` method `bool hasNextKey();` (`include/data/constructs/rfile/meta/IndexBlock.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0741 | `IndexEntry` constructor `explicit IndexEntry(std::shared_ptr<cclient::data::Key> mKey, uint32_t entryCount);` (`include/data/constructs/rfile/meta/IndexEntry.h:28`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0742 | `IndexEntry` destructor `virtual ~IndexEntry();` (`include/data/constructs/rfile/meta/IndexEntry.h:31`) | — | — | — | Missing Go | Assessed individually: `cclient::data::IndexEntry` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0743 | `IndexEntry` constructor `IndexEntry(IndexEntry &&other) = default;` (`include/data/constructs/rfile/meta/IndexEntry.h:36`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0744 | `IndexEntry` constructor `IndexEntry()` (`include/data/constructs/rfile/meta/IndexEntry.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0745 | `IndexEntry` constructor `explicit IndexEntry(bool newFormat) : newFormat(newFormat), key(NULL) {` (`include/data/constructs/rfile/meta/IndexEntry.h:45`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0746 | `IndexEntry` method `std::shared_ptr<Key> getKey() {` (`include/data/constructs/rfile/meta/IndexEntry.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0747 | `IndexEntry` method `uint32_t getNumEntries() {` (`include/data/constructs/rfile/meta/IndexEntry.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0748 | `IndexEntry` method `uint64_t read(cclient::data::streams::InputStream *in) {` (`include/data/constructs/rfile/meta/IndexEntry.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0749 | `IndexEntry` method `uint64_t write(cclient::data::streams::DataOutputStream *outStream) {` (`include/data/constructs/rfile/meta/IndexEntry.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0750 | `IndexEntry` operator `IndexEntry& operator=(const IndexEntry &other) {` (`include/data/constructs/rfile/meta/IndexEntry.h:100`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0751 | `IndexEntry` operator `IndexEntry& operator=(IndexEntry &&other) {` (`include/data/constructs/rfile/meta/IndexEntry.h:109`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0752 | `IndexEntry` operator `bool operator ==(const IndexEntry &rhs) const {` (`include/data/constructs/rfile/meta/IndexEntry.h:117`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0753 | `IndexEntry` method `uint64_t getOffset() {` (`include/data/constructs/rfile/meta/IndexEntry.h:127`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0754 | `IndexEntry` method `uint64_t getCompressedSize() {` (`include/data/constructs/rfile/meta/IndexEntry.h:131`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0755 | `IndexEntry` method `uint64_t getRawSize() {` (`include/data/constructs/rfile/meta/IndexEntry.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0756 | `IndexManager` constructor `explicit IndexManager(std::unique_ptr<cclient::data::compression::Compressor> compressorRef, cclient::data::streams::InputStream *blockReader, int version);` (`include/data/constructs/rfile/meta/IndexManager.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0757 | `IndexManager` destructor `virtual ~IndexManager() {` (`include/data/constructs/rfile/meta/IndexManager.h:37`) | — | — | — | Missing Go | Assessed individually: `cclient::data::IndexManager` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0758 | `IndexManager` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/meta/IndexManager.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0759 | `IndexManager` method `std::shared_ptr<SerializedIndex> lookup(const std::shared_ptr<Key> &key) {` (`include/data/constructs/rfile/meta/IndexManager.h:42`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0760 | `IndexManager` method `int getSize() {` (`include/data/constructs/rfile/meta/IndexManager.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0761 | `IndexManager` method `std::shared_ptr<IndexBlock> getIndexBlock(const std::shared_ptr<IndexEntry> &ie) {` (`include/data/constructs/rfile/meta/IndexManager.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0762 | `IndexMetaBlock` destructor `virtual ~IndexMetaBlock() {` (`include/data/constructs/rfile/meta/IndexMetaBlock.h:26`) | — | — | — | Missing Go | Assessed individually: `cclient::data::IndexMetaBlock` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0763 | `IndexMetaBlock` method `virtual bool hasNextKey() =0;` (`include/data/constructs/rfile/meta/IndexMetaBlock.h:29`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0764 | `KeyIndex` constructor `KeyIndex(std::vector<int> offsetList, uint8_t *datums, int dataLength)` (`include/data/constructs/rfile/meta/KeyIndex.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0765 | `KeyIndex` destructor `virtual ~KeyIndex() {` (`include/data/constructs/rfile/meta/KeyIndex.h:38`) | — | — | — | Missing Go | Assessed individually: `cclient::data::KeyIndex` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0766 | `KeyIndex` method `std::shared_ptr<Key> get(uint64_t index);` (`include/data/constructs/rfile/meta/KeyIndex.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0767 | `KeyIndex` method `int binary_search(const std::shared_ptr<Key> &search_key);` (`include/data/constructs/rfile/meta/KeyIndex.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0768 | `KeyIndex` method `int binary_search(int first, int last, const std::shared_ptr<Key> &search_key);` (`include/data/constructs/rfile/meta/KeyIndex.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0769 | `LocalityGroup` public data member `bool defaultGroup = {false};` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:30`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0770 | `LocalityGroup` public data member `std::map<std::string,long> columnFamilies;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:31`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0771 | `LocalityGroup` method `virtual std::shared_ptr<cclient::data::streams::KeyValueIterator> getIterator() = 0;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:33`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0772 | `LocalityGroup` method `auto isDefaultLocalityGroup() const{` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0773 | `LocalityGroupSeekCache` constructor `LocalityGroupSeekCache(){}` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0774 | `LocalityGroupSeekCache` method `auto getLastColumnFamilies() const {` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0775 | `LocalityGroupSeekCache` method `auto isLastInclusive() const{` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0776 | `LocalityGroupSeekCache` method `auto getLastUsed() const{` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0777 | `LocalityGroupSeekCache` method `auto getNumLGSeeked() const {` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0778 | `LocalityGroupSeekCache` public data member `std::set<std::string> lastColumnFamilies;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:65`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0779 | `LocalityGroupSeekCache` public data member `bool lastInclusive;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:66`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0780 | `LocalityGroupSeekCache` public data member `std::vector<std::shared_ptr<LocalityGroup>> lastUsed;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:67`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0781 | `LocalityGroupContext` public data member `std::vector<std::shared_ptr<LocalityGroup>> groups;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:75`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0782 | `LocalityGroupContext` public data member `std::shared_ptr<LocalityGroup> defaultGroup;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:76`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0783 | `LocalityGroupContext` public data member `std::map<std::string,std::shared_ptr<LocalityGroup>> groupByCf;` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:77`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0784 | `LocalityGroupContext` constructor `explicit LocalityGroupContext(std::vector<std::shared_ptr<LocalityGroup>> groups) : groups(groups){` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:80`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0785 | `LocalityGroupIterator` method `static std::shared_ptr<LocalityGroupSeekCache > relocate(const std::shared_ptr<HeapIterator> &hiter, LocalityGroupContext *lgContext, const std::shared_ptr<cclient::data::streams::StreamRelocation> &location, const std::shared_ptr<LocalityGroupSeekCache > &lgSeekCache );` (`include/data/constructs/rfile/meta/LocalityGroupIterator.h:106`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0786 | `LocalityGroupMetaData` constructor `explicit LocalityGroupMetaData(uint32_t startBlockVal, std::string name = "");` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0787 | `LocalityGroupMetaData` constructor `explicit LocalityGroupMetaData(std::unique_ptr<cclient::data::compression::Compressor> compressorRef, int version, cclient::data::streams::InputStream *reader);` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0788 | `LocalityGroupMetaData` destructor `~LocalityGroupMetaData();` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:50`) | — | — | — | Missing Go | Assessed individually: `cclient::data::LocalityGroupMetaData` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0789 | `LocalityGroupMetaData` method `void setFirstKey(std::shared_ptr<StreamInterface> key) {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0790 | `LocalityGroupMetaData` method `std::shared_ptr<StreamInterface> getFirstKey() {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0791 | `LocalityGroupMetaData` method `uint32_t getStartBlock() {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0792 | `LocalityGroupMetaData` method `uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0793 | `LocalityGroupMetaData` method `uint64_t write(cclient::data::streams::DataOutputStream *outStream);` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0794 | `LocalityGroupMetaData` method `void addIndexEntry(IndexEntry ind) {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0795 | `LocalityGroupMetaData` operator `LocalityGroupMetaData& operator=(const LocalityGroupMetaData &other) {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:93`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0796 | `LocalityGroupMetaData` method `void setDefaultLG() {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0797 | `LocalityGroupMetaData` method `bool getIsDefaultLG() const {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:108`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0798 | `LocalityGroupMetaData` method `std::shared_ptr<IndexManager> getIndexManager() {` (`include/data/constructs/rfile/meta/LocalityGroupMetaData.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0799 | `ReadAheadProxy` public data member `std::unique_ptr<cclient::data::streams::InputStream> stream;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:53`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0800 | `ReadAheadProxy` public data member `std::atomic<uint32_t> entries;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:54`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0801 | `ReadAheadProxy` public data member `std::atomic<bool> checkRange;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:55`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0802 | `ReadAheadProxy` public data member `std::atomic<bool> finished;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:56`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0803 | `ReadAheadProxy` public data member `std::atomic<bool> hasData;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:57`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0804 | `ReadAheadProxy` public data member `std::atomic<bool> interrupt;` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:58`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0805 | `LocalityGroupReader` constructor `explicit LocalityGroupReader(BlockCompressedFile *bcFile, cclient::data::streams::InputStream *input_stream, std::shared_ptr<LocalityGroupMetaData> metadata, cclient::data::ArrayAllocatorPool *allocatorInstancePtr,int version) : logger(logging::LoggerFactory<LocalityGroupReader>::getLogger()), bcFile(bcFile), reader(input_stream), version(version), closed(false), checkRange(false), topExists(false), interrupted(false), currentRange(NULL), iiter(NULL), prevKey( NULL), readAheadEnabled(false), readAheadRunning(false), entriesLeft(-1), entriesSkipped(0), allocatorInstance(allocatorInstancePtr) {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:112`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0806 | `LocalityGroupReader` method `void close();` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:139`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0807 | `LocalityGroupReader` destructor `virtual ~LocalityGroupReader() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:141`) | — | — | — | Missing Go | Assessed individually: `cclient::data::LocalityGroupReader` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0808 | `LocalityGroupReader` method `void enableReadAhead() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:146`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0809 | `LocalityGroupReader` method `std::shared_ptr<Key> getFirstKey() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0810 | `LocalityGroupReader` method `uint64_t getEntriesFiltered() const {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:154`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0811 | `LocalityGroupReader` method `std::shared_ptr<Key> getLastKey() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0812 | `LocalityGroupReader` method `std::shared_ptr<Key> getTopKey() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0813 | `LocalityGroupReader` method `std::shared_ptr<Value> getTopValue() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:166`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0814 | `LocalityGroupReader` method `virtual std::shared_ptr<cclient::data::streams::KeyValueIterator> getIterator() override {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:170`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0815 | `LocalityGroupReader` method `void limitVisibility(cclient::data::security::Authorizations *auths) {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:174`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0816 | `LocalityGroupReader` method `void setAgeOff(const std::shared_ptr<cclient::data::AgeOffEvaluator> &ageoff_evaluator){` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:178`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0817 | `LocalityGroupReader` method `void setKeyPredicate(const std::shared_ptr<cclient::data::KeyPredicate> &predicate){` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:182`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0818 | `LocalityGroupReader` method `bool hasTop() {` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:186`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0819 | `LocalityGroupReader` method `virtual bool hasNext() override { return topExists; }` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0820 | `LocalityGroupReader` method `void seek(const std::shared_ptr<cclient::data::streams::StreamRelocation> &position);` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:192`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0821 | `LocalityGroupReader` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) override{` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:194`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0822 | `LocalityGroupReader` method `std::vector<std::shared_ptr<cclient::data::Key>> getBlockKeys(const std::shared_ptr<cclient::data::streams::StreamRelocation> &position);` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0823 | `LocalityGroupReader` method `void f_next(bool errorOnNext=true);` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:200`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0824 | `LocalityGroupReader` method `virtual void next() override{` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:202`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0825 | `LocalityGroupReader` method `std::unique_ptr<cclient::data::streams::InputStream> getDataBlock(uint32_t index);` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:206`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0826 | `LocalityGroupReader` method `std::unique_ptr<cclient::data::streams::InputStream> getDataBlock(uint64_t offset, uint64_t compressedSize, uint64_t rawSize, bool use_cached = false);` (`include/data/constructs/rfile/meta/LocalityGroupReader.h:208`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0827 | `(namespace scope)` macro constant `#define MAGIC_NUMBER 0x20637474` (`include/data/constructs/rfile/meta/MetaBlock.h:19`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0828 | `(namespace scope)` macro constant `#define MAGIC_NUMBER2 0x74746320` (`include/data/constructs/rfile/meta/MetaBlock.h:20`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0829 | `(namespace scope)` macro constant `#define RFILE_VERSION 4` (`include/data/constructs/rfile/meta/MetaBlock.h:22`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0830 | `(namespace scope)` macro constant `#define RFILE_VERSION_3 3` (`include/data/constructs/rfile/meta/MetaBlock.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0831 | `(namespace scope)` macro constant `#define RFILE_VERSION_6 6` (`include/data/constructs/rfile/meta/MetaBlock.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0832 | `(namespace scope)` macro constant `#define RFILE_VERSION_7 7` (`include/data/constructs/rfile/meta/MetaBlock.h:25`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0833 | `(namespace scope)` macro constant `#define RFILE_VERSION_8 8` (`include/data/constructs/rfile/meta/MetaBlock.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0834 | `MetaBlock` constructor `MetaBlock();` (`include/data/constructs/rfile/meta/MetaBlock.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0835 | `MetaBlock` destructor `~MetaBlock();` (`include/data/constructs/rfile/meta/MetaBlock.h:48`) | — | — | — | Missing Go | Assessed individually: `cclient::data::MetaBlock` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0836 | `MetaBlock` method `void addLocalityGroups(std::vector<std::shared_ptr<LocalityGroupMetaData>> lgs) {` (`include/data/constructs/rfile/meta/MetaBlock.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0837 | `MetaBlock` method `uint64_t write(cclient::data::streams::DataOutputStream *outStream);` (`include/data/constructs/rfile/meta/MetaBlock.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0838 | `SerializedIndex` constructor `explicit SerializedIndex(const std::vector<int> &offsetList, uint8_t *datums, uint32_t dataLength, bool newFormat);` (`include/data/constructs/rfile/meta/SerializedIndex.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0839 | `SerializedIndex` constructor `explicit SerializedIndex(const std::shared_ptr<BaseMetaBlock> &source,const std::shared_ptr<cclient::data::Key> &key, const std::shared_ptr<BaseMetaBlock> &block);` (`include/data/constructs/rfile/meta/SerializedIndex.h:36`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0840 | `SerializedIndex` constructor `explicit SerializedIndex(const std::shared_ptr<BaseMetaBlock> &block);` (`include/data/constructs/rfile/meta/SerializedIndex.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0841 | `SerializedIndex` destructor `virtual ~SerializedIndex() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:40`) | — | — | — | Missing Go | Assessed individually: `cclient::data::SerializedIndex` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0842 | `SerializedIndex` method `size_t size() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0843 | `SerializedIndex` method `std::shared_ptr<IndexEntry> get(uint64_t index);` (`include/data/constructs/rfile/meta/SerializedIndex.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0844 | `SerializedIndex` method `std::shared_ptr<IndexEntry> get() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0845 | `SerializedIndex` method `std::shared_ptr<SerializedIndex> begin() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0846 | `SerializedIndex` method `std::shared_ptr<SerializedIndex> end() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0847 | `SerializedIndex` operator `std::shared_ptr<IndexEntry> operator*() {` (`include/data/constructs/rfile/meta/SerializedIndex.h:71`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0848 | `SerializedIndex` operator `SerializedIndex& operator++();` (`include/data/constructs/rfile/meta/SerializedIndex.h:80`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0849 | `SerializedIndex` operator `SerializedIndex& operator++(int t);` (`include/data/constructs/rfile/meta/SerializedIndex.h:83`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0850 | `SerializedIndex` method `bool isEnd();` (`include/data/constructs/rfile/meta/SerializedIndex.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0851 | `SerializedIndex` method `bool hasNextPosition();` (`include/data/constructs/rfile/meta/SerializedIndex.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0852 | `SerializedIndex` method `bool hasPrevious();` (`include/data/constructs/rfile/meta/SerializedIndex.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0853 | `SerializedIndex` method `bool hasNext();` (`include/data/constructs/rfile/meta/SerializedIndex.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0854 | `SerializedIndex` method `std::shared_ptr<IndexEntry> getPrevious();` (`include/data/constructs/rfile/meta/SerializedIndex.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0855 | `SerializedIndex` method `uint32_t getPreviousIndex();` (`include/data/constructs/rfile/meta/SerializedIndex.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0856 | `SerializedIndex` method `std::shared_ptr<IndexEntry> previous();` (`include/data/constructs/rfile/meta/SerializedIndex.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0857 | `SerializedIndex` operator `bool operator!=(const SerializedIndex &rhs) {` (`include/data/constructs/rfile/meta/SerializedIndex.h:99`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0858 | `SerializedIndex` constructor `explicit SerializedIndex(bool isEnd, int maxSize) : currentPosition(maxSize), blockParty(0), dataLength(0), data(0), ptr(0), offsets(0), newFormat(false) {` (`include/data/constructs/rfile/meta/SerializedIndex.h:103`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0859 | `GroupedReaders` public data member `std::vector< std::shared_ptr<cclient::data::streams::KeyValueIterator> > readers;` (`include/data/constructs/rfile/ParallelRFile.h:47`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0860 | `ReadersProxy` public data member `uint16_t maxThread;` (`include/data/constructs/rfile/ParallelRFile.h:51`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0861 | `ReadersProxy` public data member `size_t threadCacheCount;` (`include/data/constructs/rfile/ParallelRFile.h:52`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0862 | `ReadersProxy` public data member `std::atomic<uint16_t> currentThread;` (`include/data/constructs/rfile/ParallelRFile.h:53`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0863 | `ReadersProxy` public data member `std::atomic<bool> delegatorOnline;` (`include/data/constructs/rfile/ParallelRFile.h:54`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0864 | `ReadersProxy` public data member `std::mutex delegatorMutex;` (`include/data/constructs/rfile/ParallelRFile.h:55`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0865 | `ReadersProxy` public data member `std::condition_variable delegatorCondition;` (`include/data/constructs/rfile/ParallelRFile.h:56`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0866 | `ReadersProxy` public data member `std::atomic< moodycamel::ConcurrentQueue<std::shared_ptr<cclient::data::KeyValue>>*> resultSet;` (`include/data/constructs/rfile/ParallelRFile.h:57`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0867 | `ThreadReaders` public data member `std::shared_ptr<cclient::data::streams::KeyValueIterator> heapIter;` (`include/data/constructs/rfile/ParallelRFile.h:62`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0868 | `ThreadReaders` public data member `std::shared_ptr<cclient::data::streams::StreamRelocation> range = {};` (`include/data/constructs/rfile/ParallelRFile.h:63`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0869 | `ThreadReaders` public data member `moodycamel::ConcurrentQueue<std::shared_ptr<cclient::data::KeyValue>> resultSet;` (`include/data/constructs/rfile/ParallelRFile.h:64`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0870 | `ThreadReaders` public data member `std::atomic<bool> complete;` (`include/data/constructs/rfile/ParallelRFile.h:65`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0871 | `ThreadReaders` public data member `std::atomic<bool> started;` (`include/data/constructs/rfile/ParallelRFile.h:66`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0872 | `ThreadReaders` constructor `explicit ThreadReaders(const std::shared_ptr<cclient::data::streams::KeyValueIterator> &itr) : heapIter(itr), complete(false){` (`include/data/constructs/rfile/ParallelRFile.h:67`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0873 | `ParallelRFile` constructor `explicit ParallelRFile(std::function<std::vector<cclient::data::streams::FileStream>()> streamfn, int versions=0, bool withDeletes=false, bool propogate =false);` (`include/data/constructs/rfile/ParallelRFile.h:82`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0874 | `ParallelRFile` constructor `ParallelRFile(ParallelRFile &&other) = default;` (`include/data/constructs/rfile/ParallelRFile.h:84`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0875 | `ParallelRFile` operator `ParallelRFile& operator=(ParallelRFile &&other) = default;` (`include/data/constructs/rfile/ParallelRFile.h:86`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0876 | `ParallelRFile` destructor `virtual ~ParallelRFile();` (`include/data/constructs/rfile/ParallelRFile.h:88`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ParallelRFile` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0877 | `ParallelRFile` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) override;` (`include/data/constructs/rfile/ParallelRFile.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0878 | `ParallelRFile` method `virtual void close() override;` (`include/data/constructs/rfile/ParallelRFile.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0879 | `ParallelRFile` method `virtual bool hasNext() override;` (`include/data/constructs/rfile/ParallelRFile.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0880 | `ParallelRFile` method `virtual void next() override;` (`include/data/constructs/rfile/ParallelRFile.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0881 | `ParallelRFile` operator `virtual std::pair<std::shared_ptr<cclient::data::Key>, std::shared_ptr<cclient::data::Value>> operator*();` (`include/data/constructs/rfile/ParallelRFile.h:98`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0882 | `ParallelRFile` method `virtual uint64_t getEntriesFiltered() override;` (`include/data/constructs/rfile/ParallelRFile.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0883 | `ParallelRFile` method `virtual std::shared_ptr<Key> getTopKey() override { return current_kv->getKey(); }` (`include/data/constructs/rfile/ParallelRFile.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0884 | `ParallelRFile` method `virtual std::shared_ptr<Value> getTopValue() override { return current_kv->getValue(); }` (`include/data/constructs/rfile/ParallelRFile.h:106`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0885 | `ReadStrategy` enum `enum ReadStrategy {` (`include/data/constructs/rfile/ReadStrategy.h:23`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: SEQUENTIAL = 0; RANDOM_SEEK = 1; SEEK_SEQUENTIAL = 2; UNKNOWN = 3. Shoal exposes no equivalent type. |
| SB-CXX-0886 | `(namespace scope)` macro constant `#define VERSION_SIZE 4` (`include/data/constructs/rfile/rfile_version.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-0887 | `RFileVersion` constructor `RFileVersion()` (`include/data/constructs/rfile/rfile_version.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0888 | `RFileVersion` constructor `RFileVersion(const short majorV, const short minorV)` (`include/data/constructs/rfile/rfile_version.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0889 | `RFileVersion` constructor `explicit RFileVersion(cclient::data::streams::InputStream *input) {` (`include/data/constructs/rfile/rfile_version.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0890 | `RFileVersion` method `uint64_t read(cclient::data::streams::InputStream *input) {` (`include/data/constructs/rfile/rfile_version.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0891 | `RFileVersion` method `uint64_t write(cclient::data::streams::OutputStream *out) {` (`include/data/constructs/rfile/rfile_version.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0892 | `RFileVersion` method `void setMajor(const short val) {` (`include/data/constructs/rfile/rfile_version.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0893 | `RFileVersion` method `short getMajor() {` (`include/data/constructs/rfile/rfile_version.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0894 | `RFileVersion` method `void setMinor(const short val) {` (`include/data/constructs/rfile/rfile_version.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0895 | `RFileVersion` method `short getMinor() {` (`include/data/constructs/rfile/rfile_version.h:99`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0896 | `RFileVersion` public data member `maj` declared as `short maj, min;` (`include/data/constructs/rfile/rfile_version.h:106`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared in a multi-declarator statement; this row covers the `maj` declarator only. client-side data construct. |
| SB-CXX-0897 | `RFileVersion` public data member `min` declared as `short maj, min;` (`include/data/constructs/rfile/rfile_version.h:106`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared in a multi-declarator statement; this row covers the `min` declarator only. client-side data construct. |
| SB-CXX-0898 | `RFile` constructor `RFile(cclient::data::streams::OutputStream *output_stream, std::unique_ptr<BlockCompressedFile> bWriter);` (`include/data/constructs/rfile/RFile.h:51`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0899 | `RFile` constructor `RFile(cclient::data::streams::InputStream *input_stream, long fileLength);` (`include/data/constructs/rfile/RFile.h:59`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0900 | `RFile` destructor `virtual ~RFile();` (`include/data/constructs/rfile/RFile.h:62`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::RFile` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0901 | `RFile` method `uint32_t getMaxBlockSize() {` (`include/data/constructs/rfile/RFile.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0902 | `RFile` public data member `std::string colvis;` (`include/data/constructs/rfile/RFile.h:72`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0903 | `RFile` method `void limitVisibility(const std::string &viz) {` (`include/data/constructs/rfile/RFile.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0904 | `RFile` method `uint32_t* getEntryPtr() {` (`include/data/constructs/rfile/RFile.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0905 | `RFile` method `BlockCompressorStream* createStream() {` (`include/data/constructs/rfile/RFile.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0906 | `RFile` method `void setCurrentLocalityKey(std::shared_ptr<StreamInterface> key) {` (`include/data/constructs/rfile/RFile.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0907 | `RFile` method `static uint32_t generate_average(std::vector<std::shared_ptr<StreamInterface> > *keyValues) {` (`include/data/constructs/rfile/RFile.h:108`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0908 | `RFile` method `bool append(std::vector<std::shared_ptr<StreamInterface> > *keyValues, bool isSorted = false) {` (`include/data/constructs/rfile/RFile.h:120`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0909 | `RFile` method `bool append(std::shared_ptr<KeyValue> kv);` (`include/data/constructs/rfile/RFile.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0910 | `RFile` method `bool append(std::vector<std::shared_ptr<StreamInterface> > *keyValues, uint32_t average_recordSize, bool isSorted);` (`include/data/constructs/rfile/RFile.h:128`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0911 | `RFile` method `void addLocalityGroup(std::string name = "") {` (`include/data/constructs/rfile/RFile.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0912 | `RFile` method `void close();` (`include/data/constructs/rfile/RFile.h:145`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-010](#sec-15) already carries this obligation — `cclient::data::RFile::close` is the overload bound at `pysharkbite.cpp:502` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0913 | `RFile` method `void closeData() {` (`include/data/constructs/rfile/RFile.h:149`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0914 | `RFile` method `void closeBlock(std::shared_ptr<cclient::data::Key> lastKey) {` (`include/data/constructs/rfile/RFile.h:168`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0915 | `RFile` method `virtual bool hasNext();` (`include/data/constructs/rfile/RFile.h:176`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-007](#sec-15) already carries this obligation — `cclient::data::RFile::hasNext` is the overload bound at `pysharkbite.cpp:500` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0916 | `RFile` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) {` (`include/data/constructs/rfile/RFile.h:178`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-006](#sec-15) already carries this obligation — `cclient::data::RFile::relocate` is the overload bound at `pysharkbite.cpp:499` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0917 | `RFile` method `virtual void next();` (`include/data/constructs/rfile/RFile.h:184`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-009](#sec-15) already carries this obligation — `cclient::data::RFile::next` is the overload bound at `pysharkbite.cpp:503` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0918 | `RFile` operator `virtual DataStream<std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>>>* operator++();` (`include/data/constructs/rfile/RFile.h:186`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0919 | `RFile` method `std::shared_ptr<Key> getTopKey();` (`include/data/constructs/rfile/RFile.h:188`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0920 | `RFile` method `std::shared_ptr<Value> getTopValue();` (`include/data/constructs/rfile/RFile.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0921 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, RFile &rhs) {` (`include/data/constructs/rfile/RFile.h:193`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `RFile` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0922 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, RFile *rhs) {` (`include/data/constructs/rfile/RFile.h:207`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `RFile` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0923 | `RFile` operator `virtual std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>> operator*();` (`include/data/constructs/rfile/RFile.h:211`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0924 | `RFile` method `std::shared_ptr<cclient::data::KeyValue> getTop();` (`include/data/constructs/rfile/RFile.h:213`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-008](#sec-15) already carries this obligation — `cclient::data::RFile::getTop` is the overload bound at `pysharkbite.cpp:501` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0925 | `RFileOperations` method `static std::ifstream::pos_type filesize(const char* filename);` (`include/data/constructs/rfile/RFileOperations.h:42`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0926 | `RFileOperations` method `static std::shared_ptr<cclient::data::SequentialRFile> openForWrite(const std::string &path);` (`include/data/constructs/rfile/RFileOperations.h:43`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-001](#sec-15) already carries this obligation — that row cites this exact declaration (`include/data/constructs/rfile/RFileOperations.h:43`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0927 | `RFileOperations` method `static std::shared_ptr<cclient::data::SequentialRFile> write(const std::string &path, uint32_t compress_block_size = 32*1024);` (`include/data/constructs/rfile/RFileOperations.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0928 | `RFileOperations` method `static cclient::data::RFile *open(const std::string &);` (`include/data/constructs/rfile/RFileOperations.h:45`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-003](#sec-15) already carries this obligation — `cclient::data::RFileOperations::open` is the overload bound at `pysharkbite.cpp:588` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0929 | `RFileOperations` method `static std::shared_ptr<cclient::data::SequentialRFile> openSequential(const std::string &);` (`include/data/constructs/rfile/RFileOperations.h:46`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-002](#sec-15) already carries this obligation — `cclient::data::RFileOperations::openSequential` is the overload bound at `pysharkbite.cpp:590` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0930 | `RFileOperations` method `static std::shared_ptr<cclient::data::streams::KeyValueIterator> openManySequential(const std::vector<std::string> &rfiles,int versions=0, bool withDeletes = false, bool propogate = true, uint64_t maxtimestamp=0);` (`include/data/constructs/rfile/RFileOperations.h:55`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-004](#sec-15) already carries this obligation — `cclient::data::RFileOperations::openManySequential` is the overload bound at `pysharkbite.cpp:591` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0931 | `RFileOperations` method `static std::shared_ptr<cclient::data::streams::KeyValueIterator> openManySequentialWithPredicate(const std::vector<std::string> &rfiles,const std::shared_ptr<cclient::data::KeyPredicate> &predicate, int versions=0, bool withDeletes = false, bool propogate = true, uint64_t maxtimestamp=0);` (`include/data/constructs/rfile/RFileOperations.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0932 | `RFileOperations` method `static std::shared_ptr<cclient::data::streams::KeyValueIterator> openParallelRFiles(const std::vector<std::string> &rfiles,const std::shared_ptr<cclient::data::KeyPredicate> &predicate,int versions=0, bool withDeletes=false, bool propogate=true, uint64_t maxtimestamp=0);` (`include/data/constructs/rfile/RFileOperations.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0933 | `RFileOperations` method `static std::shared_ptr<cclient::data::streams::KeyValueIterator> openRFiles(const std::vector<std::string> &rfiles,const std::shared_ptr<cclient::data::KeyPredicate> &predicate,int versions, bool withDeletes, bool propogate, uint64_t maxtimestamp) {` (`include/data/constructs/rfile/RFileOperations.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0934 | `SequentialRFile` constructor `explicit SequentialRFile(cclient::data::streams::OutputStream *output_stream, std::unique_ptr<BlockCompressedFile> bWriter);` (`include/data/constructs/rfile/SequentialRFile.h:52`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0935 | `SequentialRFile` constructor `explicit SequentialRFile(std::unique_ptr<cclient::data::streams::OutputStream> output_stream, std::unique_ptr<BlockCompressedFile> bWriter);` (`include/data/constructs/rfile/SequentialRFile.h:54`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0936 | `SequentialRFile` constructor `explicit SequentialRFile(cclient::data::streams::InputStream *input_stream, long fileLength);` (`include/data/constructs/rfile/SequentialRFile.h:62`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0937 | `SequentialRFile` constructor `explicit SequentialRFile(std::unique_ptr<cclient::data::streams::InputStream> input_stream, long fileLength);` (`include/data/constructs/rfile/SequentialRFile.h:70`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0938 | `SequentialRFile` constructor `SequentialRFile(SequentialRFile &&other) = default;` (`include/data/constructs/rfile/SequentialRFile.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0939 | `SequentialRFile` operator `SequentialRFile& operator=(SequentialRFile &&other) = default;` (`include/data/constructs/rfile/SequentialRFile.h:74`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0940 | `SequentialRFile` destructor `virtual ~SequentialRFile();` (`include/data/constructs/rfile/SequentialRFile.h:77`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::SequentialRFile` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-0941 | `SequentialRFile` method `uint32_t getMaxBlockSize() {` (`include/data/constructs/rfile/SequentialRFile.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0942 | `SequentialRFile` public data member `std::string colvis;` (`include/data/constructs/rfile/SequentialRFile.h:87`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0943 | `SequentialRFile` method `void limitVisibility(const std::string &viz) {` (`include/data/constructs/rfile/SequentialRFile.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0944 | `SequentialRFile` method `uint32_t* getEntryPtr() {` (`include/data/constructs/rfile/SequentialRFile.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0945 | `SequentialRFile` method `void addStreams(const std::vector<cclient::data::streams::OutputStream*> &streams) {` (`include/data/constructs/rfile/SequentialRFile.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0946 | `SequentialRFile` method `BlockCompressorStream* createStream() {` (`include/data/constructs/rfile/SequentialRFile.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0947 | `SequentialRFile` method `void setCurrentLocalityKey(std::shared_ptr<StreamInterface> key) {` (`include/data/constructs/rfile/SequentialRFile.h:119`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0948 | `SequentialRFile` method `static uint32_t generate_average(std::vector<std::shared_ptr<StreamInterface> > *keyValues) {` (`include/data/constructs/rfile/SequentialRFile.h:126`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0949 | `SequentialRFile` method `bool append(std::vector<std::shared_ptr<StreamInterface> > *keyValues, bool isSorted = false) {` (`include/data/constructs/rfile/SequentialRFile.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0950 | `SequentialRFile` method `bool append(std::shared_ptr<KeyValue> kv);` (`include/data/constructs/rfile/SequentialRFile.h:142`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-011](#sec-15) already carries this obligation — `cclient::data::SequentialRFile::append` is the overload bound at `pysharkbite.cpp:530` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0951 | `SequentialRFile` method `bool append(std::vector<std::shared_ptr<StreamInterface> > *keyValues, uint32_t average_recordSize, bool isSorted);` (`include/data/constructs/rfile/SequentialRFile.h:145`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0952 | `SequentialRFile` method `void addLocalityGroup(std::string name = "") {` (`include/data/constructs/rfile/SequentialRFile.h:152`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-012](#sec-15) already carries this obligation — `cclient::data::SequentialRFile::addLocalityGroup` is the overload bound at `pysharkbite.cpp:529` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0953 | `SequentialRFile` method `void close();` (`include/data/constructs/rfile/SequentialRFile.h:162`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-028](#sec-15) already carries this obligation — `cclient::data::SequentialRFile::close` is the overload bound at `pysharkbite.cpp:531` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0954 | `SequentialRFile` method `void closeData() {` (`include/data/constructs/rfile/SequentialRFile.h:166`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0955 | `SequentialRFile` method `void closeBlock(std::shared_ptr<cclient::data::Key> lastKey) {` (`include/data/constructs/rfile/SequentialRFile.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0956 | `SequentialRFile` method `void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) {` (`include/data/constructs/rfile/SequentialRFile.h:193`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-013](#sec-15) already carries this obligation — `cclient::data::SequentialRFile::relocate` is the overload bound at `pysharkbite.cpp:524` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-0957 | `SequentialRFile` method `void setAgeOff(const std::shared_ptr<cclient::data::AgeOffEvaluator> &evaluator){` (`include/data/constructs/rfile/SequentialRFile.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0958 | `SequentialRFile` method `void setKeyPredicate(const std::shared_ptr<cclient::data::KeyPredicate> &predicate){` (`include/data/constructs/rfile/SequentialRFile.h:207`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0959 | `SequentialRFile` method `std::vector<std::shared_ptr<cclient::data::Key>> getBlocks(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location);` (`include/data/constructs/rfile/SequentialRFile.h:216`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0960 | `SequentialRFile` operator `virtual DataStream<std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>>>* operator++() override;` (`include/data/constructs/rfile/SequentialRFile.h:218`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0961 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, SequentialRFile &rhs) {` (`include/data/constructs/rfile/SequentialRFile.h:221`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `SequentialRFile` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0962 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, SequentialRFile *rhs) {` (`include/data/constructs/rfile/SequentialRFile.h:235`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `SequentialRFile` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-0963 | `SequentialRFile` operator `virtual std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>> operator*();` (`include/data/constructs/rfile/SequentialRFile.h:239`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0964 | `SequentialRFile` method `virtual uint64_t getEntriesFiltered() {` (`include/data/constructs/rfile/SequentialRFile.h:242`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0965 | `ShardedRFile` constructor `explicit ShardedRFile(cclient::data::streams::OutputStream *output_stream, std::unique_ptr<BlockCompressedFile> bWriter);` (`include/data/constructs/rfile/ShardedRFile.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0966 | `ShardedRFile` constructor `explicit ShardedRFile(std::unique_ptr<cclient::data::streams::OutputStream> output_stream, std::unique_ptr<BlockCompressedFile> bWriter);` (`include/data/constructs/rfile/ShardedRFile.h:52`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0967 | `ShardedRFile` constructor `explicit ShardedRFile(cclient::data::streams::InputStream *input_stream, long fileLength);` (`include/data/constructs/rfile/ShardedRFile.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0968 | `ShardedRFile` constructor `explicit ShardedRFile(std::unique_ptr<cclient::data::streams::InputStream> input_stream, long fileLength);` (`include/data/constructs/rfile/ShardedRFile.h:68`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0969 | `ShardedRFile` constructor `ShardedRFile(ShardedRFile &&other) = default;` (`include/data/constructs/rfile/ShardedRFile.h:70`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0970 | `ShardedRFile` operator `ShardedRFile& operator=(ShardedRFile &&other) = default;` (`include/data/constructs/rfile/ShardedRFile.h:72`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0971 | `ShardedRFile` destructor `virtual ~ShardedRFile();` (`include/data/constructs/rfile/ShardedRFile.h:75`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ShardedRFile` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0972 | `RelativeKey` constructor `explicit RelativeKey(ArrayAllocatorPool *allocins);` (`include/data/constructs/rkey.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0973 | `RelativeKey` constructor `explicit RelativeKey(const std::shared_ptr<Key> &previous_key, const std::shared_ptr<Key> &my_key, ArrayAllocatorPool *allocins);` (`include/data/constructs/rkey.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0974 | `RelativeKey` method `std::shared_ptr<StreamInterface> getStream() override;` (`include/data/constructs/rkey.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0975 | `RelativeKey` method `void setBase(const std::shared_ptr<Key> &my_key);` (`include/data/constructs/rkey.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0976 | `RelativeKey` method `virtual uint64_t read(cclient::data::streams::InputStream *in) override;` (`include/data/constructs/rkey.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0977 | `RelativeKey` method `uint64_t readFiltered(cclient::data::streams::InputStream *in);` (`include/data/constructs/rkey.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0978 | `RelativeKey` method `void setPrevious(const std::shared_ptr<Key> &previous_key);` (`include/data/constructs/rkey.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0979 | `RelativeKey` method `uint64_t write(cclient::data::streams::OutputStream *outStream) override;` (`include/data/constructs/rkey.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0980 | `RelativeKey` operator `bool operator <(const RelativeKey &rhs) const;` (`include/data/constructs/rkey.h:91`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0981 | `RelativeKey` operator `bool operator <(const RelativeKey *rhs) const;` (`include/data/constructs/rkey.h:93`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-0982 | `RelativeKey` destructor `~RelativeKey();` (`include/data/constructs/rkey.h:95`) | — | — | — | Missing Go | Assessed individually: `cclient::data::RelativeKey` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-0983 | `RelativeKey` constant `static const uint8_t ROW_SAME = 0x01;` (`include/data/constructs/rkey.h:98`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x01`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0984 | `RelativeKey` constant `static const uint8_t CF_SAME = 0x02;` (`include/data/constructs/rkey.h:99`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x02`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0985 | `RelativeKey` constant `static const uint8_t CQ_SAME = 0x04;` (`include/data/constructs/rkey.h:100`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x04`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0986 | `RelativeKey` constant `static const uint8_t CV_SAME = 0x08;` (`include/data/constructs/rkey.h:101`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x08`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0987 | `RelativeKey` constant `static const uint8_t TS_SAME = 0x10;` (`include/data/constructs/rkey.h:102`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x10`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0988 | `RelativeKey` constant `static const uint8_t DELETED = 0x20;` (`include/data/constructs/rkey.h:103`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x20`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0989 | `RelativeKey` constant `static const uint8_t ROW_PREFIX = 0x01;` (`include/data/constructs/rkey.h:105`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x01`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0990 | `RelativeKey` constant `static const uint8_t CF_PREFIX = 0x02;` (`include/data/constructs/rkey.h:106`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x02`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0991 | `RelativeKey` constant `static const uint8_t CQ_PREFIX = 0x04;` (`include/data/constructs/rkey.h:107`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x04`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0992 | `RelativeKey` constant `static const uint8_t CV_PREFIX = 0x08;` (`include/data/constructs/rkey.h:108`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x08`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0993 | `RelativeKey` constant `static const uint8_t TS_DIFF = 0x10;` (`include/data/constructs/rkey.h:109`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `0x10`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0994 | `RelativeKey` constant `static const uint8_t PREFIX_COMPRESSION_ENABLED = 128;` (`include/data/constructs/rkey.h:111`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `128`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-0995 | `RelativeKey` method `const std::shared_ptr<Key> getKey() const {` (`include/data/constructs/rkey.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0996 | `RelativeKey` method `bool isFiltered() const {` (`include/data/constructs/rkey.h:117`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0997 | `RelativeKey` method `void filterVisibility(const cclient::data::security::Authorizations &visibility, const std::shared_ptr<cclient::data::security::VisibilityEvaluator> &eval = nullptr);` (`include/data/constructs/rkey.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0998 | `RelativeKey` method `void setAgeOffEvaluator(const std::shared_ptr<cclient::data::AgeOffEvaluator> &conditions);` (`include/data/constructs/rkey.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-0999 | `RelativeKey` method `void setKeyPredicate(const std::shared_ptr<cclient::data::KeyPredicate> &conditions);` (`include/data/constructs/rkey.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1000 | `RelativeKey` method `void setFiltered();` (`include/data/constructs/rkey.h:127`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1001 | `ScanBatch` constructor `explicit ScanBatch(KeyExtent *extent) : myExtent(extent) {` (`include/data/constructs/ScanBatch.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1002 | `ScanBatch` method `void addRange(Range *range) {` (`include/data/constructs/ScanBatch.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1003 | `ScanBatch` destructor `virtual ~ScanBatch() {` (`include/data/constructs/ScanBatch.h:44`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ScanBatch` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1004 | `ScanState` constructor `ScanState() {` (`include/data/constructs/scanstate.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1005 | `ScanState` constructor `ScanState(cclient::data::security::AuthInfo *credentials, std::string tablename, cclient::data::security::Authorizations *auths, Range *range, std::set<Column*> *fetchedColumns, uint64_t size, std::vector<cclient::data::IterInfo*> *iters, std::map<std::string, std::map<std::string, std::string>> *options, bool isolated) : isolated(isolated), tableName(tablename), credentials(credentials), auths( auths), skipStartRow(false), size(size), scanId(0) {` (`include/data/constructs/scanstate.h:44`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1006 | `ScanState` constructor `ScanState(const ScanState& other);` (`include/data/constructs/scanstate.h:76`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1007 | `ScanState` destructor `virtual ~ScanState();` (`include/data/constructs/scanstate.h:77`) | — | — | — | Missing Go | Assessed individually: `cclient::data::ScanState` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1008 | `ScanState` operator `virtual ScanState& operator=(const ScanState& other);` (`include/data/constructs/scanstate.h:78`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1009 | `ScanState` operator `virtual bool operator==(const ScanState& other) const;` (`include/data/constructs/scanstate.h:79`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1010 | `ScanState` method `bool isIsolated() const {` (`include/data/constructs/scanstate.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1011 | `ScanState` method `std::string getTableName() const {` (`include/data/constructs/scanstate.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1012 | `ScanState` method `std::string getStartRow() const {` (`include/data/constructs/scanstate.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1013 | `ScanState` method `Range *getRange() const {` (`include/data/constructs/scanstate.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1014 | `ScanState` method `uint64_t getSize() const {` (`include/data/constructs/scanstate.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1015 | `ScanState` method `cclient::data::security::AuthInfo *getCredentials() const {` (`include/data/constructs/scanstate.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1016 | `ScanState` method `cclient::data::security::Authorizations *getAuths() const {` (`include/data/constructs/scanstate.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1017 | `ScanState` method `TabletLocation *getPrevious() const {` (`include/data/constructs/scanstate.h:109`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1018 | `ScanState` method `uint64_t getScanId() const {` (`include/data/constructs/scanstate.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1019 | `ScanState` method `bool isFinished() {` (`include/data/constructs/scanstate.h:117`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1020 | `ScanState` method `bool setFinished(bool fin = true) {` (`include/data/constructs/scanstate.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1021 | `ScanState` method `const std::vector<cclient::data::IterInfo*> *getIteratorList() const {` (`include/data/constructs/scanstate.h:126`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1022 | `ScanState` method `const std::map<std::string, std::map<std::string, std::string> > *getOptions() const {` (`include/data/constructs/scanstate.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1023 | `AuthInfo` constructor `AuthInfo (std::string user, std::string password, std::string instanceId);` (`include/data/constructs/security/AuthInfo.h:31`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-012](#sec-6) already carries this obligation — `cclient::data::security::AuthInfo::AuthInfo` is the overload bound at `pysharkbite.cpp:77` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1024 | `AuthInfo` constructor `AuthInfo() = default;` (`include/data/constructs/security/AuthInfo.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-040](#sec-19) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:33`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1025 | `AuthInfo` constructor `AuthInfo(AuthInfo &&other) = default;` (`include/data/constructs/security/AuthInfo.h:35`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-041](#sec-19) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:35`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1026 | `AuthInfo` constructor `AuthInfo(AuthInfo &other) = default;` (`include/data/constructs/security/AuthInfo.h:37`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-042](#sec-19) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:37`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1027 | `AuthInfo` destructor `virtual ~AuthInfo ();` (`include/data/constructs/security/AuthInfo.h:40`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-045](#sec-19) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:40`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1028 | `AuthInfo` method `std::string getUserName () const {` (`include/data/constructs/security/AuthInfo.h:43`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-013](#sec-6) already carries this obligation — `cclient::data::security::AuthInfo::getUserName` is the overload bound at `pysharkbite.cpp:78` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1029 | `AuthInfo` method `std::string getPassword () const {` (`include/data/constructs/security/AuthInfo.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-022](#sec-6) already carries this obligation — `cclient::data::security::AuthInfo::getPassword` is the overload bound at `pysharkbite.cpp:80` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1030 | `AuthInfo` method `std::string getInstanceId () const {` (`include/data/constructs/security/AuthInfo.h:55`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-015](#sec-6) already carries this obligation — `cclient::data::security::AuthInfo::getInstanceId` is the overload bound at `pysharkbite.cpp:82` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1031 | `AuthInfo` operator `AuthInfo &operator=(const AuthInfo &rhs) {` (`include/data/constructs/security/AuthInfo.h:60`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CFG-016](#sec-6) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:60`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1032 | `AuthInfo` operator `AuthInfo &operator=(AuthInfo &&other) = default;` (`include/data/constructs/security/AuthInfo.h:68`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-044](#sec-19) already carries this obligation — that row cites this exact declaration (`include/data/constructs/security/AuthInfo.h:68`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1033 | `AuthsInit` method `static AuthsInit* init() {` (`include/data/constructs/security/Authorizations.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1034 | `AuthsInit` method `bool* getDefaultAuths() {` (`include/data/constructs/security/Authorizations.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1035 | `Authorizations` constructor `Authorizations();` (`include/data/constructs/security/Authorizations.h:53`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-047](#sec-8) already carries this obligation — `cclient::data::security::Authorizations::Authorizations` is the overload bound at `pysharkbite.cpp:454` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1036 | `Authorizations` constructor `Authorizations(std::string authorizations, char *validCharacters, int valid);` (`include/data/constructs/security/Authorizations.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1037 | `Authorizations` destructor `~Authorizations();` (`include/data/constructs/security/Authorizations.h:57`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::security::Authorizations` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1038 | `Authorizations` constructor `explicit Authorizations(std::vector<std::string>*);` (`include/data/constructs/security/Authorizations.h:59`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1039 | `Authorizations` constructor `explicit Authorizations(const std::vector<std::string>&);` (`include/data/constructs/security/Authorizations.h:60`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-048](#sec-8) already carries this obligation — `cclient::data::security::Authorizations::Authorizations` is the overload bound at `pysharkbite.cpp:455` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1040 | `Authorizations` method `void addAuthorization(std::string auth);` (`include/data/constructs/security/Authorizations.h:62`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-050](#sec-8) already carries this obligation — `cclient::data::security::Authorizations::addAuthorization` is the overload bound at `pysharkbite.cpp:461` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1041 | `Authorizations` method `std::vector<std::string> getAuthorizations() const {` (`include/data/constructs/security/Authorizations.h:64`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-052](#sec-8) already carries this obligation — `cclient::data::security::Authorizations::getAuthorizations` is the overload bound at `pysharkbite.cpp:463` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1042 | `Authorizations` method `bool empty() const {` (`include/data/constructs/security/Authorizations.h:68`) | `Authorizations.Empty()` (`accumulo/authorizations.go:131`) | `shoal_authorizations_empty` | `TestAuthorizationsEqualCloneAndNilReceiver`, `TestAuthorizationsFeedTheScanPath` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact Go counterpart. A nil `*Authorizations` reports empty too, which is what a shim needs for [SB-KEEP-019](#sec-22). |
| SB-CXX-1043 | `Authorizations` method `bool contains(const std::string &viz) const {` (`include/data/constructs/security/Authorizations.h:72`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-051](#sec-8) already carries this obligation — `cclient::data::security::Authorizations::contains` is the overload bound at `pysharkbite.cpp:462` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1044 | `Authorizations` operator `bool operator ==(const Authorizations &rhs) const {` (`include/data/constructs/security/Authorizations.h:76`) | `Authorizations.Equal(other *Authorizations) bool` (`accumulo/authorizations.go:135`) | `shoal_authorizations_equal` | `TestAuthorizationsEqualCloneAndNilReceiver` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact Go counterpart: set equality over sorted, deduplicated labels, which is what comparing two `std::set<std::string>` values does. |
| SB-CXX-1045 | `Authorizations` method `static bool isValidAuthCharacter(char c);` (`include/data/constructs/security/Authorizations.h:80`) | `ValidAuthorizationCharacter(character byte) bool` (`accumulo/authorizations.go:208`) | `shoal_authorization_character_is_valid` | `TestAuthorizationsAcceptsWhatSharkbiteAccepts` (`accumulo/value_types_test.go`); linked C11/C++11 `TestSharedLibraryCABI`; `TestAuthorizationRegistryConcurrentGetters` (`cmd/shoal-capi/value_export_test.go`) | Covered | Exact Go counterpart of the declared rule: letters, digits, `_`, `-` and `:`. Shoal exposes it as a predicate and as the opt-in `Authorizations.Validate`, because the pinned release never applies it ([SB-UNSAFE-034](#sec-21)). |
| SB-CXX-1046 | `ColumnVisibility` constructor `explicit ColumnVisibility(const std::string&);` (`include/data/constructs/security/ColumnVisibility.h:37`) | `NewColumnVisibility(expression []byte) (*ColumnVisibility, error)` (`accumulo/column_visibility.go:254`) | — | `TestColumnVisibilityParsesThePinnedGrammar`, `TestColumnVisibilityRejectsMalformedExpressions` (`accumulo/column_visibility_test.go`) | Missing C ABI | Parses an expression into a tree using the pinned grammar — terms joined by AND or OR operators, which may not be mixed at one level, parenthesised sub-expressions, and double-quoted terms with `\\` and `\"` escapes — and copies the expression so the tree outlives the caller's slice, which the pinned tree cannot do because its nodes hold a raw `std::string*` into the `ColumnVisibility` that produced them. Malformed input returns `*VisibilityParseError` instead of throwing `IllegalArgumentException`. **Safe shim for unsafe C++ behavior:** a stray `)` is rejected rather than silently truncating the expression ([SB-UNSAFE-042](#sec-21)), and an unquoted term may contain `.` and `/`, which Accumulo's own grammar allows and the pinned parser rejects ([SB-UNSAFE-045](#sec-21)). No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1047 | `ColumnVisibility` method `static VisibilityNode normalize(VisibilityNode &root, const std::string &expression);` (`include/data/constructs/security/ColumnVisibility.h:39`) | `ColumnVisibility.Normalized() VisibilityNode` (`accumulo/column_visibility.go:283`) | — | `TestColumnVisibilityFlattenNormalizes`, `TestColumnVisibilityFlattenDoesNotMutateTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Rolls same-type children up into their parent, removes duplicates, orders what is left with `CompareVisibilityNodes`, and collapses a node left with one child. **Safe shim for unsafe C++ behavior:** the pinned `normalize` inserts the parent's own children instead of the matching child's, erases through a `std::vector` iterator the erase invalidates, and destructively rewrites the tree `getTree` hands out ([SB-UNSAFE-041](#sec-21)); the Go form returns a new tree and leaves the receiver untouched. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1048 | `ColumnVisibility` method `std::string flatten();` (`include/data/constructs/security/ColumnVisibility.h:41`) | `ColumnVisibility.Flatten() []byte` (`accumulo/column_visibility.go:290`) | — | `TestColumnVisibilityFlattenNormalizes` (`accumulo/column_visibility_test.go`) | Missing C ABI | Renders the normalized expression, parenthesising a child only where its type differs from its parent's, which is the pinned rule. **Safe shim for unsafe C++ behavior:** the pinned `flatten` emits the whole tail of the expression for every term because `getLength()` returns `start - end` ([SB-UNSAFE-040](#sec-21)). No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1049 | `ColumnVisibility` method `VisibilityNode* getTree() {` (`include/data/constructs/security/ColumnVisibility.h:43`) | `ColumnVisibility.Tree() VisibilityNode` (`accumulo/column_visibility.go:278`) | — | `TestColumnVisibilityFlattenDoesNotMutateTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Returns the parse tree by value. Reading or normalizing it never mutates the `ColumnVisibility`, unlike the pinned accessor, which hands out a pointer to a member that `flatten` then rewrites. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1050 | `SystemPermissions` enum `enum class SystemPermissions : int8_t {` (`include/data/constructs/security/Permissions.h:12`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: GRANT = 0; CREATE_TABLE = 1; DROP_TABLE = 2; ALTER_TABLE = 3; CREATE_USER = 4; DROP_USER = 5; ALTER_USER = 6; SYSTEM = 7; CREATE_NAMESPACE = 8; DROP_NAMESPACE = 9; ALTER_NAMESPACE = 10; OBTAIN_DELEGATION_TOKEN = 11. Shoal exposes no equivalent type. |
| SB-CXX-1051 | `TablePermissions` enum `enum class TablePermissions : int8_t{` (`include/data/constructs/security/Permissions.h:27`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: READ = 2; WRITE = 3; BULK_IMPORT = 4; ALTER_TABLE = 5; GRANT = 6; DROP_TABLE = 7; GET_SUMMARIES = 8. Shoal exposes no equivalent type. |
| SB-CXX-1052 | `NamespacePermissions` enum `enum class NamespacePermissions : int8_t {` (`include/data/constructs/security/Permissions.h:37`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: READ = 0; WRITE = 1; ALTER_NAMESPACE = 2; GRANT = 3; ALTER_TABLE = 4; CREATE_TABLE = 5; DROP_TABLE = 6; BULK_IMPORT = 7; DROP_NAMESPACE = 8. Shoal exposes no equivalent type. |
| SB-CXX-1053 | `EnableBitMaskOperators` constant `static const bool enable = true;` (`include/data/constructs/security/Permissions.h:55`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `true`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1054 | `EnableBitMaskOperators` constant `static const bool enable = true;` (`include/data/constructs/security/Permissions.h:61`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `true`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1055 | `EnableBitMaskOperators` constant `static const bool enable = true;` (`include/data/constructs/security/Permissions.h:67`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `true`. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1056 | `VisibilityEvaluator` constructor `explicit VisibilityEvaluator(const cclient::data::security::Authorizations &auths) : auths(auths) {` (`include/data/constructs/security/VisibilityEvaluator.h:42`) | `NewVisibilityEvaluator(auths *Authorizations) *VisibilityEvaluator` (`accumulo/column_visibility.go:625`) | — | `TestVisibilityEvaluatorEvaluatesExpressions`, `TestVisibilityEvaluatorDropsCachedDecisionsWhenAuthorizationsChange` (`accumulo/column_visibility_test.go`) | Missing C ABI | Builds an evaluator over a copy of the authorizations, so a later change to the caller's set cannot change a decision. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1057 | `VisibilityEvaluator` constructor `VisibilityEvaluator() {` (`include/data/constructs/security/VisibilityEvaluator.h:48`) | `NewVisibilityEvaluator(auths *Authorizations) *VisibilityEvaluator` (`accumulo/column_visibility.go:625`) | — | `TestVisibilityEvaluatorTreatsAnEmptyExpressionAsUnrestricted` (`accumulo/column_visibility_test.go`) | Missing C ABI | The pinned default constructor leaves the evaluator with empty authorizations; passing `nil` is the Go spelling of the same starting state. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1058 | `VisibilityEvaluator` method `void setVisibility(const cclient::data::security::Authorizations &at) {` (`include/data/constructs/security/VisibilityEvaluator.h:51`) | `VisibilityEvaluator.SetAuthorizations(auths *Authorizations)` (`accumulo/column_visibility.go:643`) | — | `TestVisibilityEvaluatorDropsCachedDecisionsWhenAuthorizationsChange` (`accumulo/column_visibility_test.go`) | Missing C ABI | Replaces the authorizations. **Safe shim for unsafe C++ behavior:** the pinned `setVisibility` leaves the decision cache in place, so a reused evaluator answers with the previous principal's authorizations ([SB-UNSAFE-043](#sec-21)); the Go form clears the cache. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1059 | `VisibilityEvaluator` method `bool evaluate(const std::vector<char> &expr) {` (`include/data/constructs/security/VisibilityEvaluator.h:55`) | `VisibilityEvaluator.Evaluate(expression []byte) (bool, error)` (`accumulo/column_visibility.go:657`) | — | `TestVisibilityEvaluatorEvaluatesExpressions`, `TestVisibilityEvaluatorDropsCachedDecisionsWhenAuthorizationsChange` (`accumulo/column_visibility_test.go`) | Missing C ABI | The `std::vector<char>` overload takes raw bytes; Go's byte slice is the same contract, and one method serves both pinned overloads. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1060 | `VisibilityEvaluator` method `bool evaluate(const std::string &expr) {` (`include/data/constructs/security/VisibilityEvaluator.h:59`) | `VisibilityEvaluator.Evaluate(expression []byte) (bool, error)` (`accumulo/column_visibility.go:657`) | — | `TestVisibilityEvaluatorEvaluatesExpressions`, `TestVisibilityEvaluatorTreatsAnEmptyExpressionAsUnrestricted`, `TestVisibilityEvaluatorMatchesQuotedTerms` (`accumulo/column_visibility_test.go`) | Missing C ABI | Parses and evaluates, caching the decision. **Safe shim for unsafe C++ behavior:** the pinned overload returns `false` for an empty expression when the authorizations are non-empty and throws when they are empty; an empty visibility imposes no requirement, so Go returns `true` ([SB-UNSAFE-044](#sec-21)). No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1061 | `VisibilityEvaluator` method `bool evaluate(const std::string &expression, const VisibilityNode &root);` (`include/data/constructs/security/VisibilityEvaluator.h:74`) | `VisibilityEvaluator.EvaluateTree(expression []byte, root VisibilityNode) (bool, error)` (`accumulo/column_visibility.go:688`) | — | `TestVisibilityEvaluatorUsesTheParsedTree`, `TestVisibilityEvaluatorReportsMalformedTrees`, `TestVisibilityEvaluatorRejectsATreeFromAnotherExpression` (`accumulo/column_visibility_test.go`) | Missing C ABI | Evaluates an already parsed tree against the expression it was read from, reporting `AND`/`OR` nodes with fewer than two children and unknown node types as errors rather than exceptions. The tree must be the one that expression produced: because every Go node keeps the expression it was parsed from, a mismatched or truncated expression is rejected instead of being decided against the wrong bytes, which the pinned overload cannot detect because its nodes only hold a pointer to whatever string outlived them. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1062 | `NodeType` enum `enum NodeType {` (`include/data/constructs/security/VisibilityNode.h:32`) | `VisibilityNodeType` (`accumulo/column_visibility.go:49`) | — | `TestVisibilityNodeTypeNames` (`accumulo/column_visibility_test.go`) | Missing C ABI | `VisibilityEmpty`, `VisibilityTerm`, `VisibilityOr` and `VisibilityAnd` carry the pinned numeric values 0…3, so a mapping layer needs no translation table. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1063 | `NodeExpression` constructor `explicit NodeExpression(const std::string &expr, size_t offset, size_t size) : term(expr, offset, size) {` (`include/data/constructs/security/VisibilityNode.h:43`) | `NewNodeExpression(expression []byte, offset, size int) (NodeExpression, error)` (`accumulo/column_visibility.go:83`) | — | `TestNewNodeExpressionValidatesItsSpan` (`accumulo/column_visibility_test.go`) | Missing C ABI | Copies the term out of the expression and rejects a span that runs past its end, which the pinned constructor cannot do because `std::string`'s substring constructor clamps instead. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1064 | `NodeExpression` method `std::string getTerm() const {` (`include/data/constructs/security/VisibilityNode.h:48`) | `NodeExpression.Term() []byte` (`accumulo/column_visibility.go:93`) | — | `TestVisibilityNodeTermUnquotesAndUnescapes` (`accumulo/column_visibility_test.go`) | Missing C ABI | Returns a copy of the term text. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1065 | `NodeExpression` method `const char* getBuffer() const {` (`include/data/constructs/security/VisibilityNode.h:52`) | `NodeExpression.Buffer() []byte` (`accumulo/column_visibility.go:97`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Returns the term bytes. The pinned accessor hands out the internal `c_str()` pointer, which dangles once the term is destroyed; Go returns a copy the caller owns. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1066 | `NodeExpression` method `size_t getSize() const {` (`include/data/constructs/security/VisibilityNode.h:56`) | `NodeExpression.Size() int` (`accumulo/column_visibility.go:100`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Term length in bytes. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1067 | `VisibilityNode` public data member `std::string *expression;` (`include/data/constructs/security/VisibilityNode.h:69`) | `VisibilityNode.Expression() []byte` (`accumulo/column_visibility.go:121`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | The pinned member is a raw `std::string*` into the owning `ColumnVisibility`; the Go node keeps its own copy and exposes it as an accessor, so reading a node never depends on the lifetime of what parsed it. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1068 | `VisibilityNode` public data member `NodeType type;` (`include/data/constructs/security/VisibilityNode.h:70`) | `VisibilityNode.Type() VisibilityNodeType` (`accumulo/column_visibility.go:118`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Node type, exposed as an accessor over an unexported field so a tree cannot be retyped after parsing. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1069 | `VisibilityNode` public data member `int start;` (`include/data/constructs/security/VisibilityNode.h:71`) | `VisibilityNode.TermStart() int` (`accumulo/column_visibility.go:149`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | First byte offset of the node's span. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1070 | `VisibilityNode` public data member `int end;` (`include/data/constructs/security/VisibilityNode.h:72`) | `VisibilityNode.TermEnd() int` (`accumulo/column_visibility.go:152`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Offset one past the node's last byte. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1071 | `VisibilityNode` public data member `std::vector<VisibilityNode> children;` (`include/data/constructs/security/VisibilityNode.h:73`) | `VisibilityNode.Children() []VisibilityNode` (`accumulo/column_visibility.go:124`) | — | `TestVisibilityNodeAccessorsDescribeTheTree`, `TestColumnVisibilityCopiesItsInput` (`accumulo/column_visibility_test.go`) | Missing C ABI | Returns a copy of the children, so a caller cannot reach into a parsed tree and rewrite it. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1072 | `VisibilityNode` constructor `VisibilityNode()` (`include/data/constructs/security/VisibilityNode.h:75`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1073 | `VisibilityNode` constructor `explicit VisibilityNode(std::string *expr, NodeType type, int start) : expression(expr), type(type), start(start), end(start + 1) {` (`include/data/constructs/security/VisibilityNode.h:83`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1074 | `VisibilityNode` constructor `explicit VisibilityNode(std::string *expr, int start, int end) : expression(expr), type(NodeType::TERM), start(start), end(end) {` (`include/data/constructs/security/VisibilityNode.h:91`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1075 | `VisibilityNode` method `const char* getExpression() const {` (`include/data/constructs/security/VisibilityNode.h:99`) | `VisibilityNode.Expression() []byte` (`accumulo/column_visibility.go:121`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | The pinned accessor dereferences the raw expression pointer and returns `data()`, which is undefined once the owning object dies; the Go accessor returns a copy of the node's own expression. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1076 | `VisibilityNode` method `void add(const VisibilityNode &child) {` (`include/data/constructs/security/VisibilityNode.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1077 | `VisibilityNode` method `void add(VisibilityNode &&child) {` (`include/data/constructs/security/VisibilityNode.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1078 | `VisibilityNode` method `NodeType getType() const {` (`include/data/constructs/security/VisibilityNode.h:111`) | `VisibilityNode.Type() VisibilityNodeType` (`accumulo/column_visibility.go:118`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Node type accessor. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1079 | `VisibilityNode` method `std::vector<VisibilityNode>* getChildrenRef() {` (`include/data/constructs/security/VisibilityNode.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1080 | `VisibilityNode` method `std::vector<VisibilityNode> getChildren() const {` (`include/data/constructs/security/VisibilityNode.h:119`) | `VisibilityNode.Children() []VisibilityNode` (`accumulo/column_visibility.go:124`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Returns the children by value, as the pinned accessor does. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1081 | `VisibilityNode` method `void clear() {` (`include/data/constructs/security/VisibilityNode.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1082 | `VisibilityNode` method `bool empty() const {` (`include/data/constructs/security/VisibilityNode.h:128`) | `VisibilityNode.Empty() bool` (`accumulo/column_visibility.go:135`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Reports whether the node carries no expression, which is how the pinned type recognises an unpopulated node. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1083 | `VisibilityNode` method `size_t getSize() const {` (`include/data/constructs/security/VisibilityNode.h:132`) | `VisibilityNode.Size() int` (`accumulo/column_visibility.go:138`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Number of children. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1084 | `VisibilityNode` method `int getLength() const {` (`include/data/constructs/security/VisibilityNode.h:136`) | `VisibilityNode.Len() int` (`accumulo/column_visibility.go:141`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Length of the node's span. **Safe shim for unsafe C++ behavior:** the pinned `getLength()` computes `start - end`, which is negative for every non-empty node ([SB-UNSAFE-040](#sec-21)); the Go accessor returns `end - start`. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1085 | `VisibilityNode` method `int getTermStart() const {` (`include/data/constructs/security/VisibilityNode.h:140`) | `VisibilityNode.TermStart() int` (`accumulo/column_visibility.go:149`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Start offset accessor. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1086 | `VisibilityNode` method `int getTermEnd() const {` (`include/data/constructs/security/VisibilityNode.h:144`) | `VisibilityNode.TermEnd() int` (`accumulo/column_visibility.go:152`) | — | `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | End offset accessor. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1087 | `VisibilityNode` method `NodeExpression getTerm(const std::string &expression) const {` (`include/data/constructs/security/VisibilityNode.h:148`) | `VisibilityNode.Term(expression []byte) (NodeExpression, error)` (`accumulo/column_visibility.go:156`) | — | `TestVisibilityNodeTermUnquotesAndUnescapes`, `TestVisibilityNodeAccessorsDescribeTheTree` (`accumulo/column_visibility_test.go`) | Missing C ABI | Reads the node's term out of the expression, stripping the surrounding quotes as the pinned accessor does and additionally resolving `\\` and `\"` escapes, so a quoted term compares equal to the authorization it names. A non-term node returns an error rather than throwing `std::runtime_error`. No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1088 | `ExpressionASTComparator` operator `bool operator()(const VisibilityNode &lhs, const VisibilityNode &rhs) {` (`include/data/constructs/security/VisibilityNode.h:163`) | `CompareVisibilityNodes(left, right VisibilityNode) int` (`accumulo/column_visibility.go:212`) | — | `TestCompareVisibilityNodesOrdersByTypeThenTerm` (`accumulo/column_visibility_test.go`) | Missing C ABI | The ordering normalization uses: node type, then term bytes, then child count and child order. **Safe shim for unsafe C++ behavior:** the pinned comparator assigns its recursive `bool` result to an `int`, so the "less" branch is unreachable and the relation is not a strict weak ordering — undefined behavior for the `std::set` `normalize` builds — and it indexes the right operand's term through the left operand's buffer ([SB-UNSAFE-041](#sec-21)). No declaration in `capi/include/shoal.h`. security surface. |
| SB-CXX-1089 | `RangeDefinition` constructor `explicit RangeDefinition(cclient::data::security::AuthInfo *creds, cclient::data::security::Authorizations *auths, const std::string &host, uint32_t port, std::vector<std::shared_ptr<cclient::data::Range>> *keyRange, std::vector<std::shared_ptr<KeyExtent>> *keyExt, const std::vector<Column> &columns);` (`include/data/constructs/server/RangeDefinition.h:51`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1090 | `RangeDefinition` constructor `explicit RangeDefinition(cclient::data::security::AuthInfo *creds, cclient::data::security::Authorizations *auths, const std::string &host, uint32_t port, std::vector<std::shared_ptr<cclient::data::Range>> *keyRange, std::vector<std::shared_ptr<KeyExtent>> *keyExt);` (`include/data/constructs/server/RangeDefinition.h:57`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1091 | `RangeDefinition` method `std::vector<std::shared_ptr<Range>>* getRanges();` (`include/data/constructs/server/RangeDefinition.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1092 | `RangeDefinition` method `std::vector<std::shared_ptr<KeyExtent>>* getExtents();` (`include/data/constructs/server/RangeDefinition.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1093 | `RangeDefinition` method `std::vector<Column> getColumn() const {` (`include/data/constructs/server/RangeDefinition.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1094 | `RangeDefinition` destructor `virtual ~RangeDefinition() {` (`include/data/constructs/server/RangeDefinition.h:81`) | — | — | — | Missing Go | Assessed individually: `cclient::data::tserver::RangeDefinition` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1095 | `(namespace scope)` friend operator `inline std::ostream& operator <<(std::ostream &out, const RangeDefinition &rhs) {` (`include/data/constructs/server/RangeDefinition.h:86`) | — | — | — | Missing Go | Friend operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. Declared **friend** inside `RangeDefinition` and therefore a namespace-scope function found by argument-dependent lookup, not a member: every `std::ostream << obj` call in user code binds to it, so dropping it is a source break even though it is not a member of any class. client-side data construct. |
| SB-CXX-1096 | `RangeDefinition` operator `bool operator==(const RangeDefinition *rhs) const;` (`include/data/constructs/server/RangeDefinition.h:98`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1097 | `RangeDefinition` operator `bool operator==(const RangeDefinition &other) const;` (`include/data/constructs/server/RangeDefinition.h:100`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1098 | `ServerDefinition` constructor `explicit ServerDefinition(cclient::data::security::AuthInfo *creds, cclient::data::security::Authorizations *auths, const std::string &host, uint32_t port);` (`include/data/constructs/server/ServerDefinition.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1099 | `ServerDefinition` destructor `~ServerDefinition();` (`include/data/constructs/server/ServerDefinition.h:53`) | — | — | — | Missing Go | Assessed individually: `cclient::data::tserver::ServerDefinition` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1100 | `ServerDefinition` method `cclient::data::security::AuthInfo *getCredentials();` (`include/data/constructs/server/ServerDefinition.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1101 | `ServerDefinition` method `cclient::data::security::Authorizations *getAuthorizations() const;` (`include/data/constructs/server/ServerDefinition.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1102 | `ServerDefinition` method `std::string getServer() const;` (`include/data/constructs/server/ServerDefinition.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1103 | `ServerDefinition` method `uint32_t getPort() const;` (`include/data/constructs/server/ServerDefinition.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1104 | `TextBuffer` public data member `char *buffer;` (`include/data/constructs/SkippedRelativeKey.h:37`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1105 | `TextBuffer` public data member `size_t size;` (`include/data/constructs/SkippedRelativeKey.h:38`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1106 | `TextBuffer` public data member `size_t max;` (`include/data/constructs/SkippedRelativeKey.h:39`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1107 | `TextBuffer` constructor `TextBuffer(){` (`include/data/constructs/SkippedRelativeKey.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1108 | `TextBuffer` operator `bool operator>(const TextBuffer &o) const{` (`include/data/constructs/SkippedRelativeKey.h:46`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1109 | `TextBuffer` operator `bool operator>=(const TextBuffer &o) const{` (`include/data/constructs/SkippedRelativeKey.h:56`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1110 | `TextBuffer` operator `TextBuffer operator=(const std::pair<char*,size_t> other){` (`include/data/constructs/SkippedRelativeKey.h:66`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1111 | `TextBuffer` method `std::string toString() const {` (`include/data/constructs/SkippedRelativeKey.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1112 | `SkippedRelativeKey` constructor `explicit SkippedRelativeKey(RelativeKey *rkey, int skipped, std::shared_ptr<Key> prevKey, ArrayAllocatorPool *allocator) : logger(logging::LoggerFactory<SkippedRelativeKey>::getLogger()), rkey(rkey), allocatorInstance(allocator), skipped(skipped), prevKey(prevKey) {` (`include/data/constructs/SkippedRelativeKey.h:122`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1113 | `SkippedRelativeKey` constructor `explicit SkippedRelativeKey(ArrayAllocatorPool *allocator) : SkippedRelativeKey(NULL, 0, NULL, allocator) {` (`include/data/constructs/SkippedRelativeKey.h:132`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1114 | `SkippedRelativeKey` method `bool skip(cclient::data::streams::InputStream *stream, std::shared_ptr<Key> seekKey, std::vector<char> *valCopy, std::shared_ptr<Key> prevKey, std::shared_ptr<Key> currKey, size_t entriesRemaining) {` (`include/data/constructs/SkippedRelativeKey.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1115 | `SkippedRelativeKey` method `void filterVisibility(const cclient::data::security::Authorizations &auths) {` (`include/data/constructs/SkippedRelativeKey.h:142`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1116 | `SkippedRelativeKey` method `void setAgeOff(const std::shared_ptr<cclient::data::AgeOffEvaluator> &ageoff){` (`include/data/constructs/SkippedRelativeKey.h:146`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1117 | `SkippedRelativeKey` method `void setKeyPredicate(const std::shared_ptr<cclient::data::KeyPredicate> &predicate){` (`include/data/constructs/SkippedRelativeKey.h:149`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1118 | `SkippedRelativeKey` destructor `virtual ~SkippedRelativeKey() {` (`include/data/constructs/SkippedRelativeKey.h:152`) | — | — | — | Missing Go | Assessed individually: `cclient::data::SkippedRelativeKey` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1119 | `SkippedRelativeKey` method `std::shared_ptr<RelativeKey> getRelativeKey() {` (`include/data/constructs/SkippedRelativeKey.h:155`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1120 | `SkippedRelativeKey` method `std::shared_ptr<Key> getPrevKey() {` (`include/data/constructs/SkippedRelativeKey.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1121 | `SkippedRelativeKey` method `int getSkipped() {` (`include/data/constructs/SkippedRelativeKey.h:161`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1122 | `(namespace scope)` macro constant `#define METADATA_TABLET_COLUMN_FAMILY std::string("~tab")` (`include/data/constructs/StructureDefinitions.h:22`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1123 | `(namespace scope)` macro constant `#define METADATA_CURRENT_LOCATION_COLUMN_FAMILY std::string("loc")` (`include/data/constructs/StructureDefinitions.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1124 | `(namespace scope)` macro constant `#define METADATA_FILE_COLUMN_FAMILY std::string("file")` (`include/data/constructs/StructureDefinitions.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1125 | `(namespace scope)` macro constant `#define METADATA_FUTURE_LOCATION_COLUMN_FAMILY std::string("future")` (`include/data/constructs/StructureDefinitions.h:25`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1126 | `(namespace scope)` macro constant `#define METADATA_LAST_LOCATION_COLUMN_FAMILY std::string("last")` (`include/data/constructs/StructureDefinitions.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1127 | `(namespace scope)` macro constant `#define METADATA_PREV_ROW_COLUMN_CQ std::string("~pr")` (`include/data/constructs/StructureDefinitions.h:27`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1128 | `(namespace scope)` macro constant `#define METADATA_TABLE_ID std::string("!0")` (`include/data/constructs/StructureDefinitions.h:28`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1129 | `(namespace scope)` macro constant `#define META_TABLE_ID "!0"` (`include/data/constructs/tables/meatadataTable.h:18`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1130 | `(namespace scope)` macro constant `#define META_TABLE_ID "!0"` (`include/data/constructs/tables/metadataTable.h:18`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1131 | `(namespace scope)` macro constant `#define ROOT_TABLE_ID "+r"` (`include/data/constructs/tables/rootTable.h:18`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1132 | `TabletType` enum `enum TabletType {` (`include/data/constructs/tablet/TabletType.h:28`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: ROOT = 0; METADATA = 1; USER = 2. Shoal exposes no equivalent type. |
| SB-CXX-1133 | `(namespace scope)` namespace-scope function `static TabletType fromExtent(KeyExtent *extent) {` (`include/data/constructs/tablet/TabletType.h:33`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1134 | `Text` constructor `explicit Text(ObjectAllocatorPool<Text> *);` (`include/data/constructs/Text.h:27`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1135 | `Text` constructor `explicit Text(char *, size_t, size_t);` (`include/data/constructs/Text.h:28`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1136 | `Text` constructor `explicit Text(ObjectAllocatorPool<Text> *, char *, size_t, size_t);` (`include/data/constructs/Text.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1137 | `Text` destructor `virtual ~Text();` (`include/data/constructs/Text.h:31`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Text` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1138 | `Text` method `std::pair<char*, size_t> getBuffer();` (`include/data/constructs/Text.h:33`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1139 | `Text` method `void reset(char *buffer, size_t length, size_t size);` (`include/data/constructs/Text.h:35`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1140 | `Text` method `void reset();` (`include/data/constructs/Text.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1141 | `Text` method `void clear();` (`include/data/constructs/Text.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1142 | `Text` method `bool equals(const char *str, size_t iSize) const;` (`include/data/constructs/Text.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1143 | `Text` method `std::string toString() const;` (`include/data/constructs/Text.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1144 | `Text` method `size_t getMaxSize() const;` (`include/data/constructs/Text.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1145 | `Text` method `size_t size() const;` (`include/data/constructs/Text.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1146 | `Text` method `bool empty() const{` (`include/data/constructs/Text.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1147 | `Value` constructor `Value();` (`include/data/constructs/value.h:33`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-001](#sec-8) already carries this obligation — `cclient::data::Value::Value` is the overload bound at `pysharkbite.cpp:273` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1148 | `Value` constructor `explicit Value(const std::string &val) {` (`include/data/constructs/value.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1149 | `Value` destructor `virtual ~Value();` (`include/data/constructs/value.h:42`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::Value` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1150 | `Value` method `void setValue(uint8_t *val, size_t size, uint32_t ptrOff = 0);` (`include/data/constructs/value.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1151 | `Value` method `void setValue(const std::string &);` (`include/data/constructs/value.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1152 | `Value` method `void append(uint8_t *val, size_t size);` (`include/data/constructs/value.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1153 | `Value` method `void deepCopy(Value *v);` (`include/data/constructs/value.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1154 | `Value` method `uint8_t *data();` (`include/data/constructs/value.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1155 | `Value` method `size_t size();` (`include/data/constructs/value.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1156 | `Value` method `std::shared_ptr<StreamInterface> getStream() override;` (`include/data/constructs/value.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1157 | `Value` method `std::pair<uint8_t*, size_t> getValue() const;` (`include/data/constructs/value.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1158 | `Value` method `std::string getValueAsString() const;` (`include/data/constructs/value.h:60`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-DATA-002](#sec-8) already carries this obligation — `cclient::data::Value::getValueAsString` is the overload bound at `pysharkbite.cpp:274` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1159 | `Value` method `uint64_t write (cclient::data::streams::OutputStream *outStream) override;` (`include/data/constructs/value.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1160 | `Value` method `uint64_t read (cclient::data::streams::InputStream *in) override;` (`include/data/constructs/value.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1161 | `Value` operator `bool operator ==(const Value & rhs) const;` (`include/data/constructs/value.h:66`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1162 | `Value` operator `bool operator !=(const Value &rhs) const;` (`include/data/constructs/value.h:68`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1163 | `Data` constructor `Data();` (`include/data/Data.h:26`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1164 | `Data` destructor `virtual ~Data();` (`include/data/Data.h:27`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Data` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1165 | `(namespace scope)` macro constant `#define INVALID_ZK_DATA 0` (`include/data/exceptions/ClientException.h:21`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1166 | `(namespace scope)` macro constant `#define INVALID_ZK_SERVER_DATA 1` (`include/data/exceptions/ClientException.h:22`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1167 | `(namespace scope)` macro constant `#define INVALID_ZK_SERVER_PORT 2` (`include/data/exceptions/ClientException.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1168 | `(namespace scope)` macro constant `#define INVALID_SERVER_PORT 3` (`include/data/exceptions/ClientException.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1169 | `(namespace scope)` macro constant `#define NO_MASTER_FOUND 4` (`include/data/exceptions/ClientException.h:25`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1170 | `(namespace scope)` macro constant `#define INVALID_USERNAME_PASSWORD 5` (`include/data/exceptions/ClientException.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1171 | `(namespace scope)` macro constant `#define COULD_NOT_CREATE_NAMESPACE 6` (`include/data/exceptions/ClientException.h:27`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1172 | `(namespace scope)` macro constant `#define DELETE_DEFAULT_NAMESPACE 7` (`include/data/exceptions/ClientException.h:28`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1173 | `(namespace scope)` macro constant `#define NO_LOCATION_IDENTIFIED 8` (`include/data/exceptions/ClientException.h:29`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1174 | `(namespace scope)` macro constant `#define TABLE_NOT_FOUND 9` (`include/data/exceptions/ClientException.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1175 | `(namespace scope)` macro constant `#define RANGE_NOT_SPECIFIED 10` (`include/data/exceptions/ClientException.h:31`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1176 | `(namespace scope)` macro constant `#define TABLE_OR_NAMESPACE_EMPTY 11` (`include/data/exceptions/ClientException.h:32`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1177 | `(namespace scope)` macro constant `#define ARGUMENT_CANNOT_BE_NULL 12` (`include/data/exceptions/ClientException.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1178 | `(namespace scope)` macro constant `#define SCANNER_ALREADY_STARTED 13` (`include/data/exceptions/ClientException.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1179 | `(namespace scope)` namespace-scope variable `extern const std::map<uint16_t, std::string> CLIENT_ERROR_CODES;` (`include/data/exceptions/ClientException.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-ERR-002](#sec-18) already carries this obligation — that row cites this exact declaration (`include/data/exceptions/ClientException.h:39`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1180 | `ClientException` constructor `explicit ClientException(const std::string& excp) : errorCode(-1), excp_str(excp) {}` (`include/data/exceptions/ClientException.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1181 | `ClientException` constructor `ClientException(const uint16_t errorCode) : errorCode(errorCode) {}` (`include/data/exceptions/ClientException.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1182 | `ClientException` destructor `~ClientException() throw() {}` (`include/data/exceptions/ClientException.h:48`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::ClientException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1183 | `ClientException` method `const char* what() const throw() {` (`include/data/exceptions/ClientException.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1184 | `ClientException` method `const int getErrorCode() const { return errorCode; }` (`include/data/exceptions/ClientException.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1185 | `HDFSException` constructor `explicit HDFSException(std::string excp) : excp_str(excp) {}` (`include/data/exceptions/HDFSException.h:26`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1186 | `HDFSException` destructor `~HDFSException() throw() {}` (`include/data/exceptions/HDFSException.h:28`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::HDFSException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1187 | `HDFSException` method `const char* what() const throw() { return excp_str.c_str(); }` (`include/data/exceptions/HDFSException.h:29`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1188 | `IllegalArgumentException` constructor `explicit IllegalArgumentException(const std::string &excp) : excp_str(excp) {}` (`include/data/exceptions/IllegalArgumentException.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1189 | `IllegalArgumentException` destructor `~IllegalArgumentException() throw() {}` (`include/data/exceptions/IllegalArgumentException.h:27`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::IllegalArgumentException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1190 | `IllegalArgumentException` method `const char *what() { return excp_str.c_str(); }` (`include/data/exceptions/IllegalArgumentException.h:28`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1191 | `IllegalStateException` constructor `explicit IllegalStateException(std::string excp) : excp_str(excp) {}` (`include/data/exceptions/IllegalStateException.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1192 | `IllegalStateException` destructor `~IllegalStateException() throw() {}` (`include/data/exceptions/IllegalStateException.h:27`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::IllegalStateException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1193 | `IllegalStateException` method `const char* what() { return excp_str.c_str(); }` (`include/data/exceptions/IllegalStateException.h:28`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1194 | `IterationInterruptedException` constructor `explicit IterationInterruptedException(std::string excp) : excp_str(excp) {}` (`include/data/exceptions/InterationInterruptedException.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1195 | `IterationInterruptedException` destructor `~IterationInterruptedException() throw() {}` (`include/data/exceptions/InterationInterruptedException.h:27`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::IterationInterruptedException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1196 | `IterationInterruptedException` method `const char *what() { return excp_str.c_str(); }` (`include/data/exceptions/InterationInterruptedException.h:28`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1197 | `NotServingException` constructor `explicit NotServingException(const std::string &excp) : excp_str(excp) {}` (`include/data/exceptions/NotServingException.h:24`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1198 | `NotServingException` destructor `~NotServingException() throw() {}` (`include/data/exceptions/NotServingException.h:26`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::NotServingException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1199 | `NotServingException` method `const char *what() const noexcept { return excp_str.c_str(); }` (`include/data/exceptions/NotServingException.h:27`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1200 | `VisibilityParseException` constructor `explicit VisibilityParseException(std::string message, const std::string& expr, int32_t offset) : excp_str(std::move(message)), terms(expr), offset(offset) {}` (`include/data/exceptions/VisibilityParseException.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1201 | `VisibilityParseException` destructor `~VisibilityParseException() throw() {}` (`include/data/exceptions/VisibilityParseException.h:29`) | — | — | — | Missing Go | Assessed individually: `cclient::exceptions::VisibilityParseException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1202 | `VisibilityParseException` method `const char* what() { return excp_str.c_str(); }` (`include/data/exceptions/VisibilityParseException.h:30`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1203 | `(namespace scope)` macro constant `#define INLINE __inline__ __attribute__((always_inline))` (`include/data/extern/fastmemcpy/FastMemCpy.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1204 | `(namespace scope)` macro constant `#define INLINE __inline__` (`include/data/extern/fastmemcpy/FastMemCpy.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1205 | `(namespace scope)` macro constant `#define INLINE __forceinline` (`include/data/extern/fastmemcpy/FastMemCpy.h:29`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1206 | `(namespace scope)` macro constant `#define INLINE __inline` (`include/data/extern/fastmemcpy/FastMemCpy.h:31`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1207 | `(namespace scope)` macro constant `#define INLINE` (`include/data/extern/fastmemcpy/FastMemCpy.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1208 | `(namespace scope)` namespace-scope function `static INLINE void memcpy_avx_16(void *dst, const void *src) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:43`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1209 | `(namespace scope)` namespace-scope function `static INLINE void memcpy_avx_32(void *dst, const void *src) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:53`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1210 | `(namespace scope)` namespace-scope function `static INLINE void memcpy_avx_64(void *dst, const void *src) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:58`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1211 | `(namespace scope)` namespace-scope function `static INLINE void memcpy_avx_128(void *dst, const void *src) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:65`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1212 | `(namespace scope)` namespace-scope function `static INLINE void memcpy_avx_256(void *dst, const void *src) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:76`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1213 | `(namespace scope)` namespace-scope function `static INLINE void *memcpy_tiny(void *dst, const void *src, size_t size) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:99`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1214 | `(namespace scope)` namespace-scope function `static void* memcpy_fast(void *destination, const void *source, size_t size) {` (`include/data/extern/fastmemcpy/FastMemCpy.h:370`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1215 | `(namespace scope)` namespace-scope function `static void* memcpy_fast(void *destination, const void *source, size_t size){` (`include/data/extern/fastmemcpy/FastMemCpy.h:495`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1216 | `(namespace scope)` macro constant `#define CACHESIZELIMIT 3 * 1024 * 1024` (`include/data/extern/knnspeed/avxmem.h:36`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1217 | `(namespace scope)` namespace-scope function `int AVX_memcmp(const void *str1, const void *str2, size_t numbytes, int equality);` (`include/data/extern/knnspeed/avxmem.h:44`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1218 | `(namespace scope)` namespace-scope function `int memcmp_large(const void *str1, const void *str2, size_t numbytes);` (`include/data/extern/knnspeed/avxmem.h:52`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1219 | `(namespace scope)` namespace-scope function `int memcmp_large_eq(const void *str1, const void *str2, size_t numbytes);` (`include/data/extern/knnspeed/avxmem.h:53`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1220 | `(namespace scope)` namespace-scope function `int memcmp_large_a(const void *str1, const void *str2, size_t numbytes);` (`include/data/extern/knnspeed/avxmem.h:55`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1221 | `(namespace scope)` namespace-scope function `int memcmp_large_eq_a(const void *str1, const void *str2, size_t numbytes);` (`include/data/extern/knnspeed/avxmem.h:56`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1222 | `(namespace scope)` namespace-scope function `int memcmp(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:59`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1223 | `(namespace scope)` namespace-scope function `int memcmp_eq(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:60`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1224 | `(namespace scope)` namespace-scope function `int memcmp_16bit(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:61`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1225 | `(namespace scope)` namespace-scope function `int memcmp_16bit_eq(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:62`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1226 | `(namespace scope)` namespace-scope function `int memcmp_32bit(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:63`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1227 | `(namespace scope)` namespace-scope function `int memcmp_32bit_eq(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:64`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1228 | `(namespace scope)` namespace-scope function `int memcmp_64bit(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:65`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1229 | `(namespace scope)` namespace-scope function `int memcmp_64bit_eq(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:66`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1230 | `(namespace scope)` namespace-scope function `int memcmp_128bit_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:70`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1231 | `(namespace scope)` namespace-scope function `int memcmp_128bit_eq_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:71`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1232 | `(namespace scope)` namespace-scope function `int memcmp_128bit_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:74`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1233 | `(namespace scope)` namespace-scope function `int memcmp_128bit_eq_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:75`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1234 | `(namespace scope)` namespace-scope function `int memcmp_256bit_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:80`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1235 | `(namespace scope)` namespace-scope function `int memcmp_256bit_eq_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:81`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1236 | `(namespace scope)` namespace-scope function `int memcmp_256bit_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:84`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1237 | `(namespace scope)` namespace-scope function `int memcmp_256bit_eq_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:85`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1238 | `(namespace scope)` namespace-scope function `int memcmp_512bit_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:91`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1239 | `(namespace scope)` namespace-scope function `int memcmp_512bit_eq_u(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:92`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1240 | `(namespace scope)` namespace-scope function `int memcmp_512bit_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:95`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1241 | `(namespace scope)` namespace-scope function `int memcmp_512bit_eq_a(const void *str1, const void *str2, size_t count);` (`include/data/extern/knnspeed/avxmem.h:96`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. client-side data construct. |
| SB-CXX-1242 | `DeletingMultiIterator` constructor `explicit DeletingMultiIterator(const std::vector<std::shared_ptr<cclient::data::streams::KeyValueIterator>> &itrs, bool propogate, const std::shared_ptr<cclient::data::Range> &range = nullptr) : HeapIterator(itrs, range), propogateDeletes(propogate) {` (`include/data/iterators/DeletingMultiIterator.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1243 | `DeletingMultiIterator` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) override {` (`include/data/iterators/DeletingMultiIterator.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1244 | `DeletingMultiIterator` method `virtual void next() override{` (`include/data/iterators/DeletingMultiIterator.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1245 | `DeletingMultiIterator` constructor `DeletingMultiIterator(const DeletingMultiIterator &other) = default;` (`include/data/iterators/DeletingMultiIterator.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1246 | `DeletingMultiIterator` constructor `DeletingMultiIterator(DeletingMultiIterator &&other) = default;` (`include/data/iterators/DeletingMultiIterator.h:74`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1247 | `DeletingMultiIterator` operator `DeletingMultiIterator& operator=(DeletingMultiIterator &&other) = default;` (`include/data/iterators/DeletingMultiIterator.h:76`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1248 | `DeletingMultiIterator` operator `DeletingMultiIterator& operator=(const DeletingMultiIterator &other) = default;` (`include/data/iterators/DeletingMultiIterator.h:78`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1249 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/iterators/HeapIterator.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1250 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/iterators/HeapIterator.h:31`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1251 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/iterators/HeapIterator.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1252 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/iterators/HeapIterator.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1253 | `KeyValueIteratorComparator` operator `bool operator()(const std::shared_ptr<cclient::data::streams::KeyValueIterator> &lhs, const std::shared_ptr<cclient::data::streams::KeyValueIterator> &rhs) const {` (`include/data/iterators/HeapIterator.h:42`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1254 | `HeapIterator` method `virtual bool hasNext() override;` (`include/data/iterators/HeapIterator.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1255 | `HeapIterator` constructor `HeapIterator(){}` (`include/data/iterators/HeapIterator.h:68`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1256 | `HeapIterator` constructor `explicit HeapIterator(const std::vector<std::shared_ptr<cclient::data::streams::KeyValueIterator>> &itrs, const std::shared_ptr<cclient::data::Range> &range = nullptr) {` (`include/data/iterators/HeapIterator.h:70`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1257 | `HeapIterator` constructor `HeapIterator(const HeapIterator &other) = default;` (`include/data/iterators/HeapIterator.h:74`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1258 | `HeapIterator` constructor `HeapIterator(HeapIterator &&other) = default;` (`include/data/iterators/HeapIterator.h:76`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1259 | `HeapIterator` operator `HeapIterator& operator=(HeapIterator &&other) = default;` (`include/data/iterators/HeapIterator.h:78`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1260 | `HeapIterator` operator `HeapIterator& operator=(const HeapIterator &other) = default;` (`include/data/iterators/HeapIterator.h:80`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1261 | `HeapIterator` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) override;` (`include/data/iterators/HeapIterator.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1262 | `HeapIterator` method `virtual std::shared_ptr<Key> getTopKey() override ;` (`include/data/iterators/HeapIterator.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1263 | `HeapIterator` method `virtual std::shared_ptr<Value> getTopValue() override ;` (`include/data/iterators/HeapIterator.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1264 | `HeapIterator` method `virtual std::shared_ptr<KeyValue> getTop() override;` (`include/data/iterators/HeapIterator.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1265 | `HeapIterator` operator `virtual std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>> operator*() override;` (`include/data/iterators/HeapIterator.h:90`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1266 | `HeapIterator` method `virtual void next() override;` (`include/data/iterators/HeapIterator.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1267 | `HeapIterator` operator `virtual DataStream* operator++() override {` (`include/data/iterators/HeapIterator.h:96`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1268 | `HeapIterator` operator `virtual DataStream* operator++(int t) override {` (`include/data/iterators/HeapIterator.h:101`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1269 | `HeapIterator` method `virtual uint64_t getEntriesFiltered() override;` (`include/data/iterators/HeapIterator.h:108`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1270 | `HeapIterator` method `virtual void close() override;` (`include/data/iterators/HeapIterator.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1271 | `HeapIterator` method `void clear();` (`include/data/iterators/HeapIterator.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1272 | `HeapIterator` method `void addSource(const std::shared_ptr<cclient::data::streams::KeyValueIterator> &iter);` (`include/data/iterators/HeapIterator.h:114`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1273 | `MultiIterator` constructor `explicit MultiIterator(const std::vector<std::shared_ptr<cclient::data::streams::KeyValueIterator>> &itrs, const std::shared_ptr<cclient::data::Range> &range = nullptr) : HeapIterator(itrs, range) {` (`include/data/iterators/MultiIterator.h:33`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1274 | `MultiIterator` constructor `MultiIterator(const MultiIterator &other) = default;` (`include/data/iterators/MultiIterator.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1275 | `MultiIterator` constructor `MultiIterator(MultiIterator &&other) = default;` (`include/data/iterators/MultiIterator.h:40`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1276 | `MultiIterator` operator `MultiIterator& operator=(MultiIterator &&other) = default;` (`include/data/iterators/MultiIterator.h:42`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1277 | `MultiIterator` operator `MultiIterator& operator=(const MultiIterator &other) = default;` (`include/data/iterators/MultiIterator.h:44`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1278 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/iterators/VersioningIterator.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1279 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/iterators/VersioningIterator.h:31`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1280 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/iterators/VersioningIterator.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1281 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/iterators/VersioningIterator.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1282 | `VersioningIterator` constructor `explicit VersioningIterator(const std::vector<std::shared_ptr<cclient::data::streams::KeyValueIterator>> &itrs, const std::shared_ptr<cclient::data::Range> &range = nullptr) : HeapIterator(itrs, range) {` (`include/data/iterators/VersioningIterator.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1283 | `VersioningIterator` method `virtual void relocate(const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) override {` (`include/data/iterators/VersioningIterator.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1284 | `VersioningIterator` constructor `VersioningIterator(const VersioningIterator &other) = default;` (`include/data/iterators/VersioningIterator.h:56`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1285 | `VersioningIterator` constructor `VersioningIterator(VersioningIterator &&other) = default;` (`include/data/iterators/VersioningIterator.h:58`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1286 | `VersioningIterator` operator `VersioningIterator& operator=(VersioningIterator &&other) = default;` (`include/data/iterators/VersioningIterator.h:60`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1287 | `VersioningIterator` operator `VersioningIterator& operator=(const VersioningIterator &other) = default;` (`include/data/iterators/VersioningIterator.h:62`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1288 | `VersioningIterator` method `virtual std::shared_ptr<Key> getTopKey() override;` (`include/data/iterators/VersioningIterator.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1289 | `VersioningIterator` method `virtual std::shared_ptr<Value> getTopValue() override;` (`include/data/iterators/VersioningIterator.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1290 | `VersioningIterator` method `virtual bool hasNext() override;` (`include/data/iterators/VersioningIterator.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1291 | `VersioningIterator` method `virtual std::shared_ptr<KeyValue> getTop() override;` (`include/data/iterators/VersioningIterator.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1292 | `VersioningIterator` method `virtual void next() override;` (`include/data/iterators/VersioningIterator.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1293 | `FileSystem` class (no public members) `class FileSystem {` (`include/data/services/FileSystem.h:22`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. client-side data construct. |
| SB-CXX-1294 | `FileIterator` constructor `FileIterator() {}` (`include/data/streaming/accumulo/FileIterator.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1295 | `FileIterator` method `virtual std::shared_ptr<Key> getFirstKey() = 0;` (`include/data/streaming/accumulo/FileIterator.h:26`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1296 | `FileIterator` method `virtual std::shared_ptr<Key> getLastKey() = 0;` (`include/data/streaming/accumulo/FileIterator.h:27`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1297 | `KeyValueIterator` constructor `explicit KeyValueIterator(KeyValueIterator* source, StreamConfiguration* configuration) {` (`include/data/streaming/accumulo/KeyValueIterator.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1298 | `KeyValueIterator` constructor `KeyValueIterator() {}` (`include/data/streaming/accumulo/KeyValueIterator.h:35`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-016](#sec-15) already carries this obligation — `cclient::data::streams::KeyValueIterator::KeyValueIterator` is the overload bound at `pysharkbite.cpp:507` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1299 | `KeyValueIterator` destructor `virtual ~KeyValueIterator() {}` (`include/data/streaming/accumulo/KeyValueIterator.h:37`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::streams::KeyValueIterator` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1300 | `KeyValueIterator` method `virtual bool hasNext() override { return false; }` (`include/data/streaming/accumulo/KeyValueIterator.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-029](#sec-15) already carries this obligation — `cclient::data::streams::KeyValueIterator::hasNext` is the overload bound at `pysharkbite.cpp:509` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1301 | `KeyValueIterator` method `virtual void relocate( const std::shared_ptr<cclient::data::streams::StreamRelocation>& location) override {}` (`include/data/streaming/accumulo/KeyValueIterator.h:41`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-017](#sec-15) already carries this obligation — `cclient::data::streams::KeyValueIterator::relocate` is the overload bound at `pysharkbite.cpp:508` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1302 | `KeyValueIterator` method `virtual DataStream* begin() { return this; }` (`include/data/streaming/accumulo/KeyValueIterator.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1303 | `KeyValueIterator` method `virtual DataStream* end() { return this; }` (`include/data/streaming/accumulo/KeyValueIterator.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1304 | `KeyValueIterator` method `virtual std::shared_ptr<Key> getTopKey() { return nullptr; }` (`include/data/streaming/accumulo/KeyValueIterator.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-030](#sec-15) already carries this obligation — `cclient::data::streams::KeyValueIterator::getTopKey` is the overload bound at `pysharkbite.cpp:510` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1305 | `KeyValueIterator` method `virtual std::shared_ptr<Value> getTopValue() { return nullptr; }` (`include/data/streaming/accumulo/KeyValueIterator.h:51`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-031](#sec-15) already carries this obligation — `cclient::data::streams::KeyValueIterator::getTopValue` is the overload bound at `pysharkbite.cpp:511` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1306 | `KeyValueIterator` method `virtual std::shared_ptr<KeyValue> getTop() {` (`include/data/streaming/accumulo/KeyValueIterator.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1307 | `KeyValueIterator` operator `virtual std::pair<std::shared_ptr<Key>, std::shared_ptr<Value>> operator*() override {` (`include/data/streaming/accumulo/KeyValueIterator.h:57`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1308 | `KeyValueIterator` method `virtual void next() override {}` (`include/data/streaming/accumulo/KeyValueIterator.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1309 | `KeyValueIterator` method `virtual uint64_t getEntriesFiltered() { return 0; }` (`include/data/streaming/accumulo/KeyValueIterator.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1310 | `KeyValueIterator` operator `virtual DataStream* operator++() { return this; }` (`include/data/streaming/accumulo/KeyValueIterator.h:66`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1311 | `KeyValueIterator` operator `virtual DataStream* operator++(int t) { return this; }` (`include/data/streaming/accumulo/KeyValueIterator.h:68`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1312 | `KeyValueIterator` method `virtual void close() {}` (`include/data/streaming/accumulo/KeyValueIterator.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1313 | `StreamSeekable` constructor `explicit StreamSeekable(Range &range, uint32_t threads = 1, uint32_t keysPerThread = 1000) : range(std::move(range)), inclusive(false), threads(threads) {}` (`include/data/streaming/accumulo/StreamSeekable.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1314 | `StreamSeekable` constructor `explicit StreamSeekable(Range &range, std::vector<std::string> &columnFamilies, bool inclusive, uint32_t threads = 1, uint32_t keysPerThread = 1000) : range(std::move(range)), columnFamilies(columnFamilies), inclusive(inclusive), threads(threads), keysPerThread(keysPerThread) {}` (`include/data/streaming/accumulo/StreamSeekable.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1315 | `StreamSeekable` constructor `explicit StreamSeekable(Range &range, std::vector<std::string> &columnFamilies, cclient::data::security::Authorizations &in_auths, bool inclusive, uint32_t threads = 1, uint32_t keysPerThread = 1000) : range(std::move(range)), columnFamilies(columnFamilies), inclusive(inclusive), auths(std::move(in_auths)), threads(threads), keysPerThread(keysPerThread) {}` (`include/data/streaming/accumulo/StreamSeekable.h:52`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1316 | `StreamSeekable` constructor `explicit StreamSeekable(Range *rng, std::vector<std::string> &columnFamilies, cclient::data::security::Authorizations *in_auths, bool inclusive, uint32_t threads = 1, uint32_t keysPerThread = 1000) : range(rng->getStartKey(), rng->getStartKeyInclusive(), rng->getStopKey(), rng->getStopKeyInclusive()), columnFamilies(columnFamilies), inclusive(inclusive), auths(in_auths->getAuthorizations()), threads(threads), keysPerThread(keysPerThread) {}` (`include/data/streaming/accumulo/StreamSeekable.h:64`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1317 | `StreamSeekable` method `virtual cclient::data::security::Authorizations *getAuths() override {` (`include/data/streaming/accumulo/StreamSeekable.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1318 | `StreamSeekable` method `virtual uint32_t getDesiredThreads() const override { return threads; }` (`include/data/streaming/accumulo/StreamSeekable.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1319 | `StreamSeekable` method `void setThreads(uint32_t threads) { this->threads = threads; }` (`include/data/streaming/accumulo/StreamSeekable.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1320 | `StreamSeekable` method `virtual uint32_t getKeysCachedPerThread() const override {` (`include/data/streaming/accumulo/StreamSeekable.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1321 | `StreamSeekable` method `virtual Range *getRange() override { return &range; }` (`include/data/streaming/accumulo/StreamSeekable.h:88`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-020](#sec-15) already carries this obligation — `cclient::data::streams::StreamSeekable::getRange` is the overload bound at `pysharkbite.cpp:583` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1322 | `StreamSeekable` method `virtual std::vector<std::string> *getColumnFamilies() override {` (`include/data/streaming/accumulo/StreamSeekable.h:90`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-034](#sec-15) already carries this obligation — `cclient::data::streams::StreamSeekable::getColumnFamilies` is the overload bound at `pysharkbite.cpp:584` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1323 | `StreamSeekable` method `virtual bool isInclusive() override { return inclusive; }` (`include/data/streaming/accumulo/StreamSeekable.h:94`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-RFILE-035](#sec-15) already carries this obligation — `cclient::data::streams::StreamSeekable::isInclusive` is the overload bound at `pysharkbite.cpp:585` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1324 | `DelegatedStreamSeekable` constructor `explicit DelegatedStreamSeekable(std::shared_ptr<Range> &rng, const std::shared_ptr<StreamRelocation> &ssk) : range(rng), seekable(ssk) {}` (`include/data/streaming/accumulo/StreamSeekable.h:103`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1325 | `DelegatedStreamSeekable` method `virtual cclient::data::security::Authorizations *getAuths() override {` (`include/data/streaming/accumulo/StreamSeekable.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1326 | `DelegatedStreamSeekable` method `virtual uint32_t getDesiredThreads() const override {` (`include/data/streaming/accumulo/StreamSeekable.h:111`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1327 | `DelegatedStreamSeekable` method `virtual uint32_t getKeysCachedPerThread() const override {` (`include/data/streaming/accumulo/StreamSeekable.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1328 | `DelegatedStreamSeekable` method `virtual Range *getRange() override { return range.get(); }` (`include/data/streaming/accumulo/StreamSeekable.h:119`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1329 | `DelegatedStreamSeekable` method `virtual std::vector<std::string> *getColumnFamilies() override {` (`include/data/streaming/accumulo/StreamSeekable.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1330 | `DelegatedStreamSeekable` method `virtual bool isInclusive() override { return seekable->isInclusive(); }` (`include/data/streaming/accumulo/StreamSeekable.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1331 | `ByteOutputStream` constructor `ByteOutputStream(size_t initial_size, OutputStream *out_stream = NULL);` (`include/data/streaming/ByteOutputStream.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1332 | `ByteOutputStream` destructor `virtual ~ByteOutputStream();` (`include/data/streaming/ByteOutputStream.h:40`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::ByteOutputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1333 | `ByteOutputStream` method `void flush();` (`include/data/streaming/ByteOutputStream.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1334 | `ByteOutputStream` method `virtual uint64_t getPos();` (`include/data/streaming/ByteOutputStream.h:42`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1335 | `ByteOutputStream` method `void getByteArray(char *inArray, size_t inArraySize);` (`include/data/streaming/ByteOutputStream.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1336 | `ByteOutputStream` method `char *getByteArray();` (`include/data/streaming/ByteOutputStream.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1337 | `ByteOutputStream` method `char *getByteArrayAtPosition();` (`include/data/streaming/ByteOutputStream.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1338 | `ByteOutputStream` method `size_t getSize();` (`include/data/streaming/ByteOutputStream.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1339 | `ByteOutputStream` method `void setOutputStreamRef(OutputStream *out_stream);` (`include/data/streaming/ByteOutputStream.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1340 | `ByteOutputStream` method `virtual uint64_t write(const char *bytes, long cnt);` (`include/data/streaming/ByteOutputStream.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1341 | `ByteOutputStream` method `virtual uint64_t writeByte(int byte);` (`include/data/streaming/ByteOutputStream.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1342 | `ByteOutputStream` method `virtual uint64_t writeString(std::string s);` (`include/data/streaming/ByteOutputStream.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1343 | `ByteOutputStream` method `virtual uint64_t write(const uint8_t *bytes, long cnt);` (`include/data/streaming/ByteOutputStream.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1344 | `ByteOutputStream` method `virtual uint64_t writeBytes(const char *bytes, size_t cnt) {` (`include/data/streaming/ByteOutputStream.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1345 | `ByteOutputStream` method `virtual uint64_t writeBytes(const uint8_t *bytes, size_t cnt);` (`include/data/streaming/ByteOutputStream.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1346 | `ByteOutputStream` method `virtual uint64_t writeByte(const uint8_t byte);` (`include/data/streaming/ByteOutputStream.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1347 | `ByteOutputStream` method `virtual uint64_t writeShort(const short shortVal);` (`include/data/streaming/ByteOutputStream.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1348 | `ByteOutputStream` method `virtual uint64_t writeInt(const int intVal);` (`include/data/streaming/ByteOutputStream.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1349 | `ByteOutputStream` method `virtual uint64_t writeLong(const uint64_t val);` (`include/data/streaming/ByteOutputStream.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1350 | `ByteOutputStream` method `virtual uint64_t writeBoolean(const bool val);` (`include/data/streaming/ByteOutputStream.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1351 | `ByteOutputStream` method `virtual uint64_t writeEncodedLong(const int64_t n = 0) {` (`include/data/streaming/ByteOutputStream.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1352 | `ByteOutputStream` method `void ensure(size_t size, size_t ptr = 0);` (`include/data/streaming/ByteOutputStream.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1353 | `BigEndianByteStream` constructor `BigEndianByteStream(size_t initial_size, OutputStream *out_stream = NULL)` (`include/data/streaming/ByteOutputStream.h:95`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1354 | `BigEndianByteStream` method `uint64_t writeShort(short shortVal) {` (`include/data/streaming/ByteOutputStream.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1355 | `BigEndianByteStream` method `uint64_t writeInt(int intVal) {` (`include/data/streaming/ByteOutputStream.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1356 | `BigEndianByteStream` method `uint64_t writeLong(uint64_t val) {` (`include/data/streaming/ByteOutputStream.h:106`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1357 | `BigEndianByteStream` method `virtual uint64_t writeVLong(const int64_t n = 0) {` (`include/data/streaming/ByteOutputStream.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1358 | `BigEndianByteStream` method `virtual uint64_t writeEncodedLong(const int64_t n = 0) {` (`include/data/streaming/ByteOutputStream.h:114`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1359 | `BigEndianByteStream` method `virtual uint64_t writeBytes(const char *bytes, size_t cnt) {` (`include/data/streaming/ByteOutputStream.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1360 | `DataOutputStream` constructor `explicit DataOutputStream(OutputStream *out_stream) : OutputStream(out_stream), output_stream_ref(out_stream) {}` (`include/data/streaming/DataOutputStream.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1361 | `DataOutputStream` destructor `virtual ~DataOutputStream() {}` (`include/data/streaming/DataOutputStream.h:35`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::DataOutputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1362 | `DataOutputStream` method `virtual uint64_t getPos() { return output_stream_ref->getPos(); }` (`include/data/streaming/DataOutputStream.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1363 | `DataOutputStream` method `uint64_t write(const char *bytes, long cnt) {` (`include/data/streaming/DataOutputStream.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1364 | `DataOutputStream` method `virtual uint64_t writeByte(int byte) {` (`include/data/streaming/DataOutputStream.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1365 | `DataOutputStream` method `virtual uint64_t writeString(std::string s) {` (`include/data/streaming/DataOutputStream.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1366 | `DataOutputStream` method `virtual uint64_t write(const uint8_t *bytes, long cnt) {` (`include/data/streaming/DataOutputStream.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1367 | `DataOutputStream` method `virtual uint64_t writeBytes(const char *bytes, size_t cnt) {` (`include/data/streaming/DataOutputStream.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1368 | `DataOutputStream` method `virtual uint64_t writeBytes(const uint8_t *bytes, size_t cnt) {` (`include/data/streaming/DataOutputStream.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1369 | `DataOutputStream` method `virtual uint64_t writeByte(const uint8_t byte) {` (`include/data/streaming/DataOutputStream.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1370 | `DataOutputStream` method `virtual uint64_t writeShort(const short shortVal) {` (`include/data/streaming/DataOutputStream.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1371 | `DataOutputStream` method `virtual uint64_t writeInt(const int intVal) {` (`include/data/streaming/DataOutputStream.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1372 | `DataOutputStream` method `virtual uint64_t writeLong(const uint64_t val) {` (`include/data/streaming/DataOutputStream.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1373 | `DataOutputStream` method `virtual uint64_t writeBoolean(const bool val) {` (`include/data/streaming/DataOutputStream.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1374 | `DataOutputStream` method `virtual uint64_t writeEncodedLong(const int64_t n) {` (`include/data/streaming/DataOutputStream.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1375 | `DataOutputStream` method `virtual uint64_t writeVLong(const int64_t n) {` (`include/data/streaming/DataOutputStream.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1376 | `DataStream` constructor `DataStream() {}` (`include/data/streaming/DataStream.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1377 | `DataStream` method `void setConfiguration(StreamConfiguration *configuration) {` (`include/data/streaming/DataStream.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1378 | `DataStream` destructor `virtual ~DataStream() {}` (`include/data/streaming/DataStream.h:37`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::DataStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1379 | `DataStream` method `virtual bool hasNext() = 0;` (`include/data/streaming/DataStream.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1380 | `DataStream` method `virtual void relocate( const std::shared_ptr<cclient::data::streams::StreamRelocation> &location) = 0;` (`include/data/streaming/DataStream.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1381 | `DataStream` method `virtual DataStream *begin() = 0;` (`include/data/streaming/DataStream.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1382 | `DataStream` method `virtual DataStream *end() = 0;` (`include/data/streaming/DataStream.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1383 | `DataStream` operator `virtual T operator*() = 0;` (`include/data/streaming/DataStream.h:49`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1384 | `DataStream` operator `virtual DataStream *operator++() = 0;` (`include/data/streaming/DataStream.h:51`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1385 | `DataStream` method `virtual void next() = 0;` (`include/data/streaming/DataStream.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1386 | `DataStream` operator `virtual DataStream *operator++(int t) = 0;` (`include/data/streaming/DataStream.h:55`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1387 | `EndianTranslationStream` constructor `explicit EndianTranslationStream(OutputStream *os) : HadoopDataOutputStream(os) {}` (`include/data/streaming/EndianTranslation.h:27`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1388 | `EndianTranslationStream` constructor `explicit EndianTranslationStream(std::unique_ptr<OutputStream> os) : HadoopDataOutputStream(os.get()) {` (`include/data/streaming/EndianTranslation.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1389 | `EndianTranslationStream` constructor `EndianTranslationStream(EndianTranslationStream &&) = delete;` (`include/data/streaming/EndianTranslation.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1390 | `EndianTranslationStream` operator `EndianTranslationStream &operator=(EndianTranslationStream &&) = delete;` (`include/data/streaming/EndianTranslation.h:37`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1391 | `EndianTranslationStream` destructor `virtual ~EndianTranslationStream();` (`include/data/streaming/EndianTranslation.h:39`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::EndianTranslationStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1392 | `EndianTranslationStream` method `uint64_t htonlw(uint64_t value);` (`include/data/streaming/EndianTranslation.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1393 | `EndianTranslationStream` method `uint64_t writeShort(short shortVal) override;` (`include/data/streaming/EndianTranslation.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1394 | `EndianTranslationStream` method `uint64_t writeInt(int intVal);` (`include/data/streaming/EndianTranslation.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1395 | `EndianTranslationStream` method `uint64_t writeLong(uint64_t val);` (`include/data/streaming/EndianTranslation.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1396 | `EndianTranslationStream` method `virtual uint64_t writeEncodedLong(const int64_t n);` (`include/data/streaming/EndianTranslation.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1397 | `FileStream` public data member `std::unique_ptr<cclient::data::streams::InputStream> stream;` (`include/data/streaming/FileStream.h:13`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1398 | `FileStream` public data member `size_t filesize;` (`include/data/streaming/FileStream.h:14`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. client-side data construct. |
| SB-CXX-1399 | `FileStream` constructor `explicit FileStream( std::unique_ptr<cclient::data::streams::InputStream> stream, size_t filesize) : stream(std::move(stream)), filesize(filesize) {}` (`include/data/streaming/FileStream.h:16`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1400 | `FileStream` constructor `FileStream(const FileStream &) = delete;` (`include/data/streaming/FileStream.h:21`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1401 | `FileStream` constructor `FileStream(FileStream &&o) {` (`include/data/streaming/FileStream.h:22`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1402 | `FileStream` operator `FileStream &operator=(FileStream &&o) {` (`include/data/streaming/FileStream.h:27`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1403 | `HadoopDataOutputStream` constructor `explicit HadoopDataOutputStream(OutputStream *out_stream) : DataOutputStream(out_stream), output_stream_ref(out_stream) {}` (`include/data/streaming/HdfsOutputStream.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1404 | `HadoopDataOutputStream` destructor `virtual ~HadoopDataOutputStream();` (`include/data/streaming/HdfsOutputStream.h:35`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::HadoopDataOutputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1405 | `HadoopDataOutputStream` method `virtual uint64_t getPos();` (`include/data/streaming/HdfsOutputStream.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1406 | `HadoopDataOutputStream` method `uint64_t write(const char *bytes, long cnt);` (`include/data/streaming/HdfsOutputStream.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1407 | `HadoopDataOutputStream` method `virtual uint64_t writeByte(int byte);` (`include/data/streaming/HdfsOutputStream.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1408 | `HadoopDataOutputStream` method `virtual uint64_t writeString(std::string s);` (`include/data/streaming/HdfsOutputStream.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1409 | `HadoopDataOutputStream` method `virtual uint64_t write(const uint8_t *bytes, long cnt);` (`include/data/streaming/HdfsOutputStream.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1410 | `HadoopDataOutputStream` method `virtual uint64_t writeBytes(const uint8_t *bytes, size_t cnt);` (`include/data/streaming/HdfsOutputStream.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1411 | `HadoopDataOutputStream` method `virtual uint64_t writeByte(const uint8_t byte);` (`include/data/streaming/HdfsOutputStream.h:42`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1412 | `HadoopDataOutputStream` method `virtual uint64_t writeShort(const short shortVal);` (`include/data/streaming/HdfsOutputStream.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1413 | `HadoopDataOutputStream` method `virtual uint64_t writeInt(const int intVal);` (`include/data/streaming/HdfsOutputStream.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1414 | `HadoopDataOutputStream` method `virtual uint64_t writeLong(const uint64_t val);` (`include/data/streaming/HdfsOutputStream.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1415 | `HadoopDataOutputStream` method `virtual uint64_t writeBoolean(const bool val);` (`include/data/streaming/HdfsOutputStream.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1416 | `HadoopDataOutputStream` method `virtual uint64_t writeHadoopLong(const int64_t n);` (`include/data/streaming/HdfsOutputStream.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1417 | `HdfsOutputStream` constructor `explicit HdfsOutputStream(const std::string &path);` (`include/data/streaming/HdfsOutputStream.h:56`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1418 | `HdfsOutputStream` constructor `explicit HdfsOutputStream(const std::shared_ptr<hdfs::HdfsLink> &hdfs, const std::string &path);` (`include/data/streaming/HdfsOutputStream.h:58`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1419 | `HdfsOutputStream` destructor `~HdfsOutputStream() {` (`include/data/streaming/HdfsOutputStream.h:61`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::streams::HdfsOutputStream` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1420 | `HdfsOutputStream` method `uint64_t htonlw(uint64_t value);` (`include/data/streaming/HdfsOutputStream.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1421 | `HdfsOutputStream` method `virtual uint64_t writeString(std::string s) override;` (`include/data/streaming/HdfsOutputStream.h:79`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-021](#sec-16) already carries this obligation — `cclient::data::streams::HdfsOutputStream::writeString` is the overload bound at `pysharkbite.cpp:556` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1422 | `HdfsOutputStream` method `virtual uint64_t writeShort(short shortVal) override;` (`include/data/streaming/HdfsOutputStream.h:81`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-011](#sec-16) already carries this obligation — `cclient::data::streams::HdfsOutputStream::writeShort` is the overload bound at `pysharkbite.cpp:553` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1423 | `HdfsOutputStream` method `virtual uint64_t writeInt(int intVal) override;` (`include/data/streaming/HdfsOutputStream.h:83`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-019](#sec-16) already carries this obligation — `cclient::data::streams::HdfsOutputStream::writeInt` is the overload bound at `pysharkbite.cpp:557` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1424 | `HdfsOutputStream` method `virtual uint64_t writeLong(uint64_t val) override;` (`include/data/streaming/HdfsOutputStream.h:85`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-020](#sec-16) already carries this obligation — `cclient::data::streams::HdfsOutputStream::writeLong` is the overload bound at `pysharkbite.cpp:554` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1425 | `HdfsOutputStream` method `virtual uint64_t writeByte(int byte) override;` (`include/data/streaming/HdfsOutputStream.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1426 | `HdfsOutputStream` method `virtual uint64_t writeBoolean(const bool val) override;` (`include/data/streaming/HdfsOutputStream.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1427 | `HdfsOutputStream` method `virtual uint64_t writeBytes(const char *bytes, size_t cnt) override;` (`include/data/streaming/HdfsOutputStream.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1428 | `HdfsOutputStream` method `virtual uint64_t writeBytes(const uint8_t *bytes, size_t cnt) override;` (`include/data/streaming/HdfsOutputStream.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1429 | `HdfsOutputStream` method `virtual uint64_t write(const uint8_t *bytes, long cnt) override;` (`include/data/streaming/HdfsOutputStream.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1430 | `HdfsOutputStream` method `virtual uint64_t write(const char *bytes, long cnt) override;` (`include/data/streaming/HdfsOutputStream.h:97`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-022](#sec-16) already carries this obligation — `cclient::data::streams::HdfsOutputStream::write` is the overload bound at `pysharkbite.cpp:555` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1431 | `HdfsOutputStream` method `virtual uint64_t writeEncodedLong(const int64_t n) override;` (`include/data/streaming/HdfsOutputStream.h:99`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1432 | `HdfsOutputStream` method `virtual uint64_t writeHadoopLong(const int64_t n) override;` (`include/data/streaming/HdfsOutputStream.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1433 | `HdfsOutputStream` method `virtual uint64_t writeVLong(const int64_t val) override;` (`include/data/streaming/HdfsOutputStream.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1434 | `ByteInputStream` constructor `ByteInputStream(InputStream *out_stream)` (`include/data/streaming/input/ByteInputStream.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1435 | `ByteInputStream` constructor `ByteInputStream(char *byteArray, size_t len)` (`include/data/streaming/input/ByteInputStream.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1436 | `ByteInputStream` constructor `ByteInputStream() : input_stream_ref(NULL), allocated(false) {}` (`include/data/streaming/input/ByteInputStream.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1437 | `ByteInputStream` constructor `ByteInputStream(char *byteArray, size_t len, bool allocate)` (`include/data/streaming/input/ByteInputStream.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1438 | `ByteInputStream` method `virtual uint64_t getPos() { return offset; }` (`include/data/streaming/input/ByteInputStream.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1439 | `ByteInputStream` method `void setArray(char *byteArray, size_t len, bool allocate) {` (`include/data/streaming/input/ByteInputStream.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1440 | `ByteInputStream` destructor `virtual ~ByteInputStream() {` (`include/data/streaming/input/ByteInputStream.h:78`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::ByteInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1441 | `ByteInputStream` method `virtual InputStream *seek(uint64_t pos) {` (`include/data/streaming/input/ByteInputStream.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1442 | `ByteInputStream` method `virtual INLINE uint64_t readBytes(uint8_t *bytes, size_t cnt) {` (`include/data/streaming/input/ByteInputStream.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1443 | `ByteInputStream` method `virtual INLINE uint64_t readBytes(char *bytes, size_t cnt) {` (`include/data/streaming/input/ByteInputStream.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1444 | `ByteInputStream` method `virtual uint64_t bytesAvailable() { return (length - offset); }` (`include/data/streaming/input/ByteInputStream.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1445 | `HadoopDataInputStream` constructor `HadoopDataInputStream(InputStream *out_stream)` (`include/data/streaming/input/HdfsInputStream.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1446 | `HadoopDataInputStream` destructor `virtual ~HadoopDataInputStream() {}` (`include/data/streaming/input/HdfsInputStream.h:38`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::HadoopDataInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1447 | `HadoopDataInputStream` method `virtual InputStream *seek(uint64_t pos) {` (`include/data/streaming/input/HdfsInputStream.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1448 | `HadoopDataInputStream` method `virtual uint64_t readBytes(const uint8_t *bytes, size_t cnt) {` (`include/data/streaming/input/HdfsInputStream.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1449 | `HdfsInputStream` constructor `explicit HdfsInputStream(const std::string &path);` (`include/data/streaming/input/HdfsInputStream.h:57`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1450 | `HdfsInputStream` constructor `explicit HdfsInputStream(const std::shared_ptr<hdfs::HdfsLink> &hdfs, const std::string &path);` (`include/data/streaming/input/HdfsInputStream.h:59`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1451 | `HdfsInputStream` destructor `virtual ~HdfsInputStream() {` (`include/data/streaming/input/HdfsInputStream.h:62`) | — | — | — | Behavior mismatch | Assessed individually: `cclient::data::streams::HdfsInputStream` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1452 | `HdfsInputStream` method `virtual InputStream *seek(uint64_t pos) {` (`include/data/streaming/input/HdfsInputStream.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1453 | `HdfsInputStream` method `virtual uint64_t readBytes(uint8_t *bytes, size_t cnt) override {` (`include/data/streaming/input/HdfsInputStream.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1454 | `HdfsInputStream` method `virtual uint64_t readBytes(char *bytes, size_t cnt) override {` (`include/data/streaming/input/HdfsInputStream.h:76`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-HDFS-026](#sec-16) already carries this obligation — `cclient::data::streams::HdfsInputStream::readBytes` is the overload bound at `pysharkbite.cpp:562` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1455 | `HdfsInputStream` method `size_t getFileSize() const { return size; }` (`include/data/streaming/input/HdfsInputStream.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1456 | `(namespace scope)` macro constant `#define _MAP_POPULATE_AVAILABLE` (`include/data/streaming/input/InputStream.h:38`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1457 | `(namespace scope)` macro constant `#define MMAP_FLAGS (MAP_PRIVATE &#124; MAP_POPULATE)` (`include/data/streaming/input/InputStream.h:43`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1458 | `(namespace scope)` macro constant `#define MMAP_FLAGS MAP_PRIVATE` (`include/data/streaming/input/InputStream.h:45`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1459 | `InputStream` constructor `InputStream(std::istream *ptr, uint64_t pos)` (`include/data/streaming/input/InputStream.h:59`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1460 | `InputStream` constructor `InputStream(std::unique_ptr<std::istream> ptr, uint64_t pos)` (`include/data/streaming/input/InputStream.h:65`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1461 | `InputStream` constructor `InputStream() : istream_ref(NULL), position(0), own_istream(false) {}` (`include/data/streaming/input/InputStream.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1462 | `InputStream` constructor `InputStream(InputStream &&) = default;` (`include/data/streaming/input/InputStream.h:74`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1463 | `InputStream` operator `InputStream &operator=(InputStream &&) = default;` (`include/data/streaming/input/InputStream.h:76`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. client-side data construct. |
| SB-CXX-1464 | `InputStream` method `virtual void close() {}` (`include/data/streaming/input/InputStream.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1465 | `InputStream` method `virtual InputStream *seek(uint64_t pos) {` (`include/data/streaming/input/InputStream.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1466 | `InputStream` destructor `virtual ~InputStream() {` (`include/data/streaming/input/InputStream.h:86`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::InputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1467 | `InputStream` method `virtual uint64_t getPos() { return position; }` (`include/data/streaming/input/InputStream.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1468 | `InputStream` method `virtual std::string readString() {` (`include/data/streaming/input/InputStream.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1469 | `InputStream` method `virtual std::string readUTF() {` (`include/data/streaming/input/InputStream.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1470 | `InputStream` method `void adviseSequentialRead(std::ifstream *ifs) {}` (`include/data/streaming/input/InputStream.h:175`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1471 | `InputStream` method `virtual INLINE uint64_t readBytes(uint8_t *bytes, size_t cnt) {` (`include/data/streaming/input/InputStream.h:177`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1472 | `InputStream` method `virtual INLINE uint64_t readBytes(char *bytes, size_t cnt) {` (`include/data/streaming/input/InputStream.h:183`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1473 | `InputStream` method `virtual INLINE uint64_t readBytes(uint8_t **bytes, size_t cnt) {` (`include/data/streaming/input/InputStream.h:189`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1474 | `InputStream` method `virtual uint8_t readByte() {` (`include/data/streaming/input/InputStream.h:196`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1475 | `InputStream` method `virtual int8_t readSignedByte() {` (`include/data/streaming/input/InputStream.h:202`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1476 | `InputStream` method `virtual int64_t readSignedByteAsInt() {` (`include/data/streaming/input/InputStream.h:208`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1477 | `InputStream` method `virtual short readShort() {` (`include/data/streaming/input/InputStream.h:214`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1478 | `InputStream` method `virtual unsigned short readUnsignedShort() {` (`include/data/streaming/input/InputStream.h:220`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1479 | `InputStream` method `virtual int readInt() {` (`include/data/streaming/input/InputStream.h:226`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1480 | `InputStream` method `virtual uint64_t readLong() {` (`include/data/streaming/input/InputStream.h:232`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1481 | `InputStream` method `virtual uint64_t readBoolean() {` (`include/data/streaming/input/InputStream.h:238`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1482 | `InputStream` method `int64_t readHadoopLong() {` (`include/data/streaming/input/InputStream.h:250`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1483 | `InputStream` method `int64_t readEncodedVLong() {` (`include/data/streaming/input/InputStream.h:298`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1484 | `InputStream` method `long readEncodedLong() {` (`include/data/streaming/input/InputStream.h:320`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1485 | `InputStream` method `virtual uint32_t bytesRead() { return position; }` (`include/data/streaming/input/InputStream.h:346`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1486 | `InputStream` method `virtual uint64_t bytesAvailable() {` (`include/data/streaming/input/InputStream.h:348`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1487 | `(namespace scope)` macro constant `#define _MAP_POPULATE_AVAILABLE` (`include/data/streaming/input/MemorymappedInputStream.h:32`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1488 | `(namespace scope)` macro constant `#define MMAP_FLAGS (MAP_PRIVATE &#124; MAP_POPULATE)` (`include/data/streaming/input/MemorymappedInputStream.h:37`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1489 | `(namespace scope)` macro constant `#define MMAP_FLAGS MAP_PRIVATE` (`include/data/streaming/input/MemorymappedInputStream.h:39`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1490 | `MemoryMappedInputStream` constructor `explicit MemoryMappedInputStream(const std::string &file) : offset(0) {` (`include/data/streaming/input/MemorymappedInputStream.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1491 | `MemoryMappedInputStream` destructor `virtual ~MemoryMappedInputStream() { munmap(ptr, filesize); }` (`include/data/streaming/input/MemorymappedInputStream.h:61`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::MemoryMappedInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1492 | `MemoryMappedInputStream` method `virtual uint64_t getPos() { return offset; }` (`include/data/streaming/input/MemorymappedInputStream.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1493 | `MemoryMappedInputStream` method `virtual InputStream *seek(uint64_t pos) {` (`include/data/streaming/input/MemorymappedInputStream.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1494 | `MemoryMappedInputStream` method `virtual inline uint64_t readBytes(uint8_t *bytes, size_t cnt) {` (`include/data/streaming/input/MemorymappedInputStream.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1495 | `MemoryMappedInputStream` method `virtual uint64_t readBytes(char *bytes, size_t cnt) {` (`include/data/streaming/input/MemorymappedInputStream.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1496 | `MemoryMappedInputStream` method `virtual uint64_t bytesAvailable() { return (filesize - offset); }` (`include/data/streaming/input/MemorymappedInputStream.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1497 | `(namespace scope)` macro constant `#define ntohll(x) (((uint64_t)(ntohl((uint32_t)((x << 32) >> 32))) << 32) &#124; ntohl(((uint32_t)(x >> 32))))` (`include/data/streaming/input/NetworkOrderInputStream.h:32`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1498 | `EndianInputStream` constructor `explicit EndianInputStream(InputStream *out_stream) : ByteInputStream(out_stream) {}` (`include/data/streaming/input/NetworkOrderInputStream.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1499 | `EndianInputStream` constructor `explicit EndianInputStream(char *byteArray, size_t len) : ByteInputStream(byteArray, len) {}` (`include/data/streaming/input/NetworkOrderInputStream.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1500 | `EndianInputStream` constructor `explicit EndianInputStream(char *byteArray, size_t len, bool allocated) : ByteInputStream(byteArray, len, allocated) {}` (`include/data/streaming/input/NetworkOrderInputStream.h:49`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1501 | `EndianInputStream` constructor `EndianInputStream() : ByteInputStream() {}` (`include/data/streaming/input/NetworkOrderInputStream.h:52`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1502 | `EndianInputStream` destructor `virtual ~EndianInputStream() {}` (`include/data/streaming/input/NetworkOrderInputStream.h:54`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::EndianInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1503 | `EndianInputStream` method `virtual short readShort() override {` (`include/data/streaming/input/NetworkOrderInputStream.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1504 | `EndianInputStream` method `virtual unsigned short readUnsignedShort() override {` (`include/data/streaming/input/NetworkOrderInputStream.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1505 | `EndianInputStream` method `virtual int readInt() override {` (`include/data/streaming/input/NetworkOrderInputStream.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1506 | `EndianInputStream` method `virtual uint64_t readLong() override {` (`include/data/streaming/input/NetworkOrderInputStream.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1507 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1508 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1509 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:36`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1510 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:37`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1511 | `(namespace scope)` macro constant `#define ntohll(x) (((uint64_t)(ntohl((uint32_t)((x << 32) >> 32))) << 32) &#124; ntohl(((uint32_t)(x >> 32))))` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:42`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1512 | `NonCopyMemoryMappedInputStream` constructor `explicit NonCopyMemoryMappedInputStream(const std::string &file) : MemoryMappedInputStream(file) {}` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:53`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1513 | `NonCopyMemoryMappedInputStream` destructor `virtual ~NonCopyMemoryMappedInputStream() {}` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:56`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::NonCopyMemoryMappedInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1514 | `NonCopyMemoryMappedInputStream` method `virtual short readShort() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1515 | `NonCopyMemoryMappedInputStream` method `virtual unsigned short readUnsignedShort() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1516 | `NonCopyMemoryMappedInputStream` method `virtual uint8_t readByte() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1517 | `NonCopyMemoryMappedInputStream` method `virtual int8_t readSignedByte() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1518 | `NonCopyMemoryMappedInputStream` method `virtual int64_t readSignedByteAsInt() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1519 | `NonCopyMemoryMappedInputStream` method `virtual int readInt() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1520 | `NonCopyMemoryMappedInputStream` method `virtual uint64_t readLong() override {` (`include/data/streaming/input/NonCopyMemoryMappedStream.h:120`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1521 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1522 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:35`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1523 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:37`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1524 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:38`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1525 | `(namespace scope)` macro constant `#define ntohll(x) (((uint64_t)(ntohl((uint32_t)((x << 32) >> 32))) << 32) &#124; ntohl(((uint32_t)(x >> 32))))` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:43`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1526 | `NonCopyEndianInputStream` constructor `explicit NonCopyEndianInputStream(InputStream *out_stream) : ByteInputStream(out_stream), bosref(nullptr), queueref(nullptr) {}` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1527 | `NonCopyEndianInputStream` constructor `explicit NonCopyEndianInputStream(char *byteArray, size_t len) : ByteInputStream(byteArray, len), bosref(nullptr), queueref(nullptr) {}` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:63`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1528 | `NonCopyEndianInputStream` constructor `explicit NonCopyEndianInputStream(char *byteArray, size_t len, bool allocated) : ByteInputStream(byteArray, len, allocated), bosref(nullptr), queueref(nullptr) {}` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:66`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1529 | `NonCopyEndianInputStream` constructor `explicit NonCopyEndianInputStream( cclient::data::streams::ByteOutputStream *ref, moodycamel::ConcurrentQueue<cclient::data::streams::ByteOutputStream *> *queueref) : ByteInputStream(ref->getByteArray(), ref->getSize(), false), bosref(ref), queueref(queueref) {}` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:71`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1530 | `NonCopyEndianInputStream` constructor `NonCopyEndianInputStream()` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:79`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1531 | `NonCopyEndianInputStream` destructor `virtual ~NonCopyEndianInputStream() {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:82`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::NonCopyEndianInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1532 | `NonCopyEndianInputStream` method `virtual INLINE short readShort() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1533 | `NonCopyEndianInputStream` method `virtual INLINE unsigned short readUnsignedShort() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1534 | `NonCopyEndianInputStream` method `virtual INLINE uint8_t readByte() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1535 | `NonCopyEndianInputStream` method `virtual INLINE int8_t readSignedByte() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:122`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1536 | `NonCopyEndianInputStream` method `virtual INLINE int64_t readSignedByteAsInt() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:131`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1537 | `NonCopyEndianInputStream` method `virtual INLINE int readInt() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:140`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1538 | `NonCopyEndianInputStream` method `virtual INLINE uint64_t readLong() override {` (`include/data/streaming/input/NonCopyNetworkOrderStream.h:153`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1539 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (__builtin_expect((val), 0))` (`include/data/streaming/input/ReadAheadInputStream.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1540 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (__builtin_expect((val), 1))` (`include/data/streaming/input/ReadAheadInputStream.h:34`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1541 | `(namespace scope)` macro constant `#define SH_UNLIKELY(val) (val)` (`include/data/streaming/input/ReadAheadInputStream.h:36`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1542 | `(namespace scope)` macro constant `#define SH_LIKELY(val) (val)` (`include/data/streaming/input/ReadAheadInputStream.h:37`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. client-side data construct. |
| SB-CXX-1543 | `ReadAheadInputStream` constructor `explicit ReadAheadInputStream(std::unique_ptr<InputStream> out_stream, size_t readahead, size_t maxBuffer, size_t lng) : ReadAheadInputStream(out_stream.get(), readahead, maxBuffer, lng) {` (`include/data/streaming/input/ReadAheadInputStream.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1544 | `ReadAheadInputStream` constructor `explicit ReadAheadInputStream(InputStream *out_stream, size_t readahead, size_t maxBuffer, size_t lng) : ByteInputStream(out_stream), readAheadSize(readahead), readAheadStart(0), currentReadAheadSize(0), currentBatchOffset(0) {` (`include/data/streaming/input/ReadAheadInputStream.h:56`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1545 | `ReadAheadInputStream` destructor `virtual ~ReadAheadInputStream() {}` (`include/data/streaming/input/ReadAheadInputStream.h:72`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::ReadAheadInputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1546 | `ReadAheadInputStream` method `virtual INLINE uint64_t readBytes(uint8_t *bytes, size_t cnt) override {` (`include/data/streaming/input/ReadAheadInputStream.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1547 | `ReadAheadInputStream` method `virtual INLINE uint64_t readBytes(char *bytes, size_t cnt) override {` (`include/data/streaming/input/ReadAheadInputStream.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1548 | `ReadAheadInputStream` method `virtual InputStream *seek(uint64_t pos) override {` (`include/data/streaming/input/ReadAheadInputStream.h:100`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1549 | `ReadAheadInputStream` method `virtual INLINE short readShort() override {` (`include/data/streaming/input/ReadAheadInputStream.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1550 | `ReadAheadInputStream` method `virtual INLINE unsigned short readUnsignedShort() override {` (`include/data/streaming/input/ReadAheadInputStream.h:124`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1551 | `ReadAheadInputStream` method `virtual INLINE uint8_t readByte() override {` (`include/data/streaming/input/ReadAheadInputStream.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1552 | `ReadAheadInputStream` method `virtual INLINE int8_t readSignedByte() override {` (`include/data/streaming/input/ReadAheadInputStream.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1553 | `ReadAheadInputStream` method `virtual INLINE int64_t readSignedByteAsInt() override {` (`include/data/streaming/input/ReadAheadInputStream.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1554 | `ReadAheadInputStream` method `virtual INLINE int readInt() override {` (`include/data/streaming/input/ReadAheadInputStream.h:174`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1555 | `ReadAheadInputStream` method `virtual INLINE uint64_t readLong() override {` (`include/data/streaming/input/ReadAheadInputStream.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1556 | `BigEndianOutStream` constructor `explicit BigEndianOutStream(OutputStream *os) : DataOutputStream(os) {}` (`include/data/streaming/NetworkOrderStream.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1557 | `BigEndianOutStream` method `uint64_t htonlw(uint64_t value) {` (`include/data/streaming/NetworkOrderStream.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1558 | `BigEndianOutStream` method `uint64_t writeShort(short shortVal) override {` (`include/data/streaming/NetworkOrderStream.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1559 | `BigEndianOutStream` method `uint64_t writeInt(int intVal) override {` (`include/data/streaming/NetworkOrderStream.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1560 | `BigEndianOutStream` method `uint64_t writeLong(uint64_t val) override {` (`include/data/streaming/NetworkOrderStream.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1561 | `BigEndianOutStream` method `uint64_t writeBytes(const char *bytes, size_t cnt) override {` (`include/data/streaming/NetworkOrderStream.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1562 | `OutputStream` constructor `explicit OutputStream(std::unique_ptr<std::ostream> ptr, uint64_t pos) : ostream_ref(ptr.get()), position(pos), copy(false) {` (`include/data/streaming/OutputStream.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1563 | `OutputStream` constructor `explicit OutputStream(std::ostream *ptr, uint64_t pos);` (`include/data/streaming/OutputStream.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1564 | `OutputStream` constructor `OutputStream();` (`include/data/streaming/OutputStream.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1565 | `OutputStream` destructor `virtual ~OutputStream();` (`include/data/streaming/OutputStream.h:43`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::OutputStream` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1566 | `OutputStream` method `virtual void flush();` (`include/data/streaming/OutputStream.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1567 | `OutputStream` method `virtual uint64_t getPos();` (`include/data/streaming/OutputStream.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1568 | `OutputStream` method `virtual uint64_t write(const char *bytes, long cnt);` (`include/data/streaming/OutputStream.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1569 | `OutputStream` method `virtual uint64_t writeByte(int byte);` (`include/data/streaming/OutputStream.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1570 | `OutputStream` method `virtual uint64_t writeString(std::string s);` (`include/data/streaming/OutputStream.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1571 | `OutputStream` method `virtual uint64_t write(const uint8_t *bytes, long cnt);` (`include/data/streaming/OutputStream.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1572 | `OutputStream` method `virtual uint64_t writeBytes(const uint8_t *bytes, size_t cnt);` (`include/data/streaming/OutputStream.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1573 | `OutputStream` method `virtual uint64_t writeBytes(const char *bytes, size_t cnt);` (`include/data/streaming/OutputStream.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1574 | `OutputStream` method `virtual uint64_t writeByte(const uint8_t byte);` (`include/data/streaming/OutputStream.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1575 | `OutputStream` method `virtual uint64_t writeShort(const short shortVal);` (`include/data/streaming/OutputStream.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1576 | `OutputStream` method `virtual uint64_t writeInt(const int intVal);` (`include/data/streaming/OutputStream.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1577 | `OutputStream` method `virtual uint64_t writeLong(const uint64_t val);` (`include/data/streaming/OutputStream.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1578 | `OutputStream` method `virtual uint64_t writeVLong(const int64_t val);` (`include/data/streaming/OutputStream.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1579 | `OutputStream` method `virtual uint64_t writeBoolean(const bool val);` (`include/data/streaming/OutputStream.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1580 | `OutputStream` method `virtual uint64_t writeHadoopLong(const int64_t n = 0);` (`include/data/streaming/OutputStream.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1581 | `OutputStream` method `virtual uint64_t writeEncodedLong(const int64_t n = 0);` (`include/data/streaming/OutputStream.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1582 | `OutputStream` method `virtual uint32_t bytesWritten();` (`include/data/streaming/OutputStream.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1583 | `StreamConfiguration` constructor `StreamConfiguration() {}` (`include/data/streaming/StreamEnvironment.h:23`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1584 | `StreamRelocation` constructor `StreamRelocation() {}` (`include/data/streaming/StreamRelocation.h:26`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1585 | `StreamRelocation` destructor `virtual ~StreamRelocation() {}` (`include/data/streaming/StreamRelocation.h:28`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::StreamRelocation` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1586 | `StreamRelocation` method `virtual cclient::data::security::Authorizations* getAuths() = 0;` (`include/data/streaming/StreamRelocation.h:30`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1587 | `StreamRelocation` method `virtual cclient::data::Range* getRange() = 0;` (`include/data/streaming/StreamRelocation.h:32`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1588 | `StreamRelocation` method `virtual std::vector<std::string>* getColumnFamilies() = 0;` (`include/data/streaming/StreamRelocation.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1589 | `StreamRelocation` method `virtual uint32_t getDesiredThreads() const = 0;` (`include/data/streaming/StreamRelocation.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1590 | `StreamRelocation` method `virtual uint32_t getKeysCachedPerThread() const = 0;` (`include/data/streaming/StreamRelocation.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1591 | `StreamRelocation` method `virtual bool isInclusive() = 0;` (`include/data/streaming/StreamRelocation.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1592 | `StreamInterface` constructor `StreamInterface();` (`include/data/streaming/Streams.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1593 | `StreamInterface` destructor `virtual ~StreamInterface();` (`include/data/streaming/Streams.h:36`) | — | — | — | Missing Go | Assessed individually: `cclient::data::streams::StreamInterface` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. client-side data construct. |
| SB-CXX-1594 | `StreamInterface` method `virtual std::shared_ptr<StreamInterface> getStream();` (`include/data/streaming/Streams.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1595 | `StreamInterface` method `virtual uint64_t write(cclient::data::streams::OutputStream *out);` (`include/data/streaming/Streams.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1596 | `StreamInterface` method `virtual uint64_t read(cclient::data::streams::InputStream *in);` (`include/data/streaming/Streams.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1597 | `StreamInterface` method `virtual DataOutputStream *createDataStream(DataOutputStream *out, std::string name = "") {` (`include/data/streaming/Streams.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. client-side data construct. |
| SB-CXX-1598 | `CKey` public data member `char *row;` (`include/extern/accumulo_data.h:24`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1599 | `CKey` public data member `char *colFamily;` (`include/extern/accumulo_data.h:31`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1600 | `CKey` public data member `char *colQualifier;` (`include/extern/accumulo_data.h:40`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1601 | `CKey` public data member `char *keyVisibility;` (`include/extern/accumulo_data.h:44`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1602 | `CKey` public data member `uint64_t timestamp;` (`include/extern/accumulo_data.h:45`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1603 | `CKey` public bit-field data member `unsigned deleted : 1;` (`include/extern/accumulo_data.h:47`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared as a **bit-field**: its width is part of the ABI layout, and it is independently readable and writable, so it is one obligation of its own rather than part of a packed group. flat C API surface. |
| SB-CXX-1604 | `CAuthorizations` public data member `char **auths;` (`include/extern/accumulo_data.h:55`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1605 | `CAuthorizations` public data member `size_t auth_size;` (`include/extern/accumulo_data.h:56`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1606 | `CValue` public data member `uint8_t *value;` (`include/extern/accumulo_data.h:62`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1607 | `CValue` public data member `uint16_t offset;` (`include/extern/accumulo_data.h:64`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1608 | `CValue` public data member `size_t valueSize;` (`include/extern/accumulo_data.h:66`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1609 | `CKeyValue` public data member `struct CKey *key;` (`include/extern/accumulo_data.h:71`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1610 | `CKeyValue` public data member `struct CValue *value;` (`include/extern/accumulo_data.h:72`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1611 | `KeyValueList` public data member `struct CKeyValue **kvs;` (`include/extern/accumulo_data.h:77`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1612 | `KeyValueList` public data member `uint32_t kv_size;` (`include/extern/accumulo_data.h:78`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1613 | `CRange` public data member `struct CKey *start;` (`include/extern/accumulo_data.h:83`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1614 | `CRange` public data member `struct CKey *stop;` (`include/extern/accumulo_data.h:84`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1615 | `CRange` public bit-field data member `unsigned startKeyInclusive : 1;` (`include/extern/accumulo_data.h:85`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared as a **bit-field**: its width is part of the ABI layout, and it is independently readable and writable, so it is one obligation of its own rather than part of a packed group. flat C API surface. |
| SB-CXX-1616 | `CRange` public bit-field data member `unsigned stopKeyInclusive : 1;` (`include/extern/accumulo_data.h:86`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared as a **bit-field**: its width is part of the ABI layout, and it is independently readable and writable, so it is one obligation of its own rather than part of a packed group. flat C API surface. |
| SB-CXX-1617 | `CRange` public bit-field data member `unsigned infiniteStartKey :1;` (`include/extern/accumulo_data.h:87`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared as a **bit-field**: its width is part of the ABI layout, and it is independently readable and writable, so it is one obligation of its own rather than part of a packed group. flat C API surface. |
| SB-CXX-1618 | `CRange` public bit-field data member `unsigned infiniteStopKey : 1;` (`include/extern/accumulo_data.h:88`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. Declared as a **bit-field**: its width is part of the ABI layout, and it is independently readable and writable, so it is one obligation of its own rather than part of a packed group. flat C API surface. |
| SB-CXX-1619 | `ScanRes` constructor `ScanRes(scanners::BatchScanner *scan) : scan_(scan) {` (`include/extern/accumulo.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. flat C API surface. |
| SB-CXX-1620 | `ScanRes` method `bool hasNext(){` (`include/extern/accumulo.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. flat C API surface. |
| SB-CXX-1621 | `ScanRes` method `std::shared_ptr<cclient::data::Key> next(){` (`include/extern/accumulo.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. flat C API surface. |
| SB-CXX-1622 | `TableOps` public data member `char *table_name;` (`include/extern/accumulo.h:75`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1623 | `TableOps` public data member `struct connector *parent;` (`include/extern/accumulo.h:77`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1624 | `TableOps` public data member `void *tableOpsPtr;` (`include/extern/accumulo.h:78`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1625 | `BatchScan` public data member `struct TableOps *tableOps;` (`include/extern/accumulo.h:82`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1626 | `BatchScan` public data member `void *scannerPtr;` (`include/extern/accumulo.h:84`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1627 | `BatchScan` public data member `void *res;` (`include/extern/accumulo.h:85`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1628 | `BatchScan` public data member `void *on_next_fn;` (`include/extern/accumulo.h:86`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1629 | `BatchWriter` public data member `void *writerPtr;` (`include/extern/accumulo.h:91`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1630 | `CMutation` public data member `void *mutationPtr;` (`include/extern/accumulo.h:95`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1631 | `connector` public data member `void *masterPtr;` (`include/extern/accumulo.h:101`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1632 | `connector` public data member `void *zk;` (`include/extern/accumulo.h:102`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. flat C API surface. |
| SB-CXX-1633 | `(namespace scope)` namespace-scope function `struct connector *create_connector(char *instance, char *zks, char *username, char *password);` (`include/extern/accumulo.h:108`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-001](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:108`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1634 | `(namespace scope)` namespace-scope function `int free_connector(struct connector *connector);` (`include/extern/accumulo.h:111`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-015](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:111`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1635 | `(namespace scope)` namespace-scope function `struct TableOps *open_table(struct connector *connector, char *tableName);` (`include/extern/accumulo.h:113`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-002](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:113`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1636 | `(namespace scope)` namespace-scope function `struct TableOps *create_table(struct connector *connector, char *tableName);` (`include/extern/accumulo.h:115`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-016](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:115`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1637 | `(namespace scope)` namespace-scope function `int drop_table(struct TableOps *tableOps);` (`include/extern/accumulo.h:117`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-017](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:117`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1638 | `(namespace scope)` namespace-scope function `int free_table(struct TableOps *tableOps);` (`include/extern/accumulo.h:119`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-018](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:119`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1639 | `(namespace scope)` namespace-scope function `struct CMutation *createMutation(char *row);` (`include/extern/accumulo.h:123`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-003](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:123`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1640 | `(namespace scope)` namespace-scope function `void freeMutation(struct CMutation *);` (`include/extern/accumulo.h:125`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-020](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:125`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1641 | `(namespace scope)` namespace-scope function `void put(struct CMutation *, char *cf, char *cq, char *cv);` (`include/extern/accumulo.h:127`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-019](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:127`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1642 | `(namespace scope)` namespace-scope function `struct BatchScan *createScanner(struct TableOps *tableOps, short threads);` (`include/extern/accumulo.h:129`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-005](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:129`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1643 | `(namespace scope)` namespace-scope function `struct BatchWriter *createWriter(struct TableOps *tableOps, short threads);` (`include/extern/accumulo.h:131`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-004](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:131`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1644 | `(namespace scope)` namespace-scope function `int addMutation(struct BatchWriter *writer, struct CMutation *mutation);` (`include/extern/accumulo.h:133`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-021](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:133`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1645 | `(namespace scope)` namespace-scope function `int addRange(struct BatchScan *scanner, struct CRange *range);` (`include/extern/accumulo.h:135`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-023](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:135`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1646 | `(namespace scope)` namespace-scope function `int addRanges(struct BatchScan *scanner, struct CRange **range, int size);` (`include/extern/accumulo.h:137`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-024](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:137`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1647 | `(namespace scope)` namespace-scope function `bool hasNext(struct BatchScan *scanner);` (`include/extern/accumulo.h:139`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-025](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:139`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1648 | `(namespace scope)` namespace-scope function `int onNext(struct BatchScan *scanner, struct CKeyValue *kv);` (`include/extern/accumulo.h:141`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-026](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:141`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1649 | `(namespace scope)` namespace-scope function `CKeyValue *next(struct BatchScan *scanner);` (`include/extern/accumulo.h:143`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-027](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:143`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1650 | `(namespace scope)` namespace-scope function `int nextMany(struct BatchScan *scanner, struct KeyValueList *kvl);` (`include/extern/accumulo.h:145`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-028](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:145`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1651 | `(namespace scope)` namespace-scope function `int closeScanner(struct BatchScan *scanner);` (`include/extern/accumulo.h:147`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-029](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:147`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1652 | `(namespace scope)` namespace-scope function `int closeWriter(struct BatchWriter *writer);` (`include/extern/accumulo.h:149`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-022](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:149`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1653 | `(namespace scope)` namespace-scope function `struct CKey *createKey(char *row, char *cf, char *cq, char *cv, uint64_t timestamp);` (`include/extern/accumulo.h:153`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-006](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:153`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1654 | `(namespace scope)` namespace-scope function `std::shared_ptr<cclient::data::Key> toKey(CKey *key);` (`include/extern/accumulo.h:157`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-035](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:157`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1655 | `(namespace scope)` namespace-scope function `void populateKey(CKey *key, const std::shared_ptr<cclient::data::Key> &otherKey);` (`include/extern/accumulo.h:159`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-036](#sec-19) already carries this obligation — that row cites this exact declaration (`include/extern/accumulo.h:159`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1656 | `(namespace scope)` namespace-scope static variable `static TransportPool<interconnect::AccumuloCoordinatorTransporter> ACCUMULO_COORDINATOR;` (`include/interconnect/Accumulo.h:50`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-010](#sec-7) already carries this obligation — that row cites this exact declaration (`include/interconnect/Accumulo.h:50`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1657 | `AccumuloConnector` constructor `explicit AccumuloConnector(cclient::data::security::AuthInfo &credentials, std::shared_ptr<cclient::data::Instance> instance);` (`include/interconnect/Accumulo.h:61`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-001](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::AccumuloConnector` is the overload bound at `pysharkbite.cpp:259` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1658 | `AccumuloConnector` constructor `explicit AccumuloConnector(cclient::data::security::AuthInfo *credentials, std::shared_ptr<cclient::data::Instance> instance) : AccumuloConnector(*credentials, instance) {}` (`include/interconnect/Accumulo.h:69`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1659 | `AccumuloConnector` method `std::shared_ptr<AccumuloTableOperations> tableOps(const std::string &table);` (`include/interconnect/Accumulo.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1660 | `AccumuloConnector` method `std::shared_ptr<AccumuloTableInfo> tableInfo();` (`include/interconnect/Accumulo.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1661 | `AccumuloConnector` method `std::shared_ptr<NamespaceOperations> namespaceOps(const std::string &nm = "");` (`include/interconnect/Accumulo.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1662 | `AccumuloConnector` method `cclient::data::AccumuloInfo getStatistics();` (`include/interconnect/Accumulo.h:98`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-007](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::getStatistics` is the overload bound at `pysharkbite.cpp:268` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1663 | `AccumuloConnector` method `std::shared_ptr<SecurityOperations> securityOps();` (`include/interconnect/Accumulo.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1664 | `AccumuloConnector` method `virtual PythonTableOperations table_operations( const std::string &table) override {` (`include/interconnect/Accumulo.h:106`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-003](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::table_operations` is the overload bound at `pysharkbite.cpp:269` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1665 | `AccumuloConnector` method `virtual PythonNamespaceOperations namespace_operations( const std::string &nm = "") override {` (`include/interconnect/Accumulo.h:111`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-005](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::namespace_operations` is the overload bound at `pysharkbite.cpp:267` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1666 | `AccumuloConnector` method `virtual PythonSecurityOperations security_operations() override {` (`include/interconnect/Accumulo.h:116`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-004](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::security_operations` is the overload bound at `pysharkbite.cpp:266` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1667 | `AccumuloConnector` method `virtual PythonTableInfo table_info() override {` (`include/interconnect/Accumulo.h:120`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-006](#sec-7) already carries this obligation — `interconnect::AccumuloConnector::table_info` is the overload bound at `pysharkbite.cpp:270` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1668 | `AccumuloConnector` destructor `virtual ~AccumuloConnector();` (`include/interconnect/Accumulo.h:124`) | — | — | — | Behavior mismatch | Assessed individually: `interconnect::AccumuloConnector` is bound into Python elsewhere in this document, so its destruction point is user-observable. Sharkbite destroys at last reference or scope exit; the Shoal-backed equivalent requires an explicit close and treats a dropped handle as a leak ([SB-XCUT-001](#sec-20)). The shim owns that bridge. |
| SB-CXX-1669 | `AccumuloConnector` operator `AccumuloConnector &operator=(const AccumuloConnector &other) {` (`include/interconnect/Accumulo.h:126`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-009](#sec-7) already carries this obligation — that row cites this exact declaration (`include/interconnect/Accumulo.h:126`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1670 | `AccumuloCoordinatorFacade` constructor `AccumuloCoordinatorFacade( const std::string &host, int version, std::function<void()> fx, std::function<std::shared_ptr<apache::thrift::transport::TTransport>()> tfx)` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1671 | `AccumuloCoordinatorFacade` destructor `virtual ~AccumuloCoordinatorFacade() {}` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:59`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloCoordinatorFacade` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1672 | `AccumuloCoordinatorFacade` method `virtual void createCoordinatorClient( std::shared_ptr<apache::thrift::transport::TTransport> underlyingTransport) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1673 | `AccumuloCoordinatorFacade` method `void recreateCoordinatorTransport() { createCoordinatorTransport(); }` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1674 | `AccumuloCoordinatorFacade` method `virtual std::string doFateOperations( cclient::data::security::AuthInfo *auth, AccumuloFateOperation mytype, const std::vector<std::string> &tableArgs, const std::map<std::string, std::string> &options, bool wait = false) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1675 | `AccumuloCoordinatorFacade` method `virtual cclient::data::AccumuloInfo getStatistics( cclient::data::security::AuthInfo *auth) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1676 | `AccumuloCoordinatorFacade` method `virtual bool createTable(cclient::data::security::AuthInfo *auth, const std::string &table) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1677 | `AccumuloCoordinatorFacade` method `virtual bool importDirectory(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &dir, const std::string &failure_dir, bool setTime) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1678 | `AccumuloCoordinatorFacade` method `virtual bool compactFallBack(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1679 | `AccumuloCoordinatorFacade` method `virtual bool compact(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1680 | `AccumuloCoordinatorFacade` method `virtual bool flush(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1681 | `AccumuloCoordinatorFacade` method `virtual bool removeTable(cclient::data::security::AuthInfo *auth, const std::string &table) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1682 | `AccumuloCoordinatorFacade` method `virtual void removeTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1683 | `AccumuloCoordinatorFacade` method `virtual void setTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property, const std::string &value) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1684 | `AccumuloCoordinatorFacade` method `virtual bool createNamespace(cclient::data::security::AuthInfo *auth, const std::string &name) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:109`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1685 | `AccumuloCoordinatorFacade` method `virtual bool deletenamespace(cclient::data::security::AuthInfo *auth, const std::string &name) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1686 | `AccumuloCoordinatorFacade` method `virtual bool renamenamespace(cclient::data::security::AuthInfo *auth, const std::string &oldName, const std::string &newName) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1687 | `AccumuloCoordinatorFacade` method `virtual void removeNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:119`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1688 | `AccumuloCoordinatorFacade` method `virtual void setNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property, const std::string &value) = 0;` (`include/interconnect/accumulo/AccumuloCoordinatorFacade.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1689 | `AccumuloCoordinatorFacadeV1` constructor `AccumuloCoordinatorFacadeV1( const std::string &host, std::function<void()> fx, std::function<std::shared_ptr<apache::thrift::transport::TTransport>()> tfx)` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:453`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1690 | `AccumuloCoordinatorFacadeV1` method `void createCoordinatorClient( std::shared_ptr<apache::thrift::transport::TTransport> underlyingTransport) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:461`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1691 | `AccumuloCoordinatorFacadeV1` method `std::string doFateOperations( cclient::data::security::AuthInfo *auth, AccumuloFateOperation mytype, const std::vector<std::string> &tableArgs, const std::map<std::string, std::string> &options, bool wait = false) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:467`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1692 | `AccumuloCoordinatorFacadeV1` method `std::string v1_doFateOperations( cclient::data::security::AuthInfo *auth, AccumuloFateOperation mytype, const std::vector<std::string> &tableArgs, const std::map<std::string, std::string> &options, bool wait = false) {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:479`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1693 | `AccumuloCoordinatorFacadeV1` method `bool createTable(cclient::data::security::AuthInfo *auth, const std::string &table) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:615`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1694 | `AccumuloCoordinatorFacadeV1` method `virtual cclient::data::AccumuloInfo getStatistics( cclient::data::security::AuthInfo *auth) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:620`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1695 | `AccumuloCoordinatorFacadeV1` method `bool importDirectory(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &dir, const std::string &failure_dir, bool setTime) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:633`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1696 | `AccumuloCoordinatorFacadeV1` method `bool compactFallBack(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:639`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1697 | `AccumuloCoordinatorFacadeV1` method `bool compact(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:645`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1698 | `AccumuloCoordinatorFacadeV1` method `bool flush(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:651`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1699 | `AccumuloCoordinatorFacadeV1` method `bool removeTable(cclient::data::security::AuthInfo *auth, const std::string &table) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:657`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1700 | `AccumuloCoordinatorFacadeV1` method `void removeTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:662`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1701 | `AccumuloCoordinatorFacadeV1` method `void setTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property, const std::string &value) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:668`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1702 | `AccumuloCoordinatorFacadeV1` method `bool createNamespace(cclient::data::security::AuthInfo *auth, const std::string &name) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:676`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1703 | `AccumuloCoordinatorFacadeV1` method `bool deletenamespace(cclient::data::security::AuthInfo *auth, const std::string &name) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:681`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1704 | `AccumuloCoordinatorFacadeV1` method `bool renamenamespace(cclient::data::security::AuthInfo *auth, const std::string &oldName, const std::string &newName) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:686`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1705 | `AccumuloCoordinatorFacadeV1` method `void removeNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:692`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1706 | `AccumuloCoordinatorFacadeV1` method `void setNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property, const std::string &value) override {` (`include/interconnect/accumulo/AccumuloCoordinatorOne.h:698`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1707 | `AccumuloCoordinatorFacadeV2` constructor `AccumuloCoordinatorFacadeV2( const std::string &host, std::function<void()> fx, std::function<std::shared_ptr<apache::thrift::transport::TTransport>()> tfx)` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:392`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1708 | `AccumuloCoordinatorFacadeV2` method `void createCoordinatorClient( std::shared_ptr<apache::thrift::transport::TTransport> underlyingTransport) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:400`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1709 | `AccumuloCoordinatorFacadeV2` method `virtual cclient::data::AccumuloInfo getStatistics( cclient::data::security::AuthInfo *auth) override {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:406`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1710 | `AccumuloCoordinatorFacadeV2` method `std::string doFateOperations( cclient::data::security::AuthInfo *auth, AccumuloFateOperation mytype, const std::vector<std::string> &tableArgs, const std::map<std::string, std::string> &options, bool wait = false) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:419`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1711 | `AccumuloCoordinatorFacadeV2` method `std::string v2_doFateOperations( cclient::data::security::AuthInfo *auth, AccumuloFateOperation mytype, const std::vector<std::string> &tableArgs, const std::map<std::string, std::string> &options, bool wait = false) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:428`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1712 | `AccumuloCoordinatorFacadeV2` method `bool createTable(cclient::data::security::AuthInfo *auth, const std::string &table) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:566`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1713 | `AccumuloCoordinatorFacadeV2` method `bool importDirectory(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &dir, const std::string &failure_dir, bool setTime) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:571`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1714 | `AccumuloCoordinatorFacadeV2` method `bool compactFallBack(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:577`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1715 | `AccumuloCoordinatorFacadeV2` method `bool compact(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:583`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1716 | `AccumuloCoordinatorFacadeV2` method `bool flush(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:589`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1717 | `AccumuloCoordinatorFacadeV2` method `bool removeTable(cclient::data::security::AuthInfo *auth, const std::string &table) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:595`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1718 | `AccumuloCoordinatorFacadeV2` method `void removeTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:600`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1719 | `AccumuloCoordinatorFacadeV2` method `void setTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property, const std::string &value) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:606`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1720 | `AccumuloCoordinatorFacadeV2` method `bool createNamespace(cclient::data::security::AuthInfo *auth, const std::string &name) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:614`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1721 | `AccumuloCoordinatorFacadeV2` method `bool deletenamespace(cclient::data::security::AuthInfo *auth, const std::string &name) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:619`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1722 | `AccumuloCoordinatorFacadeV2` method `bool renamenamespace(cclient::data::security::AuthInfo *auth, const std::string &oldName, const std::string &newName) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:624`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1723 | `AccumuloCoordinatorFacadeV2` method `void removeNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:629`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1724 | `AccumuloCoordinatorFacadeV2` method `void setNamespaceProperty(cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName, const std::string &property, const std::string &value) {` (`include/interconnect/accumulo/AccumuloCoordinatorTwo.h:635`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1725 | `(namespace scope)` macro constant `#define ACCUMULO_UNKNOWN 0` (`include/interconnect/accumulo/AccumuloFacade.h:17`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1726 | `(namespace scope)` macro constant `#define ACCUMULO_ONE 1` (`include/interconnect/accumulo/AccumuloFacade.h:19`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1727 | `(namespace scope)` macro constant `#define ACCUMULO_TWO 2` (`include/interconnect/accumulo/AccumuloFacade.h:21`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1728 | `AccumuloFateOperation` enum `enum AccumuloFateOperation {` (`include/interconnect/accumulo/AccumuloFacade.h:23`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: TABLE_CREATE = 0; TABLE_CLONE = 1; TABLE_DELETE = 2; TABLE_RENAME = 3; TABLE_ONLINE = 4; TABLE_OFFLINE = 5; TABLE_MERGE = 6; TABLE_DELETE_RANGE = 7; TABLE_BULK_IMPORT = 8; TABLE_COMPACT = 9; TABLE_IMPORT = 10; TABLE_EXPORT = 11; TABLE_CANCEL_COMPACT = 12; NAMESPACE_CREATE = 13; NAMESPACE_DELETE = 14; NAMESPACE_RENAME = 15. Shoal exposes no equivalent type. |
| SB-CXX-1729 | `AccumuloServerFacade` constructor `explicit AccumuloServerFacade(int version);` (`include/interconnect/accumulo/AccumuloServerFacade.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1730 | `AccumuloServerFacade` destructor `virtual ~AccumuloServerFacade() {}` (`include/interconnect/accumulo/AccumuloServerFacade.h:39`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloServerFacade` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1731 | `AccumuloServerFacade` method `virtual Scan *multiScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1732 | `AccumuloServerFacade` method `virtual Scan *singleScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1733 | `AccumuloServerFacade` method `virtual Scan *beginScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1734 | `AccumuloServerFacade` method `virtual Scan *continueScan(Scan *originalScan) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1735 | `AccumuloServerFacade` method `virtual void *write( cclient::data::security::AuthInfo *auth, std::map<cclient::data::KeyExtent, std::vector<std::shared_ptr<cclient::data::Mutation>>> *request) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1736 | `AccumuloServerFacade` method `virtual bool dropUser(cclient::data::security::AuthInfo *auth, const std::string &user) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1737 | `AccumuloServerFacade` method `virtual bool changeUserPassword(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1738 | `AccumuloServerFacade` method `virtual bool createUser(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1739 | `AccumuloServerFacade` method `virtual std::map<std::string, std::string> getTableConfiguration( cclient::data::security::AuthInfo *auth, const std::string &table) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1740 | `AccumuloServerFacade` method `virtual cclient::data::security::Authorizations *getUserAuths( cclient::data::security::AuthInfo *auth, const std::string &user) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1741 | `AccumuloServerFacade` method `virtual void changeUserAuths( cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::security::Authorizations *auths) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1742 | `AccumuloServerFacade` method `virtual void splitTablet( cclient::data::security::AuthInfo *auth, const std::shared_ptr<cclient::data::KeyExtent> extent, std::string split) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1743 | `AccumuloServerFacade` method `virtual void registerService(std::string instance, std::string clusterManagers) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1744 | `AccumuloServerFacade` method `virtual void close() = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1745 | `AccumuloServerFacade` method `virtual void initialize( std::shared_ptr<apache::thrift::protocol::TProtocol> protocolPtr, bool callRegistration = true) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1746 | `AccumuloServerFacade` method `virtual std::map<std::string, std::string> getNamespaceConfiguration( cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1747 | `AccumuloServerFacade` method `virtual void authenticate(cclient::data::security::AuthInfo *auth) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:106`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1748 | `AccumuloServerFacade` method `virtual bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1749 | `AccumuloServerFacade` method `virtual bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1750 | `AccumuloServerFacade` method `virtual bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1751 | `AccumuloServerFacade` method `virtual bool grant(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1752 | `AccumuloServerFacade` method `virtual bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:124`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1753 | `AccumuloServerFacade` method `virtual bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:127`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1754 | `AccumuloServerFacade` method `virtual bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1755 | `AccumuloServerFacade` method `virtual bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1756 | `AccumuloServerFacade` method `virtual bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) = 0;` (`include/interconnect/accumulo/AccumuloServerFacade.h:136`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1757 | `AccumuloServerFacadeV1` constructor `AccumuloServerFacadeV1();` (`include/interconnect/accumulo/AccumuloServerOne.h:116`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1758 | `AccumuloServerFacadeV1` method `Scan *multiScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1759 | `AccumuloServerFacadeV1` method `Scan *singleScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:124`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1760 | `AccumuloServerFacadeV1` method `Scan *beginScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1761 | `AccumuloServerFacadeV1` method `Scan *continueScan(Scan *originalScan) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:136`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1762 | `AccumuloServerFacadeV1` method `void *write(cclient::data::security::AuthInfo *auth, std::map<cclient::data::KeyExtent, std::vector<std::shared_ptr<cclient::data::Mutation>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1763 | `AccumuloServerFacadeV1` method `bool dropUser(cclient::data::security::AuthInfo *auth, const std::string &user) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:143`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1764 | `AccumuloServerFacadeV1` method `bool changeUserPassword(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:146`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1765 | `AccumuloServerFacadeV1` method `bool createUser(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1766 | `AccumuloServerFacadeV1` method `std::map<std::string, std::string> getTableConfiguration( cclient::data::security::AuthInfo *auth, const std::string &table) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:154`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1767 | `AccumuloServerFacadeV1` method `cclient::data::security::Authorizations *getUserAuths( cclient::data::security::AuthInfo *auth, const std::string &user) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1768 | `AccumuloServerFacadeV1` method `void changeUserAuths(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::security::Authorizations *auths) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:162`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1769 | `AccumuloServerFacadeV1` method `void splitTablet(cclient::data::security::AuthInfo *auth, const std::shared_ptr<cclient::data::KeyExtent> extent, std::string split) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:166`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1770 | `AccumuloServerFacadeV1` method `void registerService(std::string instance, std::string clusterManagers) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:170`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1771 | `AccumuloServerFacadeV1` method `void close() override;` (`include/interconnect/accumulo/AccumuloServerOne.h:173`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1772 | `AccumuloServerFacadeV1` method `void initialize( std::shared_ptr<apache::thrift::protocol::TProtocol> protocolPtr, bool callRegistration = true) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:175`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1773 | `AccumuloServerFacadeV1` method `std::map<std::string, std::string> getNamespaceConfiguration( cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:179`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1774 | `AccumuloServerFacadeV1` method `void authenticate(cclient::data::security::AuthInfo *auth) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:183`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1775 | `AccumuloServerFacadeV1` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1776 | `AccumuloServerFacadeV1` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:188`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1777 | `AccumuloServerFacadeV1` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1778 | `AccumuloServerFacadeV1` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:194`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1779 | `AccumuloServerFacadeV1` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:196`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1780 | `AccumuloServerFacadeV1` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:199`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1781 | `AccumuloServerFacadeV1` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:202`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1782 | `AccumuloServerFacadeV1` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1783 | `AccumuloServerFacadeV1` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerOne.h:207`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1784 | `AccumuloServerFacadeV2` constructor `AccumuloServerFacadeV2();` (`include/interconnect/accumulo/AccumuloServerTwo.h:113`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1785 | `AccumuloServerFacadeV2` method `Scan *multiScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1786 | `AccumuloServerFacadeV2` method `Scan *singleScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1787 | `AccumuloServerFacadeV2` method `Scan *beginScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:127`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1788 | `AccumuloServerFacadeV2` method `Scan *continueScan(Scan *originalScan) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1789 | `AccumuloServerFacadeV2` method `void *write(cclient::data::security::AuthInfo *auth, std::map<cclient::data::KeyExtent, std::vector<std::shared_ptr<cclient::data::Mutation>>> *request) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1790 | `AccumuloServerFacadeV2` method `bool dropUser(cclient::data::security::AuthInfo *auth, const std::string &user) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:140`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1791 | `AccumuloServerFacadeV2` method `bool changeUserPassword(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:143`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1792 | `AccumuloServerFacadeV2` method `bool createUser(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:147`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1793 | `AccumuloServerFacadeV2` method `std::map<std::string, std::string> getTableConfiguration( cclient::data::security::AuthInfo *auth, const std::string &table) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:151`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1794 | `AccumuloServerFacadeV2` method `cclient::data::security::Authorizations *getUserAuths( cclient::data::security::AuthInfo *auth, const std::string &user) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:155`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1795 | `AccumuloServerFacadeV2` method `void changeUserAuths(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::security::Authorizations *auths) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:159`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1796 | `AccumuloServerFacadeV2` method `void splitTablet(cclient::data::security::AuthInfo *auth, const std::shared_ptr<cclient::data::KeyExtent> extent, std::string split) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:163`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1797 | `AccumuloServerFacadeV2` method `void registerService(std::string instance, std::string clusterManagers) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:167`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1798 | `AccumuloServerFacadeV2` method `void close() override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:170`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1799 | `AccumuloServerFacadeV2` method `void initialize( std::shared_ptr<apache::thrift::protocol::TProtocol> protocolPtr, bool callRegistration = true) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:172`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1800 | `AccumuloServerFacadeV2` method `std::map<std::string, std::string> getNamespaceConfiguration( cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:176`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1801 | `AccumuloServerFacadeV2` method `void authenticate(cclient::data::security::AuthInfo *auth) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:180`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1802 | `AccumuloServerFacadeV2` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:182`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1803 | `AccumuloServerFacadeV2` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1804 | `AccumuloServerFacadeV2` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:188`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1805 | `AccumuloServerFacadeV2` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1806 | `AccumuloServerFacadeV2` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:193`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1807 | `AccumuloServerFacadeV2` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:196`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1808 | `AccumuloServerFacadeV2` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, cclient::data::SystemPermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:199`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1809 | `AccumuloServerFacadeV2` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &table, cclient::data::TablePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:201`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1810 | `AccumuloServerFacadeV2` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string user, const std::string &nsp, cclient::data::NamespacePermissions perm) override;` (`include/interconnect/accumulo/AccumuloServerTwo.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1811 | `AccumuloStreams` constructor `AccumuloStreams( std::shared_ptr<cclient::data::Instance> instance, TableOperations<cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>> *tops, cclient::data::security::Authorizations *auths, uint16_t threads);` (`include/interconnect/accumulo/AccumuloStreams.h:24`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1812 | `AccumuloStreams` destructor `virtual ~AccumuloStreams();` (`include/interconnect/accumulo/AccumuloStreams.h:29`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloStreams` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1813 | `AccumuloBaseConnector` constructor `AccumuloBaseConnector()` (`include/interconnect/AccumuloConnector.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1814 | `AccumuloBaseConnector` constructor `AccumuloBaseConnector(const std::string &host, const int port)` (`include/interconnect/AccumuloConnector.h:53`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1815 | `AccumuloBaseConnector` destructor `virtual ~AccumuloBaseConnector() {}` (`include/interconnect/AccumuloConnector.h:62`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloBaseConnector` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1816 | `AccumuloBaseConnector` method `cclient::data::security::AuthInfo *getCredentials() { return &credentials; }` (`include/interconnect/AccumuloConnector.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1817 | `AccumuloBaseConnector` method `std::shared_ptr<cclient::data::tserver::RangeDefinition> getRangesDefinition() {` (`include/interconnect/AccumuloConnector.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1818 | `ClientInterface` constructor `explicit ClientInterface(const std::string &host, const int port);` (`include/interconnect/ClientInterface.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1819 | `ClientInterface` constructor `ClientInterface() : server_port(9997), authenticated(false) {}` (`include/interconnect/ClientInterface.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1820 | `ClientInterface` destructor `virtual ~ClientInterface();` (`include/interconnect/ClientInterface.h:43`) | — | — | — | Missing Go | Assessed individually: `interconnect::ClientInterface` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1821 | `ClientInterface` method `virtual void authenticate(const std::string &username, const std::string &password) = 0;` (`include/interconnect/ClientInterface.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1822 | `ClientInterface` method `void authenticate(const cclient::data::security::AuthInfo *const auth) {` (`include/interconnect/ClientInterface.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1823 | `ClientInterface` method `void setTransport(const std::shared_ptr<Tr> &transporty) {` (`include/interconnect/ClientInterface.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1824 | `ClientInterface` method `bool setAuthenticated(bool auth) {` (`include/interconnect/ClientInterface.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1825 | `ClientInterface` method `void setCredentials(const std::string &user, const std::string &password) {` (`include/interconnect/ClientInterface.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1826 | `ClientInterface` method `std::shared_ptr<Tr> getTransport() { return transport; }` (`include/interconnect/ClientInterface.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1827 | `APIException` class (no public members) `class APIException : public std::exception {` (`include/interconnect/exceptions/APIException.h:23`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-ERR-012](#sec-18) already carries this obligation — that row cites this exact declaration (`include/interconnect/exceptions/APIException.h:23`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1828 | `(namespace scope)` macro constant `#define TSERVER_PORT_OPT "tserver.port.client"` (`include/interconnect/InterConnect.h:47`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1829 | `(namespace scope)` macro constant `#define TSERVER_DEFAULT_PORT 9997` (`include/interconnect/InterConnect.h:48`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1830 | `(namespace scope)` macro constant `#define GENERAL_RPC_TIMEOUT_OPT "general.rpc.timeout"` (`include/interconnect/InterConnect.h:50`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1831 | `(namespace scope)` macro constant `#define GENERAL_RPC_TIMEOUT 120 * 1000` (`include/interconnect/InterConnect.h:51`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1832 | `INTERCONNECT_TYPES` enum `enum INTERCONNECT_TYPES { TSERV_CLIENT, MASTER_CLIENT, GC_CLIENT };` (`include/interconnect/InterConnect.h:53`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: TSERV_CLIENT = 0; MASTER_CLIENT = 1; GC_CLIENT = 2. Shoal exposes no equivalent type. |
| SB-CXX-1833 | `(namespace scope)` macro constant `#define SERVICE_SEPARATOR ';'` (`include/interconnect/InterConnect.h:55`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1834 | `(namespace scope)` macro constant `#define SEPARATOR_CHAR '='` (`include/interconnect/InterConnect.h:56`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1835 | `EnumParser` constructor `EnumParser() {}` (`include/interconnect/InterConnect.h:64`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1836 | `EnumParser` method `T ParseSomeEnum(const std::string &value) {` (`include/interconnect/InterConnect.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1837 | `TYPE_PARSER` constructor `TYPE_PARSER() {` (`include/interconnect/InterConnect.h:76`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1838 | `ConnectorService` constructor `ConnectorService(std::string service, std::string server, uint16_t port) {` (`include/interconnect/InterConnect.h:98`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1839 | `ConnectorService` method `std::string getAddressString(INTERCONNECT_TYPES type) {` (`include/interconnect/InterConnect.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1840 | `(namespace scope)` macro constant `#define ERROR_THRESHOLD 20L` (`include/interconnect/InterConnect.h:112`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1841 | `(namespace scope)` macro constant `#define STUCK_THRESHOLD 2 * 60 * 1000` (`include/interconnect/InterConnect.h:113`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-1842 | `(namespace scope)` namespace-scope static variable `static TransportPool<interconnect::AccumuloCoordinatorTransporter> MASTER_COORDINATOR;` (`include/interconnect/Master.h:50`) | — | — | — | Missing Go | Namespace-scope variable declared `static`, so it has **internal linkage**: every translation unit that includes this header gets its own independent copy, initialised separately. It is not a process-wide object and consumers cannot link against one shared instance — any port that replaces it with a single shared value changes observable behavior, and any port that keeps per-consumer state matches it. Shoal exposes no equivalent. connector, transport, or table-operation surface. |
| SB-CXX-1843 | `MasterConnect` constructor `explicit MasterConnect(cclient::data::security::AuthInfo &credentials, std::shared_ptr<cclient::data::Instance> instance);` (`include/interconnect/Master.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1844 | `MasterConnect` constructor `explicit MasterConnect(cclient::data::security::AuthInfo *credentials, std::shared_ptr<cclient::data::Instance> instance) : MasterConnect(*credentials, instance) {}` (`include/interconnect/Master.h:68`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1845 | `MasterConnect` constructor `MasterConnect() : instance(nullptr) {}` (`include/interconnect/Master.h:72`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1846 | `MasterConnect` method `std::unique_ptr<AccumuloTableOperations> tableOps(const std::string &table);` (`include/interconnect/Master.h:79`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1847 | `MasterConnect` method `std::unique_ptr<NamespaceOperations> namespaceOps(const std::string &nm = "");` (`include/interconnect/Master.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1848 | `MasterConnect` method `std::unique_ptr<SecurityOperations> securityOps();` (`include/interconnect/Master.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1849 | `MasterConnect` destructor `virtual ~MasterConnect();` (`include/interconnect/Master.h:94`) | — | — | — | Missing Go | Assessed individually: `interconnect::MasterConnect` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1850 | `MasterConnect` operator `MasterConnect &operator=(const MasterConnect &other) {` (`include/interconnect/Master.h:96`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-1851 | `NamespaceOperations` constructor `explicit NamespaceOperations( cclient::data::security::AuthInfo *creds, std::shared_ptr<cclient::data::Instance> instance, RootInterface<interconnect::AccumuloCoordinatorTransporter, cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>> *interface, TransportPool<interconnect::AccumuloCoordinatorTransporter> *distributedConnector) : NamespaceOperations(creds, "", instance, interface, distributedConnector) {` (`include/interconnect/namespaceOps/NamespaceOperations.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1852 | `NamespaceOperations` constructor `explicit NamespaceOperations( cclient::data::security::AuthInfo *creds, std::string myNamespace, std::shared_ptr<cclient::data::Instance> instance, RootInterface<interconnect::AccumuloCoordinatorTransporter, cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>> *interface, TransportPool<interconnect::AccumuloCoordinatorTransporter> *distributedConnector) : myNamespace(myNamespace), credentials(creds), myInstance(instance), clientInterface(interface) {` (`include/interconnect/namespaceOps/NamespaceOperations.h:65`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1853 | `NamespaceOperations` method `std::vector<std::string> list();` (`include/interconnect/namespaceOps/NamespaceOperations.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1854 | `NamespaceOperations` destructor `virtual ~NamespaceOperations();` (`include/interconnect/namespaceOps/NamespaceOperations.h:91`) | — | — | — | Missing Go | Assessed individually: `interconnect::NamespaceOperations` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1855 | `NamespaceOperations` method `std::string systemNamespace();` (`include/interconnect/namespaceOps/NamespaceOperations.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1856 | `NamespaceOperations` method `std::string defaultNamespace() { return ""; }` (`include/interconnect/namespaceOps/NamespaceOperations.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1857 | `NamespaceOperations` method `bool remove(std::string name = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:110`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1858 | `NamespaceOperations` method `bool exists(std::string name = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:117`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1859 | `NamespaceOperations` method `void create(std::string name = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1860 | `NamespaceOperations` method `void rename(std::string newName, std::string oldName = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1861 | `NamespaceOperations` method `virtual void setProperty(std::string property, std::string value, std::string nm = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1862 | `NamespaceOperations` method `virtual void removeProperty(std::string property, std::string nm = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:144`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1863 | `NamespaceOperations` method `virtual std::map<std::string, std::string> getProperties(std::string nm = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1864 | `NamespaceOperations` method `virtual void attachIterator(cclient::data::IterInfo setting, cclient::data::ITERATOR_TYPES scope, std::string nm = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1865 | `NamespaceOperations` method `virtual void removeIterator(std::string name, cclient::data::ITERATOR_TYPES scope, std::string nm = "");` (`include/interconnect/namespaceOps/NamespaceOperations.h:168`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1866 | `PythonConnector` class (no public members) `class PythonConnector {` (`include/interconnect/python/PythonConnector.h:24`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. connector, transport, or table-operation surface. |
| SB-CXX-1867 | `PythonTableInfo` constructor `explicit PythonTableInfo(const std::shared_ptr<AccumuloTableInfo> &tops) : table_info(tops) {}` (`include/interconnect/python/PythonStructures.h:27`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1868 | `PythonTableInfo` method `bool exists(const std::string table) { return table_info->exists(table); }` (`include/interconnect/python/PythonStructures.h:34`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-019](#sec-11) already carries this obligation — `interconnect::PythonTableInfo::exists` is the overload bound at `pysharkbite.cpp:256` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1869 | `PythonTableInfo` method `std::string getTableId(const std::string &table) {` (`include/interconnect/python/PythonStructures.h:40`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-016](#sec-11) already carries this obligation — `interconnect::PythonTableInfo::getTableId` is the overload bound at `pysharkbite.cpp:253` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1870 | `PythonTableInfo` method `std::string getTableName(const std::string &tableid) {` (`include/interconnect/python/PythonStructures.h:49`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-017](#sec-11) already carries this obligation — `interconnect::PythonTableInfo::getTableName` is the overload bound at `pysharkbite.cpp:254` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1871 | `PythonTableInfo` method `std::set<std::string> getTables() const { return table_info->getTables(); }` (`include/interconnect/python/PythonStructures.h:57`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-018](#sec-11) already carries this obligation — `interconnect::PythonTableInfo::getTables` is the overload bound at `pysharkbite.cpp:255` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1872 | `PythonTableOperations` constructor `explicit PythonTableOperations( const std::shared_ptr<AccumuloTableOperations> &tops) : table_opts(tops) {}` (`include/interconnect/python/PythonStructures.h:65`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1873 | `PythonTableOperations` method `bool create(bool recreate = false) { return table_opts->create(recreate); }` (`include/interconnect/python/PythonStructures.h:74`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-001](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::create` is the overload bound at `pysharkbite.cpp:250` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1874 | `PythonTableOperations` method `bool remove() { return table_opts->remove(); }` (`include/interconnect/python/PythonStructures.h:80`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-003](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::remove` is the overload bound at `pysharkbite.cpp:239` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1875 | `PythonTableOperations` method `bool exists(bool createIfNot = false) {` (`include/interconnect/python/PythonStructures.h:87`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-002](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::exists` is the overload bound at `pysharkbite.cpp:240` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1876 | `PythonTableOperations` method `bool import(std::string dir, std::string fail_path, bool setTime = false) {` (`include/interconnect/python/PythonStructures.h:98`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-013](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::import` is the overload bound at `pysharkbite.cpp:241` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1877 | `PythonTableOperations` method `int8_t flush(std::string startRow, std::string endRow, bool wait) {` (`include/interconnect/python/PythonStructures.h:111`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-005](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::flush` is the overload bound at `pysharkbite.cpp:242` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1878 | `PythonTableOperations` method `int8_t compact(std::string startRow, std::string endRow, bool wait) {` (`include/interconnect/python/PythonStructures.h:124`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-006](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::compact` is the overload bound at `pysharkbite.cpp:243` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1879 | `PythonTableOperations` method `int8_t setProperty(std::string property, std::string value) {` (`include/interconnect/python/PythonStructures.h:135`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-007](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::setProperty` is the overload bound at `pysharkbite.cpp:244` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1880 | `PythonTableOperations` method `int8_t removeProperty(std::string property) {` (`include/interconnect/python/PythonStructures.h:145`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-008](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::removeProperty` is the overload bound at `pysharkbite.cpp:245` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1881 | `PythonTableOperations` method `void addSplits(std::set<std::string> partitions) {` (`include/interconnect/python/PythonStructures.h:153`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-010](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::addSplits` is the overload bound at `pysharkbite.cpp:246` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1882 | `PythonTableOperations` method `int addConstraint(std::string constraintClassName) {` (`include/interconnect/python/PythonStructures.h:162`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-TABLE-012](#sec-11) already carries this obligation — `interconnect::PythonTableOperations::addConstraint` is the overload bound at `pysharkbite.cpp:247` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1883 | `PythonTableOperations` method `auto createScanner(cclient::data::security::Authorizations *auths, uint16_t threads) {` (`include/interconnect/python/PythonStructures.h:172`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-038](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/python/PythonStructures.h:172`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1884 | `PythonTableOperations` method `auto createWriter(cclient::data::security::Authorizations *auths, uint16_t threads) {` (`include/interconnect/python/PythonStructures.h:177`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-039](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/python/PythonStructures.h:177`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1885 | `PythonNamespaceOperations` constructor `explicit PythonNamespaceOperations( const std::shared_ptr<NamespaceOperations> &tops) : namespace_ops(tops) {}` (`include/interconnect/python/PythonStructures.h:188`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1886 | `PythonNamespaceOperations` method `std::vector<std::string> list() { return namespace_ops->list(); }` (`include/interconnect/python/PythonStructures.h:196`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-001](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::list` is the overload bound at `pysharkbite.cpp:230` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1887 | `PythonNamespaceOperations` method `bool remove(std::string name = "") { return namespace_ops->remove(name); }` (`include/interconnect/python/PythonStructures.h:202`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-003](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::remove` is the overload bound at `pysharkbite.cpp:231` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1888 | `PythonNamespaceOperations` method `bool exists(std::string name = "") { return namespace_ops->exists(name); }` (`include/interconnect/python/PythonStructures.h:209`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-004](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::exists` is the overload bound at `pysharkbite.cpp:232` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1889 | `PythonNamespaceOperations` method `void create(std::string name = "") { return namespace_ops->create(name); }` (`include/interconnect/python/PythonStructures.h:215`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-002](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::create` is the overload bound at `pysharkbite.cpp:236` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1890 | `PythonNamespaceOperations` method `void rename(std::string newName, std::string oldName = "") {` (`include/interconnect/python/PythonStructures.h:222`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-005](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::rename` is the overload bound at `pysharkbite.cpp:233` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1891 | `PythonNamespaceOperations` method `void setProperty(std::string property, std::string value, std::string nm = "") {` (`include/interconnect/python/PythonStructures.h:231`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-006](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::setProperty` is the overload bound at `pysharkbite.cpp:234` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1892 | `PythonNamespaceOperations` method `void removeProperty(std::string property, std::string nm = "") {` (`include/interconnect/python/PythonStructures.h:240`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-NS-007](#sec-12) already carries this obligation — `interconnect::PythonNamespaceOperations::removeProperty` is the overload bound at `pysharkbite.cpp:235` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1893 | `PythonSecurityOperations` constructor `explicit PythonSecurityOperations( const std::shared_ptr<SecurityOperations> &tops) : security_ops(tops) {}` (`include/interconnect/python/PythonStructures.h:250`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1894 | `PythonSecurityOperations` method `int8_t createUser(std::string user, std::string password) {` (`include/interconnect/python/PythonStructures.h:263`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-001](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::createUser` is the overload bound at `pysharkbite.cpp:595` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1895 | `PythonSecurityOperations` method `int8_t changeUserPassword(std::string user, std::string password) {` (`include/interconnect/python/PythonStructures.h:276`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-002](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::changeUserPassword` is the overload bound at `pysharkbite.cpp:596` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1896 | `PythonSecurityOperations` method `int8_t dropUser(std::string user) { return security_ops->dropUser(user); }` (`include/interconnect/python/PythonStructures.h:288`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-003](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::dropUser` is the overload bound at `pysharkbite.cpp:597` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1897 | `PythonSecurityOperations` method `std::unique_ptr<cclient::data::security::Authorizations> getAuths( std::string user) {` (`include/interconnect/python/PythonStructures.h:295`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-004](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::getAuths` is the overload bound at `pysharkbite.cpp:598` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1898 | `PythonSecurityOperations` method `int8_t grantAuthorizations(cclient::data::security::Authorizations *auths, std::string user) {` (`include/interconnect/python/PythonStructures.h:310`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-005](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::grantAuthorizations` is the overload bound at `pysharkbite.cpp:608` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1899 | `PythonSecurityOperations` method `bool hasSystemPermission(const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/python/PythonStructures.h:321`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-006](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::hasSystemPermission` is the overload bound at `pysharkbite.cpp:599` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1900 | `PythonSecurityOperations` method `bool hasTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/python/PythonStructures.h:333`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-007](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::hasTablePermission` is the overload bound at `pysharkbite.cpp:600` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1901 | `PythonSecurityOperations` method `bool hasNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/python/PythonStructures.h:345`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-008](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::hasNamespacePermission` is the overload bound at `pysharkbite.cpp:601` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1902 | `PythonSecurityOperations` method `int8_t grantSystemPermission(const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/python/PythonStructures.h:358`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-009](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::grantSystemPermission` is the overload bound at `pysharkbite.cpp:602` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1903 | `PythonSecurityOperations` method `int8_t revokeSystemPermission(const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/python/PythonStructures.h:370`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-010](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::revokeSystemPermission` is the overload bound at `pysharkbite.cpp:603` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1904 | `PythonSecurityOperations` method `int8_t grantTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/python/PythonStructures.h:384`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-011](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::grantTablePermission` is the overload bound at `pysharkbite.cpp:604` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1905 | `PythonSecurityOperations` method `int8_t revokeTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/python/PythonStructures.h:397`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-012](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::revokeTablePermission` is the overload bound at `pysharkbite.cpp:605` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1906 | `PythonSecurityOperations` method `int8_t grantNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/python/PythonStructures.h:412`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-013](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::grantNamespacePermission` is the overload bound at `pysharkbite.cpp:606` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1907 | `PythonSecurityOperations` method `int8_t revokeNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/python/PythonStructures.h:426`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SEC-014](#sec-13) already carries this obligation — `interconnect::PythonSecurityOperations::revokeNamespacePermission` is the overload bound at `pysharkbite.cpp:607` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-1908 | `RootInterface` constructor `explicit RootInterface(cclient::data::security::AuthInfo &creds, cclient::data::Instance *instance) : AccumuloBaseConnector<T>() {}` (`include/interconnect/RootInterface.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1909 | `RootInterface` constructor `explicit RootInterface(cclient::data::security::AuthInfo &creds, std::shared_ptr<cclient::data::Instance> instance) : AccumuloBaseConnector<T>() {}` (`include/interconnect/RootInterface.h:55`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1910 | `RootInterface` destructor `virtual ~RootInterface() {}` (`include/interconnect/RootInterface.h:59`) | — | — | — | Missing Go | Assessed individually: `interconnect::RootInterface` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1911 | `RootInterface` method `virtual void authenticate(const std::string &username, const std::string &password) override {}` (`include/interconnect/RootInterface.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1912 | `Scan` constructor `explicit Scan(std::atomic<bool> *isRunning);` (`include/interconnect/Scan.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1913 | `Scan` destructor `~Scan();` (`include/interconnect/Scan.h:44`) | — | — | — | Missing Go | Assessed individually: `interconnect::Scan` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1914 | `Scan` method `bool setNextResults( std::vector<std::shared_ptr<cclient::data::KeyValue> > *resultSet);` (`include/interconnect/Scan.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1915 | `Scan` method `bool isClientRunning();` (`include/interconnect/Scan.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1916 | `Scan` method `void setMultiIterator( std::shared_ptr<cclient::data::streams::KeyValueIterator> &itr);` (`include/interconnect/Scan.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1917 | `Scan` method `std::shared_ptr<cclient::data::streams::KeyValueIterator> getMultiIterator();` (`include/interconnect/Scan.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1918 | `Scan` method `bool getNextResults( std::vector<std::shared_ptr<cclient::data::KeyValue> > *resultSet);` (`include/interconnect/Scan.h:65`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1919 | `Scan` method `void setHasMore(bool more);` (`include/interconnect/Scan.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1920 | `Scan` method `bool getHasMore() const;` (`include/interconnect/Scan.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1921 | `Scan` method `int64_t getId() const;` (`include/interconnect/Scan.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1922 | `Scan` method `void setMultiScan(bool isMulti);` (`include/interconnect/Scan.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1923 | `Scan` method `bool isMultiScan() const;` (`include/interconnect/Scan.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1924 | `Scan` method `void setRFileScan(bool isRFile);` (`include/interconnect/Scan.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1925 | `Scan` method `bool isRFileScan() const;` (`include/interconnect/Scan.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1926 | `Scan` method `bool empty() const;` (`include/interconnect/Scan.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1927 | `Scan` method `void setException(std::string exc);` (`include/interconnect/Scan.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1928 | `Scan` method `std::string getException() const;` (`include/interconnect/Scan.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1929 | `Scan` method `bool hasException() const;` (`include/interconnect/Scan.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1930 | `Scan` method `void setScanId(int64_t scanId);` (`include/interconnect/Scan.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1931 | `Scan` method `void setTopKey(std::shared_ptr<cclient::data::Key> key);` (`include/interconnect/Scan.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1932 | `Scan` method `std::shared_ptr<cclient::data::Key> getTopKey() const;` (`include/interconnect/Scan.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1933 | `ScanArbiter` constructor `explicit ScanArbiter(uint16_t desired, bool disableRpc);` (`include/interconnect/ScanArbiter.h:37`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1934 | `ScanArbiter` destructor `~ScanArbiter();` (`include/interconnect/ScanArbiter.h:39`) | — | — | — | Missing Go | Assessed individually: `interconnect::ScanArbiter` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1935 | `ScanArbiter` method `Scan* wait();` (`include/interconnect/ScanArbiter.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1936 | `ScanArbiter` method `void add(Scan* scan);` (`include/interconnect/ScanArbiter.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1937 | `ScannerInterface` constructor `ScannerInterface();` (`include/interconnect/ScannerInterface.h:26`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1938 | `ScannerInterface` method `virtual Scan beginScan() = 0;` (`include/interconnect/ScannerInterface.h:28`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1939 | `ScannerInterface` destructor `virtual ~ScannerInterface();` (`include/interconnect/ScannerInterface.h:29`) | — | — | — | Missing Go | Assessed individually: `interconnect::ScannerInterface` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1940 | `CopyKey` operator `typename T::first_type operator()(T keyValuePair) const {` (`include/interconnect/scanrequest/ScanIdentifier.h:24`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-1941 | `ScanIdentifier` constructor `ScanIdentifier() : identSize(0) {}` (`include/interconnect/scanrequest/ScanIdentifier.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1942 | `ScanIdentifier` method `size_t size() const { return identSize; }` (`include/interconnect/scanrequest/ScanIdentifier.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1943 | `ScanIdentifier` method `std::vector<V> getIdentifiers(M m) { return globalIdentifierMapping[m]; }` (`include/interconnect/scanrequest/ScanIdentifier.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1944 | `ScanIdentifier` method `void putIdentifier(M m, V v) {` (`include/interconnect/scanrequest/ScanIdentifier.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1945 | `ScanIdentifier` method `std::vector<M> getGlobalMapping() {` (`include/interconnect/scanrequest/ScanIdentifier.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1946 | `ScanRequest` constructor `ScanRequest(cclient::data::security::AuthInfo *credentials, cclient::data::security::Authorizations *auths, std::shared_ptr<ServerConnection> server)` (`include/interconnect/scanrequest/ScanRequest.h:37`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1947 | `ScanRequest` destructor `virtual ~ScanRequest() {` (`include/interconnect/scanrequest/ScanRequest.h:50`) | — | — | — | Missing Go | Assessed individually: `interconnect::ScanRequest` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1948 | `ScanRequest` method `void setBufferSize(uint32_t size) { singlebufferSize = size; }` (`include/interconnect/scanrequest/ScanRequest.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1949 | `ScanRequest` method `virtual void setIters(const std::vector<cclient::data::IterInfo> &iters) {` (`include/interconnect/scanrequest/ScanRequest.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1950 | `ScanRequest` method `void addColumn(cclient::data::Column *col) { columns.push_back(*col); }` (`include/interconnect/scanrequest/ScanRequest.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1951 | `ScanRequest` method `void addColumn(const cclient::data::Column &col) { columns.push_back(col); }` (`include/interconnect/scanrequest/ScanRequest.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1952 | `ScanRequest` method `void addColumns(const std::vector<cclient::data::Column> &cols) {` (`include/interconnect/scanrequest/ScanRequest.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1953 | `ScanRequest` method `std::vector<cclient::data::Column> getColumns() const { return columns; }` (`include/interconnect/scanrequest/ScanRequest.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1954 | `ScanRequest` method `cclient::data::security::AuthInfo *getCredentials() const { return creds; }` (`include/interconnect/scanrequest/ScanRequest.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1955 | `ScanRequest` method `cclient::data::security::Authorizations *getAuthorizations() const {` (`include/interconnect/scanrequest/ScanRequest.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1956 | `ScanRequest` method `uint32_t getBuffersize() { return singlebufferSize; }` (`include/interconnect/scanrequest/ScanRequest.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1957 | `ScanRequest` method `std::vector<cclient::data::IterInfo> getIterators() { return iterators; }` (`include/interconnect/scanrequest/ScanRequest.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1958 | `ScanRequest` method `void putIdentifier(I *ident) { identifiers.push_back(ident); }` (`include/interconnect/scanrequest/ScanRequest.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1959 | `ScanRequest` method `std::vector<I *> *getRangeIdentifiers() { return &identifiers; }` (`include/interconnect/scanrequest/ScanRequest.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1960 | `ScanRequest` method `size_t size() const { return identifiers.size(); }` (`include/interconnect/scanrequest/ScanRequest.h:86`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1961 | `SecurityOperations` constructor `SecurityOperations( cclient::data::security::AuthInfo *creds, std::shared_ptr<cclient::data::Instance> instance, std::shared_ptr< CachedTransport<interconnect::AccumuloCoordinatorTransporter>> interface, TransportPool<interconnect::AccumuloCoordinatorTransporter> *distributedConnector)` (`include/interconnect/securityOps/SecurityOperations.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1962 | `SecurityOperations` destructor `~SecurityOperations();` (`include/interconnect/securityOps/SecurityOperations.h:66`) | — | — | — | Missing Go | Assessed individually: `interconnect::SecurityOperations` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1963 | `SecurityOperations` method `int8_t createUser(std::string user, std::string password);` (`include/interconnect/securityOps/SecurityOperations.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1964 | `SecurityOperations` method `int8_t changeUserPassword(std::string user, std::string password);` (`include/interconnect/securityOps/SecurityOperations.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1965 | `SecurityOperations` method `int8_t dropUser(std::string user);` (`include/interconnect/securityOps/SecurityOperations.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1966 | `SecurityOperations` method `std::unique_ptr<cclient::data::security::Authorizations> getAuths( std::string user);` (`include/interconnect/securityOps/SecurityOperations.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1967 | `SecurityOperations` method `int8_t grantAuthorizations(cclient::data::security::Authorizations *auths, std::string user);` (`include/interconnect/securityOps/SecurityOperations.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1968 | `SecurityOperations` method `bool hasSystemPermission(const std::string &user, cclient::data::SystemPermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:127`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1969 | `SecurityOperations` method `bool hasTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1970 | `SecurityOperations` method `bool hasNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:147`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1971 | `SecurityOperations` method `int8_t grantSystemPermission(const std::string &user, cclient::data::SystemPermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:158`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1972 | `SecurityOperations` method `int8_t revokeSystemPermission(const std::string &user, cclient::data::SystemPermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:168`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1973 | `SecurityOperations` method `int8_t grantTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:180`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1974 | `SecurityOperations` method `int8_t revokeTablePermission(const std::string &user, const std::string &table, cclient::data::TablePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1975 | `SecurityOperations` method `int8_t grantNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1976 | `SecurityOperations` method `int8_t revokeNamespacePermission(const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm);` (`include/interconnect/securityOps/SecurityOperations.h:216`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1977 | `AccumuloTableInfo` constructor `explicit AccumuloTableInfo(cclient::data::security::AuthInfo *creds, std::shared_ptr<cclient::data::Instance> instance) : credentials(creds), myInstance(instance), logger(logging::LoggerFactory<AccumuloTableInfo>::getLogger()) {` (`include/interconnect/tableOps/ClientTableInfo.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1978 | `AccumuloTableInfo` destructor `virtual ~AccumuloTableInfo() {}` (`include/interconnect/tableOps/ClientTableInfo.h:50`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloTableInfo` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1979 | `AccumuloTableInfo` method `bool exists(const std::string &table) {` (`include/interconnect/tableOps/ClientTableInfo.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1980 | `AccumuloTableInfo` method `std::string getTableId(const std::string &table) {` (`include/interconnect/tableOps/ClientTableInfo.h:64`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1981 | `AccumuloTableInfo` method `std::string getTableName(const std::string &tableid) {` (`include/interconnect/tableOps/ClientTableInfo.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1982 | `AccumuloTableInfo` method `std::set<std::string> getTables() const {` (`include/interconnect/tableOps/ClientTableInfo.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1983 | `AccumuloTableOperations` constructor `explicit AccumuloTableOperations( cclient::data::security::AuthInfo *creds, std::shared_ptr<cclient::data::Instance> instance, std::string table, RootInterface<interconnect::AccumuloCoordinatorTransporter, cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>> *interface, std::shared_ptr< CachedTransport<interconnect::AccumuloCoordinatorTransporter>> tserverConn, TransportPool<interconnect::AccumuloCoordinatorTransporter> *distributedConnector) : TableOperations<cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>>( creds, instance, table), clientInterface(interface), tserverConn(tserverConn), distributedConnector(distributedConnector), logger(logging::LoggerFactory<AccumuloTableOperations>::getLogger()) {` (`include/interconnect/tableOps/ClientTableOps.h:44`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1984 | `AccumuloTableOperations` destructor `virtual ~AccumuloTableOperations();` (`include/interconnect/tableOps/ClientTableOps.h:65`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloTableOperations` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-1985 | `AccumuloTableOperations` method `virtual bool create(bool recreate = false) override;` (`include/interconnect/tableOps/ClientTableOps.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1986 | `AccumuloTableOperations` method `virtual bool remove() override;` (`include/interconnect/tableOps/ClientTableOps.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1987 | `AccumuloTableOperations` method `virtual bool exists(bool createIfNot = false) override;` (`include/interconnect/tableOps/ClientTableOps.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1988 | `AccumuloTableOperations` method `virtual bool import(std::string dir, std::string fail_path, bool setTime = false);` (`include/interconnect/tableOps/ClientTableOps.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1989 | `AccumuloTableOperations` method `virtual int8_t flush(std::string startRow, std::string endRow, bool wait);` (`include/interconnect/tableOps/ClientTableOps.h:106`) | `Connector.FlushTableRange(ctx, tableName string, startRow, endRow []byte, wait bool) error` (`accumulo/table_flush.go:33`) | `shoal_connector_flush_table_range` (`capi/include/shoal.h`) | `TestFlushTableRangeSendsRowBounds` (`accumulo/table_constraints_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestABIDiscoveryQueriesAreConcurrentAndStable` (`cmd/shoal-capi/abi_export_test.go`) | Covered | Exact Go counterpart of the row-bounded flush, added with [SB-TABLE-005](#sec-11). The `int8` status becomes an error, which carries the reason the status code discards. Revision 29 exposes the owned, deadline-aware ABI with connector-close coordination and allocation-failure coverage. |
| SB-CXX-1990 | `AccumuloTableOperations` method `virtual int8_t compact(std::string startRow, std::string endRow, bool wait) override;` (`include/interconnect/tableOps/ClientTableOps.h:117`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1991 | `AccumuloTableOperations` method `std::string getTableId() override;` (`include/interconnect/tableOps/ClientTableOps.h:124`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1992 | `AccumuloTableOperations` method `virtual int8_t setProperty(std::string property, std::string value) override;` (`include/interconnect/tableOps/ClientTableOps.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1993 | `AccumuloTableOperations` method `virtual int8_t removeProperty(std::string property) override;` (`include/interconnect/tableOps/ClientTableOps.h:141`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1994 | `AccumuloTableOperations` method `virtual std::map<std::string, std::string> getProperties() override;` (`include/interconnect/tableOps/ClientTableOps.h:147`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1995 | `AccumuloTableOperations` method `std::vector<std::string> listSplits();` (`include/interconnect/tableOps/ClientTableOps.h:153`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1996 | `AccumuloTableOperations` method `void addSplits(std::set<std::string> partitions);` (`include/interconnect/tableOps/ClientTableOps.h:159`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1997 | `AccumuloTableOperations` method `int addConstraint(std::string constraintClassName);` (`include/interconnect/tableOps/ClientTableOps.h:166`) | `Connector.AddConstraint(ctx, tableName, className string) (int32, error)` (`accumulo/table_constraints.go:57`) | `shoal_connector_add_table_constraint` (`capi/include/shoal.h`) | `TestAddConstraintAssignsTheLowestFreeNumber` (`accumulo/table_constraints_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestABIDiscoveryQueriesAreConcurrentAndStable` (`cmd/shoal-capi/abi_export_test.go`) | Covered | Exact Go counterpart, added with [SB-TABLE-012](#sec-11) — and, unlike the pinned member, it installs the constraint ([SB-UNSAFE-036](#sec-21)). Revision 29 exposes the owned, deadline-aware ABI with connector-close coordination and allocation-failure coverage. |
| SB-CXX-1998 | `AccumuloTableOperations` method `std::unique_ptr<scanners::Source< cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>>> createScanner(cclient::data::security::Authorizations *auths, uint16_t threads) override;` (`include/interconnect/tableOps/ClientTableOps.h:176`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-1999 | `AccumuloTableOperations` method `std::unique_ptr<writer::Sink<cclient::data::KeyValue>> createWriter( cclient::data::security::Authorizations *auths, uint16_t threads) override;` (`include/interconnect/tableOps/ClientTableOps.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2000 | `AccumuloTableOperations` method `std::shared_ptr<scanners::Source< cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>>> createSharedScanner(cclient::data::security::Authorizations *auths, uint16_t threads) override;` (`include/interconnect/tableOps/ClientTableOps.h:197`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2001 | `AccumuloTableOperations` method `std::shared_ptr<writer::Sink<cclient::data::KeyValue>> createSharedWriter( cclient::data::security::Authorizations *auths, uint16_t threads) override;` (`include/interconnect/tableOps/ClientTableOps.h:206`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2002 | `(namespace scope)` namespace-scope variable `extern std::map<std::string, std::string> nameSpaceIds;` (`include/interconnect/tableOps/ClientTableOps.h:234`) | — | — | — | Missing Go | Namespace-scope variable with external linkage (`extern`), so consumers link against this one object and it is part of the ABI as well as the API. Shoal exposes no equivalent global. connector, transport, or table-operation surface. |
| SB-CXX-2003 | `Tables` method `static Tables &getInstance() {` (`include/interconnect/tableOps/TableOperations.h:40`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CONN-011](#sec-7) already carries this obligation — that row cites this exact declaration (`include/interconnect/tableOps/TableOperations.h:40`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2004 | `Tables` method `bool exists(const std::string table) const {` (`include/interconnect/tableOps/TableOperations.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2005 | `Tables` method `std::string getTableId(const std::string &table) const {` (`include/interconnect/tableOps/TableOperations.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2006 | `Tables` method `std::string getTableName(const std::string &tableid) const {` (`include/interconnect/tableOps/TableOperations.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2007 | `Tables` method `void setCachedTableName(const std::string tableName, const std::string tableid) {` (`include/interconnect/tableOps/TableOperations.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2008 | `Tables` method `void set(std::map<std::string, std::string> cachedTableIds, std::set<std::string> tableNames) {` (`include/interconnect/tableOps/TableOperations.h:93`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2009 | `Tables` method `std::set<std::string> getTables() const { return tableNames; }` (`include/interconnect/tableOps/TableOperations.h:100`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2010 | `TableOperations` constructor `TableOperations(cclient::data::security::AuthInfo *creds, std::shared_ptr<cclient::data::Instance> instance, std::string table);` (`include/interconnect/tableOps/TableOperations.h:112`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2011 | `TableOperations` method `void createTable(std::string table);` (`include/interconnect/tableOps/TableOperations.h:121`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2012 | `TableOperations` method `virtual std::unique_ptr<scanners::Source<K, V>> createScanner( cclient::data::security::Authorizations *auths, uint16_t threads) = 0;` (`include/interconnect/tableOps/TableOperations.h:129`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-008](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/tableOps/TableOperations.h:129`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2013 | `TableOperations` method `virtual std::unique_ptr<writer::Sink<K>> createWriter( cclient::data::security::Authorizations *auths, uint16_t threads) = 0;` (`include/interconnect/tableOps/TableOperations.h:138`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-037](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/tableOps/TableOperations.h:138`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2014 | `TableOperations` method `virtual std::shared_ptr<scanners::Source<K, V>> createSharedScanner( cclient::data::security::Authorizations *auths, uint16_t threads) = 0;` (`include/interconnect/tableOps/TableOperations.h:147`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-038](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/tableOps/TableOperations.h:147`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2015 | `TableOperations` method `virtual std::shared_ptr<writer::Sink<K>> createSharedWriter( cclient::data::security::Authorizations *auths, uint16_t threads) = 0;` (`include/interconnect/tableOps/TableOperations.h:156`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-039](#sec-19) already carries this obligation — that row cites this exact declaration (`include/interconnect/tableOps/TableOperations.h:156`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2016 | `TableOperations` destructor `virtual ~TableOperations();` (`include/interconnect/tableOps/TableOperations.h:159`) | — | — | — | Missing Go | Assessed individually: `interconnect::TableOperations` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2017 | `TableOperations` method `virtual std::string getTableId() = 0;` (`include/interconnect/tableOps/TableOperations.h:165`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2018 | `TableOperations` method `virtual bool create(bool recreate = false) = 0;` (`include/interconnect/tableOps/TableOperations.h:172`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2019 | `TableOperations` method `virtual bool remove() = 0;` (`include/interconnect/tableOps/TableOperations.h:178`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2020 | `TableOperations` method `virtual bool exists(bool createIfNot = false) = 0;` (`include/interconnect/tableOps/TableOperations.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2021 | `TableOperations` method `virtual int8_t setProperty(std::string property, std::string value) = 0;` (`include/interconnect/tableOps/TableOperations.h:192`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2022 | `TableOperations` method `virtual int8_t removeProperty(std::string property) = 0;` (`include/interconnect/tableOps/TableOperations.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2023 | `TableOperations` method `virtual int8_t compact(std::string startRow, std::string endRow, bool wait) = 0;` (`include/interconnect/tableOps/TableOperations.h:207`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2024 | `TableOperations` method `virtual std::map<std::string, std::string> getProperties() = 0;` (`include/interconnect/tableOps/TableOperations.h:214`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2025 | `TableOperations` method `virtual std::set<std::string> listTables() {` (`include/interconnect/tableOps/TableOperations.h:220`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2026 | `TableOperations` method `cclient::data::security::AuthInfo *getCredentials() { return credentials; }` (`include/interconnect/tableOps/TableOperations.h:228`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2027 | `(namespace scope)` macro constant `#define SIGNED_RIGHT_SHIFT_IS 5` (`include/interconnect/TabletServer.h:19`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-2028 | `(namespace scope)` macro constant `#define ARITHMETIC_RIGHT_SHIFT 5` (`include/interconnect/TabletServer.h:23`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-2029 | `(namespace scope)` namespace-scope static variable `static TransportPool<ThriftTransporter> CLUSTER_COORDINATOR;` (`include/interconnect/TabletServer.h:57`) | — | — | — | Missing Go | Namespace-scope variable declared `static`, so it has **internal linkage**: every translation unit that includes this header gets its own independent copy, initialised separately. It is not a process-wide object and consumers cannot link against one shared instance — any port that replaces it with a single shared value changes observable behavior, and any port that keeps per-consumer state matches it. Shoal exposes no equivalent. connector, transport, or table-operation surface. |
| SB-CXX-2030 | `(namespace scope)` namespace-scope function `extern void closeAllThriftConnections();` (`include/interconnect/TabletServer.h:59`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. connector, transport, or table-operation surface. |
| SB-CXX-2031 | `ServerInterconnect` constructor `explicit ServerInterconnect(const std::string &host, const int port, const cclient::impl::Configuration *conf, TransportPool<ThriftTransporter> *distributedConnector = &CLUSTER_COORDINATOR);` (`include/interconnect/TabletServer.h:79`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2032 | `ServerInterconnect` constructor `explicit ServerInterconnect( std::shared_ptr<cclient::data::tserver::RangeDefinition> rangeDef, const cclient::impl::Configuration *conf, TransportPool<ThriftTransporter> *distributedConnector = &CLUSTER_COORDINATOR);` (`include/interconnect/TabletServer.h:84`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2033 | `ServerInterconnect` method `Scan *hedgedScan( std::shared_ptr<interconnect::ScanArbiter> &arbiter, std::atomic<bool> *isRunning, const std::vector<cclient::data::Column> &cols, const std::vector<cclient::data::IterInfo> &serverSideIterators, cclient::data::IterInfo &versioningIterator, uint32_t batchSize = 1000, bool disableRpc = false);` (`include/interconnect/TabletServer.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2034 | `ServerInterconnect` method `Scan *scan(std::atomic<bool> *isRunning, const std::vector<cclient::data::Column> &cols, const std::vector<cclient::data::IterInfo> &serverSideIterators, uint32_t batchSize = 1000);` (`include/interconnect/TabletServer.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2035 | `ServerInterconnect` constructor `explicit ServerInterconnect( std::shared_ptr<cclient::data::tserver::ServerDefinition> rangeDef, const cclient::impl::Configuration *conf, TransportPool<ThriftTransporter> *distributedConnector = &CLUSTER_COORDINATOR);` (`include/interconnect/TabletServer.h:103`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2036 | `ServerInterconnect` method `Scan *scan(std::atomic<bool> *isRunning);` (`include/interconnect/TabletServer.h:109`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2037 | `ServerInterconnect` method `Scan *continueScan(Scan *scan);` (`include/interconnect/TabletServer.h:111`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2038 | `ServerInterconnect` method `std::shared_ptr<cclient::data::TabletServerMutations> write( std::shared_ptr<cclient::data::TabletServerMutations> mutations);` (`include/interconnect/TabletServer.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2039 | `ServerInterconnect` method `void halt() {}` (`include/interconnect/TabletServer.h:116`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2040 | `ServerInterconnect` method `void authenticate(cclient::data::security::AuthInfo *credentials);` (`include/interconnect/TabletServer.h:118`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2041 | `ServerInterconnect` method `virtual void authenticate(const std::string &username, const std::string &password) override {}` (`include/interconnect/TabletServer.h:120`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2042 | `ServerInterconnect` destructor `virtual ~ServerInterconnect();` (`include/interconnect/TabletServer.h:123`) | — | — | — | Missing Go | Assessed individually: `interconnect::ServerInterconnect` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2043 | `AccumuloCoordinatorTransporter` method `virtual void registerService(std::string instance, std::string clusterManagers) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:183`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2044 | `AccumuloCoordinatorTransporter` constructor `explicit AccumuloCoordinatorTransporter( std::shared_ptr<ServerConnection> conn) : interconnect::ThriftTransporter(conn, false), interconnect::ServerTransport<apache::thrift::transport::TTransport, cclient::data::KeyExtent, std::shared_ptr<cclient::data::Range>, std::shared_ptr<cclient::data::Mutation>>( conn), logger(logging::LoggerFactory< AccumuloCoordinatorTransporter>::getLogger()) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:189`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2045 | `AccumuloCoordinatorTransporter` method `bool createTable(cclient::data::security::AuthInfo *auth, const std::string &table) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2046 | `AccumuloCoordinatorTransporter` method `bool importDirectory(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &dir, std::string failure_dir, bool setTime) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:210`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2047 | `AccumuloCoordinatorTransporter` method `cclient::data::AccumuloInfo getStatistics( cclient::data::security::AuthInfo *auth) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:219`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2048 | `AccumuloCoordinatorTransporter` method `bool compactFallBack(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:227`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2049 | `AccumuloCoordinatorTransporter` method `bool compact(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:235`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2050 | `AccumuloCoordinatorTransporter` method `bool flush(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &startrow, const std::string &endrow, bool wait) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:243`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2051 | `AccumuloCoordinatorTransporter` method `bool removeTable(cclient::data::security::AuthInfo *auth, const std::string &table) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:251`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2052 | `AccumuloCoordinatorTransporter` method `void removeTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:257`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2053 | `AccumuloCoordinatorTransporter` method `void setTableProperty(cclient::data::security::AuthInfo *auth, const std::string &table, const std::string &property, const std::string &value) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:265`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2054 | `AccumuloCoordinatorTransporter` method `bool createNamespace(cclient::data::security::AuthInfo *auth, std::string name) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:275`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2055 | `AccumuloCoordinatorTransporter` method `bool deletenamespace(cclient::data::security::AuthInfo *auth, std::string name) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:281`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2056 | `AccumuloCoordinatorTransporter` method `bool renamenamespace(cclient::data::security::AuthInfo *auth, std::string oldName, std::string newName) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:287`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2057 | `AccumuloCoordinatorTransporter` method `void removeNamespaceProperty(cclient::data::security::AuthInfo *auth, std::string nameSpaceName, const std::string &property) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:294`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2058 | `AccumuloCoordinatorTransporter` method `void setNamespaceProperty(cclient::data::security::AuthInfo *auth, std::string nameSpaceName, const std::string &property, const std::string &value) {` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:303`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2059 | `AccumuloCoordinatorTransporter` destructor `virtual ~AccumuloCoordinatorTransporter() {}` (`include/interconnect/transport/AccumuloCoordinatorTransporter.h:313`) | — | — | — | Missing Go | Assessed individually: `interconnect::AccumuloCoordinatorTransporter` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2060 | `AccumuloServerFacade` constructor `explicit AccumuloServerFacade(const std::shared_ptr<ServerConnection> &conn) : ThriftTransporter(conn) {}` (`include/interconnect/transport/AccumuloServerTransport.h:60`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2061 | `AccumuloServerFacade` method `virtual void newTransporter( const std::shared_ptr<ServerConnection> &conn) override {}` (`include/interconnect/transport/AccumuloServerTransport.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2062 | `AccumuloServerFacade` method `virtual void registerService(const std::string &instance, const std::string &clusterManagers) override {}` (`include/interconnect/transport/AccumuloServerTransport.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2063 | `ThriftTransporter` constructor `explicit ThriftTransporter(const std::shared_ptr<ServerConnection> &conn, bool reg = true);` (`include/interconnect/transport/BaseTransport.h:180`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2064 | `ThriftTransporter` destructor `virtual ~ThriftTransporter();` (`include/interconnect/transport/BaseTransport.h:183`) | — | — | — | Missing Go | Assessed individually: `interconnect::ThriftTransporter` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2065 | `ThriftTransporter` method `std::map<std::string, std::string> getNamespaceConfiguration( cclient::data::security::AuthInfo *auth, const std::string &nameSpaceName);` (`include/interconnect/transport/BaseTransport.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2066 | `ThriftTransporter` method `apache::thrift::transport::TTransport getTransport() override;` (`include/interconnect/transport/BaseTransport.h:189`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2067 | `ThriftTransporter` method `virtual void authenticate(cclient::data::security::AuthInfo *auth) override;` (`include/interconnect/transport/BaseTransport.h:191`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2068 | `ThriftTransporter` method `void createIfClosed();` (`include/interconnect/transport/BaseTransport.h:192`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2069 | `ThriftTransporter` method `void closeAndCreateClient();` (`include/interconnect/transport/BaseTransport.h:194`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2070 | `ThriftTransporter` method `void createClientService(bool callRegistration = false);` (`include/interconnect/transport/BaseTransport.h:196`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2071 | `ThriftTransporter` method `virtual void registerService(const std::string &instance, const std::string &clusterManagers) override {` (`include/interconnect/transport/BaseTransport.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2072 | `ThriftTransporter` method `Scan *beginScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<cclient::data::KeyExtent>, std::shared_ptr<cclient::data::Range>>> *request) override {` (`include/interconnect/transport/BaseTransport.h:204`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2073 | `ThriftTransporter` method `Scan *continueScan(Scan *originalScan) {` (`include/interconnect/transport/BaseTransport.h:213`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2074 | `ThriftTransporter` method `void *write(cclient::data::security::AuthInfo *auth, std::map<cclient::data::KeyExtent, std::vector<std::shared_ptr<cclient::data::Mutation>>> *request) override {` (`include/interconnect/transport/BaseTransport.h:218`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2075 | `ThriftTransporter` method `bool dropUser(cclient::data::security::AuthInfo *auth, const std::string &user) {` (`include/interconnect/transport/BaseTransport.h:226`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2076 | `ThriftTransporter` method `bool changeUserPassword(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) {` (`include/interconnect/transport/BaseTransport.h:232`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2077 | `ThriftTransporter` method `bool createUser(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &password) {` (`include/interconnect/transport/BaseTransport.h:240`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2078 | `ThriftTransporter` method `std::map<std::string, std::string> getTableConfiguration( cclient::data::security::AuthInfo *auth, const std::string &table) {` (`include/interconnect/transport/BaseTransport.h:246`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2079 | `ThriftTransporter` method `cclient::data::security::Authorizations *getUserAuths( cclient::data::security::AuthInfo *auth, const std::string &user) {` (`include/interconnect/transport/BaseTransport.h:254`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2080 | `ThriftTransporter` method `void changeUserAuths(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::security::Authorizations *auths) {` (`include/interconnect/transport/BaseTransport.h:263`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2081 | `ThriftTransporter` method `void splitTablet(cclient::data::security::AuthInfo *auth, std::shared_ptr<cclient::data::KeyExtent> extent, const std::string &split) {` (`include/interconnect/transport/BaseTransport.h:270`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2082 | `ThriftTransporter` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/transport/BaseTransport.h:277`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2083 | `ThriftTransporter` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:283`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2084 | `ThriftTransporter` method `bool grant(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:289`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2085 | `ThriftTransporter` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/transport/BaseTransport.h:295`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2086 | `ThriftTransporter` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:301`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2087 | `ThriftTransporter` method `bool revoke(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:307`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2088 | `ThriftTransporter` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, cclient::data::SystemPermissions perm) {` (`include/interconnect/transport/BaseTransport.h:314`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2089 | `ThriftTransporter` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &table, cclient::data::TablePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:321`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2090 | `ThriftTransporter` method `bool hasPermission(cclient::data::security::AuthInfo *auth, const std::string &user, const std::string &nsp, cclient::data::NamespacePermissions perm) {` (`include/interconnect/transport/BaseTransport.h:329`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2091 | `ThriftTransporter` method `void close() override {` (`include/interconnect/transport/BaseTransport.h:336`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2092 | `ThriftTransporter` method `void close(Scan *scan) {}` (`include/interconnect/transport/BaseTransport.h:342`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2093 | `ThriftTransporter` method `bool open() override { return underlyingTransport->isOpen(); }` (`include/interconnect/transport/BaseTransport.h:344`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2094 | `ThriftTransporter` method `bool isOpen() override { return underlyingTransport->isOpen(); }` (`include/interconnect/transport/BaseTransport.h:346`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2095 | `CachedTransport` constructor `CachedTransport(std::shared_ptr<T> transport, std::shared_ptr<ServerConnection> key)` (`include/interconnect/transport/CachedTransport.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2096 | `CachedTransport` destructor `~CachedTransport() { close(); }` (`include/interconnect/transport/CachedTransport.h:43`) | — | — | — | Missing Go | Assessed individually: `interconnect::CachedTransport` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2097 | `CachedTransport` method `void reserve(bool reserve = true) { reserved = reserve; }` (`include/interconnect/transport/CachedTransport.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2098 | `CachedTransport` method `bool isReserved() { return reserved; }` (`include/interconnect/transport/CachedTransport.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2099 | `CachedTransport` method `bool open() {` (`include/interconnect/transport/CachedTransport.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2100 | `CachedTransport` method `void close() {` (`include/interconnect/transport/CachedTransport.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2101 | `CachedTransport` method `std::shared_ptr<T> getTransport() { return serverTransport; }` (`include/interconnect/transport/CachedTransport.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2102 | `CachedTransport` method `bool hasError() { return foundError; }` (`include/interconnect/transport/CachedTransport.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2103 | `CachedTransport` method `std::shared_ptr<ServerConnection> getCacheKey() { return cacheKey; }` (`include/interconnect/transport/CachedTransport.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2104 | `CachedTransport` method `std::shared_ptr<T> getTransporter() { return serverTransport; }` (`include/interconnect/transport/CachedTransport.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2105 | `CachedTransport` operator `bool operator==(const CachedTransport *rhs) const {` (`include/interconnect/transport/CachedTransport.h:76`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2106 | `CachedTransport` method `bool isOpen() { return serverTransport->isOpen(); }` (`include/interconnect/transport/CachedTransport.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2107 | `CachedTransport` method `void registerService(const std::string &instance, const std::string &clusterManagers) {` (`include/interconnect/transport/CachedTransport.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2108 | `CachedTransport` method `void setReturnTime(uint64_t t) { lastReturnTime = t; }` (`include/interconnect/transport/CachedTransport.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2109 | `CachedTransport` method `uint64_t getLastReturnTime() { return lastReturnTime; }` (`include/interconnect/transport/CachedTransport.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2110 | `CachedTransport` method `void sawError(bool sawError) { foundError = true; }` (`include/interconnect/transport/CachedTransport.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2111 | `(namespace scope)` macro constant `#define CLIENTTRANSPORT_H_` (`include/interconnect/transport/ClientTransport.h:16`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. connector, transport, or table-operation surface. |
| SB-CXX-2112 | `ClientTransport` class (no public members) `class ClientTransport : public Transporter<T> {` (`include/interconnect/transport/ClientTransport.h:24`) | — | — | — | Missing Go | Public type that declares no public member at class scope (interface with protected members, tag type, or trait). Enumerated so no declaration is unrepresented. Shoal exports no equivalent type. connector, transport, or table-operation surface. |
| SB-CXX-2113 | `CoordinatorInterface` method `virtual void recreateCoordinatorClient() = 0;` (`include/interconnect/transport/CoordinatorInterface.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2114 | `CoordinatorInterface` method `virtual std::shared_ptr<apache::thrift::transport::TTransport> recreateTransport() = 0;` (`include/interconnect/transport/CoordinatorInterface.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2115 | `FateInterface` destructor `virtual ~FateInterface() {}` (`include/interconnect/transport/FateInterface.h:68`) | — | — | — | Missing Go | Assessed individually: `interconnect::FateInterface` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2116 | `ServerConnection` constructor `explicit ServerConnection(const std::shared_ptr<ServerConnection> &other) : host(other->host), port(other->port), timeout(other->timeout) {` (`include/interconnect/transport/ServerConnection.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2117 | `ServerConnection` constructor `ServerConnection(std::string loc, uint16_t port, uint64_t timeout)` (`include/interconnect/transport/ServerConnection.h:33`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2118 | `ServerConnection` destructor `virtual ~ServerConnection() {}` (`include/interconnect/transport/ServerConnection.h:42`) | — | — | — | Missing Go | Assessed individually: `interconnect::ServerConnection` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2119 | `ServerConnection` method `std::string getHost() const { return host; }` (`include/interconnect/transport/ServerConnection.h:44`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2120 | `ServerConnection` method `uint16_t getPort() const { return port; }` (`include/interconnect/transport/ServerConnection.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2121 | `ServerConnection` method `uint64_t getTimeout() const { return timeout; }` (`include/interconnect/transport/ServerConnection.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2122 | `ServerConnection` operator `std::ostream &operator<<(std::ostream &os) {` (`include/interconnect/transport/ServerConnection.h:50`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2123 | `ServerConnection` method `std::string toString() const { return hostAndPort; }` (`include/interconnect/transport/ServerConnection.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2124 | `ServerConnection` operator `ServerConnection &operator=(const ServerConnection &rhs) {` (`include/interconnect/transport/ServerConnection.h:56`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2125 | `ServerConnection` operator `bool operator==(const ServerConnection &rhs) {` (`include/interconnect/transport/ServerConnection.h:64`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2126 | `ServerConnection` operator `bool operator!=(const ServerConnection &rhs) { return !(*this == rhs); }` (`include/interconnect/transport/ServerConnection.h:77`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2127 | `ServerConnection` operator `bool operator<(const ServerConnection &rhs) const {` (`include/interconnect/transport/ServerConnection.h:79`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2128 | `ServerConnection` operator `bool operator<(const std::shared_ptr<ServerConnection> &rhs) const {` (`include/interconnect/transport/ServerConnection.h:92`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2129 | `ServerConnection` operator `bool operator>(const ServerConnection &rhs) const {` (`include/interconnect/transport/ServerConnection.h:105`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2130 | `Cmp_ServerConnection` operator `bool operator()(const ServerConnection &first, const ServerConnection &second) {` (`include/interconnect/transport/ServerConnection.h:126`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2131 | `Cmp_ServerConnectionSP` operator `bool operator()(const std::shared_ptr<ServerConnection> &first, const std::shared_ptr<ServerConnection> &second) const {` (`include/interconnect/transport/ServerConnection.h:151`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. connector, transport, or table-operation surface. |
| SB-CXX-2132 | `ServerTransport` constructor `explicit ServerTransport(std::shared_ptr<ServerConnection> conn) : Transporter<T>(conn) {}` (`include/interconnect/transport/ServerTransport.h:29`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2133 | `ServerTransport` destructor `virtual ~ServerTransport() {}` (`include/interconnect/transport/ServerTransport.h:31`) | — | — | — | Missing Go | Assessed individually: `interconnect::ServerTransport` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2134 | `ServerTransport` method `virtual void authenticate(cclient::data::security::AuthInfo *auth) = 0;` (`include/interconnect/transport/ServerTransport.h:33`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2135 | `ServerTransport` method `virtual Scan *beginScan( std::atomic<bool> *isRunning, ScanRequest<ScanIdentifier<std::shared_ptr<K>, V>> *req) = 0;` (`include/interconnect/transport/ServerTransport.h:35`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2136 | `ServerTransport` method `virtual void *write(cclient::data::security::AuthInfo *auth, std::map<K, std::vector<W>> *request) = 0;` (`include/interconnect/transport/ServerTransport.h:39`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2137 | `Transporter` constructor `explicit Transporter(const std::shared_ptr<ServerConnection> &conn) {}` (`include/interconnect/transport/Transport.h:28`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2138 | `Transporter` destructor `virtual ~Transporter() {}` (`include/interconnect/transport/Transport.h:30`) | — | — | — | Missing Go | Assessed individually: `interconnect::Transporter` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2139 | `Transporter` method `virtual T getTransport() = 0;` (`include/interconnect/transport/Transport.h:32`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2140 | `Transporter` method `virtual void registerService(const std::string &instance, const std::string &clusterManagers) = 0;` (`include/interconnect/transport/Transport.h:34`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2141 | `Transporter` method `virtual bool open() = 0;` (`include/interconnect/transport/Transport.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2142 | `Transporter` method `virtual bool isOpen() = 0;` (`include/interconnect/transport/Transport.h:37`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2143 | `Transporter` method `virtual void close() = 0;` (`include/interconnect/transport/Transport.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2144 | `TransportPool` constructor `TransportPool();` (`include/interconnect/TransportPool.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2145 | `TransportPool` destructor `~TransportPool();` (`include/interconnect/TransportPool.h:48`) | — | — | — | Missing Go | Assessed individually: `interconnect::TransportPool` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2146 | `TransportPool` method `void freeTransport(std::shared_ptr<CachedTransport<Tr>> cachedTransport);` (`include/interconnect/TransportPool.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2147 | `TransportPool` method `std::pair<std::string, std::shared_ptr<CachedTransport<Tr>>> getTransporter( const std::vector<std::shared_ptr<ServerConnection>> *servers, const bool preferCachedConnection);` (`include/interconnect/TransportPool.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2148 | `TransportPool` method `std::shared_ptr<CachedTransport<Tr>> getTransporter( std::shared_ptr<ServerConnection> conn) {` (`include/interconnect/TransportPool.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2149 | `TransportPool` method `void closeAll() {` (`include/interconnect/TransportPool.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2150 | `Write` constructor `Write();` (`include/interconnect/Write.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. connector, transport, or table-operation surface. |
| SB-CXX-2151 | `Write` destructor `virtual ~Write();` (`include/interconnect/Write.h:26`) | — | — | — | Missing Go | Assessed individually: `cclient::data::Write` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. connector, transport, or table-operation surface. |
| SB-CXX-2152 | `AccumuloKey` constructor `AccumuloKey(JNIEnv *env, jobject key)` (`include/jni/AccumuloKey.h:30`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2153 | `AccumuloKey` method `bool init(JNIEnv *env) {` (`include/jni/AccumuloKey.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2154 | `AccumuloKey` constructor `AccumuloKey(const std::shared_ptr<cclient::data::Key> &key)` (`include/jni/AccumuloKey.h:98`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2155 | `AccumuloKey` method `std::shared_ptr<cclient::data::Key> toKey() {` (`include/jni/AccumuloKey.h:111`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2156 | `AccumuloKey` method `jobject getAccumuloKey(JNIEnv *env) const {` (`include/jni/AccumuloKey.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2157 | `AccumuloKey` method `std::string getRow() const {` (`include/jni/AccumuloKey.h:169`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2158 | `AccumuloKey` method `std::string getCf() const {` (`include/jni/AccumuloKey.h:173`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2159 | `AccumuloKey` method `std::string getCq() const {` (`include/jni/AccumuloKey.h:177`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2160 | `AccumuloKey` method `std::string getCv() const {` (`include/jni/AccumuloKey.h:181`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2161 | `AccumuloKey` method `long getTimeStamp() const {` (`include/jni/AccumuloKey.h:185`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2162 | `AccumuloRange` constructor `AccumuloRange(JNIEnv *env, jobject range)` (`include/jni/AccumuloRange.h:33`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2163 | `AccumuloRange` method `bool init(JNIEnv *env) {` (`include/jni/AccumuloRange.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2164 | `AccumuloRange` constructor `AccumuloRange(const std::shared_ptr<cclient::data::Range> &range)` (`include/jni/AccumuloRange.h:103`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2165 | `AccumuloRange` method `jobject getAccumuloRange(JNIEnv *env) const {` (`include/jni/AccumuloRange.h:111`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2166 | `AccumuloRange` method `std::shared_ptr<cclient::data::Range> getRange() const {` (`include/jni/AccumuloRange.h:163`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2167 | `AccumuloValue` constructor `AccumuloValue(JNIEnv *env, jobject value)` (`include/jni/AccumuloValue.h:35`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2168 | `AccumuloValue` method `bool init(JNIEnv *env) {` (`include/jni/AccumuloValue.h:43`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2169 | `AccumuloValue` constructor `AccumuloValue(const std::shared_ptr<cclient::data::Value> &incomingValue)` (`include/jni/AccumuloValue.h:64`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2170 | `AccumuloValue` method `std::shared_ptr<cclient::data::Value> toValue() {` (`include/jni/AccumuloValue.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2171 | `AccumuloValue` method `jobject getAccumuloValue(JNIEnv *env) const {` (`include/jni/AccumuloValue.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2172 | `(namespace scope)` namespace-scope function `void rethrow_cpp_exception_as_java_exception(JNIEnv*);` (`include/jni/DSLIterator.h:27`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2173 | `(namespace scope)` namespace-scope function `JNIEXPORT void JNICALL Java_org_apache_accumulo_tserver_tablet_NativeCompactor_callCompact(JNIEnv* env, jobject me, jstring output_file, jobject arraylist_files, jobject longadder_resulted, jobject longadder_total, jobject longadder_filesize, jlong ageoff);` (`include/jni/DSLIterator.h:29`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2174 | `AccumuloIterator` constructor `AccumuloIterator(JNIEnv *env, jobject itr)` (`include/jni/Iterators.h:45`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2175 | `AccumuloIterator` method `bool hasTop() {` (`include/jni/Iterators.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2176 | `AccumuloIterator` method `void next() {` (`include/jni/Iterators.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2177 | `AccumuloIterator` method `void seek(const std::shared_ptr<cclient::data::Range> &rng) {` (`include/jni/Iterators.h:109`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2178 | `AccumuloIterator` method `std::shared_ptr<cclient::data::Key> getTopKey() {` (`include/jni/Iterators.h:155`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2179 | `AccumuloIterator` method `std::shared_ptr<cclient::data::Value> getTopValue() {` (`include/jni/Iterators.h:187`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2180 | `AccumuloIterator` method `bool setTopKey(JNIEnv *env, jobject itr, const std::shared_ptr<cclient::data::Key> &passedKey = nullptr) {` (`include/jni/Iterators.h:190`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2181 | `AccumuloIterator` method `bool setTopValue(JNIEnv *env, jobject itr, const std::shared_ptr<cclient::data::Value> &passedValue = nullptr) {` (`include/jni/Iterators.h:206`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2182 | `DSLIterator` constructor `DSLIterator()` (`include/jni/Iterators.h:226`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2183 | `DSLIterator` destructor `virtual ~DSLIterator() {` (`include/jni/Iterators.h:231`) | — | — | — | Missing Go | Assessed individually: `cclient::jni::DSLIterator` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. JNI iterator-bridge surface. |
| SB-CXX-2184 | `DSLIterator` method `virtual void callNext() = 0;` (`include/jni/Iterators.h:235`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2185 | `DSLIterator` method `virtual bool callHasTop() = 0;` (`include/jni/Iterators.h:237`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2186 | `DSLIterator` method `virtual void callGetTopKey() = 0;` (`include/jni/Iterators.h:239`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2187 | `DSLIterator` method `virtual void callGetTopValue() = 0;` (`include/jni/Iterators.h:241`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2188 | `DSLIterator` method `virtual void callSeek(const std::shared_ptr<cclient::data::Range> &range) = 0;` (`include/jni/Iterators.h:243`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2189 | `DSLIterator` method `virtual void seek(const std::shared_ptr<cclient::data::Range> &range) {` (`include/jni/Iterators.h:245`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2190 | `DSLIterator` method `virtual void setIter(AccumuloIterator *accIter) {` (`include/jni/Iterators.h:249`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2191 | `DSLIterator` method `virtual void setDSL(const std::string &dsl) = 0;` (`include/jni/Iterators.h:252`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2192 | `DSLIterator` method `virtual std::shared_ptr<cclient::data::Key> getTopKey() {` (`include/jni/Iterators.h:254`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2193 | `DSLIterator` method `virtual std::shared_ptr<cclient::data::Value> getTopValue() {` (`include/jni/Iterators.h:257`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2194 | `DSLIterator` method `virtual std::shared_ptr<cclient::data::Key> getIteratorTopKey() {` (`include/jni/Iterators.h:261`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2195 | `DSLIterator` method `virtual std::shared_ptr<cclient::data::Value> getIteratorTopValue() {` (`include/jni/Iterators.h:264`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2196 | `DSLIterator` method `virtual bool hasTop() = 0;` (`include/jni/Iterators.h:268`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2197 | `DSLIterator` method `virtual bool iteratorHasTop() {` (`include/jni/Iterators.h:270`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2198 | `DSLIterator` method `virtual void next() {` (`include/jni/Iterators.h:274`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2199 | `JavaClass` constructor `JavaClass()` (`include/jni/JavaClass.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2200 | `JavaClass` constructor `explicit JavaClass(const std::string &name, jclass classref, JNIEnv *jenv) : name_(name) {` (`include/jni/JavaClass.h:53`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2201 | `JavaClass` destructor `~JavaClass() {` (`include/jni/JavaClass.h:60`) | — | — | — | Missing Go | Assessed individually: `cclient::jni::JavaClass` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. JNI iterator-bridge surface. |
| SB-CXX-2202 | `JavaClass` method `std::string getName() const {` (`include/jni/JavaClass.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2203 | `JavaClass` method `jclass getReference() const {` (`include/jni/JavaClass.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2204 | `JavaClass` operator `JavaClass& operator=(const JavaClass &o) = default;` (`include/jni/JavaClass.h:75`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. JNI iterator-bridge surface. |
| SB-CXX-2205 | `JavaClass` method `JNIEXPORT jobject newInstance(JNIEnv *env) {` (`include/jni/JavaClass.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2206 | `JavaClass` method `jmethodID getClassMethod(JNIEnv *env, const std::string &methodName, const std::string &type) {` (`include/jni/JavaClass.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2207 | `JavaClass` method `void registerMethods(JNIEnv *env, JNINativeMethod *methods, size_t size) {` (`include/jni/JavaClass.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2208 | `JavaClass` method `void registerMethods(JNIEnv *env, JavaSignatures &signatures) {` (`include/jni/JavaClass.h:107`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2209 | `JavaClass` method `void callVoidMethod(JNIEnv *env, jobject obj, const std::string &methodName, const std::string &type, Args ... args) {` (`include/jni/JavaClass.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2210 | `(namespace scope)` macro constant `#define EXCEPTION_CLASS "java/lang/Exception"` (`include/jni/JavaDefs.h:22`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2211 | `(namespace scope)` macro constant `#define NO_FF_OBJECT "Calling function on null flow file"` (`include/jni/JavaDefs.h:24`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2212 | `(namespace scope)` macro constant `#define JVM_ERROR_MSG "Error calling toString on callable. JVM error likely"` (`include/jni/JavaDefs.h:26`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2213 | `JavaException` constructor `JavaException(std::string errorMsg)` (`include/jni/JavaException.h:42`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2214 | `JavaException` destructor `virtual ~JavaException() noexcept {` (`include/jni/JavaException.h:48`) | — | — | — | Missing Go | Assessed individually: `cclient::jni::JavaException` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. JNI iterator-bridge surface. |
| SB-CXX-2215 | `JavaException` method `virtual const char* what() const noexcept {` (`include/jni/JavaException.h:50`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2216 | `(namespace scope)` namespace-scope function `static std::string getMessage(JNIEnv *env, jthrowable throwable) {` (`include/jni/JavaException.h:60`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2217 | `(namespace scope)` namespace-scope function `static inline void ThrowIf(JNIEnv *env) {` (`include/jni/JavaException.h:87`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2218 | `(namespace scope)` namespace-scope function `static inline void ThrowJava(JNIEnv *env, const char *message) {` (`include/jni/JavaException.h:96`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2219 | `(namespace scope)` macro constant `#define THROW_IF_NULL(expr, env, message) if (UNLIKELY(expr == nullptr)) cclient::jni::ThrowJava(env,message)` (`include/jni/JavaException.h:108`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2220 | `(namespace scope)` macro constant `#define THROW_IF(expr, env, message) if (UNLIKELY(expr)) cclient::jni::ThrowJava(env,message)` (`include/jni/JavaException.h:110`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2221 | `JavaMethodSignature` constructor `JavaMethodSignature(const JavaMethodSignature &other) = delete;` (`include/jni/JniMethod.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2222 | `JavaMethodSignature` constructor `JavaMethodSignature(JavaMethodSignature &&other) = default;` (`include/jni/JniMethod.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2223 | `JavaMethodSignature` constructor `JavaMethodSignature(const std::string &name, const std::string &params, void *ptr)` (`include/jni/JniMethod.h:40`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2224 | `JavaMethodSignature` method `const char* getName() const {` (`include/jni/JniMethod.h:51`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2225 | `JavaMethodSignature` method `const char* getParameters() const {` (`include/jni/JniMethod.h:59`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2226 | `JavaMethodSignature` method `const void* getPointer() const {` (`include/jni/JniMethod.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2227 | `JavaMethodSignature` operator `JavaMethodSignature& operator=(const JavaMethodSignature &other) = delete;` (`include/jni/JniMethod.h:70`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. JNI iterator-bridge surface. |
| SB-CXX-2228 | `JavaMethodSignature` operator `JavaMethodSignature& operator=(JavaMethodSignature &&other) = default;` (`include/jni/JniMethod.h:71`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. JNI iterator-bridge surface. |
| SB-CXX-2229 | `JavaSignatures` constructor `JavaSignatures()` (`include/jni/JniMethod.h:85`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2230 | `JavaSignatures` method `void addSignature(JavaMethodSignature &&signature) {` (`include/jni/JniMethod.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2231 | `JavaSignatures` method `bool empty() const {` (`include/jni/JniMethod.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2232 | `JavaSignatures` method `const JNINativeMethod* getSignatures() const {` (`include/jni/JniMethod.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2233 | `JavaSignatures` method `size_t getSize() const {` (`include/jni/JniMethod.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2234 | `(namespace scope)` namespace-scope function `static inline std::string JniStringToUTF(JNIEnv *env, jstring jStr) {` (`include/jni/JNIUtil.h:25`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2235 | `(namespace scope)` namespace-scope function `static inline jbyteArray toByteArray(JNIEnv *env, const std::string &str) {` (`include/jni/JNIUtil.h:46`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2236 | `(namespace scope)` macro constant `#define LIKELY(x) (__builtin_expect((x), 1))` (`include/jni/JNIUtil.h:63`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2237 | `(namespace scope)` macro constant `#define UNLIKELY(x) (__builtin_expect((x), 0))` (`include/jni/JNIUtil.h:64`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2238 | `(namespace scope)` macro constant `#define LIKELY(x) (x)` (`include/jni/JNIUtil.h:66`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2239 | `(namespace scope)` macro constant `#define UNLIKELY(x) (x)` (`include/jni/JNIUtil.h:67`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. JNI iterator-bridge surface. |
| SB-CXX-2240 | `(namespace scope)` namespace-scope function `static inline std::string getClassName() {` (`include/jni/JVMLoader.h:43`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2241 | `FieldMapping` method `jfieldID getField(const std::string &clazz, const std::string &fnArg) {` (`include/jni/JVMLoader.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2242 | `FieldMapping` method `void putField(const std::string &clazz, const std::string &fnArg, jfieldID field) {` (`include/jni/JVMLoader.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2243 | `(namespace scope)` alias `typedef jint (*registerNatives_t)(JNIEnv *env, jclass clazz);` (`include/jni/JVMLoader.h:91`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. This alias names a **function-pointer type**, not a function: it declares no callable of its own, and a port must expose an equivalent callback type rather than a function. Shoal exposes no equivalent alias. JNI iterator-bridge surface. |
| SB-CXX-2244 | `(namespace scope)` namespace-scope function `jfieldID getPtrField(JNIEnv *env, jobject obj);` (`include/jni/JVMLoader.h:93`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2245 | `(namespace scope)` namespace-scope function `T* getPtr(JNIEnv *env, jobject obj);` (`include/jni/JVMLoader.h:96`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2246 | `(namespace scope)` namespace-scope function `void setPtr(JNIEnv *env, jobject obj, T *t);` (`include/jni/JVMLoader.h:99`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. JNI iterator-bridge surface. |
| SB-CXX-2247 | `JVMLoader` method `bool initialized() {` (`include/jni/JVMLoader.h:111`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2248 | `JVMLoader` method `void remove_class(JNIEnv *env, const std::string &name) {` (`include/jni/JVMLoader.h:119`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2249 | `JVMLoader` method `static JVMLoader* getInstance() {` (`include/jni/JVMLoader.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2250 | `JVMLoader` method `void setReference(jobject obj, JNIEnv *env, T *t) {` (`include/jni/JVMLoader.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2251 | `JVMLoader` method `T* getReference(JNIEnv *env, jobject obj) {` (`include/jni/JVMLoader.h:142`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2252 | `JVMLoader` method `static jfieldID getPtrField(const std::string &className, JNIEnv *env, jobject obj) {` (`include/jni/JVMLoader.h:152`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2253 | `JVMLoader` method `static T* getPtr(JNIEnv *env, jobject obj) {` (`include/jni/JVMLoader.h:164`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2254 | `JVMLoader` method `static void setPtr(JNIEnv *env, jobject obj, T *t) {` (`include/jni/JVMLoader.h:170`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2255 | `JVMLoader` method `static void putClassMapping(JNIEnv *env, JavaClass &clazz, const std::string &fieldStr, const std::string &arg) {` (`include/jni/JVMLoader.h:176`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. JNI iterator-bridge surface. |
| SB-CXX-2256 | `(namespace scope)` macro constant `#define LOG_BUFFER_SIZE 1024` (`include/logging/Logger.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2257 | `LoggerControl` constructor `LoggerControl() : is_enabled_(true) {}` (`include/logging/Logger.h:34`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2258 | `LoggerControl` method `bool is_enabled() { return is_enabled_; }` (`include/logging/Logger.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2259 | `LoggerControl` method `void setEnabled(bool status) { is_enabled_ = status; }` (`include/logging/Logger.h:38`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2260 | `(namespace scope)` namespace-scope function `inline std::string format_string(char const* format_str, Args&&... args) {` (`include/logging/Logger.h:45`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. logging surface. |
| SB-CXX-2261 | `(namespace scope)` namespace-scope function `inline std::string format_string(char const* format_str) { return format_str; }` (`include/logging/Logger.h:51`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. logging surface. |
| SB-CXX-2262 | `(namespace scope)` namespace-scope function `inline char const* conditional_conversion(std::string const& str) {` (`include/logging/Logger.h:53`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. logging surface. |
| SB-CXX-2263 | `(namespace scope)` namespace-scope function `inline T conditional_conversion(T const& t) {` (`include/logging/Logger.h:58`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. logging surface. |
| SB-CXX-2264 | `LOG_LEVEL` enum `typedef enum { ... } LOG_LEVEL;` (`include/logging/Logger.h:62`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: trace = 0; debug = 1; info = 2; warn = 3; err = 4; critical = 5; off = 6. Shoal exposes no equivalent type. |
| SB-CXX-2265 | `BaseLogger` destructor `virtual ~BaseLogger() {}` (`include/logging/Logger.h:74`) | — | — | — | Missing Go | Assessed individually: `logging::BaseLogger` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. logging surface. |
| SB-CXX-2266 | `BaseLogger` method `virtual void log_string(LOG_LEVEL level, std::string str) = 0;` (`include/logging/Logger.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2267 | `BaseLogger` method `virtual bool should_log(const LOG_LEVEL& level) { return true; }` (`include/logging/Logger.h:77`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2268 | `LogBuilder` constructor `LogBuilder(BaseLogger* l, LOG_LEVEL level)` (`include/logging/Logger.h:87`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2269 | `LogBuilder` destructor `~LogBuilder() {` (`include/logging/Logger.h:94`) | — | — | — | Missing Go | Assessed individually: `logging::LogBuilder` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. logging surface. |
| SB-CXX-2270 | `LogBuilder` method `void setIgnore() { ignore = true; }` (`include/logging/Logger.h:98`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2271 | `LogBuilder` method `void log_string(LOG_LEVEL level) { ptr->log_string(level, str.str()); }` (`include/logging/Logger.h:100`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2272 | `LogBuilder` operator `LogBuilder& operator<<(const T& o) {` (`include/logging/Logger.h:103`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. logging surface. |
| SB-CXX-2273 | `LogBuilder` public data member `bool ignore;` (`include/logging/Logger.h:108`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2274 | `LogBuilder` public data member `BaseLogger* ptr;` (`include/logging/Logger.h:109`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2275 | `LogBuilder` public data member `std::stringstream str;` (`include/logging/Logger.h:110`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2276 | `LogBuilder` public data member `LOG_LEVEL level;` (`include/logging/Logger.h:111`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2277 | `Logger` method `void log_error(const char* const format, const Args&... args) {` (`include/logging/Logger.h:123`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2278 | `Logger` method `void log_warn(const char* const format, const Args&... args) {` (`include/logging/Logger.h:134`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2279 | `Logger` method `void log_info(const char* const format, const Args&... args) {` (`include/logging/Logger.h:145`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2280 | `Logger` method `void log_debug(const char* const format, const Args&... args) {` (`include/logging/Logger.h:156`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2281 | `Logger` method `void log_trace(const char* const format, const Args&... args) {` (`include/logging/Logger.h:167`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2282 | `Logger` method `bool should_log(const LOG_LEVEL& level) override {` (`include/logging/Logger.h:171`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2283 | `(namespace scope)` macro constant `#define LOG_DEBUG(x) LogBuilder(x.get(), logging::LOG_LEVEL::debug)` (`include/logging/Logger.h:258`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2284 | `(namespace scope)` macro constant `#define LOG_INFO(x) LogBuilder(x.get(), logging::LOG_LEVEL::info)` (`include/logging/Logger.h:260`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2285 | `(namespace scope)` macro constant `#define LOG_TRACE(x) LogBuilder(x.get(), logging::LOG_LEVEL::trace)` (`include/logging/Logger.h:262`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2286 | `(namespace scope)` macro constant `#define LOG_ERROR(x) LogBuilder(x.get(), logging::LOG_LEVEL::err)` (`include/logging/Logger.h:264`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2287 | `(namespace scope)` macro constant `#define LOG_WARN(x) LogBuilder(x.get(), logging::LOG_LEVEL::warn)` (`include/logging/Logger.h:266`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2288 | `LoggerNamespace` public data member `spdlog::level::level_enum level;` (`include/logging/LoggerConfiguration.h:38`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2289 | `LoggerNamespace` public data member `bool has_level;` (`include/logging/LoggerConfiguration.h:39`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2290 | `LoggerNamespace` public data member `std::vector<std::shared_ptr<spdlog::sinks::sink>> sinks;` (`include/logging/LoggerConfiguration.h:40`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2291 | `LoggerNamespace` public data member `std::map<std::string, std::shared_ptr<LoggerNamespace>> children;` (`include/logging/LoggerConfiguration.h:41`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2292 | `LoggerNamespace` constructor `LoggerNamespace()` (`include/logging/LoggerConfiguration.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2293 | `LoggerProperties` constructor `LoggerProperties() : Properties("Logger properties") {}` (`include/logging/LoggerConfiguration.h:53`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2294 | `LoggerProperties` method `std::vector<std::string> get_keys_of_type(const std::string &type);` (`include/logging/LoggerConfiguration.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2295 | `LoggerProperties` method `void add_sink(const std::string &name, std::shared_ptr<spdlog::sinks::sink> sink) {` (`include/logging/LoggerConfiguration.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2296 | `LoggerProperties` method `std::map<std::string, std::shared_ptr<spdlog::sinks::sink>> initial_sinks() {` (`include/logging/LoggerConfiguration.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2297 | `LoggerProperties` public data member `static const char *appender_prefix;` (`include/logging/LoggerConfiguration.h:76`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2298 | `LoggerProperties` public data member `static const char *logger_prefix;` (`include/logging/LoggerConfiguration.h:77`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2299 | `LoggerConfiguration` method `static LoggerConfiguration &getConfiguration() {` (`include/logging/LoggerConfiguration.h:88`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2300 | `LoggerConfiguration` method `static void enableLogger() { getConfiguration().enableLogging(); }` (`include/logging/LoggerConfiguration.h:93`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-LOG-001](#sec-17) already carries this obligation — `logging::LoggerConfiguration::enableLogger` is the overload bound at `pysharkbite.cpp:66` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2301 | `LoggerConfiguration` method `static void enableTraceLogger() {` (`include/logging/LoggerConfiguration.h:95`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-LOG-002](#sec-17) already carries this obligation — `logging::LoggerConfiguration::enableTraceLogger` is the overload bound at `pysharkbite.cpp:67` and that row carries the Python contract. That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2302 | `LoggerConfiguration` method `static std::unique_ptr<LoggerConfiguration> newInstance() {` (`include/logging/LoggerConfiguration.h:99`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2303 | `LoggerConfiguration` method `void disableLogging() { controller_->setEnabled(false); }` (`include/logging/LoggerConfiguration.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2304 | `LoggerConfiguration` method `void enableLogging(LOG_LEVEL level = LOG_LEVEL::debug, bool use_stdout = true) {` (`include/logging/LoggerConfiguration.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2305 | `LoggerConfiguration` method `bool shortenClassNames() const { return shorten_names_; }` (`include/logging/LoggerConfiguration.h:143`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2306 | `LoggerConfiguration` method `void initialize(const std::shared_ptr<LoggerProperties> &logger_properties);` (`include/logging/LoggerConfiguration.h:147`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2307 | `LoggerConfiguration` method `std::shared_ptr<Logger> getLogger(const std::string &name);` (`include/logging/LoggerConfiguration.h:153`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2308 | `LoggerConfiguration` public data member `static const char *spdlog_default_pattern;` (`include/logging/LoggerConfiguration.h:154`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. logging surface. |
| SB-CXX-2309 | `LoggerFactory` method `static std::shared_ptr<Logger> getLogger() {` (`include/logging/LoggerConfiguration.h:198`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2310 | `LoggerFactory` method `static std::shared_ptr<Logger> getAliasedLogger(const std::string &alias) {` (`include/logging/LoggerConfiguration.h:205`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2311 | `(namespace scope)` macro constant `#define FILE_SEPARATOR '\\'` (`include/logging/Properties.h:36`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2312 | `(namespace scope)` macro constant `#define FILE_SEPARATOR '/'` (`include/logging/Properties.h:38`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2313 | `(namespace scope)` macro constant `#define PATH_MAX 2048` (`include/logging/Properties.h:43`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. logging surface. |
| SB-CXX-2314 | `Properties` constructor `Properties(const std::string &name = "");` (`include/logging/Properties.h:48`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2315 | `Properties` destructor `virtual ~Properties() {}` (`include/logging/Properties.h:50`) | — | — | — | Missing Go | Assessed individually: `Properties` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. logging surface. |
| SB-CXX-2316 | `Properties` method `virtual const std::string &getName() { return name_; }` (`include/logging/Properties.h:52`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2317 | `Properties` method `void clear() {` (`include/logging/Properties.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2318 | `Properties` method `void set(const std::string &key, const std::string &value) {` (`include/logging/Properties.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2319 | `Properties` method `bool has(std::string key) {` (`include/logging/Properties.h:66`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2320 | `Properties` method `bool get(const std::string &key, std::string &value);` (`include/logging/Properties.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2321 | `Properties` method `bool get(const std::string &key, const std::string &alternate_key, std::string &value);` (`include/logging/Properties.h:87`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2322 | `Properties` method `int getInt(const std::string &key, int default_value);` (`include/logging/Properties.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2323 | `Properties` method `bool parseConfigureFileLine(char *buf, std::string &prop_key, std::string &prop_value);` (`include/logging/Properties.h:97`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2324 | `Properties` method `void loadConfigurationFile(const char *fileName);` (`include/logging/Properties.h:100`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2325 | `Properties` method `void setHome(std::string minifiHome) { minifi_home_ = minifiHome; }` (`include/logging/Properties.h:102`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2326 | `Properties` method `std::vector<std::string> getConfiguredKeys() {` (`include/logging/Properties.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2327 | `Properties` method `std::string getHome() const { return minifi_home_; }` (`include/logging/Properties.h:113`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2328 | `Properties` method `void parseCommandLine(int argc, char **argv);` (`include/logging/Properties.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. logging surface. |
| SB-CXX-2329 | `(namespace scope)` macro constant `#define DEFINE_RUNTIME_EXCEPTION(name, type) class name : public pybind11::builtin_exception { public: using pybind11::builtin_exception::builtin_exception; name() : name("") { } void set_error() const override { PyErr_SetString(type, what()); } };` (`include/python/bindings.h:20`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. pybind11 binding-support surface. |
| SB-CXX-2330 | `(namespace scope)` macro-declared type `DEFINE_RUNTIME_EXCEPTION(stop_async_iteration, PyExc_StopAsyncIteration)` (`include/python/bindings.h:28`) | — | — | — | Missing Go | Public type declared by macro expansion: the expansion defines a type consumers can name and catch. Shoal exposes no equivalent. pybind11 binding-support surface. |
| SB-CXX-2331 | `HedgedScannerHeuristic` constructor `explicit HedgedScannerHeuristic(short numThreads = 10) : ScannerHeuristic(numThreads), logger(logging::LoggerFactory<HedgedScannerHeuristic>::getLogger()) {}` (`include/scanner/constructs/HedgedHeuristic.h:50`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2332 | `HedgedScannerHeuristic` destructor `~HedgedScannerHeuristic() { close(); }` (`include/scanner/constructs/HedgedHeuristic.h:54`) | — | — | — | Missing Go | Assessed individually: `scanners::HedgedScannerHeuristic` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2333 | `HedgedScannerHeuristic` method `virtual void close() override;` (`include/scanner/constructs/HedgedHeuristic.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2334 | `HedgedScannerHeuristic` method `virtual uint16_t scan( Source<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>> *source) override;` (`include/scanner/constructs/HedgedHeuristic.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2335 | `HedgedScannerHeuristic` method `void setTableIterators(std::vector<cclient::data::IterInfo> iters);` (`include/scanner/constructs/HedgedHeuristic.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2336 | `Heuristic` constructor `Heuristic() {}` (`include/scanner/constructs/Heuristic.h:38`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2337 | `Heuristic` destructor `virtual ~Heuristic() {}` (`include/scanner/constructs/Heuristic.h:43`) | — | — | — | Missing Go | Assessed individually: `scanners::Heuristic` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2338 | `ResultBlock` constructor `ResultBlock(SourceConditions *conditionals, moodycamel::ConcurrentQueue<std::shared_ptr<T>> *queue, bool setEnd = false)` (`include/scanner/constructs/Results.h:59`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2339 | `ResultBlock` constructor `ResultBlock() : result_excp(nullptr) {}` (`include/scanner/constructs/Results.h:67`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2340 | `ResultBlock` method `ResultBlock<T> begin() {` (`include/scanner/constructs/Results.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2341 | `ResultBlock` method `bool isEndOfRange() const { return isEnd; }` (`include/scanner/constructs/Results.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2342 | `ResultBlock` method `void setException(const std::exception_ptr &e) { result_excp = e; }` (`include/scanner/constructs/Results.h:76`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2343 | `ResultBlock` method `ResultBlock<T> end() {` (`include/scanner/constructs/Results.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2344 | `ResultBlock` method `SourceConditions *getSourceConditionals() const { return sourceConditionals; }` (`include/scanner/constructs/Results.h:83`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2345 | `ResultBlock` method `moodycamel::ConcurrentQueue<std::shared_ptr<T>> *getResultSet() const {` (`include/scanner/constructs/Results.h:85`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2346 | `ResultBlock` method `std::shared_ptr<T> get() { return current; }` (`include/scanner/constructs/Results.h:89`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2347 | `ResultBlock` operator `std::shared_ptr<T> operator*() { return current; }` (`include/scanner/constructs/Results.h:91`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2348 | `ResultBlock` method `inline void getNextResult() {` (`include/scanner/constructs/Results.h:93`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SCAN-004](#sec-9) already carries this obligation — that row cites this exact declaration (`include/scanner/constructs/Results.h:93`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2349 | `ResultBlock` operator `ResultBlock &operator++() {` (`include/scanner/constructs/Results.h:114`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2350 | `ResultBlock` operator `ResultBlock &operator++(int t) {` (`include/scanner/constructs/Results.h:119`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2351 | `ResultBlock` operator `ResultBlock &operator=(const ResultBlock &rhs) {` (`include/scanner/constructs/Results.h:127`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2352 | `ResultBlock` operator `bool operator==(const ResultIter<std::unique_ptr<T>> &rhs) {` (`include/scanner/constructs/Results.h:134`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2353 | `ResultBlock` operator `bool operator!=(const ResultBlock &rhs) { return !(isEnd == rhs.isEnd); }` (`include/scanner/constructs/Results.h:138`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2354 | `ResultBlock` method `void add(T *t) {` (`include/scanner/constructs/Results.h:140`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2355 | `ResultBlock` method `void add(std::vector<std::unique_ptr<T>> *t) {` (`include/scanner/constructs/Results.h:146`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2356 | `ResultBlock` method `void add(std::vector<std::shared_ptr<T>> *t) {` (`include/scanner/constructs/Results.h:156`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2357 | `ResultBlock` destructor `virtual ~ResultBlock() {}` (`include/scanner/constructs/Results.h:166`) | — | — | — | Missing Go | Assessed individually: `scanners::ResultBlock` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2358 | `ResultIter` constructor `ResultIter(ResultBlock<T> *copyResultSet, bool end = false)` (`include/scanner/constructs/Results.h:178`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2359 | `ResultIter` constructor `ResultIter(ResultBlock<T> &copyResultSet, bool end = false)` (`include/scanner/constructs/Results.h:188`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2360 | `ResultIter` constructor `ResultIter(ResultIter<T> *copyResultSet, bool end = false)` (`include/scanner/constructs/Results.h:198`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2361 | `ResultIter` constructor `ResultIter(ResultIter<T> &copyResultSet, bool end = false)` (`include/scanner/constructs/Results.h:208`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2362 | `ResultIter` method `ResultBlock<T> begin() { return *this; }` (`include/scanner/constructs/Results.h:218`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2363 | `ResultIter` method `ResultBlock<T> end() { return parent->end(); }` (`include/scanner/constructs/Results.h:220`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2364 | `ResultIter` method `ResultBlock<T> *getParent() { return parent; }` (`include/scanner/constructs/Results.h:222`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2365 | `ResultIter` operator `std::unique_ptr<T> operator*() { return std::move((parent->operator*())); }` (`include/scanner/constructs/Results.h:224`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2366 | `ResultIter` operator `ResultIter &operator++() {` (`include/scanner/constructs/Results.h:226`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2367 | `ResultIter` operator `ResultIter &operator++(int t) {` (`include/scanner/constructs/Results.h:231`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2368 | `ResultIter` method `bool isEndOfRange() const { return parent->isEndOfRange(); }` (`include/scanner/constructs/Results.h:236`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2369 | `ResultIter` operator `bool operator==(const ResultIter &rhs) {` (`include/scanner/constructs/Results.h:238`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2370 | `ResultIter` operator `bool operator==(const ResultBlock<T> &rhs) {` (`include/scanner/constructs/Results.h:242`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2371 | `ResultIter` operator `bool operator!=(const ResultIter<T> &rhs) { return !(*this == rhs); }` (`include/scanner/constructs/Results.h:246`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2372 | `ResultIter` operator `bool operator!=(const ResultBlock<T> &rhs) { return !(*this == rhs); }` (`include/scanner/constructs/Results.h:248`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2373 | `ResultIter` destructor `virtual ~ResultIter() {}` (`include/scanner/constructs/Results.h:250`) | — | — | — | Missing Go | Assessed individually: `scanners::ResultIter` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2374 | `Results` alias `typedef BlockType iterator;` (`include/scanner/constructs/Results.h:265`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. scanner surface. |
| SB-CXX-2375 | `Results` constructor `Results() : resultSet(2000) {` (`include/scanner/constructs/Results.h:267`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2376 | `Results` method `void setException(std::exception_ptr e) { iter->setException(e); }` (`include/scanner/constructs/Results.h:272`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2377 | `Results` method `void add(T *t) { iter->add(t); }` (`include/scanner/constructs/Results.h:274`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2378 | `Results` method `void add_ptr(std::vector<std::shared_ptr<T>> *t) { iter->add(t); }` (`include/scanner/constructs/Results.h:276`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2379 | `Results` method `bool isEndOfRange() const { return iter->isEndOfRange(); }` (`include/scanner/constructs/Results.h:278`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2380 | `Results` method `auto next() {` (`include/scanner/constructs/Results.h:280`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SCAN-018](#sec-9) already carries this obligation — that row cites this exact declaration (`include/scanner/constructs/Results.h:280`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2381 | `Results` method `iterator begin() { return iter->begin(); }` (`include/scanner/constructs/Results.h:290`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2382 | `Results` method `iterator end() { return iter->end(); }` (`include/scanner/constructs/Results.h:292`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2383 | `Results` method `void decrementProducers() {` (`include/scanner/constructs/Results.h:294`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2384 | `Results` method `void registerProducer() { producers++; }` (`include/scanner/constructs/Results.h:300`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-XCUT-010](#sec-20) already carries this obligation — that row cites this exact declaration (`include/scanner/constructs/Results.h:300`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2385 | `Results` destructor `~Results() {}` (`include/scanner/constructs/Results.h:302`) | — | — | — | Missing Go | Assessed individually: `scanners::Results` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2386 | `ScanPair` public data member `Source<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>> *src;` (`include/scanner/constructs/ServerHeuristic.h:39`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2387 | `ScanPair` public data member `Heuristic<T> *heuristic;` (`include/scanner/constructs/ServerHeuristic.h:40`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2388 | `ScanPair` public data member `bool disableRpc;` (`include/scanner/constructs/ServerHeuristic.h:41`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2389 | `ScanPair` public data member `void *ownedAdditionalFeatures;` (`include/scanner/constructs/ServerHeuristic.h:42`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2390 | `ScanPair` public data member `std::atomic<bool> *runningFlag;` (`include/scanner/constructs/ServerHeuristic.h:43`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2391 | `ScannerHeuristic` method `void addClientInterface( std::shared_ptr< interconnect::ClientInterface<interconnect::ThriftTransporter>> serverIfc) {` (`include/scanner/constructs/ServerHeuristic.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2392 | `ScannerHeuristic` constructor `explicit ScannerHeuristic(short numThreads = 10) : logger(logging::LoggerFactory<ScannerHeuristic>::getLogger()), threadCount(numThreads), started(false), running(false), disableRpc(false) {}` (`include/scanner/constructs/ServerHeuristic.h:69`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2393 | `ScannerHeuristic` destructor `~ScannerHeuristic() { close(); }` (`include/scanner/constructs/ServerHeuristic.h:76`) | — | — | — | Missing Go | Assessed individually: `scanners::ScannerHeuristic` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2394 | `ScannerHeuristic` method `bool isRunning() const { return running; }` (`include/scanner/constructs/ServerHeuristic.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2395 | `ScannerHeuristic` method `void disableRpcCalls() { disableRpc = true; }` (`include/scanner/constructs/ServerHeuristic.h:80`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2396 | `ScannerHeuristic` method `virtual void close() {` (`include/scanner/constructs/ServerHeuristic.h:82`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2397 | `ScannerHeuristic` method `void haltOnException( ScanPair<interconnect::ThriftTransporter> *scanResource, const std::shared_ptr<interconnect::ServerInterconnect> &server, interconnect::Scan *scan, std::exception_ptr e) {` (`include/scanner/constructs/ServerHeuristic.h:95`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2398 | `ScannerHeuristic` method `virtual uint16_t scan(Source<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>> *source) {` (`include/scanner/constructs/ServerHeuristic.h:104`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2399 | `SourceConditions` constructor `SourceConditions() {` (`include/scanner/constructs/SourceConditions.h:33`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2400 | `SourceConditions` method `void waitForResults() {` (`include/scanner/constructs/SourceConditions.h:41`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2401 | `SourceConditions` method `void awakeThreadsForResults() {` (`include/scanner/constructs/SourceConditions.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2402 | `SourceConditions` method `bool isAlive() { return alive; }` (`include/scanner/constructs/SourceConditions.h:61`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2403 | `SourceConditions` method `void awakeThreadsFinished() {` (`include/scanner/constructs/SourceConditions.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2404 | `SourceConditions` method `inline void incrementCount() { ++num_results; }` (`include/scanner/constructs/SourceConditions.h:69`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2405 | `SourceConditions` method `inline void decrementCount() { num_results--; }` (`include/scanner/constructs/SourceConditions.h:71`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2406 | `SourceConditions` method `inline int size() { return num_results; }` (`include/scanner/constructs/SourceConditions.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2407 | `TypedResults` constructor `explicit TypedResults(void *copyResultSet) : ResultIter<T>(reinterpret_cast<ResultIter<T> *>(copyResultSet)) {}` (`include/scanner/constructs/TypedResults.h:28`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2408 | `TypedResults` constructor `explicit TypedResults(ResultBlock<T> *copyResultSet) : ResultIter<T>(copyResultSet) {}` (`include/scanner/constructs/TypedResults.h:31`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2409 | `TypedResults` operator `std::unique_ptr<TypeTo> operator*() {` (`include/scanner/constructs/TypedResults.h:34`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. scanner surface. |
| SB-CXX-2410 | `TypedResults` destructor `virtual ~TypedResults() {}` (`include/scanner/constructs/TypedResults.h:39`) | — | — | — | Missing Go | Assessed individually: `scanners::TypedResults` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2411 | `Scanner` constructor `explicit Scanner( std::shared_ptr<cclient::data::Instance> instance, interconnect::TableOperations<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>> *tops, cclient::data::security::Authorizations *auths, uint16_t threads);` (`include/scanner/impl/Scanner.h:61`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2412 | `Scanner` method `virtual void addRange(const cclient::data::Range &range) override;` (`include/scanner/impl/Scanner.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2413 | `Scanner` method `virtual void addRange(std::unique_ptr<cclient::data::Range> range) override;` (`include/scanner/impl/Scanner.h:72`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2414 | `Scanner` method `std::shared_ptr<Results<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>>> getResultSet() override;` (`include/scanner/impl/Scanner.h:79`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scanner_stream`, `shoal_batch_scanner_stream` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | The exact streaming cursor factory is now exported for both scanner kinds with owned lifetime and stable terminal errors. |
| SB-CXX-2415 | `Scanner` method `void addResults( shared_ptr<Results<std::shared_ptr<cclient::data::KeyValue>, ResultBlock<std::shared_ptr<cclient::data::KeyValue>>>> results) {` (`include/scanner/impl/Scanner.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2416 | `Scanner` destructor `virtual ~Scanner() {` (`include/scanner/impl/Scanner.h:87`) | — | — | — | Missing Go | Assessed individually: `scanners::Scanner` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. scanner surface. |
| SB-CXX-2417 | `Scanner` method `std::shared_ptr<cclient::data::Instance> getInstance() override {` (`include/scanner/impl/Scanner.h:90`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2418 | `Scanner` method `void locateFailedTablet( std::vector<std::shared_ptr<cclient::data::Range>> ranges, std::vector<std::shared_ptr<cclient::data::tserver::RangeDefinition>> *locatedTablets) override;` (`include/scanner/impl/Scanner.h:94`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2419 | `Scanner` method `void close() override { scannerHeuristic->close(); }` (`include/scanner/impl/Scanner.h:99`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2420 | `Scanner` method `virtual void setOption(ScannerOptions opt) override;` (`include/scanner/impl/Scanner.h:101`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2421 | `Scanner` method `virtual void removeOption(ScannerOptions opt) override;` (`include/scanner/impl/Scanner.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. scanner surface. |
| SB-CXX-2422 | `(namespace scope)` alias `using Iterator = Results<T, ResultBlock<T>>;` (`include/scanner/impl/Scanner.h:162`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. scanner surface. |
| SB-CXX-2423 | `(namespace scope)` alias `using BatchScanner = scanners::Source<cclient::data::KeyValue, ResultBlock<cclient::data::KeyValue>>;` (`include/scanner/impl/Scanner.h:164`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. scanner surface. |
| SB-CXX-2424 | `ScannerOptions` enum `enum class ScannerOptions : unsigned int {` (`include/scanner/ScannerOptions.h:23`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: BASIC_SCANNER = 0; ENABLE_HEDGED_READS = 1; ENABLE_RFILE_SCANNER = 2. Shoal exposes no equivalent type. |
| SB-CXX-2425 | `EnableBitMaskOperators` constant `static const bool enable = true;` (`include/scanner/ScannerOptions.h:31`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `true`. Shoal exposes no equivalent. scanner surface. |
| SB-CXX-2426 | `Source` constructor `Source() : sourceOptions(ScannerOptions::BASIC_SCANNER) {}` (`include/scanner/Source.h:35`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-053](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:35`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2427 | `Source` method `virtual void addRange(std::unique_ptr<cclient::data::Range> range) = 0;` (`include/scanner/Source.h:37`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-054](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:37`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2428 | `Source` method `virtual void addRange(const cclient::data::Range &range) = 0;` (`include/scanner/Source.h:39`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-055](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:39`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2429 | `Source` method `void addIterators(const std::vector<cclient::data::IterInfo> &iterV) {` (`include/scanner/Source.h:44`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-056](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:44`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2430 | `Source` method `void addIterator(const cclient::data::IterInfo &iterV) {` (`include/scanner/Source.h:48`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-057](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:48`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2431 | `Source` method `void addPythonIterator(const cclient::data::python::PythonIterInfo &iterV) {` (`include/scanner/Source.h:52`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-058](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:52`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2432 | `Source` method `virtual void locateFailedTablet( std::vector<std::shared_ptr<cclient::data::Range>> ranges, std::vector<std::shared_ptr<cclient::data::tserver::RangeDefinition>> *locatedTablets) = 0;` (`include/scanner/Source.h:56`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-059](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:56`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2433 | `Source` method `virtual std::shared_ptr<Results<T, BlockType>> getResultSet() = 0;` (`include/scanner/Source.h:61`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-060](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:61`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2434 | `Source` destructor `virtual ~Source() {}` (`include/scanner/Source.h:63`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-061](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:63`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2435 | `Source` method `virtual void close() {}` (`include/scanner/Source.h:65`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-062](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:65`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2436 | `Source` method `std::vector<cclient::data::Column> getColumns() const { return columns; }` (`include/scanner/Source.h:67`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-063](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:67`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2437 | `Source` method `std::vector<cclient::data::IterInfo> getIters() const { return iters; }` (`include/scanner/Source.h:69`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-064](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:69`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2438 | `Source` method `virtual void setTableOptions(std::map<std::string, std::string> options) {` (`include/scanner/Source.h:71`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-065](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:71`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2439 | `Source` method `virtual void setOption(ScannerOptions opt) { sourceOptions &#124;= opt; }` (`include/scanner/Source.h:75`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-066](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:75`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2440 | `Source` method `virtual void removeOption(ScannerOptions opt) { sourceOptions &= ~opt; }` (`include/scanner/Source.h:77`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-067](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:77`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2441 | `Source` method `virtual bool containsOption(ScannerOptions option) {` (`include/scanner/Source.h:79`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-068](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:79`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2442 | `Source` method `virtual std::shared_ptr<cclient::data::Instance> getInstance() = 0;` (`include/scanner/Source.h:83`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-069](#sec-19) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:83`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2443 | `Source` method `void fetchColumn(std::string col, std::string colqual = "") {` (`include/scanner/Source.h:85`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-SCAN-005](#sec-9) already carries this obligation — that row cites this exact declaration (`include/scanner/Source.h:85`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2444 | `(namespace scope)` macro constant `#define TRACE_BUFFER_SIZE 128` (`include/utils/BackTrace.h:30`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. utility surface published in a public header. |
| SB-CXX-2445 | `BackTrace` constructor `BackTrace() {}` (`include/utils/BackTrace.h:44`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2446 | `BackTrace` constructor `BackTrace(const std::string &name) : name_(name) {}` (`include/utils/BackTrace.h:45`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2447 | `BackTrace` constructor `BackTrace(BackTrace &&) = default;` (`include/utils/BackTrace.h:46`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2448 | `BackTrace` constructor `BackTrace(BackTrace &) = delete;` (`include/utils/BackTrace.h:47`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2449 | `BackTrace` method `std::vector<std::string> getTraces() const { return trace_; }` (`include/utils/BackTrace.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2450 | `BackTrace` operator `BackTrace &operator=(BackTrace &&other) = default;` (`include/utils/BackTrace.h:51`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2451 | `BackTrace` method `std::string getName() const { return name_; }` (`include/utils/BackTrace.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2452 | `(namespace scope)` namespace-scope function `void pull_trace(const uint8_t frames_to_skip = 1);` (`include/utils/BackTrace.h:73`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2453 | `(namespace scope)` namespace-scope function `void handler(int signr, siginfo_t *info, void *secret);` (`include/utils/BackTrace.h:79`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2454 | `(namespace scope)` namespace-scope function `void emplace_handler();` (`include/utils/BackTrace.h:84`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2455 | `TraceResolver` method `BackTrace &&getBackTrace(const std::string &thread_name, std::thread::native_handle_type thread);` (`include/utils/BackTrace.h:96`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2456 | `TraceResolver` method `BackTrace &&getBackTrace(const std::string &thread_name) {` (`include/utils/BackTrace.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2457 | `TraceResolver` method `static TraceResolver &getResolver() {` (`include/utils/BackTrace.h:115`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2458 | `TraceResolver` method `void addTraceLine(const char *symbol_line, const char *func = nullptr) {` (`include/utils/BackTrace.h:125`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2459 | `TraceResolver` method `const std::thread::native_handle_type getThreadHandle() {` (`include/utils/BackTrace.h:137`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2460 | `TraceResolver` method `const std::thread::native_handle_type getCallerHandle() {` (`include/utils/BackTrace.h:144`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2461 | `(namespace scope)` namespace-scope function `static inline std::string getClassName() {` (`include/utils/ClassUtils.h:32`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2462 | `(namespace scope)` namespace-scope function `bool shortenClassName(const std::string &class_name, std::string &out);` (`include/utils/ClassUtils.h:57`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2463 | `ConcurrentQueue` constructor `ConcurrentQueue() = default;` (`include/utils/ConcurrentQueue.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2464 | `ConcurrentQueue` constructor `ConcurrentQueue(const ConcurrentQueue& other) = delete;` (`include/utils/ConcurrentQueue.h:41`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2465 | `ConcurrentQueue` operator `ConcurrentQueue& operator=(const ConcurrentQueue& other) = delete;` (`include/utils/ConcurrentQueue.h:42`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2466 | `ConcurrentQueue` constructor `ConcurrentQueue(ConcurrentQueue&& other)` (`include/utils/ConcurrentQueue.h:43`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2467 | `ConcurrentQueue` operator `ConcurrentQueue& operator=(ConcurrentQueue&& other) {` (`include/utils/ConcurrentQueue.h:47`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2468 | `ConcurrentQueue` method `bool tryDequeue(T& out) {` (`include/utils/ConcurrentQueue.h:57`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2469 | `ConcurrentQueue` method `bool consume(Functor&& fun) {` (`include/utils/ConcurrentQueue.h:63`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2470 | `ConcurrentQueue` method `bool empty() const {` (`include/utils/ConcurrentQueue.h:68`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2471 | `ConcurrentQueue` method `size_t size() const {` (`include/utils/ConcurrentQueue.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2472 | `ConcurrentQueue` method `void clear() {` (`include/utils/ConcurrentQueue.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2473 | `ConcurrentQueue` method `void enqueue(Args&&... args) {` (`include/utils/ConcurrentQueue.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2474 | `ConditionConcurrentQueue` constructor `explicit ConditionConcurrentQueue(bool start = true) : ConcurrentQueue<T>{}, running_{start} {}` (`include/utils/ConcurrentQueue.h:148`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2475 | `ConditionConcurrentQueue` constructor `ConditionConcurrentQueue(const ConditionConcurrentQueue& other) = delete;` (`include/utils/ConcurrentQueue.h:151`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2476 | `ConditionConcurrentQueue` operator `ConditionConcurrentQueue& operator=(const ConditionConcurrentQueue& other) = delete;` (`include/utils/ConcurrentQueue.h:152`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2477 | `ConditionConcurrentQueue` constructor `ConditionConcurrentQueue(ConditionConcurrentQueue&& other) = delete;` (`include/utils/ConcurrentQueue.h:154`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2478 | `ConditionConcurrentQueue` operator `ConditionConcurrentQueue& operator=(ConditionConcurrentQueue&& other) = delete;` (`include/utils/ConcurrentQueue.h:155`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2479 | `ConditionConcurrentQueue` public data member `using ConcurrentQueue<T>::size;` (`include/utils/ConcurrentQueue.h:158`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2480 | `ConditionConcurrentQueue` public data member `using ConcurrentQueue<T>::empty;` (`include/utils/ConcurrentQueue.h:159`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2481 | `ConditionConcurrentQueue` public data member `using ConcurrentQueue<T>::clear;` (`include/utils/ConcurrentQueue.h:160`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2482 | `ConditionConcurrentQueue` method `void enqueue(Args&&... args) {` (`include/utils/ConcurrentQueue.h:163`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2483 | `ConditionConcurrentQueue` method `bool dequeueWait(T& out) {` (`include/utils/ConcurrentQueue.h:170`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2484 | `ConditionConcurrentQueue` method `bool consumeWait(Functor&& fun) {` (`include/utils/ConcurrentQueue.h:179`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2485 | `ConditionConcurrentQueue` method `bool dequeueWaitFor(T& out, const std::chrono::duration<Rep, Period>& time) {` (`include/utils/ConcurrentQueue.h:189`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2486 | `ConditionConcurrentQueue` method `bool dequeueWaitUntil(T& out, const std::chrono::system_clock::time_point& time) {` (`include/utils/ConcurrentQueue.h:197`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2487 | `ConditionConcurrentQueue` method `bool consumeWaitFor(Functor&& fun, const std::chrono::duration<Rep, Period>& time) {` (`include/utils/ConcurrentQueue.h:207`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2488 | `ConditionConcurrentQueue` method `bool consumeWaitUntil(Functor&& fun, const std::chrono::system_clock::time_point& time) {` (`include/utils/ConcurrentQueue.h:218`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2489 | `ConditionConcurrentQueue` method `bool tryDequeue(T& out) {` (`include/utils/ConcurrentQueue.h:228`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2490 | `ConditionConcurrentQueue` method `void stop() {` (`include/utils/ConcurrentQueue.h:233`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2491 | `ConditionConcurrentQueue` method `void start() {` (`include/utils/ConcurrentQueue.h:239`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2492 | `ConditionConcurrentQueue` method `bool isRunning() const {` (`include/utils/ConcurrentQueue.h:244`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2493 | `(namespace scope)` namespace-scope function `constexpr typename std::underlying_type<E>::type to_base(E e) noexcept {` (`include/utils/EnumClass.h:29`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2494 | `EnableBitMaskOperators` constant `static const bool enable = false;` (`include/utils/EnumClass.h:35`) | — | — | — | Missing Go | Public constant object or value. Initialiser exactly as written: `false`. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2495 | `(namespace scope)` namespace-scope operator `operator&#124;=(Enum lhs, Enum rhs) {` (`include/utils/EnumClass.h:40`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2496 | `(namespace scope)` namespace-scope operator `operator&=(Enum lhs, Enum rhs) {` (`include/utils/EnumClass.h:49`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2497 | `(namespace scope)` namespace-scope operator `operator&#124;(Enum lhs, Enum rhs) {` (`include/utils/EnumClass.h:58`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2498 | `(namespace scope)` namespace-scope operator `operator&(Enum lhs, Enum rhs) {` (`include/utils/EnumClass.h:66`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2499 | `(namespace scope)` namespace-scope operator `operator~(Enum lhs) {` (`include/utils/EnumClass.h:74`) | — | — | — | Missing Go | Namespace-scope operator, callable without an object. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2500 | `(namespace scope)` namespace-scope function `INLINE int compare(const void *str1, const void *str2, size_t numbytes, int equality = 0) {` (`include/utils/MemoryUtils.h:28`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2501 | `(namespace scope)` namespace-scope function `INLINE int compare(const void *str1, size_t str1_numbytes, const void *str2, size_t str2_numbytes, int equality = 0) {` (`include/utils/MemoryUtils.h:33`) | — | — | — | Missing Go | Namespace-scope free function, callable without an object. Shoal exports no equivalent. utility surface published in a public header. |
| SB-CXX-2502 | `TimeUnit` enum `enum TimeUnit { DAY, HOUR, MINUTE, SECOND, MILLISECOND, NANOSECOND };` (`include/utils/StringUtils.h:29`) | — | — | — | Missing Go | Ordinal-set row ([§4.2](#sec-4)): the contract is the complete value set with each enumerator's numeric ordinal, which are meaningful only together. Values and ordinals: DAY = 0; HOUR = 1; MINUTE = 2; SECOND = 3; MILLISECOND = 4; NANOSECOND = 5. Shoal exposes no equivalent type. |
| SB-CXX-2503 | `(namespace scope)` macro constant `#define HAVE_REGEX_CPP 1` (`include/utils/StringUtils.h:33`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. utility surface published in a public header. |
| SB-CXX-2504 | `(namespace scope)` macro constant `#define HAVE_REGEX_CPP 0` (`include/utils/StringUtils.h:35`) | — | — | — | Missing Go | Public preprocessor constant: it is part of the compile-time contract of any C++ consumer that includes this header, and changing or dropping it breaks compilation rather than behavior. Shoal exposes no equivalent constant. utility surface published in a public header. |
| SB-CXX-2505 | `StringUtils` method `static bool StringToBool(std::string input, bool& output);` (`include/utils/StringUtils.h:75`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2506 | `StringUtils` method `static std::string trim(std::string s);` (`include/utils/StringUtils.h:84`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2507 | `StringUtils` method `static inline std::string trimLeft(std::string s) {` (`include/utils/StringUtils.h:91`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2508 | `StringUtils` method `static inline std::string trimRight(std::string s) {` (`include/utils/StringUtils.h:105`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2509 | `StringUtils` method `static inline bool equalsIgnoreCase(const std::string& left, const std::string& right) {` (`include/utils/StringUtils.h:117`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2510 | `StringUtils` method `static std::vector<std::string> split(const std::string& str, const std::string& delimiter);` (`include/utils/StringUtils.h:129`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2511 | `StringUtils` method `static bool StringToFloat(std::string input, float& output);` (`include/utils/StringUtils.h:138`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2512 | `StringUtils` method `static std::string replaceEnvironmentVariables(std::string& original_string);` (`include/utils/StringUtils.h:140`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2513 | `StringUtils` method `static std::string& replaceAll(std::string& source_string, const std::string& from_string, const std::string& to_string);` (`include/utils/StringUtils.h:142`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2514 | `StringUtils` method `inline static bool endsWithIgnoreCase(const std::string& value, const std::string& endString) {` (`include/utils/StringUtils.h:146`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2515 | `StringUtils` method `inline static bool endsWith(const std::string& value, const std::string& endString) {` (`include/utils/StringUtils.h:155`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2516 | `StringUtils` method `inline static std::string hex_ascii(const std::string& in) {` (`include/utils/StringUtils.h:161`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2517 | `StringUtils` method `static std::basic_string<TChar> join( const std::basic_string<TChar>& separator, const U& container) {` (`include/utils/StringUtils.h:186`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2518 | `StringUtils` method `static std::basic_string<TChar> join(const TChar* separator, const U& container) {` (`include/utils/StringUtils.h:209`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2519 | `StringUtils` method `static std::basic_string<TChar> join( const std::basic_string<TChar>& separator, const U& container) {` (`include/utils/StringUtils.h:230`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2520 | `StringUtils` method `static std::basic_string<TChar> join(const TChar* separator, const U& container) {` (`include/utils/StringUtils.h:255`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2521 | `StringUtils` method `static std::string replaceMap( std::string source_string, const std::map<std::string, std::string>& replace_map);` (`include/utils/StringUtils.h:259`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2522 | `StringUtils` method `static std::string getEscapedString(std::string const& s);` (`include/utils/StringUtils.h:263`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2523 | `(namespace scope)` alias `using TaskId = std::string;` (`include/utils/ThreadPool.h:41`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. utility surface published in a public header. |
| SB-CXX-2524 | `AfterExecute` destructor `virtual ~AfterExecute() = default;` (`include/utils/ThreadPool.h:50`) | — | — | — | Missing Go | Assessed individually: `utils::AfterExecute` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. utility surface published in a public header. |
| SB-CXX-2525 | `AfterExecute` constructor `AfterExecute() = default;` (`include/utils/ThreadPool.h:52`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2526 | `AfterExecute` constructor `explicit AfterExecute(AfterExecute &&other) {}` (`include/utils/ThreadPool.h:54`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2527 | `AfterExecute` method `virtual bool isFinished(const T &result) = 0;` (`include/utils/ThreadPool.h:55`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2528 | `AfterExecute` method `virtual bool isCancelled(const T &result) = 0;` (`include/utils/ThreadPool.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2529 | `AfterExecute` method `virtual std::chrono::milliseconds wait_time() = 0;` (`include/utils/ThreadPool.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2530 | `TimerAwareMonitor` constructor `TimerAwareMonitor(std::atomic<bool> *run_monitor)` (`include/utils/ThreadPool.h:71`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2531 | `TimerAwareMonitor` method `bool isFinished(const std::chrono::milliseconds &result) override {` (`include/utils/ThreadPool.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2532 | `TimerAwareMonitor` method `bool isCancelled(const std::chrono::milliseconds & ) override {` (`include/utils/ThreadPool.h:81`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2533 | `TimerAwareMonitor` method `std::chrono::milliseconds wait_time() override {` (`include/utils/ThreadPool.h:92`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2534 | `TaskRescheduleInfo` constructor `TaskRescheduleInfo(bool result, std::chrono::milliseconds wait_time)` (`include/utils/ThreadPool.h:102`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2535 | `TaskRescheduleInfo` public data member `std::chrono::milliseconds wait_time_;` (`include/utils/ThreadPool.h:105`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2536 | `TaskRescheduleInfo` public data member `bool finished_;` (`include/utils/ThreadPool.h:106`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2537 | `TaskRescheduleInfo` method `static TaskRescheduleInfo Done() {` (`include/utils/ThreadPool.h:108`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2538 | `TaskRescheduleInfo` method `static TaskRescheduleInfo RetryIn(std::chrono::milliseconds interval) {` (`include/utils/ThreadPool.h:112`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2539 | `TaskRescheduleInfo` method `static TaskRescheduleInfo RetryImmediately() {` (`include/utils/ThreadPool.h:116`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2540 | `ComplexMonitor` constructor `ComplexMonitor() = default;` (`include/utils/ThreadPool.h:133`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2541 | `ComplexMonitor` method `bool isFinished(const TaskRescheduleInfo &result) override {` (`include/utils/ThreadPool.h:135`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2542 | `ComplexMonitor` method `bool isCancelled(const TaskRescheduleInfo & ) override {` (`include/utils/ThreadPool.h:142`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2543 | `ComplexMonitor` method `std::chrono::milliseconds wait_time() override {` (`include/utils/ThreadPool.h:150`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2544 | `Worker` constructor `explicit Worker(const std::function<T()> &task, const TaskId &identifier, std::unique_ptr<AfterExecute<T>> run_determinant) : identifier_(identifier), next_exec_time_(std::chrono::steady_clock::now()), task(task), run_determinant_(std::move(run_determinant)) {` (`include/utils/ThreadPool.h:170`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2545 | `Worker` constructor `explicit Worker(const std::function<T()> &task, const TaskId &identifier) : identifier_(identifier), next_exec_time_(std::chrono::steady_clock::now()), task(task), run_determinant_(nullptr) {` (`include/utils/ThreadPool.h:179`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2546 | `Worker` constructor `explicit Worker(const TaskId &identifier = {})` (`include/utils/ThreadPool.h:187`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2547 | `Worker` destructor `virtual ~Worker() = default;` (`include/utils/ThreadPool.h:191`) | — | — | — | Missing Go | Assessed individually: `utils::Worker` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. utility surface published in a public header. |
| SB-CXX-2548 | `Worker` constructor `Worker(Worker &&other) noexcept : identifier_(std::move(other.identifier_)), next_exec_time_(std::move(other.next_exec_time_)), task(std::move(other.task)), run_determinant_(std::move(other.run_determinant_)), promise(other.promise) {}` (`include/utils/ThreadPool.h:196`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2549 | `Worker` method `virtual bool run() {` (`include/utils/ThreadPool.h:210`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2550 | `Worker` method `virtual void setIdentifier(const TaskId &identifier) {` (`include/utils/ThreadPool.h:223`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2551 | `Worker` method `virtual std::chrono::time_point<std::chrono::steady_clock> getNextExecutionTime() const {` (`include/utils/ThreadPool.h:228`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2552 | `Worker` method `virtual std::chrono::milliseconds getWaitTime() const {` (`include/utils/ThreadPool.h:232`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2553 | `Worker` operator `Worker<T> &operator=(const Worker<T> &) = delete;` (`include/utils/ThreadPool.h:237`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2554 | `Worker` operator `Worker<T> &operator=(Worker<T> &&) noexcept;` (`include/utils/ThreadPool.h:239`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2555 | `Worker` method `std::shared_ptr<std::promise<T>> getPromise() const;` (`include/utils/ThreadPool.h:241`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2556 | `Worker` method `const TaskId &getIdentifier() const { return identifier_; }` (`include/utils/ThreadPool.h:243`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2557 | `DelayedTaskComparator` operator `bool operator()(Worker<T> &a, Worker<T> &b) {` (`include/utils/ThreadPool.h:256`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2558 | `WorkerThread` constructor `explicit WorkerThread(std::thread thread, const std::string &name = "NamelessWorker") : is_running_(false), thread_(std::move(thread)), name_(name) {}` (`include/utils/ThreadPool.h:278`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2559 | `WorkerThread` constructor `WorkerThread(const std::string &name = "NamelessWorker")` (`include/utils/ThreadPool.h:281`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2560 | `WorkerThread` public data member `std::atomic<bool> is_running_;` (`include/utils/ThreadPool.h:283`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2561 | `WorkerThread` public data member `std::thread thread_;` (`include/utils/ThreadPool.h:284`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2562 | `WorkerThread` public data member `std::string name_;` (`include/utils/ThreadPool.h:285`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2563 | `ThreadPool` constructor `ThreadPool(int max_worker_threads = 2, bool daemon_threads = false, const std::string &name = "NamelessPool")` (`include/utils/ThreadPool.h:297`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2564 | `ThreadPool` constructor `ThreadPool(const ThreadPool<T> &other) = delete;` (`include/utils/ThreadPool.h:309`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2565 | `ThreadPool` operator `ThreadPool<T> &operator=(const ThreadPool<T> &other) = delete;` (`include/utils/ThreadPool.h:310`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2566 | `ThreadPool` constructor `ThreadPool(ThreadPool<T> &&other) = delete;` (`include/utils/ThreadPool.h:311`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2567 | `ThreadPool` operator `ThreadPool<T> &operator=(ThreadPool<T> &&other) = delete;` (`include/utils/ThreadPool.h:312`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. utility surface published in a public header. |
| SB-CXX-2568 | `ThreadPool` destructor `~ThreadPool() { shutdown(); }` (`include/utils/ThreadPool.h:314`) | — | — | — | Missing Go | Assessed individually: `utils::ThreadPool` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. utility surface published in a public header. |
| SB-CXX-2569 | `ThreadPool` method `bool execute(Worker<T> &&task, std::future<T> &future);` (`include/utils/ThreadPool.h:325`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2570 | `ThreadPool` method `void stopTasks(const TaskId &identifier);` (`include/utils/ThreadPool.h:332`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2571 | `ThreadPool` method `void resume();` (`include/utils/ThreadPool.h:337`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2572 | `ThreadPool` method `void pause();` (`include/utils/ThreadPool.h:342`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2573 | `ThreadPool` method `bool isTaskRunning(const TaskId &identifier) {` (`include/utils/ThreadPool.h:347`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2574 | `ThreadPool` method `bool isRunning() const { return running_.load(); }` (`include/utils/ThreadPool.h:354`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2575 | `ThreadPool` method `void start();` (`include/utils/ThreadPool.h:359`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2576 | `ThreadPool` method `void shutdown();` (`include/utils/ThreadPool.h:364`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2577 | `ThreadPool` method `void setMaxConcurrentTasks(uint16_t max) {` (`include/utils/ThreadPool.h:370`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2578 | `(namespace scope)` alias `using void_t = void;` (`include/utils/TryMoveCall.h:23`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. utility surface published in a public header. |
| SB-CXX-2579 | `(namespace scope)` using-declaration `using std::void_t;` (`include/utils/TryMoveCall.h:25`) | — | — | — | Missing Go | **Using-declaration**: it re-declares an existing name from another namespace inside this one, so consumers may write the unqualified name. It declares no object of its own and has no linkage; the obligation is that the name remains reachable unqualified. Shoal exposes no equivalent. utility surface published in a public header. |
| SB-CXX-2580 | `TryMoveCall` method `static auto call(Fun&& fun, T& elem) -> decltype(std::forward<Fun>(fun)(elem)) {` (`include/utils/TryMoveCall.h:36`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2581 | `TryMoveCall` method `static auto call(Fun&& fun, T& elem) -> decltype(std::forward<Fun>(fun)(std::move(elem))) {` (`include/utils/TryMoveCall.h:70`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. utility surface published in a public header. |
| SB-CXX-2582 | `Writer` constructor `Writer(std::shared_ptr<cclient::data::Instance> instance, interconnect::TableOperations< cclient::data::KeyValue, scanners::ResultBlock<cclient::data::KeyValue>> *tops, cclient::data::security::Authorizations *auths, uint16_t threads);` (`include/writer/impl/SinkImpl.h:39`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2583 | `Writer` destructor `virtual ~Writer();` (`include/writer/impl/SinkImpl.h:44`) | — | — | — | Missing Go | Assessed individually: `writer::Writer` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. writer surface. |
| SB-CXX-2584 | `Writer` method `void flush(bool override = false);` (`include/writer/impl/SinkImpl.h:46`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2585 | `Writer` method `void setHeuristic( scanners::Heuristic<interconnect::ThriftTransporter> *heuristic) {` (`include/writer/impl/SinkImpl.h:48`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2586 | `Writer` method `virtual bool addMutation( const std::shared_ptr<cclient::data::Mutation> &mut) {` (`include/writer/impl/SinkImpl.h:53`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2587 | `Writer` method `virtual bool addMutation(std::unique_ptr<cclient::data::Mutation> obj) {` (`include/writer/impl/SinkImpl.h:62`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2588 | `Writer` method `inline virtual size_t size() {` (`include/writer/impl/SinkImpl.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2589 | `(namespace scope)` alias `using BatchWriter = writer::Sink<cclient::data::KeyValue>;` (`include/writer/impl/SinkImpl.h:107`) | — | — | — | Missing Go | Public type alias: consumers name this type directly, so removing or re-pointing it is a source-breaking change. Shoal exposes no equivalent alias. writer surface. |
| SB-CXX-2590 | `WritePair` constructor `explicit WritePair( std::shared_ptr<cclient::data::tserver::ServerDefinition> rangeDef, void *ref, const cclient::impl::Configuration *conf, std::shared_ptr<cclient::data::TabletServerMutations> mutations) : rangeDef(rangeDef), conf(conf), ref(ref), mutations(mutations) {}` (`include/writer/impl/WriterHeuristic.h:32`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2591 | `WritePair` constructor `explicit WritePair(const WritePair &&other) : rangeDef(std::move(other.rangeDef)), conf(other.conf), ref(std::move(other.ref)), mutations(std::move(other.mutations)) {}` (`include/writer/impl/WriterHeuristic.h:37`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2592 | `WritePair` operator `WritePair &operator=(const WritePair &&other) {` (`include/writer/impl/WriterHeuristic.h:43`) | — | — | — | Missing Go | Member operator. Shoal exports no equivalent; C++ operator overloading has no Go or C ABI analogue, so a port must expose a named function and change call syntax. writer surface. |
| SB-CXX-2593 | `WritePair` public data member `std::shared_ptr<cclient::data::tserver::ServerDefinition> rangeDef;` (`include/writer/impl/WriterHeuristic.h:51`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. writer surface. |
| SB-CXX-2594 | `WritePair` public data member `const cclient::impl::Configuration *conf;` (`include/writer/impl/WriterHeuristic.h:52`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. writer surface. |
| SB-CXX-2595 | `WritePair` public data member `std::shared_ptr<cclient::data::TabletServerMutations> mutations;` (`include/writer/impl/WriterHeuristic.h:53`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. writer surface. |
| SB-CXX-2596 | `WritePair` public data member `void *ref;` (`include/writer/impl/WriterHeuristic.h:54`) | — | — | — | Missing Go | Public data member: readable and writable without an accessor, so it is part of the ABI shape as well as the API. Shoal exposes no equivalent. writer surface. |
| SB-CXX-2597 | `WriterHeuristic` constructor `WriterHeuristic(short numThreads = 10, uint32_t queueSize = 2000);` (`include/writer/impl/WriterHeuristic.h:63`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2598 | `WriterHeuristic` method `void addClientInterface( std::shared_ptr< interconnect::ClientInterface<interconnect::ThriftTransporter>> serverIfc) {` (`include/writer/impl/WriterHeuristic.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2599 | `WriterHeuristic` method `uint16_t write( std::shared_ptr<cclient::data::tserver::ServerDefinition> rangeDef, const cclient::impl::Configuration *conf, std::shared_ptr<cclient::data::TabletServerMutations> mutations) {` (`include/writer/impl/WriterHeuristic.h:74`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2600 | `WriterHeuristic` method `int close() {` (`include/writer/impl/WriterHeuristic.h:103`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2601 | `WriterHeuristic` method `uint64_t maxThreads() { return threadCount; }` (`include/writer/impl/WriterHeuristic.h:128`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2602 | `WriterHeuristic` method `uint64_t size() { return conditionals->getMutationCount(); }` (`include/writer/impl/WriterHeuristic.h:130`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2603 | `WriterHeuristic` destructor `virtual ~WriterHeuristic();` (`include/writer/impl/WriterHeuristic.h:131`) | — | — | — | Missing Go | Assessed individually: `writer::WriterHeuristic` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. writer surface. |
| SB-CXX-2604 | `WriterHeuristic` method `void restart_failures( std::vector<std::shared_ptr<cclient::data::Mutation>> *mutations) {` (`include/writer/impl/WriterHeuristic.h:133`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2605 | `WriterHeuristic` method `void addFailedMutation( std::shared_ptr<cclient::data::TabletServerMutations> mutation) {` (`include/writer/impl/WriterHeuristic.h:140`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2606 | `WriterHeuristic` method `void push_failures( std::vector<std::shared_ptr<cclient::data::Mutation>> *mutations) {` (`include/writer/impl/WriterHeuristic.h:151`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2607 | `Sink` constructor `Sink(uint16_t maxQueue) : queueSize(maxQueue), sinkQueue((maxQueue * 1.5)) {}` (`include/writer/Sink.h:43`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-046](#sec-19) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:43`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2608 | `Sink` destructor `virtual ~Sink() {}` (`include/writer/Sink.h:45`) | — | — | — | Missing Go | Assessed individually: `writer::Sink` has no Python binding, so this destructor is reachable only by a C++ consumer that owns the object. Shoal exports no equivalent type, so there is nothing to destroy — the gap is the type, and this row records its destruction contract rather than excluding it. writer surface. |
| SB-CXX-2609 | `Sink` method `bool push(std::unique_ptr<T> obj);` (`include/writer/Sink.h:51`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-047](#sec-19) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:51`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2610 | `Sink` method `bool push(std::shared_ptr<T> obj);` (`include/writer/Sink.h:57`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-048](#sec-19) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:57`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2611 | `Sink` method `virtual void flush(bool override = false) = 0;` (`include/writer/Sink.h:62`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-WRITE-003](#sec-10) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:62`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2612 | `Sink` method `virtual bool addMutation( const std::shared_ptr<cclient::data::Mutation> &mut) = 0;` (`include/writer/Sink.h:67`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2613 | `Sink` method `virtual bool addMutation(std::unique_ptr<cclient::data::Mutation> obj) = 0;` (`include/writer/Sink.h:73`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2614 | `Sink` method `virtual void close() { flush(true); }` (`include/writer/Sink.h:78`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2615 | `Sink` method `virtual void exit() { flush(true); }` (`include/writer/Sink.h:80`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-051](#sec-19) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:80`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2616 | `Sink` method `inline virtual size_t size() { return sinkQueue.size_approx(); }` (`include/writer/Sink.h:82`) | — | — | — | Not required (rationale required) | Rationale: the normative row [SB-CPP-052](#sec-19) already carries this obligation — that row cites this exact declaration (`include/writer/Sink.h:82`). That row keeps the status and the evidence; this row exists only so the declaration-to-row map stays 1:1, and it must never be counted as a second gap. |
| SB-CXX-2617 | `SinkConditions` constructor `SinkConditions() {` (`include/writer/SinkConditionals.h:25`) | — | — | — | Missing Go | Public constructor with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2618 | `SinkConditions` method `void waitForResults() {` (`include/writer/SinkConditionals.h:31`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2619 | `SinkConditions` method `void awakeThreadsForResults() {` (`include/writer/SinkConditionals.h:40`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2620 | `SinkConditions` method `bool isAlive() { return alive; }` (`include/writer/SinkConditionals.h:45`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2621 | `SinkConditions` method `bool isClosing() { return closing; }` (`include/writer/SinkConditionals.h:47`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2622 | `SinkConditions` method `void close() {` (`include/writer/SinkConditionals.h:49`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2623 | `SinkConditions` method `void incrementMutationCount(uint32_t count = 1) { mutation_count += count; }` (`include/writer/SinkConditionals.h:54`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2624 | `SinkConditions` method `uint64_t getMutationCount() { return mutation_count; }` (`include/writer/SinkConditionals.h:56`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2625 | `SinkConditions` method `void decrementMutationCount(uint32_t count = 1) { mutation_count -= count; }` (`include/writer/SinkConditionals.h:58`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |
| SB-CXX-2626 | `SinkConditions` method `void awakeThreadsFinished() {` (`include/writer/SinkConditionals.h:60`) | — | — | — | Missing Go | Public method with no exported Shoal equivalent in `accumulo/` and no declaration in `capi/include/shoal.h`. writer surface. |

<a id="sec-19-3"></a>

### 19.3 Declaration-to-row verifier

The verifier is a mechanical check, re-run after every edit, that fails on any
of these conditions:

1. **Identity** — `rows ≠ members + enums + memberless types`.
2. **Orphan citation** — a `SB-CXX` row cites a `header:line` that the scanner
   did not record.
3. **Unenumerated declaration** — a scanned live declaration with no row.
4. **Artifact** — a row whose signature is a bare fragment rather than a
   declaration.
5. **Scope leak** — a row for a private or protected member, an anonymous
   namespace member, or a vendored header.
6. **Empty enum** — an enum row with no enumerator list.
7. **Unresolved duplicate** — a row whose member matches a normative row
   without a `Not required` status and an explicit cross-reference.
8. **Malformed row** — wrong column count, unbalanced backticks, or a status
   outside the six permitted values.
9. **Callable misclassification** — a function recorded as a constant or field.
10. **Silent drop** — a public member present in the header with no record.
11. **Truncated signature** — a row signature that is not a character-for-
    character copy of its scanner record, a record that ends mid-declaration,
    or a record with an unbalanced parameter list.
12. **Unproved duplicate** — a `Not required` duplicate whose stated rule
    cannot be re-derived from the cited evidence. `SB-KEEP`, `SB-UNSAFE`,
    `SB-DIV`, and `SB-GAP` entries are rejected as duplicate targets.
13. **Grouped declarator** — a declaration statement with more than one
    declarator that produced fewer rows than declarators.
14. **Enum ordinal** — ordinals that do not match the values recomputed from
    the header.
15. **Cross-reference anchor** — a `[SB-…](#sec-…)` link whose anchor is not
    the section the referenced row physically lives in.

Revision 9 adds five checks, each derived from a class of omission the seventh
audit found. These are **class coverage** checks: they compare the inventory
against the headers, not the document against itself, because every revision-8
defect was invisible to document-internal checking.

16. **Class coverage** — for every public class in a live header, the count of
    public members between its `public:` marker and its closing brace is
    recompared against the rows emitted for it. `writer::Sink` (10),
    `cclient::data::KeyAllocatorPool` (5), and `utils::StringUtils` (18,
    including all four templated `join` overloads) are asserted explicitly.
17. **Scope-state leak** — after every anonymous namespace closes, the next
    public declaration in that file must be enumerated. Any file with an
    anonymous namespace and zero live records after it fails.
18. **Vendored justification** — every excluded header must match a vendored
    path or carry a third-party license block in its own comment header. A
    header excluded because it *references* a vendored symbol fails, and the
    Python-exposed `Hdfs`, `HdfsInputStream`, and `HdfsOutputStream` wrappers
    are asserted present.
19. **Namespace-scope coverage** — declarations at namespace scope, including
    those inside `extern "C"` blocks and namespace-scope variables, must be
    enumerated. The ten declarations the audit named are asserted by line.
20. **Guard artifact** — no row may be a `#define` whose preceding directive is
    the matching `#ifndef`/`#if !defined`, in either the valueless or the
    `1`-valued spelling.

Revision 10 adds four more, each derived from a defect the ninth audit found.

21. **Memberless type** — a type recorded as having no public member is
    re-checked against templated and operator members before the row is
    emitted. `interconnect::CopyKey` is asserted to carry
    `operator()`.
22. **Conversion operator** — declarations of the form `operator T(...)` must be
    enumerated as operators, not dropped;
    `AccumuloInfoBuilder::operator AccumuloInfo &&` and
    `TabletServerStatusBuilder::operator TabletServerStatus &&` are asserted by
    line.
23. **Unevaluated value** — no enum or constant row may present a
    template-dependent or otherwise unevaluated initialiser as a numeric
    ordinal. Rows whose initialiser cannot be evaluated must say so and quote
    the source text.
24. **Narrative reconciliation** — every count stated in prose or in a metadata
    field (live-API headers, vendored headers, declaration-free headers,
    members, enums, memberless types, rows, required rows) must equal the value
    recomputed from the inventory and from the matrix rows. The check scans
    **every occurrence**, not one canonical sentence: it walks all non-row lines
    with their neighbours, extracts each numeric or spelled-out claim, and
    compares it with the derived value, so a stale figure in a revision-history
    bullet fails exactly like a stale figure in [§1](#sec-1). It also requires
    the header partition to be stated as arithmetic that sums
    (live + vendored + declaration-free = total) and every declaration-free
    header to be named.

Revision 11 adds three more, from the tenth audit:

25. **Friend function** — every `friend` function or operator declared or
    defined in a live header must have a row, and no `friend class` declaration
    may produce one. The twelve `friend operator<<` overloads are asserted by
    line.
26. **Out-of-class definition** — a qualified definition (`A<T>::m`) may not
    produce a row of its own when the class declares `m`; the definition is
    merged and the merge is recorded with the in-class access.
27. **Access classification** — no row may correspond to a protected or private
    declaration, and every excluded member must carry its access label in the
    inventory, so the exclusion can be re-derived rather than trusted.

Revision 12 adds four more, from the eleventh audit:

28. **Bit-field member** — every named bit-field in a live header must have its
    own row; the five in `include/extern/accumulo_data.h` are asserted by line.
29. **Typedef versus function** — no `typedef`/`using` declaration may be
    classified as a callable, and function-pointer aliases must say they name a
    callback type.
30. **Macro-invocation artifact** — an invocation of a macro defined outside the
    pinned tree may not produce a declaration row, while macro definitions and
    macro-declared types keep theirs.
31. **Linkage classification** — a namespace-scope variable declared `static`
    must be described as internal linkage with one copy per translation unit,
    and no row or divergence rationale may call it process-wide.

Revision 13 adds three more, from the twelfth audit:

32. **Forward declaration** — no `class`, `struct`, `union`, or `enum` forward
    declaration may be recorded as a variable or a function. Where the pinned
    tree defines the type, the declaration is merged into that definition and
    the merge names the defining line; where it does not, the row describes an
    opaque type.
33. **Using form** — using-declarations, using aliases, namespace aliases, and
    `using namespace` directives are classified distinctly, and none of them
    may be a variable.
34. **Namespace-variable population** — the count of namespace-scope variables
    and their linkage split are re-derived **from the headers by a second,
    independent walk** that tracks brace, namespace, and class depth without
    consulting the inventory, then compared with the inventory, with the matrix
    rows, and with every prose figure anywhere in the document. Agreement of two
    independent parsers plus the rows is the evidence; a claim that matches none
    of the derived numbers fails.

35. **Registration DSL** — a header that registers types or members through a
    binding DSL (`PYBIND11_EMBEDDED_MODULE`, `pybind11::class_`, `.def`) is a
    public surface even when it contains no C++ declaration. Every registration
    must have a row, and no such header may be counted declaration-free. The
    check walks each registration from its opening token until its parentheses
    balance, so a chained call that spans lines is one registration with one
    span, and it compares the row against that span rather than against the
    first line.
37. **Registration completeness** — for every registration, the row must cite
    the full span (`file:start-end` when it spans lines) and must contain the
    complete registered contract: the callable name, and every
    `pybind11::arg`/`"name"_a` keyword with its default token exactly as
    written. A row that keeps only the callable name, or that stops at a line
    break, fails.
36. **Unbuilt public header** — a header that no translation unit includes, or
    whose feature option defaults to `OFF`, may not be represented as a working
    capability anywhere in the document. Its rows must state the build
    exclusion, and any capability row that depends on it must cite the same
    evidence.

Checks 35 and 36 are enforced against the source, not the prose: check 35
re-scans every `PYBIND11_EMBEDDED_MODULE`, `pybind11::class_`, and `.def`
registration in every non-vendored header and requires one row each (35 in the
one such header), and check 36 re-counts references to that header across the
whole pinned tree and fails if the dead-code rationale ever becomes stale.

Result on this revision: **0 exceptions in all thirty-seven checks.** Checks 24
and 34 now scan every occurrence in the document rather than a single canonical
line, which is how revision 13's two surviving contradictions were found. In
addition,
every `pysharkbite.cpp` citation on a Python-visible row is verified to contain
the binding it claims.

<a id="sec-19-4"></a>

### 19.4 Matrix: dead embedded-module surface (`SB-EMB`)

`include/jni/PythonBindings.h` registers a complete pybind11 module,
`sharkbite_iterator`, that a server-side Python iterator would import: five
types and twenty-nine constructor and method registrations. Revision 14 treated
the header as declaration-free, because its two macro invocations are not C++
declarations. That was mechanically defensible and semantically wrong: a
registration DSL **is** a public surface, and silently dropping it hid a whole
Python API from the matrix.

It is enumerated here, one row per registration, with the single status the
evidence supports — `Not required` — because the surface is **pinned dead
code**:

| Fact | Evidence |
| --- | --- |
| No translation unit includes the header | zero references to `PythonBindings` anywhere in the pinned tree, including `CMakeLists.txt` |
| The feature is off by default | `option(PYTHON_ITERATOR_SUPPORT "Builds python iterator support." OFF)` (`CMakeLists.txt:37`) |
| The registered iterator type does not exist | `cclient::jni::python::PythonIterator` is never declared; `include/jni/PythonIterator.h:28-35` contains empty namespaces |
| Its implementation is commented out | `src/jni/PythonIterator.cpp:26-73` is one comment block; only empty namespaces remain |
| The executor it depends on is empty too | `include/jni/IteratorExecutor.h:28-33` declares nothing |
| Therefore it cannot compile as written | `include/jni/PythonBindings.h:89-95` takes the address of members of a type that does not exist |

**This is not a silent exclusion and not a claim of parity.** Every
registration has a row, so an auditor can see exactly what a revived module
would owe. If the project revives it, each row becomes an obligation and the
`Not required` rationale is withdrawn in the same change. The capability-level
consequence is recorded in [SB-SCAN-016](#sec-9), which no longer describes
server-side Python iterators as a working Sharkbite capability, and in
[SB-DIV-007](#sec-26).

**Registrations are recorded as spans, not as lines.** A pybind11 registration
is a chained call that may continue across lines, so each row is derived by
walking from the registration's opening token until its parentheses balance.
The citation is `file:start-end` whenever the registration is multi-line, and
the row repeats the callable name together with every `py::arg`/`"name"_a`
keyword and its default token exactly as written. Of the 35 registrations, 34
are single-line and one — `SB-EMB-010`, the five-argument `Key` constructor at
`include/jni/PythonBindings.h:65-66` — spans two lines and carries the module's
only keyword-argument contract: `row`, `columnfamily=""`,
`columnqualifier=""`, `columnvisibility=""`, and
`timestamp=9223372036854775807L`. Recording only the first line would have kept
the arity and lost the defaults. Check 37 ([§19.3](#sec-19)) re-derives every
span from the header and fails if a row cites a partial span, omits a keyword,
or drops a default.

| ID | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| SB-EMB-001 | Embedded module registration `PYBIND11_EMBEDDED_MODULE(sharkbite_iterator, m)`: declares the Python module a server-side iterator would import (`include/jni/PythonBindings.h:51`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-002 | `sharkbite_iterator.Value` Python type registration (C++ `cclient::data::Value`) `pybind11::class_<cclient::data::Value, std::shared_ptr<cclient::data::Value>>(m, "Value")` (`include/jni/PythonBindings.h:56`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-003 | `sharkbite_iterator.Value.__init__` overload registration `.def(pybind11::init<>())` (`include/jni/PythonBindings.h:57`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-004 | `sharkbite_iterator.Value.__init__` overload registration `.def(pybind11::init<const std::string&>())` (`include/jni/PythonBindings.h:58`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-005 | `sharkbite_iterator.Value.get` method registration `.def("get", &cclient::data::Value::getValueAsString, "Returns the value as a UTF-8 string")` (`include/jni/PythonBindings.h:59`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-006 | `sharkbite_iterator.Value.set` method registration `.def("set",(void (cclient::data::Value::*)(const std::string &) ) &cclient::data::Value::setValue, "Sets the byte array value")` (`include/jni/PythonBindings.h:60`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-007 | `sharkbite_iterator.Key` Python type registration (C++ `cclient::data::Key`) `pybind11::class_<cclient::data::Key, std::shared_ptr<cclient::data::Key>>(m, "Key")` (`include/jni/PythonBindings.h:62`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-008 | `sharkbite_iterator.Key.__init__` overload registration `.def(pybind11::init<>())` (`include/jni/PythonBindings.h:63`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-009 | `sharkbite_iterator.Key.__init__` overload registration `.def(pybind11::init<std::shared_ptr<cclient::data::Key>>())` (`include/jni/PythonBindings.h:64`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-010 | `sharkbite_iterator.Key.__init__` overload registration `.def(pybind11::init<const std::string &,const std::string &, const std::string &, const std::string &, int64_t>(), "row"_a, "columnfamily"_a="","columnqualifier"_a="","columnvisibility"_a="","timestamp"_a=9223372036854775807L)` (`include/jni/PythonBindings.h:65-66`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. Keyword-argument contract exactly as registered: row; columnfamily=""; columnqualifier=""; columnvisibility=""; timestamp=9223372036854775807L. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-011 | `sharkbite_iterator.Key.setRow` method registration `.def("setRow",(void (cclient::data::Key::*)(const std::string &) ) &cclient::data::Key::setRow, "Sets the row")` (`include/jni/PythonBindings.h:67`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-012 | `sharkbite_iterator.Key.setColumnFamily` method registration `.def("setColumnFamily",(void (cclient::data::Key::*)(const std::string &) ) &cclient::data::Key::setColFamily, "Sets the column family")` (`include/jni/PythonBindings.h:68`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-013 | `sharkbite_iterator.Key.setColumnQualifier` method registration `.def("setColumnQualifier",(void (cclient::data::Key::*)(const std::string &) ) &cclient::data::Key::setColQualifier, "Sets the column qualifier")` (`include/jni/PythonBindings.h:69`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-014 | `sharkbite_iterator.Key.getRow` method registration `.def("getRow", &cclient::data::Key::getRowStr, "Gets the row")` (`include/jni/PythonBindings.h:70`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-015 | `sharkbite_iterator.Key.getColumnFamily` method registration `.def("getColumnFamily", &cclient::data::Key::getColFamilyStr, "Gets the Column Family")` (`include/jni/PythonBindings.h:71`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-016 | `sharkbite_iterator.Key.getColumnVisibility` method registration `.def("getColumnVisibility", &cclient::data::Key::getColVisibilityStr, "Gets the Column Visibility")` (`include/jni/PythonBindings.h:72`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-017 | `sharkbite_iterator.Key.getTimestamp` method registration `.def("getTimestamp", &cclient::data::Key::getTimeStamp, "Gets the key timestamp")` (`include/jni/PythonBindings.h:73`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-018 | `sharkbite_iterator.Key.getColumnQualifier` method registration `.def("getColumnQualifier", &cclient::data::Key::getColQualifierStr, "Gets the Column Qualifier")` (`include/jni/PythonBindings.h:74`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-019 | `sharkbite_iterator.KeyValue` Python type registration (C++ `cclient::data::KeyValue`) `pybind11::class_<cclient::data::KeyValue, std::shared_ptr<cclient::data::KeyValue>>(m, "KeyValue")` (`include/jni/PythonBindings.h:76`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-020 | `sharkbite_iterator.KeyValue.__init__` overload registration `.def(pybind11::init<>())` (`include/jni/PythonBindings.h:77`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-021 | `sharkbite_iterator.KeyValue.__init__` overload registration `.def(pybind11::init<const std::shared_ptr<cclient::data::Key> &, const std::shared_ptr<cclient::data::Value> &>())` (`include/jni/PythonBindings.h:78`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-022 | `sharkbite_iterator.KeyValue.setKey` method registration `.def("setKey", &cclient::data::KeyValue::setKey, "Sets the key")` (`include/jni/PythonBindings.h:79`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-023 | `sharkbite_iterator.KeyValue.setValue` method registration `.def("setValue", (void (cclient::data::KeyValue::*)(const std::shared_ptr<cclient::data::Value> &) ) &cclient::data::KeyValue::setValue, "Sets the value")` (`include/jni/PythonBindings.h:80`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-024 | `sharkbite_iterator.KeyValue.getKey` method registration `.def("getKey", &cclient::data::KeyValue::getKey, "Gets the key")` (`include/jni/PythonBindings.h:81`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-025 | `sharkbite_iterator.KeyValue.getValue` method registration `.def("getValue", &cclient::data::KeyValue::getValue, "Gets the value")` (`include/jni/PythonBindings.h:82`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-026 | `sharkbite_iterator.Range` Python type registration (C++ `cclient::data::Range`) `pybind11::class_<cclient::data::Range,std::shared_ptr<cclient::data::Range>>(m, "Range")` (`include/jni/PythonBindings.h:84`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-027 | `sharkbite_iterator.Range.__init__` overload registration `.def(pybind11::init<const std::string&>())` (`include/jni/PythonBindings.h:85`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-028 | `sharkbite_iterator.Range.__init__` overload registration `.def(pybind11::init<std::shared_ptr<cclient::data::Key>,bool,std::shared_ptr<cclient::data::Key>,bool>())` (`include/jni/PythonBindings.h:86`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-029 | `sharkbite_iterator.PythonIterator` Python type registration (C++ `cclient::jni::python::PythonIterator`) `pybind11::class_<cclient::jni::python::PythonIterator>(m, "PythonIterator")` (`include/jni/PythonBindings.h:89`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-030 | `sharkbite_iterator.PythonIterator.__init__` overload registration `.def(pybind11::init<>())` (`include/jni/PythonBindings.h:90`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-031 | `sharkbite_iterator.PythonIterator.seek` method registration `.def("seek",&cclient::jni::python::PythonIterator::seek)` (`include/jni/PythonBindings.h:91`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-032 | `sharkbite_iterator.PythonIterator.next` method registration `.def("next", &cclient::jni::python::PythonIterator::next)` (`include/jni/PythonBindings.h:92`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-033 | `sharkbite_iterator.PythonIterator.hasTop` method registration `.def("hasTop", &cclient::jni::python::PythonIterator::iteratorHasTop)` (`include/jni/PythonBindings.h:93`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-034 | `sharkbite_iterator.PythonIterator.getTopKey` method registration `.def("getTopKey", &cclient::jni::python::PythonIterator::getIteratorTopKey)` (`include/jni/PythonBindings.h:94`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |
| SB-EMB-035 | `sharkbite_iterator.PythonIterator.getTopValue` method registration `.def("getTopValue", &cclient::jni::python::PythonIterator::getIteratorTopValue)` (`include/jni/PythonBindings.h:95`) | — | — | — | Not required (rationale required) | Rationale: **pinned dead code, not a shipping surface.** This registration is never compiled: no translation unit in the pinned tree includes `include/jni/PythonBindings.h` (zero references tree-wide), `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`), `cclient::jni::python::PythonIterator` is never declared (`include/jni/PythonIterator.h:28-35` contains empty namespaces), its implementation is commented out (`src/jni/PythonIterator.cpp:26-73`), and `include/jni/IteratorExecutor.h:28-33` is likewise empty. This registration additionally names a type the pinned tree does not declare, so the module could not compile even if a translation unit included it. It is enumerated rather than excluded so the surface is visible and auditable: if the project ever revives the embedded module, every row here becomes an obligation and this rationale is withdrawn. See [SB-SCAN-016](#sec-9) for the capability-level statement and [SB-DIV-007](#sec-26). |

<a id="sec-20"></a>

## 20. Matrix: cross-cutting semantics (`SB-XCUT`)

| ID | Concern | Sharkbite | Shoal Go | Shoal C ABI | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SB-XCUT-001 | Object lifecycle / ownership | `shared_ptr` holders for most types; `unique_ptr` from `createScanner`/`createWriter`; raw owning `RFile*` from `RFileOperations::open`; `ZookeeperInstance` owns raw `ZooCache`/`ZooKeeper` pointers deleted in its destructor | Go GC + explicit `Close` on `Connector`, `BatchWriter`, `Instance` | Opaque handles; `*_close` then `*_free`; free implies best-effort close and NULLs the caller's variable (`capi/README.md`) | `TestConnectorRegistryLifecycle` (`cmd/shoal-capi/state_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | audit #92: the models are not equivalent. Sharkbite frees on last reference with no user-visible close, so a Python program that drops the last reference releases server resources deterministically; Shoal requires an explicit close, and a dropped handle leaks until the shim's finaliser runs. The shim owns that bridge and no test covers it. |
| SB-XCUT-002 | Double free / use after free | Undefined behavior | n/a | 32 typed free functions — `shoal_key_value_result_free`, `shoal_authorizations_free`, `shoal_rfile_writer_free`, `shoal_rfile_reader_free`, `shoal_rfile_seekable_free`, `shoal_rfile_entry_result_free`, `shoal_configuration_free`, `shoal_bytes_result_free`, `shoal_string_list_free`, `shoal_server_list_free`, `shoal_range_free`, `shoal_iterator_setting_free`, `shoal_connector_free`, `shoal_client_free`, `shoal_connector_identity_free`, `shoal_table_list_free`, `shoal_table_constraint_list_free`, `shoal_table_properties_free`, `shoal_namespace_list_free`, `shoal_namespace_properties_free`, `shoal_versioned_properties_free`, `shoal_bytes_list_free`, `shoal_scanner_free`, `shoal_batch_scanner_free`, `shoal_cancellation_free`, `shoal_scan_cursor_free`, `shoal_scan_result_free`, `shoal_mutation_free`, `shoal_batch_writer_free`, `shoal_accumulo_writer_free`, `shoal_write_failure_free`, `shoal_error_free` (`capi/include/shoal.h`) | `main()` in `capi/tests/lifecycle.c` double-frees configuration/topology results, populated data descriptors and value results, authorization handles, all administration result families, connector identity, byte-list results, connector, client, scanner, and scan-cursor handles, mutations, batch and buffered writers, write failures, and populated security errors; `main()` in `capi/tests/header_cpp_test.cpp` calls typed free functions on unset handles | Behavior mismatch | `docs/validate_sharkbite_matrix.py` derives the typed free-function inventory directly from `shoal.h` and fails if the SB-XCUT-002 list drifts. The broader Sharkbite-parity row remains a mismatch because use after free remains invalid. |
| SB-XCUT-003 | Copied vs borrowed memory | `Key` holds raw `char *` buffers paired with `disown*` flags — `disownRow`/`row` (`include/data/constructs/Key.h:344-345`), `disownColumnFamily`/`colFamily` (`include/data/constructs/Key.h:352-354`), `disownColumnQualifier`/`colQualifier` (`include/data/constructs/Key.h:360-361`), `disownColumnVisibility`/`keyVisibility` (`include/data/constructs/Key.h:365-366`), toggled by the `setDisown*` methods (`include/data/constructs/Key.h:215-227`); callers can be handed borrowed buffers | All public byte accessors return defensive copies (`cloneRow`, `accumulo/discovery.go:266`; `Column.Family()`, `accumulo/scanner.go:56`; `Mutation.Row()`, `accumulo/mutation.go:33`) | `shoal_connector_config`, `shoal_scanner_config`, `shoal_range`, and `shoal_key_value_view` (`capi/include/shoal_types.h`) borrow inputs for the call only and borrow outputs until the owning result is freed | `TestDiscoveryInvalidationAndDefensiveCopies` (`accumulo/discovery_test.go:352`); `TestEffectiveTablePropertiesConcurrentCopyIsolation` (`accumulo/table_property_reads_test.go:211`); `main()` in `capi/tests/result_bridge.c` | Behavior mismatch | audit #2: the two models are opposites at the boundary the shim must cross. Go copies out; the ABI borrows out. Sharkbite's `Key` can borrow *or* own the same `char *` depending on the disown flags, which no Shoal layer reproduces. A shim that forwards ABI views into Python objects without copying reintroduces the borrowed-buffer hazard the Go layer removed. |
| SB-XCUT-004 | Binary safety (non-UTF-8, embedded NUL) | Broken in several places: `Value.get()`, all `Key.get*()` accessors, and the `Key(const char*...)` constructor. Only `Value.get_bytes()` is safe | `[]byte` everywhere; no transcoding anywhere in `accumulo/` | `shoal_bytes{data,length}` everywhere; no NUL termination | `TestBatchWriterRoutesBatchesAndCopiesMutation` (`accumulo/batch_writer_test.go:194`); `main()` in `capi/tests/result_bridge.c` (embedded NUL + empty fields); `main()` in `capi/tests/lifecycle.c` (embedded-NUL row and value) | Behavior mismatch | audit #92: Shoal's layers are byte-exact and proven, but that is a *difference* from the Sharkbite Python contract, not equivalence — the pinned Python API decodes to `str` and raises or corrupts on the same inputs Shoal round-trips. The shim decides which behavior wins; see [§21](#sec-21) and [§22](#sec-22). Closes when a differential test pins each accessor's Python-visible type and error. |
| SB-XCUT-005 | Empty vs absent | Sharkbite conflates `""` with "unbounded" in `Range` | `Range` distinguishes them; nil boundaries are preserved through discovery and multi-scan | `SHOAL_RANGE_BOUND_UNBOUNDED` is a distinct kind | `TestDiscoveryPreservesEmptyRowBoundaries` (`accumulo/discovery_test.go:185`); `TestMultiScanExtentIdentityPreservesNilBoundaries` (`accumulo/scanner_test.go:691`) | Behavior mismatch | The shim must map Sharkbite's `""`/`None` bounds onto `UNBOUNDED` to keep `test/python/TestRanges.py:120,147` semantics. |
| SB-XCUT-006 | Concurrency | Internal mutexes protect pools/queues; no documented guarantee that one `Scanner`/`Writer` is safe for concurrent **user** calls | `BatchWriter` is mutex-guarded; `Connector` discovery is concurrency-safe | "Scanner and batch-scanner handles support concurrent scan calls" (`capi/README.md`) | `TestBatchWriterConcurrentAdds` (`accumulo/batch_writer_test.go:595`); `TestOwnedScannerCloseIsConcurrentAndIdempotent` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | audit #92: Shoal states and proves a guarantee Sharkbite never made, so the two are not equivalent — code written against Sharkbite may serialize defensively, and code written against Shoal will deadlock or serialize if the shim holds the GIL ([SB-XCUT-017](#sec-20)). Superset, not parity. |
| SB-XCUT-007 | Deadlines | Only the ZooKeeper session timeout | `context.Context` on every call | `timeout_ms` on scan, writer add/flush/close (`capi/include/shoal.h`) | `TestOwnedScannerDeadline` (`cmd/shoal-capi/state_test.go`); `TestOwnedBatchWriterCloseDeadlineBoundsActiveWait` (`cmd/shoal-capi/state_test.go`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | audit #92: there is nothing to be equivalent to — Sharkbite exposes no per-call deadline, and Shoal's deadline is sticky on the writer (`main()` in `capi/tests/lifecycle.c`), which permanently fails a writer that Sharkbite would have left usable. The shim must choose a default `timeout_ms` for every call, and that choice is a behavior change requiring a decision. |
| SB-XCUT-008 | Cancellation | None | `context.Context` cancellation, with cleanup that survives cancellation | `shoal_cancellation_create`, `shoal_cancellation_cancel`, `shoal_scanner_scan_with_cancellation`, `shoal_batch_scanner_scan_with_cancellation`, `shoal_cancellation_free` (`capi/include/shoal.h`) | `TestScannerCancellationStillClosesServerScan` (`accumulo/scanner_test.go:396`); `TestBatchWriterParallelCancellationWaitsForWorkers` (`accumulo/batch_writer_test.go:743`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `TestOwnedCancellationCancelsCurrentAndFutureScans`, `TestOwnedCancellationCloseCancelsAndJoinsActiveScan`, `TestOwnedCancellationConcurrentCancelIsIdempotent` (`cmd/shoal-capi/connector_control_export_test.go`) | Covered | Revision 30 adds one-shot owned cancellation handles whose cancel/free operations interrupt and join registered scans without closing the scanner or connector. Already-canceled handles cancel later scans immediately. |
| SB-XCUT-009 | Retry and ambiguity | Transport-level protocol-version fallback (`include/interconnect/transport/BaseTransport.h:57`) and shuffled server re-selection bounded by the server-list length (`TransportPool.h:222`); no notion of an ambiguous commit and no bounded per-mutation retry | Bounded retries for provably safe failures only (`accumulo/batch_writer.go:743`); ambiguous failures are terminal and sticky | `SHOAL_STATUS_AMBIGUOUS_WRITE`, `SHOAL_STATUS_RETRY_EXHAUSTED`, `SHOAL_WRITE_FAILURE_AMBIGUOUS_COMMIT`, `SHOAL_WRITE_FAILURE_RETRY_EXHAUSTED`, and `SHOAL_WRITE_FAILURE_AUTOMATIC_FLUSH` (`capi/include/shoal_types.h`) | `TestBatchWriterPreAcceptanceRetryExhaustionIsNotSticky` (`accumulo/batch_writer_test.go:1318`); `TestBatchWriterRelocatesExplicitlyUncommittedSuffix` (`accumulo/batch_writer_test.go:1063`); `TestBatchWriterParallelRetryDoesNotReplaySuccessfulServer` (`accumulo/batch_writer_test.go:1137`); `main()` in `capi/tests/lifecycle.c` | Behavior mismatch | audit #92: the semantics differ, and the difference is user-visible. Sharkbite retries at the transport layer without classifying safety and never reports ambiguity; Shoal refuses to retry ambiguous submissions and becomes permanently terminal. Programs that survived a flaky tablet server under Sharkbite will now see a sticky failure. Safer, but a change requiring [SB-DIV-005](#sec-26). |
| SB-XCUT-010 | Result iteration model | Streaming pull with background producers (`include/scanner/constructs/Results.h:93,300`) | `Scanner.Stream(ctx, scanRange) (*ResultStream, error)`, `BatchScanner.Stream(ctx, ranges) (*ResultStream, error)`, `ResultStream.Next`, `Entry`, `Err`, `Close` (`accumulo/scan_stream.go`) | `shoal_scanner_stream`, `shoal_batch_scanner_stream`, `shoal_scan_cursor_next` (`capi/include/shoal.h`) | `TestOwnedScanCursorChunksAndExhausts`, `TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`, `TestScannerCloseCancelsAndJoinsOwnedCursor`, `TestClientCloseCancelsAndJoinsOwnedCursor` (`cmd/shoal-capi/scan_cursor_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`); `main()` in `capi/tests/lifecycle.c` | Covered | The ABI now exposes the bounded ResultStream model: one live cursor, bounded owned chunks, per-call discovery context, and explicit close/free coordinated with parent shutdown. |
| SB-XCUT-011 | ABI versioning | n/a (C++ ABI, no versioning) | n/a | `SHOAL_ABI_VERSION` macro + `shoal_abi_version()` (`capi/include/shoal_types.h`, `capi/include/shoal.h`), plus a `struct_size` field and `*_config_init` on every config struct (`capi/include/shoal_types.h`) | `main()` in `capi/tests/lifecycle.c` matches header and library versions; `main()` in `capi/tests/lifecycle.c` assert that `*_init` sets `struct_size` and that an **undersized** connector or writer config is rejected; `static_assert`s in `capi/tests/header_cpp_test.cpp` static-asserts the version and status width | Behavior mismatch | audit #3: forward compatibility is the claim, and it is untested. No test passes an **oversized** `struct_size` — the case a future ABI version creates and the one the documented convention ("version 1 readers ignore bytes beyond the known structure", `capi/include/shoal_types.h`) actually promises — and no test rejects an undersized **scanner** config or `shoal_range`, though both carry the same field. Closes with oversized-struct acceptance plus undersized rejection for all four config types. |
| SB-XCUT-012 | Capability discovery | n/a | n/a | `shoal_abi_capability_count()`, `shoal_abi_capability_word_count()`, `shoal_abi_capability_word()`, and `shoal_abi_has_capability()` (`capi/include/shoal.h`); `SHOAL_ABI_CAPABILITY_TABLE_ADMIN`, `SHOAL_ABI_CAPABILITY_SECURITY_ADMIN`, `SHOAL_ABI_CAPABILITY_TABLE_SPLITS`, and `SHOAL_ABI_CAPABILITY_WORD0` (`capi/include/shoal_types.h`) | `TestABIDiscoveryValues` (`cmd/shoal-capi/abi_export_test.go`); `TestABIDiscoveryQueriesAreConcurrentAndStable` (`cmd/shoal-capi/abi_export_test.go`); `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`) compiles and runs `main()` in `capi/tests/lifecycle.c`, `main()` in `capi/tests/header_cpp_test.cpp`, and `main()` in `capi/tests/shared_library_query.c` | Covered | Append-only capability IDs and bitsets let a wheel gate optional administration, RFile, and HDFS surfaces without probing for missing symbols. The contract is exercised through direct Go tests and dynamically loaded C and C++ consumers. |
| SB-XCUT-013 | Symbol visibility and calling convention, Linux/gcc | n/a | n/a | `SHOAL_API` = `__attribute__((visibility("default")))`, `SHOAL_CALL` empty (`capi/include/shoal_types.h`), applied to 238 declared exports in `capi/include/shoal.h` | `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`) builds the `c-shared` library and compiles, links, and runs `main()` in `capi/tests/lifecycle.c`, `main()` in `capi/tests/header_cpp_test.cpp`, and `main()` in `capi/tests/shared_library_query.c`; the checked-in compiled manifest `docs/sharkbite-compatibility-revision36-cabi-symbols.txt` records that those exact objects reference **233 of the 238** exports | Behavior mismatch | Five exports are not referenced by any linked test, so their visibility and callability remain unproven even on Linux/gcc — `shoal_scanner_scan`, `shoal_batch_scanner_scan`, `shoal_write_failure_get_constraint`, `shoal_write_failure_get_authorization`, `shoal_write_failure_get_cleanup` (`capi/include/shoal.h`). `compiled_c_abi_reference_inventory()` and `write_c_abi_symbol_manifest()` (`docs/validate_sharkbite_matrix.py`) regenerate that manifest from actual compiled objects, so comments, string literals, and disabled `#if 0` blocks cannot count as evidence. Closes when a linked test references all 238, on each platform claimed. |
| SB-XCUT-014 | C ABI build and CI | n/a | n/a | `make capi` (`Makefile:179-185`); C/C++ tests compiled and executed by `TestSharedLibraryCABI` (`cmd/shoal-capi/cabi_test.go`) | `.github/workflows/ci.yml` build/vet/race jobs include `cmd/shoal-capi` | Behavior mismatch | There is no dedicated CI job for `make capi`, and no Windows or macOS runner, so the packaged artifact path is unverified on the platforms a wheel matrix will need. |
| SB-XCUT-015 | Live-cluster conformance | Live tests exist but are excluded from CI (`.github/workflows/ccpp.yml` runs only three no-cluster CTest targets) | All 90 `accumulo/` tests are unit tests with fake adapters (no build tags, no env gating, no testcontainers) | C tests use compile-time seams (`capi/tests/test_seam.h`) | `make test-hdfs` + `.github/workflows/hdfs-integration.yml` are the only live-service tests in Shoal, and they cover HDFS only | Missing Go | Neither project verifies Accumulo client behavior against a live cluster in CI. See [§24](#sec-24). |
| SB-XCUT-016 | Wire compatibility of visibility expressions | `ColumnVisibility`/`VisibilityEvaluator` (`test/constructs/TestSecurity.cpp`) | `internal/visfilter` — **internal**, used by the embedded engine | Labels are passed through to the server | `internal/visfilter` unit tests | Behavior mismatch | For the client path both projects delegate evaluation to the tablet server, so parity is a live-cluster question. |
| SB-XCUT-017 | GIL behavior | pybind11 holds the GIL unless explicitly released; `Results.__anext__` resolves futures inline | n/a | n/a | `examples/asyncexample.py` | Missing C ABI | Shim obligation: release the GIL around every blocking ABI call. Without it, `asyncio` and threads regress relative to Sharkbite. |
| SB-XCUT-018 | Fork safety | Not documented; process-wide singleton pools make `fork()` unsafe | Per-connector pools | Handle table in `cmd/shoal-capi/state.go` | `TestConnectorRegistryLifecycle` (`cmd/shoal-capi/state_test.go`) | Behavior mismatch | The Go runtime is not fork-safe; `multiprocessing` with the default `fork` start method will break in ways Sharkbite users may not expect. Must be documented in the wheel's README and validated. |
| SB-XCUT-019 | Symbol visibility and calling convention, Windows and macOS | n/a | n/a | `SHOAL_API` = `__declspec(dllexport)`/`__declspec(dllimport)`, `SHOAL_CALL` = `__cdecl` (`capi/include/shoal_types.h`) | — (`TestSharedLibraryCABI` names `shoal.dll` and `libshoal.dylib` at `cmd/shoal-capi/cabi_test.go`, but `.github/workflows/ci.yml` runs Linux only, so those branches never execute) | Missing C ABI | audit #2: split out of [SB-XCUT-013](#sec-20) so tested and untested platforms are not claimed together. A wheel matrix ([SB-PKG-008](#sec-5), [SB-PKG-011](#sec-5)) cannot ship on a platform whose export decoration and calling convention have never been compiled, linked, or run. |

<a id="sec-21"></a>

## 21. Unsafe or defective Sharkbite behavior (do not reproduce)

Each item is a defect in the pinned Sharkbite release, not a contract. The
compatibility shim must implement the listed replacement instead of copying the
behavior. None of these may be silently "fixed" without recording the change
here, because some code depends on the broken shape.

| ID | Defect | Location | Why it is unsafe | Compatibility shim |
| --- | --- | --- | --- | --- |
| SB-UNSAFE-001 | `Authorizations.__str__`/`__repr__` dereference `vec.end()-1` and `vec.back()` on an empty vector | `pysharkbite.cpp:464-484` | Undefined behavior — `str(Authorizations())` can crash the interpreter. `Authorizations()` with no entries is the most common construction in the pinned tests. | Return `[ ]` for empty, `[ a, b ]` otherwise. Match the non-empty format exactly. |
| SB-UNSAFE-002 | `Value.get()` transcodes bytes to `str` | `pysharkbite.cpp:274` | Raises `UnicodeDecodeError` on binary values; silently corrupts data on lossy platforms. | Keep `get()` but decode with `errors='surrogateescape'`; add `get_bytes()` (already in the contract) and document `get_bytes()` as the correct accessor. |
| SB-UNSAFE-003 | `Key.getRow()`/`getColumnFamily()`/`getColumnQualifier()`/`getColumnVisibility()` return `str` with no bytes alternative | `pysharkbite.cpp:321-325` | Binary row keys — normal in Accumulo — are unreachable from Python and raise on decode. | Same `surrogateescape` rule plus new `*_bytes()` accessors. Additive, so existing code is unaffected. |
| SB-UNSAFE-004 | `Key(const char*, ...)` constructor truncates at the first NUL | `pysharkbite.cpp:299-300` | Silent data corruption for binary components. | Accept `bytes` and pass length explicitly through `shoal_bytes`. |
| SB-UNSAFE-005 | `BatchWriter.flush()` cannot be called with zero arguments | `pysharkbite.cpp:487` vs `include/writer/Sink.h:62` | The documented API is unusable; pybind11 does not inherit the C++ default. | Expose `flush(override: bool = False)`. |
| SB-UNSAFE-006 | `BatchScanner.fetchColumn(cf)` cannot be called with one argument | `pysharkbite.cpp:436` vs `include/scanner/Source.h:85` | Same class of defect; column-family-only fetching is unreachable. | Expose `fetchColumn(cf, cq=None)`. |
| SB-UNSAFE-007 | `AccumuloScanner.select_column` calls `fetchColumn` with the wrong arity on both branches | `sharkbite/__init__.py:140-144` | The helper never works: the `cq` branch passes one argument, the no-`cq` branch passes `None`. | Fix the branches; keep the method name and signature. |
| SB-UNSAFE-008 | `AccumuloIterator.__next__` consumes an entry from the underlying result set and *then* raises `StopIteration` once `_chunkCounter >= _chunkSize`, discarding the entry it just pulled | `sharkbite/__init__.py:190-194`; `pandashark/pandassharkbite.py:27-34` | Two distinct defects. (1) **Boundary-row loss**: the row fetched on the chunk-limit call is consumed from the result set and never returned to anyone, so every chunk boundary silently drops one entry. (2) **Truncation for direct users**: `AccumuloScanner.get(...)` hands the iterator straight to callers (`sharkbite/__init__.py:146-154`), and nothing there ever calls `nextBatch()`, so a plain `for kv in scanner.get(row, chunksize=1000)` stops after 1000 entries and reports success. `nextBatch()` **is** called — but only by `pandashark`'s `DataFrameIterator.__next__` on `StopIteration` (`pandashark/pandassharkbite.py:60-72`), which is why the pandas path yields successive frames while the direct path truncates. | Do not reproduce either defect. Read the boundary entry once and return it; treat `chunksize` as a batching hint that never terminates iteration; keep `nextBatch()` as a no-op-compatible method so `DataFrameIterator` keeps working. Record as [SB-DIV-006](#sec-26) because it changes result counts for both paths. |
| SB-UNSAFE-009 | `AccumuloIterator.__init__` is defined twice; the first definition is dead | `sharkbite/__init__.py:174-181` | The one-argument constructor documented by the first definition does not exist at runtime. | Provide `__init__(self, scanner, chunkSize=0)`. |
| SB-UNSAFE-010 | `AccumuloScanner` is defined twice in the same module; the first definition (with a `pass` constructor) is dead | `sharkbite/__init__.py:60-63` and `126-148` | `AccumuloBase.to_scanner()` silently depends on which definition wins. | Single definition. |
| SB-UNSAFE-011 | `AuthInfo::operator=` fails to copy the password | `include/data/constructs/security/AuthInfo.h:60-67` | Copy-assigned credentials silently authenticate with an empty password. | Shoal's `Credentials.clone()` copies everything (`accumulo/credentials.go:44`). Do not reproduce. |
| SB-UNSAFE-012 | `CompressorFactory::create()` constructs but never throws its error | `include/data/constructs/compressor/CompressorFactory.h:39-48` | An unsupported codec silently falls through instead of failing. | Fail loudly with `ClientException`. |
| SB-UNSAFE-013 | `RFileOperations::open` returns a raw owning pointer while its four siblings return `shared_ptr` | `include/data/constructs/rfile/RFileOperations.h:45` | Guaranteed leak from Python, which never deletes it. | Single owning model; close deterministically via context manager. |
| SB-UNSAFE-014 | `CompressorFactory::getZlibCompressor()` leaks a process-wide singleton | `include/data/constructs/compressor/CompressorFactory.h:35` | Intentional leak; harmless but not a contract. | Do not reproduce. |
| SB-UNSAFE-015 | `TableRates` registers `query_rate_byte` four times and never registers an ingest-rate property | `pysharkbite.cpp:110-120` | Three registrations are dead; the property set does not match the getters. | Register each property once; keep every getter name. |
| SB-UNSAFE-016 | `tableOps.import(...)` and `IterInfo.class()` use Python reserved words | `pysharkbite.cpp:93` (`IterInfo.class`), `pysharkbite.cpp:241` (`tableOps.import`) | Unreachable through normal attribute syntax. | Keep the reserved-word names for `getattr` users and add usable aliases. |
| SB-UNSAFE-017 | `AccumuloWriter.__del__` performs network I/O during interpreter teardown | `sharkbite/__init__.py:128-129` | Exceptions are swallowed; module globals may already be torn down; writes can be silently lost. | Keep a best-effort `__del__`, emit a `ResourceWarning` when it has real work to do, and document `with`/`close()`. |
| SB-UNSAFE-018 | Server-side update errors are never surfaced to Python | `pysharkbite.cpp:486-497` (no failure accessor on the `Sink` binding) | Rejected mutations look like successful writes. | Surface `MutationRejectionError` through `ClientException`; see SB-WRITE-010 and [§26](#sec-26). |
| SB-UNSAFE-019 | `ZookeeperInstance` owns raw `ZooCache`/`ZooKeeper` pointers with manual `delete` | `include/data/constructs/client/zookeeperinstance.h` | Inconsistent with the rest of the API and easy to double-free. | Shoal's `Instance.Close()` is idempotent (`accumulo/instance.go:143`). |
| SB-UNSAFE-020 | Committed build artifacts in the test tree (`test/zookeeper/construct_test`, `test/zookeeper/testInstance`) and an empty `test/services/CMakeLists.txt` | `test/` | The mock-server test path is dead; the harness cannot be trusted as evidence. | Ignore as evidence; port the behavior into live-cluster conformance instead. |
| SB-UNSAFE-021 | `AccumuloDataset.coerce` calls `key.getKey().getValue()` — `Key` has no `getValue` binding (`pysharkbite.cpp:297-326` defines `getRow`, `getColumnFamily`, `getColumnQualifier`, `getColumnVisibility`, `getTimestamp` only) | `sharkbite/torch.py:24-36` | Every default (no `user_lambda`) coercion raises `AttributeError` inside the `try`, prints the exception, and **returns `0`**. A PyTorch training loop therefore trains on an all-zero tensor stream while looking healthy. The same swallow-and-return-0 path hides genuine user-lambda errors. | Call `keyvalue.getValue()` on the `KeyValue`, not on the `Key`; propagate coercion errors instead of returning a sentinel. Both changes alter observable output — recorded as [SB-DIV-011](#sec-26). |
| SB-UNSAFE-022 | `AccumuloValueDataset.__init__` calls `super.__init__(cluster, table, start, end)` — missing parentheses on `super`, and four positional arguments against a nine-parameter base signature | `sharkbite/torch.py:64-66` vs `sharkbite/torch.py:14` | Constructing `AccumuloValueDataset` always raises `AttributeError: 'super' object has no attribute '__init__'`; even with `super()`, the argument list does not match `AccumuloDataset.__init__`. The class is unusable in the pinned release. | Implement it correctly (`super().__init__(...)` with the full argument list). Because no working program can depend on the broken behavior, this is a defect fix, not a divergence. |
| SB-UNSAFE-023 | `AccumuloBase.set_threads` mutates `_threads` after scanners and writers have already been constructed, and `AccumuloBase.to_scanner` forwards neither `_threads` nor a live connector | `sharkbite/__init__.py:21-22,52-53,136-138` | `AccumuloScanner.__init__` calls `createScanner(self._auths, self._threads)` once at construction, so `set_threads` after construction silently has no effect; `to_scanner()` builds a brand-new `AccumuloScanner` from the stored credentials, discarding the caller's thread count and opening a second connector. Users get a different parallelism than they asked for and twice the connections. | Apply `set_threads` lazily at scan time and forward the current thread count (and connector) through `to_scanner`. Document the changed connection count. |
| SB-UNSAFE-024 | `pandashark.read_accumulo` calls `scanner.get(chunksize)`, but `AccumuloScanner.get`'s first parameter is `begin_row` | `pandashark/__init__.py:64-98` vs `sharkbite/__init__.py:146` | The chunk size is passed as the **start row**: `read_accumulo(scanner, None, None, 25)` (`examples/dataframe.py:69`) becomes `get(begin_row=25)` → `Range(25)` with an `int`, and the real `chunksize` argument stays at its default `1000`. The documented pandas entry point cannot work as written. | Pass `chunksize` by keyword and require an explicit range (or reuse ranges already added through `fetch_range`/`fetch_ranges`). |
| SB-UNSAFE-025 | `read_accumulo` is defined twice — the first definition is decorated `@overload` from `typing` and its body is therefore dead | `pandashark/__init__.py:51-63` | The connector-plus-ranges form advertised by the first signature does not exist at runtime; the second definition wins, so calling `read_accumulo(connector, ranges)` binds `ranges` to `columns` and iterates it as column names. | Provide one real implementation with an explicit, documented signature; keep `@overload` only alongside a genuine dispatch implementation. |
| SB-UNSAFE-026 | `DataFrameIterator.__next__` never reaches a terminal `StopIteration` and returns empty `DataFrame`s forever | `pandashark/pandassharkbite.py:60-72` (with `pandashark/pandassharkbite.py:24-25,27-34`) | `__next__` catches `StopIteration` from the wrapped iterator, calls `nextBatch()` — which only resets `_chunkCounter` — and returns the frame it accumulated. It never re-raises. Once the underlying `Results` is genuinely exhausted, every subsequent call re-enters, immediately catches `StopIteration` again, and returns an **empty** `DataFrame`. A `for frame in DataFrameIterator(...)` loop therefore never ends, and `read_accumulo_nex` ([SB-PANDA-003](#sec-9-3)) silently returns empty frames instead of signalling the end of data. Distinguishing "chunk finished" from "scan finished" is impossible with the pinned design because both arrive as the same exception. | Track exhaustion of the underlying result set separately from the chunk limit; re-raise `StopIteration` once the scan is drained and return the final partial frame before it. Record as [SB-DIV-014](#sec-26) because loops that terminated by other means will now see a clean end and callers stop receiving empty frames. |
| SB-UNSAFE-027 | `set_table` and `set_authorizations` do not reconfigure a scanner or writer that already exists | `sharkbite/__init__.py:43-50,55-58` versus `sharkbite/__init__.py:136-138` and `sharkbite/__init__.py:73-80` | `AccumuloScanner.__init__` calls `createScanner(self._auths, self._threads)` at construction, and `AccumuloWriter` caches `_writer` on first use. `set_table` rebinds only `_table_operations`, and `set_authorizations` rebinds only `_auths`, so an existing scanner keeps scanning the **old table with the old authorizations** while the object reports the new ones. The failure is silent: results look plausible but come from the wrong table or carry the wrong visibility filter. Same class of defect as [SB-UNSAFE-023](#sec-21). | Defer scanner and writer construction until first use, and invalidate any cached handle when the table, authorizations, or thread count change. Record as [SB-DIV-015](#sec-26) because code that called the setters after constructing a scanner will now read different data. |
| SB-UNSAFE-028 | `DataFrameIterator` declares a class attribute `_iterator` that nothing reads, and stores its real state in the name-mangled `self.__iterator` | `pandashark/pandassharkbite.py:39,41,48,66,68` | The declared `_iterator = None` is dead, while the live attribute becomes `_DataFrameIterator__iterator`. Anything that subclasses `DataFrameIterator` and expects to read or replace `_iterator` — the obvious extension point, and the name the class advertises — silently sees `None` and cannot reach the wrapped iterator at all. Combined with the class-level mutable `_columns` set ([SB-PANDA-004](#sec-9-3)), the class's declared attribute surface is misleading in both directions: one attribute is shared when it should be per-instance, the other is inaccessible when it should be inherited. | Use one clearly named instance attribute, initialise it in `__init__`, and drop the dead class attribute. No approval needed: no working program can depend on reading a value that is never written. |
| SB-UNSAFE-029 | The public three-argument `ZookeeperInstance(in, zks, zkTimeoutMs)` constructor dereferences a default-constructed null `shared_ptr` | `include/data/constructs/client/zookeeperinstance.h:120-125`, member declared at `include/data/constructs/client/zookeeperinstance.h:241` | The initialiser list sets `instanceName`, `zookeeperList`, and `timeoutMs` but never `myConfiguration`, which is a `std::shared_ptr<Configuration>` and therefore null. The first statement of the body is `myConfiguration->set("FILE_SYSTEM_ROOT", "/accumulo")` — a null-pointer dereference, undefined behavior, reached before the empty-argument validation at `include/data/constructs/client/zookeeperinstance.h:127-130` can run. The overload is published in a public header ([SB-CFG-027](#sec-6)); it is simply unusable. | Do not reproduce. A Shoal-backed replacement must either require a configuration argument or default-construct one before use. No divergence entry is needed: a constructor that always invokes undefined behavior cannot have working dependants, so fixing it removes no observable behavior. |
| SB-UNSAFE-030 | `RFileOperations::openManySequential` ignores the `versions` argument and silently disables delete handling whenever it is non-zero | `include/data/constructs/rfile/RFileOperations.h:162-171`, `include/data/iterators/VersioningIterator.h:46-48` | The dispatch is `if (versions == 0) { MultiIterator or DeletingMultiIterator } else { VersioningIterator(iters) }`. `VersioningIterator`'s only constructor takes the iterators and an optional range — the number is never passed and no version count is stored, so `versions=5` and `versions=1` behave identically and keep one version per key. Worse, the `else` branch drops `withDeletes` and `propogate` entirely, so asking for any version limit silently turns off the delete processing the caller also asked for. | Honour the count and keep the delete options independent of it. `MergeOptions.Versions` limits versions, `MergeOptions.ApplyDeletes` and `MergeOptions.Propagate` still apply, and `Versions: 1` reproduces the pinned behavior. No approval needed for the independence: a program cannot depend on an option it asked for being ignored. |
| SB-UNSAFE-031 | `DeletingMultiIterator` performs no delete handling at all when `propogate` is true, which is `openManySequential`'s documented default | `include/data/iterators/DeletingMultiIterator.h:35-52` | `findTop()` runs the suppression sweep only in the `!propogateDeletes` branch; the `else` branch is a bare `multiNext()`. With the published defaults `openManySequential(rfiles, 0, true, true, 0)` therefore returns tombstones **and** every entry they cover, which is the same output as the plain `MultiIterator` the `withDeletes=false` path selects. Accumulo's own `DeletingIterator` propagates the tombstone while still suppressing what it covers, so an RFile merge that looks delete-aware is not. | Follow Accumulo: with `MergeOptions.ApplyDeletes` the tombstone suppresses the entries beneath it, and `MergeOptions.Propagate` only decides whether the tombstone itself is emitted for the next level. A caller that wants the raw merge asks for it by leaving `ApplyDeletes` false. |
| SB-UNSAFE-032 | The `maxtimestamp` argument of `openManySequential` throws for every non-zero value, and the field it feeds is an *earliest allowed* timestamp | `include/data/constructs/rfile/RFileOperations.h:76-81`, `include/data/constructs/ageoff/AgeOffConditions.h:47-63,82-88` | The call site builds `AgeOffCondition(AgeOffType::DEFAULT, "default", maxtimestamp)` and hands it to `AgeOffEvaluator`, whose constructor throws `std::runtime_error("Cannot have more than one default condition")` when a `DEFAULT` condition carries a non-zero timestamp — the only way the argument is ever used. Even if it were reachable, `filtered()` drops keys whose timestamp is **less than** the value, so the argument named `maxtimestamp` is a minimum. The parameter is therefore unusable in the pinned release. | Implement the age-off the field describes and name it for what it does: `MergeOptions.MinTimestamp` drops entries stamped before the cutoff, per source file, exactly where Sharkbite installs its evaluator. Never reproduce the throw: no working program can pass a non-zero value today. |
| SB-UNSAFE-033 | `SequentialRFile::append` never checks key order, so an out-of-order append silently writes a malformed RFile | `src/data/constructs/rfile/SequentialRFile.cpp:178-208` | The method encodes each cell as a `RelativeKey` against the previous one and writes it. RFile requires non-decreasing keys, but nothing compares the new key with `lastKeyValue`, and `append` returns `true` either way. The resulting file passes `close()` and only fails later, in whatever reads it — including a tablet server that bulk-imports it. | Reject the append instead of writing it: `Writer.Append` returns `ErrOutOfOrder` and writes nothing. No approval needed — a malformed RFile has no working dependants — but the shim must surface it as a Python exception rather than the `False` return the signature suggests. |
| SB-UNSAFE-034 | `Authorizations` never validates its labels: the declared character rule is unreachable code | `include/data/constructs/security/Authorizations.h:84-92` versus `src/data/constructs/security/Authorizations.cpp:48-79` | `validateAuths()` is `protected` and is called by nothing: neither the default constructor, nor either vector constructor, nor `addAuthorization` invokes it, so no pinned program can raise "Invalid authorization character". Every byte is accepted, including an empty label and characters the rule forbids. The three-argument constructor is worse: it *widens* the rule by writing into the process-wide `AuthsInit` table, so one object permanently changes what every other `Authorizations` in the process would consider valid — and that constructor is not bound to Python, so the widening is unreachable from a wheel too. | Accept every label, exactly as the pinned release does, and publish the declared rule as an explicit, opt-in check (`Authorizations.Validate`, `ValidAuthorizationCharacter`). Rejecting labels at construction would break programs that store them today; no approval is needed for the opt-in form because nothing calls it unless a caller asks. This entry corrects [SB-KEEP-016](#sec-22), which recorded the unreachable throw as a contract. |
| SB-UNSAFE-035 | `Range`'s inclusive end row is left unadjusted from Python, so the range stops before the row it names | `include/data/constructs/Range.h:53` and `src/data/constructs/Range.cpp:51-59` versus `pysharkbite.cpp:338,341` | The C++ constructor takes `update = true`, which rewrites an inclusive end row to `row + '\0'` exclusive — the conversion Accumulo requires, because every cell of a row sorts after the key that spells the row. Both Python constructors declare `"update"_a=false`, so the adjustment never runs for a Python-built range: the stop key stays `Key(endRow)` inclusive and every cell of the end row with a non-empty column family, or with a timestamp below the maximum, falls outside the range. `after_end_key` inherits the same bound. | Always resolve row bounds, which is what `Range.KeyBounds` does: an inclusive end row becomes the following-row boundary and an exclusive start row skips the whole row. A shim must not offer the `update` argument at all, because the only value that yields Accumulo's documented semantics is the one Python cannot select. |
| SB-UNSAFE-036 | `tableOperations.addConstraint` is a stub: it installs nothing and always returns 0 | `src/interconnect/tableOps/ClientTableOps.cpp:397-400` | The body is a comment (`// set the table prop for the constraint`) and `return 0;`. No table property is written, so the constraint is never loaded and every mutation the constraint would have rejected is accepted for the life of the table. The published contract says the return value is the "unique number assigned to the constraint" (`include/interconnect/python/PythonStructures.h:157-164`), and Accumulo numbers constraints from 1, so the returned 0 is not even a number the operation could legitimately assign. A caller has no way to tell installation apart from silence. | Install the constraint and return its real number: `Connector.AddConstraint` writes `table.constraint.<number>` with the lowest free positive number and returns it. No approval is needed — a stub that writes nothing has no behavior to preserve — but the shim must not keep returning 0, because 0 is indistinguishable from the stub. |
| SB-UNSAFE-037 | `tableOperations.setProperty(property, "")` silently *removes* the property instead of setting it empty | `src/interconnect/tableOps/ClientTableOps.cpp:359-366` | `setProperty` begins `if (IsEmpty(&value)) return removeProperty(property);`, so writing an empty value deletes the property and returns the removal's status. A table property that is legitimately empty — Accumulo accepts empty values, and several properties treat empty as "unset the override" differently from "inherit" — cannot be expressed at all, and a caller that clears a value silently drops back to the inherited one. The same collapse hides failures: `removeProperty` returns 0 for success, which `setProperty` returns unchanged, so the caller sees the success code of an operation it did not request. | Send what the caller asked for. `Connector.SetTableProperty` sets the value it is given, including the empty string, and `Connector.RemoveTableProperty` is the only way to remove one. Record it as a shim-visible difference: a Sharkbite program that relied on the collapse must call the removal entry point explicitly. |
| SB-UNSAFE-038 | `Results.__iter__`, `__aiter__` and `__await__` each consume one result before iteration starts | `pysharkbite.cpp:377-385`, `include/scanner/constructs/Results.h:69,93,280-288` | All three bindings call `it->begin()`, and `ResultBlock::begin()` calls `getNextResult()`, which **dequeues** the next entry into `current` and then returns a copy of the block that the binding discards. `__next__` returns `current` and only then advances, so the first entry survives a single `iter()`. Every **additional** call silently drops a row: `await results` followed by `for kv in results` loses the first entry, and `iter(results)` twice loses one more each time. The loss is silent — no exception, no short count the caller can detect — and the discarded copy means the priming is pure side effect on shared state. Calling `next(results)` before any `iter()` is the mirror image: `current` is still a default-constructed `shared_ptr`, which pybind11 converts to `None`, so the caller gets `None` where a `KeyValue` is documented. | Do not reproduce the priming side effect. Shoal's cursor is created empty and advanced only by `ResultStream.Next`, so obtaining a stream consumes nothing; `Scanner.Stream` may be called repeatedly and each call returns an independent cursor that reads the range from the beginning (`TestScannerStreamsAreIndependentCursors`, `accumulo/scan_stream_test.go`). A Python shim implements `__iter__`/`__aiter__`/`__await__` by opening a new stream, never by priming a shared one. |
| SB-UNSAFE-039 | Postfix `ResultBlock::operator++` advances like prefix and returns the already-advanced block, and `operator!=` compares only the end flag | `include/scanner/constructs/Results.h:114-124,138` | `operator++(int t)` runs `getNextResult()` `t + 1` times and returns `*this`, so the standard postfix call `it++` — which passes `t = 0` — advances once and yields the **new** position: `*it++` skips an entry, the opposite of the required copy-then-advance contract. `operator!=(const ResultBlock &rhs)` returns `!(isEnd == rhs.isEnd)`, so any two blocks that are both mid-iteration compare equal; only comparison against `end()` behaves, which makes the type unusable with the STL algorithms its `begin`/`end` pair advertises. Both defects silently drop or mis-order data rather than failing. | Do not export a C++-style iterator pair. Shoal's cursor has one advance operation, `ResultStream.Next`, which reports whether `Entry` holds a new result, so there is no postfix form to get wrong and no iterator equality to define. Ordering and completeness are pinned against the materialized path by `TestStreamAndScanReturnTheSameEntries` (`accumulo/scan_stream_test.go`). |
| SB-UNSAFE-040 | `VisibilityNode::getLength()` returns `start - end`, so `ColumnVisibility::flatten()` emits the rest of the expression for every term | `include/data/constructs/security/VisibilityNode.h:136`, `src/data/constructs/security/ColumnVisibility.cpp:41` | `getLength()` computes `start - end`, which is negative for every non-empty node. `astToSTring` passes it straight into `std::string(expression, root.start, root.getLength())`, whose `count` parameter is `size_t`: the negative value converts to a huge unsigned number and the constructor clamps it to the remainder of the string. Every `TERM` therefore renders as the whole tail of the expression from its start offset, so `flatten()` on `a&b` does not return `a&b`. The method is public, silent, and its output is what a caller would store or compare. | Compute the span the obvious way. `VisibilityNode.Len()` returns `end - start` and `ColumnVisibility.Flatten()` slices exactly that span, pinned by `TestColumnVisibilityFlattenNormalizes` (`accumulo/column_visibility_test.go`). |
| SB-UNSAFE-041 | `ColumnVisibility::normalize` rolls up the wrong nodes, erases through an invalidated iterator, mutates the tree `getTree()` handed out, and orders with a comparator that is not a strict weak ordering | `src/data/constructs/security/ColumnVisibility.cpp:196-222`, `include/data/constructs/security/VisibilityNode.h:157-193` | Four defects in one public path, all reachable from `flatten()`. (1) When a child normalizes to the parent's own type the code inserts **`children->begin(), children->end()`** — the parent's entire child list — instead of the matching child's children, so `a&(b&c)` never flattens to `a&b&c`. (2) It then calls `children->erase(nxt)` on a `std::vector` while `it` already points past `nxt`; `vector::erase` invalidates every iterator at or after the erase point, so the loop continues through a dangling iterator. (3) `normalize` takes the root by reference and clears and refills its children, so the tree returned by `getTree()` is rewritten underneath any caller holding it, and a second `flatten()` sees a different tree. (4) The `std::set` it builds is ordered by `ExpressionASTComparator`, whose `AND`/`OR` branch assigns the recursive `bool` to `int diff`, leaving `diff < 0` unreachable — the relation is not a strict weak ordering, which is undefined behavior for `std::set`, and the same branch indexes the right operand's term through `lhs.getExpression()`. | Normalize functionally. `ColumnVisibility.Normalized()` builds a new tree, rolls up the matching child's own children, deduplicates and orders with the exported `CompareVisibilityNodes`, and leaves the receiver untouched; `TestColumnVisibilityFlattenDoesNotMutateTheTree` and `TestCompareVisibilityNodesOrdersByTypeThenTerm` (`accumulo/column_visibility_test.go`) pin both halves. |
| SB-UNSAFE-042 | An unbalanced `)` silently truncates the expression into a more permissive one | `src/data/constructs/security/ColumnVisibility.cpp:105-121` | `parse_` handles `)` by decrementing `parens` — a `size_t`, so a stray close wraps it to its maximum — and then **returns**, without ever checking that a matching `(` was seen or that the input is exhausted. `ColumnVisibility("a&b)&c")` therefore parses as `a&b` and discards the trailing `&c`: the resulting expression is satisfied by strictly more principals than the one the caller wrote. There is no diagnostic, and the `parens` counter it maintains is never read. | Reject it. `NewColumnVisibility` reports `unbalanced closing parenthesis` with the offset, and refuses to leave input unconsumed, pinned by `TestColumnVisibilityRejectsAnUnbalancedCloseInsteadOfTruncating` (`accumulo/column_visibility_test.go`). |
| SB-UNSAFE-043 | `VisibilityEvaluator` caches decisions by expression and `setVisibility` does not clear the cache | `include/data/constructs/security/VisibilityEvaluator.h:38-40,51-66` | `evaluate` memoises its answer in `resultMap`, keyed only by the expression text, while `setVisibility` replaces `auths` and leaves the map alone. An evaluator reused across principals — which is what `setVisibility` exists for — returns the **previous** principal's decision for every expression it has already seen, granting or denying visibility on someone else's authorizations. The map also grows without bound. | Tie the cache to the authorizations and bound it. `VisibilityEvaluator.SetAuthorizations` clears every cached decision and copies the incoming set, an evaluation started before a replacement can no longer store its answer in the newer cache because the cache carries a generation, and the cache itself is a least-recently-used map capped at 1024 entries, pinned by `TestVisibilityEvaluatorDropsCachedDecisionsWhenAuthorizationsChange`, `TestVisibilityEvaluatorDoesNotCacheAcrossAnAuthorizationChange`, `TestVisibilityEvaluatorAuthorizationsAreCopied` and `TestVisibilityEvaluatorCacheIsBounded` (`accumulo/column_visibility_test.go`). |
| SB-UNSAFE-044 | An empty visibility is not treated as unrestricted, and `VisibilityParseException` hides the detail it exists to carry | `include/data/constructs/security/VisibilityEvaluator.h:55-58`, `include/data/exceptions/VisibilityParseException.h:23-31` | `evaluate(expr)` starts with `if (expr.empty() && !auths.empty()) return false;`. An entry with no column visibility is unrestricted in Accumulo, so returning `false` hides every unlabeled cell from any principal that holds authorizations; and when the authorizations are empty the guard does not fire, so the empty expression reaches `ColumnVisibility("")`, which throws `IllegalArgumentException("empty term")` — a predicate that neither answers nor reports a parse problem. The exception raised elsewhere on this path, `VisibilityParseException`, keeps `terms` and `offset` private with no accessors, and its `what()` is declared neither `const` nor `noexcept`, so it does not override `std::exception::what()`: the message is lost through `catch (const std::exception &e)`. | Treat an empty expression as satisfied and make the detail readable. `VisibilityEvaluator.Evaluate` returns `true` for an empty expression whatever the authorizations, and `*VisibilityParseError` exports `Reason`, `Terms` and `Offset` and unwraps to `ErrVisibilityParse`; `TestVisibilityEvaluatorTreatsAnEmptyExpressionAsUnrestricted` and `TestColumnVisibilityRejectsMalformedExpressions` (`accumulo/column_visibility_test.go`) pin both. |
| SB-UNSAFE-045 | The visibility parser rejects the `.` and `/` characters Accumulo's own grammar allows | `src/data/constructs/security/ColumnVisibility.cpp:175-180`, `src/data/constructs/security/Authorizations.cpp:14-32` | An unquoted term is validated with `Authorizations::isValidAuthCharacter`, whose table admits only letters, digits, `_`, `-` and `:`. Accumulo's visibility grammar also admits `.` and `/`, so an expression the server accepted at write time - `team.alpha`, `org/admin` - throws `IllegalArgumentException("bad character")` on the client. The data is unreadable through Sharkbite even though the principal holds the authorization, and the failure is a parse error rather than a visibility denial, so it does not degrade into "hidden": it aborts the call. | Widen the parser, not the pinned predicate. `ValidAuthorizationCharacter` still answers the Sharkbite question exactly, and the visibility parser adds `.` and `/` on top of it, pinned by `TestColumnVisibilityAcceptsAccumulosTermCharacters` (`accumulo/column_visibility_test.go`). |

<a id="sec-22"></a>

## 22. Behavior that must be preserved bit-for-bit

These are contracts, not defects. Changing any of them breaks working
Sharkbite programs, so each must be reproduced exactly and covered by a
compatibility test.

| ID | Contract | Source of truth |
| --- | --- | --- |
| SB-KEEP-001 | Both module names import: `import sharkbite` and `import pysharkbite`, with identical symbol sets. | `PYTHONREADME.md:10`; `sharkbite/__init__.py:5`; `test/python/testmodule/__init__.py:64` |
| SB-KEEP-002 | Every alias pair is present: `getInstanceName`/`instance_name`, `getInstanceId`/`instance_id`, `getUserName`/`username`, `getPassword`/`password`, `getPriority`/`priority`, `getName`/`name`, `getClass`/`class`. | `pysharkbite.cpp:71-74,78-83,89-94,99-104` |
| SB-KEEP-003 | `Key()` default timestamp is `9223372036854775807`. | `pysharkbite.cpp:299-300` |
| SB-KEEP-004 | `Mutation.put` default timestamp is `0` and is written literally. | `pysharkbite.cpp:363-364` |
| SB-KEEP-005 | `Range` accepts `""` and `None` as unbounded ends. | `test/python/TestRanges.py:120,147` |
| SB-KEEP-006 | `Range(row)` covers the whole row. | `pysharkbite.cpp:336`; `test/python/TestRanges.py:64` |
| SB-KEEP-007 | Scanner and writer both implement the context-manager protocol and close on exit. | `pysharkbite.cpp:438-443,489-495`; `test/python/TestWithFx.py:30,50` |
| SB-KEEP-008 | `scanner.withRange(...)` returns the scanner. | `pysharkbite.cpp:444-446`; `test/python/TestWithFx.py:50` |
| SB-KEEP-009 | `getResultSet()` may be called repeatedly and re-executes the accumulated ranges. | `test/python/TestRanges.py:64-86` |
| SB-KEEP-010 | Iterating a result set yields `KeyValue` objects and terminates with `StopIteration`. | `include/scanner/constructs/Results.h:280-288` |
| SB-KEEP-011 | `async for` over a result set works. | `pysharkbite.cpp:381-396`; `examples/asyncexample.py` |
| SB-KEEP-012 | `tableOperations.exists(False)` then `create(False)` is the canonical bootstrap sequence, and `create` on an existing table returns `False` rather than raising. | `test/python/TestWrites.py:15-19` |
| SB-KEEP-013 | `tableOperations.remove()` is the canonical teardown and must succeed on an existing table. | every `test/python/*.py` |
| SB-KEEP-014 | Writing an empty value is legal and scans back as an empty value. | `test/python/TestWrites.py:36,73-83` |
| SB-KEEP-015 | `Authorizations()` with no entries is legal for both scanning and writing. | `test/python/TestWrites.py:23,29` |
| SB-KEEP-016 | *(withdrawn by audit #114 — not a contract)* Invalid authorization characters do **not** raise: the throw at `include/data/constructs/security/Authorizations.h:88` sits in `validateAuths()`, which no constructor and no `addAuthorization` ever calls (`src/data/constructs/security/Authorizations.cpp:48-79`). A shim that rejected such labels would break programs the pinned release accepts. See [SB-UNSAFE-034](#sec-21). | `include/data/constructs/security/Authorizations.h:84-92`; `src/data/constructs/security/Authorizations.cpp:48-79` |
| SB-KEEP-017 | Cells whose visibility is not satisfied are omitted from scan results without error. | `test/python/TestAuthorizations.py:68-78` |
| SB-KEEP-018 | `ClientException` is catchable by name. It is **not** a `RuntimeError` subclass: `pybind11::exception<>` derives from `Exception`, and the pinned tests list `pysharkbite.ClientException`, `RuntimeError`, `TypeError`, and `NameError` as separate entries in one `except` tuple. A shim must preserve which failures raise which type. | `pysharkbite.cpp:613`; `test/python/TestBadOperations.py:63,71,79,94` |
| SB-KEEP-019 | Passing `None` where an `Authorizations` is required raises rather than crashing. | `test/python/TestBadOperations.py:75-79` |
| SB-KEEP-020 | Operating on a removed table raises. | `test/python/TestBadOperations.py:33-62` |
| SB-KEEP-021 | Re-authenticating as a dropped user raises `ClientException`. | `test/python/TestSecurityOperations.py:110-116` |
| SB-KEEP-022 | Scans survive tablet relocation transparently. | `test/vandv/testScanLocationMove.h` |
| SB-KEEP-023 | After `writer.addMutation(m)` the caller may drop or rebind its Python reference to `m` and build a new `Mutation` for the next row. This is **all** the pinned tests prove: they never mutate an already-added mutation. Sharkbite enqueues the same `shared_ptr`, so mutating an added mutation is aliasing, not a supported contract ([SB-DATA-046](#sec-8)). | `test/python/TestWrites.py:46-49`; `sharkbite/__init__.py:79-95` |
| SB-KEEP-024 | Repeated create-writer/close-writer cycles on the same table handle work. | `test/python/TestWrites.py:29-51` |
| SB-KEEP-025 | A 1000-row write followed by a full scan returns exactly 1003 entries in the pinned fixture. | `test/python/TestWrites.py:93-97` |
| SB-KEEP-026 | `Value.get_bytes()` returns exact bytes with no transcoding. | `pysharkbite.cpp:275-279` |
| SB-KEEP-027 | RFile round trip: `openForWrite` → `append` → `close` → `sequentialRead` → `seek(Seekable(Range()))` → `hasNext`/`getTopKey`/`next` yields the written rows in order. | `test/python/TestRFileWrites.py:35-64` |
| SB-KEEP-028 | HDFS round trip: `write` → `writeString` → `read` → `readString` → `remove(path, False)`. | `examples/hdfswrite.py` |
| SB-KEEP-029 | `AccumuloWriter.put` substitutes the current epoch millisecond when `timestamp == 0`, and flushes the pending mutation when the row changes. | `sharkbite/__init__.py:76-95` |
| SB-KEEP-030 | Default thread count for the high-level helpers is 10. | `sharkbite/__init__.py:19` |
| SB-KEEP-031 | The ZooKeeper session timeout used by the convenience connector constructor is 1000 ms. | `pysharkbite.cpp:262`; `sharkbite/__init__.py:30` |
| SB-KEEP-032 | Status objects accept arbitrary user attributes (`dynamic_attr`). | `pysharkbite.cpp:108,121,128,136,144,158,186,209` |

<a id="sec-23"></a>

## 23. Gap ledger and ordered prerequisites

Work proceeds strictly in stage order. A stage may not start until the previous
stage is complete for the rows it depends on.

This ledger is the ordered-gap deliverable of Shoal issue
[#81](https://github.com/phrocker/shoal-oss/issues/81). Every `SB-GAP-*` entry
below without an existing issue link needs one filed and linked back to #81
before its stage starts; #81 stays open until every stage-1 through stage-4
entry is either closed or reclassified in [§26](#sec-26).

### 23.1 Stage 1 — Go parity (blocks everything)

| ID | Gap | Matrix rows | Existing issue/PR | Notes |
| --- | --- | --- | --- | --- |
| SB-GAP-GO-001 | Security operations: users, permissions, authorizations | SB-SEC-001…SB-SEC-017, SB-CONN-004 | [#95](https://github.com/phrocker/shoal-oss/issues/95) / merged PR [#99](https://github.com/phrocker/shoal-oss/pull/99) | **Complete in Go.** The remaining row differences are Python object/return semantics and live-cluster visibility enforcement. |
| SB-GAP-GO-002 | Namespace operations | SB-NS-001…SB-NS-007, SB-CONN-005 | [#94](https://github.com/phrocker/shoal-oss/issues/94) / merged PR [#97](https://github.com/phrocker/shoal-oss/pull/97), on merged PR [#91](https://github.com/phrocker/shoal-oss/pull/91) | **Complete in Go.** Namespace discovery, lifecycle, and effective/local/versioned property reads are public. |
| SB-GAP-GO-003 | Cluster status / `getStatistics` | SB-STAT-001…SB-STAT-038, SB-CONN-007 | [#96](https://github.com/phrocker/shoal-oss/issues/96); PR [#98](https://github.com/phrocker/shoal-oss/pull/98) **withdrawn** | **Not buildable — removed upstream.** Accumulo commit `f0841e4` deleted `getManagerStats`, `ManagerMonitorInfo`, `DeadServer`, and the legacy monitor resources 113 commits before the pinned Accumulo 4 target `317c288`; the surviving REST-v2/metrics/server APIs cannot reconstruct SB-STAT-001…SB-STAT-037. PR #98 was withdrawn for exactly this reason. This is no longer a Go work item: it is an approval decision, [SB-DIV-016](#sec-26). Only SB-STAT-038 (a Python `dynamic_attr` property) remains a Python-layer gap, and it is moot unless the divergence is rejected. |
| SB-GAP-GO-004 | Constraints, online compaction, row-bounded flush, and a Sharkbite-compatible bulk-import adapter | SB-TABLE-006, SB-TABLE-012, SB-TABLE-013, SB-TABLE-005 | [#65](https://github.com/phrocker/shoal-oss/issues/65) (online compaction), merged PR [#78](https://github.com/phrocker/shoal-oss/pull/78) (Bulk Import V2), [#119](https://github.com/phrocker/shoal-oss/issues/119)/PR [#121](https://github.com/phrocker/shoal-oss/pull/121) (constraints and row-bounded flush) | **Two of four delivered.** Constraint installation, listing and removal are public, and flush now carries row bounds. Bulk Import V2 is public, but its staged-load-map contract does not close Sharkbite's `dir`/`fail_path` row. Online compaction stays blocked on an interface gap rather than on effort: the pinned Accumulo 4 Thrift has no compaction RPC and encodes the FATE argument outside the IDL ([SB-TABLE-006](#sec-11)). |
| SB-GAP-GO-005 | Public RFile API (read, sequential read, write, locality groups, iterators, seekables) | SB-RFILE-001…SB-RFILE-021, SB-RFILE-025…SB-RFILE-036 | [#113](https://github.com/phrocker/shoal-oss/issues/113)/PR [#114](https://github.com/phrocker/shoal-oss/pull/114) | **31 of 32 rows delivered.** The public `rfile` package exports the writer, the random-access and sequential readers, the merged multi-file read, the iterator contract, and the relocation types, with lifecycle, cancellation, concurrency, binary-safety, and copy-semantics tests. The one remaining Go gap is [SB-RFILE-012](#sec-15): `internal/rfile` writes the default locality group only. Reading named groups is covered — the public readers merge every group in the file — so the remaining work is write-side. |
| SB-GAP-GO-006 | Public HDFS API (typed streams, dir entries, rename/move/chown/mkdir) | SB-HDFS-001…SB-HDFS-014 | [#10](https://github.com/phrocker/shoal-oss/issues/10)/PR [#85](https://github.com/phrocker/shoal-oss/pull/85) (cancellation), [#9](https://github.com/phrocker/shoal-oss/issues/9) (live coverage, merged) | Land cancellation before exposing HDFS to Python. |
| SB-GAP-GO-007 | Streaming scan results (cursor, not slice) | SB-SCAN-004, SB-XCUT-010, SB-CPP-005 | [#135](https://github.com/phrocker/shoal-oss/issues/135)/PR [#136](https://github.com/phrocker/shoal-oss/pull/136) | **Delivered in Go.** `Scanner.Stream` and `BatchScanner.Stream` return `*ResultStream`, a forward-only cursor that keeps one server batch resident, honours the context on every RPC, closes its session even after cancellation, and reads tablets sequentially so a batch scan is bounded too. [SB-CPP-005](#sec-19) stays `Behavior mismatch`: it is about `createScanner`'s `threads` argument, which streaming does not change ([SB-SCAN-012](#sec-9)). The ABI shape now follows in [SB-GAP-C-005](#sec-23). |
| SB-GAP-GO-008 | Server-side Python iterators | SB-SCAN-016, SB-DATA-056, SB-DATA-061…SB-DATA-064, SB-SCAN-007, SB-CPP-014 | none | Needs a decision: reimplement the JNI iterator shim, transpile to Go iterators (see `docs/iterator-forge-design.md`), or record as an intentional divergence. |
| SB-GAP-GO-009 | Hedged reads and RFile-only scanning options | SB-SCAN-014, SB-SCAN-015 | none | Published, documented Sharkbite options. |
| SB-GAP-GO-010 | Logging control | SB-LOG-001…SB-LOG-003 | none | Needs a public, process-global switch. |
| SB-GAP-GO-011 | Range predicates (`after_end_key`, `before_start_key`) and `Range.__str__` | SB-DATA-031…SB-DATA-033, SB-DATA-074 | [#115](https://github.com/phrocker/shoal-oss/issues/115)/PR [#116](https://github.com/phrocker/shoal-oss/pull/116) | **Complete in Go.** `Range.AfterEndKey`, `Range.BeforeStartKey`, `Range.Contains` and `Range.String` are public, and the predicates resolve row bounds through `Range.KeyBounds` so they cover whole rows. Only the ABI remains. |
| SB-GAP-GO-012 | Authorizations as a first-class type | SB-DATA-047…SB-DATA-052 | [#115](https://github.com/phrocker/shoal-oss/issues/115)/PR [#116](https://github.com/phrocker/shoal-oss/pull/116) | **Complete in Go.** `accumulo.Authorizations` is a sorted, deduplicated, byte-safe set with `Add`, `Remove`, `Contains`, `List`, `Strings`, `Equal`, `Clone` and a crash-free `String`. Validation is opt-in rather than enforced, because the pinned release never validates ([SB-UNSAFE-034](#sec-21) withdraws [SB-KEEP-016](#sec-22)). Only the ABI remains. |
| SB-GAP-GO-014 | Writer buffered-size accessor | SB-WRITE-006 | none | |
| SB-GAP-GO-015 | Live-cluster conformance harness | SB-XCUT-015, SB-CPP-010, SB-CPP-013 | [#74](https://github.com/phrocker/shoal-oss/issues/74) (conformance release gates) | Delivery vehicle for [§24](#sec-24). |
| SB-GAP-GO-016 | Column-visibility expressions: parse, inspect, normalize, flatten and evaluate | SB-ERR-010, SB-CXX-1046…SB-CXX-1049, SB-CXX-1056…SB-CXX-1088 | [#146](https://github.com/phrocker/shoal-oss/issues/146)/PR [#147](https://github.com/phrocker/shoal-oss/pull/147) | **Delivered in Go.** `accumulo.ColumnVisibility` parses the pinned grammar into an immutable `VisibilityNode` tree with term offsets, normalizes and flattens it without mutating what it returns, and `accumulo.VisibilityEvaluator` decides an expression against `Authorizations` with a cache that is invalidated when the authorizations change. The seven remaining [§19.2](#sec-19-2) rows are the pinned type's **mutation** entry points — the three `VisibilityNode` constructors, both `add` overloads, `getChildrenRef` and `clear` — which have no counterpart because a Go tree is a value produced by the parser and never edited in place; recording them as delivered would claim a capability that does not exist. Six pinned defects are recorded in [§21](#sec-21) rather than reproduced. |

### 23.2 Stage 2 — C ABI parity (blocked by Stage 1 per row)

| ID | Gap | Matrix rows | Existing issue/PR | Notes |
| --- | --- | --- | --- | --- |
| SB-GAP-C-001 | Table administration on the ABI | SB-TABLE-001…SB-TABLE-003, SB-TABLE-007, SB-TABLE-008, SB-TABLE-016…SB-TABLE-019, SB-CPP-016, SB-CPP-017 | [#82](https://github.com/phrocker/shoal-oss/issues/82) / merged PR [#84](https://github.com/phrocker/shoal-oss/pull/84) | **Complete on the ABI for the listed connector-scoped entry points.** `open_table` / `free_table` handle helpers remain separate flat-C gaps ([SB-CPP-002](#sec-19), [SB-CPP-018](#sec-19)), but the merged ABI now covers the rows listed here. [SB-TABLE-005](#sec-11) left this list in revision 27: `shoal_connector_flush_table` exports only the unbounded flush, so once Go gained the row-bounded form the row's contract stopped being reachable through the ABI. |
| SB-GAP-C-002 | Capability discovery | SB-XCUT-012 | [#86](https://github.com/phrocker/shoal-oss/issues/86) / merged PR [#87](https://github.com/phrocker/shoal-oss/pull/87) | **Complete on the ABI.** Append-only capability bits now advertise the merged administration slices and are validated in Go plus dynamically loaded C/C++ consumers. |
| SB-GAP-C-003 | Cancellation handle | SB-XCUT-008, SB-CONN-013 | [#138](https://github.com/phrocker/shoal-oss/issues/138) | Completed in revision 30 for single and batch scans with owned one-shot handles, idempotent cancel, cancel-and-join free, stable cancellation status, and C11/C++11/race evidence. SB-CONN-013 remains a behavior mismatch because Sharkbite exposes no cancellation at all. |
| SB-GAP-C-004 | Security, namespace, and table-split ABI | SB-SEC-*, SB-NS-*, SB-TABLE-010 | [#102](https://github.com/phrocker/shoal-oss/issues/102) | **Complete on the ABI.** This exact-head slice adds copied inputs, owned outputs, structured security errors, deadlines, close cancellation, and C11/C++11/race coverage. |
| SB-GAP-C-005 | Streaming scan cursor on the ABI | SB-SCAN-004, SB-SCAN-019…SB-SCAN-021, SB-SCAN-024, SB-BASE-011, SB-BASE-016…SB-BASE-019, SB-XCUT-010, SB-CXX-2414 | [#148](https://github.com/phrocker/shoal-oss/issues/148) | Completed in revision 36 with owned scanner, batch-scanner, and high-level client cursor factories, bounded independently owned chunks, deadlines and one-shot cancellation, parent-close cancellation and joining, stable terminal errors, allocation-failure injection, and C11/C++11/race evidence. |
| SB-GAP-C-006 | Cluster status ABI | SB-STAT-* | none, and none possible | **Cannot be scheduled.** Its Go prerequisite ([SB-GAP-GO-003](#sec-23)) is not a work item but an upstream removal, so there is nothing for an ABI to wrap. This entry stays only to record that the dependency is unsatisfiable rather than pending; it is resolved by the [SB-DIV-016](#sec-26) decision, and if that divergence is rejected the blocking work is upstream in Accumulo, not in Shoal. |
| SB-GAP-C-007 | RFile and HDFS ABI | SB-RFILE-*, SB-HDFS-* | [#125](https://github.com/phrocker/shoal-oss/issues/125) | The 31-row RFile half completed in revision 25 with owned reader/writer/seekable handles, copied entry and relocation inputs, owned result families, deadlines, close cancellation, and C11/C++11/race coverage. SB-RFILE-012 remains `Missing Go`; the HDFS half still follows SB-GAP-GO-006. |
| SB-GAP-C-008 | Range and key accessors on the ABI | SB-DATA-025…SB-DATA-030 | this change | **Complete on the ABI.** Owned copied range results expose full key bounds, inclusivity, and explicit unbounded flags through a versioned view. |
| SB-GAP-C-009 | Iterator-setting accessors | SB-DATA-057…SB-DATA-059, SB-DATA-069…SB-DATA-071 | this change | **Complete on the ABI.** Owned copied iterator descriptors expose deterministic name, class, priority, and option views. |
| SB-GAP-C-010 | Windows and macOS artifact verification | SB-XCUT-014, SB-PKG-008 | none | `make capi` is not exercised by CI on any platform. |
| SB-GAP-C-011 | Connector identity discovery | SB-CFG-009, SB-CFG-019, SB-CFG-010, SB-CFG-020, SB-CFG-013, SB-CFG-021, SB-CFG-015, SB-CFG-023 | this change | **Complete on the ABI.** One deadline-aware operation returns copied instance name, instance ID, and principal in an owned result with versioned view sizing, idempotent free, close cancellation, and C11/C++11/race coverage. |
| SB-GAP-C-012 | Client configuration and instance topology on the ABI | SB-CFG-001…SB-CFG-005, SB-CFG-024, SB-CFG-029…SB-CFG-032, SB-CFG-035, SB-CFG-036 | [#104](https://github.com/phrocker/shoal-oss/issues/104)/PR [#105](https://github.com/phrocker/shoal-oss/pull/105) delivered the Go surface; [#122](https://github.com/phrocker/shoal-oss/issues/122) tracks the ABI tranche | Completed in revision 24 with binary-safe configuration handles, owned bytes/string/server results, connector-close coordination, per-call live-discovery deadlines, and C11/C++11/race evidence. |
| SB-GAP-C-013 | Data-model value operations on the ABI | SB-DATA-015, SB-DATA-017, SB-DATA-031…SB-DATA-033, SB-DATA-051, SB-DATA-052, SB-DATA-073, SB-DATA-074, SB-CXX-0511…SB-CXX-0513, SB-CXX-0618, SB-CXX-0619, SB-CXX-1042, SB-CXX-1044, SB-CXX-1045 | [#130](https://github.com/phrocker/shoal-oss/issues/130) | Completed in revision 26 with binary-safe key/range formatting and predicates, copied owned KeyValue results, immutable copied authorization handles, owned list results, stable errors, allocation-failure injection, and C11/C++11/race evidence. |
| SB-GAP-C-014 | High-level buffered writer on the ABI | SB-WRITE-016…SB-WRITE-019 | [#132](https://github.com/phrocker/shoal-oss/issues/132) | Completed in revision 28 with lazy creation, copied binary updates, row buffering, timestamp parity, connector-close cancellation, concurrent close, structured errors, and C11/C++11/race evidence. |
| SB-GAP-C-015 | Row-bounded flush and constraint administration on the ABI | SB-TABLE-005, SB-TABLE-012, SB-CXX-1989, SB-CXX-1997 | [#134](https://github.com/phrocker/shoal-oss/issues/134) | Completed in revision 29 with copied nullable binary bounds, owned ordered constraint snapshots, forward-sized borrowed views, stable errors, deadlines, connector-close coordination, allocation-failure injection, and C11/C++11/race evidence. |
| SB-GAP-C-016 | Connector cache invalidation on the ABI | SB-CONN-011 | [#138](https://github.com/phrocker/shoal-oss/issues/138) | Completed in revision 30 with copied table IDs, table/all-discovery invalidation, connector-close coordination, and C11/C++11/race evidence. |
| SB-GAP-C-017 | High-level client facade on the ABI | SB-BASE-001…SB-BASE-008 | [#140](https://github.com/phrocker/shoal-oss/issues/140) / merged PR [#141](https://github.com/phrocker/shoal-oss/pull/141) | Completed in revision 31 with copied configuration/table/binary authorizations, synchronized mutable settings, owned table/scanner results, lazy buffered writers, connector lifecycle coordination, stable errors, allocation-failure injection, and C11/C++11/race evidence. |
| SB-GAP-C-018 | High-level scanner facade on the ABI | SB-BASE-009, SB-BASE-012…SB-BASE-014, SB-BASE-020 | [#142](https://github.com/phrocker/shoal-oss/issues/142) | Completed in revision 32 with copied binary column selection, atomic settings snapshots, owned single/multi-range results, deadlines, one-shot cancellation, client-close joining, stable errors, allocation-failure injection, and C11/C++11/race evidence. SB-BASE-011 remains with streaming cursor gap SB-GAP-C-005 to avoid overlap with PR #136. |

### 23.3 Stage 3 — Compatibility tests (blocked by Stages 1 and 2)

| ID | Gap | Matrix rows | Notes |
| --- | --- | --- | --- |
| SB-GAP-T-001 | Port `test/vandv/*.h` behavioral assertions into a Shoal live-cluster suite | SB-CPP-010, SB-XCUT-015, SB-ERR-002 | Error codes, tablet moves, auth changes mid-session, empty ranges, invalid table ops. The numeric-code assertions in `invalidscans.h` are C++-scope only ([SB-ERR-002](#sec-18)); the Python acceptance suite ([SB-GAP-T-002](#sec-23)) asserts exception types instead, because `getErrorCode` is unbound. |
| SB-GAP-T-002 | Port the ten pinned `test/python/*.py` files as the Python acceptance suite | all of [§5](#sec-5)–[§20](#sec-20) | They must pass **unmodified** except for the `-s/--solocation` argument. |
| SB-GAP-T-003 | Binary-safety differential tests (non-UTF-8 rows, values, visibilities; embedded NULs) | SB-XCUT-004, SB-UNSAFE-002…SB-UNSAFE-004 | Must run against a live cluster, not just the ABI. |
| SB-GAP-T-004 | Visibility-enforcement conformance | SB-SEC-018, SB-KEEP-017 | |
| SB-GAP-T-005 | Error-code mapping table test, **C++ scope only** | SB-ERR-002, SB-CPP-010 | Asserts a Sharkbite `CLIENT_ERROR_CODES` value for every `shoal_status` a C++ compatibility layer can surface, matching what `test/vandv/invalidscans.h` asserts today. No Python obligation: `getErrorCode` is unbound ([SB-ERR-002](#sec-18)), so the Python acceptance suite asserts exception *types* instead. |
| SB-GAP-T-006 | Scale test equivalent to `QuarterMillionWrites.py` | SB-XCUT-010 | Proves the streaming decision. |
| SB-GAP-T-007 | Concurrency and GIL tests | SB-XCUT-006, SB-XCUT-017 | Threads plus `asyncio`. |
| SB-GAP-T-008 | Fork-safety test | SB-XCUT-018 | |

### 23.4 Stage 4 — Python and wheels (blocked by Stages 1–3)

| ID | Gap | Matrix rows | Notes |
| --- | --- | --- | --- |
| SB-GAP-C-019 | Stable compatibility error classification | SB-ERR-001, SB-ERR-008, SB-ERR-011, SB-ERR-014 | [#144](https://github.com/phrocker/shoal-oss/issues/144) | Completed in revision 34 with owned-error source categories, distinct ClientException/RuntimeError binding classes, immutable library-owned names, concurrent getters, and linked C11/C++11/race evidence. |

| SB-GAP-P-001 | `sharkbite` package with `pysharkbite` compatibility module | SB-PKG-002…SB-PKG-004 | |
| SB-GAP-P-002 | Exception mapping that preserves `ClientException` as distinct from `RuntimeError` | SB-ERR-001, SB-ERR-014, SB-KEEP-018 | The pinned tests catch `ClientException`, `RuntimeError`, `TypeError`, and `NameError` separately; the shim must preserve those distinctions. |
| SB-GAP-P-003 | High-level helpers (`AccumuloBase`, `AccumuloWriter`, `AccumuloScanner`, `AccumuloIterator`) with the SB-UNSAFE fixes | SB-BASE-001…SB-BASE-020, SB-WRITE-016…SB-WRITE-020, SB-SCAN-024 | 20 distinct helper rows in [§10.1](#sec-10-1), including `select_column` ([SB-BASE-020](#sec-10-1)); covers the chunk-boundary, `set_threads`/`to_scanner`, and `set_table`/`set_authorizations` defects ([SB-UNSAFE-008](#sec-21), [SB-UNSAFE-023](#sec-21), [SB-UNSAFE-027](#sec-21)). |
| SB-GAP-P-004 | `sharkbite.torch` optional extra | SB-PKG-012, SB-TORCH-001…SB-TORCH-009 | Includes fixing `coerce` ([SB-UNSAFE-021](#sec-21)) and `AccumuloValueDataset.__init__` ([SB-UNSAFE-022](#sec-21)). |
| SB-GAP-P-007 | `pandashark` distribution rebuilt on the new helpers | SB-PKG-013, SB-PANDA-001…SB-PANDA-021 | All 21 pandas rows: the two `read_accumulo` forms and `read_accumulo_nex`, the `DataFrameIterator` members, the duplicated `pandashark.AccumuloIterator` ([SB-PANDA-008](#sec-9-3)…[SB-PANDA-012](#sec-9-3)), and the byte-identical `pandashark.torch` copy ([SB-PANDA-014](#sec-9-3)…[SB-PANDA-021](#sec-9-3)). Must fix `chunksize`-as-`begin_row` ([SB-UNSAFE-024](#sec-21)), the dead `@overload` ([SB-UNSAFE-025](#sec-21)), non-terminating iteration ([SB-UNSAFE-026](#sec-21)), the shared class-level `_columns` set, and the name-mangled `__iterator` ([SB-UNSAFE-028](#sec-21)), while keeping `nextBatch()` callable. |
| SB-GAP-P-005 | Wheel matrix and `manylinux` build | SB-PKG-007…SB-PKG-011 | Includes the Python floor decision. |
| SB-GAP-P-006 | Migration guide covering every approved divergence | [§26](#sec-26) | Ships with the first release. |

<a id="sec-23-5"></a>

### 23.5 Related umbrella work

This matrix, its audit, and this ledger are tracked by Shoal issue
[#81](https://github.com/phrocker/shoal-oss/issues/81).
Issue [#59](https://github.com/phrocker/shoal-oss/issues/59) is #81's parent and
tracks the overall migration;
[#75](https://github.com/phrocker/shoal-oss/issues/75) tracks the
execution plane. Merged prerequisites already in `origin/main`:
[#48](https://github.com/phrocker/shoal-oss/issues/48)/PR [#52](https://github.com/phrocker/shoal-oss/pull/52) (connector lifecycle ABI),
[#61](https://github.com/phrocker/shoal-oss/issues/61)/PR [#62](https://github.com/phrocker/shoal-oss/pull/62) (scanner and owned result ABI),
[#63](https://github.com/phrocker/shoal-oss/issues/63)/PR [#64](https://github.com/phrocker/shoal-oss/pull/64) (mutation and BatchWriter ABI),
[#53](https://github.com/phrocker/shoal-oss/issues/53)/PR [#55](https://github.com/phrocker/shoal-oss/pull/55) (BatchWriter latency flushing),
[#54](https://github.com/phrocker/shoal-oss/issues/54)/PR [#56](https://github.com/phrocker/shoal-oss/pull/56) (effective table property reads),
PR [#78](https://github.com/phrocker/shoal-oss/pull/78) (Bulk Import V2),
and PR [#79](https://github.com/phrocker/shoal-oss/pull/79) (reachable embed service).
Merged follow-up slices now reflected in the audited rows:
PR [#83](https://github.com/phrocker/shoal-oss/pull/83) (table split listing),
PR [#84](https://github.com/phrocker/shoal-oss/pull/84) (table administration ABI),
PR [#85](https://github.com/phrocker/shoal-oss/pull/85) (HDFS cancellation),
PR [#87](https://github.com/phrocker/shoal-oss/pull/87) (ABI version and capability discovery, stacked on #84),
PR [#91](https://github.com/phrocker/shoal-oss/pull/91) (namespace discovery, issue [#89](https://github.com/phrocker/shoal-oss/issues/89)),
PR [#93](https://github.com/phrocker/shoal-oss/pull/93) (table splits through manager FATE, issue [#88](https://github.com/phrocker/shoal-oss/issues/88)),
PR [#97](https://github.com/phrocker/shoal-oss/pull/97) (namespace administration, issue [#94](https://github.com/phrocker/shoal-oss/issues/94), stacked on #91),
and PR [#99](https://github.com/phrocker/shoal-oss/pull/99) (security administration, issue [#95](https://github.com/phrocker/shoal-oss/issues/95)).
PR [#98](https://github.com/phrocker/shoal-oss/pull/98) (cluster status, issue
[#96](https://github.com/phrocker/shoal-oss/issues/96)) was **withdrawn**: the
upstream RPCs no longer exist ([§14](#sec-14), [SB-DIV-016](#sec-26)).

<a id="sec-23-6"></a>

### 23.6 Pin discipline for in-flight work

Row statuses describe the pinned Shoal commit in [§1](#sec-1) and nothing else.
An open pull request — however complete, however green its CI — never changes a
row from `Missing Go` or `Missing C ABI`, and never contributes evidence: a
test that does not exist on `main` cannot be named by a `Covered` row under the
[§2](#sec-2) gate. In-flight work is recorded only in this ledger and in
[§23.5](#sec-23-5).

When such a PR merges, the repin is a deliberate, auditable step:

1. Update the `Shoal reference` row in [§1](#sec-1) to the new verified
   `origin/main` SHA and update the `§3.2` inventory counts.
2. Re-audit the affected rows against the merged code and its **named** tests,
   applying the [§4.2](#sec-4) rules — including "unproven equivalence counts
   as difference". Landing an API is not the same as proving Sharkbite
   semantics; expect most rows to move `Missing Go` → `Behavior mismatch`
   rather than straight to `Covered`.
3. Re-run the mechanical count extraction and update [§25](#sec-25) in the same
   change.
4. Record any semantics that cannot be reproduced as a divergence in
   [§26](#sec-26). Note that a divergence is **not** contingent on a pull
   request: [SB-DIV-016](#sec-26) and [SB-DIV-013](#sec-26) exist because
   Accumulo removed the capability upstream, and PR
   [#98](https://github.com/phrocker/shoal-oss/pull/98) was withdrawn rather
   than merged. Nothing in this ledger queues withdrawn work.

<a id="sec-24"></a>

## 24. Live-cluster conformance requirements

All 94 Go tests in `accumulo/` are unit tests driven by fake adapters: a search
for build tags, `testing.Short()`, environment gating, and testcontainers in
`accumulo/*_test.go` returns nothing. The only live-service coverage in Shoal is
HDFS (`make test-hdfs`, `.github/workflows/hdfs-integration.yml`). Sharkbite's
own live tests are equally excluded from its CI. Therefore **no row whose
semantics are defined by a real tablet server may be marked `Covered` on unit
evidence alone**.

Rows that require live-cluster evidence before they can close:

Some rows below are already `Covered` at the Go and C ABI layers. That status
records what Shoal's own layers prove; the live-cluster test additionally
validates the end-to-end Python shim against a real tablet server. A row that is
not yet `Covered` cannot be closed by a live test alone either — it needs both.

| Area | Rows | What must be proven against a real Accumulo |
| --- | --- | --- |
| Authentication | SB-CONN-012, SB-KEEP-021 | Bad credentials and dropped users raise the mapped exception. |
| Visibility | SB-SEC-018, SB-KEEP-017 | Cells are filtered exactly as `test/python/TestAuthorizations.py` asserts. |
| Server-side iterators | SB-SCAN-006, SB-SCAN-016 | A configured iterator actually transforms results. |
| Tablet movement | SB-SCAN-026, SB-KEEP-022 | Scans survive real splits and migrations. |
| Write failures | SB-WRITE-010, SB-WRITE-011, SB-WRITE-012 | Constraint violations, authorization failures, and ambiguous commits behave as modeled. |
| Binary safety | SB-XCUT-004, SB-GAP-T-003 | Non-UTF-8 and NUL-containing components survive a real round trip. |
| Range semantics | SB-DATA-020…SB-DATA-024, SB-KEEP-005, SB-KEEP-006 | Empty, `None`, and whole-row ranges return the pinned row counts. |
| Scale and streaming | SB-XCUT-010, SB-GAP-T-006 | 250k-row write and scan complete without materializing the result set. |
| Table lifecycle | SB-TABLE-001…SB-TABLE-003, SB-KEEP-012, SB-KEEP-013 | `exists`/`create`/`remove` sequencing matches, including the `create` returning `False` case. |

Minimum harness: an ephemeral single-node Accumulo 4 plus ZooKeeper (the role
Sharkbite's `test/19x/st` SMAC project played), driven from CI, with the ported
`test/python/*.py` suite as the acceptance gate.

<a id="sec-25"></a>

## 25. Counts by status and category

### 25.1 By status

| Status | Rows |
| --- | --- |
| Covered | 100 |
| Missing Go | 2326 |
| Missing C ABI | 79 |
| Behavior mismatch | 219 |
| Intentional divergence (approval required) | 87 |
| Not required (rationale required) | 392 |
| **Total** | **3203** |

2811 rows are **required** by the final release gate ([§2.2](#sec-2)); the 392
`Not required` rows are excluded by construction, and 304 of those are the
evidence-proved duplicates described in [§19.1](#sec-19-1). All 87 divergence
rows are unapproved, so they block the gate exactly like an unimplemented row
([§26](#sec-26)). Enumeration is complete: every live public declaration in
every non-vendored header maps to exactly one row, proved arithmetically in
[§19.1](#sec-19-1) and re-checked by the thirty-seven-check verifier in
[§19.3](#sec-19-3).

### 25.2 By category

| Section | Prefix | Rows | Covered | Missing Go | Missing C ABI | Behavior mismatch | Intentional divergence | Not required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| [§5](#sec-5) Packaging and imports | `SB-PKG` | 14 | 0 | 0 | 10 | 1 | 1 | 2 |
| [§6](#sec-6) Configuration and credentials | `SB-CFG` | 36 | 12 | 0 | 0 | 16 | 2 | 6 |
| [§7](#sec-7) Connector and session | `SB-CONN` | 13 | 1 | 1 | 0 | 10 | 1 | 0 |
| [§8](#sec-8) Data model | `SB-DATA` | 75 | 9 | 9 | 1 | 50 | 0 | 6 |
| [§9](#sec-9) Scanners and results | `SB-SCAN` | 28 | 5 | 5 | 3 | 10 | 0 | 5 |
| [§9.2](#sec-9-2) PyTorch datasets | `SB-TORCH` | 9 | 0 | 0 | 9 | 0 | 0 | 0 |
| [§9.3](#sec-9-3) pandas surface | `SB-PANDA` | 21 | 0 | 0 | 20 | 0 | 0 | 1 |
| [§10](#sec-10) Writers | `SB-WRITE` | 23 | 4 | 1 | 2 | 7 | 0 | 9 |
| [§10.1](#sec-10-1) High-level helpers | `SB-BASE` | 20 | 18 | 0 | 0 | 0 | 0 | 2 |
| [§11](#sec-11) Table operations | `SB-TABLE` | 22 | 2 | 1 | 0 | 13 | 0 | 6 |
| [§12](#sec-12) Namespaces | `SB-NS` | 8 | 0 | 0 | 0 | 8 | 0 | 0 |
| [§13](#sec-13) Security | `SB-SEC` | 19 | 0 | 0 | 0 | 18 | 0 | 1 |
| [§14](#sec-14) Cluster status | `SB-STAT` | 84 | 0 | 1 | 0 | 0 | 82 | 1 |
| [§15](#sec-15) RFile and streams | `SB-RFILE` | 36 | 31 | 1 | 0 | 1 | 0 | 3 |
| [§16](#sec-16) HDFS | `SB-HDFS` | 26 | 0 | 26 | 0 | 0 | 0 | 0 |
| [§17](#sec-17) Logging | `SB-LOG` | 3 | 0 | 2 | 0 | 1 | 0 | 0 |
| [§18](#sec-18) Errors | `SB-ERR` | 16 | 4 | 1 | 1 | 6 | 1 | 3 |
| [§19](#sec-19) C++ public surfaces, curated | `SB-CPP` | 70 | 0 | 11 | 1 | 50 | 0 | 8 |
| [§19.2](#sec-19-2) C++ complete member enumeration | `SB-CXX` | 2626 | 11 | 2266 | 30 | 15 | 0 | 304 |
| [§19.4](#sec-19-4) Dead embedded-module surface | `SB-EMB` | 35 | 0 | 0 | 0 | 0 | 0 | 35 |
| [§20](#sec-20) Cross-cutting | `SB-XCUT` | 19 | 3 | 1 | 2 | 13 | 0 | 0 |
| **Total** |  | **3203** | **100** | **2326** | **79** | **219** | **87** | **392** |

### 25.3 Reading the counts

**Exactly 100 rows are `Covered`: [SB-XCUT-012](#sec-20), the twelve configuration/topology rows completed in revision 24, the 31 RFile/stream rows completed in revision 25, the 17 data-model value rows completed in revision 26, the four buffered-writer rows completed in revision 28, the four row-bounded flush/constraint rows completed in revision 29, the connector invalidation/cancellation rows completed in revision 30, the eight high-level client rows completed in revision 31, the five high-level scanner rows completed in revision 32, the four compatibility-error rows completed in revision 34, and the twelve streaming cursor rows completed in revision 36.** Capability discovery, configuration/topology, RFile, the supported data-value surface, the high-level buffered writer, table maintenance, connector controls, and bounded scan streaming are
now exported end to end: Go pins the append-only bitset contract, and linked C
and C++ consumers exercise them through the shared library. Every other
previously-claimed coverage row still needs either a live-cluster parity test
or a shim-level behavioral proof.

The shape of the work is visible in the 2326 `Missing Go` rows, of which 2266
are the C++ members in [§19.2](#sec-19-2) that no Shoal layer exports. That
number is large because Sharkbite publishes its entire client implementation in
public headers — 2603 live public members plus 16 enums across 204 non-vendored
headers — and every one of them is a surface a C++ consumer may already depend
on. The Python-visible subset is far smaller, which is why [§5](#sec-5)–
[§18](#sec-18) remain the practical scope for a wheel, and why
[§19.2](#sec-19-2) rows carry a per-area rationale rather than an implied
promise to port all of it.

`Behavior mismatch` (219) is the bucket that sets the schedule: 204 rows on the
Python-visible and curated C++ surface each need a differential test against a
live cluster or the exported ABI, and 15 are destructors of classes bound into
Python, where the destruction point is user-observable and the model differs
from Go finalisation ([§19.1](#sec-19-1)). `Intentional divergence` (87) is
dominated by one upstream fact: 82 rows are cluster-status accessors Accumulo
itself deleted ([§14](#sec-14), [SB-DIV-016](#sec-26)). `Missing C ABI` (79)
is now led by the visibility expression surface (30), pandas (20), packaging/import
scaffolding (10), PyTorch (9), the remaining scanner rows (3), and the remaining
data-model row (1).

### 25.4 Revision history

| Bucket | Rev 24 | Rev 25 | Rev 26 | Rev 27 | Rev 28 | Rev 29 | Rev 30 | Rev 31 | Rev 32 | Rev 33 | Rev 34 | Rev 35 | Rev 36 | Rev 35 → 36 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Covered | 13 | 44 | 61 | 61 | 65 | 69 | 71 | 79 | 84 | 84 | 88 | 88 | 100 | +12 |
| Missing Go | 2364 | 2364 | 2364 | 2361 | 2361 | 2361 | 2361 | 2361 | 2361 | 2357 | 2357 | 2326 | 2326 | 0 |
| Missing C ABI | 125 | 94 | 77 | 81 | 77 | 73 | 71 | 63 | 58 | 64 | 60 | 91 | 79 | −12 |
| Behavior mismatch | 222 | 222 | 222 | 221 | 221 | 221 | 221 | 221 | 221 | 219 | 219 | 219 | 219 | 0 |
| Intentional divergence | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 87 | 0 |
| Not required | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 392 | 0 |
| **Total** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **3203** | **0** |

Revision 36 changes twelve statuses without changing the inventory.
SB-SCAN-004, SB-SCAN-019 through SB-SCAN-021, SB-SCAN-024, SB-BASE-011,
SB-BASE-016 through SB-BASE-019, SB-CXX-2414, and SB-XCUT-010 move from
`Missing C ABI` to `Covered`. ABI 1.13.0 capability 24 adds owned streaming
cursors for scanner, batch-scanner, and high-level client calls; independently
owned bounded chunks; deadline and one-shot cancellation for the cursor
lifetime; scanner/client/connector-close cancellation and joining; stable
terminal errors; allocation-failure injection; and NULL-safe idempotent free.
`TestSharedLibraryCABI`, `TestOwnedScanCursorChunksAndExhausts`,
`TestOwnedScanCursorCloseCancelsAndJoinsBlockedNext`,
`TestScannerCloseCancelsAndJoinsOwnedCursor`,
`TestClientCloseCancelsAndJoinsOwnedCursor`,
`go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`,
`go test ./...`, and the revision-36 manifests and validator are the release
evidence.

Revision 35 changes 31 statuses without changing the
inventory. Shoal published the column-visibility surface: `accumulo.ColumnVisibility`
parses the pinned grammar into an immutable `VisibilityNode` tree that carries
term offsets, normalizes and flattens it without rewriting what it returns, and
`accumulo.VisibilityEvaluator` decides an expression against `Authorizations`
with a cache that is dropped whenever those authorizations change.
[SB-ERR-010](#sec-18) and 30 [§19.2](#sec-19-2) members move from `Missing Go`
to `Missing C ABI` under rule 2 of [§4.2](#sec-4); the seven mutation entry
points of the pinned node type stay `Missing Go` because a Go tree is a value
the parser produces and never edits in place ([SB-GAP-GO-016](#sec-23)).
Auditing the pinned sources proved six defects, recorded in [§21](#sec-21)
rather than reproduced: [SB-UNSAFE-040](#sec-21), where `getLength()` returns a
negative span so `flatten()` emits the tail of the expression for every term;
[SB-UNSAFE-041](#sec-21), four defects in `normalize` including an erase through
an invalidated iterator and a comparator that is not a strict weak ordering;
[SB-UNSAFE-042](#sec-21), where a stray `)` silently truncates an expression
into a more permissive one; [SB-UNSAFE-043](#sec-21), where `setVisibility`
leaves cached decisions in place so a reused evaluator answers with the previous
principal's authorizations; and [SB-UNSAFE-044](#sec-21), where an empty
visibility is treated as hidden rather than unrestricted; and
[SB-UNSAFE-045](#sec-21), where the parser rejects the `.` and `/` characters
Accumulo's own visibility grammar allows. No row became
`Covered` and the row population is unchanged.

Revision 34 changes four statuses without changing the inventory. SB-ERR-001, SB-ERR-008, SB-ERR-011, and SB-ERR-014 move from `Missing C ABI` to `Covered`. ABI 1.12.0 capability 23 adds immutable classification on every owned error: the closest Sharkbite C++ source category, the exact Python-facing `ClientException` versus `RuntimeError` class, and library-owned class names. The getters allocate nothing, expose no Go memory, remain stable under concurrent access, and preserve NULL-safe idempotent error free. `TestSharedLibraryCABI`, `TestCompatibilityErrorClasses`, `TestCompatibilityErrorClassificationIsConcurrent`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the revision-34 manifests and validator are the release evidence.

Revision 33 changes 6 statuses without changing the inventory. Shoal
published the streaming scan cursor the matrix has called the single most
consequential structural difference since revision 1: `Scanner.Stream` and
`BatchScanner.Stream` return a `*ResultStream` that keeps one server batch
resident, fetches the next only when the current one is drained, reads tablets
sequentially so a multi-tablet scan is bounded too, observes its context on
every RPC, and releases the server-side scan session on `Close` even after the
context is cancelled. [SB-SCAN-004](#sec-9) and [SB-XCUT-010](#sec-20) move from
`Behavior mismatch` to `Missing C ABI` because the ABI still buffers a whole
result, and [SB-SCAN-019](#sec-9)…[SB-SCAN-021](#sec-9) and
[SB-CXX-2414](#sec-19-2) move there from `Missing Go` under rule 2 of
[§4.2](#sec-4). Auditing the pinned `Results` template proved two further
defects, [SB-UNSAFE-038](#sec-21) — `__iter__`, `__aiter__` and `__await__`
each dequeue an entry, so re-entering iteration silently drops rows — and
[SB-UNSAFE-039](#sec-21), where postfix `operator++` behaves like prefix and
`operator!=` compares only the end flag. No row became `Covered` and the row
population is unchanged.

Revision 32 changes five statuses without changing the inventory. SB-BASE-009, SB-BASE-012, SB-BASE-013, SB-BASE-014, and SB-BASE-020 move from `Missing C ABI` to `Covered`; SB-BASE-011 remains pending because its chunked iterator contract overlaps the streaming work in PR #136. ABI 1.11.0 capability 22 adds copied binary column selection, atomic client scan snapshots, owned single/multi-range results, deadlines, one-shot cancellation, client-close cancellation and joining, stable errors, allocation-failure injection, and C11/C++11/race evidence. `TestSharedLibraryCABI`, `TestOwnedClientCopiesColumnSelectionsIntoSnapshots`, `TestOwnedClientScanHooksReceiveAtomicSnapshot`, `TestOwnedClientScanCancellationJoinsBeforeCloseReturns`, `TestOwnedClientConcurrentColumnSelectionAndSnapshots`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the revision-32 manifests and validator are the release evidence.

Revision 31 changes eight statuses without changing the inventory. SB-BASE-001 through SB-BASE-008 move from `Missing C ABI` to `Covered`. ABI 1.10.0 capability 21 adds an owned high-level client with copied configuration/table/binary authorizations, the Sharkbite thread default, synchronized setters, owned table snapshots and scanners, lazy buffered writers, connector-close cancellation, stable errors, allocation-failure injection, and C11/C++11/race evidence. `TestSharedLibraryCABI`, `TestOwnedClientCopiesAndSnapshotsMutableSettings`, `TestOwnedClientRequiresTableForScannerAndWriterSnapshot`, `TestOwnedClientConcurrentSettingsAndSnapshots`, `TestOwnedClientCloseCancelsAndJoinsActiveCall`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the revision-31 manifests and validator are the release evidence.

Revision 30 changes two statuses without changing the inventory. SB-CONN-011 and SB-XCUT-008 move from `Missing C ABI` to `Covered`. ABI 1.9.0 capability 20 adds owned one-shot scan cancellation, cancel/free joining, concurrent idempotent cancellation, copied table-ID invalidation, connector-close coordination, stable errors, allocation-failure injection, and C11/C++11/race evidence. `TestSharedLibraryCABI`, `TestOwnedCancellationCancelsCurrentAndFutureScans`, `TestOwnedCancellationCloseCancelsAndJoinsActiveScan`, `TestOwnedCancellationConcurrentCancelIsIdempotent`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the revision-30 manifests and validator are the release evidence.

Revision 29 changes four statuses without changing the inventory. SB-TABLE-005,
SB-TABLE-012, SB-CXX-1989, and SB-CXX-1997 move from `Missing C ABI` to
`Covered`. ABI 1.8.0 capability 19 adds copied nullable binary row bounds,
owned ordered constraint snapshots, forward-sized borrowed views, stable error
mapping, per-call deadlines, connector-close cancellation and joining,
allocation-failure injection, and NULL-safe idempotent free.
`TestSharedLibraryCABI`, `TestABIDiscoveryQueriesAreConcurrentAndStable`,
`go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`,
`go test ./...`, the C11/C++11 linked runtime, and the revision-29 manifests
and matrix validator are the release evidence.

Revision 28 changes four statuses without changing the inventory. SB-WRITE-016…SB-WRITE-019 move from `Missing C ABI` to `Covered`. ABI 1.7.0 capability 18 adds an owned lazy buffered writer with copied binary-safe updates, row-change and close flushing, exact put/delete timestamp semantics, per-call deadlines, connector-close cancellation, concurrent close/join, structured write failures, allocation-failure injection, and idempotent bounded free. `main()` in `capi/tests/lifecycle.c`, `TestSharedLibraryCABI`, the buffered-writer lifecycle and race tests, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the matrix validator are the release evidence.

Revision 27 changes 4 statuses without changing the inventory. Shoal published
constraint administration — install, list and remove, with the number Accumulo
assigns and a compare-and-set that makes two clients allocate different ones — and
gave flush the row bounds Sharkbite's signature carries, so
[SB-TABLE-012](#sec-11) and the two [§19.2](#sec-19-2) members behind those
operations move from `Missing Go` to `Missing C ABI`, and [SB-TABLE-005](#sec-11)
moves there from `Behavior mismatch` because `shoal_connector_flush_table` takes
no rows, so rule 2 of [§4.2](#sec-4) applies before rule 3. Online compaction is
deliberately not implemented: the pinned Accumulo 4 interface has no compaction
RPC at all and encodes the FATE argument outside the Thrift IDL, so
[SB-TABLE-006](#sec-11) records that interface gap instead of a guessed payload.
Two further pinned defects are recorded, [SB-UNSAFE-036](#sec-21) — the
`addConstraint` stub that installs nothing and returns 0 — and
[SB-UNSAFE-037](#sec-21), where setting a property to the empty string silently
removes it. No row became `Covered` and the row population is unchanged.

Revision 26 changes 17 statuses without changing the inventory. Nine §8 data-model rows and eight §19.2 aliases move from `Missing C ABI` to `Covered`; SB-DATA-040 remains `Missing C ABI` because keyword arguments are binding-layer work. ABI 1.6.0 capability 17 adds copied key/range operations, immutable owned authorization handles, owned key/value and text/list results, stable invalid-input/allocation errors, forward-sized key/value input, concurrent getters, and C11/C++11/race evidence. `main()` in `capi/tests/lifecycle.c`, `TestSharedLibraryCABI`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the matrix validator are the release evidence.

Revision 25 changes 31 statuses without changing the inventory. Every RFile and client-stream row that had a merged public Go API moves from `Missing C ABI` to `Covered`; SB-RFILE-012 remains `Missing Go`, and SB-RFILE-005 remains `Behavior mismatch`. ABI 1.5.0 capability 16 adds owned reader, writer, seekable, entry, value and range results; copied binary-safe inputs; stable error mapping; deadlines and close cancellation; forward-sized configs/views; and idempotent bounded frees. `main()` in `capi/tests/lifecycle.c`, `TestSharedLibraryCABI`, the RFile lifecycle race tests, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the matrix validator are the release evidence.

Revision 24 changes 12 statuses without changing the inventory. SB-CFG-001…005, SB-CFG-024, SB-CFG-029…032, SB-CFG-035 and SB-CFG-036 move from `Missing C ABI` to `Covered`. The ABI version is 1.4.0 with capability 15; `main()` in `capi/tests/lifecycle.c` exercises binary-safe configuration, owned/free behavior, allocation failure, static and live topology, result views, and deadlines. `TestSharedLibraryCABI`, `go test -race -tags=shoal_capi_test ./cmd/shoal-capi`, `go vet ./...`, `go test ./...`, and the matrix validator are the release evidence.

Revision 23 changes 16 statuses without changing the inventory. Shoal published the
data-model value types the Python surface exposes: `accumulo.Authorizations` is a
sorted, deduplicated, byte-safe set with membership, listing, equality and a
crash-free text form; `Range` gained the two containment predicates, their
composition, and the `operator<<` text form; and `Key` gained the `toString` text
form with the pinned escaping rules. Six [§8](#sec-8) rows move from `Missing Go`
to `Missing C ABI` under rule 2 of [§4.2](#sec-4), and the two `Key` text rows
move from `Behavior mismatch` to `Missing C ABI` because they never had a Go
mapping and rule 3 needs both layers. Eight [§19.2](#sec-19-2) rows follow them:
`Key::toString` and its two stream operators, the two `Range` stream operators,
and `Authorizations`' `empty`, `operator==` and `isValidAuthCharacter` all have
exact Go counterparts now, so leaving them `Missing Go` would understate the
surface. Auditing the two types against the pinned
sources proved two further defects, [SB-UNSAFE-034](#sec-21) and
[SB-UNSAFE-035](#sec-21); the first withdraws [SB-KEEP-016](#sec-22), which had
recorded an unreachable throw as a contract. No row became `Covered` and the row
population is unchanged.

Revision 22 changes 31 statuses without changing the inventory. Shoal grew a
public `rfile` package that exports the RFile writer, the random-access and
sequential readers, the merged multi-file read, the client-side iterator
contract, and the relocation types, so 31 of the 32 [§15](#sec-15) rows that
had no Go implementation move from `Missing Go` to `Missing C ABI` under rule 2
of [§4.2](#sec-4). [SB-RFILE-012](#sec-15) stays `Missing Go`:
`addLocalityGroup` has a public Go entry point, but Shoal's writer emits only
the default locality group, so the capability does not exist and the entry
point reports `ErrLocalityGroupUnsupported` rather than pretending otherwise.
Auditing the merge arguments against the pinned sources also proved four
defects that are now recorded in [§21](#sec-21) as shims rather than contracts
([SB-UNSAFE-030](#sec-21)…[SB-UNSAFE-033](#sec-21)): the ignored `versions`
count, the `propogate=True` default that disables delete handling, the
`maxtimestamp` argument that throws for every non-zero value, and the
unchecked append order. No row became `Covered`, the row population is
unchanged, and every other section, including the revision-21 data-descriptor
ABI classifications, is untouched.

Revision 21 changes 13 statuses without changing the inventory: six range
accessors, six iterator-setting accessor spellings, and timestamped mutation
deletion move from `Missing C ABI` to `Behavior mismatch`. Additive ABI 1.3
returns copied range and iterator data through owned results and versioned
views; the linked C lifecycle also executes `shoal_mutation_delete`. Python
aliases, object shapes, defaults, and exception mapping remain shim work.

Revision 20 changes 12 statuses without changing the inventory. Shoal grew the
complete Go surface for Sharkbite's client `Configuration` and for the instance
topology accessors of its `Instance` base class, so the 11 rows in
[§6](#sec-6) that had no Go implementation move from `Missing Go` to
`Missing C ABI` under rule 2 of [§4.2](#sec-4), and the constructor row
[SB-CFG-001](#sec-6) moves from `Behavior mismatch` to the same bucket because
`accumulo.NewConfiguration` now builds the untyped bag Sharkbite builds instead
of mapping the constructor onto Shoal's typed transport options. No row became
`Covered`, the row population is unchanged, and every other section, including
the revision-19 C ABI classifications, is untouched.

Revision 19 changed 8 statuses without changing the inventory: the two
published spellings each for instance name, ZooKeeper instance ID, principal,
and `AuthInfo` instance ID move from `Missing C ABI` to `Behavior mismatch`.
The additive ABI 1.2 capability returns all three immutable connector identity
values through one owned result; alias registration, `retry`, and Python object
and exception shapes remain shim work.

Revision 18 changes 4 statuses without changing the inventory: the merged
connector table-info row and the flat-C `create_table`/`drop_table` helpers move
from `Missing C ABI` to `Behavior mismatch`, and capability discovery becomes
the first surviving `Covered` row. The merged administration and capability
slices now have exact Go/C evidence; the remaining differences are object
handles, Python exception shapes, and return-value semantics.

Revision 17 changes 41 statuses without changing the inventory: 27 merged Go
namespace/security/split and connector rows move from `Missing Go` to
`Behavior mismatch`, and 14 merged administration, capability-discovery, error,
and compatibility-handle rows move from `Missing C ABI` to `Behavior mismatch`.
Exact Go and C evidence now exists, while Python-visible defaults, return
values, and object composition still differ.

Revision 16 changes no count: the fifteenth audit found a truncated registration
contract, not a missing or misclassified obligation. Fourteen of the 35
`SB-EMB` row texts changed (full spans, keyword names, and default tokens); the
population, the statuses, and every other section are unchanged.

Revisions 1–9 (333, 368, 385, 473, 542, 2358, 2938, 3029, and 3175 rows, with
42, 14, 3, 1, 0, 0, 0, 0, and 0 `Covered`) are recorded in the merged text
described in [§3.4](#sec-3).

Revision 16 answers the fifteenth audit, which found one registration contract
recorded incompletely:

- **`SB-EMB-010` was truncated at a line break.** The `Key` constructor
  registration spans `include/jni/PythonBindings.h:65-66`, and its second line
  carries the entire keyword-argument contract: `row`, `columnfamily=""`,
  `columnqualifier=""`, `columnvisibility=""`, and
  `timestamp=9223372036854775807L`. The row cited line 65 only and stopped at
  the comma, which recorded the arity but dropped the defaults — exactly the
  class of loss that [§4.2](#sec-4) forbids for Python-visible rows.
- **The enumerator is now span-based.** Each registration is walked from its
  opening token until its parentheses balance, so a chained call that spans
  lines is one registration with one span. All 35 registrations were re-derived
  this way: 34 are single-line and one, `SB-EMB-010`, spans two lines. Every
  row now cites `file:start-end` where the span is multi-line and repeats the
  registered keyword names and defaults verbatim.
- **Counts and statuses are unchanged** — 35 rows, all `Not required` — because
  the audit found a truncation, not an omission. Fourteen row texts changed;
  the registration population did not.
- **Check 37 is added** and enforces this against the source: for every
  registration it compares the row's cited span with the derived span and
  requires every `"name"_a` keyword and default token to appear in the row.

Revision 15 answered the fourteenth audit, which found a semantic
misclassification rather than an arithmetic one:

- **A whole Python surface was dropped as "declaration-free".**
  `include/jni/PythonBindings.h` registers the `sharkbite_iterator` module: 5
  types and 29 constructor and method registrations. Revision 14 treated the
  header as declaration-free because pybind11 registrations are not C++
  declarations. That is mechanically true and semantically wrong, so all 35
  registrations are now enumerated in [§19.4](#sec-19-4), one row each.
- **They are `Not required`, and the evidence is exhaustive, not asserted.** No
  translation unit includes the header (zero references tree-wide);
  `PYTHON_ITERATOR_SUPPORT` defaults to `OFF` (`CMakeLists.txt:37`);
  `cclient::jni::python::PythonIterator` is never declared
  (`include/jni/PythonIterator.h:28-35`); its implementation is commented out
  (`src/jni/PythonIterator.cpp:26-73`); `include/jni/IteratorExecutor.h:28-33`
  is empty; and `PythonBindings.h:89-95` therefore takes the address of members
  of a type that does not exist, so the module cannot compile. Each row says so
  and says what happens if the module is revived: every row becomes an
  obligation and the rationale is withdrawn.
- **[SB-SCAN-016](#sec-9) no longer claims a working capability.** It was
  `Missing Go` and called "the single largest functional gap", which asserted
  that Sharkbite performs server-side Python iteration. It does not. The row is
  now `Not required` with the same five citations, and it separates what does
  ship — the client-side `PythonIterInfo` descriptor bound at
  `pysharkbite.cpp:96-104`, whose rows keep their obligations — from the dead
  runtime. [SB-DIV-007](#sec-26) is rescoped accordingly: choosing not to build
  server-side execution is a decision about new capability, not a regression.
- **Two verifier checks are added** so the class of error cannot recur: check
  35 requires registration-DSL surfaces to be enumerated and forbids counting
  such a header declaration-free; check 36 forbids any working-capability claim
  for a header no translation unit includes.
- **The header partition changes accordingly**: 205 headers hold rows, 152 are
  vendored, 6 are declaration-free, and 205 + 152 + 6 = 363.

Revision 14 answered the thirteenth audit, which found two mechanical
contradictions left in prose after revision 13 corrected the rows:

- **The header partition did not sum.** [§1](#sec-1) said 204 headers carry
  live public API and 152 are vendored, then called the remainder 6. The
  remainder is **7**, because `include/jni/PythonBindings.h` joined that set in
  revision 12 when its two pybind11 macro invocations stopped counting as
  declarations. The partition is now stated as arithmetic —
  `204 + 152 + 7 = 363` — and all seven declaration-free headers are named:
  `include/data/constructs/tables/tables.h`,
  `include/interconnect/Connector.h`,
  `include/interconnect/transport/TransportIdentifiers.h`,
  `include/jni/IteratorExecutor.h`, `include/jni/PythonBindings.h`,
  `include/jni/PythonIterator.h`, and `include/python/TableOps.h`.
- **A revision-history bullet still claimed seven `static` namespace-scope
  variables.** There are **five** — `ROOT_EXTENT`, `syncBarrier`,
  `ACCUMULO_COORDINATOR`, `MASTER_COORDINATOR`, and `CLUSTER_COORDINATOR` —
  because revision 13 removed the six forward declarations and the
  using-declaration that had been miscounted as variables. Every sentence in
  the document that states a variable or linkage count now agrees with the
  source.
- **The two checks that should have caught this are rewritten.** Checks 24 and
  34 previously compared one canonical sentence; they now scan every non-row
  line, extract numeric and spelled-out claims with their context, and compare
  against values derived twice — once from the inventory and once by an
  independent walk of the headers that tracks brace, namespace, and class
  depth. Row content is unchanged from revision 13; this revision is prose and
  verifier only.

Revision 13 answered the twelfth audit, which rejected two classification
defects that turned type and name declarations into phantom data gaps:

- **Six class forward declarations were counted as external-linkage
  variables.** `class AccumuloInfoBuilder;`, `class TabletServerStatusBuilder;`,
  `class ResultBlock;`, `class ResultIter;`, `class Results;`, and
  `class TraceResolver;` each ended in a semicolon and fell through to the
  data-member branch, which took the trailing identifier as a variable name.
  The pinned tree defines all six — at `AccumuloInfo.h:151`,
  `TabletServerStatus.h:141`, `Results.h:44`, `Results.h:171`, `Results.h:254`,
  and `BackTrace.h:90` — so each declaration is now merged into its definition
  and named in the exclusion ledger. A forward declaration of a type the tree
  never defines would still get a row, described honestly as an opaque type.
- **`using std::void_t;` was counted as a variable.** It is a
  using-declaration: it re-exports a name into `utils` and declares no object.
  The four using forms are now separated — type alias, using-declaration,
  namespace alias, and `using namespace` directive — and only the first two
  carry rows.
- **The namespace-variable population is now derived, not asserted.** The
  document claimed 15 variables with 7 `static`; the real population is **8**
  variables, **5** of them `static` with internal linkage
  (`ROOT_EXTENT`, `syncBarrier`, `ACCUMULO_COORDINATOR`, `MASTER_COORDINATOR`,
  `CLUSTER_COORDINATOR`) and **3** `extern` with external linkage
  (`cachedLocators`, `CLIENT_ERROR_CODES`, `nameSpaceIds`). Check 34
  recomputes both numbers and fails on any prose that disagrees.

Revision 12 answered the eleventh audit, which rejected one omission class and
three misclassifications:

- **Five public bit-fields are enumerated.** The declarator parser took the
  last identifier of a declaration, which for `unsigned deleted : 1;` is the
  width, so `CKey::deleted` (`include/extern/accumulo_data.h:47`) and the four
  `CRange` inclusive/infinite flags (lines 85-88) had no rows. Widths are now
  stripped per declarator, each named bit-field is one row, and the row records
  that the width is part of the ABI layout. These five are flat-C-API fields
  that a caller reads and writes directly.
- **A function-pointer typedef is an alias, not a function.**
  `typedef jint (*registerNatives_t)(JNIEnv *, jclass);`
  (`include/jni/JVMLoader.h:91`) was recorded as a callable free function. It
  now carries an alias row that says it names a callback type and declares no
  callable of its own.
- **pybind11 macro invocations are not declarations.**
  `PYBIND11_DECLARE_HOLDER_TYPE(...)` and `PYBIND11_EMBEDDED_MODULE(...)`
  (`include/jni/PythonBindings.h:49,51`) were recorded as free functions. Macro
  invocations whose macro is defined outside the pinned tree are recorded and
  excluded with a rationale; macro definitions and macro-declared types such as
  `stop_async_iteration` keep their rows. The Python surface that
  `PYBIND11_EMBEDDED_MODULE` registers is the server-side iterator module
  tracked by [SB-SCAN-016](#sec-9), not a C++ declaration surface.
- **`static` namespace-scope variables are internal linkage.** All five of
  them — `ROOT_EXTENT`, `syncBarrier`, `ACCUMULO_COORDINATOR`,
  `MASTER_COORDINATOR`, and `CLUSTER_COORDINATOR` — were described as objects
  consumers link against. They are `static`, so each translation unit gets its
  own copy. Their rows now say so, and [SB-CONN-010](#sec-7) is corrected:
  Sharkbite's pool is shared per translation unit, not process-wide, which
  changes what the divergence actually is. (Revision 12 stated seven here
  because forward declarations and a using-declaration were still miscounted as
  variables; revision 13 removed them and revision 14 corrected this sentence.)

Revision 11 answered the tenth audit, which rejected three scanner defects that
both hid obligations and inflated the required total:

- **Twelve `friend operator<<` functions are enumerated.** `friend` was a skip
  word, so every stream-insertion operator defined inside a class body was
  invisible: `Key` (two), `KeyExtent`, `KeyValue` (two), `Range` (two), `RFile`
  (two), `SequentialRFile` (two), and `RangeDefinition`. Each is a
  namespace-scope obligation reached by argument-dependent lookup, and each row
  names the befriending class. `friend class X;` declarations produce no row,
  so friendship is never mistaken for a type declaration.
- **Sixteen out-of-class definitions are merged, not duplicated.** A qualified
  definition such as `bool Sink<T>::enqueue(...)` was parsed as a new free
  function, which both double-counted the member and lost its access. Each is
  now matched to its in-class declaration by owner and member name; the merge
  and the in-class access are recorded.
- **Protected and private members can no longer be required.** Every
  declaration carries its access label, 700 protected and 346 private members
  are excluded by rule with their access recorded, and three rows revision 10
  required — `TransportPool::createNewTransport`, `Sink::enqueue`, and
  `Sink::exceedQueue` — are gone from the required total because their
  declarations are protected.
- **Net effect: −6 rows** (+12 friend operators, −16 merged definitions, −2
  namespace records that were a `friend class` forward declaration and a
  `using namespace` directive).

Revision 10 answered the ninth audit, which accepted the enumeration but
rejected four release-gating defects:

- **Templated members no longer make a type look memberless.** `typename`-
  introduced declarations were being skipped as statements, so
  `interconnect::CopyKey::operator()`
  (`include/interconnect/scanrequest/ScanIdentifier.h:24`) vanished and its
  owner was recorded as memberless. Memberless types fall from 8 to 7 and every
  remaining one was re-checked against templated members.
- **Conversion operators are parsed.** The operator matcher required the
  operator token to abut its parameter list, so
  `AccumuloInfoBuilder::operator AccumuloInfo &&()`
  (`include/data/constructs/coordinator/AccumuloInfo.h:202`) and
  `TabletServerStatusBuilder::operator TabletServerStatus &&()`
  (`include/data/constructs/coordinator/TabletServerStatus.h:222`) were dropped.
  Member operators rise from 141 to 144, and both conversion operators carry a
  note that implicit convertibility is itself the contract.
- **No unevaluated expression is presented as a numeric ordinal.** The
  anonymous enum in `include/data/constructs/inputvalidation.h:36` initialises
  `value` from `sizeof(test<T>(0)) == sizeof(char)`, which cannot be evaluated
  without instantiation. Its row now states that explicitly and quotes the
  initialiser verbatim instead of implying a computed value. Every enum and
  constant was re-tested: this is the only template-dependent initialiser in
  the live surface, and `true`/`false` and brace aggregates are evaluated
  exactly rather than flagged.
- **Narrative counts are reconciled mechanically.** [§1](#sec-1) claimed 199
  live-API headers while [§19.1](#sec-19-1) totalled 205; both now derive from
  the same extraction, and the verifier fails if any narrative count disagrees
  with the inventory.

Revision 9 answered the eighth audit, which rejected revision 8 for omissions
caused by scanner state and by an unsound vendoring heuristic:

- **+146 rows net.** Whole public sets that revision 8 dropped are enumerated:
  all of `utils::StringUtils`, `writer::Sink`, `cclient::data::KeyAllocatorPool`,
  and the `Hdfs`/`HdfsInputStream`/`HdfsOutputStream` wrappers, the four
  templated `StringUtils::join` overloads that a non-balanced template-prefix
  strip had discarded, plus 108
  namespace-scope functions (was 78), namespace-scope variables (was 0), and
  the `LOG_LEVEL` enum.
- **Vendoring is decided by path and by the file's own license**, never by a
  reference the file makes; the exclusion set is 152 headers, itemised in
  [§19.1](#sec-19-1).
- **Nine include-guard artifacts are gone**, including the `#define NAME 1`
  spelling that revision 8's guard test missed.
- **Five new class-coverage checks** ([§19.3](#sec-19-3)) compare the inventory
  against the headers rather than the document against itself, which is the
  only way this class of omission is detectable.
- **No new `Covered` rows.** Nothing in this revision adds evidence, so the
  count stays at zero, as required.

Counts are mechanically derived from this file: each matrix row is a table line
whose first cell begins with `SB-` and that contains exactly one of the six
status strings in its own cell. Re-run that extraction after any edit and update
this section.

<a id="sec-26"></a>

## 26. Divergences requiring explicit approval

No entry below is approved. Each blocks the gate until a named approver signs
it with a date. Approvals are recorded on Shoal issue
[#81](https://github.com/phrocker/shoal-oss/issues/81) and then mirrored into
this table in the same change; a comment on #81 alone does not lift the gate,
and neither does an edit here without the corresponding #81 decision. Adding a
divergence to this table is not approval.

**87 matrix rows currently carry the `Intentional divergence (approval
required)` status, and every one of them is unapproved and therefore
release-blocking.** They are: 82 cluster-status rows covered by the single
decision [SB-DIV-016](#sec-26) ([§14](#sec-14)), plus SB-PKG-014
(Accumulo 4 only), SB-CFG-014 and SB-CFG-022 (no password read-back, both
spellings), SB-CONN-010 (per-connector pools), and SB-ERR-003 (no Thrift
types). The table below lists 16 decisions because one decision can cover many
rows; the row count and the decision count are different quantities and must
not be conflated.

The remaining entries are **proposed** divergences attached to rows that
currently carry another status; approving one changes that row's status to
`Intentional divergence`, and rejecting it leaves the row as a gap that must be
implemented.

| ID | Divergence | Rows | Impact on existing Sharkbite programs | Approver | Date |
| --- | --- | --- | --- | --- | --- |
| SB-DIV-001 | Accumulo 4 only; 1.6.x–2.x unsupported | SB-PKG-014 | Every user on Accumulo 1.x/2.x cannot upgrade. Largest divergence in this document. | _unapproved_ | — |
| SB-DIV-002 | No password read-back (`AuthInfo.getPassword`/`password`) | SB-CFG-014, SB-CFG-022 | Code that re-reads its own password breaks; credential leaks stop. Covers **both** published spellings — the getter and its alias — which are two matrix rows carrying one decision. | _unapproved_ | — |
| SB-DIV-003 | Per-connector transport and table-name caches instead of process-wide singletons | SB-CONN-010, SB-CONN-011 | Connection counts and cache-invalidation blast radius change. | _unapproved_ | — |
| SB-DIV-004 | Generated Thrift types never leak; `TApplicationException` is not raised | SB-ERR-003 | `except TApplicationException` blocks stop matching. | _unapproved_ | — |
| SB-DIV-005 | Server-side update errors are raised instead of silently discarded | SB-WRITE-010, SB-UNSAFE-018 | Programs that ignored rejected writes will now see exceptions. This is the correct behavior and should be approved, but it is a behavior change. | _unapproved_ | — |
| SB-DIV-006 | `AccumuloIterator` chunk size no longer truncates iteration or drops the boundary entry | SB-SCAN-024, SB-BASE-016, SB-BASE-019, SB-PANDA-009, SB-PANDA-012, SB-UNSAFE-008 | Programs that relied on the truncation to bound work will now read the full range, and every chunk boundary yields one more entry than it does today. Applies to both copies of the iterator — `sharkbite.AccumuloIterator` and `pandashark.AccumuloIterator` — so pandas frames change size as well. | _unapproved_ | — |
| SB-DIV-007 | Server-side Python iterator **execution** is not implemented | SB-SCAN-016 and [§19.4](#sec-19-4) | Rescoped in revision 15: the pinned Sharkbite tree does not execute Python iterators either — the embedded module is unreferenced, the iterator type is undeclared, and the implementation is commented out — so this is not a regression against a working capability but a decision about whether to build something new. Impact if the decision is "no": programs written against the documented-but-non-functional feature stay non-functional, exactly as today. Alternative: transpile to Go iterators (`docs/iterator-forge-design.md`). | _unapproved_ | — |
| SB-DIV-008 | Hedged reads and RFile-only scanning are not reimplemented (if this route is chosen) | SB-SCAN-014, SB-SCAN-015 | Documented beta options disappear. | _unapproved_ | — |
| SB-DIV-009 | Python floor raised above 3.6 | SB-PKG-007 | Users on Python 3.6–3.8 cannot upgrade. | _unapproved_ | — |
| SB-DIV-010 | Reversed ranges raise at construction instead of returning no rows | SB-DATA-034 | Programs that built reversed ranges and expected empty results now fail loudly. | _unapproved_ | — |
| SB-DIV-011 | `AccumuloDataset.coerce` propagates errors instead of printing them and returning `0` | SB-TORCH-004, SB-UNSAFE-021 | Training loops that silently consumed zeros will now raise. Correct, but it changes the data a PyTorch job sees. | _unapproved_ | — |
| SB-DIV-012 | `pandashark.read_accumulo` requires an explicit range and honours `chunksize` | SB-PANDA-001, SB-UNSAFE-024 | The published call form in `examples/dataframe.py` changes meaning; today it cannot work at all. | _unapproved_ | — |
| SB-DIV-013 | `TableCompactions.majors` cannot report major-compaction counts | SB-STAT-030, SB-GAP-GO-003 | Accumulo 4 dropped major-compaction counts specifically, on top of the whole-section removal in [SB-DIV-016](#sec-26). Sharkbite programs read `.majors.running`/`.majors.queued`. The decision to record is *how* the shim surfaces the absence: raise, return `None`, or return a zeroed `Compacting`. Must not be resolved by marking SB-STAT-030 `Covered`. | _unapproved_ | — |
| SB-DIV-014 | `DataFrameIterator` terminates instead of returning empty frames forever | SB-PANDA-013, SB-PANDA-003, SB-UNSAFE-026 | Loops that today never end (and callers that treat an empty frame as data) will now see a clean `StopIteration`. Any code that counted on an infinite supply of empty frames, or that distinguished end-of-data by frame emptiness, changes behavior. | _unapproved_ | — |
| SB-DIV-015 | `set_table` and `set_authorizations` reconfigure existing scanners and writers | SB-BASE-004, SB-BASE-005, SB-BASE-009, SB-UNSAFE-027 | Code that called a setter after constructing a scanner currently keeps reading the old table with the old authorizations; after the fix it reads the new one. That is the intended contract, but it changes which rows a running program returns. | _unapproved_ | — |
| SB-DIV-016 | Cluster status and monitoring cannot be provided at all | All 82 capability rows in [§14](#sec-14) — every `SB-STAT-*` row except SB-STAT-028 (a binding defect) and SB-STAT-038 (a Python attribute property) — plus SB-CONN-007 and SB-GAP-GO-003 | Accumulo commit `f0841e4` removed `getManagerStats`, `ManagerMonitorInfo`, `DeadServer`, and the legacy monitor resources 113 commits before the pinned target `317c288`, and the surviving REST-v2/metrics/server APIs cannot reconstruct the object graph. Shoal issue [#96](https://github.com/phrocker/shoal-oss/issues/96) attempted it; PR [#98](https://github.com/phrocker/shoal-oss/pull/98) was withdrawn. Every `getStatistics()` consumer — `examples/pythonstats.py` is the published one — stops working, and there is no implementation that can change that. The decision to record is what `connector.getStatistics()` does: raise `ClientException`, raise `NotImplementedError`, or return a degraded object populated from metrics endpoints with most fields absent. This is the largest capability loss in the document after [SB-DIV-001](#sec-26). | _unapproved_ | — |

## Related documents

- `capi/README.md` — C ABI ownership, lifetime, and bootstrap contract.
- `accumulo/doc.go` — public Go client scope statement.
- `ARCHITECTURE.md` — component map.
- `docs/iterator-forge-design.md` — candidate route for SB-SCAN-016.
