# Explorer Release Checklist

Before sharing a demo, confirm the CLI workflow, graph relationship, and web
workspace start from the same corpus.

## Web workspace

Start the optional workspace on `127.0.0.1:8080` and inspect `/api/v1/meta`
before using the browser.
