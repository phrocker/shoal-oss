apiVersion: v1
kind: Service
metadata:
  name: shoal-read
  labels:
    app.kubernetes.io/name: shoal
    app.kubernetes.io/component: read-fleet
    app.kubernetes.io/part-of: shoal-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shoal
    app.kubernetes.io/component: read-fleet
  ports:
    - name: thrift
      port: 9800
      targetPort: thrift
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shoal-read
  labels:
    app.kubernetes.io/name: shoal
    app.kubernetes.io/component: read-fleet
    app.kubernetes.io/part-of: shoal-platform
spec:
  replicas: 3
  # Explicit rollout contract instead of relying on the apps/v1 default
  # (which happens to already be these exact values today). Keeps at
  # least 2 of 3 replicas serving during a rollout (maxUnavailable: 1)
  # while allowing one extra pod briefly during the transition
  # (maxSurge: 1) so the new revision's readiness is proven before an old
  # replica is removed.
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: shoal
      app.kubernetes.io/component: read-fleet
  template:
    metadata:
      labels:
        app.kubernetes.io/name: shoal
        app.kubernetes.io/component: read-fleet
        app.kubernetes.io/part-of: shoal-platform
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: shoal
                    app.kubernetes.io/component: read-fleet
                topologyKey: kubernetes.io/hostname
      containers:
        - name: shoal
          image: ghcr.io/example/shoal:dev
          imagePullPolicy: IfNotPresent
          command: ["/shoal"]
          args:
            - -listen=$(SHOAL_THRIFT_LISTEN)
            - -zk=$(SHOAL_ZK)
            - -instance=$(SHOAL_INSTANCE)
            - -accumulo-version=$(SHOAL_ACCUMULO_VERSION)
            - -user=$(SHOAL_METADATA_USER)
            - -storage=$(SHOAL_STORAGE_SCHEME)
            - -cache-bytes=$(SHOAL_CACHE_BYTES)
            - -log-level=$(SHOAL_LOG_LEVEL)
            - -prewarm-tables=$(SHOAL_PREWARM_TABLES)
            - -prewarm-parallelism=$(SHOAL_PREWARM_PARALLELISM)
          envFrom:
            - configMapRef:
                name: shoal-platform-config
          env:
            - name: SHOAL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shoal-object-storage-credentials
                  key: accumulo-password
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /var/run/secrets/shoal/gcs/key.json
          ports:
            - name: thrift
              containerPort: 9800
          volumeMounts:
            - name: object-storage-credentials
              mountPath: /var/run/secrets/shoal/gcs
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: thrift
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: thrift
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: object-storage-credentials
          secret:
            secretName: shoal-object-storage-credentials
            optional: true
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: shoal-read
  labels:
    app.kubernetes.io/name: shoal
    app.kubernetes.io/component: read-fleet
    app.kubernetes.io/part-of: shoal-platform
spec:
  # Stateless read replicas spread across nodes (see the pod anti-affinity
  # above): allow one voluntary disruption at a time so at least 2 of 3
  # stay available to serve reads.
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: shoal
      app.kubernetes.io/component: read-fleet
